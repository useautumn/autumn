/**
 * atmn scenarios/motion — fixture exported by name and listed by reference (`plans: [pro, free]`) → remote delete removes the literal, its export, its import, and the reference; remote update rewrites the literal where it is
 *
 * code in motion: the config's shape is the user's; pull edits the AST, never rewrites a file
 *
 * One line of plans/atmn-v3/07_tests.md. [a, b] is a matrix looped INSIDE this file.
 */

import { expect, test } from "bun:test";
import {
	configBody,
	enterpriseWithSeats,
	everyFeatureType,
	freePlan,
	paidMonthly,
	seatPlan,
	versionedPro,
} from "@tests/utils/atmnUtils/baseConfigs.js";
import { expectPreviewNone, expectRoundTrip } from "@tests/utils/atmnUtils/expectRoundTrip.js";
import { atmnImports, initAtmnScenario } from "@tests/utils/atmnUtils/initAtmnScenario.js";
import { s } from "@tests/utils/testInitUtils/initScenario.js";

test.todo("fixture exported by name and listed by reference (`plans: [pro, free]`) \u2192 remote delete removes the literal, its export, its import, and the reference; remote update rewrites the literal where it is", () => {});
