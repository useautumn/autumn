# Units of work

Order: predicate → simple reset → flush → tick → rollovers → anchor → pools → cron → server.
Each unit ends with a passing test and stops for review.

| # | unit | ends with this passing | status |
|---|---|---|---|
| 1 | **The engine knows what is due.** Pure `fullSubjectToDueRows({ fullSubject, asOf })` in `balance-engine`, ported from `getResettableCustomerEntitlements` + `classifyNoAction`: overdue `next_reset_at`, not price-backed (unless prepaid with separate reset interval), past-due only with `ignore_past_due`. Pool sources never load, and the lifetime-pool rule lands with unit 7 (it needs `reset_mode` on the pool row). No mutation yet. | engine unit `fullSubjectToDueRows.test.ts`, one case per rule | done 2026-09-22 |
| 2 | **The `reset` command, engine only.** `computeReset({ fullSubject, command })` → one mutation (or null when nothing is due): per due row an `update` change guarded on the old `next_reset_at` (the existing change kind; no new one needed), the row math is the server's own, moved to `shared/utils/cusEntUtils/resetUtils/` (`getNextResetAt` with a `now` param, `clampNextResetAtToPendingBillingCycleAnchor`, `getResetBalancesUpdate`, and the new `customerEntitlementToNextResetAt` + `resetNeedsBillingCycleAnchor` that hold today's `getResetAtUpdate` decision with the Stripe anchor passed in); `getResetAtUpdate` and the engine both call them; `persist_free_overage` rides on `CommandOrg`; `billingCycleAnchors` on the reset command, filled by unit 6. Result `{ type: "reset", rows }` on the log. Registered in the mutation schemas and the worker fingerprint. | shared `customerEntitlementToNextResetAt.test.ts` 5/5 + moved `getResetBalancesUpdate.test.ts`; engine `computeReset.test.ts` 8/8; server `reset-anchor-date-check` 8/8; worker unit 546/546 | done 2026-09-22 |
| 3 | **Every command brings its subject current first.** `processor/actions/ensureSubjectCurrent/`: `ensure()` (rows resident) → `readResetAnchors()` (unit 6) → `advanceResets()` decides a synthetic `reset` (id `<commandId>:reset`, the trigger's `occurredAt`) before track / check / finalize; `kind: "reply"` when nothing is due. The existing `update` flush lands it, guarded on the old `next_reset_at`. | `track-commits.test.ts` reset case 1/1 (track refills then draws; retry is a duplicate; check refills and allows); worker unit 546/546 | done 2026-09-22 |
| 4 | **The explicit `reset`.** No separate tick: the `reset` command is exposed as is — client `reset` / `queue.reset`, `/v1/reset`, command-topic type, processor handler sharing `decideReset` with the implicit path. | `track-commits.test.ts` explicit-reset case 1/1: due → refilled and landed; nothing due → `result: null`, bookmark unchanged; queued → consumed the same way | done 2026-09-22 |
| 5 | **Rollovers.** `getRolloverUpdates`, `cusEntToEffectiveRolloverMax`, `performMaximumClearing` moved verbatim to `shared/utils/cusEntUtils/rolloverUtils/` (ids via `@autumn/ksuid`, `roll_` prefix as before); the batch path's insert/update/delete merge hoisted as `clearRolloversOverMax`, called by `computeResetMutation` and the engine. The engine emits rollover insert / update (guarded on the old balance) / delete changes on the reset record. | engine `computeReset.test.ts` 2 rollover cases; server `perform-maximum-clearing-entity-mode` 4/4; `track-commits.test.ts` rollover case 1/1 | done 2026-09-22 |
| 6 | **Anchor-aligned `next_reset_at`.** `packages/postgres` `getBillingCycleAnchors` (plan id → `subscriptions.billing_cycle_anchor_seconds` via `subscription_ids[1]`, tenant-scoped); worker `readResetAnchors` runs it only for due rows `fullSubjectToPlansNeedingBillingCycleAnchor` names, before the critical section; anchors ride on the reset command. The worker never calls Stripe. | postgres `billingCycleAnchorsSql.test.ts`; `track-commits.test.ts` edge-date case 1/1 (30 Apr cycle end, 31 Jan anchor → 31 May, not 30 May) | done 2026-09-22 |
| 7 | **Pooled promotion.** Contributions never load (up to 1M per pool): `readResetInputs` runs one idempotent statement per due limited pool, `promoteDuePooledContributions` (promote due rows, return Σ at promoted values from the pre-update snapshot), and the grant rides on the reset command as `pooledGranted`. The engine moves the resident pool row (`pooledBalances` update, guarded on the old `granted`; new change kind in `rowChangeSchema`, `applyChanges`, `revertChanges`, the flush) and refills and caps from the promoted grant. `reset_mode` on the pool row; lifetime pools never due; unlimited pools skip promotion. | engine `computeReset.test.ts` 4 pooled cases; postgres `promoteDuePooledContributionsSql.test.ts`; `track-commits.test.ts` pooled case 1/1 (100/40/30 → granted 170, contributions promoted, pool cusEnt 165 after a track of 5) | done 2026-09-22 |
| 8 | **The sweep has a worker lane.** `runResetLoopV2` picks a lane once per sweep from `isBalanceWorkerRolloutEnabled`: worker → `enqueueWorkerResets` (scan row carries `internal_customer_id`, `internal_entity_id`, `customer_product_id`; `getResetSubjects` resolves one subject per (customer, entity), the entity coming off the plan for an entity plan's rows; org settings via `getOrgWithFeaturesCached`; one `reset` per subject with id `reset_<customer>_<entity|customer>_<dueBefore>` so a re-enqueued page is a duplicate) on `queue.reset`; sql → the batch lane as before. | `batch-reset-v2-worker-lane.test.ts` 3/3: due row refilled by the worker once per sweep; a track racing the cron's reset refills exactly once; two entities' own rows reset under each entity's subject. Found and fixed on the way: a queued `reset` whose id was already applied threw out of the command consumer and crash-looped the worker on that offset; queued commands now settle at the stream's boundary, `settleQueuedFailure` in the catch of `createCommandRecordHandler` (the HTTP error handler's counterpart); `consumeTrack` and `consumeReset` just run and describe their reply (`command-record-handler.test.ts` redelivered-reset case) | done 2026-09-22 |
| 9 | **`CusService.getFull` asks the worker.** On the rollout flag, `resetCustomerEntitlementsViaWorker` groups `getCusEntsNeedingReset` by subject (the customer, or the entity a due row belongs to via the plan) and sends one `reset` per subject with `durability: "store"`, all awaited; when something refilled, getFull re-reads with `skipReset`. Durability rides as the reset request's `payload`; the worker's `reset` honors it and, for a store-durable idle reply, waits for earlier records to land. The legacy lazy write stays for the flag-off path. `customers.get` / `entities.get` are out of scope: they move to the worker. | `get-full-worker-reset.test.ts` 2/2: a due row is refilled by the worker, landed, read back fresh, a second read changes nothing; two entity plans' due rows refilled under each entity. Worker-level: `track-commits.test.ts` two tracks and a check arriving together at the cycle end refill once | done 2026-09-22 |
