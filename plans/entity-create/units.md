# Units of work

Each unit is end to end: the route runs the new action for the cases it covers, the legacy path keeps the
rest, and the unit ends on green integration tests. Stop for review after each.

| # | unit | covers | ends with this passing |
|---|---|---|---|
| 1 | **Done 2026-09-23.** **The action, free seats.** `createEntities` setup → errors → compute → execute; `insertEntities` + seat `balanceChange −N` + per-entity `ce.entities` seeds in one plan. `entities.create` takes it when no seat is paid, nothing is claimed, and no entity defaults apply; else legacy. | multiple entities, usage_limit, consumable, duplicates, billing_controls | `create-entity-race`, `create-entity-allocated-v2`, `new-per-entity` 3, `concurrent-track5`, `track-race-condition4`, `update-entity-billing-controls`, `attach-addon-entity-usage` |
| 2 | **Done.** **Entity defaults in the plan.** `computeEntityDefaults`: one plan for all entities' free defaults + in-plan pooled transition. Delete `attachDefaultProductsToEntities`. | defaults, pools | `default-applies-to-entity`, `default-version-attach`, a new pooled-default case |
| 3 | **Done** (a lost claim throws `EntityAlreadyExists`; the worker resolves it in unit 6). **Id-less and claim.** `updateEntities` facet (shared schema + Postgres step). Claim = update + per-entity key seed, no seat change (decision 1). | id-less, claim | `attach-edge-cases` 3; new: create id-less → claim → seat used once, `ce.entities[newId]` seeded |
| 4 | **Done** (one paid feature per request; `entity2` post-delete invoice read left as is; `entity5` step 4 is the worker track path). **Paid seats.** `computeSeatChangePlan` calls the allocatedInvoice compute; reused replaceables move their per-entity keys; Stripe first, unpaid → nothing written. The route drops its legacy branch; delete the create half of `createEntityForCusProduct` / `adjustAllowance`. | allocated v1 invoices | `create-entity-paid` entity1–5, `legacy-upgrade-usage` 3 |
| 5 | **Done** (`check-misc` 5/5, `track-misc` 1 & 6, new `check-entity-data` 2/2; the other `track-misc` cases fail with `feature_not_found` on the worker path independently). **`entity_data` on the worker path only.** `withCreateIfMissing` creates the missing entity through `createEntitiesV2({ allowPaidFeatures: false })` (a lost race to `EntityAlreadyExists` is fine: the re-run reads it); `validateBalanceWorkerRequest` deleted. Legacy `autoCreateEntity` stays for the Redis paths until they retire. | auto-create | `check-misc` 1/2/4/5, `track-misc` 1/6, `attach-misc`, new: paid feature → 400 |
| 6 | **Done** (engine 237/237, worker unit 548/548, worker `track-commits` entity cases 8/8, server billing-plan units; `entity1`/`entity4`/race/claim/entity_data green on the worker lane). **The worker lane.** `entity_exists` reply: created entities are hydrated before the decision, so one already held (in memory or Postgres) replies instead of colliding; the server throws `EntityAlreadyExists` unless it is its own resend. `moveEntries` op: re-keys per-entity entries against the live map in `decide`, so paid creates that reuse replaceables route to the worker. **Claims stay on the Postgres lane** (no subject key before the claim, zero prod usage). Found: an evict landing between `ensure` and `read` in `readCurrentSubject` answers a check with `NOT_INITIALIZED` (409); pre-existing, seen once on `entity4`. | atomic on the worker | done as listed |
| 7 | **Parked 2026-09-23**, with the other structural writers in `plans/balance-worker-apply-plan/`. **Delete on the same unit.** `removed` on the entity balances plan (+1 or a replaceable; per-entity key moved to the replaceable id via `moveEntries`), entity products cancelled in the plan, soft delete so free entity plans stop cascading to customer level, drop the `deductionPersisted` flag. Until then delete is a legacy writer covered by the route's evict, and `adjustAllowance` / `handleProratedDowngrade` / `createEntityForCusProduct` stay for it. | delete | `create-entity-paid` delete cases, `delete-entity` |
