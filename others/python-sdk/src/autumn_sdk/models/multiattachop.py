"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .customerdata import CustomerData, CustomerDataTypedDict
from autumn_sdk.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
    UnrecognizedStr,
)
from autumn_sdk.utils import FieldMetadata, HeaderMetadata, validate_const
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Any, Dict, List, Literal, Optional, Union
from typing_extensions import Annotated, NotRequired, TypeAliasType, TypedDict


class MultiAttachGlobalsTypedDict(TypedDict):
    x_api_version: NotRequired[str]


class MultiAttachGlobals(BaseModel):
    x_api_version: Annotated[
        Optional[str],
        pydantic.Field(alias="x-api-version"),
        FieldMetadata(header=HeaderMetadata(style="simple", explode=False)),
    ] = "2.4.0"

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["x-api-version"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


MultiAttachPriceInterval = Literal[
    "one_off",
    "week",
    "month",
    "quarter",
    "semi_annual",
    "year",
]
r"""Billing interval (e.g. 'month', 'year')."""


class MultiAttachAdditionalCurrencyTypedDict(TypedDict):
    currency: str
    r"""Three-letter Stripe-supported currency code (e.g. 'eur', 'gbp')."""
    amount: float
    r"""Price amount in this currency. Set explicitly per currency, not converted from the base amount."""


class MultiAttachAdditionalCurrency(BaseModel):
    currency: str
    r"""Three-letter Stripe-supported currency code (e.g. 'eur', 'gbp')."""

    amount: float
    r"""Price amount in this currency. Set explicitly per currency, not converted from the base amount."""


class MultiAttachBasePriceTypedDict(TypedDict):
    r"""Base price configuration for a plan."""

    amount: float
    r"""Base price amount for the plan, in major currency units (e.g. dollars)."""
    interval: MultiAttachPriceInterval
    r"""Billing interval (e.g. 'month', 'year')."""
    interval_count: NotRequired[float]
    r"""Number of intervals per billing cycle. Defaults to 1."""
    additional_currencies: NotRequired[List[MultiAttachAdditionalCurrencyTypedDict]]
    r"""Base price amounts in additional currencies. The base 'amount' is in the org's default currency."""


class MultiAttachBasePrice(BaseModel):
    r"""Base price configuration for a plan."""

    amount: float
    r"""Base price amount for the plan, in major currency units (e.g. dollars)."""

    interval: MultiAttachPriceInterval
    r"""Billing interval (e.g. 'month', 'year')."""

    interval_count: Optional[float] = None
    r"""Number of intervals per billing cycle. Defaults to 1."""

    additional_currencies: Optional[List[MultiAttachAdditionalCurrency]] = None
    r"""Base price amounts in additional currencies. The base 'amount' is in the org's default currency."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["interval_count", "additional_currencies"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


MultiAttachResetInterval = Literal[
    "one_off",
    "minute",
    "hour",
    "day",
    "week",
    "month",
    "quarter",
    "semi_annual",
    "year",
]
r"""Interval at which balance resets (e.g. 'month', 'year'). For consumable features only."""


class MultiAttachResetTypedDict(TypedDict):
    r"""Reset configuration for consumable features. Omit for non-consumable features like seats."""

    interval: MultiAttachResetInterval
    r"""Interval at which balance resets (e.g. 'month', 'year'). For consumable features only."""
    interval_count: NotRequired[float]
    r"""Number of intervals between resets. Defaults to 1."""


class MultiAttachReset(BaseModel):
    r"""Reset configuration for consumable features. Omit for non-consumable features like seats."""

    interval: MultiAttachResetInterval
    r"""Interval at which balance resets (e.g. 'month', 'year'). For consumable features only."""

    interval_count: Optional[float] = None
    r"""Number of intervals between resets. Defaults to 1."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["interval_count"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class MultiAttachItemAdditionalCurrencyTypedDict(TypedDict):
    currency: str
    r"""Three-letter Stripe-supported currency code (e.g. 'eur', 'gbp')."""
    amount: float
    r"""Price amount in this currency. Set explicitly per currency, not converted from the base amount."""


class MultiAttachItemAdditionalCurrency(BaseModel):
    currency: str
    r"""Three-letter Stripe-supported currency code (e.g. 'eur', 'gbp')."""

    amount: float
    r"""Price amount in this currency. Set explicitly per currency, not converted from the base amount."""


MultiAttachPriceToTypedDict = TypeAliasType(
    "MultiAttachPriceToTypedDict", Union[float, str]
)


MultiAttachPriceTo = TypeAliasType("MultiAttachPriceTo", Union[float, str])


class MultiAttachTierAdditionalCurrencyTypedDict(TypedDict):
    currency: str
    r"""Three-letter Stripe-supported currency code (e.g. 'eur', 'gbp')."""
    amount: NotRequired[float]
    r"""Per-unit amount for this tier in this currency."""
    flat_amount: NotRequired[float]
    r"""Flat amount for this tier in this currency, if the tier uses one."""


class MultiAttachTierAdditionalCurrency(BaseModel):
    currency: str
    r"""Three-letter Stripe-supported currency code (e.g. 'eur', 'gbp')."""

    amount: Optional[float] = None
    r"""Per-unit amount for this tier in this currency."""

    flat_amount: Optional[float] = None
    r"""Flat amount for this tier in this currency, if the tier uses one."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["amount", "flat_amount"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class MultiAttachPriceTierTypedDict(TypedDict):
    to: MultiAttachPriceToTypedDict
    amount: NotRequired[float]
    flat_amount: NotRequired[float]
    additional_currencies: NotRequired[List[MultiAttachTierAdditionalCurrencyTypedDict]]
    r"""Per-currency amounts for this tier. Tier boundaries ('to') are shared across all currencies."""


class MultiAttachPriceTier(BaseModel):
    to: MultiAttachPriceTo

    amount: Optional[float] = None

    flat_amount: Optional[float] = None

    additional_currencies: Optional[List[MultiAttachTierAdditionalCurrency]] = None
    r"""Per-currency amounts for this tier. Tier boundaries ('to') are shared across all currencies."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["amount", "flat_amount", "additional_currencies"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


MultiAttachTierBehavior = Literal[
    "graduated",
    "volume",
]


MultiAttachItemPriceInterval = Literal[
    "one_off",
    "week",
    "month",
    "quarter",
    "semi_annual",
    "year",
]
r"""Billing interval. For consumable features, should match reset.interval."""


MultiAttachBillingMethod = Literal[
    "prepaid",
    "usage_based",
]
r"""'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go."""


class MultiAttachPriceTypedDict(TypedDict):
    r"""Pricing for usage beyond included units. Omit for free features."""

    interval: MultiAttachItemPriceInterval
    r"""Billing interval. For consumable features, should match reset.interval."""
    billing_method: MultiAttachBillingMethod
    r"""'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go."""
    amount: NotRequired[float]
    r"""Price per billing_units after included usage. Either 'amount' or 'tiers' is required."""
    additional_currencies: NotRequired[List[MultiAttachItemAdditionalCurrencyTypedDict]]
    r"""Amounts in additional currencies for this flat price. The base 'amount' is in the org's default currency. Only valid with 'amount', not 'tiers'."""
    tiers: NotRequired[List[MultiAttachPriceTierTypedDict]]
    r"""Tiered pricing.  Either 'amount' or 'tiers' is required."""
    tier_behavior: NotRequired[MultiAttachTierBehavior]
    interval_count: NotRequired[float]
    r"""Number of intervals per billing cycle. Defaults to 1."""
    billing_units: NotRequired[float]
    r"""Units per price increment. Usage is rounded UP when billed (e.g. billing_units=100 means 101 rounds to 200)."""
    max_purchase: NotRequired[Nullable[float]]
    r"""Max units purchasable beyond included. E.g. included=100, max_purchase=300 allows 400 total. Null for no limit."""


class MultiAttachPrice(BaseModel):
    r"""Pricing for usage beyond included units. Omit for free features."""

    interval: MultiAttachItemPriceInterval
    r"""Billing interval. For consumable features, should match reset.interval."""

    billing_method: MultiAttachBillingMethod
    r"""'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go."""

    amount: Optional[float] = None
    r"""Price per billing_units after included usage. Either 'amount' or 'tiers' is required."""

    additional_currencies: Optional[List[MultiAttachItemAdditionalCurrency]] = None
    r"""Amounts in additional currencies for this flat price. The base 'amount' is in the org's default currency. Only valid with 'amount', not 'tiers'."""

    tiers: Optional[List[MultiAttachPriceTier]] = None
    r"""Tiered pricing.  Either 'amount' or 'tiers' is required."""

    tier_behavior: Optional[MultiAttachTierBehavior] = None

    interval_count: Optional[float] = 1
    r"""Number of intervals per billing cycle. Defaults to 1."""

    billing_units: Optional[float] = 1
    r"""Units per price increment. Usage is rounded UP when billed (e.g. billing_units=100 means 101 rounds to 200)."""

    max_purchase: OptionalNullable[float] = UNSET
    r"""Max units purchasable beyond included. E.g. included=100, max_purchase=300 allows 400 total. Null for no limit."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "amount",
                "additional_currencies",
                "tiers",
                "tier_behavior",
                "interval_count",
                "billing_units",
                "max_purchase",
            ]
        )
        nullable_fields = set(["max_purchase"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


MultiAttachOnIncrease = Literal[
    "bill_immediately",
    "prorate_immediately",
    "prorate_next_cycle",
    "bill_next_cycle",
]
r"""Billing behavior when quantity increases mid-cycle."""


MultiAttachOnDecrease = Literal[
    "prorate",
    "prorate_immediately",
    "prorate_next_cycle",
    "none",
    "no_prorations",
]
r"""Credit behavior when quantity decreases mid-cycle."""


class MultiAttachProrationTypedDict(TypedDict):
    r"""Proration settings for prepaid features. Controls mid-cycle quantity change billing."""

    on_increase: MultiAttachOnIncrease
    r"""Billing behavior when quantity increases mid-cycle."""
    on_decrease: MultiAttachOnDecrease
    r"""Credit behavior when quantity decreases mid-cycle."""


class MultiAttachProration(BaseModel):
    r"""Proration settings for prepaid features. Controls mid-cycle quantity change billing."""

    on_increase: MultiAttachOnIncrease
    r"""Billing behavior when quantity increases mid-cycle."""

    on_decrease: MultiAttachOnDecrease
    r"""Credit behavior when quantity decreases mid-cycle."""


MultiAttachExpiryDurationType = Literal[
    "month",
    "forever",
]
r"""When rolled over units expire."""


class MultiAttachRolloverTypedDict(TypedDict):
    r"""Rollover config for unused units. If set, unused included units carry over."""

    expiry_duration_type: MultiAttachExpiryDurationType
    r"""When rolled over units expire."""
    max: NotRequired[float]
    r"""Max rollover units. Omit for unlimited rollover."""
    max_percentage: NotRequired[float]
    r"""Maximum rollover as a percentage (0-100) of included + prepaid grant. Mutually exclusive with max."""
    expiry_duration_length: NotRequired[float]
    r"""Number of periods before expiry."""


class MultiAttachRollover(BaseModel):
    r"""Rollover config for unused units. If set, unused included units carry over."""

    expiry_duration_type: MultiAttachExpiryDurationType
    r"""When rolled over units expire."""

    max: Optional[float] = None
    r"""Max rollover units. Omit for unlimited rollover."""

    max_percentage: Optional[float] = None
    r"""Maximum rollover as a percentage (0-100) of included + prepaid grant. Mutually exclusive with max."""

    expiry_duration_length: Optional[float] = None
    r"""Number of periods before expiry."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["max", "max_percentage", "expiry_duration_length"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


MultiAttachDimensionsMatch4TypedDict = TypeAliasType(
    "MultiAttachDimensionsMatch4TypedDict", Union[str, float, bool]
)


MultiAttachDimensionsMatch4 = TypeAliasType(
    "MultiAttachDimensionsMatch4", Union[str, float, bool]
)


class MultiAttachDimensions4TypedDict(TypedDict):
    match: Dict[str, MultiAttachDimensionsMatch4TypedDict]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""
    credit_cost: float
    r"""Credits consumed per billing-unit group when this dimension matches."""
    priority: NotRequired[int]
    r"""Breaks ties between dimensions that match the same number of keys. Higher wins."""


class MultiAttachDimensions4(BaseModel):
    match: Dict[str, MultiAttachDimensionsMatch4]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""

    credit_cost: float
    r"""Credits consumed per billing-unit group when this dimension matches."""

    priority: Optional[int] = None
    r"""Breaks ties between dimensions that match the same number of keys. Higher wins."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["priority"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


MultiAttachDimensionsMatch3TypedDict = TypeAliasType(
    "MultiAttachDimensionsMatch3TypedDict", Union[str, float, bool]
)


MultiAttachDimensionsMatch3 = TypeAliasType(
    "MultiAttachDimensionsMatch3", Union[str, float, bool]
)


MultiAttachDimensionsToEnum2 = Literal["inf",]


MultiAttachDimensionsToUnion2TypedDict = TypeAliasType(
    "MultiAttachDimensionsToUnion2TypedDict", Union[float, MultiAttachDimensionsToEnum2]
)
r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""


MultiAttachDimensionsToUnion2 = TypeAliasType(
    "MultiAttachDimensionsToUnion2", Union[float, MultiAttachDimensionsToEnum2]
)
r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""


class MultiAttachDimensionsTier2TypedDict(TypedDict):
    to: MultiAttachDimensionsToUnion2TypedDict
    r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""
    credit_cost: float
    r"""Credits consumed per billing-unit group within this tier."""


class MultiAttachDimensionsTier2(BaseModel):
    to: MultiAttachDimensionsToUnion2
    r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""

    credit_cost: float
    r"""Credits consumed per billing-unit group within this tier."""


class MultiAttachDimensions3TypedDict(TypedDict):
    match: Dict[str, MultiAttachDimensionsMatch3TypedDict]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""
    tiers: List[MultiAttachDimensionsTier2TypedDict]
    priority: NotRequired[int]
    r"""Breaks ties between dimensions that match the same number of keys. Higher wins."""
    tier_behavior: Literal["graduated"]


class MultiAttachDimensions3(BaseModel):
    match: Dict[str, MultiAttachDimensionsMatch3]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""

    tiers: List[MultiAttachDimensionsTier2]

    priority: Optional[int] = None
    r"""Breaks ties between dimensions that match the same number of keys. Higher wins."""

    tier_behavior: Annotated[
        Annotated[Literal["graduated"], AfterValidator(validate_const("graduated"))],
        pydantic.Field(alias="tier_behavior"),
    ] = "graduated"

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["priority"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


MultiAttachDimensionsUnion2TypedDict = TypeAliasType(
    "MultiAttachDimensionsUnion2TypedDict",
    Union[MultiAttachDimensions4TypedDict, MultiAttachDimensions3TypedDict],
)


MultiAttachDimensionsUnion2 = TypeAliasType(
    "MultiAttachDimensionsUnion2", Union[MultiAttachDimensions4, MultiAttachDimensions3]
)


MultiAttachMultipliersMatch2TypedDict = TypeAliasType(
    "MultiAttachMultipliersMatch2TypedDict", Union[str, float, bool]
)


MultiAttachMultipliersMatch2 = TypeAliasType(
    "MultiAttachMultipliersMatch2", Union[str, float, bool]
)


class MultiAttachMultipliers2TypedDict(TypedDict):
    match: Dict[str, MultiAttachMultipliersMatch2TypedDict]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""
    factor: NotRequired[float]
    r"""Multiplies the matched rate. All matching multipliers stack."""
    add: NotRequired[float]
    r"""Added to the rate after every factor is applied, in credits per billing-unit group."""


class MultiAttachMultipliers2(BaseModel):
    match: Dict[str, MultiAttachMultipliersMatch2]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""

    factor: Optional[float] = None
    r"""Multiplies the matched rate. All matching multipliers stack."""

    add: Optional[float] = None
    r"""Added to the rate after every factor is applied, in credits per billing-unit group."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["factor", "add"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class MultiAttachCreditSchema2TypedDict(TypedDict):
    metered_feature_id: str
    r"""ID of the metered feature that draws from this credit system."""
    credit_cost: float
    r"""Credits consumed per billing-unit group."""
    billing_units: NotRequired[float]
    r"""Number of metered-feature units priced together. Defaults to one when omitted."""
    dimensions: NotRequired[Dict[str, MultiAttachDimensionsUnion2TypedDict]]
    r"""Named rates chosen by event properties. The most specific match sets the rate; with no match the item's own rate applies."""
    multipliers: NotRequired[Dict[str, MultiAttachMultipliers2TypedDict]]
    r"""Named adjustments chosen by event properties. Every match applies: factors multiply, then adds are summed."""


class MultiAttachCreditSchema2(BaseModel):
    metered_feature_id: str
    r"""ID of the metered feature that draws from this credit system."""

    credit_cost: float
    r"""Credits consumed per billing-unit group."""

    billing_units: Optional[float] = None
    r"""Number of metered-feature units priced together. Defaults to one when omitted."""

    dimensions: Optional[Dict[str, MultiAttachDimensionsUnion2]] = None
    r"""Named rates chosen by event properties. The most specific match sets the rate; with no match the item's own rate applies."""

    multipliers: Optional[Dict[str, MultiAttachMultipliers2]] = None
    r"""Named adjustments chosen by event properties. Every match applies: factors multiply, then adds are summed."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["billing_units", "dimensions", "multipliers"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


MultiAttachDimensionsMatch2TypedDict = TypeAliasType(
    "MultiAttachDimensionsMatch2TypedDict", Union[str, float, bool]
)


MultiAttachDimensionsMatch2 = TypeAliasType(
    "MultiAttachDimensionsMatch2", Union[str, float, bool]
)


class MultiAttachDimensions2TypedDict(TypedDict):
    match: Dict[str, MultiAttachDimensionsMatch2TypedDict]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""
    credit_cost: float
    r"""Credits consumed per billing-unit group when this dimension matches."""
    priority: NotRequired[int]
    r"""Breaks ties between dimensions that match the same number of keys. Higher wins."""


class MultiAttachDimensions2(BaseModel):
    match: Dict[str, MultiAttachDimensionsMatch2]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""

    credit_cost: float
    r"""Credits consumed per billing-unit group when this dimension matches."""

    priority: Optional[int] = None
    r"""Breaks ties between dimensions that match the same number of keys. Higher wins."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["priority"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


MultiAttachDimensionsMatch1TypedDict = TypeAliasType(
    "MultiAttachDimensionsMatch1TypedDict", Union[str, float, bool]
)


MultiAttachDimensionsMatch1 = TypeAliasType(
    "MultiAttachDimensionsMatch1", Union[str, float, bool]
)


MultiAttachDimensionsToEnum1 = Literal["inf",]


MultiAttachDimensionsToUnion1TypedDict = TypeAliasType(
    "MultiAttachDimensionsToUnion1TypedDict", Union[float, MultiAttachDimensionsToEnum1]
)
r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""


MultiAttachDimensionsToUnion1 = TypeAliasType(
    "MultiAttachDimensionsToUnion1", Union[float, MultiAttachDimensionsToEnum1]
)
r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""


class MultiAttachDimensionsTier1TypedDict(TypedDict):
    to: MultiAttachDimensionsToUnion1TypedDict
    r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""
    credit_cost: float
    r"""Credits consumed per billing-unit group within this tier."""


class MultiAttachDimensionsTier1(BaseModel):
    to: MultiAttachDimensionsToUnion1
    r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""

    credit_cost: float
    r"""Credits consumed per billing-unit group within this tier."""


class MultiAttachDimensions1TypedDict(TypedDict):
    match: Dict[str, MultiAttachDimensionsMatch1TypedDict]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""
    tiers: List[MultiAttachDimensionsTier1TypedDict]
    priority: NotRequired[int]
    r"""Breaks ties between dimensions that match the same number of keys. Higher wins."""
    tier_behavior: Literal["graduated"]


class MultiAttachDimensions1(BaseModel):
    match: Dict[str, MultiAttachDimensionsMatch1]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""

    tiers: List[MultiAttachDimensionsTier1]

    priority: Optional[int] = None
    r"""Breaks ties between dimensions that match the same number of keys. Higher wins."""

    tier_behavior: Annotated[
        Annotated[Literal["graduated"], AfterValidator(validate_const("graduated"))],
        pydantic.Field(alias="tier_behavior"),
    ] = "graduated"

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["priority"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


MultiAttachDimensionsUnion1TypedDict = TypeAliasType(
    "MultiAttachDimensionsUnion1TypedDict",
    Union[MultiAttachDimensions2TypedDict, MultiAttachDimensions1TypedDict],
)


MultiAttachDimensionsUnion1 = TypeAliasType(
    "MultiAttachDimensionsUnion1", Union[MultiAttachDimensions2, MultiAttachDimensions1]
)


MultiAttachMultipliersMatch1TypedDict = TypeAliasType(
    "MultiAttachMultipliersMatch1TypedDict", Union[str, float, bool]
)


MultiAttachMultipliersMatch1 = TypeAliasType(
    "MultiAttachMultipliersMatch1", Union[str, float, bool]
)


class MultiAttachMultipliers1TypedDict(TypedDict):
    match: Dict[str, MultiAttachMultipliersMatch1TypedDict]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""
    factor: NotRequired[float]
    r"""Multiplies the matched rate. All matching multipliers stack."""
    add: NotRequired[float]
    r"""Added to the rate after every factor is applied, in credits per billing-unit group."""


class MultiAttachMultipliers1(BaseModel):
    match: Dict[str, MultiAttachMultipliersMatch1]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""

    factor: Optional[float] = None
    r"""Multiplies the matched rate. All matching multipliers stack."""

    add: Optional[float] = None
    r"""Added to the rate after every factor is applied, in credits per billing-unit group."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["factor", "add"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


MultiAttachToEnum = Literal["inf",]


MultiAttachFeatureOverrideToUnionTypedDict = TypeAliasType(
    "MultiAttachFeatureOverrideToUnionTypedDict", Union[float, MultiAttachToEnum]
)
r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""


MultiAttachFeatureOverrideToUnion = TypeAliasType(
    "MultiAttachFeatureOverrideToUnion", Union[float, MultiAttachToEnum]
)
r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""


class MultiAttachFeatureOverrideTierTypedDict(TypedDict):
    to: MultiAttachFeatureOverrideToUnionTypedDict
    r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""
    credit_cost: float
    r"""Credits consumed per billing-unit group within this tier."""


class MultiAttachFeatureOverrideTier(BaseModel):
    to: MultiAttachFeatureOverrideToUnion
    r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""

    credit_cost: float
    r"""Credits consumed per billing-unit group within this tier."""


class MultiAttachCreditSchema1TypedDict(TypedDict):
    metered_feature_id: str
    r"""ID of the metered feature that draws from this credit system."""
    tiers: List[MultiAttachFeatureOverrideTierTypedDict]
    billing_units: NotRequired[float]
    r"""Number of metered-feature units priced together. Defaults to one when omitted."""
    dimensions: NotRequired[Dict[str, MultiAttachDimensionsUnion1TypedDict]]
    r"""Named rates chosen by event properties. The most specific match sets the rate; with no match the item's own rate applies."""
    multipliers: NotRequired[Dict[str, MultiAttachMultipliers1TypedDict]]
    r"""Named adjustments chosen by event properties. Every match applies: factors multiply, then adds are summed."""
    tier_behavior: Literal["graduated"]


class MultiAttachCreditSchema1(BaseModel):
    metered_feature_id: str
    r"""ID of the metered feature that draws from this credit system."""

    tiers: List[MultiAttachFeatureOverrideTier]

    billing_units: Optional[float] = None
    r"""Number of metered-feature units priced together. Defaults to one when omitted."""

    dimensions: Optional[Dict[str, MultiAttachDimensionsUnion1]] = None
    r"""Named rates chosen by event properties. The most specific match sets the rate; with no match the item's own rate applies."""

    multipliers: Optional[Dict[str, MultiAttachMultipliers1]] = None
    r"""Named adjustments chosen by event properties. Every match applies: factors multiply, then adds are summed."""

    tier_behavior: Annotated[
        Annotated[Literal["graduated"], AfterValidator(validate_const("graduated"))],
        pydantic.Field(alias="tier_behavior"),
    ] = "graduated"

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["billing_units", "dimensions", "multipliers"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


MultiAttachCreditSchemaUnionTypedDict = TypeAliasType(
    "MultiAttachCreditSchemaUnionTypedDict",
    Union[MultiAttachCreditSchema2TypedDict, MultiAttachCreditSchema1TypedDict],
)


MultiAttachCreditSchemaUnion = TypeAliasType(
    "MultiAttachCreditSchemaUnion",
    Union[MultiAttachCreditSchema2, MultiAttachCreditSchema1],
)


class MultiAttachFeatureOverrideTypedDict(TypedDict):
    r"""Overrides fields of this item's feature for customers on this plan (e.g. a credit system's credit_schema)."""

    credit_schema: NotRequired[List[MultiAttachCreditSchemaUnionTypedDict]]
    r"""For credit system features: replaces the feature's credit_schema entirely for customers on this plan."""


class MultiAttachFeatureOverride(BaseModel):
    r"""Overrides fields of this item's feature for customers on this plan (e.g. a credit system's credit_schema)."""

    credit_schema: Optional[List[MultiAttachCreditSchemaUnion]] = None
    r"""For credit system features: replaces the feature's credit_schema entirely for customers on this plan."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["credit_schema"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class MultiAttachPlanItemTypedDict(TypedDict):
    r"""Configuration for a feature item in a plan, including usage limits, pricing, and rollover settings."""

    feature_id: str
    r"""The ID of the feature to configure."""
    included: NotRequired[float]
    r"""Number of free units included. Balance resets to this each interval for consumable features."""
    unlimited: NotRequired[bool]
    r"""If true, customer has unlimited access to this feature."""
    pooled: NotRequired[bool]
    r"""Whether entity-level grants contribute to a shared customer balance."""
    reset: NotRequired[MultiAttachResetTypedDict]
    r"""Reset configuration for consumable features. Omit for non-consumable features like seats."""
    price: NotRequired[MultiAttachPriceTypedDict]
    r"""Pricing for usage beyond included units. Omit for free features."""
    proration: NotRequired[MultiAttachProrationTypedDict]
    r"""Proration settings for prepaid features. Controls mid-cycle quantity change billing."""
    rollover: NotRequired[MultiAttachRolloverTypedDict]
    r"""Rollover config for unused units. If set, unused included units carry over."""
    feature_override: NotRequired[MultiAttachFeatureOverrideTypedDict]
    r"""Overrides fields of this item's feature for customers on this plan (e.g. a credit system's credit_schema)."""


class MultiAttachPlanItem(BaseModel):
    r"""Configuration for a feature item in a plan, including usage limits, pricing, and rollover settings."""

    feature_id: str
    r"""The ID of the feature to configure."""

    included: Optional[float] = None
    r"""Number of free units included. Balance resets to this each interval for consumable features."""

    unlimited: Optional[bool] = None
    r"""If true, customer has unlimited access to this feature."""

    pooled: Optional[bool] = False
    r"""Whether entity-level grants contribute to a shared customer balance."""

    reset: Optional[MultiAttachReset] = None
    r"""Reset configuration for consumable features. Omit for non-consumable features like seats."""

    price: Optional[MultiAttachPrice] = None
    r"""Pricing for usage beyond included units. Omit for free features."""

    proration: Optional[MultiAttachProration] = None
    r"""Proration settings for prepaid features. Controls mid-cycle quantity change billing."""

    rollover: Optional[MultiAttachRollover] = None
    r"""Rollover config for unused units. If set, unused included units carry over."""

    feature_override: Optional[MultiAttachFeatureOverride] = None
    r"""Overrides fields of this item's feature for customers on this plan (e.g. a credit system's credit_schema)."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "included",
                "unlimited",
                "pooled",
                "reset",
                "price",
                "proration",
                "rollover",
                "feature_override",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class MultiAttachCustomizeTypedDict(TypedDict):
    r"""Customize the plan to attach. Can override its price or items."""

    price: NotRequired[Nullable[MultiAttachBasePriceTypedDict]]
    r"""Override the base price of the plan. Pass null to remove the base price."""
    items: NotRequired[List[MultiAttachPlanItemTypedDict]]
    r"""Override the items in the plan."""


class MultiAttachCustomize(BaseModel):
    r"""Customize the plan to attach. Can override its price or items."""

    price: OptionalNullable[MultiAttachBasePrice] = UNSET
    r"""Override the base price of the plan. Pass null to remove the base price."""

    items: Optional[List[MultiAttachPlanItem]] = None
    r"""Override the items in the plan."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["price", "items"])
        nullable_fields = set(["price"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


class MultiAttachFeatureQuantityTypedDict(TypedDict):
    r"""Quantity configuration for a prepaid feature."""

    feature_id: str
    r"""The ID of the feature to set quantity for."""
    quantity: NotRequired[float]
    r"""The quantity of the feature."""
    adjustable: NotRequired[bool]
    r"""Whether the customer can adjust the quantity."""


class MultiAttachFeatureQuantity(BaseModel):
    r"""Quantity configuration for a prepaid feature."""

    feature_id: str
    r"""The ID of the feature to set quantity for."""

    quantity: Optional[float] = None
    r"""The quantity of the feature."""

    adjustable: Optional[bool] = None
    r"""Whether the customer can adjust the quantity."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["quantity", "adjustable"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class MultiAttachPlanTypedDict(TypedDict):
    plan_id: str
    r"""The ID of the plan to attach."""
    customize: NotRequired[MultiAttachCustomizeTypedDict]
    r"""Customize the plan to attach. Can override its price or items."""
    feature_quantities: NotRequired[List[MultiAttachFeatureQuantityTypedDict]]
    r"""If this plan contains prepaid features, use this field to specify the quantity of each prepaid feature."""
    version: NotRequired[float]
    r"""The version of the plan to attach."""
    subscription_id: NotRequired[str]
    r"""A unique ID to identify this subscription. Useful when attaching the same plan multiple times."""
    entity_id: NotRequired[Nullable[str]]
    r"""The entity scope for this plan. Omit to inherit the request scope, or pass null for customer-level."""


class MultiAttachPlan(BaseModel):
    plan_id: str
    r"""The ID of the plan to attach."""

    customize: Optional[MultiAttachCustomize] = None
    r"""Customize the plan to attach. Can override its price or items."""

    feature_quantities: Optional[List[MultiAttachFeatureQuantity]] = None
    r"""If this plan contains prepaid features, use this field to specify the quantity of each prepaid feature."""

    version: Optional[float] = None
    r"""The version of the plan to attach."""

    subscription_id: Optional[str] = None
    r"""A unique ID to identify this subscription. Useful when attaching the same plan multiple times."""

    entity_id: OptionalNullable[str] = UNSET
    r"""The entity scope for this plan. Omit to inherit the request scope, or pass null for customer-level."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "customize",
                "feature_quantities",
                "version",
                "subscription_id",
                "entity_id",
            ]
        )
        nullable_fields = set(["entity_id"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


MultiAttachDurationType = Literal[
    "day",
    "month",
    "year",
]
r"""Unit of time for the trial ('day', 'month', 'year')."""


MultiAttachOnEnd = Literal[
    "bill",
    "revert",
]
r"""Behavior when the trial ends. 'bill' charges the customer (default). 'revert' expires the trial and restores the customer's previous plan."""


class MultiAttachFreeTrialParamsTypedDict(TypedDict):
    r"""Free trial configuration for a plan."""

    duration_length: float
    r"""Number of duration_type periods the trial lasts."""
    duration_type: NotRequired[MultiAttachDurationType]
    r"""Unit of time for the trial ('day', 'month', 'year')."""
    card_required: NotRequired[bool]
    r"""If true, a payment method is required to start the trial and the customer is charged when it ends. Defaults to false."""
    on_end: NotRequired[MultiAttachOnEnd]
    r"""Behavior when the trial ends. 'bill' charges the customer (default). 'revert' expires the trial and restores the customer's previous plan."""


class MultiAttachFreeTrialParams(BaseModel):
    r"""Free trial configuration for a plan."""

    duration_length: float
    r"""Number of duration_type periods the trial lasts."""

    duration_type: Optional[MultiAttachDurationType] = "month"
    r"""Unit of time for the trial ('day', 'month', 'year')."""

    card_required: Optional[bool] = False
    r"""If true, a payment method is required to start the trial and the customer is charged when it ends. Defaults to false."""

    on_end: Optional[MultiAttachOnEnd] = None
    r"""Behavior when the trial ends. 'bill' charges the customer (default). 'revert' expires the trial and restores the customer's previous plan."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["duration_type", "card_required", "on_end"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class MultiAttachInvoiceModeTypedDict(TypedDict):
    r"""Invoice mode creates a draft or open invoice and sends it to the customer, instead of charging their card immediately."""

    enabled: bool
    r"""When true, creates an invoice and sends it to the customer instead of charging their card immediately. Uses Stripe's send_invoice collection method."""
    enable_plan_immediately: NotRequired[bool]
    r"""If true, enables the plan immediately even though the invoice is not paid yet."""
    finalize: NotRequired[bool]
    r"""If true, finalizes the invoice so it can be sent to the customer. If false, keeps it as a draft for manual review."""
    invoice_template_id: NotRequired[str]
    r"""ID of an invoice template (configured in billing settings) whose footer (e.g. bank details) is applied to the invoice."""
    net_terms_days: NotRequired[int]
    r"""Number of days the customer has to pay the invoice before it is due (Stripe days_until_due)."""


class MultiAttachInvoiceMode(BaseModel):
    r"""Invoice mode creates a draft or open invoice and sends it to the customer, instead of charging their card immediately."""

    enabled: bool
    r"""When true, creates an invoice and sends it to the customer instead of charging their card immediately. Uses Stripe's send_invoice collection method."""

    enable_plan_immediately: Optional[bool] = False
    r"""If true, enables the plan immediately even though the invoice is not paid yet."""

    finalize: Optional[bool] = True
    r"""If true, finalizes the invoice so it can be sent to the customer. If false, keeps it as a draft for manual review."""

    invoice_template_id: Optional[str] = None
    r"""ID of an invoice template (configured in billing settings) whose footer (e.g. bank details) is applied to the invoice."""

    net_terms_days: Optional[int] = None
    r"""Number of days the customer has to pay the invoice before it is due (Stripe days_until_due)."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "enable_plan_immediately",
                "finalize",
                "invoice_template_id",
                "net_terms_days",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class MultiAttachAttachDiscountTypedDict(TypedDict):
    r"""A discount to apply. Can be either a reward ID or a promotion code."""

    reward_id: NotRequired[str]
    r"""The ID of the reward to apply as a discount."""
    promotion_code: NotRequired[str]
    r"""The promotion code to apply as a discount."""


class MultiAttachAttachDiscount(BaseModel):
    r"""A discount to apply. Can be either a reward ID or a promotion code."""

    reward_id: Optional[str] = None
    r"""The ID of the reward to apply as a discount."""

    promotion_code: Optional[str] = None
    r"""The promotion code to apply as a discount."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["reward_id", "promotion_code"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


MultiAttachBillingBehavior = Literal[
    "prorate_immediately",
    "none",
]
r"""How to handle billing. 'prorate_immediately' charges/credits prorated amounts now, 'none' does not charge/credit anything."""


MultiAttachRedirectMode = Literal[
    "always",
    "if_required",
    "never",
]
r"""Controls when to return a checkout URL. 'always' returns a URL even if payment succeeds, 'if_required' only when payment action is needed, 'never' disables redirects."""


MultiAttachLimitType = Literal[
    "absolute",
    "usage_percentage",
]
r"""How overage_limit is interpreted: an absolute overage cap (default) or a percentage of the main-plan allowance."""


class MultiAttachSpendLimitTypedDict(TypedDict):
    feature_id: NotRequired[str]
    r"""Optional feature ID this spend limit applies to."""
    enabled: NotRequired[bool]
    r"""Whether the overage spend limit is enabled."""
    limit_type: NotRequired[MultiAttachLimitType]
    r"""How overage_limit is interpreted: an absolute overage cap (default) or a percentage of the main-plan allowance."""
    overage_limit: NotRequired[float]
    r"""Overage cap for the feature: absolute units, or a percent (e.g. 120) when limit_type is usage_percentage."""
    skip_overage_billing: NotRequired[bool]
    r"""When true, overage for this feature is not posted to Stripe. Usage tracking and balance resets still behave normally."""


class MultiAttachSpendLimit(BaseModel):
    feature_id: Optional[str] = None
    r"""Optional feature ID this spend limit applies to."""

    enabled: Optional[bool] = False
    r"""Whether the overage spend limit is enabled."""

    limit_type: Optional[MultiAttachLimitType] = None
    r"""How overage_limit is interpreted: an absolute overage cap (default) or a percentage of the main-plan allowance."""

    overage_limit: Optional[float] = None
    r"""Overage cap for the feature: absolute units, or a percent (e.g. 120) when limit_type is usage_percentage."""

    skip_overage_billing: Optional[bool] = None
    r"""When true, overage for this feature is not posted to Stripe. Usage tracking and balance resets still behave normally."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "feature_id",
                "enabled",
                "limit_type",
                "overage_limit",
                "skip_overage_billing",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


MultiAttachEntityDataInterval = Literal[
    "day",
    "week",
    "month",
    "year",
]
r"""Interval for the cap, aligned to the customer's billing cycle."""


MultiAttachAnchor = Literal[
    "billing_cycle",
    "utc",
]
r"""Window alignment. 'billing_cycle' phases the interval to the customer's renewal time; 'utc' aligns to the UTC calendar."""


MultiAttachUsageLimitPropertiesTypedDict = TypeAliasType(
    "MultiAttachUsageLimitPropertiesTypedDict", Union[str, float, bool]
)


MultiAttachUsageLimitProperties = TypeAliasType(
    "MultiAttachUsageLimitProperties", Union[str, float, bool]
)


class MultiAttachUsageLimitFilterTypedDict(TypedDict):
    r"""When set, only usage from events whose properties match counts toward this cap. Omit to count all usage of the feature."""

    properties: Dict[str, MultiAttachUsageLimitPropertiesTypedDict]


class MultiAttachUsageLimitFilter(BaseModel):
    r"""When set, only usage from events whose properties match counts toward this cap. Omit to count all usage of the feature."""

    properties: Dict[str, MultiAttachUsageLimitProperties]


class MultiAttachUsageLimitTypedDict(TypedDict):
    feature_id: str
    r"""The feature this usage limit applies to."""
    limit: float
    r"""Maximum units allowed per interval."""
    interval: MultiAttachEntityDataInterval
    r"""Interval for the cap, aligned to the customer's billing cycle."""
    enabled: NotRequired[bool]
    r"""Whether this usage limit is enabled."""
    anchor: NotRequired[MultiAttachAnchor]
    r"""Window alignment. 'billing_cycle' phases the interval to the customer's renewal time; 'utc' aligns to the UTC calendar."""
    filter_: NotRequired[MultiAttachUsageLimitFilterTypedDict]
    r"""When set, only usage from events whose properties match counts toward this cap. Omit to count all usage of the feature."""


class MultiAttachUsageLimit(BaseModel):
    feature_id: str
    r"""The feature this usage limit applies to."""

    limit: float
    r"""Maximum units allowed per interval."""

    interval: MultiAttachEntityDataInterval
    r"""Interval for the cap, aligned to the customer's billing cycle."""

    enabled: Optional[bool] = True
    r"""Whether this usage limit is enabled."""

    anchor: Optional[MultiAttachAnchor] = None
    r"""Window alignment. 'billing_cycle' phases the interval to the customer's renewal time; 'utc' aligns to the UTC calendar."""

    filter_: Annotated[
        Optional[MultiAttachUsageLimitFilter], pydantic.Field(alias="filter")
    ] = None
    r"""When set, only usage from events whose properties match counts toward this cap. Omit to count all usage of the feature."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["enabled", "anchor", "filter"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


MultiAttachThresholdType = Literal[
    "usage",
    "usage_percentage",
    "remaining",
    "remaining_percentage",
]
r"""Whether the threshold is an absolute count or a percentage of the usage allowance or remaining balance."""


MultiAttachBasis = Literal[
    "balance",
    "included",
    "recurring",
    "usage_limit",
]
r"""What 100% means. balance: every grant on the feature. included: the plan allowance only. recurring: grants that reset. usage_limit: the cap of the usage limit with the same feature and filter."""


MultiAttachUsageAlertPropertiesTypedDict = TypeAliasType(
    "MultiAttachUsageAlertPropertiesTypedDict", Union[str, float, bool]
)


MultiAttachUsageAlertProperties = TypeAliasType(
    "MultiAttachUsageAlertProperties", Union[str, float, bool]
)


class MultiAttachUsageAlertFilterTypedDict(TypedDict):
    r"""Only valid with basis usage_limit. Points the alert at the usage limit carrying the same filter."""

    properties: Dict[str, MultiAttachUsageAlertPropertiesTypedDict]


class MultiAttachUsageAlertFilter(BaseModel):
    r"""Only valid with basis usage_limit. Points the alert at the usage limit carrying the same filter."""

    properties: Dict[str, MultiAttachUsageAlertProperties]


class MultiAttachUsageAlertTypedDict(TypedDict):
    threshold: float
    r"""The threshold value that triggers the alert. For usage or remaining, this is an absolute count. For usage_percentage or remaining_percentage, this is a percentage (0-100)."""
    threshold_type: MultiAttachThresholdType
    r"""Whether the threshold is an absolute count or a percentage of the usage allowance or remaining balance."""
    feature_id: NotRequired[str]
    r"""The feature ID this alert applies to."""
    enabled: NotRequired[bool]
    r"""Whether this usage alert is enabled."""
    basis: NotRequired[MultiAttachBasis]
    r"""What 100% means. balance: every grant on the feature. included: the plan allowance only. recurring: grants that reset. usage_limit: the cap of the usage limit with the same feature and filter."""
    filter_: NotRequired[MultiAttachUsageAlertFilterTypedDict]
    r"""Only valid with basis usage_limit. Points the alert at the usage limit carrying the same filter."""
    name: NotRequired[str]
    r"""Optional user-defined label to distinguish multiple alerts on the same feature."""


class MultiAttachUsageAlert(BaseModel):
    threshold: float
    r"""The threshold value that triggers the alert. For usage or remaining, this is an absolute count. For usage_percentage or remaining_percentage, this is a percentage (0-100)."""

    threshold_type: MultiAttachThresholdType
    r"""Whether the threshold is an absolute count or a percentage of the usage allowance or remaining balance."""

    feature_id: Optional[str] = None
    r"""The feature ID this alert applies to."""

    enabled: Optional[bool] = True
    r"""Whether this usage alert is enabled."""

    basis: Optional[MultiAttachBasis] = "balance"
    r"""What 100% means. balance: every grant on the feature. included: the plan allowance only. recurring: grants that reset. usage_limit: the cap of the usage limit with the same feature and filter."""

    filter_: Annotated[
        Optional[MultiAttachUsageAlertFilter], pydantic.Field(alias="filter")
    ] = None
    r"""Only valid with basis usage_limit. Points the alert at the usage limit carrying the same filter."""

    name: Optional[str] = None
    r"""Optional user-defined label to distinguish multiple alerts on the same feature."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["feature_id", "enabled", "basis", "filter", "name"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class MultiAttachOverageAllowedTypedDict(TypedDict):
    feature_id: str
    r"""The feature ID this overage allowed control applies to."""
    enabled: NotRequired[bool]
    r"""Whether overage is allowed for this feature."""


class MultiAttachOverageAllowed(BaseModel):
    feature_id: str
    r"""The feature ID this overage allowed control applies to."""

    enabled: Optional[bool] = False
    r"""Whether overage is allowed for this feature."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["enabled"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class MultiAttachBillingControlsTypedDict(TypedDict):
    r"""Billing controls for the entity."""

    spend_limits: NotRequired[List[MultiAttachSpendLimitTypedDict]]
    r"""List of spend limits per feature. Each entry caps overage (overage_limit) and/or per-interval usage (usage_limit)."""
    usage_limits: NotRequired[List[MultiAttachUsageLimitTypedDict]]
    r"""List of hard usage caps per feature for this entity. An entity entry overrides the customer's for that feature."""
    usage_alerts: NotRequired[List[MultiAttachUsageAlertTypedDict]]
    r"""List of usage alert configurations per feature."""
    overage_allowed: NotRequired[List[MultiAttachOverageAllowedTypedDict]]
    r"""List of overage allowed controls per feature. When enabled, usage can exceed balance."""


class MultiAttachBillingControls(BaseModel):
    r"""Billing controls for the entity."""

    spend_limits: Optional[List[MultiAttachSpendLimit]] = None
    r"""List of spend limits per feature. Each entry caps overage (overage_limit) and/or per-interval usage (usage_limit)."""

    usage_limits: Optional[List[MultiAttachUsageLimit]] = None
    r"""List of hard usage caps per feature for this entity. An entity entry overrides the customer's for that feature."""

    usage_alerts: Optional[List[MultiAttachUsageAlert]] = None
    r"""List of usage alert configurations per feature."""

    overage_allowed: Optional[List[MultiAttachOverageAllowed]] = None
    r"""List of overage allowed controls per feature. When enabled, usage can exceed balance."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            ["spend_limits", "usage_limits", "usage_alerts", "overage_allowed"]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class MultiAttachEntityDataTypedDict(TypedDict):
    feature_id: str
    r"""The feature ID that this entity is associated with"""
    name: NotRequired[str]
    r"""Name of the entity"""
    billing_controls: NotRequired[MultiAttachBillingControlsTypedDict]
    r"""Billing controls for the entity."""


class MultiAttachEntityData(BaseModel):
    feature_id: str
    r"""The feature ID that this entity is associated with"""

    name: Optional[str] = None
    r"""Name of the entity"""

    billing_controls: Optional[MultiAttachBillingControls] = None
    r"""Billing controls for the entity."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["name", "billing_controls"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class MultiAttachParamsTypedDict(TypedDict):
    customer_id: str
    r"""The ID of the customer to attach the plans to."""
    plans: List[MultiAttachPlanTypedDict]
    r"""The list of plans to attach to the customer."""
    entity_id: NotRequired[str]
    r"""The ID of the entity to attach the plans to."""
    free_trial: NotRequired[Nullable[MultiAttachFreeTrialParamsTypedDict]]
    r"""Free trial configuration applied to all plans. Pass an object to set a custom trial, or null to remove any trial."""
    starts_at: NotRequired[int]
    r"""Unix timestamp in milliseconds for backdating every plan in this multi-attach."""
    currency: NotRequired[str]
    r"""Currency to bill this multi-attach in (e.g. usd, eur). Must match the customer's currency if they are already locked to one, and every plan must offer a paid price in it. Defaults to the customer's currency, then the org default."""
    invoice_mode: NotRequired[MultiAttachInvoiceModeTypedDict]
    r"""Invoice mode creates a draft or open invoice and sends it to the customer, instead of charging their card immediately."""
    discounts: NotRequired[List[MultiAttachAttachDiscountTypedDict]]
    r"""List of discounts to apply. Each discount can be an Autumn reward ID, Stripe coupon ID, or Stripe promotion code."""
    billing_behavior: NotRequired[MultiAttachBillingBehavior]
    r"""How to handle billing. 'prorate_immediately' charges/credits prorated amounts now, 'none' does not charge/credit anything."""
    billing_cycle_anchor: Literal["now"]
    r"""Pass 'now' to reset the billing cycle of every plan on the subscription to the time of this request."""
    success_url: NotRequired[str]
    r"""URL to redirect to after successful checkout."""
    checkout_session_params: NotRequired[Dict[str, Any]]
    r"""Additional parameters to pass into the creation of the Stripe checkout session."""
    redirect_mode: NotRequired[MultiAttachRedirectMode]
    r"""Controls when to return a checkout URL. 'always' returns a URL even if payment succeeds, 'if_required' only when payment action is needed, 'never' disables redirects."""
    new_billing_subscription: NotRequired[bool]
    r"""Only applicable when the customer has an existing Stripe subscription. If true, creates a new separate subscription instead of merging into the existing one."""
    enable_plan_immediately: NotRequired[bool]
    r"""If true, the cusProducts are activated immediately even when payment is pending via Stripe checkout."""
    customer_data: NotRequired[CustomerDataTypedDict]
    r"""Customer details to set when creating a customer"""
    entity_data: NotRequired[MultiAttachEntityDataTypedDict]


class MultiAttachParams(BaseModel):
    customer_id: str
    r"""The ID of the customer to attach the plans to."""

    plans: List[MultiAttachPlan]
    r"""The list of plans to attach to the customer."""

    entity_id: Optional[str] = None
    r"""The ID of the entity to attach the plans to."""

    free_trial: OptionalNullable[MultiAttachFreeTrialParams] = UNSET
    r"""Free trial configuration applied to all plans. Pass an object to set a custom trial, or null to remove any trial."""

    starts_at: Optional[int] = None
    r"""Unix timestamp in milliseconds for backdating every plan in this multi-attach."""

    currency: Optional[str] = None
    r"""Currency to bill this multi-attach in (e.g. usd, eur). Must match the customer's currency if they are already locked to one, and every plan must offer a paid price in it. Defaults to the customer's currency, then the org default."""

    invoice_mode: Optional[MultiAttachInvoiceMode] = None
    r"""Invoice mode creates a draft or open invoice and sends it to the customer, instead of charging their card immediately."""

    discounts: Optional[List[MultiAttachAttachDiscount]] = None
    r"""List of discounts to apply. Each discount can be an Autumn reward ID, Stripe coupon ID, or Stripe promotion code."""

    billing_behavior: Optional[MultiAttachBillingBehavior] = None
    r"""How to handle billing. 'prorate_immediately' charges/credits prorated amounts now, 'none' does not charge/credit anything."""

    billing_cycle_anchor: Annotated[
        Annotated[Optional[Literal["now"]], AfterValidator(validate_const("now"))],
        pydantic.Field(alias="billing_cycle_anchor"),
    ] = "now"
    r"""Pass 'now' to reset the billing cycle of every plan on the subscription to the time of this request."""

    success_url: Optional[str] = None
    r"""URL to redirect to after successful checkout."""

    checkout_session_params: Optional[Dict[str, Any]] = None
    r"""Additional parameters to pass into the creation of the Stripe checkout session."""

    redirect_mode: Optional[MultiAttachRedirectMode] = "if_required"
    r"""Controls when to return a checkout URL. 'always' returns a URL even if payment succeeds, 'if_required' only when payment action is needed, 'never' disables redirects."""

    new_billing_subscription: Optional[bool] = None
    r"""Only applicable when the customer has an existing Stripe subscription. If true, creates a new separate subscription instead of merging into the existing one."""

    enable_plan_immediately: Optional[bool] = None
    r"""If true, the cusProducts are activated immediately even when payment is pending via Stripe checkout."""

    customer_data: Optional[CustomerData] = None
    r"""Customer details to set when creating a customer"""

    entity_data: Optional[MultiAttachEntityData] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "entity_id",
                "free_trial",
                "starts_at",
                "currency",
                "invoice_mode",
                "discounts",
                "billing_behavior",
                "billing_cycle_anchor",
                "success_url",
                "checkout_session_params",
                "redirect_mode",
                "new_billing_subscription",
                "enable_plan_immediately",
                "customer_data",
                "entity_data",
            ]
        )
        nullable_fields = set(["free_trial"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


class MultiAttachInvoiceTypedDict(TypedDict):
    r"""Invoice details if an invoice was created. Only present when a charge was made."""

    status: Nullable[str]
    r"""The status of the invoice (e.g., 'paid', 'open', 'draft')."""
    stripe_id: str
    r"""The Stripe invoice ID."""
    total: float
    r"""The total amount of the invoice in cents."""
    currency: str
    r"""The three-letter ISO currency code (e.g., 'usd')."""
    hosted_invoice_url: Nullable[str]
    r"""URL to the hosted invoice page where the customer can view and pay the invoice."""


class MultiAttachInvoice(BaseModel):
    r"""Invoice details if an invoice was created. Only present when a charge was made."""

    status: Nullable[str]
    r"""The status of the invoice (e.g., 'paid', 'open', 'draft')."""

    stripe_id: str
    r"""The Stripe invoice ID."""

    total: float
    r"""The total amount of the invoice in cents."""

    currency: str
    r"""The three-letter ISO currency code (e.g., 'usd')."""

    hosted_invoice_url: Nullable[str]
    r"""URL to the hosted invoice page where the customer can view and pay the invoice."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                m[k] = val

        return m


MultiAttachCode = Union[
    Literal[
        "3ds_required",
        "payment_method_required",
        "payment_failed",
        "payment_processing",
    ],
    UnrecognizedStr,
]
r"""The type of action required to complete the payment."""


class MultiAttachRequiredActionTypedDict(TypedDict):
    r"""Details about any action required to complete the payment. Present when the payment could not be processed automatically."""

    code: MultiAttachCode
    r"""The type of action required to complete the payment."""
    reason: str
    r"""A human-readable explanation of why this action is required."""


class MultiAttachRequiredAction(BaseModel):
    r"""Details about any action required to complete the payment. Present when the payment could not be processed automatically."""

    code: MultiAttachCode
    r"""The type of action required to complete the payment."""

    reason: str
    r"""A human-readable explanation of why this action is required."""


class MultiAttachResponseTypedDict(TypedDict):
    r"""OK"""

    customer_id: str
    r"""The ID of the customer."""
    payment_url: Nullable[str]
    r"""URL to redirect the customer to complete payment. Null if no payment action is required."""
    entity_id: NotRequired[str]
    r"""The ID of the entity, if the plan was attached to an entity."""
    invoice: NotRequired[MultiAttachInvoiceTypedDict]
    r"""Invoice details if an invoice was created. Only present when a charge was made."""
    required_action: NotRequired[MultiAttachRequiredActionTypedDict]
    r"""Details about any action required to complete the payment. Present when the payment could not be processed automatically."""


class MultiAttachResponse(BaseModel):
    r"""OK"""

    customer_id: str
    r"""The ID of the customer."""

    payment_url: Nullable[str]
    r"""URL to redirect the customer to complete payment. Null if no payment action is required."""

    entity_id: Optional[str] = None
    r"""The ID of the entity, if the plan was attached to an entity."""

    invoice: Optional[MultiAttachInvoice] = None
    r"""Invoice details if an invoice was created. Only present when a charge was made."""

    required_action: Optional[MultiAttachRequiredAction] = None
    r"""Details about any action required to complete the payment. Present when the payment could not be processed automatically."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["entity_id", "invoice", "required_action"])
        nullable_fields = set(["payment_url"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


try:
    MultiAttachDimensions3.model_rebuild()
except NameError:
    pass
try:
    MultiAttachDimensions1.model_rebuild()
except NameError:
    pass
try:
    MultiAttachCreditSchema1.model_rebuild()
except NameError:
    pass
try:
    MultiAttachUsageLimit.model_rebuild()
except NameError:
    pass
try:
    MultiAttachUsageAlert.model_rebuild()
except NameError:
    pass
try:
    MultiAttachParams.model_rebuild()
except NameError:
    pass
