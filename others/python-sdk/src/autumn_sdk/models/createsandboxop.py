"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from autumn_sdk.types import BaseModel, UNSET_SENTINEL, UnrecognizedStr
from autumn_sdk.utils import FieldMetadata, HeaderMetadata
import pydantic
from pydantic import model_serializer
from typing import Literal, Optional, Union
from typing_extensions import Annotated, NotRequired, TypedDict


class CreateSandboxGlobalsTypedDict(TypedDict):
    x_api_version: NotRequired[str]


class CreateSandboxGlobals(BaseModel):
    x_api_version: Annotated[
        Optional[str],
        pydantic.Field(alias="x-api-version"),
        FieldMetadata(header=HeaderMetadata(style="simple", explode=False)),
    ] = "2.4.0"

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["x-api-version"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


ColorRequest = Literal[
    "gray",
    "blue",
    "green",
    "amber",
    "red",
    "pink",
]
r"""Colour the dashboard uses to label the sandbox. Defaults to `gray`."""


class CreateSandboxParamsTypedDict(TypedDict):
    name: str
    r"""A name for the sandbox, unique within your organization."""
    color: NotRequired[ColorRequest]
    r"""Colour the dashboard uses to label the sandbox. Defaults to `gray`."""
    icon: NotRequired[str]
    r"""Icon the dashboard uses to label the sandbox. Defaults to `Flask`."""


class CreateSandboxParams(BaseModel):
    name: str
    r"""A name for the sandbox, unique within your organization."""

    color: Optional[ColorRequest] = None
    r"""Colour the dashboard uses to label the sandbox. Defaults to `gray`."""

    icon: Optional[str] = None
    r"""Icon the dashboard uses to label the sandbox. Defaults to `Flask`."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["color", "icon"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


CreateSandboxColorResponse = Union[
    Literal[
        "gray",
        "blue",
        "green",
        "amber",
        "red",
        "pink",
    ],
    UnrecognizedStr,
]
r"""Colour the dashboard uses to label the sandbox."""


class CreateSandboxResponseTypedDict(TypedDict):
    r"""OK"""

    id: str
    r"""The sandbox's organization ID."""
    name: str
    r"""The sandbox's name."""
    slug: str
    r"""The sandbox's slug, derived from its name."""
    created_at: float
    r"""When the sandbox was created, ms since epoch."""
    color: CreateSandboxColorResponse
    r"""Colour the dashboard uses to label the sandbox."""
    icon: str
    r"""Icon the dashboard uses to label the sandbox."""
    secret_key: str
    r"""The sandbox's own secret key. Shown once, here — store it before you discard the response."""


class CreateSandboxResponse(BaseModel):
    r"""OK"""

    id: str
    r"""The sandbox's organization ID."""

    name: str
    r"""The sandbox's name."""

    slug: str
    r"""The sandbox's slug, derived from its name."""

    created_at: float
    r"""When the sandbox was created, ms since epoch."""

    color: CreateSandboxColorResponse
    r"""Colour the dashboard uses to label the sandbox."""

    icon: str
    r"""Icon the dashboard uses to label the sandbox."""

    secret_key: str
    r"""The sandbox's own secret key. Shown once, here — store it before you discard the response."""
