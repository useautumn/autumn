"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from autumn_sdk.types import BaseModel, Nullable, UNSET_SENTINEL, UnrecognizedStr
from autumn_sdk.utils import FieldMetadata, HeaderMetadata, validate_const
from autumn_sdk.utils.unions import parse_open_union
from functools import partial
import pydantic
from pydantic import ConfigDict, model_serializer
from pydantic.functional_validators import AfterValidator, BeforeValidator
from typing import Any, List, Literal, Optional, Union
from typing_extensions import Annotated, NotRequired, TypeAliasType, TypedDict


class VerifyGlobalsTypedDict(TypedDict):
    x_api_version: NotRequired[str]


class VerifyGlobals(BaseModel):
    x_api_version: Annotated[
        Optional[str],
        pydantic.Field(alias="x-api-version"),
        FieldMetadata(header=HeaderMetadata(style="simple", explode=False)),
    ] = "2.4.0"

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["x-api-version"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class VerifyParamsTypedDict(TypedDict):
    customer_id: str
    r"""Autumn customer whose Stripe subscriptions should be checked against Autumn's customer_products."""
    subscription_ids: NotRequired[List[str]]
    r"""Optional whitelist of Stripe subscription IDs to verify. Defaults to every subscription linked to the customer's plans, plus any other active Stripe subscription on the customer, which is reported as not linked to Autumn."""
    strict: NotRequired[bool]
    r"""When true, report missing usage-based items and unexpected metered Stripe items. Defaults to false."""


class VerifyParams(BaseModel):
    customer_id: str
    r"""Autumn customer whose Stripe subscriptions should be checked against Autumn's customer_products."""

    subscription_ids: Optional[List[str]] = None
    r"""Optional whitelist of Stripe subscription IDs to verify. Defaults to every subscription linked to the customer's plans, plus any other active Stripe subscription on the customer, which is reported as not linked to Autumn."""

    strict: Optional[bool] = None
    r"""When true, report missing usage-based items and unexpected metered Stripe items. Defaults to false."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["subscription_ids", "strict"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


CustomerMismatchSeverity12 = Union[
    Literal[
        "error",
        "warning",
    ],
    UnrecognizedStr,
]
r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""


class CustomerMismatchOtherCustomerTypedDict(TypedDict):
    id: str
    name: Nullable[str]
    email: Nullable[str]


class CustomerMismatchOtherCustomer(BaseModel):
    id: str

    name: Nullable[str]

    email: Nullable[str]

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                m[k] = val

        return m


class CustomerMismatch12TypedDict(TypedDict):
    stripe_customer_id: str
    other_customer_ids: List[str]
    type: Literal["shared_stripe_customer"]
    message: NotRequired[str]
    r"""Human-readable one-line description of the mismatch."""
    severity: NotRequired[CustomerMismatchSeverity12]
    r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""
    other_customers: NotRequired[List[CustomerMismatchOtherCustomerTypedDict]]


class CustomerMismatch12(BaseModel):
    stripe_customer_id: str

    other_customer_ids: List[str]

    type: Annotated[
        Annotated[
            Literal["shared_stripe_customer"],
            AfterValidator(validate_const("shared_stripe_customer")),
        ],
        pydantic.Field(alias="type"),
    ] = "shared_stripe_customer"

    message: Optional[str] = None
    r"""Human-readable one-line description of the mismatch."""

    severity: Optional[CustomerMismatchSeverity12] = None
    r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""

    other_customers: Optional[List[CustomerMismatchOtherCustomer]] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["message", "severity", "other_customers"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


CustomerMismatchSeverity11 = Union[
    Literal[
        "error",
        "warning",
    ],
    UnrecognizedStr,
]
r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""


class CustomerMismatch11TypedDict(TypedDict):
    error: str
    type: Literal["expected_state_error"]
    message: NotRequired[str]
    r"""Human-readable one-line description of the mismatch."""
    severity: NotRequired[CustomerMismatchSeverity11]
    r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""


class CustomerMismatch11(BaseModel):
    error: str

    type: Annotated[
        Annotated[
            Literal["expected_state_error"],
            AfterValidator(validate_const("expected_state_error")),
        ],
        pydantic.Field(alias="type"),
    ] = "expected_state_error"

    message: Optional[str] = None
    r"""Human-readable one-line description of the mismatch."""

    severity: Optional[CustomerMismatchSeverity11] = None
    r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["message", "severity"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


CustomerMismatchSeverity10 = Union[
    Literal[
        "error",
        "warning",
    ],
    UnrecognizedStr,
]
r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""


class CustomerMismatch10TypedDict(TypedDict):
    type: Literal["stale_subscription_link"]
    message: NotRequired[str]
    r"""Human-readable one-line description of the mismatch."""
    severity: NotRequired[CustomerMismatchSeverity10]
    r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""


class CustomerMismatch10(BaseModel):
    type: Annotated[
        Annotated[
            Literal["stale_subscription_link"],
            AfterValidator(validate_const("stale_subscription_link")),
        ],
        pydantic.Field(alias="type"),
    ] = "stale_subscription_link"

    message: Optional[str] = None
    r"""Human-readable one-line description of the mismatch."""

    severity: Optional[CustomerMismatchSeverity10] = None
    r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["message", "severity"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


CustomerMismatchSeverity9 = Union[
    Literal[
        "error",
        "warning",
    ],
    UnrecognizedStr,
]
r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""


class CustomerMismatch9TypedDict(TypedDict):
    ending_subscription_id: str
    type: Literal["plan_on_ending_subscription"]
    message: NotRequired[str]
    r"""Human-readable one-line description of the mismatch."""
    severity: NotRequired[CustomerMismatchSeverity9]
    r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""


class CustomerMismatch9(BaseModel):
    ending_subscription_id: str

    type: Annotated[
        Annotated[
            Literal["plan_on_ending_subscription"],
            AfterValidator(validate_const("plan_on_ending_subscription")),
        ],
        pydantic.Field(alias="type"),
    ] = "plan_on_ending_subscription"

    message: Optional[str] = None
    r"""Human-readable one-line description of the mismatch."""

    severity: Optional[CustomerMismatchSeverity9] = None
    r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["message", "severity"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


CustomerMismatchSeverity8 = Union[
    Literal[
        "error",
        "warning",
    ],
    UnrecognizedStr,
]
r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""


class CustomerMismatch8TypedDict(TypedDict):
    type: Literal["stripe_sub_not_in_autumn"]
    message: NotRequired[str]
    r"""Human-readable one-line description of the mismatch."""
    severity: NotRequired[CustomerMismatchSeverity8]
    r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""


class CustomerMismatch8(BaseModel):
    type: Annotated[
        Annotated[
            Literal["stripe_sub_not_in_autumn"],
            AfterValidator(validate_const("stripe_sub_not_in_autumn")),
        ],
        pydantic.Field(alias="type"),
    ] = "stripe_sub_not_in_autumn"

    message: Optional[str] = None
    r"""Human-readable one-line description of the mismatch."""

    severity: Optional[CustomerMismatchSeverity8] = None
    r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["message", "severity"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


CustomerMismatchSeverity7 = Union[
    Literal[
        "error",
        "warning",
    ],
    UnrecognizedStr,
]
r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""


class CustomerMismatch7TypedDict(TypedDict):
    missing_reward_ids: List[str]
    unexpected_reward_ids: List[str]
    type: Literal["reward_mismatch"]
    message: NotRequired[str]
    r"""Human-readable one-line description of the mismatch."""
    severity: NotRequired[CustomerMismatchSeverity7]
    r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""


class CustomerMismatch7(BaseModel):
    missing_reward_ids: List[str]

    unexpected_reward_ids: List[str]

    type: Annotated[
        Annotated[
            Literal["reward_mismatch"],
            AfterValidator(validate_const("reward_mismatch")),
        ],
        pydantic.Field(alias="type"),
    ] = "reward_mismatch"

    message: Optional[str] = None
    r"""Human-readable one-line description of the mismatch."""

    severity: Optional[CustomerMismatchSeverity7] = None
    r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["message", "severity"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


CustomerMismatchSeverity6 = Union[
    Literal[
        "error",
        "warning",
    ],
    UnrecognizedStr,
]
r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""


class CustomerMismatch6TypedDict(TypedDict):
    expected_canceling: bool
    actual_canceling: bool
    type: Literal["cancel_state_mismatch"]
    message: NotRequired[str]
    r"""Human-readable one-line description of the mismatch."""
    severity: NotRequired[CustomerMismatchSeverity6]
    r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""


class CustomerMismatch6(BaseModel):
    expected_canceling: bool

    actual_canceling: bool

    type: Annotated[
        Annotated[
            Literal["cancel_state_mismatch"],
            AfterValidator(validate_const("cancel_state_mismatch")),
        ],
        pydantic.Field(alias="type"),
    ] = "cancel_state_mismatch"

    message: Optional[str] = None
    r"""Human-readable one-line description of the mismatch."""

    severity: Optional[CustomerMismatchSeverity6] = None
    r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["message", "severity"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


CustomerMismatchSeverity5 = Union[
    Literal[
        "error",
        "warning",
    ],
    UnrecognizedStr,
]
r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""


CustomerMismatchReason3 = Union[
    Literal[
        "missing_schedule",
        "unexpected_schedule",
        "phase_count_mismatch",
        "phase_start_mismatch",
        "billing_cycle_anchor_mismatch",
    ],
    UnrecognizedStr,
]


class CustomerMismatch5TypedDict(TypedDict):
    reason: CustomerMismatchReason3
    type: Literal["schedule_mismatch"]
    message: NotRequired[str]
    r"""Human-readable one-line description of the mismatch."""
    severity: NotRequired[CustomerMismatchSeverity5]
    r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""
    expected_phase_count: NotRequired[float]
    actual_phase_count: NotRequired[float]
    actual_phase_starts_at: NotRequired[List[float]]
    phase_starts_at: NotRequired[float]


class CustomerMismatch5(BaseModel):
    reason: CustomerMismatchReason3

    type: Annotated[
        Annotated[
            Literal["schedule_mismatch"],
            AfterValidator(validate_const("schedule_mismatch")),
        ],
        pydantic.Field(alias="type"),
    ] = "schedule_mismatch"

    message: Optional[str] = None
    r"""Human-readable one-line description of the mismatch."""

    severity: Optional[CustomerMismatchSeverity5] = None
    r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""

    expected_phase_count: Optional[float] = None

    actual_phase_count: Optional[float] = None

    actual_phase_starts_at: Optional[List[float]] = None

    phase_starts_at: Optional[float] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "message",
                "severity",
                "expected_phase_count",
                "actual_phase_count",
                "actual_phase_starts_at",
                "phase_starts_at",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


CustomerMismatchSeverity4 = Union[
    Literal[
        "error",
        "warning",
    ],
    UnrecognizedStr,
]
r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""


class CustomerMismatch4TypedDict(TypedDict):
    feature_id: str
    expected_unit_amount: str
    actual_unit_amount: str
    type: Literal["prepaid_price_mismatch"]
    message: NotRequired[str]
    r"""Human-readable one-line description of the mismatch."""
    severity: NotRequired[CustomerMismatchSeverity4]
    r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""
    actual_price_id: NotRequired[str]
    r"""The Stripe price of the item this mismatch is about."""
    phase_starts_at: NotRequired[float]


class CustomerMismatch4(BaseModel):
    feature_id: str

    expected_unit_amount: str

    actual_unit_amount: str

    type: Annotated[
        Annotated[
            Literal["prepaid_price_mismatch"],
            AfterValidator(validate_const("prepaid_price_mismatch")),
        ],
        pydantic.Field(alias="type"),
    ] = "prepaid_price_mismatch"

    message: Optional[str] = None
    r"""Human-readable one-line description of the mismatch."""

    severity: Optional[CustomerMismatchSeverity4] = None
    r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""

    actual_price_id: Optional[str] = None
    r"""The Stripe price of the item this mismatch is about."""

    phase_starts_at: Optional[float] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            ["message", "severity", "actual_price_id", "phase_starts_at"]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


CustomerMismatchSeverity3 = Union[
    Literal[
        "error",
        "warning",
    ],
    UnrecognizedStr,
]
r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""


class CustomerMismatch3TypedDict(TypedDict):
    feature_id: str
    expected_quantity: float
    actual_quantity: float
    type: Literal["prepaid_quantity_mismatch"]
    message: NotRequired[str]
    r"""Human-readable one-line description of the mismatch."""
    severity: NotRequired[CustomerMismatchSeverity3]
    r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""
    actual_price_id: NotRequired[str]
    r"""The Stripe price of the item this mismatch is about."""
    plan_id: NotRequired[str]
    r"""The Autumn plan this mismatch belongs to, when resolvable."""
    phase_starts_at: NotRequired[float]


class CustomerMismatch3(BaseModel):
    feature_id: str

    expected_quantity: float

    actual_quantity: float

    type: Annotated[
        Annotated[
            Literal["prepaid_quantity_mismatch"],
            AfterValidator(validate_const("prepaid_quantity_mismatch")),
        ],
        pydantic.Field(alias="type"),
    ] = "prepaid_quantity_mismatch"

    message: Optional[str] = None
    r"""Human-readable one-line description of the mismatch."""

    severity: Optional[CustomerMismatchSeverity3] = None
    r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""

    actual_price_id: Optional[str] = None
    r"""The Stripe price of the item this mismatch is about."""

    plan_id: Optional[str] = None
    r"""The Autumn plan this mismatch belongs to, when resolvable."""

    phase_starts_at: Optional[float] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            ["message", "severity", "actual_price_id", "plan_id", "phase_starts_at"]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


CustomerMismatchSeverity2 = Union[
    Literal[
        "error",
        "warning",
    ],
    UnrecognizedStr,
]
r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""


CustomerMismatchReason2 = Union[
    Literal[
        "missing",
        "unexpected",
        "quantity_mismatch",
        "price_mismatch",
    ],
    UnrecognizedStr,
]


CustomerMismatchPriceType = Union[
    Literal[
        "usage",
        "prepaid",
        "allocated",
        "fixed",
    ],
    UnrecognizedStr,
]


class CustomerMismatch2TypedDict(TypedDict):
    reason: CustomerMismatchReason2
    type: Literal["item_mismatch"]
    message: NotRequired[str]
    r"""Human-readable one-line description of the mismatch."""
    severity: NotRequired[CustomerMismatchSeverity2]
    r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""
    expected_price_id: NotRequired[str]
    actual_price_id: NotRequired[str]
    price_type: NotRequired[CustomerMismatchPriceType]
    feature_id: NotRequired[str]
    expected_quantity: NotRequired[float]
    actual_quantity: NotRequired[float]
    plan_name: NotRequired[str]
    plan_id: NotRequired[str]
    r"""The Autumn plan this mismatch belongs to, when resolvable."""
    price_amount: NotRequired[float]
    price_interval: NotRequired[str]
    price_interval_count: NotRequired[float]
    phase_starts_at: NotRequired[float]
    r"""Present when the mismatch is inside a future scheduled phase rather than the current subscription."""


class CustomerMismatch2(BaseModel):
    reason: CustomerMismatchReason2

    type: Annotated[
        Annotated[
            Literal["item_mismatch"], AfterValidator(validate_const("item_mismatch"))
        ],
        pydantic.Field(alias="type"),
    ] = "item_mismatch"

    message: Optional[str] = None
    r"""Human-readable one-line description of the mismatch."""

    severity: Optional[CustomerMismatchSeverity2] = None
    r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""

    expected_price_id: Optional[str] = None

    actual_price_id: Optional[str] = None

    price_type: Optional[CustomerMismatchPriceType] = None

    feature_id: Optional[str] = None

    expected_quantity: Optional[float] = None

    actual_quantity: Optional[float] = None

    plan_name: Optional[str] = None

    plan_id: Optional[str] = None
    r"""The Autumn plan this mismatch belongs to, when resolvable."""

    price_amount: Optional[float] = None

    price_interval: Optional[str] = None

    price_interval_count: Optional[float] = None

    phase_starts_at: Optional[float] = None
    r"""Present when the mismatch is inside a future scheduled phase rather than the current subscription."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "message",
                "severity",
                "expected_price_id",
                "actual_price_id",
                "price_type",
                "feature_id",
                "expected_quantity",
                "actual_quantity",
                "plan_name",
                "plan_id",
                "price_amount",
                "price_interval",
                "price_interval_count",
                "phase_starts_at",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


CustomerMismatchSeverity1 = Union[
    Literal[
        "error",
        "warning",
    ],
    UnrecognizedStr,
]
r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""


CustomerMismatchReason1 = Union[
    Literal[
        "missing",
        "unexpected",
        "amount_mismatch",
    ],
    UnrecognizedStr,
]


class CustomerMismatch1TypedDict(TypedDict):
    reason: CustomerMismatchReason1
    type: Literal["base_price_mismatch"]
    message: NotRequired[str]
    r"""Human-readable one-line description of the mismatch."""
    severity: NotRequired[CustomerMismatchSeverity1]
    r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""
    expected_price_id: NotRequired[str]
    actual_price_id: NotRequired[str]
    expected_amount: NotRequired[str]
    actual_amount: NotRequired[str]
    plan_name: NotRequired[str]
    plan_id: NotRequired[str]
    r"""The Autumn plan this mismatch belongs to, when resolvable."""
    price_amount: NotRequired[float]
    price_interval: NotRequired[str]
    price_interval_count: NotRequired[float]
    expected_quantity: NotRequired[float]
    phase_starts_at: NotRequired[float]


class CustomerMismatch1(BaseModel):
    reason: CustomerMismatchReason1

    type: Annotated[
        Annotated[
            Literal["base_price_mismatch"],
            AfterValidator(validate_const("base_price_mismatch")),
        ],
        pydantic.Field(alias="type"),
    ] = "base_price_mismatch"

    message: Optional[str] = None
    r"""Human-readable one-line description of the mismatch."""

    severity: Optional[CustomerMismatchSeverity1] = None
    r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""

    expected_price_id: Optional[str] = None

    actual_price_id: Optional[str] = None

    expected_amount: Optional[str] = None

    actual_amount: Optional[str] = None

    plan_name: Optional[str] = None

    plan_id: Optional[str] = None
    r"""The Autumn plan this mismatch belongs to, when resolvable."""

    price_amount: Optional[float] = None

    price_interval: Optional[str] = None

    price_interval_count: Optional[float] = None

    expected_quantity: Optional[float] = None

    phase_starts_at: Optional[float] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "message",
                "severity",
                "expected_price_id",
                "actual_price_id",
                "expected_amount",
                "actual_amount",
                "plan_name",
                "plan_id",
                "price_amount",
                "price_interval",
                "price_interval_count",
                "expected_quantity",
                "phase_starts_at",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


CustomerMismatchUnionTypedDict = TypeAliasType(
    "CustomerMismatchUnionTypedDict",
    Union[
        CustomerMismatch8TypedDict,
        CustomerMismatch10TypedDict,
        CustomerMismatch9TypedDict,
        CustomerMismatch11TypedDict,
        CustomerMismatch6TypedDict,
        CustomerMismatch7TypedDict,
        CustomerMismatch12TypedDict,
        CustomerMismatch4TypedDict,
        CustomerMismatch5TypedDict,
        CustomerMismatch3TypedDict,
        CustomerMismatch1TypedDict,
        CustomerMismatch2TypedDict,
    ],
)


class UnknownCustomerMismatchUnion(BaseModel):
    r"""A CustomerMismatchUnion variant the SDK doesn't recognize. Preserves the raw payload."""

    type: Literal["UNKNOWN"] = "UNKNOWN"
    raw: Any
    is_unknown: Literal[True] = True

    model_config = ConfigDict(frozen=True)


_CUSTOMER_MISMATCH_UNION_VARIANTS: dict[str, Any] = {
    "base_price_mismatch": CustomerMismatch1,
    "item_mismatch": CustomerMismatch2,
    "prepaid_quantity_mismatch": CustomerMismatch3,
    "prepaid_price_mismatch": CustomerMismatch4,
    "schedule_mismatch": CustomerMismatch5,
    "cancel_state_mismatch": CustomerMismatch6,
    "reward_mismatch": CustomerMismatch7,
    "stripe_sub_not_in_autumn": CustomerMismatch8,
    "plan_on_ending_subscription": CustomerMismatch9,
    "stale_subscription_link": CustomerMismatch10,
    "expected_state_error": CustomerMismatch11,
    "shared_stripe_customer": CustomerMismatch12,
}


CustomerMismatchUnion = Annotated[
    Union[
        CustomerMismatch1,
        CustomerMismatch2,
        CustomerMismatch3,
        CustomerMismatch4,
        CustomerMismatch5,
        CustomerMismatch6,
        CustomerMismatch7,
        CustomerMismatch8,
        CustomerMismatch9,
        CustomerMismatch10,
        CustomerMismatch11,
        CustomerMismatch12,
        UnknownCustomerMismatchUnion,
    ],
    BeforeValidator(
        partial(
            parse_open_union,
            disc_key="type",
            variants=_CUSTOMER_MISMATCH_UNION_VARIANTS,
            unknown_cls=UnknownCustomerMismatchUnion,
            union_name="CustomerMismatchUnion",
        )
    ),
]


VerifyStatus = Union[
    Literal[
        "correct",
        "mismatched",
    ],
    UnrecognizedStr,
]


MismatchSeverity12 = Union[
    Literal[
        "error",
        "warning",
    ],
    UnrecognizedStr,
]
r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""


class MismatchOtherCustomerTypedDict(TypedDict):
    id: str
    name: Nullable[str]
    email: Nullable[str]


class MismatchOtherCustomer(BaseModel):
    id: str

    name: Nullable[str]

    email: Nullable[str]

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                m[k] = val

        return m


class Mismatch12TypedDict(TypedDict):
    stripe_customer_id: str
    other_customer_ids: List[str]
    type: Literal["shared_stripe_customer"]
    message: NotRequired[str]
    r"""Human-readable one-line description of the mismatch."""
    severity: NotRequired[MismatchSeverity12]
    r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""
    other_customers: NotRequired[List[MismatchOtherCustomerTypedDict]]


class Mismatch12(BaseModel):
    stripe_customer_id: str

    other_customer_ids: List[str]

    type: Annotated[
        Annotated[
            Literal["shared_stripe_customer"],
            AfterValidator(validate_const("shared_stripe_customer")),
        ],
        pydantic.Field(alias="type"),
    ] = "shared_stripe_customer"

    message: Optional[str] = None
    r"""Human-readable one-line description of the mismatch."""

    severity: Optional[MismatchSeverity12] = None
    r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""

    other_customers: Optional[List[MismatchOtherCustomer]] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["message", "severity", "other_customers"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


MismatchSeverity11 = Union[
    Literal[
        "error",
        "warning",
    ],
    UnrecognizedStr,
]
r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""


class Mismatch11TypedDict(TypedDict):
    error: str
    type: Literal["expected_state_error"]
    message: NotRequired[str]
    r"""Human-readable one-line description of the mismatch."""
    severity: NotRequired[MismatchSeverity11]
    r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""


class Mismatch11(BaseModel):
    error: str

    type: Annotated[
        Annotated[
            Literal["expected_state_error"],
            AfterValidator(validate_const("expected_state_error")),
        ],
        pydantic.Field(alias="type"),
    ] = "expected_state_error"

    message: Optional[str] = None
    r"""Human-readable one-line description of the mismatch."""

    severity: Optional[MismatchSeverity11] = None
    r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["message", "severity"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


MismatchSeverity10 = Union[
    Literal[
        "error",
        "warning",
    ],
    UnrecognizedStr,
]
r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""


class Mismatch10TypedDict(TypedDict):
    type: Literal["stale_subscription_link"]
    message: NotRequired[str]
    r"""Human-readable one-line description of the mismatch."""
    severity: NotRequired[MismatchSeverity10]
    r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""


class Mismatch10(BaseModel):
    type: Annotated[
        Annotated[
            Literal["stale_subscription_link"],
            AfterValidator(validate_const("stale_subscription_link")),
        ],
        pydantic.Field(alias="type"),
    ] = "stale_subscription_link"

    message: Optional[str] = None
    r"""Human-readable one-line description of the mismatch."""

    severity: Optional[MismatchSeverity10] = None
    r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["message", "severity"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


MismatchSeverity9 = Union[
    Literal[
        "error",
        "warning",
    ],
    UnrecognizedStr,
]
r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""


class Mismatch9TypedDict(TypedDict):
    ending_subscription_id: str
    type: Literal["plan_on_ending_subscription"]
    message: NotRequired[str]
    r"""Human-readable one-line description of the mismatch."""
    severity: NotRequired[MismatchSeverity9]
    r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""


class Mismatch9(BaseModel):
    ending_subscription_id: str

    type: Annotated[
        Annotated[
            Literal["plan_on_ending_subscription"],
            AfterValidator(validate_const("plan_on_ending_subscription")),
        ],
        pydantic.Field(alias="type"),
    ] = "plan_on_ending_subscription"

    message: Optional[str] = None
    r"""Human-readable one-line description of the mismatch."""

    severity: Optional[MismatchSeverity9] = None
    r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["message", "severity"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


MismatchSeverity8 = Union[
    Literal[
        "error",
        "warning",
    ],
    UnrecognizedStr,
]
r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""


class Mismatch8TypedDict(TypedDict):
    type: Literal["stripe_sub_not_in_autumn"]
    message: NotRequired[str]
    r"""Human-readable one-line description of the mismatch."""
    severity: NotRequired[MismatchSeverity8]
    r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""


class Mismatch8(BaseModel):
    type: Annotated[
        Annotated[
            Literal["stripe_sub_not_in_autumn"],
            AfterValidator(validate_const("stripe_sub_not_in_autumn")),
        ],
        pydantic.Field(alias="type"),
    ] = "stripe_sub_not_in_autumn"

    message: Optional[str] = None
    r"""Human-readable one-line description of the mismatch."""

    severity: Optional[MismatchSeverity8] = None
    r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["message", "severity"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


MismatchSeverity7 = Union[
    Literal[
        "error",
        "warning",
    ],
    UnrecognizedStr,
]
r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""


class Mismatch7TypedDict(TypedDict):
    missing_reward_ids: List[str]
    unexpected_reward_ids: List[str]
    type: Literal["reward_mismatch"]
    message: NotRequired[str]
    r"""Human-readable one-line description of the mismatch."""
    severity: NotRequired[MismatchSeverity7]
    r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""


class Mismatch7(BaseModel):
    missing_reward_ids: List[str]

    unexpected_reward_ids: List[str]

    type: Annotated[
        Annotated[
            Literal["reward_mismatch"],
            AfterValidator(validate_const("reward_mismatch")),
        ],
        pydantic.Field(alias="type"),
    ] = "reward_mismatch"

    message: Optional[str] = None
    r"""Human-readable one-line description of the mismatch."""

    severity: Optional[MismatchSeverity7] = None
    r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["message", "severity"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


MismatchSeverity6 = Union[
    Literal[
        "error",
        "warning",
    ],
    UnrecognizedStr,
]
r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""


class Mismatch6TypedDict(TypedDict):
    expected_canceling: bool
    actual_canceling: bool
    type: Literal["cancel_state_mismatch"]
    message: NotRequired[str]
    r"""Human-readable one-line description of the mismatch."""
    severity: NotRequired[MismatchSeverity6]
    r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""


class Mismatch6(BaseModel):
    expected_canceling: bool

    actual_canceling: bool

    type: Annotated[
        Annotated[
            Literal["cancel_state_mismatch"],
            AfterValidator(validate_const("cancel_state_mismatch")),
        ],
        pydantic.Field(alias="type"),
    ] = "cancel_state_mismatch"

    message: Optional[str] = None
    r"""Human-readable one-line description of the mismatch."""

    severity: Optional[MismatchSeverity6] = None
    r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["message", "severity"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


MismatchSeverity5 = Union[
    Literal[
        "error",
        "warning",
    ],
    UnrecognizedStr,
]
r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""


MismatchReason3 = Union[
    Literal[
        "missing_schedule",
        "unexpected_schedule",
        "phase_count_mismatch",
        "phase_start_mismatch",
        "billing_cycle_anchor_mismatch",
    ],
    UnrecognizedStr,
]


class Mismatch5TypedDict(TypedDict):
    reason: MismatchReason3
    type: Literal["schedule_mismatch"]
    message: NotRequired[str]
    r"""Human-readable one-line description of the mismatch."""
    severity: NotRequired[MismatchSeverity5]
    r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""
    expected_phase_count: NotRequired[float]
    actual_phase_count: NotRequired[float]
    actual_phase_starts_at: NotRequired[List[float]]
    phase_starts_at: NotRequired[float]


class Mismatch5(BaseModel):
    reason: MismatchReason3

    type: Annotated[
        Annotated[
            Literal["schedule_mismatch"],
            AfterValidator(validate_const("schedule_mismatch")),
        ],
        pydantic.Field(alias="type"),
    ] = "schedule_mismatch"

    message: Optional[str] = None
    r"""Human-readable one-line description of the mismatch."""

    severity: Optional[MismatchSeverity5] = None
    r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""

    expected_phase_count: Optional[float] = None

    actual_phase_count: Optional[float] = None

    actual_phase_starts_at: Optional[List[float]] = None

    phase_starts_at: Optional[float] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "message",
                "severity",
                "expected_phase_count",
                "actual_phase_count",
                "actual_phase_starts_at",
                "phase_starts_at",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


MismatchSeverity4 = Union[
    Literal[
        "error",
        "warning",
    ],
    UnrecognizedStr,
]
r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""


class Mismatch4TypedDict(TypedDict):
    feature_id: str
    expected_unit_amount: str
    actual_unit_amount: str
    type: Literal["prepaid_price_mismatch"]
    message: NotRequired[str]
    r"""Human-readable one-line description of the mismatch."""
    severity: NotRequired[MismatchSeverity4]
    r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""
    actual_price_id: NotRequired[str]
    r"""The Stripe price of the item this mismatch is about."""
    phase_starts_at: NotRequired[float]


class Mismatch4(BaseModel):
    feature_id: str

    expected_unit_amount: str

    actual_unit_amount: str

    type: Annotated[
        Annotated[
            Literal["prepaid_price_mismatch"],
            AfterValidator(validate_const("prepaid_price_mismatch")),
        ],
        pydantic.Field(alias="type"),
    ] = "prepaid_price_mismatch"

    message: Optional[str] = None
    r"""Human-readable one-line description of the mismatch."""

    severity: Optional[MismatchSeverity4] = None
    r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""

    actual_price_id: Optional[str] = None
    r"""The Stripe price of the item this mismatch is about."""

    phase_starts_at: Optional[float] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            ["message", "severity", "actual_price_id", "phase_starts_at"]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


MismatchSeverity3 = Union[
    Literal[
        "error",
        "warning",
    ],
    UnrecognizedStr,
]
r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""


class Mismatch3TypedDict(TypedDict):
    feature_id: str
    expected_quantity: float
    actual_quantity: float
    type: Literal["prepaid_quantity_mismatch"]
    message: NotRequired[str]
    r"""Human-readable one-line description of the mismatch."""
    severity: NotRequired[MismatchSeverity3]
    r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""
    actual_price_id: NotRequired[str]
    r"""The Stripe price of the item this mismatch is about."""
    plan_id: NotRequired[str]
    r"""The Autumn plan this mismatch belongs to, when resolvable."""
    phase_starts_at: NotRequired[float]


class Mismatch3(BaseModel):
    feature_id: str

    expected_quantity: float

    actual_quantity: float

    type: Annotated[
        Annotated[
            Literal["prepaid_quantity_mismatch"],
            AfterValidator(validate_const("prepaid_quantity_mismatch")),
        ],
        pydantic.Field(alias="type"),
    ] = "prepaid_quantity_mismatch"

    message: Optional[str] = None
    r"""Human-readable one-line description of the mismatch."""

    severity: Optional[MismatchSeverity3] = None
    r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""

    actual_price_id: Optional[str] = None
    r"""The Stripe price of the item this mismatch is about."""

    plan_id: Optional[str] = None
    r"""The Autumn plan this mismatch belongs to, when resolvable."""

    phase_starts_at: Optional[float] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            ["message", "severity", "actual_price_id", "plan_id", "phase_starts_at"]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


MismatchSeverity2 = Union[
    Literal[
        "error",
        "warning",
    ],
    UnrecognizedStr,
]
r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""


MismatchReason2 = Union[
    Literal[
        "missing",
        "unexpected",
        "quantity_mismatch",
        "price_mismatch",
    ],
    UnrecognizedStr,
]


MismatchPriceType = Union[
    Literal[
        "usage",
        "prepaid",
        "allocated",
        "fixed",
    ],
    UnrecognizedStr,
]


class Mismatch2TypedDict(TypedDict):
    reason: MismatchReason2
    type: Literal["item_mismatch"]
    message: NotRequired[str]
    r"""Human-readable one-line description of the mismatch."""
    severity: NotRequired[MismatchSeverity2]
    r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""
    expected_price_id: NotRequired[str]
    actual_price_id: NotRequired[str]
    price_type: NotRequired[MismatchPriceType]
    feature_id: NotRequired[str]
    expected_quantity: NotRequired[float]
    actual_quantity: NotRequired[float]
    plan_name: NotRequired[str]
    plan_id: NotRequired[str]
    r"""The Autumn plan this mismatch belongs to, when resolvable."""
    price_amount: NotRequired[float]
    price_interval: NotRequired[str]
    price_interval_count: NotRequired[float]
    phase_starts_at: NotRequired[float]
    r"""Present when the mismatch is inside a future scheduled phase rather than the current subscription."""


class Mismatch2(BaseModel):
    reason: MismatchReason2

    type: Annotated[
        Annotated[
            Literal["item_mismatch"], AfterValidator(validate_const("item_mismatch"))
        ],
        pydantic.Field(alias="type"),
    ] = "item_mismatch"

    message: Optional[str] = None
    r"""Human-readable one-line description of the mismatch."""

    severity: Optional[MismatchSeverity2] = None
    r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""

    expected_price_id: Optional[str] = None

    actual_price_id: Optional[str] = None

    price_type: Optional[MismatchPriceType] = None

    feature_id: Optional[str] = None

    expected_quantity: Optional[float] = None

    actual_quantity: Optional[float] = None

    plan_name: Optional[str] = None

    plan_id: Optional[str] = None
    r"""The Autumn plan this mismatch belongs to, when resolvable."""

    price_amount: Optional[float] = None

    price_interval: Optional[str] = None

    price_interval_count: Optional[float] = None

    phase_starts_at: Optional[float] = None
    r"""Present when the mismatch is inside a future scheduled phase rather than the current subscription."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "message",
                "severity",
                "expected_price_id",
                "actual_price_id",
                "price_type",
                "feature_id",
                "expected_quantity",
                "actual_quantity",
                "plan_name",
                "plan_id",
                "price_amount",
                "price_interval",
                "price_interval_count",
                "phase_starts_at",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


MismatchSeverity1 = Union[
    Literal[
        "error",
        "warning",
    ],
    UnrecognizedStr,
]
r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""


MismatchReason1 = Union[
    Literal[
        "missing",
        "unexpected",
        "amount_mismatch",
    ],
    UnrecognizedStr,
]


class Mismatch1TypedDict(TypedDict):
    reason: MismatchReason1
    type: Literal["base_price_mismatch"]
    message: NotRequired[str]
    r"""Human-readable one-line description of the mismatch."""
    severity: NotRequired[MismatchSeverity1]
    r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""
    expected_price_id: NotRequired[str]
    actual_price_id: NotRequired[str]
    expected_amount: NotRequired[str]
    actual_amount: NotRequired[str]
    plan_name: NotRequired[str]
    plan_id: NotRequired[str]
    r"""The Autumn plan this mismatch belongs to, when resolvable."""
    price_amount: NotRequired[float]
    price_interval: NotRequired[str]
    price_interval_count: NotRequired[float]
    expected_quantity: NotRequired[float]
    phase_starts_at: NotRequired[float]


class Mismatch1(BaseModel):
    reason: MismatchReason1

    type: Annotated[
        Annotated[
            Literal["base_price_mismatch"],
            AfterValidator(validate_const("base_price_mismatch")),
        ],
        pydantic.Field(alias="type"),
    ] = "base_price_mismatch"

    message: Optional[str] = None
    r"""Human-readable one-line description of the mismatch."""

    severity: Optional[MismatchSeverity1] = None
    r"""How serious the mismatch is — warnings (e.g. a Stripe-only subscription) can be deliberate."""

    expected_price_id: Optional[str] = None

    actual_price_id: Optional[str] = None

    expected_amount: Optional[str] = None

    actual_amount: Optional[str] = None

    plan_name: Optional[str] = None

    plan_id: Optional[str] = None
    r"""The Autumn plan this mismatch belongs to, when resolvable."""

    price_amount: Optional[float] = None

    price_interval: Optional[str] = None

    price_interval_count: Optional[float] = None

    expected_quantity: Optional[float] = None

    phase_starts_at: Optional[float] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "message",
                "severity",
                "expected_price_id",
                "actual_price_id",
                "expected_amount",
                "actual_amount",
                "plan_name",
                "plan_id",
                "price_amount",
                "price_interval",
                "price_interval_count",
                "expected_quantity",
                "phase_starts_at",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


MismatchUnionTypedDict = TypeAliasType(
    "MismatchUnionTypedDict",
    Union[
        Mismatch8TypedDict,
        Mismatch10TypedDict,
        Mismatch9TypedDict,
        Mismatch11TypedDict,
        Mismatch6TypedDict,
        Mismatch7TypedDict,
        Mismatch12TypedDict,
        Mismatch4TypedDict,
        Mismatch5TypedDict,
        Mismatch3TypedDict,
        Mismatch1TypedDict,
        Mismatch2TypedDict,
    ],
)


class UnknownMismatchUnion(BaseModel):
    r"""A MismatchUnion variant the SDK doesn't recognize. Preserves the raw payload."""

    type: Literal["UNKNOWN"] = "UNKNOWN"
    raw: Any
    is_unknown: Literal[True] = True

    model_config = ConfigDict(frozen=True)


_MISMATCH_UNION_VARIANTS: dict[str, Any] = {
    "base_price_mismatch": Mismatch1,
    "item_mismatch": Mismatch2,
    "prepaid_quantity_mismatch": Mismatch3,
    "prepaid_price_mismatch": Mismatch4,
    "schedule_mismatch": Mismatch5,
    "cancel_state_mismatch": Mismatch6,
    "reward_mismatch": Mismatch7,
    "stripe_sub_not_in_autumn": Mismatch8,
    "plan_on_ending_subscription": Mismatch9,
    "stale_subscription_link": Mismatch10,
    "expected_state_error": Mismatch11,
    "shared_stripe_customer": Mismatch12,
}


MismatchUnion = Annotated[
    Union[
        Mismatch1,
        Mismatch2,
        Mismatch3,
        Mismatch4,
        Mismatch5,
        Mismatch6,
        Mismatch7,
        Mismatch8,
        Mismatch9,
        Mismatch10,
        Mismatch11,
        Mismatch12,
        UnknownMismatchUnion,
    ],
    BeforeValidator(
        partial(
            parse_open_union,
            disc_key="type",
            variants=_MISMATCH_UNION_VARIANTS,
            unknown_cls=UnknownMismatchUnion,
            union_name="MismatchUnion",
        )
    ),
]


class VerifySubscriptionTypedDict(TypedDict):
    stripe_subscription_id: str
    status: VerifyStatus
    mismatches: List[MismatchUnionTypedDict]


class VerifySubscription(BaseModel):
    stripe_subscription_id: str

    status: VerifyStatus

    mismatches: List[MismatchUnion]


class VerifyResponseTypedDict(TypedDict):
    r"""OK"""

    customer_id: str
    customer_mismatches: List[CustomerMismatchUnionTypedDict]
    subscriptions: List[VerifySubscriptionTypedDict]


class VerifyResponse(BaseModel):
    r"""OK"""

    customer_id: str

    customer_mismatches: List[CustomerMismatchUnion]

    subscriptions: List[VerifySubscription]


try:
    CustomerMismatch12.model_rebuild()
except NameError:
    pass
try:
    CustomerMismatch11.model_rebuild()
except NameError:
    pass
try:
    CustomerMismatch10.model_rebuild()
except NameError:
    pass
try:
    CustomerMismatch9.model_rebuild()
except NameError:
    pass
try:
    CustomerMismatch8.model_rebuild()
except NameError:
    pass
try:
    CustomerMismatch7.model_rebuild()
except NameError:
    pass
try:
    CustomerMismatch6.model_rebuild()
except NameError:
    pass
try:
    CustomerMismatch5.model_rebuild()
except NameError:
    pass
try:
    CustomerMismatch4.model_rebuild()
except NameError:
    pass
try:
    CustomerMismatch3.model_rebuild()
except NameError:
    pass
try:
    CustomerMismatch2.model_rebuild()
except NameError:
    pass
try:
    CustomerMismatch1.model_rebuild()
except NameError:
    pass
try:
    Mismatch12.model_rebuild()
except NameError:
    pass
try:
    Mismatch11.model_rebuild()
except NameError:
    pass
try:
    Mismatch10.model_rebuild()
except NameError:
    pass
try:
    Mismatch9.model_rebuild()
except NameError:
    pass
try:
    Mismatch8.model_rebuild()
except NameError:
    pass
try:
    Mismatch7.model_rebuild()
except NameError:
    pass
try:
    Mismatch6.model_rebuild()
except NameError:
    pass
try:
    Mismatch5.model_rebuild()
except NameError:
    pass
try:
    Mismatch4.model_rebuild()
except NameError:
    pass
try:
    Mismatch3.model_rebuild()
except NameError:
    pass
try:
    Mismatch2.model_rebuild()
except NameError:
    pass
try:
    Mismatch1.model_rebuild()
except NameError:
    pass
