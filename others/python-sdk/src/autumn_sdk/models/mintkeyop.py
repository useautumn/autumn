"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from autumn_sdk.types import BaseModel, Nullable, UNSET_SENTINEL
from autumn_sdk.utils import FieldMetadata, HeaderMetadata
import pydantic
from pydantic import model_serializer
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class MintKeyGlobalsTypedDict(TypedDict):
    x_api_version: NotRequired[str]


class MintKeyGlobals(BaseModel):
    x_api_version: Annotated[
        Optional[str],
        pydantic.Field(alias="x-api-version"),
        FieldMetadata(header=HeaderMetadata(style="simple", explode=False)),
    ] = "2.3.0"

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["x-api-version"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class MintKeyParamsTypedDict(TypedDict):
    customer_id: str
    r"""The customer to mint a token for."""
    indefinite: NotRequired[bool]
    r"""If true, mint a non-expiring access token (no refresh token). Revoke via keys.revoke."""


class MintKeyParams(BaseModel):
    customer_id: str
    r"""The customer to mint a token for."""

    indefinite: Optional[bool] = None
    r"""If true, mint a non-expiring access token (no refresh token). Revoke via keys.revoke."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["indefinite"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class MintKeyResponseTypedDict(TypedDict):
    r"""OK"""

    access_token: str
    r"""Access token (1h, or non-expiring if indefinite), prefixed `am_jwt_`."""
    expires_at: Nullable[float]
    r"""Access-token expiry, ms since epoch. null for indefinite tokens."""
    refresh_token: NotRequired[str]
    r"""Rotating refresh token (24h). Omitted for indefinite tokens."""
    refresh_expires_at: NotRequired[float]
    r"""Refresh-token expiry, ms since epoch. Omitted for indefinite tokens."""


class MintKeyResponse(BaseModel):
    r"""OK"""

    access_token: str
    r"""Access token (1h, or non-expiring if indefinite), prefixed `am_jwt_`."""

    expires_at: Nullable[float]
    r"""Access-token expiry, ms since epoch. null for indefinite tokens."""

    refresh_token: Optional[str] = None
    r"""Rotating refresh token (24h). Omitted for indefinite tokens."""

    refresh_expires_at: Optional[float] = None
    r"""Refresh-token expiry, ms since epoch. Omitted for indefinite tokens."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["refresh_token", "refresh_expires_at"])
        nullable_fields = set(["expires_at"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m
