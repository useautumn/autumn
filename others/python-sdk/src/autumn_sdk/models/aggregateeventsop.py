"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from autumn_sdk.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
    UnrecognizedStr,
)
from autumn_sdk.utils import FieldMetadata, HeaderMetadata, validate_const
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Dict, List, Literal, Optional, Union
from typing_extensions import Annotated, NotRequired, TypeAliasType, TypedDict


class AggregateEventsGlobalsTypedDict(TypedDict):
    x_api_version: NotRequired[str]


class AggregateEventsGlobals(BaseModel):
    x_api_version: Annotated[
        Optional[str],
        pydantic.Field(alias="x-api-version"),
        FieldMetadata(header=HeaderMetadata(style="simple", explode=False)),
    ] = "2.3.0"

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["x-api-version"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


AggregateEventsFeatureIDTypedDict = TypeAliasType(
    "AggregateEventsFeatureIDTypedDict", Union[str, List[str]]
)
r"""Feature ID(s) to aggregate events for"""


AggregateEventsFeatureID = TypeAliasType(
    "AggregateEventsFeatureID", Union[str, List[str]]
)
r"""Feature ID(s) to aggregate events for"""


Range = Literal[
    "24h",
    "7d",
    "30d",
    "90d",
    "last_cycle",
    "1bc",
    "3bc",
]
r"""Time range to aggregate events for. Either range or custom_range must be provided"""


BinSize = Literal[
    "day",
    "hour",
    "week",
    "month",
]
r"""Size of the time bins to aggregate events for. Defaults to hour if range is 24h, otherwise day"""


class AggregateEventsCustomRangeTypedDict(TypedDict):
    r"""Custom time range to aggregate events for. If provided, range must not be provided"""

    start: float
    end: float


class AggregateEventsCustomRange(BaseModel):
    r"""Custom time range to aggregate events for. If provided, range must not be provided"""

    start: float

    end: float


class EventsAggregateParamsTypedDict(TypedDict):
    feature_id: AggregateEventsFeatureIDTypedDict
    r"""Feature ID(s) to aggregate events for"""
    customer_id: NotRequired[str]
    r"""Customer ID to aggregate events for"""
    entity_id: NotRequired[str]
    r"""Entity ID to filter aggregated events for (e.g., per-seat or per-resource limits)"""
    group_by: NotRequired[str]
    r"""Property to group events by (e.g. \"properties.region\"), or \"$customer_id\" / \"$entity_id\" / \"$plan_id\" to group by those columns"""
    range: NotRequired[Range]
    r"""Time range to aggregate events for. Either range or custom_range must be provided"""
    bin_size: NotRequired[BinSize]
    r"""Size of the time bins to aggregate events for. Defaults to hour if range is 24h, otherwise day"""
    custom_range: NotRequired[AggregateEventsCustomRangeTypedDict]
    r"""Custom time range to aggregate events for. If provided, range must not be provided"""
    filter_by: NotRequired[Dict[str, str]]
    r"""Filter events by property values, e.g. {\"model\": \"gpt-4\", \"region\": \"us\"}. Maximum 5 filters."""
    max_groups: NotRequired[int]
    r"""Maximum number of distinct group values to return per time bin when using group_by. Remaining values are bundled into an 'Other' bucket. Defaults to 9"""
    aggregate_on: Literal["deducted"]
    r"""Set to \"deducted\" to additionally return a per-balance breakdown of what each event consumed, under `deductions`. Purely additive: `list` and `total` are unchanged. Requires customer_id."""


class EventsAggregateParams(BaseModel):
    feature_id: AggregateEventsFeatureID
    r"""Feature ID(s) to aggregate events for"""

    customer_id: Optional[str] = None
    r"""Customer ID to aggregate events for"""

    entity_id: Optional[str] = None
    r"""Entity ID to filter aggregated events for (e.g., per-seat or per-resource limits)"""

    group_by: Optional[str] = None
    r"""Property to group events by (e.g. \"properties.region\"), or \"$customer_id\" / \"$entity_id\" / \"$plan_id\" to group by those columns"""

    range: Optional[Range] = None
    r"""Time range to aggregate events for. Either range or custom_range must be provided"""

    bin_size: Optional[BinSize] = "day"
    r"""Size of the time bins to aggregate events for. Defaults to hour if range is 24h, otherwise day"""

    custom_range: Optional[AggregateEventsCustomRange] = None
    r"""Custom time range to aggregate events for. If provided, range must not be provided"""

    filter_by: Optional[Dict[str, str]] = None
    r"""Filter events by property values, e.g. {\"model\": \"gpt-4\", \"region\": \"us\"}. Maximum 5 filters."""

    max_groups: Optional[int] = None
    r"""Maximum number of distinct group values to return per time bin when using group_by. Remaining values are bundled into an 'Other' bucket. Defaults to 9"""

    aggregate_on: Annotated[
        Annotated[
            Optional[Literal["deducted"]], AfterValidator(validate_const("deducted"))
        ],
        pydantic.Field(alias="aggregate_on"),
    ] = "deducted"
    r"""Set to \"deducted\" to additionally return a per-balance breakdown of what each event consumed, under `deductions`. Purely additive: `list` and `total` are unchanged. Requires customer_id."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "customer_id",
                "entity_id",
                "group_by",
                "range",
                "bin_size",
                "custom_range",
                "filter_by",
                "max_groups",
                "aggregate_on",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class AggregateEventsListTypedDict(TypedDict):
    period: float
    r"""Unix timestamp (epoch ms) for this time period"""
    values: Dict[str, float]
    r"""Aggregated values per feature: { [featureId]: number }"""
    grouped_values: NotRequired[Dict[str, Dict[str, float]]]
    r"""Values broken down by group (only present when group_by is used): { [featureId]: { [groupValue]: number } }"""


class AggregateEventsList(BaseModel):
    period: float
    r"""Unix timestamp (epoch ms) for this time period"""

    values: Dict[str, float]
    r"""Aggregated values per feature: { [featureId]: number }"""

    grouped_values: Optional[Dict[str, Dict[str, float]]] = None
    r"""Values broken down by group (only present when group_by is used): { [featureId]: { [groupValue]: number } }"""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["grouped_values"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class TotalTypedDict(TypedDict):
    count: float
    r"""Number of events for this feature"""
    sum: float
    r"""Sum of event values for this feature"""


class Total(BaseModel):
    count: float
    r"""Number of events for this feature"""

    sum: float
    r"""Sum of event values for this feature"""


AggregateEventsFeatureType = Union[
    Literal[
        "metered",
        "credit_system",
    ],
    UnrecognizedStr,
]
r"""credit_system means `deducted` is credits; metered means it is that feature's own amount."""


class AggregateEventsResetTypedDict(TypedDict):
    interval: str
    resets_at: Nullable[float]


class AggregateEventsReset(BaseModel):
    interval: str

    resets_at: Nullable[float]

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                m[k] = val

        return m


class AggregateEventsBalanceTypedDict(TypedDict):
    balance_id: str
    r"""ID of the balance row drawn from (customer_entitlement or rollover)."""
    entity_id: Nullable[str]
    r"""Entity that owns this balance, or null when it is customer-level and shared."""
    plan_id: Nullable[str]
    r"""Plan the balance came with. Null for balances created outside a plan."""
    reset: Nullable[AggregateEventsResetTypedDict]
    r"""Reset config for this balance, captured at deduction time."""
    credit_cost: Nullable[float]
    r"""Multiplier applied converting the tracked feature into this balance. Null when 1:1, or when the query spans sources converting at different rates."""
    deducted: float
    events: float


class AggregateEventsBalance(BaseModel):
    balance_id: str
    r"""ID of the balance row drawn from (customer_entitlement or rollover)."""

    entity_id: Nullable[str]
    r"""Entity that owns this balance, or null when it is customer-level and shared."""

    plan_id: Nullable[str]
    r"""Plan the balance came with. Null for balances created outside a plan."""

    reset: Nullable[AggregateEventsReset]
    r"""Reset config for this balance, captured at deduction time."""

    credit_cost: Nullable[float]
    r"""Multiplier applied converting the tracked feature into this balance. Null when 1:1, or when the query spans sources converting at different rates."""

    deducted: float

    events: float

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                m[k] = val

        return m


class ValuesTypedDict(TypedDict):
    feature_type: AggregateEventsFeatureType
    r"""credit_system means `deducted` is credits; metered means it is that feature's own amount."""
    deducted: float
    events: float
    balances: List[AggregateEventsBalanceTypedDict]


class Values(BaseModel):
    feature_type: AggregateEventsFeatureType
    r"""credit_system means `deducted` is credits; metered means it is that feature's own amount."""

    deducted: float

    events: float

    balances: List[AggregateEventsBalance]


class GroupedValuesTypedDict(TypedDict):
    deducted: float
    credit_cost: NotRequired[Nullable[float]]


class GroupedValues(BaseModel):
    deducted: float

    credit_cost: OptionalNullable[float] = UNSET

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["credit_cost"])
        nullable_fields = set(["credit_cost"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


class AggregateEventsDeductionTypedDict(TypedDict):
    period: float
    r"""Unix timestamp (epoch ms), same basis as `list`."""
    values: Dict[str, ValuesTypedDict]
    r"""Keyed by the feature that OWNS the balance drawn from, not the feature that was tracked."""
    grouped_values: NotRequired[Dict[str, Dict[str, GroupedValuesTypedDict]]]
    r"""Present only when group_by is used. Keyed by balance_id, then by group value — the only way to attribute a shared balance to the entity that spent from it."""


class AggregateEventsDeduction(BaseModel):
    period: float
    r"""Unix timestamp (epoch ms), same basis as `list`."""

    values: Dict[str, Values]
    r"""Keyed by the feature that OWNS the balance drawn from, not the feature that was tracked."""

    grouped_values: Optional[Dict[str, Dict[str, GroupedValues]]] = None
    r"""Present only when group_by is used. Keyed by balance_id, then by group value — the only way to attribute a shared balance to the entity that spent from it."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["grouped_values"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class AggregateEventsResponseTypedDict(TypedDict):
    r"""OK"""

    list: List[AggregateEventsListTypedDict]
    r"""Array of time periods with aggregated values"""
    total: Dict[str, TotalTypedDict]
    r"""Total aggregations per feature. Keys are feature IDs, values contain count and sum."""
    deductions: NotRequired[List[AggregateEventsDeductionTypedDict]]
    r"""Per-balance breakdown of what was consumed. Present only when aggregate_on is \"deducted\"."""


class AggregateEventsResponse(BaseModel):
    r"""OK"""

    list: List[AggregateEventsList]
    r"""Array of time periods with aggregated values"""

    total: Dict[str, Total]
    r"""Total aggregations per feature. Keys are feature IDs, values contain count and sum."""

    deductions: Optional[List[AggregateEventsDeduction]] = None
    r"""Per-balance breakdown of what was consumed. Present only when aggregate_on is \"deducted\"."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["deductions"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    EventsAggregateParams.model_rebuild()
except NameError:
    pass
