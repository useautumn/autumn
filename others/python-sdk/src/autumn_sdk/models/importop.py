"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .customer import Customer, CustomerTypedDict
from autumn_sdk.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
from autumn_sdk.utils import FieldMetadata, HeaderMetadata
import pydantic
from pydantic import model_serializer
from typing import List, Literal, Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class ImportGlobalsTypedDict(TypedDict):
    x_api_version: NotRequired[str]


class ImportGlobals(BaseModel):
    x_api_version: Annotated[
        Optional[str],
        pydantic.Field(alias="x-api-version"),
        FieldMetadata(header=HeaderMetadata(style="simple", explode=False)),
    ] = "2.3.0"

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["x-api-version"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class ImportCustomerDataTypedDict(TypedDict):
    r"""Optional identity fields upserted onto the customer (applied to existing customers too)."""

    name: NotRequired[str]
    r"""Display name for the customer."""
    email: NotRequired[str]
    r"""Email address for the customer."""
    fingerprint: NotRequired[str]
    r"""Anti-fraud fingerprint for the customer."""


class ImportCustomerData(BaseModel):
    r"""Optional identity fields upserted onto the customer (applied to existing customers too)."""

    name: Optional[str] = None
    r"""Display name for the customer."""

    email: Optional[str] = None
    r"""Email address for the customer."""

    fingerprint: Optional[str] = None
    r"""Anti-fraud fingerprint for the customer."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["name", "email", "fingerprint"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


ImportType = Literal[
    "stripe",
    "revenuecat",
]
r"""The processor this identity belongs to."""


class ImportProcessorTypedDict(TypedDict):
    type: ImportType
    r"""The processor this identity belongs to."""
    id: str
    r"""The customer's id in that processor (Stripe customer id, or RevenueCat app_user_id)."""


class ImportProcessor(BaseModel):
    type: ImportType
    r"""The processor this identity belongs to."""

    id: str
    r"""The customer's id in that processor (Stripe customer id, or RevenueCat app_user_id)."""


BillableProcessor = Literal[
    "stripe",
    "revenuecat",
]
r"""The processor that owns this billable (stripe or revenuecat). Omit for plans with no processor, e.g. a free plan."""


class LinkTypedDict(TypedDict):
    r"""Existing processor billing object this billable is adopted from; omit for paid one-offs."""

    subscription_id: NotRequired[str]
    r"""Existing processor subscription id this billable is adopted from."""
    schedule_id: NotRequired[str]
    r"""Existing processor subscription-schedule id this billable is adopted from."""


class Link(BaseModel):
    r"""Existing processor billing object this billable is adopted from; omit for paid one-offs."""

    subscription_id: Optional[str] = None
    r"""Existing processor subscription id this billable is adopted from."""

    schedule_id: Optional[str] = None
    r"""Existing processor subscription-schedule id this billable is adopted from."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["subscription_id", "schedule_id"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


ImportStatus = Literal[
    "active",
    "trialing",
    "past_due",
    "canceled",
    "expired",
]
r"""Set the status of the plan to be flashed. Active if undefined."""


class ImportFeatureQuantityTypedDict(TypedDict):
    feature_id: str
    r"""The prepaid feature being quantified."""
    quantity: float
    r"""Purchased quantity for this prepaid feature."""


class ImportFeatureQuantity(BaseModel):
    feature_id: str
    r"""The prepaid feature being quantified."""

    quantity: float
    r"""Purchased quantity for this prepaid feature."""


ImportInterval = Literal[
    "lifetime",
    "minute",
    "hour",
    "day",
    "week",
    "month",
    "quarter",
    "semi_annual",
    "year",
]


ImportBillingBehavior = Literal[
    "included",
    "prepaid",
    "usage_based",
]
r"""Selects the included vs prepaid vs usage-based (pay-per-use) entitlement line when a feature has several."""


class FilterTypedDict(TypedDict):
    r"""Disambiguates which entitlement line to target when the feature has multiple."""

    interval: NotRequired[Nullable[ImportInterval]]
    r"""Reset interval selecting which entitlement line to target when a feature has several ('lifetime' or null = the non-resetting one-off line)."""
    billing_behavior: NotRequired[ImportBillingBehavior]
    r"""Selects the included vs prepaid vs usage-based (pay-per-use) entitlement line when a feature has several."""


class Filter(BaseModel):
    r"""Disambiguates which entitlement line to target when the feature has multiple."""

    interval: OptionalNullable[ImportInterval] = UNSET
    r"""Reset interval selecting which entitlement line to target when a feature has several ('lifetime' or null = the non-resetting one-off line)."""

    billing_behavior: Optional[ImportBillingBehavior] = None
    r"""Selects the included vs prepaid vs usage-based (pay-per-use) entitlement line when a feature has several."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["interval", "billing_behavior"])
        nullable_fields = set(["interval"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


class ImportBalanceTypedDict(TypedDict):
    feature_id: str
    r"""The feature whose balance is being set."""
    filter_: NotRequired[FilterTypedDict]
    r"""Disambiguates which entitlement line to target when the feature has multiple."""
    usage: NotRequired[float]
    r"""Units already consumed; remaining balance is derived from the plan allowance minus this."""
    balance: NotRequired[float]
    r"""Explicit remaining balance override (mutually exclusive with usage)."""
    next_reset_at: NotRequired[float]
    r"""Unix ms timestamp of this line's next reset."""


class ImportBalance(BaseModel):
    feature_id: str
    r"""The feature whose balance is being set."""

    filter_: Annotated[Optional[Filter], pydantic.Field(alias="filter")] = None
    r"""Disambiguates which entitlement line to target when the feature has multiple."""

    usage: Optional[float] = None
    r"""Units already consumed; remaining balance is derived from the plan allowance minus this."""

    balance: Optional[float] = None
    r"""Explicit remaining balance override (mutually exclusive with usage)."""

    next_reset_at: Optional[float] = None
    r"""Unix ms timestamp of this line's next reset."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["filter", "usage", "balance", "next_reset_at"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class ImportPlanTypedDict(TypedDict):
    r"""The single plan on this billable (provide either plan or phases, not both)."""

    plan_id: str
    r"""The Autumn plan to attach to the customer."""
    version: NotRequired[float]
    r"""Specific plan version to attach; defaults to the latest."""
    status: NotRequired[ImportStatus]
    r"""Set the status of the plan to be flashed. Active if undefined."""
    started_at: NotRequired[float]
    r"""When the plan started (Unix ms). Defaults to the linked subscription's start, else the import time. Set this for one-off purchases to record the real purchase date."""
    quantity: NotRequired[float]
    r"""Seat/unit quantity for the plan."""
    feature_quantities: NotRequired[List[ImportFeatureQuantityTypedDict]]
    r"""Purchased prepaid quantities per feature."""
    balances: NotRequired[List[ImportBalanceTypedDict]]
    r"""Per-feature balances to image onto the plan."""


class ImportPlan(BaseModel):
    r"""The single plan on this billable (provide either plan or phases, not both)."""

    plan_id: str
    r"""The Autumn plan to attach to the customer."""

    version: Optional[float] = None
    r"""Specific plan version to attach; defaults to the latest."""

    status: Optional[ImportStatus] = None
    r"""Set the status of the plan to be flashed. Active if undefined."""

    started_at: Optional[float] = None
    r"""When the plan started (Unix ms). Defaults to the linked subscription's start, else the import time. Set this for one-off purchases to record the real purchase date."""

    quantity: Optional[float] = None
    r"""Seat/unit quantity for the plan."""

    feature_quantities: Optional[List[ImportFeatureQuantity]] = None
    r"""Purchased prepaid quantities per feature."""

    balances: Optional[List[ImportBalance]] = None
    r"""Per-feature balances to image onto the plan."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "version",
                "status",
                "started_at",
                "quantity",
                "feature_quantities",
                "balances",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class BillableTypedDict(TypedDict):
    processor: NotRequired[BillableProcessor]
    r"""The processor that owns this billable (stripe or revenuecat). Omit for plans with no processor, e.g. a free plan."""
    link: NotRequired[LinkTypedDict]
    r"""Existing processor billing object this billable is adopted from; omit for paid one-offs."""
    billing_cycle_anchor: NotRequired[float]
    r"""Unix ms billing anchor shared by co-billed plans on this billable."""
    plan: NotRequired[ImportPlanTypedDict]
    r"""The single plan on this billable (provide either plan or phases, not both)."""


class Billable(BaseModel):
    processor: Optional[BillableProcessor] = None
    r"""The processor that owns this billable (stripe or revenuecat). Omit for plans with no processor, e.g. a free plan."""

    link: Optional[Link] = None
    r"""Existing processor billing object this billable is adopted from; omit for paid one-offs."""

    billing_cycle_anchor: Optional[float] = None
    r"""Unix ms billing anchor shared by co-billed plans on this billable."""

    plan: Optional[ImportPlan] = None
    r"""The single plan on this billable (provide either plan or phases, not both)."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["processor", "link", "billing_cycle_anchor", "plan"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class DfuFlashParamsTypedDict(TypedDict):
    customer_id: str
    r"""Autumn customer to image into."""
    billables: List[BillableTypedDict]
    r"""The billing objects (subscriptions, one-offs) to image, each carrying its plan."""
    customer_data: NotRequired[ImportCustomerDataTypedDict]
    r"""Optional identity fields upserted onto the customer (applied to existing customers too)."""
    processors: NotRequired[List[ImportProcessorTypedDict]]
    r"""The customer's processor identities (e.g. Stripe customer id, RevenueCat app_user_id). Omit for customers with no processor, e.g. those only ever on a free plan."""
    dry_run: NotRequired[bool]
    r"""If true, validate and compute without persisting; returns what would be flashed."""


class DfuFlashParams(BaseModel):
    customer_id: str
    r"""Autumn customer to image into."""

    billables: List[Billable]
    r"""The billing objects (subscriptions, one-offs) to image, each carrying its plan."""

    customer_data: Optional[ImportCustomerData] = None
    r"""Optional identity fields upserted onto the customer (applied to existing customers too)."""

    processors: Optional[List[ImportProcessor]] = None
    r"""The customer's processor identities (e.g. Stripe customer id, RevenueCat app_user_id). Omit for customers with no processor, e.g. those only ever on a free plan."""

    dry_run: Optional[bool] = None
    r"""If true, validate and compute without persisting; returns what would be flashed."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["customer_data", "processors", "dry_run"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class FlashedTypedDict(TypedDict):
    plan_id: str
    r"""The plan that was imaged."""
    processor: str
    r"""The processor that owns the imaged plan."""
    customer_product_id: Nullable[str]
    r"""The created (or existing) customer product id, if any."""
    status: str
    r"""The resulting status of the imaged plan."""
    skipped: bool
    r"""True if an active plan already existed and this one was left untouched."""
    expired: NotRequired[bool]
    r"""True if this was an existing active plan expired because it was absent from the imaged desired state."""
    mismatch: NotRequired[bool]
    r"""True when the imaged state may be wrong — e.g. a resetting plan with no resolvable billing anchor, or a paid recurring plan with no linked subscription for Autumn to manage. The plan is still imaged; see `reason` and fix by supplying started_at or a subscription_id."""
    reason: NotRequired[str]
    r"""Why the plan was skipped, expired, or flagged as a mismatch, when applicable."""


class Flashed(BaseModel):
    plan_id: str
    r"""The plan that was imaged."""

    processor: str
    r"""The processor that owns the imaged plan."""

    customer_product_id: Nullable[str]
    r"""The created (or existing) customer product id, if any."""

    status: str
    r"""The resulting status of the imaged plan."""

    skipped: bool
    r"""True if an active plan already existed and this one was left untouched."""

    expired: Optional[bool] = None
    r"""True if this was an existing active plan expired because it was absent from the imaged desired state."""

    mismatch: Optional[bool] = None
    r"""True when the imaged state may be wrong — e.g. a resetting plan with no resolvable billing anchor, or a paid recurring plan with no linked subscription for Autumn to manage. The plan is still imaged; see `reason` and fix by supplying started_at or a subscription_id."""

    reason: Optional[str] = None
    r"""Why the plan was skipped, expired, or flagged as a mismatch, when applicable."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["expired", "mismatch", "reason"])
        nullable_fields = set(["customer_product_id"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


class DfuFlashResultTypedDict(TypedDict):
    r"""OK"""

    customer_id: str
    r"""The imaged customer's id."""
    flashed: List[FlashedTypedDict]
    r"""Per-plan outcome of the flash."""
    customer: Nullable[CustomerTypedDict]
    r"""The freshly-read imaged customer; null for dry_run."""


class DfuFlashResult(BaseModel):
    r"""OK"""

    customer_id: str
    r"""The imaged customer's id."""

    flashed: List[Flashed]
    r"""Per-plan outcome of the flash."""

    customer: Nullable[Customer]
    r"""The freshly-read imaged customer; null for dry_run."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                m[k] = val

        return m


try:
    ImportBalance.model_rebuild()
except NameError:
    pass
