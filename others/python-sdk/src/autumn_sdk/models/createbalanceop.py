"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from autumn_sdk.types import BaseModel, UNSET_SENTINEL
from autumn_sdk.utils import FieldMetadata, HeaderMetadata
import pydantic
from pydantic import model_serializer
from typing import Literal, Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class CreateBalanceGlobalsTypedDict(TypedDict):
    x_api_version: NotRequired[str]


class CreateBalanceGlobals(BaseModel):
    x_api_version: Annotated[
        Optional[str],
        pydantic.Field(alias="x-api-version"),
        FieldMetadata(header=HeaderMetadata(style="simple", explode=False)),
    ] = "2.1"

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["x-api-version"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


CreateBalanceInterval = Literal[
    "one_off",
    "minute",
    "hour",
    "day",
    "week",
    "month",
    "quarter",
    "semi_annual",
    "year",
]
r"""The interval at which the balance resets (e.g., 'month', 'day', 'year')."""


class CreateBalanceResetTypedDict(TypedDict):
    r"""Reset configuration for the balance. If not provided, the balance is a one-time grant that never resets."""

    interval: CreateBalanceInterval
    r"""The interval at which the balance resets (e.g., 'month', 'day', 'year')."""
    interval_count: NotRequired[float]
    r"""Number of intervals between resets. Defaults to 1 (e.g., interval_count: 2 with interval: 'month' resets every 2 months)."""


class CreateBalanceReset(BaseModel):
    r"""Reset configuration for the balance. If not provided, the balance is a one-time grant that never resets."""

    interval: CreateBalanceInterval
    r"""The interval at which the balance resets (e.g., 'month', 'day', 'year')."""

    interval_count: Optional[float] = None
    r"""Number of intervals between resets. Defaults to 1 (e.g., interval_count: 2 with interval: 'month' resets every 2 months)."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["interval_count"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class CreateBalanceParamsTypedDict(TypedDict):
    customer_id: str
    r"""The ID of the customer."""
    feature_id: str
    r"""The ID of the feature."""
    entity_id: NotRequired[str]
    r"""The ID of the entity for entity-scoped balances (e.g., per-seat limits)."""
    included: NotRequired[float]
    r"""The initial balance amount to grant. For metered features, this is the number of units the customer can use."""
    unlimited: NotRequired[bool]
    r"""If true, the balance has unlimited usage. Cannot be combined with 'included'."""
    reset: NotRequired[CreateBalanceResetTypedDict]
    r"""Reset configuration for the balance. If not provided, the balance is a one-time grant that never resets."""
    expires_at: NotRequired[float]
    r"""Unix timestamp (milliseconds) when the balance expires. Mutually exclusive with reset."""
    granted_balance: NotRequired[float]


class CreateBalanceParams(BaseModel):
    customer_id: str
    r"""The ID of the customer."""

    feature_id: str
    r"""The ID of the feature."""

    entity_id: Optional[str] = None
    r"""The ID of the entity for entity-scoped balances (e.g., per-seat limits)."""

    included: Optional[float] = None
    r"""The initial balance amount to grant. For metered features, this is the number of units the customer can use."""

    unlimited: Optional[bool] = None
    r"""If true, the balance has unlimited usage. Cannot be combined with 'included'."""

    reset: Optional[CreateBalanceReset] = None
    r"""Reset configuration for the balance. If not provided, the balance is a one-time grant that never resets."""

    expires_at: Optional[float] = None
    r"""Unix timestamp (milliseconds) when the balance expires. Mutually exclusive with reset."""

    granted_balance: Optional[float] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "entity_id",
                "included",
                "unlimited",
                "reset",
                "expires_at",
                "granted_balance",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class CreateBalanceResponseTypedDict(TypedDict):
    r"""OK"""

    success: bool


class CreateBalanceResponse(BaseModel):
    r"""OK"""

    success: bool
