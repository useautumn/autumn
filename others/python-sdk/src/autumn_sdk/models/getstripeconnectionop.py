"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from autumn_sdk.types import BaseModel, Nullable, UNSET_SENTINEL
from autumn_sdk.utils import FieldMetadata, HeaderMetadata
import pydantic
from pydantic import model_serializer
from typing import Literal, Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class GetStripeConnectionGlobalsTypedDict(TypedDict):
    x_api_version: NotRequired[str]


class GetStripeConnectionGlobals(BaseModel):
    x_api_version: Annotated[
        Optional[str],
        pydantic.Field(alias="x-api-version"),
        FieldMetadata(header=HeaderMetadata(style="simple", explode=False)),
    ] = "2.4.0"

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["x-api-version"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


GetStripeConnectionEnv = Literal[
    "test",
    "live",
]
r"""Stripe connection environment to inspect or disconnect."""


class GetStripeConnectionParamsTypedDict(TypedDict):
    organization_slug: str
    r"""Public tenant organization slug, without the master organization suffix."""
    env: GetStripeConnectionEnv
    r"""Stripe connection environment to inspect or disconnect."""


class GetStripeConnectionParams(BaseModel):
    organization_slug: str
    r"""Public tenant organization slug, without the master organization suffix."""

    env: GetStripeConnectionEnv
    r"""Stripe connection environment to inspect or disconnect."""


class GetStripeConnectionResponseTypedDict(TypedDict):
    r"""OK"""

    connected: bool
    r"""Whether an OAuth connection is stored. Excludes platform-managed accounts, default sandbox accounts, and separate secret-key connections."""
    account_id: Nullable[str]
    r"""OAuth-connected Stripe account ID, or null when absent."""
    connected_at: Nullable[float]
    r"""Connection timestamp in Unix milliseconds, or null for historical connections without a timestamp and when disconnected."""


class GetStripeConnectionResponse(BaseModel):
    r"""OK"""

    connected: bool
    r"""Whether an OAuth connection is stored. Excludes platform-managed accounts, default sandbox accounts, and separate secret-key connections."""

    account_id: Nullable[str]
    r"""OAuth-connected Stripe account ID, or null when absent."""

    connected_at: Nullable[float]
    r"""Connection timestamp in Unix milliseconds, or null for historical connections without a timestamp and when disconnected."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                m[k] = val

        return m
