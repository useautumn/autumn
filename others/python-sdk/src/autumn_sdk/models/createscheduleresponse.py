"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .phasestart_remove_item_billing_method import (
    PhaseStartAddItemPlanItem,
    PhaseStartAddItemPlanItemTypedDict,
    PhaseStartBasePrice,
    PhaseStartBasePriceTypedDict,
    PhaseStartFeatureQuantity,
    PhaseStartFeatureQuantityTypedDict,
    PhaseStartItemPlanItem,
    PhaseStartItemPlanItemTypedDict,
    PhaseStartRemoveItemBillingMethod,
    StartingAfter2,
    StartingAfter2TypedDict,
    StartsAt2,
    StartsAt2TypedDict,
    UnscheduledPlan,
    UnscheduledPlanTypedDict,
)
from .unscheduled_plan_upsert_license_expiry_duration_type import (
    CreateScheduleAttachDiscount,
    CreateScheduleAttachDiscountTypedDict,
    CreateScheduleBillingBehavior,
    CreateScheduleFreeTrialParams,
    CreateScheduleFreeTrialParamsTypedDict,
    CreateScheduleInvoiceMode,
    CreateScheduleInvoiceModeTypedDict,
    CreateScheduleRedirectMode,
)
from autumn_sdk.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
    UnrecognizedStr,
)
from autumn_sdk.utils import validate_const
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Any, Dict, List, Literal, Optional, Union
from typing_extensions import Annotated, NotRequired, TypeAliasType, TypedDict


PhaseStartIntervalRemoveItemEnum2 = Literal[
    "one_off",
    "minute",
    "hour",
    "day",
    "week",
    "month",
    "quarter",
    "semi_annual",
    "year",
]


PhaseStartIntervalRemoveItemEnum1 = Literal[
    "one_off",
    "week",
    "month",
    "quarter",
    "semi_annual",
    "year",
]


PhaseStartIntervalUnionTypedDict = TypeAliasType(
    "PhaseStartIntervalUnionTypedDict",
    Union[PhaseStartIntervalRemoveItemEnum1, PhaseStartIntervalRemoveItemEnum2],
)
r"""Match items with this interval. Accepts either a BillingInterval (price-side) or a ResetInterval (reset-side, includes day/hour/minute) so price-less items keyed by reset.interval can be disambiguated."""


PhaseStartIntervalUnion = TypeAliasType(
    "PhaseStartIntervalUnion",
    Union[PhaseStartIntervalRemoveItemEnum1, PhaseStartIntervalRemoveItemEnum2],
)
r"""Match items with this interval. Accepts either a BillingInterval (price-side) or a ResetInterval (reset-side, includes day/hour/minute) so price-less items keyed by reset.interval can be disambiguated."""


class PhaseStartPlanItemFilterTypedDict(TypedDict):
    r"""Filter for matching plan items. All provided fields must match (AND)."""

    feature_id: NotRequired[str]
    r"""Match items linked to this feature."""
    billing_method: NotRequired[PhaseStartRemoveItemBillingMethod]
    r"""Match items with this billing method (prepaid or usage_based)."""
    interval: NotRequired[PhaseStartIntervalUnionTypedDict]
    r"""Match items with this interval. Accepts either a BillingInterval (price-side) or a ResetInterval (reset-side, includes day/hour/minute) so price-less items keyed by reset.interval can be disambiguated."""
    interval_count: NotRequired[int]
    r"""Match items with this interval_count. Disambiguates between items that share an interval but differ in count."""
    included: NotRequired[float]
    r"""Match items whose grant equals this included usage. Omitted is a wildcard."""


class PhaseStartPlanItemFilter(BaseModel):
    r"""Filter for matching plan items. All provided fields must match (AND)."""

    feature_id: Optional[str] = None
    r"""Match items linked to this feature."""

    billing_method: Optional[PhaseStartRemoveItemBillingMethod] = None
    r"""Match items with this billing method (prepaid or usage_based)."""

    interval: Optional[PhaseStartIntervalUnion] = None
    r"""Match items with this interval. Accepts either a BillingInterval (price-side) or a ResetInterval (reset-side, includes day/hour/minute) so price-less items keyed by reset.interval can be disambiguated."""

    interval_count: Optional[int] = None
    r"""Match items with this interval_count. Disambiguates between items that share an interval but differ in count."""

    included: Optional[float] = None
    r"""Match items whose grant equals this included usage. Omitted is a wildcard."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            ["feature_id", "billing_method", "interval", "interval_count", "included"]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PhaseStartAutoTopupInterval = Literal[
    "hour",
    "day",
    "week",
    "month",
]
r"""The time interval for the purchase limit window."""


class PhaseStartPurchaseLimitTypedDict(TypedDict):
    r"""Optional rate limit to cap how often auto top-ups occur. Pass count to set the current window's consumed top-ups."""

    interval: PhaseStartAutoTopupInterval
    r"""The time interval for the purchase limit window."""
    limit: float
    r"""Maximum number of auto top-ups allowed within the interval."""
    interval_count: NotRequired[float]
    r"""Number of intervals in the purchase limit window."""
    count: NotRequired[float]
    r"""Set the current window's consumed auto top-up count. Omit to leave runtime state unchanged."""


class PhaseStartPurchaseLimit(BaseModel):
    r"""Optional rate limit to cap how often auto top-ups occur. Pass count to set the current window's consumed top-ups."""

    interval: PhaseStartAutoTopupInterval
    r"""The time interval for the purchase limit window."""

    limit: float
    r"""Maximum number of auto top-ups allowed within the interval."""

    interval_count: Optional[float] = 1
    r"""Number of intervals in the purchase limit window."""

    count: Optional[float] = None
    r"""Set the current window's consumed auto top-up count. Omit to leave runtime state unchanged."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["interval_count", "count"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class PhaseStartAutoTopupTypedDict(TypedDict):
    feature_id: str
    r"""The ID of the feature (credit balance) to auto top-up."""
    threshold: float
    r"""When the balance drops below this threshold, an auto top-up will be purchased."""
    quantity: float
    r"""Amount of credits to add per auto top-up."""
    enabled: NotRequired[bool]
    r"""Whether auto top-up is enabled."""
    purchase_limit: NotRequired[PhaseStartPurchaseLimitTypedDict]
    r"""Optional rate limit to cap how often auto top-ups occur. Pass count to set the current window's consumed top-ups."""
    invoice_mode: NotRequired[bool]
    r"""When true, auto top-up creates a send_invoice invoice instead of auto-charging."""


class PhaseStartAutoTopup(BaseModel):
    feature_id: str
    r"""The ID of the feature (credit balance) to auto top-up."""

    threshold: float
    r"""When the balance drops below this threshold, an auto top-up will be purchased."""

    quantity: float
    r"""Amount of credits to add per auto top-up."""

    enabled: Optional[bool] = False
    r"""Whether auto top-up is enabled."""

    purchase_limit: Optional[PhaseStartPurchaseLimit] = None
    r"""Optional rate limit to cap how often auto top-ups occur. Pass count to set the current window's consumed top-ups."""

    invoice_mode: Optional[bool] = None
    r"""When true, auto top-up creates a send_invoice invoice instead of auto-charging."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["enabled", "purchase_limit", "invoice_mode"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PhaseStartLimitType = Literal[
    "absolute",
    "usage_percentage",
]
r"""How overage_limit is interpreted: an absolute overage cap (default) or a percentage of the main-plan allowance."""


class PhaseStartSpendLimitTypedDict(TypedDict):
    feature_id: NotRequired[str]
    r"""Optional feature ID this spend limit applies to."""
    enabled: NotRequired[bool]
    r"""Whether the overage spend limit is enabled."""
    limit_type: NotRequired[PhaseStartLimitType]
    r"""How overage_limit is interpreted: an absolute overage cap (default) or a percentage of the main-plan allowance."""
    overage_limit: NotRequired[float]
    r"""Overage cap for the feature: absolute units, or a percent (e.g. 120) when limit_type is usage_percentage."""
    skip_overage_billing: NotRequired[bool]
    r"""When true, overage for this feature is not posted to Stripe. Usage tracking and balance resets still behave normally."""


class PhaseStartSpendLimit(BaseModel):
    feature_id: Optional[str] = None
    r"""Optional feature ID this spend limit applies to."""

    enabled: Optional[bool] = False
    r"""Whether the overage spend limit is enabled."""

    limit_type: Optional[PhaseStartLimitType] = None
    r"""How overage_limit is interpreted: an absolute overage cap (default) or a percentage of the main-plan allowance."""

    overage_limit: Optional[float] = None
    r"""Overage cap for the feature: absolute units, or a percent (e.g. 120) when limit_type is usage_percentage."""

    skip_overage_billing: Optional[bool] = None
    r"""When true, overage for this feature is not posted to Stripe. Usage tracking and balance resets still behave normally."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "feature_id",
                "enabled",
                "limit_type",
                "overage_limit",
                "skip_overage_billing",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PhaseStartUsageLimitInterval = Literal[
    "day",
    "week",
    "month",
    "year",
]
r"""Interval for the cap, aligned to the customer's billing cycle."""


PhaseStartAnchor = Literal[
    "billing_cycle",
    "utc",
]
r"""Window alignment. 'billing_cycle' phases the interval to the customer's renewal time; 'utc' aligns to the UTC calendar."""


PhaseStartUsageLimitPropertiesTypedDict = TypeAliasType(
    "PhaseStartUsageLimitPropertiesTypedDict", Union[str, float, bool]
)


PhaseStartUsageLimitProperties = TypeAliasType(
    "PhaseStartUsageLimitProperties", Union[str, float, bool]
)


class PhaseStartUsageLimitFilterTypedDict(TypedDict):
    r"""When set, only usage from events whose properties match counts toward this cap. Omit to count all usage of the feature."""

    properties: Dict[str, PhaseStartUsageLimitPropertiesTypedDict]


class PhaseStartUsageLimitFilter(BaseModel):
    r"""When set, only usage from events whose properties match counts toward this cap. Omit to count all usage of the feature."""

    properties: Dict[str, PhaseStartUsageLimitProperties]


class PhaseStartUsageLimitTypedDict(TypedDict):
    feature_id: str
    r"""The feature this usage limit applies to."""
    limit: float
    r"""Maximum units allowed per interval."""
    interval: PhaseStartUsageLimitInterval
    r"""Interval for the cap, aligned to the customer's billing cycle."""
    enabled: NotRequired[bool]
    r"""Whether this usage limit is enabled."""
    anchor: NotRequired[PhaseStartAnchor]
    r"""Window alignment. 'billing_cycle' phases the interval to the customer's renewal time; 'utc' aligns to the UTC calendar."""
    filter_: NotRequired[PhaseStartUsageLimitFilterTypedDict]
    r"""When set, only usage from events whose properties match counts toward this cap. Omit to count all usage of the feature."""


class PhaseStartUsageLimit(BaseModel):
    feature_id: str
    r"""The feature this usage limit applies to."""

    limit: float
    r"""Maximum units allowed per interval."""

    interval: PhaseStartUsageLimitInterval
    r"""Interval for the cap, aligned to the customer's billing cycle."""

    enabled: Optional[bool] = True
    r"""Whether this usage limit is enabled."""

    anchor: Optional[PhaseStartAnchor] = None
    r"""Window alignment. 'billing_cycle' phases the interval to the customer's renewal time; 'utc' aligns to the UTC calendar."""

    filter_: Annotated[
        Optional[PhaseStartUsageLimitFilter], pydantic.Field(alias="filter")
    ] = None
    r"""When set, only usage from events whose properties match counts toward this cap. Omit to count all usage of the feature."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["enabled", "anchor", "filter"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PhaseStartThresholdType = Literal[
    "usage",
    "usage_percentage",
    "remaining",
    "remaining_percentage",
]
r"""Whether the threshold is an absolute count or a percentage of the usage allowance or remaining balance."""


PhaseStartBasis = Literal[
    "balance",
    "included",
    "recurring",
    "usage_limit",
]
r"""What 100% means. balance: every grant on the feature. included: the plan allowance only. recurring: grants that reset. usage_limit: the cap of the usage limit with the same feature and filter."""


PhaseStartUsageAlertPropertiesTypedDict = TypeAliasType(
    "PhaseStartUsageAlertPropertiesTypedDict", Union[str, float, bool]
)


PhaseStartUsageAlertProperties = TypeAliasType(
    "PhaseStartUsageAlertProperties", Union[str, float, bool]
)


class PhaseStartUsageAlertFilterTypedDict(TypedDict):
    r"""Only valid with basis usage_limit. Points the alert at the usage limit carrying the same filter."""

    properties: Dict[str, PhaseStartUsageAlertPropertiesTypedDict]


class PhaseStartUsageAlertFilter(BaseModel):
    r"""Only valid with basis usage_limit. Points the alert at the usage limit carrying the same filter."""

    properties: Dict[str, PhaseStartUsageAlertProperties]


class PhaseStartUsageAlertTypedDict(TypedDict):
    threshold: float
    r"""The threshold value that triggers the alert. For usage or remaining, this is an absolute count. For usage_percentage or remaining_percentage, this is a percentage (0-100)."""
    threshold_type: PhaseStartThresholdType
    r"""Whether the threshold is an absolute count or a percentage of the usage allowance or remaining balance."""
    feature_id: NotRequired[str]
    r"""The feature ID this alert applies to."""
    enabled: NotRequired[bool]
    r"""Whether this usage alert is enabled."""
    basis: NotRequired[PhaseStartBasis]
    r"""What 100% means. balance: every grant on the feature. included: the plan allowance only. recurring: grants that reset. usage_limit: the cap of the usage limit with the same feature and filter."""
    filter_: NotRequired[PhaseStartUsageAlertFilterTypedDict]
    r"""Only valid with basis usage_limit. Points the alert at the usage limit carrying the same filter."""
    name: NotRequired[str]
    r"""Optional user-defined label to distinguish multiple alerts on the same feature."""


class PhaseStartUsageAlert(BaseModel):
    threshold: float
    r"""The threshold value that triggers the alert. For usage or remaining, this is an absolute count. For usage_percentage or remaining_percentage, this is a percentage (0-100)."""

    threshold_type: PhaseStartThresholdType
    r"""Whether the threshold is an absolute count or a percentage of the usage allowance or remaining balance."""

    feature_id: Optional[str] = None
    r"""The feature ID this alert applies to."""

    enabled: Optional[bool] = True
    r"""Whether this usage alert is enabled."""

    basis: Optional[PhaseStartBasis] = "balance"
    r"""What 100% means. balance: every grant on the feature. included: the plan allowance only. recurring: grants that reset. usage_limit: the cap of the usage limit with the same feature and filter."""

    filter_: Annotated[
        Optional[PhaseStartUsageAlertFilter], pydantic.Field(alias="filter")
    ] = None
    r"""Only valid with basis usage_limit. Points the alert at the usage limit carrying the same filter."""

    name: Optional[str] = None
    r"""Optional user-defined label to distinguish multiple alerts on the same feature."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["feature_id", "enabled", "basis", "filter", "name"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class PhaseStartOverageAllowedTypedDict(TypedDict):
    feature_id: str
    r"""The feature ID this overage allowed control applies to."""
    enabled: NotRequired[bool]
    r"""Whether overage is allowed for this feature."""


class PhaseStartOverageAllowed(BaseModel):
    feature_id: str
    r"""The feature ID this overage allowed control applies to."""

    enabled: Optional[bool] = False
    r"""Whether overage is allowed for this feature."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["enabled"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class PhaseStartBillingControlsTypedDict(TypedDict):
    r"""Override the plan's billing controls (auto top-ups, spend limits, usage limits, usage alerts, overage allowed) for this customer."""

    auto_topups: NotRequired[List[PhaseStartAutoTopupTypedDict]]
    r"""List of auto top-up configurations per feature."""
    spend_limits: NotRequired[List[PhaseStartSpendLimitTypedDict]]
    r"""List of overage spend limits per feature (caps overage spend)."""
    usage_limits: NotRequired[List[PhaseStartUsageLimitTypedDict]]
    r"""List of hard usage caps per feature (max units per interval)."""
    usage_alerts: NotRequired[List[PhaseStartUsageAlertTypedDict]]
    r"""List of usage alert configurations per feature."""
    overage_allowed: NotRequired[List[PhaseStartOverageAllowedTypedDict]]
    r"""List of overage allowed controls per feature. When enabled, usage can exceed balance."""


class PhaseStartBillingControls(BaseModel):
    r"""Override the plan's billing controls (auto top-ups, spend limits, usage limits, usage alerts, overage allowed) for this customer."""

    auto_topups: Optional[List[PhaseStartAutoTopup]] = None
    r"""List of auto top-up configurations per feature."""

    spend_limits: Optional[List[PhaseStartSpendLimit]] = None
    r"""List of overage spend limits per feature (caps overage spend)."""

    usage_limits: Optional[List[PhaseStartUsageLimit]] = None
    r"""List of hard usage caps per feature (max units per interval)."""

    usage_alerts: Optional[List[PhaseStartUsageAlert]] = None
    r"""List of usage alert configurations per feature."""

    overage_allowed: Optional[List[PhaseStartOverageAllowed]] = None
    r"""List of overage allowed controls per feature. When enabled, usage can exceed balance."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "auto_topups",
                "spend_limits",
                "usage_limits",
                "usage_alerts",
                "overage_allowed",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PhaseStartPriceUpsertLicenseInterval = Literal[
    "one_off",
    "week",
    "month",
    "quarter",
    "semi_annual",
    "year",
]
r"""Billing interval (e.g. 'month', 'year')."""


class PhaseStartUpsertLicenseAdditionalCurrencyTypedDict(TypedDict):
    currency: str
    r"""Three-letter Stripe-supported currency code (e.g. 'eur', 'gbp')."""
    amount: float
    r"""Price amount in this currency. Set explicitly per currency, not converted from the base amount."""


class PhaseStartUpsertLicenseAdditionalCurrency(BaseModel):
    currency: str
    r"""Three-letter Stripe-supported currency code (e.g. 'eur', 'gbp')."""

    amount: float
    r"""Price amount in this currency. Set explicitly per currency, not converted from the base amount."""


class PhaseStartUpsertLicenseBasePriceTypedDict(TypedDict):
    r"""Base price configuration for a plan."""

    amount: float
    r"""Base price amount for the plan, in major currency units (e.g. dollars)."""
    interval: PhaseStartPriceUpsertLicenseInterval
    r"""Billing interval (e.g. 'month', 'year')."""
    interval_count: NotRequired[float]
    r"""Number of intervals per billing cycle. Defaults to 1."""
    additional_currencies: NotRequired[
        List[PhaseStartUpsertLicenseAdditionalCurrencyTypedDict]
    ]
    r"""Base price amounts in additional currencies. The base 'amount' is in the org's default currency."""


class PhaseStartUpsertLicenseBasePrice(BaseModel):
    r"""Base price configuration for a plan."""

    amount: float
    r"""Base price amount for the plan, in major currency units (e.g. dollars)."""

    interval: PhaseStartPriceUpsertLicenseInterval
    r"""Billing interval (e.g. 'month', 'year')."""

    interval_count: Optional[float] = 1
    r"""Number of intervals per billing cycle. Defaults to 1."""

    additional_currencies: Optional[List[PhaseStartUpsertLicenseAdditionalCurrency]] = (
        None
    )
    r"""Base price amounts in additional currencies. The base 'amount' is in the org's default currency."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["interval_count", "additional_currencies"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class PhaseStartUpsertLicenseThresholdBillingTypedDict(TypedDict):
    threshold: float


class PhaseStartUpsertLicenseThresholdBilling(BaseModel):
    threshold: float


PhaseStartUpsertLicenseResetInterval = Literal[
    "one_off",
    "minute",
    "hour",
    "day",
    "week",
    "month",
    "quarter",
    "semi_annual",
    "year",
]
r"""Interval at which balance resets (e.g. 'month', 'year'). For consumable features only."""


class PhaseStartUpsertLicenseResetTypedDict(TypedDict):
    r"""Reset configuration for consumable features. Omit for non-consumable features like seats."""

    interval: PhaseStartUpsertLicenseResetInterval
    r"""Interval at which balance resets (e.g. 'month', 'year'). For consumable features only."""
    interval_count: NotRequired[float]
    r"""Number of intervals between resets. Defaults to 1."""


class PhaseStartUpsertLicenseReset(BaseModel):
    r"""Reset configuration for consumable features. Omit for non-consumable features like seats."""

    interval: PhaseStartUpsertLicenseResetInterval
    r"""Interval at which balance resets (e.g. 'month', 'year'). For consumable features only."""

    interval_count: Optional[float] = 1
    r"""Number of intervals between resets. Defaults to 1."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["interval_count"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class PhaseStartUpsertLicenseAddItemAdditionalCurrencyTypedDict(TypedDict):
    currency: str
    r"""Three-letter Stripe-supported currency code (e.g. 'eur', 'gbp')."""
    amount: float
    r"""Price amount in this currency. Set explicitly per currency, not converted from the base amount."""


class PhaseStartUpsertLicenseAddItemAdditionalCurrency(BaseModel):
    currency: str
    r"""Three-letter Stripe-supported currency code (e.g. 'eur', 'gbp')."""

    amount: float
    r"""Price amount in this currency. Set explicitly per currency, not converted from the base amount."""


PhaseStartUpsertLicensePriceToTypedDict = TypeAliasType(
    "PhaseStartUpsertLicensePriceToTypedDict", Union[float, str]
)


PhaseStartUpsertLicensePriceTo = TypeAliasType(
    "PhaseStartUpsertLicensePriceTo", Union[float, str]
)


class PhaseStartUpsertLicenseTierAdditionalCurrencyTypedDict(TypedDict):
    currency: str
    r"""Three-letter Stripe-supported currency code (e.g. 'eur', 'gbp')."""
    amount: NotRequired[float]
    r"""Per-unit amount for this tier in this currency."""
    flat_amount: NotRequired[float]
    r"""Flat amount for this tier in this currency, if the tier uses one."""


class PhaseStartUpsertLicenseTierAdditionalCurrency(BaseModel):
    currency: str
    r"""Three-letter Stripe-supported currency code (e.g. 'eur', 'gbp')."""

    amount: Optional[float] = None
    r"""Per-unit amount for this tier in this currency."""

    flat_amount: Optional[float] = None
    r"""Flat amount for this tier in this currency, if the tier uses one."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["amount", "flat_amount"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class PhaseStartUpsertLicensePriceTierTypedDict(TypedDict):
    to: PhaseStartUpsertLicensePriceToTypedDict
    amount: NotRequired[float]
    flat_amount: NotRequired[float]
    additional_currencies: NotRequired[
        List[PhaseStartUpsertLicenseTierAdditionalCurrencyTypedDict]
    ]
    r"""Per-currency amounts for this tier. Tier boundaries ('to') are shared across all currencies."""


class PhaseStartUpsertLicensePriceTier(BaseModel):
    to: PhaseStartUpsertLicensePriceTo

    amount: Optional[float] = None

    flat_amount: Optional[float] = None

    additional_currencies: Optional[
        List[PhaseStartUpsertLicenseTierAdditionalCurrency]
    ] = None
    r"""Per-currency amounts for this tier. Tier boundaries ('to') are shared across all currencies."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["amount", "flat_amount", "additional_currencies"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PhaseStartUpsertLicenseTierBehavior = Literal[
    "graduated",
    "volume",
]


PhaseStartUpsertLicenseAddItemPriceInterval = Literal[
    "one_off",
    "week",
    "month",
    "quarter",
    "semi_annual",
    "year",
]
r"""Billing interval. For consumable features, should match reset.interval."""


PhaseStartUpsertLicenseAddItemBillingMethod = Literal[
    "prepaid",
    "usage_based",
]
r"""'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go."""


class PhaseStartUpsertLicensePriceTypedDict(TypedDict):
    r"""Pricing for usage beyond included units. Omit for free features."""

    interval: PhaseStartUpsertLicenseAddItemPriceInterval
    r"""Billing interval. For consumable features, should match reset.interval."""
    billing_method: PhaseStartUpsertLicenseAddItemBillingMethod
    r"""'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go."""
    amount: NotRequired[float]
    r"""Price per billing_units after included usage. Either 'amount' or 'tiers' is required."""
    additional_currencies: NotRequired[
        List[PhaseStartUpsertLicenseAddItemAdditionalCurrencyTypedDict]
    ]
    r"""Amounts in additional currencies for this flat price. The base 'amount' is in the org's default currency. Only valid with 'amount', not 'tiers'."""
    tiers: NotRequired[List[PhaseStartUpsertLicensePriceTierTypedDict]]
    r"""Tiered pricing.  Either 'amount' or 'tiers' is required."""
    tier_behavior: NotRequired[PhaseStartUpsertLicenseTierBehavior]
    interval_count: NotRequired[float]
    r"""Number of intervals per billing cycle. Defaults to 1."""
    billing_units: NotRequired[float]
    r"""Units per price increment. Usage is rounded UP when billed (e.g. billing_units=100 means 101 rounds to 200)."""
    max_purchase: NotRequired[Nullable[float]]
    r"""Max units purchasable beyond included. E.g. included=100, max_purchase=300 allows 400 total. Null for no limit."""


class PhaseStartUpsertLicensePrice(BaseModel):
    r"""Pricing for usage beyond included units. Omit for free features."""

    interval: PhaseStartUpsertLicenseAddItemPriceInterval
    r"""Billing interval. For consumable features, should match reset.interval."""

    billing_method: PhaseStartUpsertLicenseAddItemBillingMethod
    r"""'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go."""

    amount: Optional[float] = None
    r"""Price per billing_units after included usage. Either 'amount' or 'tiers' is required."""

    additional_currencies: Optional[
        List[PhaseStartUpsertLicenseAddItemAdditionalCurrency]
    ] = None
    r"""Amounts in additional currencies for this flat price. The base 'amount' is in the org's default currency. Only valid with 'amount', not 'tiers'."""

    tiers: Optional[List[PhaseStartUpsertLicensePriceTier]] = None
    r"""Tiered pricing.  Either 'amount' or 'tiers' is required."""

    tier_behavior: Optional[PhaseStartUpsertLicenseTierBehavior] = None

    interval_count: Optional[float] = 1
    r"""Number of intervals per billing cycle. Defaults to 1."""

    billing_units: Optional[float] = 1
    r"""Units per price increment. Usage is rounded UP when billed (e.g. billing_units=100 means 101 rounds to 200)."""

    max_purchase: OptionalNullable[float] = UNSET
    r"""Max units purchasable beyond included. E.g. included=100, max_purchase=300 allows 400 total. Null for no limit."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "amount",
                "additional_currencies",
                "tiers",
                "tier_behavior",
                "interval_count",
                "billing_units",
                "max_purchase",
            ]
        )
        nullable_fields = set(["max_purchase"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


PhaseStartUpsertLicenseOnIncrease = Literal[
    "bill_immediately",
    "prorate_immediately",
    "prorate_next_cycle",
    "bill_next_cycle",
]
r"""Billing behavior when quantity increases mid-cycle."""


PhaseStartUpsertLicenseOnDecrease = Literal[
    "prorate",
    "prorate_immediately",
    "prorate_next_cycle",
    "none",
    "no_prorations",
]
r"""Credit behavior when quantity decreases mid-cycle."""


class PhaseStartUpsertLicenseProrationTypedDict(TypedDict):
    r"""Proration settings for prepaid features. Controls mid-cycle quantity change billing."""

    on_increase: PhaseStartUpsertLicenseOnIncrease
    r"""Billing behavior when quantity increases mid-cycle."""
    on_decrease: PhaseStartUpsertLicenseOnDecrease
    r"""Credit behavior when quantity decreases mid-cycle."""


class PhaseStartUpsertLicenseProration(BaseModel):
    r"""Proration settings for prepaid features. Controls mid-cycle quantity change billing."""

    on_increase: PhaseStartUpsertLicenseOnIncrease
    r"""Billing behavior when quantity increases mid-cycle."""

    on_decrease: PhaseStartUpsertLicenseOnDecrease
    r"""Credit behavior when quantity decreases mid-cycle."""


PhaseStartUpsertLicenseExpiryDurationType = Literal[
    "month",
    "forever",
]
r"""When rolled over units expire."""


class PhaseStartUpsertLicenseRolloverTypedDict(TypedDict):
    r"""Rollover config for unused units. If set, unused included units carry over."""

    expiry_duration_type: PhaseStartUpsertLicenseExpiryDurationType
    r"""When rolled over units expire."""
    max: NotRequired[float]
    r"""Max rollover units. Omit for unlimited rollover."""
    max_percentage: NotRequired[float]
    r"""Maximum rollover as a percentage (0-100) of included + prepaid grant. Mutually exclusive with max."""
    expiry_duration_length: NotRequired[float]
    r"""Number of periods before expiry."""


class PhaseStartUpsertLicenseRollover(BaseModel):
    r"""Rollover config for unused units. If set, unused included units carry over."""

    expiry_duration_type: PhaseStartUpsertLicenseExpiryDurationType
    r"""When rolled over units expire."""

    max: Optional[float] = None
    r"""Max rollover units. Omit for unlimited rollover."""

    max_percentage: Optional[float] = None
    r"""Maximum rollover as a percentage (0-100) of included + prepaid grant. Mutually exclusive with max."""

    expiry_duration_length: Optional[float] = None
    r"""Number of periods before expiry."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["max", "max_percentage", "expiry_duration_length"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PhaseStartUpsertLicenseDuration = Literal[
    "day",
    "week",
    "month",
    "year",
]


class PhaseStartUpsertLicenseExpiryTypedDict(TypedDict):
    r"""Purchased units expire this long after each purchase. One-off prepaid consumable items only."""

    duration: PhaseStartUpsertLicenseDuration
    length: float


class PhaseStartUpsertLicenseExpiry(BaseModel):
    r"""Purchased units expire this long after each purchase. One-off prepaid consumable items only."""

    duration: PhaseStartUpsertLicenseDuration

    length: float


PhaseStartDimensionsUpsertLicenseMatch4TypedDict = TypeAliasType(
    "PhaseStartDimensionsUpsertLicenseMatch4TypedDict", Union[str, float, bool]
)


PhaseStartDimensionsUpsertLicenseMatch4 = TypeAliasType(
    "PhaseStartDimensionsUpsertLicenseMatch4", Union[str, float, bool]
)


class PhaseStartDimensionsUpsertLicense4TypedDict(TypedDict):
    match: Dict[str, PhaseStartDimensionsUpsertLicenseMatch4TypedDict]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""
    credit_cost: float
    r"""Credits consumed per billing-unit group when this dimension matches."""
    priority: NotRequired[int]
    r"""Breaks ties between dimensions that match the same number of keys. Higher wins."""


class PhaseStartDimensionsUpsertLicense4(BaseModel):
    match: Dict[str, PhaseStartDimensionsUpsertLicenseMatch4]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""

    credit_cost: float
    r"""Credits consumed per billing-unit group when this dimension matches."""

    priority: Optional[int] = None
    r"""Breaks ties between dimensions that match the same number of keys. Higher wins."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["priority"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PhaseStartDimensionsUpsertLicenseMatch3TypedDict = TypeAliasType(
    "PhaseStartDimensionsUpsertLicenseMatch3TypedDict", Union[str, float, bool]
)


PhaseStartDimensionsUpsertLicenseMatch3 = TypeAliasType(
    "PhaseStartDimensionsUpsertLicenseMatch3", Union[str, float, bool]
)


class PhaseStartDimensionsUpsertLicenseTier2TypedDict(TypedDict):
    credit_cost: float
    r"""Credits consumed per billing-unit group within this tier."""
    to: NotRequired[Any]
    r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""


class PhaseStartDimensionsUpsertLicenseTier2(BaseModel):
    credit_cost: float
    r"""Credits consumed per billing-unit group within this tier."""

    to: Optional[Any] = None
    r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["to"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class PhaseStartDimensionsUpsertLicense3TypedDict(TypedDict):
    match: Dict[str, PhaseStartDimensionsUpsertLicenseMatch3TypedDict]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""
    tiers: List[PhaseStartDimensionsUpsertLicenseTier2TypedDict]
    priority: NotRequired[int]
    r"""Breaks ties between dimensions that match the same number of keys. Higher wins."""
    tier_behavior: Literal["graduated"]


class PhaseStartDimensionsUpsertLicense3(BaseModel):
    match: Dict[str, PhaseStartDimensionsUpsertLicenseMatch3]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""

    tiers: List[PhaseStartDimensionsUpsertLicenseTier2]

    priority: Optional[int] = None
    r"""Breaks ties between dimensions that match the same number of keys. Higher wins."""

    tier_behavior: Annotated[
        Annotated[Literal["graduated"], AfterValidator(validate_const("graduated"))],
        pydantic.Field(alias="tier_behavior"),
    ] = "graduated"

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["priority"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PhaseStartUpsertLicenseDimensionsUnion2TypedDict = TypeAliasType(
    "PhaseStartUpsertLicenseDimensionsUnion2TypedDict",
    Union[
        PhaseStartDimensionsUpsertLicense4TypedDict,
        PhaseStartDimensionsUpsertLicense3TypedDict,
    ],
)


PhaseStartUpsertLicenseDimensionsUnion2 = TypeAliasType(
    "PhaseStartUpsertLicenseDimensionsUnion2",
    Union[PhaseStartDimensionsUpsertLicense4, PhaseStartDimensionsUpsertLicense3],
)


PhaseStartUpsertLicenseMultipliersMatch2TypedDict = TypeAliasType(
    "PhaseStartUpsertLicenseMultipliersMatch2TypedDict", Union[str, float, bool]
)


PhaseStartUpsertLicenseMultipliersMatch2 = TypeAliasType(
    "PhaseStartUpsertLicenseMultipliersMatch2", Union[str, float, bool]
)


class PhaseStartUpsertLicenseMultipliers2TypedDict(TypedDict):
    match: Dict[str, PhaseStartUpsertLicenseMultipliersMatch2TypedDict]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""
    factor: NotRequired[float]
    r"""Multiplies the matched rate. All matching multipliers stack."""
    add: NotRequired[float]
    r"""Added to the rate after every factor is applied, in credits per billing-unit group."""


class PhaseStartUpsertLicenseMultipliers2(BaseModel):
    match: Dict[str, PhaseStartUpsertLicenseMultipliersMatch2]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""

    factor: Optional[float] = None
    r"""Multiplies the matched rate. All matching multipliers stack."""

    add: Optional[float] = None
    r"""Added to the rate after every factor is applied, in credits per billing-unit group."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["factor", "add"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class PhaseStartCreditSchemaUpsertLicense2TypedDict(TypedDict):
    metered_feature_id: str
    r"""ID of the metered feature that draws from this credit system."""
    credit_cost: float
    r"""Credits consumed per billing-unit group."""
    billing_units: NotRequired[float]
    r"""Number of metered-feature units priced together. Defaults to one when omitted."""
    dimensions: NotRequired[Dict[str, PhaseStartUpsertLicenseDimensionsUnion2TypedDict]]
    r"""Named rates chosen by event properties. The most specific match sets the rate; with no match the item's own rate applies."""
    multipliers: NotRequired[Dict[str, PhaseStartUpsertLicenseMultipliers2TypedDict]]
    r"""Named adjustments chosen by event properties. Every match applies: factors multiply, then adds are summed."""


class PhaseStartCreditSchemaUpsertLicense2(BaseModel):
    metered_feature_id: str
    r"""ID of the metered feature that draws from this credit system."""

    credit_cost: float
    r"""Credits consumed per billing-unit group."""

    billing_units: Optional[float] = None
    r"""Number of metered-feature units priced together. Defaults to one when omitted."""

    dimensions: Optional[Dict[str, PhaseStartUpsertLicenseDimensionsUnion2]] = None
    r"""Named rates chosen by event properties. The most specific match sets the rate; with no match the item's own rate applies."""

    multipliers: Optional[Dict[str, PhaseStartUpsertLicenseMultipliers2]] = None
    r"""Named adjustments chosen by event properties. Every match applies: factors multiply, then adds are summed."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["billing_units", "dimensions", "multipliers"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PhaseStartDimensionsUpsertLicenseMatch2TypedDict = TypeAliasType(
    "PhaseStartDimensionsUpsertLicenseMatch2TypedDict", Union[str, float, bool]
)


PhaseStartDimensionsUpsertLicenseMatch2 = TypeAliasType(
    "PhaseStartDimensionsUpsertLicenseMatch2", Union[str, float, bool]
)


class PhaseStartDimensionsUpsertLicense2TypedDict(TypedDict):
    match: Dict[str, PhaseStartDimensionsUpsertLicenseMatch2TypedDict]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""
    credit_cost: float
    r"""Credits consumed per billing-unit group when this dimension matches."""
    priority: NotRequired[int]
    r"""Breaks ties between dimensions that match the same number of keys. Higher wins."""


class PhaseStartDimensionsUpsertLicense2(BaseModel):
    match: Dict[str, PhaseStartDimensionsUpsertLicenseMatch2]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""

    credit_cost: float
    r"""Credits consumed per billing-unit group when this dimension matches."""

    priority: Optional[int] = None
    r"""Breaks ties between dimensions that match the same number of keys. Higher wins."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["priority"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PhaseStartDimensionsUpsertLicenseMatch1TypedDict = TypeAliasType(
    "PhaseStartDimensionsUpsertLicenseMatch1TypedDict", Union[str, float, bool]
)


PhaseStartDimensionsUpsertLicenseMatch1 = TypeAliasType(
    "PhaseStartDimensionsUpsertLicenseMatch1", Union[str, float, bool]
)


class PhaseStartDimensionsUpsertLicenseTier1TypedDict(TypedDict):
    credit_cost: float
    r"""Credits consumed per billing-unit group within this tier."""
    to: NotRequired[Any]
    r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""


class PhaseStartDimensionsUpsertLicenseTier1(BaseModel):
    credit_cost: float
    r"""Credits consumed per billing-unit group within this tier."""

    to: Optional[Any] = None
    r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["to"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class PhaseStartDimensionsUpsertLicense1TypedDict(TypedDict):
    match: Dict[str, PhaseStartDimensionsUpsertLicenseMatch1TypedDict]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""
    tiers: List[PhaseStartDimensionsUpsertLicenseTier1TypedDict]
    priority: NotRequired[int]
    r"""Breaks ties between dimensions that match the same number of keys. Higher wins."""
    tier_behavior: Literal["graduated"]


class PhaseStartDimensionsUpsertLicense1(BaseModel):
    match: Dict[str, PhaseStartDimensionsUpsertLicenseMatch1]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""

    tiers: List[PhaseStartDimensionsUpsertLicenseTier1]

    priority: Optional[int] = None
    r"""Breaks ties between dimensions that match the same number of keys. Higher wins."""

    tier_behavior: Annotated[
        Annotated[Literal["graduated"], AfterValidator(validate_const("graduated"))],
        pydantic.Field(alias="tier_behavior"),
    ] = "graduated"

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["priority"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PhaseStartUpsertLicenseDimensionsUnion1TypedDict = TypeAliasType(
    "PhaseStartUpsertLicenseDimensionsUnion1TypedDict",
    Union[
        PhaseStartDimensionsUpsertLicense2TypedDict,
        PhaseStartDimensionsUpsertLicense1TypedDict,
    ],
)


PhaseStartUpsertLicenseDimensionsUnion1 = TypeAliasType(
    "PhaseStartUpsertLicenseDimensionsUnion1",
    Union[PhaseStartDimensionsUpsertLicense2, PhaseStartDimensionsUpsertLicense1],
)


PhaseStartUpsertLicenseMultipliersMatch1TypedDict = TypeAliasType(
    "PhaseStartUpsertLicenseMultipliersMatch1TypedDict", Union[str, float, bool]
)


PhaseStartUpsertLicenseMultipliersMatch1 = TypeAliasType(
    "PhaseStartUpsertLicenseMultipliersMatch1", Union[str, float, bool]
)


class PhaseStartUpsertLicenseMultipliers1TypedDict(TypedDict):
    match: Dict[str, PhaseStartUpsertLicenseMultipliersMatch1TypedDict]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""
    factor: NotRequired[float]
    r"""Multiplies the matched rate. All matching multipliers stack."""
    add: NotRequired[float]
    r"""Added to the rate after every factor is applied, in credits per billing-unit group."""


class PhaseStartUpsertLicenseMultipliers1(BaseModel):
    match: Dict[str, PhaseStartUpsertLicenseMultipliersMatch1]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""

    factor: Optional[float] = None
    r"""Multiplies the matched rate. All matching multipliers stack."""

    add: Optional[float] = None
    r"""Added to the rate after every factor is applied, in credits per billing-unit group."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["factor", "add"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PhaseStartToUpsertLicenseEnum = Literal["inf",]


PhaseStartUpsertLicenseFeatureOverrideToUnionTypedDict = TypeAliasType(
    "PhaseStartUpsertLicenseFeatureOverrideToUnionTypedDict",
    Union[float, PhaseStartToUpsertLicenseEnum],
)
r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""


PhaseStartUpsertLicenseFeatureOverrideToUnion = TypeAliasType(
    "PhaseStartUpsertLicenseFeatureOverrideToUnion",
    Union[float, PhaseStartToUpsertLicenseEnum],
)
r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""


class PhaseStartUpsertLicenseFeatureOverrideTierTypedDict(TypedDict):
    to: PhaseStartUpsertLicenseFeatureOverrideToUnionTypedDict
    r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""
    credit_cost: float
    r"""Credits consumed per billing-unit group within this tier."""


class PhaseStartUpsertLicenseFeatureOverrideTier(BaseModel):
    to: PhaseStartUpsertLicenseFeatureOverrideToUnion
    r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""

    credit_cost: float
    r"""Credits consumed per billing-unit group within this tier."""


class PhaseStartCreditSchemaUpsertLicense1TypedDict(TypedDict):
    metered_feature_id: str
    r"""ID of the metered feature that draws from this credit system."""
    tiers: List[PhaseStartUpsertLicenseFeatureOverrideTierTypedDict]
    billing_units: NotRequired[float]
    r"""Number of metered-feature units priced together. Defaults to one when omitted."""
    dimensions: NotRequired[Dict[str, PhaseStartUpsertLicenseDimensionsUnion1TypedDict]]
    r"""Named rates chosen by event properties. The most specific match sets the rate; with no match the item's own rate applies."""
    multipliers: NotRequired[Dict[str, PhaseStartUpsertLicenseMultipliers1TypedDict]]
    r"""Named adjustments chosen by event properties. Every match applies: factors multiply, then adds are summed."""
    tier_behavior: Literal["graduated"]


class PhaseStartCreditSchemaUpsertLicense1(BaseModel):
    metered_feature_id: str
    r"""ID of the metered feature that draws from this credit system."""

    tiers: List[PhaseStartUpsertLicenseFeatureOverrideTier]

    billing_units: Optional[float] = None
    r"""Number of metered-feature units priced together. Defaults to one when omitted."""

    dimensions: Optional[Dict[str, PhaseStartUpsertLicenseDimensionsUnion1]] = None
    r"""Named rates chosen by event properties. The most specific match sets the rate; with no match the item's own rate applies."""

    multipliers: Optional[Dict[str, PhaseStartUpsertLicenseMultipliers1]] = None
    r"""Named adjustments chosen by event properties. Every match applies: factors multiply, then adds are summed."""

    tier_behavior: Annotated[
        Annotated[Literal["graduated"], AfterValidator(validate_const("graduated"))],
        pydantic.Field(alias="tier_behavior"),
    ] = "graduated"

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["billing_units", "dimensions", "multipliers"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PhaseStartUpsertLicenseCreditSchemaUnionTypedDict = TypeAliasType(
    "PhaseStartUpsertLicenseCreditSchemaUnionTypedDict",
    Union[
        PhaseStartCreditSchemaUpsertLicense2TypedDict,
        PhaseStartCreditSchemaUpsertLicense1TypedDict,
    ],
)


PhaseStartUpsertLicenseCreditSchemaUnion = TypeAliasType(
    "PhaseStartUpsertLicenseCreditSchemaUnion",
    Union[PhaseStartCreditSchemaUpsertLicense2, PhaseStartCreditSchemaUpsertLicense1],
)


class PhaseStartUpsertLicenseProviderMarkupsTypedDict(TypedDict):
    markup: float


class PhaseStartUpsertLicenseProviderMarkups(BaseModel):
    markup: float


class PhaseStartUpsertLicenseModelMarkupsTypedDict(TypedDict):
    markup: NotRequired[float]
    input_cost: NotRequired[float]
    output_cost: NotRequired[float]


class PhaseStartUpsertLicenseModelMarkups(BaseModel):
    markup: Optional[float] = None

    input_cost: Optional[float] = None

    output_cost: Optional[float] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["markup", "input_cost", "output_cost"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class PhaseStartUpsertLicenseMarkupsTypedDict(TypedDict):
    r"""For AI credit system features: replaces the feature's markup chain entirely for customers on this plan. An unset level means no markup at that level rather than inheriting the feature's."""

    default_markup: NotRequired[float]
    r"""Default percentage markup for customers on this plan. Use -100 to make usage free."""
    provider_markups: NotRequired[
        Nullable[Dict[str, PhaseStartUpsertLicenseProviderMarkupsTypedDict]]
    ]
    r"""Per-provider markup percentages for customers on this plan."""
    model_markups: NotRequired[
        Nullable[Dict[str, PhaseStartUpsertLicenseModelMarkupsTypedDict]]
    ]
    r"""Per-model markup overrides for customers on this plan."""


class PhaseStartUpsertLicenseMarkups(BaseModel):
    r"""For AI credit system features: replaces the feature's markup chain entirely for customers on this plan. An unset level means no markup at that level rather than inheriting the feature's."""

    default_markup: Optional[float] = None
    r"""Default percentage markup for customers on this plan. Use -100 to make usage free."""

    provider_markups: OptionalNullable[
        Dict[str, PhaseStartUpsertLicenseProviderMarkups]
    ] = UNSET
    r"""Per-provider markup percentages for customers on this plan."""

    model_markups: OptionalNullable[Dict[str, PhaseStartUpsertLicenseModelMarkups]] = (
        UNSET
    )
    r"""Per-model markup overrides for customers on this plan."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["default_markup", "provider_markups", "model_markups"])
        nullable_fields = set(["provider_markups", "model_markups"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


class PhaseStartUpsertLicenseFeatureOverrideTypedDict(TypedDict):
    r"""Overrides fields of this item's feature for customers on this plan (e.g. a credit system's credit_schema)."""

    credit_schema: NotRequired[List[PhaseStartUpsertLicenseCreditSchemaUnionTypedDict]]
    r"""For credit system features: replaces the feature's credit_schema entirely for customers on this plan."""
    markups: NotRequired[PhaseStartUpsertLicenseMarkupsTypedDict]
    r"""For AI credit system features: replaces the feature's markup chain entirely for customers on this plan. An unset level means no markup at that level rather than inheriting the feature's."""


class PhaseStartUpsertLicenseFeatureOverride(BaseModel):
    r"""Overrides fields of this item's feature for customers on this plan (e.g. a credit system's credit_schema)."""

    credit_schema: Optional[List[PhaseStartUpsertLicenseCreditSchemaUnion]] = None
    r"""For credit system features: replaces the feature's credit_schema entirely for customers on this plan."""

    markups: Optional[PhaseStartUpsertLicenseMarkups] = None
    r"""For AI credit system features: replaces the feature's markup chain entirely for customers on this plan. An unset level means no markup at that level rather than inheriting the feature's."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["credit_schema", "markups"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class PhaseStartUpsertLicensePlanItemTypedDict(TypedDict):
    r"""Configuration for a feature item in a plan, including usage limits, pricing, and rollover settings."""

    feature_id: str
    r"""The ID of the feature to configure."""
    threshold_billing: NotRequired[
        Nullable[PhaseStartUpsertLicenseThresholdBillingTypedDict]
    ]
    r"""Bills this many feature units when outstanding overage reaches it."""
    included: NotRequired[float]
    r"""Number of free units included. Balance resets to this each interval for consumable features."""
    unlimited: NotRequired[bool]
    r"""If true, customer has unlimited access to this feature."""
    pooled: NotRequired[bool]
    r"""Whether entity-level grants contribute to a shared customer balance."""
    reset: NotRequired[PhaseStartUpsertLicenseResetTypedDict]
    r"""Reset configuration for consumable features. Omit for non-consumable features like seats."""
    price: NotRequired[PhaseStartUpsertLicensePriceTypedDict]
    r"""Pricing for usage beyond included units. Omit for free features."""
    proration: NotRequired[PhaseStartUpsertLicenseProrationTypedDict]
    r"""Proration settings for prepaid features. Controls mid-cycle quantity change billing."""
    rollover: NotRequired[PhaseStartUpsertLicenseRolloverTypedDict]
    r"""Rollover config for unused units. If set, unused included units carry over."""
    expiry: NotRequired[PhaseStartUpsertLicenseExpiryTypedDict]
    r"""Purchased units expire this long after each purchase. One-off prepaid consumable items only."""
    feature_override: NotRequired[PhaseStartUpsertLicenseFeatureOverrideTypedDict]
    r"""Overrides fields of this item's feature for customers on this plan (e.g. a credit system's credit_schema)."""


class PhaseStartUpsertLicensePlanItem(BaseModel):
    r"""Configuration for a feature item in a plan, including usage limits, pricing, and rollover settings."""

    feature_id: str
    r"""The ID of the feature to configure."""

    threshold_billing: OptionalNullable[PhaseStartUpsertLicenseThresholdBilling] = UNSET
    r"""Bills this many feature units when outstanding overage reaches it."""

    included: Optional[float] = None
    r"""Number of free units included. Balance resets to this each interval for consumable features."""

    unlimited: Optional[bool] = None
    r"""If true, customer has unlimited access to this feature."""

    pooled: Optional[bool] = False
    r"""Whether entity-level grants contribute to a shared customer balance."""

    reset: Optional[PhaseStartUpsertLicenseReset] = None
    r"""Reset configuration for consumable features. Omit for non-consumable features like seats."""

    price: Optional[PhaseStartUpsertLicensePrice] = None
    r"""Pricing for usage beyond included units. Omit for free features."""

    proration: Optional[PhaseStartUpsertLicenseProration] = None
    r"""Proration settings for prepaid features. Controls mid-cycle quantity change billing."""

    rollover: Optional[PhaseStartUpsertLicenseRollover] = None
    r"""Rollover config for unused units. If set, unused included units carry over."""

    expiry: Optional[PhaseStartUpsertLicenseExpiry] = None
    r"""Purchased units expire this long after each purchase. One-off prepaid consumable items only."""

    feature_override: Optional[PhaseStartUpsertLicenseFeatureOverride] = None
    r"""Overrides fields of this item's feature for customers on this plan (e.g. a credit system's credit_schema)."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "threshold_billing",
                "included",
                "unlimited",
                "pooled",
                "reset",
                "price",
                "proration",
                "rollover",
                "expiry",
                "feature_override",
            ]
        )
        nullable_fields = set(["threshold_billing"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


PhaseStartUpsertLicenseRemoveItemBillingMethod = Literal[
    "prepaid",
    "usage_based",
]
r"""Match items with this billing method (prepaid or usage_based)."""


PhaseStartIntervalUpsertLicenseRemoveItemEnum2 = Literal[
    "one_off",
    "minute",
    "hour",
    "day",
    "week",
    "month",
    "quarter",
    "semi_annual",
    "year",
]


PhaseStartIntervalUpsertLicenseRemoveItemEnum1 = Literal[
    "one_off",
    "week",
    "month",
    "quarter",
    "semi_annual",
    "year",
]


PhaseStartUpsertLicenseIntervalUnionTypedDict = TypeAliasType(
    "PhaseStartUpsertLicenseIntervalUnionTypedDict",
    Union[
        PhaseStartIntervalUpsertLicenseRemoveItemEnum1,
        PhaseStartIntervalUpsertLicenseRemoveItemEnum2,
    ],
)
r"""Match items with this interval. Accepts either a BillingInterval (price-side) or a ResetInterval (reset-side, includes day/hour/minute) so price-less items keyed by reset.interval can be disambiguated."""


PhaseStartUpsertLicenseIntervalUnion = TypeAliasType(
    "PhaseStartUpsertLicenseIntervalUnion",
    Union[
        PhaseStartIntervalUpsertLicenseRemoveItemEnum1,
        PhaseStartIntervalUpsertLicenseRemoveItemEnum2,
    ],
)
r"""Match items with this interval. Accepts either a BillingInterval (price-side) or a ResetInterval (reset-side, includes day/hour/minute) so price-less items keyed by reset.interval can be disambiguated."""


class PhaseStartUpsertLicensePlanItemFilterTypedDict(TypedDict):
    r"""Filter for matching plan items. All provided fields must match (AND)."""

    feature_id: NotRequired[str]
    r"""Match items linked to this feature."""
    billing_method: NotRequired[PhaseStartUpsertLicenseRemoveItemBillingMethod]
    r"""Match items with this billing method (prepaid or usage_based)."""
    interval: NotRequired[PhaseStartUpsertLicenseIntervalUnionTypedDict]
    r"""Match items with this interval. Accepts either a BillingInterval (price-side) or a ResetInterval (reset-side, includes day/hour/minute) so price-less items keyed by reset.interval can be disambiguated."""
    interval_count: NotRequired[int]
    r"""Match items with this interval_count. Disambiguates between items that share an interval but differ in count."""
    included: NotRequired[float]
    r"""Match items whose grant equals this included usage. Omitted is a wildcard."""


class PhaseStartUpsertLicensePlanItemFilter(BaseModel):
    r"""Filter for matching plan items. All provided fields must match (AND)."""

    feature_id: Optional[str] = None
    r"""Match items linked to this feature."""

    billing_method: Optional[PhaseStartUpsertLicenseRemoveItemBillingMethod] = None
    r"""Match items with this billing method (prepaid or usage_based)."""

    interval: Optional[PhaseStartUpsertLicenseIntervalUnion] = None
    r"""Match items with this interval. Accepts either a BillingInterval (price-side) or a ResetInterval (reset-side, includes day/hour/minute) so price-less items keyed by reset.interval can be disambiguated."""

    interval_count: Optional[int] = None
    r"""Match items with this interval_count. Disambiguates between items that share an interval but differ in count."""

    included: Optional[float] = None
    r"""Match items whose grant equals this included usage. Omitted is a wildcard."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            ["feature_id", "billing_method", "interval", "interval_count", "included"]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class PhaseStartUpsertLicenseCustomizeTypedDict(TypedDict):
    price: NotRequired[Nullable[PhaseStartUpsertLicenseBasePriceTypedDict]]
    add_items: NotRequired[List[PhaseStartUpsertLicensePlanItemTypedDict]]
    remove_items: NotRequired[List[PhaseStartUpsertLicensePlanItemFilterTypedDict]]


class PhaseStartUpsertLicenseCustomize(BaseModel):
    price: OptionalNullable[PhaseStartUpsertLicenseBasePrice] = UNSET

    add_items: Optional[List[PhaseStartUpsertLicensePlanItem]] = None

    remove_items: Optional[List[PhaseStartUpsertLicensePlanItemFilter]] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["price", "add_items", "remove_items"])
        nullable_fields = set(["price"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


class PhaseStartUpsertLicenseTypedDict(TypedDict):
    license_plan_id: str
    version_slug: NotRequired[str]
    included: NotRequired[int]
    prepaid_only: NotRequired[bool]
    customize: NotRequired[Nullable[PhaseStartUpsertLicenseCustomizeTypedDict]]
    metadata: NotRequired[Dict[str, Any]]


class PhaseStartUpsertLicense(BaseModel):
    license_plan_id: str

    version_slug: Optional[str] = None

    included: Optional[int] = None

    prepaid_only: Optional[bool] = None

    customize: OptionalNullable[PhaseStartUpsertLicenseCustomize] = UNSET

    metadata: Optional[Dict[str, Any]] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            ["version_slug", "included", "prepaid_only", "customize", "metadata"]
        )
        nullable_fields = set(["customize"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


class PhaseStartRemoveLicenseTypedDict(TypedDict):
    license_plan_id: str


class PhaseStartRemoveLicense(BaseModel):
    license_plan_id: str


class PhaseStartCustomizeTypedDict(TypedDict):
    r"""Customize the plan to schedule. Can override price, replace items, or patch items with add_items and remove_items."""

    price: NotRequired[Nullable[PhaseStartBasePriceTypedDict]]
    r"""Override the base price of the plan. Pass null to remove the base price."""
    items: NotRequired[List[PhaseStartItemPlanItemTypedDict]]
    r"""Override the items in the plan (PUT-style — replaces all existing items). Mutually exclusive with add_items / remove_items / deprecated update_items."""
    add_items: NotRequired[List[PhaseStartAddItemPlanItemTypedDict]]
    r"""Items to add to the plan."""
    remove_items: NotRequired[List[PhaseStartPlanItemFilterTypedDict]]
    r"""Filters selecting items to remove from the plan."""
    billing_controls: NotRequired[PhaseStartBillingControlsTypedDict]
    r"""Override the plan's billing controls (auto top-ups, spend limits, usage limits, usage alerts, overage allowed) for this customer."""
    upsert_licenses: NotRequired[List[PhaseStartUpsertLicenseTypedDict]]
    r"""License links to add or override for this customer, keyed by license_plan_id. Omitted fields inherit the plan catalog link (included defaults to 1 when the license is not in the catalog). A bare entry restores the license to pure catalog inheritance."""
    remove_licenses: NotRequired[List[PhaseStartRemoveLicenseTypedDict]]
    r"""License links to drop, keyed by license_plan_id. Parallel to remove_items."""


class PhaseStartCustomize(BaseModel):
    r"""Customize the plan to schedule. Can override price, replace items, or patch items with add_items and remove_items."""

    price: OptionalNullable[PhaseStartBasePrice] = UNSET
    r"""Override the base price of the plan. Pass null to remove the base price."""

    items: Optional[List[PhaseStartItemPlanItem]] = None
    r"""Override the items in the plan (PUT-style — replaces all existing items). Mutually exclusive with add_items / remove_items / deprecated update_items."""

    add_items: Optional[List[PhaseStartAddItemPlanItem]] = None
    r"""Items to add to the plan."""

    remove_items: Optional[List[PhaseStartPlanItemFilter]] = None
    r"""Filters selecting items to remove from the plan."""

    billing_controls: Optional[PhaseStartBillingControls] = None
    r"""Override the plan's billing controls (auto top-ups, spend limits, usage limits, usage alerts, overage allowed) for this customer."""

    upsert_licenses: Optional[List[PhaseStartUpsertLicense]] = None
    r"""License links to add or override for this customer, keyed by license_plan_id. Omitted fields inherit the plan catalog link (included defaults to 1 when the license is not in the catalog). A bare entry restores the license to pure catalog inheritance."""

    remove_licenses: Optional[List[PhaseStartRemoveLicense]] = None
    r"""License links to drop, keyed by license_plan_id. Parallel to remove_items."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "price",
                "items",
                "add_items",
                "remove_items",
                "billing_controls",
                "upsert_licenses",
                "remove_licenses",
            ]
        )
        nullable_fields = set(["price"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


class CreateSchedulePlan2TypedDict(TypedDict):
    plan_id: str
    r"""The ID of the plan to schedule in this phase."""
    entity_id: NotRequired[Nullable[str]]
    r"""The plan scope. Omit to inherit the request entity, pass null for customer-level, or pass an entity ID. On phases after the first, the entity must already be scoped by the first phase — a schedule cannot change scope mid-flight."""
    feature_quantities: NotRequired[List[PhaseStartFeatureQuantityTypedDict]]
    r"""Optional prepaid feature quantities for this phase's plan."""
    version: NotRequired[float]
    r"""Optional explicit plan version to schedule."""
    customize: NotRequired[PhaseStartCustomizeTypedDict]
    r"""Customize the plan to schedule. Can override price, replace items, or patch items with add_items and remove_items."""
    subscription_id: NotRequired[str]
    r"""A unique ID to identify this subscription. Useful when scheduling the same plan multiple times."""


class CreateSchedulePlan2(BaseModel):
    plan_id: str
    r"""The ID of the plan to schedule in this phase."""

    entity_id: OptionalNullable[str] = UNSET
    r"""The plan scope. Omit to inherit the request entity, pass null for customer-level, or pass an entity ID. On phases after the first, the entity must already be scoped by the first phase — a schedule cannot change scope mid-flight."""

    feature_quantities: Optional[List[PhaseStartFeatureQuantity]] = None
    r"""Optional prepaid feature quantities for this phase's plan."""

    version: Optional[float] = None
    r"""Optional explicit plan version to schedule."""

    customize: Optional[PhaseStartCustomize] = None
    r"""Customize the plan to schedule. Can override price, replace items, or patch items with add_items and remove_items."""

    subscription_id: Optional[str] = None
    r"""A unique ID to identify this subscription. Useful when scheduling the same plan multiple times."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "entity_id",
                "feature_quantities",
                "version",
                "customize",
                "subscription_id",
            ]
        )
        nullable_fields = set(["entity_id"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


CreateScheduleBillingCycleAnchor2 = Literal["phase_start",]
r"""Pass 'phase_start' to reset the Stripe billing cycle anchor when this phase starts."""


class PhaseStartTypedDict(TypedDict):
    plans: List[CreateSchedulePlan2TypedDict]
    r"""Plans to materialize for this phase."""
    starts_at: NotRequired[StartsAt2TypedDict]
    r"""When this phase should start, in epoch milliseconds, or 'now' for the immediate phase."""
    starting_after: NotRequired[StartingAfter2TypedDict]
    r"""Relative start offset from the previous resolved schedule phase."""
    billing_cycle_anchor: NotRequired[CreateScheduleBillingCycleAnchor2]
    r"""Pass 'phase_start' to reset the Stripe billing cycle anchor when this phase starts."""


class PhaseStart(BaseModel):
    plans: List[CreateSchedulePlan2]
    r"""Plans to materialize for this phase."""

    starts_at: Optional[StartsAt2] = None
    r"""When this phase should start, in epoch milliseconds, or 'now' for the immediate phase."""

    starting_after: Optional[StartingAfter2] = None
    r"""Relative start offset from the previous resolved schedule phase."""

    billing_cycle_anchor: Optional[CreateScheduleBillingCycleAnchor2] = None
    r"""Pass 'phase_start' to reset the Stripe billing cycle anchor when this phase starts."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["starts_at", "starting_after", "billing_cycle_anchor"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PhaseStartUnionTypedDict = PhaseStartTypedDict


PhaseStartUnion = PhaseStart


class CreateScheduleParamsTypedDict(TypedDict):
    customer_id: str
    r"""The ID of the customer to create the schedule for."""
    phases: List[PhaseStartUnionTypedDict]
    r"""Ordered phase definitions for the schedule."""
    entity_id: NotRequired[str]
    r"""Optional entity ID for an entity-scoped schedule."""
    free_trial: NotRequired[Nullable[CreateScheduleFreeTrialParamsTypedDict]]
    r"""Free trial configuration applied to every plan in the immediate phase."""
    currency: NotRequired[str]
    r"""Three-letter Stripe-supported currency code used to bill the immediate phase (for example, 'usd')."""
    invoice_mode: NotRequired[CreateScheduleInvoiceModeTypedDict]
    r"""Invoice mode creates and sends an invoice instead of charging the customer's payment method immediately for the first phase."""
    discounts: NotRequired[List[CreateScheduleAttachDiscountTypedDict]]
    r"""List of discounts to apply to the immediate phase. Each discount can be an Autumn reward ID, Stripe coupon ID, or Stripe promotion code."""
    success_url: NotRequired[str]
    r"""URL to redirect to after successful checkout."""
    checkout_session_params: NotRequired[Dict[str, Any]]
    r"""Additional parameters to pass into the creation of the Stripe checkout session."""
    redirect_mode: NotRequired[CreateScheduleRedirectMode]
    r"""Controls when to return a checkout URL for the immediate phase. 'always' forces a confirmation or checkout flow, 'if_required' only redirects when needed, and 'never' disables redirects."""
    billing_behavior: NotRequired[CreateScheduleBillingBehavior]
    r"""Whether to prorate the immediate phase. 'none' skips proration charges and credits."""
    no_billing_changes: NotRequired[bool]
    r"""If true, skips any billing changes for the schedule."""
    billing_cycle_anchor: Literal["now"]
    r"""Pass 'now' to reset the billing cycle anchor of the immediate phase to the current time."""
    enable_plan_immediately: NotRequired[bool]
    r"""If true, the immediate-phase cusProducts are activated immediately (and scheduled-phase cusProducts pre-inserted) even when payment is pending via Stripe checkout. The Autumn schedule rows are persisted on checkout.session.completed."""
    preserve_add_ons: NotRequired[bool]
    r"""Deprecated and ignored. Active plans the schedule does not declare are always retained."""
    unscheduled_plans: NotRequired[List[UnscheduledPlanTypedDict]]
    r"""Plans billed with the immediate phase that the schedule never expires or replaces. No phase may declare a plan in the same group and scope."""


class CreateScheduleParams(BaseModel):
    customer_id: str
    r"""The ID of the customer to create the schedule for."""

    phases: List[PhaseStartUnion]
    r"""Ordered phase definitions for the schedule."""

    entity_id: Optional[str] = None
    r"""Optional entity ID for an entity-scoped schedule."""

    free_trial: OptionalNullable[CreateScheduleFreeTrialParams] = UNSET
    r"""Free trial configuration applied to every plan in the immediate phase."""

    currency: Optional[str] = None
    r"""Three-letter Stripe-supported currency code used to bill the immediate phase (for example, 'usd')."""

    invoice_mode: Optional[CreateScheduleInvoiceMode] = None
    r"""Invoice mode creates and sends an invoice instead of charging the customer's payment method immediately for the first phase."""

    discounts: Optional[List[CreateScheduleAttachDiscount]] = None
    r"""List of discounts to apply to the immediate phase. Each discount can be an Autumn reward ID, Stripe coupon ID, or Stripe promotion code."""

    success_url: Optional[str] = None
    r"""URL to redirect to after successful checkout."""

    checkout_session_params: Optional[Dict[str, Any]] = None
    r"""Additional parameters to pass into the creation of the Stripe checkout session."""

    redirect_mode: Optional[CreateScheduleRedirectMode] = "if_required"
    r"""Controls when to return a checkout URL for the immediate phase. 'always' forces a confirmation or checkout flow, 'if_required' only redirects when needed, and 'never' disables redirects."""

    billing_behavior: Optional[CreateScheduleBillingBehavior] = None
    r"""Whether to prorate the immediate phase. 'none' skips proration charges and credits."""

    no_billing_changes: Optional[bool] = None
    r"""If true, skips any billing changes for the schedule."""

    billing_cycle_anchor: Annotated[
        Annotated[Optional[Literal["now"]], AfterValidator(validate_const("now"))],
        pydantic.Field(alias="billing_cycle_anchor"),
    ] = "now"
    r"""Pass 'now' to reset the billing cycle anchor of the immediate phase to the current time."""

    enable_plan_immediately: Optional[bool] = None
    r"""If true, the immediate-phase cusProducts are activated immediately (and scheduled-phase cusProducts pre-inserted) even when payment is pending via Stripe checkout. The Autumn schedule rows are persisted on checkout.session.completed."""

    preserve_add_ons: Optional[bool] = None
    r"""Deprecated and ignored. Active plans the schedule does not declare are always retained."""

    unscheduled_plans: Optional[List[UnscheduledPlan]] = None
    r"""Plans billed with the immediate phase that the schedule never expires or replaces. No phase may declare a plan in the same group and scope."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "entity_id",
                "free_trial",
                "currency",
                "invoice_mode",
                "discounts",
                "success_url",
                "checkout_session_params",
                "redirect_mode",
                "billing_behavior",
                "no_billing_changes",
                "billing_cycle_anchor",
                "enable_plan_immediately",
                "preserve_add_ons",
                "unscheduled_plans",
            ]
        )
        nullable_fields = set(["free_trial"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


CreateScheduleStatus = Union[
    Literal[
        "created",
        "pending_payment",
    ],
    UnrecognizedStr,
]
r"""Whether the schedule is fully created or waiting for payment or confirmation to complete."""


class PhaseResponseTypedDict(TypedDict):
    phase_id: str
    r"""The ID of the persisted phase row."""
    starts_at: float
    r"""When this phase starts, in epoch milliseconds."""
    customer_product_ids: List[str]
    r"""Customer products materialized for this phase."""


class PhaseResponse(BaseModel):
    phase_id: str
    r"""The ID of the persisted phase row."""

    starts_at: float
    r"""When this phase starts, in epoch milliseconds."""

    customer_product_ids: List[str]
    r"""Customer products materialized for this phase."""


class CreateScheduleInvoiceTypedDict(TypedDict):
    r"""Invoice details if an invoice was created. Only present when a charge was made."""

    status: Nullable[str]
    r"""The status of the invoice (e.g., 'paid', 'open', 'draft')."""
    stripe_id: str
    r"""The Stripe invoice ID."""
    total: float
    r"""The total amount of the invoice in cents."""
    currency: str
    r"""The three-letter ISO currency code (e.g., 'usd')."""
    hosted_invoice_url: Nullable[str]
    r"""URL to the hosted invoice page where the customer can view and pay the invoice."""


class CreateScheduleInvoice(BaseModel):
    r"""Invoice details if an invoice was created. Only present when a charge was made."""

    status: Nullable[str]
    r"""The status of the invoice (e.g., 'paid', 'open', 'draft')."""

    stripe_id: str
    r"""The Stripe invoice ID."""

    total: float
    r"""The total amount of the invoice in cents."""

    currency: str
    r"""The three-letter ISO currency code (e.g., 'usd')."""

    hosted_invoice_url: Nullable[str]
    r"""URL to the hosted invoice page where the customer can view and pay the invoice."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                m[k] = val

        return m


CreateScheduleCode = Union[
    Literal[
        "3ds_required",
        "payment_method_required",
        "payment_failed",
        "payment_processing",
    ],
    UnrecognizedStr,
]
r"""The type of action required to complete the payment."""


class CreateScheduleRequiredActionTypedDict(TypedDict):
    code: CreateScheduleCode
    r"""The type of action required to complete the payment."""
    reason: str
    r"""A human-readable explanation of why this action is required."""


class CreateScheduleRequiredAction(BaseModel):
    code: CreateScheduleCode
    r"""The type of action required to complete the payment."""

    reason: str
    r"""A human-readable explanation of why this action is required."""


class CreateScheduleResponseTypedDict(TypedDict):
    r"""OK"""

    customer_id: str
    r"""The ID of the customer."""
    entity_id: Nullable[str]
    r"""The entity ID for the schedule, or null when customer-level."""
    status: CreateScheduleStatus
    r"""Whether the schedule is fully created or waiting for payment or confirmation to complete."""
    schedule_id: Nullable[str]
    r"""The ID of the created schedule. Null when the schedule is waiting on Autumn checkout confirmation."""
    phases: List[PhaseResponseTypedDict]
    r"""Persisted phases in ascending starts_at order. Empty when waiting on Autumn checkout confirmation."""
    payment_url: Nullable[str]
    r"""URL to redirect the customer to complete payment. Null if no payment action is required."""
    invoice: NotRequired[CreateScheduleInvoiceTypedDict]
    r"""Invoice details if an invoice was created. Only present when a charge was made."""
    required_action: NotRequired[CreateScheduleRequiredActionTypedDict]


class CreateScheduleResponse(BaseModel):
    r"""OK"""

    customer_id: str
    r"""The ID of the customer."""

    entity_id: Nullable[str]
    r"""The entity ID for the schedule, or null when customer-level."""

    status: CreateScheduleStatus
    r"""Whether the schedule is fully created or waiting for payment or confirmation to complete."""

    schedule_id: Nullable[str]
    r"""The ID of the created schedule. Null when the schedule is waiting on Autumn checkout confirmation."""

    phases: List[PhaseResponse]
    r"""Persisted phases in ascending starts_at order. Empty when waiting on Autumn checkout confirmation."""

    payment_url: Nullable[str]
    r"""URL to redirect the customer to complete payment. Null if no payment action is required."""

    invoice: Optional[CreateScheduleInvoice] = None
    r"""Invoice details if an invoice was created. Only present when a charge was made."""

    required_action: Optional[CreateScheduleRequiredAction] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["invoice", "required_action"])
        nullable_fields = set(["entity_id", "schedule_id", "payment_url"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


try:
    PhaseStartUsageLimit.model_rebuild()
except NameError:
    pass
try:
    PhaseStartUsageAlert.model_rebuild()
except NameError:
    pass
try:
    PhaseStartDimensionsUpsertLicense3.model_rebuild()
except NameError:
    pass
try:
    PhaseStartDimensionsUpsertLicense1.model_rebuild()
except NameError:
    pass
try:
    PhaseStartCreditSchemaUpsertLicense1.model_rebuild()
except NameError:
    pass
try:
    CreateScheduleParams.model_rebuild()
except NameError:
    pass
