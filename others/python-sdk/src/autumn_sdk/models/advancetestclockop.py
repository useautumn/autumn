"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from autumn_sdk.types import BaseModel, UNSET_SENTINEL, UnrecognizedStr
from autumn_sdk.utils import FieldMetadata, HeaderMetadata
import pydantic
from pydantic import model_serializer
from typing import Literal, Optional, Union
from typing_extensions import Annotated, NotRequired, TypedDict


class AdvanceTestClockGlobalsTypedDict(TypedDict):
    x_api_version: NotRequired[str]


class AdvanceTestClockGlobals(BaseModel):
    x_api_version: Annotated[
        Optional[str],
        pydantic.Field(alias="x-api-version"),
        FieldMetadata(header=HeaderMetadata(style="simple", explode=False)),
    ] = "2.4.0"

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["x-api-version"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class AdvanceTestClockParamsTypedDict(TypedDict):
    customer_id: str
    r"""The ID of the customer whose Stripe test clock to advance."""
    frozen_time: int
    r"""Target time as a Unix timestamp in milliseconds. Rounded down to whole seconds; must be later than the current clock time."""


class AdvanceTestClockParams(BaseModel):
    customer_id: str
    r"""The ID of the customer whose Stripe test clock to advance."""

    frozen_time: int
    r"""Target time as a Unix timestamp in milliseconds. Rounded down to whole seconds; must be later than the current clock time."""


AdvanceTestClockStatus = Union[
    Literal[
        "advancing",
        "internal_failure",
        "ready",
    ],
    UnrecognizedStr,
]


class AdvanceTestClockResponseTypedDict(TypedDict):
    r"""OK"""

    customer_id: str
    frozen_time: int
    r"""The Stripe test clock's frozen time in milliseconds."""
    status: AdvanceTestClockStatus


class AdvanceTestClockResponse(BaseModel):
    r"""OK"""

    customer_id: str

    frozen_time: int
    r"""The Stripe test clock's frozen time in milliseconds."""

    status: AdvanceTestClockStatus
