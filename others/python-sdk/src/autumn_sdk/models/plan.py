"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from autumn_sdk.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
    UnrecognizedStr,
)
from autumn_sdk.utils import validate_const
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Any, Dict, List, Literal, Optional, Union
from typing_extensions import Annotated, NotRequired, TypeAliasType, TypedDict


class AdditionalCurrencyTypedDict(TypedDict):
    currency: str
    r"""Three-letter Stripe-supported currency code (e.g. 'eur', 'gbp')."""
    amount: float
    r"""Price amount in this currency. Set explicitly per currency, not converted from the base amount."""


class AdditionalCurrency(BaseModel):
    currency: str
    r"""Three-letter Stripe-supported currency code (e.g. 'eur', 'gbp')."""

    amount: float
    r"""Price amount in this currency. Set explicitly per currency, not converted from the base amount."""


PlanPriceInterval = Union[
    Literal[
        "one_off",
        "week",
        "month",
        "quarter",
        "semi_annual",
        "year",
    ],
    UnrecognizedStr,
]
r"""Billing interval (e.g. 'month', 'year')."""


class PlanPriceDisplayTypedDict(TypedDict):
    r"""Display text for showing this price in pricing pages."""

    primary_text: str
    r"""Main display text (e.g. '$10' or '100 messages')."""
    secondary_text: NotRequired[str]
    r"""Secondary display text (e.g. 'per month' or 'then $0.5 per 100')."""


class PlanPriceDisplay(BaseModel):
    r"""Display text for showing this price in pricing pages."""

    primary_text: str
    r"""Main display text (e.g. '$10' or '100 messages')."""

    secondary_text: Optional[str] = None
    r"""Secondary display text (e.g. 'per month' or 'then $0.5 per 100')."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["secondary_text"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class PlanPriceStripeTypedDict(TypedDict):
    price_id: str
    r"""Stripe price ID. For prepaid with included > 0 this is the V2 price."""


class PlanPriceStripe(BaseModel):
    price_id: str
    r"""Stripe price ID. For prepaid with included > 0 this is the V2 price."""


class PlanPriceProcessorsTypedDict(TypedDict):
    r"""Payment processors this base price is connected to. Omitted when unset."""

    stripe: NotRequired[Nullable[PlanPriceStripeTypedDict]]


class PlanPriceProcessors(BaseModel):
    r"""Payment processors this base price is connected to. Omitted when unset."""

    stripe: OptionalNullable[PlanPriceStripe] = UNSET

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["stripe"])
        nullable_fields = set(["stripe"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


class PlanPriceTypedDict(TypedDict):
    amount: float
    r"""Base price amount for the plan, in major currency units (e.g. dollars)."""
    interval: PlanPriceInterval
    r"""Billing interval (e.g. 'month', 'year')."""
    additional_currencies: NotRequired[List[AdditionalCurrencyTypedDict]]
    r"""Base price amounts in additional currencies. The base 'amount' is in the org's default currency."""
    interval_count: NotRequired[float]
    r"""Number of intervals per billing cycle. Defaults to 1."""
    display: NotRequired[PlanPriceDisplayTypedDict]
    r"""Display text for showing this price in pricing pages."""
    processors: NotRequired[PlanPriceProcessorsTypedDict]
    r"""Payment processors this base price is connected to. Omitted when unset."""


class PlanPrice(BaseModel):
    amount: float
    r"""Base price amount for the plan, in major currency units (e.g. dollars)."""

    interval: PlanPriceInterval
    r"""Billing interval (e.g. 'month', 'year')."""

    additional_currencies: Optional[List[AdditionalCurrency]] = None
    r"""Base price amounts in additional currencies. The base 'amount' is in the org's default currency."""

    interval_count: Optional[float] = None
    r"""Number of intervals per billing cycle. Defaults to 1."""

    display: Optional[PlanPriceDisplay] = None
    r"""Display text for showing this price in pricing pages."""

    processors: Optional[PlanPriceProcessors] = None
    r"""Payment processors this base price is connected to. Omitted when unset."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            ["additional_currencies", "interval_count", "display", "processors"]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PlanType = Union[
    Literal[
        "static",
        "boolean",
        "single_use",
        "continuous_use",
        "credit_system",
        "ai_credit_system",
    ],
    UnrecognizedStr,
]
r"""The type of the feature"""


class PlanFeatureDisplayTypedDict(TypedDict):
    singular: str
    r"""The singular display name for the feature."""
    plural: str
    r"""The plural display name for the feature."""


class PlanFeatureDisplay(BaseModel):
    singular: str
    r"""The singular display name for the feature."""

    plural: str
    r"""The plural display name for the feature."""


class PlanFeatureCreditSchemaTypedDict(TypedDict):
    metered_feature_id: str
    r"""The ID of the metered feature (should be a single_use feature)."""
    credit_cost: float
    r"""The credit cost of the metered feature."""


class PlanFeatureCreditSchema(BaseModel):
    metered_feature_id: str
    r"""The ID of the metered feature (should be a single_use feature)."""

    credit_cost: float
    r"""The credit cost of the metered feature."""


class PlanFeatureTypedDict(TypedDict):
    r"""The full feature object if expanded."""

    id: str
    r"""The ID of the feature, used to refer to it in other API calls like /track or /check."""
    type: PlanType
    r"""The type of the feature"""
    name: NotRequired[Nullable[str]]
    r"""The name of the feature."""
    display: NotRequired[Nullable[PlanFeatureDisplayTypedDict]]
    r"""Singular and plural display names for the feature."""
    credit_schema: NotRequired[Nullable[List[PlanFeatureCreditSchemaTypedDict]]]
    r"""Credit cost schema for credit system features."""
    archived: NotRequired[Nullable[bool]]
    r"""Whether or not the feature is archived."""


class PlanFeature(BaseModel):
    r"""The full feature object if expanded."""

    id: str
    r"""The ID of the feature, used to refer to it in other API calls like /track or /check."""

    type: PlanType
    r"""The type of the feature"""

    name: OptionalNullable[str] = UNSET
    r"""The name of the feature."""

    display: OptionalNullable[PlanFeatureDisplay] = UNSET
    r"""Singular and plural display names for the feature."""

    credit_schema: OptionalNullable[List[PlanFeatureCreditSchema]] = UNSET
    r"""Credit cost schema for credit system features."""

    archived: OptionalNullable[bool] = UNSET
    r"""Whether or not the feature is archived."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["name", "display", "credit_schema", "archived"])
        nullable_fields = set(["name", "display", "credit_schema", "archived"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


PlanResetItemInterval = Union[
    Literal[
        "one_off",
        "minute",
        "hour",
        "day",
        "week",
        "month",
        "quarter",
        "semi_annual",
        "year",
    ],
    UnrecognizedStr,
]
r"""The interval at which the feature balance resets (e.g. 'month', 'year'). For consumable features, usage resets to 0 and included units are restored."""


class PlanItemResetTypedDict(TypedDict):
    interval: PlanResetItemInterval
    r"""The interval at which the feature balance resets (e.g. 'month', 'year'). For consumable features, usage resets to 0 and included units are restored."""
    interval_count: NotRequired[float]
    r"""Number of intervals between resets. Defaults to 1."""


class PlanItemReset(BaseModel):
    interval: PlanResetItemInterval
    r"""The interval at which the feature balance resets (e.g. 'month', 'year'). For consumable features, usage resets to 0 and included units are restored."""

    interval_count: Optional[float] = None
    r"""Number of intervals between resets. Defaults to 1."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["interval_count"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class ItemAdditionalCurrencyTypedDict(TypedDict):
    currency: str
    r"""Three-letter Stripe-supported currency code (e.g. 'eur', 'gbp')."""
    amount: float
    r"""Price amount in this currency. Set explicitly per currency, not converted from the base amount."""


class ItemAdditionalCurrency(BaseModel):
    currency: str
    r"""Three-letter Stripe-supported currency code (e.g. 'eur', 'gbp')."""

    amount: float
    r"""Price amount in this currency. Set explicitly per currency, not converted from the base amount."""


PlanPriceItemToTypedDict = TypeAliasType("PlanPriceItemToTypedDict", Union[float, str])


PlanPriceItemTo = TypeAliasType("PlanPriceItemTo", Union[float, str])


class ItemTierAdditionalCurrencyTypedDict(TypedDict):
    currency: str
    r"""Three-letter Stripe-supported currency code (e.g. 'eur', 'gbp')."""
    amount: NotRequired[float]
    r"""Per-unit amount for this tier in this currency."""
    flat_amount: NotRequired[float]
    r"""Flat amount for this tier in this currency, if the tier uses one."""


class ItemTierAdditionalCurrency(BaseModel):
    currency: str
    r"""Three-letter Stripe-supported currency code (e.g. 'eur', 'gbp')."""

    amount: Optional[float] = None
    r"""Per-unit amount for this tier in this currency."""

    flat_amount: Optional[float] = None
    r"""Flat amount for this tier in this currency, if the tier uses one."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["amount", "flat_amount"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class PlanPriceItemTierTypedDict(TypedDict):
    to: PlanPriceItemToTypedDict
    amount: float
    flat_amount: NotRequired[float]
    additional_currencies: NotRequired[List[ItemTierAdditionalCurrencyTypedDict]]


class PlanPriceItemTier(BaseModel):
    to: PlanPriceItemTo

    amount: float

    flat_amount: Optional[float] = None

    additional_currencies: Optional[List[ItemTierAdditionalCurrency]] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["flat_amount", "additional_currencies"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PlanItemTierBehavior = Union[
    Literal[
        "graduated",
        "volume",
    ],
    UnrecognizedStr,
]


PlanPriceItemInterval = Union[
    Literal[
        "one_off",
        "week",
        "month",
        "quarter",
        "semi_annual",
        "year",
    ],
    UnrecognizedStr,
]
r"""Billing interval for this price. For consumable features, should match reset.interval."""


PlanItemBillingMethod = Union[
    Literal[
        "prepaid",
        "usage_based",
    ],
    UnrecognizedStr,
]
r"""'prepaid' for features like seats where customers pay upfront, 'usage_based' for pay-as-you-go after included usage."""


class PlanItemStripeTypedDict(TypedDict):
    price_id: str
    r"""Stripe price ID. For prepaid with included > 0 this is the V2 price."""


class PlanItemStripe(BaseModel):
    price_id: str
    r"""Stripe price ID. For prepaid with included > 0 this is the V2 price."""


class PlanItemProcessorsTypedDict(TypedDict):
    r"""Payment processors this item price is connected to. Omitted when unset."""

    stripe: NotRequired[Nullable[PlanItemStripeTypedDict]]


class PlanItemProcessors(BaseModel):
    r"""Payment processors this item price is connected to. Omitted when unset."""

    stripe: OptionalNullable[PlanItemStripe] = UNSET

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["stripe"])
        nullable_fields = set(["stripe"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


class PlanItemPriceTypedDict(TypedDict):
    interval: PlanPriceItemInterval
    r"""Billing interval for this price. For consumable features, should match reset.interval."""
    billing_units: float
    r"""Number of units per price increment. Usage is rounded UP to the nearest billing_units when billed (e.g. billing_units=100 means 101 usage rounds to 200)."""
    billing_method: PlanItemBillingMethod
    r"""'prepaid' for features like seats where customers pay upfront, 'usage_based' for pay-as-you-go after included usage."""
    max_purchase: Nullable[float]
    r"""Maximum units a customer can purchase beyond included. E.g. if included=100 and max_purchase=300, customer can use up to 400 total before usage is capped. Null for no limit."""
    amount: NotRequired[float]
    r"""Price per billing_units after included usage is consumed. Mutually exclusive with tiers."""
    additional_currencies: NotRequired[List[ItemAdditionalCurrencyTypedDict]]
    r"""Amounts in additional currencies for this flat price. The base 'amount' is in the org's default currency. Only valid with 'amount', not 'tiers' (tiered prices carry per-currency amounts on each tier)."""
    tiers: NotRequired[List[PlanPriceItemTierTypedDict]]
    r"""Tiered pricing configuration. Each tier's 'to' INCLUDES the included amount. Either 'tiers' or 'amount' is required."""
    tier_behavior: NotRequired[PlanItemTierBehavior]
    interval_count: NotRequired[float]
    r"""Number of intervals per billing cycle. Defaults to 1."""
    processors: NotRequired[PlanItemProcessorsTypedDict]
    r"""Payment processors this item price is connected to. Omitted when unset."""


class PlanItemPrice(BaseModel):
    interval: PlanPriceItemInterval
    r"""Billing interval for this price. For consumable features, should match reset.interval."""

    billing_units: float
    r"""Number of units per price increment. Usage is rounded UP to the nearest billing_units when billed (e.g. billing_units=100 means 101 usage rounds to 200)."""

    billing_method: PlanItemBillingMethod
    r"""'prepaid' for features like seats where customers pay upfront, 'usage_based' for pay-as-you-go after included usage."""

    max_purchase: Nullable[float]
    r"""Maximum units a customer can purchase beyond included. E.g. if included=100 and max_purchase=300, customer can use up to 400 total before usage is capped. Null for no limit."""

    amount: Optional[float] = None
    r"""Price per billing_units after included usage is consumed. Mutually exclusive with tiers."""

    additional_currencies: Optional[List[ItemAdditionalCurrency]] = None
    r"""Amounts in additional currencies for this flat price. The base 'amount' is in the org's default currency. Only valid with 'amount', not 'tiers' (tiered prices carry per-currency amounts on each tier)."""

    tiers: Optional[List[PlanPriceItemTier]] = None
    r"""Tiered pricing configuration. Each tier's 'to' INCLUDES the included amount. Either 'tiers' or 'amount' is required."""

    tier_behavior: Optional[PlanItemTierBehavior] = None

    interval_count: Optional[float] = None
    r"""Number of intervals per billing cycle. Defaults to 1."""

    processors: Optional[PlanItemProcessors] = None
    r"""Payment processors this item price is connected to. Omitted when unset."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "amount",
                "additional_currencies",
                "tiers",
                "tier_behavior",
                "interval_count",
                "processors",
            ]
        )
        nullable_fields = set(["max_purchase"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


class PlanItemDisplayTypedDict(TypedDict):
    r"""Display text for showing this item in pricing pages."""

    primary_text: str
    r"""Main display text (e.g. '$10' or '100 messages')."""
    secondary_text: NotRequired[str]
    r"""Secondary display text (e.g. 'per month' or 'then $0.5 per 100')."""


class PlanItemDisplay(BaseModel):
    r"""Display text for showing this item in pricing pages."""

    primary_text: str
    r"""Main display text (e.g. '$10' or '100 messages')."""

    secondary_text: Optional[str] = None
    r"""Secondary display text (e.g. 'per month' or 'then $0.5 per 100')."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["secondary_text"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


ItemExpiryDurationType = Union[
    Literal[
        "month",
        "forever",
    ],
    UnrecognizedStr,
]
r"""When rolled over units expire."""


class PlanItemRolloverTypedDict(TypedDict):
    r"""Rollover configuration for unused units. If set, unused included units roll over to the next period."""

    max: Nullable[float]
    r"""Maximum rollover units. Null for unlimited rollover."""
    expiry_duration_type: ItemExpiryDurationType
    r"""When rolled over units expire."""
    max_percentage: NotRequired[Nullable[float]]
    r"""Maximum rollover as a percentage (0-100) of included + prepaid grant. Mutually exclusive with max."""
    expiry_duration_length: NotRequired[float]
    r"""Number of periods before expiry."""


class PlanItemRollover(BaseModel):
    r"""Rollover configuration for unused units. If set, unused included units roll over to the next period."""

    max: Nullable[float]
    r"""Maximum rollover units. Null for unlimited rollover."""

    expiry_duration_type: ItemExpiryDurationType
    r"""When rolled over units expire."""

    max_percentage: OptionalNullable[float] = UNSET
    r"""Maximum rollover as a percentage (0-100) of included + prepaid grant. Mutually exclusive with max."""

    expiry_duration_length: Optional[float] = None
    r"""Number of periods before expiry."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["max_percentage", "expiry_duration_length"])
        nullable_fields = set(["max", "max_percentage"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


class PlanDimensionsItem4TypedDict(TypedDict):
    match: Dict[str, str]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""
    credit_cost: float
    r"""Credits consumed per billing-unit group when this dimension matches."""
    priority: NotRequired[int]
    r"""Breaks ties between dimensions that match the same number of keys. Higher wins."""


class PlanDimensionsItem4(BaseModel):
    match: Dict[str, str]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""

    credit_cost: float
    r"""Credits consumed per billing-unit group when this dimension matches."""

    priority: Optional[int] = None
    r"""Breaks ties between dimensions that match the same number of keys. Higher wins."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["priority"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PlanDimensionsToItemEnum2 = Literal["inf",]


PlanDimensionsItemToUnion2TypedDict = TypeAliasType(
    "PlanDimensionsItemToUnion2TypedDict", Union[float, PlanDimensionsToItemEnum2]
)
r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""


PlanDimensionsItemToUnion2 = TypeAliasType(
    "PlanDimensionsItemToUnion2", Union[float, PlanDimensionsToItemEnum2]
)
r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""


class PlanDimensionsItemTier2TypedDict(TypedDict):
    to: PlanDimensionsItemToUnion2TypedDict
    r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""
    credit_cost: float
    r"""Credits consumed per billing-unit group within this tier."""


class PlanDimensionsItemTier2(BaseModel):
    to: PlanDimensionsItemToUnion2
    r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""

    credit_cost: float
    r"""Credits consumed per billing-unit group within this tier."""


class PlanDimensionsItem3TypedDict(TypedDict):
    match: Dict[str, str]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""
    tiers: List[PlanDimensionsItemTier2TypedDict]
    priority: NotRequired[int]
    r"""Breaks ties between dimensions that match the same number of keys. Higher wins."""
    tier_behavior: Literal["graduated"]


class PlanDimensionsItem3(BaseModel):
    match: Dict[str, str]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""

    tiers: List[PlanDimensionsItemTier2]

    priority: Optional[int] = None
    r"""Breaks ties between dimensions that match the same number of keys. Higher wins."""

    tier_behavior: Annotated[
        Annotated[Literal["graduated"], AfterValidator(validate_const("graduated"))],
        pydantic.Field(alias="tier_behavior"),
    ] = "graduated"

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["priority"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PlanItemDimensionsUnion2TypedDict = TypeAliasType(
    "PlanItemDimensionsUnion2TypedDict",
    Union[PlanDimensionsItem4TypedDict, PlanDimensionsItem3TypedDict],
)


PlanItemDimensionsUnion2 = TypeAliasType(
    "PlanItemDimensionsUnion2", Union[PlanDimensionsItem4, PlanDimensionsItem3]
)


class PlanItemMultipliers2TypedDict(TypedDict):
    match: Dict[str, str]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""
    factor: NotRequired[float]
    r"""Multiplies the matched rate. All matching multipliers stack."""
    add: NotRequired[float]
    r"""Added to the rate after every factor is applied, in credits per billing-unit group."""


class PlanItemMultipliers2(BaseModel):
    match: Dict[str, str]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""

    factor: Optional[float] = None
    r"""Multiplies the matched rate. All matching multipliers stack."""

    add: Optional[float] = None
    r"""Added to the rate after every factor is applied, in credits per billing-unit group."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["factor", "add"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class PlanCreditSchemaItemFeatureOverride2TypedDict(TypedDict):
    metered_feature_id: str
    r"""ID of the metered feature that draws from this credit system."""
    credit_cost: float
    r"""Credits consumed per billing-unit group."""
    billing_units: NotRequired[float]
    r"""Number of metered-feature units priced together. Defaults to one when omitted."""
    dimensions: NotRequired[Dict[str, PlanItemDimensionsUnion2TypedDict]]
    r"""Named rates chosen by event properties. The most specific match sets the rate; with no match the item's own rate applies."""
    multipliers: NotRequired[Dict[str, PlanItemMultipliers2TypedDict]]
    r"""Named adjustments chosen by event properties. Every match applies: factors multiply, then adds are summed."""


class PlanCreditSchemaItemFeatureOverride2(BaseModel):
    metered_feature_id: str
    r"""ID of the metered feature that draws from this credit system."""

    credit_cost: float
    r"""Credits consumed per billing-unit group."""

    billing_units: Optional[float] = None
    r"""Number of metered-feature units priced together. Defaults to one when omitted."""

    dimensions: Optional[Dict[str, PlanItemDimensionsUnion2]] = None
    r"""Named rates chosen by event properties. The most specific match sets the rate; with no match the item's own rate applies."""

    multipliers: Optional[Dict[str, PlanItemMultipliers2]] = None
    r"""Named adjustments chosen by event properties. Every match applies: factors multiply, then adds are summed."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["billing_units", "dimensions", "multipliers"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class PlanDimensionsItem2TypedDict(TypedDict):
    match: Dict[str, str]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""
    credit_cost: float
    r"""Credits consumed per billing-unit group when this dimension matches."""
    priority: NotRequired[int]
    r"""Breaks ties between dimensions that match the same number of keys. Higher wins."""


class PlanDimensionsItem2(BaseModel):
    match: Dict[str, str]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""

    credit_cost: float
    r"""Credits consumed per billing-unit group when this dimension matches."""

    priority: Optional[int] = None
    r"""Breaks ties between dimensions that match the same number of keys. Higher wins."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["priority"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PlanDimensionsToItemEnum1 = Literal["inf",]


PlanDimensionsItemToUnion1TypedDict = TypeAliasType(
    "PlanDimensionsItemToUnion1TypedDict", Union[float, PlanDimensionsToItemEnum1]
)
r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""


PlanDimensionsItemToUnion1 = TypeAliasType(
    "PlanDimensionsItemToUnion1", Union[float, PlanDimensionsToItemEnum1]
)
r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""


class PlanDimensionsItemTier1TypedDict(TypedDict):
    to: PlanDimensionsItemToUnion1TypedDict
    r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""
    credit_cost: float
    r"""Credits consumed per billing-unit group within this tier."""


class PlanDimensionsItemTier1(BaseModel):
    to: PlanDimensionsItemToUnion1
    r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""

    credit_cost: float
    r"""Credits consumed per billing-unit group within this tier."""


class PlanDimensionsItem1TypedDict(TypedDict):
    match: Dict[str, str]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""
    tiers: List[PlanDimensionsItemTier1TypedDict]
    priority: NotRequired[int]
    r"""Breaks ties between dimensions that match the same number of keys. Higher wins."""
    tier_behavior: Literal["graduated"]


class PlanDimensionsItem1(BaseModel):
    match: Dict[str, str]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""

    tiers: List[PlanDimensionsItemTier1]

    priority: Optional[int] = None
    r"""Breaks ties between dimensions that match the same number of keys. Higher wins."""

    tier_behavior: Annotated[
        Annotated[Literal["graduated"], AfterValidator(validate_const("graduated"))],
        pydantic.Field(alias="tier_behavior"),
    ] = "graduated"

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["priority"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PlanItemDimensionsUnion1TypedDict = TypeAliasType(
    "PlanItemDimensionsUnion1TypedDict",
    Union[PlanDimensionsItem2TypedDict, PlanDimensionsItem1TypedDict],
)


PlanItemDimensionsUnion1 = TypeAliasType(
    "PlanItemDimensionsUnion1", Union[PlanDimensionsItem2, PlanDimensionsItem1]
)


class PlanItemMultipliers1TypedDict(TypedDict):
    match: Dict[str, str]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""
    factor: NotRequired[float]
    r"""Multiplies the matched rate. All matching multipliers stack."""
    add: NotRequired[float]
    r"""Added to the rate after every factor is applied, in credits per billing-unit group."""


class PlanItemMultipliers1(BaseModel):
    match: Dict[str, str]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""

    factor: Optional[float] = None
    r"""Multiplies the matched rate. All matching multipliers stack."""

    add: Optional[float] = None
    r"""Added to the rate after every factor is applied, in credits per billing-unit group."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["factor", "add"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PlanToItemEnum = Literal["inf",]


PlanItemFeatureOverrideToUnionTypedDict = TypeAliasType(
    "PlanItemFeatureOverrideToUnionTypedDict", Union[float, PlanToItemEnum]
)
r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""


PlanItemFeatureOverrideToUnion = TypeAliasType(
    "PlanItemFeatureOverrideToUnion", Union[float, PlanToItemEnum]
)
r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""


class PlanItemFeatureOverrideTierTypedDict(TypedDict):
    to: PlanItemFeatureOverrideToUnionTypedDict
    r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""
    credit_cost: float
    r"""Credits consumed per billing-unit group within this tier."""


class PlanItemFeatureOverrideTier(BaseModel):
    to: PlanItemFeatureOverrideToUnion
    r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""

    credit_cost: float
    r"""Credits consumed per billing-unit group within this tier."""


class PlanCreditSchemaItemFeatureOverride1TypedDict(TypedDict):
    metered_feature_id: str
    r"""ID of the metered feature that draws from this credit system."""
    tiers: List[PlanItemFeatureOverrideTierTypedDict]
    billing_units: NotRequired[float]
    r"""Number of metered-feature units priced together. Defaults to one when omitted."""
    dimensions: NotRequired[Dict[str, PlanItemDimensionsUnion1TypedDict]]
    r"""Named rates chosen by event properties. The most specific match sets the rate; with no match the item's own rate applies."""
    multipliers: NotRequired[Dict[str, PlanItemMultipliers1TypedDict]]
    r"""Named adjustments chosen by event properties. Every match applies: factors multiply, then adds are summed."""
    tier_behavior: Literal["graduated"]


class PlanCreditSchemaItemFeatureOverride1(BaseModel):
    metered_feature_id: str
    r"""ID of the metered feature that draws from this credit system."""

    tiers: List[PlanItemFeatureOverrideTier]

    billing_units: Optional[float] = None
    r"""Number of metered-feature units priced together. Defaults to one when omitted."""

    dimensions: Optional[Dict[str, PlanItemDimensionsUnion1]] = None
    r"""Named rates chosen by event properties. The most specific match sets the rate; with no match the item's own rate applies."""

    multipliers: Optional[Dict[str, PlanItemMultipliers1]] = None
    r"""Named adjustments chosen by event properties. Every match applies: factors multiply, then adds are summed."""

    tier_behavior: Annotated[
        Annotated[Literal["graduated"], AfterValidator(validate_const("graduated"))],
        pydantic.Field(alias="tier_behavior"),
    ] = "graduated"

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["billing_units", "dimensions", "multipliers"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PlanItemCreditSchemaUnionTypedDict = TypeAliasType(
    "PlanItemCreditSchemaUnionTypedDict",
    Union[
        PlanCreditSchemaItemFeatureOverride2TypedDict,
        PlanCreditSchemaItemFeatureOverride1TypedDict,
    ],
)


PlanItemCreditSchemaUnion = TypeAliasType(
    "PlanItemCreditSchemaUnion",
    Union[PlanCreditSchemaItemFeatureOverride2, PlanCreditSchemaItemFeatureOverride1],
)


class ItemFeatureOverrideTypedDict(TypedDict):
    r"""Overrides fields of this item's feature for customers on this plan (e.g. a credit system's credit_schema)."""

    credit_schema: NotRequired[List[PlanItemCreditSchemaUnionTypedDict]]
    r"""For credit system features: replaces the feature's credit_schema entirely for customers on this plan."""


class ItemFeatureOverride(BaseModel):
    r"""Overrides fields of this item's feature for customers on this plan (e.g. a credit system's credit_schema)."""

    credit_schema: Optional[List[PlanItemCreditSchemaUnion]] = None
    r"""For credit system features: replaces the feature's credit_schema entirely for customers on this plan."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["credit_schema"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class ItemTypedDict(TypedDict):
    feature_id: str
    r"""The ID of the feature this item configures."""
    included: float
    r"""Number of free units included. For consumable features, balance resets to this number each interval."""
    unlimited: bool
    r"""Whether the customer has unlimited access to this feature."""
    reset: Nullable[PlanItemResetTypedDict]
    r"""Reset configuration for consumable features. Null for non-consumable features like seats where usage persists across billing cycles."""
    price: Nullable[PlanItemPriceTypedDict]
    r"""Pricing configuration for usage beyond included units. Null if feature is entirely free."""
    feature: NotRequired[PlanFeatureTypedDict]
    r"""The full feature object if expanded."""
    pooled: NotRequired[bool]
    r"""Whether entity-level grants contribute to a shared customer balance."""
    display: NotRequired[PlanItemDisplayTypedDict]
    r"""Display text for showing this item in pricing pages."""
    rollover: NotRequired[PlanItemRolloverTypedDict]
    r"""Rollover configuration for unused units. If set, unused included units roll over to the next period."""
    feature_override: NotRequired[ItemFeatureOverrideTypedDict]
    r"""Overrides fields of this item's feature for customers on this plan (e.g. a credit system's credit_schema)."""


class Item(BaseModel):
    feature_id: str
    r"""The ID of the feature this item configures."""

    included: float
    r"""Number of free units included. For consumable features, balance resets to this number each interval."""

    unlimited: bool
    r"""Whether the customer has unlimited access to this feature."""

    reset: Nullable[PlanItemReset]
    r"""Reset configuration for consumable features. Null for non-consumable features like seats where usage persists across billing cycles."""

    price: Nullable[PlanItemPrice]
    r"""Pricing configuration for usage beyond included units. Null if feature is entirely free."""

    feature: Optional[PlanFeature] = None
    r"""The full feature object if expanded."""

    pooled: Optional[bool] = False
    r"""Whether entity-level grants contribute to a shared customer balance."""

    display: Optional[PlanItemDisplay] = None
    r"""Display text for showing this item in pricing pages."""

    rollover: Optional[PlanItemRollover] = None
    r"""Rollover configuration for unused units. If set, unused included units roll over to the next period."""

    feature_override: Optional[ItemFeatureOverride] = None
    r"""Overrides fields of this item's feature for customers on this plan (e.g. a credit system's credit_schema)."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            ["feature", "pooled", "display", "rollover", "feature_override"]
        )
        nullable_fields = set(["reset", "price"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


class PlanStripeTypedDict(TypedDict):
    product_id: str
    r"""Stripe product ID this plan is billed under."""
    additional_product_ids: NotRequired[List[str]]
    r"""Extra Stripe product IDs aliased to this plan."""


class PlanStripe(BaseModel):
    product_id: str
    r"""Stripe product ID this plan is billed under."""

    additional_product_ids: Optional[List[str]] = None
    r"""Extra Stripe product IDs aliased to this plan."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["additional_product_ids"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class FeatureQuantityTypedDict(TypedDict):
    feature_id: str
    quantity: NotRequired[float]


class FeatureQuantity(BaseModel):
    feature_id: str

    quantity: Optional[float] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["quantity"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class ProductTypedDict(TypedDict):
    product_id: str
    r"""RevenueCat product ID that grants this plan when purchased."""
    feature_quantities: NotRequired[List[FeatureQuantityTypedDict]]
    r"""Prepaid quantities granted when this specific RevenueCat product is purchased, in feature units."""


class Product(BaseModel):
    product_id: str
    r"""RevenueCat product ID that grants this plan when purchased."""

    feature_quantities: Optional[List[FeatureQuantity]] = None
    r"""Prepaid quantities granted when this specific RevenueCat product is purchased, in feature units."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["feature_quantities"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class PlanRevenuecatTypedDict(TypedDict):
    products: List[ProductTypedDict]
    r"""Every RevenueCat product that maps to this plan. Replaces the current set."""


class PlanRevenuecat(BaseModel):
    products: List[Product]
    r"""Every RevenueCat product that maps to this plan. Replaces the current set."""


class PlanProcessorsTypedDict(TypedDict):
    r"""Payment processors this plan is connected to. Omitted when unset."""

    stripe: NotRequired[Nullable[PlanStripeTypedDict]]
    revenuecat: NotRequired[Nullable[PlanRevenuecatTypedDict]]


class PlanProcessors(BaseModel):
    r"""Payment processors this plan is connected to. Omitted when unset."""

    stripe: OptionalNullable[PlanStripe] = UNSET

    revenuecat: OptionalNullable[PlanRevenuecat] = UNSET

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["stripe", "revenuecat"])
        nullable_fields = set(["stripe", "revenuecat"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


PlanDurationType = Union[
    Literal[
        "day",
        "month",
        "year",
    ],
    UnrecognizedStr,
]
r"""Unit of time for the trial duration ('day', 'month', 'year')."""


OnEnd = Union[
    Literal[
        "bill",
        "revert",
    ],
    UnrecognizedStr,
]


class FreeTrialTypedDict(TypedDict):
    r"""Free trial configuration. If set, new customers can try this plan before being charged."""

    duration_length: float
    r"""Number of duration_type periods the trial lasts."""
    duration_type: PlanDurationType
    r"""Unit of time for the trial duration ('day', 'month', 'year')."""
    card_required: bool
    r"""Whether a payment method is required to start the trial. If true, customer will be charged after trial ends."""
    on_end: NotRequired[Nullable[OnEnd]]
    r"""Behavior when the trial ends. 'bill' charges the customer (default). 'revert' expires the trial and restores the customer's previous plan."""


class FreeTrial(BaseModel):
    r"""Free trial configuration. If set, new customers can try this plan before being charged."""

    duration_length: float
    r"""Number of duration_type periods the trial lasts."""

    duration_type: PlanDurationType
    r"""Unit of time for the trial duration ('day', 'month', 'year')."""

    card_required: bool
    r"""Whether a payment method is required to start the trial. If true, customer will be charged after trial ends."""

    on_end: OptionalNullable[OnEnd] = UNSET
    r"""Behavior when the trial ends. 'bill' charges the customer (default). 'revert' expires the trial and restores the customer's previous plan."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["on_end"])
        nullable_fields = set(["on_end"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


PlanEnv = Union[
    Literal[
        "sandbox",
        "live",
    ],
    UnrecognizedStr,
]
r"""Environment this plan belongs to ('sandbox' or 'live')."""


class PlanConfigTypedDict(TypedDict):
    r"""Miscellaneous plan-level configuration flags."""

    ignore_past_due: NotRequired[bool]
    r"""If true, entitlements attached to this plan will still reset on schedule even when the customer's product is in a past_due state."""


class PlanConfig(BaseModel):
    r"""Miscellaneous plan-level configuration flags."""

    ignore_past_due: Optional[bool] = False
    r"""If true, entitlements attached to this plan will still reset on schedule even when the customer's product is in a past_due state."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["ignore_past_due"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PlanPurchaseLimitInterval = Union[
    Literal[
        "hour",
        "day",
        "week",
        "month",
    ],
    UnrecognizedStr,
]
r"""The time interval for the purchase limit window."""


class PlanPurchaseLimitTypedDict(TypedDict):
    r"""Optional rate limit to cap how often auto top-ups occur."""

    interval: PlanPurchaseLimitInterval
    r"""The time interval for the purchase limit window."""
    limit: float
    r"""Maximum number of auto top-ups allowed within the interval."""
    interval_count: NotRequired[float]
    r"""Number of intervals in the purchase limit window."""


class PlanPurchaseLimit(BaseModel):
    r"""Optional rate limit to cap how often auto top-ups occur."""

    interval: PlanPurchaseLimitInterval
    r"""The time interval for the purchase limit window."""

    limit: float
    r"""Maximum number of auto top-ups allowed within the interval."""

    interval_count: Optional[float] = 1
    r"""Number of intervals in the purchase limit window."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["interval_count"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class PlanAutoTopupTypedDict(TypedDict):
    feature_id: str
    r"""The ID of the feature (credit balance) to auto top-up."""
    threshold: float
    r"""When the balance drops below this threshold, an auto top-up will be purchased."""
    quantity: float
    r"""Amount of credits to add per auto top-up."""
    enabled: NotRequired[bool]
    r"""Whether auto top-up is enabled."""
    purchase_limit: NotRequired[PlanPurchaseLimitTypedDict]
    r"""Optional rate limit to cap how often auto top-ups occur."""
    invoice_mode: NotRequired[bool]
    r"""When true, auto top-up creates a send_invoice invoice instead of auto-charging."""


class PlanAutoTopup(BaseModel):
    feature_id: str
    r"""The ID of the feature (credit balance) to auto top-up."""

    threshold: float
    r"""When the balance drops below this threshold, an auto top-up will be purchased."""

    quantity: float
    r"""Amount of credits to add per auto top-up."""

    enabled: Optional[bool] = False
    r"""Whether auto top-up is enabled."""

    purchase_limit: Optional[PlanPurchaseLimit] = None
    r"""Optional rate limit to cap how often auto top-ups occur."""

    invoice_mode: Optional[bool] = None
    r"""When true, auto top-up creates a send_invoice invoice instead of auto-charging."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["enabled", "purchase_limit", "invoice_mode"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PlanLimitType = Union[
    Literal[
        "absolute",
        "usage_percentage",
    ],
    UnrecognizedStr,
]
r"""How overage_limit is interpreted: an absolute overage cap (default) or a percentage of the main-plan allowance."""


class PlanSpendLimitTypedDict(TypedDict):
    feature_id: NotRequired[str]
    r"""Optional feature ID this spend limit applies to."""
    enabled: NotRequired[bool]
    r"""Whether the overage spend limit is enabled."""
    limit_type: NotRequired[PlanLimitType]
    r"""How overage_limit is interpreted: an absolute overage cap (default) or a percentage of the main-plan allowance."""
    overage_limit: NotRequired[float]
    r"""Overage cap for the feature: absolute units, or a percent (e.g. 120) when limit_type is usage_percentage."""
    skip_overage_billing: NotRequired[bool]
    r"""When true, overage for this feature is not posted to Stripe. Usage tracking and balance resets still behave normally."""


class PlanSpendLimit(BaseModel):
    feature_id: Optional[str] = None
    r"""Optional feature ID this spend limit applies to."""

    enabled: Optional[bool] = False
    r"""Whether the overage spend limit is enabled."""

    limit_type: Optional[PlanLimitType] = None
    r"""How overage_limit is interpreted: an absolute overage cap (default) or a percentage of the main-plan allowance."""

    overage_limit: Optional[float] = None
    r"""Overage cap for the feature: absolute units, or a percent (e.g. 120) when limit_type is usage_percentage."""

    skip_overage_billing: Optional[bool] = None
    r"""When true, overage for this feature is not posted to Stripe. Usage tracking and balance resets still behave normally."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "feature_id",
                "enabled",
                "limit_type",
                "overage_limit",
                "skip_overage_billing",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PlanUsageLimitInterval = Union[
    Literal[
        "day",
        "week",
        "month",
        "year",
    ],
    UnrecognizedStr,
]
r"""Interval for the cap, aligned to the customer's billing cycle."""


PlanAnchor = Union[
    Literal[
        "billing_cycle",
        "utc",
    ],
    UnrecognizedStr,
]
r"""Window alignment. 'billing_cycle' phases the interval to the customer's renewal time; 'utc' aligns to the UTC calendar."""


class PlanUsageLimitFilterTypedDict(TypedDict):
    r"""When set, only usage from events whose properties match counts toward this cap. Omit to count all usage of the feature."""

    properties: Dict[str, str]


class PlanUsageLimitFilter(BaseModel):
    r"""When set, only usage from events whose properties match counts toward this cap. Omit to count all usage of the feature."""

    properties: Dict[str, str]


class PlanUsageLimitTypedDict(TypedDict):
    feature_id: str
    r"""The feature this usage limit applies to."""
    limit: float
    r"""Maximum units allowed per interval."""
    interval: PlanUsageLimitInterval
    r"""Interval for the cap, aligned to the customer's billing cycle."""
    enabled: NotRequired[bool]
    r"""Whether this usage limit is enabled."""
    anchor: NotRequired[PlanAnchor]
    r"""Window alignment. 'billing_cycle' phases the interval to the customer's renewal time; 'utc' aligns to the UTC calendar."""
    filter_: NotRequired[PlanUsageLimitFilterTypedDict]
    r"""When set, only usage from events whose properties match counts toward this cap. Omit to count all usage of the feature."""


class PlanUsageLimit(BaseModel):
    feature_id: str
    r"""The feature this usage limit applies to."""

    limit: float
    r"""Maximum units allowed per interval."""

    interval: PlanUsageLimitInterval
    r"""Interval for the cap, aligned to the customer's billing cycle."""

    enabled: Optional[bool] = True
    r"""Whether this usage limit is enabled."""

    anchor: Optional[PlanAnchor] = None
    r"""Window alignment. 'billing_cycle' phases the interval to the customer's renewal time; 'utc' aligns to the UTC calendar."""

    filter_: Annotated[
        Optional[PlanUsageLimitFilter], pydantic.Field(alias="filter")
    ] = None
    r"""When set, only usage from events whose properties match counts toward this cap. Omit to count all usage of the feature."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["enabled", "anchor", "filter"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PlanThresholdType = Union[
    Literal[
        "usage",
        "usage_percentage",
        "remaining",
        "remaining_percentage",
    ],
    UnrecognizedStr,
]
r"""Whether the threshold is an absolute count or a percentage of the usage allowance or remaining balance."""


PlanBasis = Union[
    Literal[
        "balance",
        "included",
        "recurring",
        "usage_limit",
    ],
    UnrecognizedStr,
]
r"""What 100% means. balance: every grant on the feature. included: the plan allowance only. recurring: grants that reset. usage_limit: the cap of the usage limit with the same feature and filter."""


class PlanUsageAlertFilterTypedDict(TypedDict):
    r"""Only valid with basis usage_limit. Points the alert at the usage limit carrying the same filter."""

    properties: Dict[str, str]


class PlanUsageAlertFilter(BaseModel):
    r"""Only valid with basis usage_limit. Points the alert at the usage limit carrying the same filter."""

    properties: Dict[str, str]


class PlanUsageAlertTypedDict(TypedDict):
    threshold: float
    r"""The threshold value that triggers the alert. For usage or remaining, this is an absolute count. For usage_percentage or remaining_percentage, this is a percentage (0-100)."""
    threshold_type: PlanThresholdType
    r"""Whether the threshold is an absolute count or a percentage of the usage allowance or remaining balance."""
    feature_id: NotRequired[str]
    r"""The feature ID this alert applies to."""
    enabled: NotRequired[bool]
    r"""Whether this usage alert is enabled."""
    basis: NotRequired[PlanBasis]
    r"""What 100% means. balance: every grant on the feature. included: the plan allowance only. recurring: grants that reset. usage_limit: the cap of the usage limit with the same feature and filter."""
    filter_: NotRequired[PlanUsageAlertFilterTypedDict]
    r"""Only valid with basis usage_limit. Points the alert at the usage limit carrying the same filter."""
    name: NotRequired[str]
    r"""Optional user-defined label to distinguish multiple alerts on the same feature."""


class PlanUsageAlert(BaseModel):
    threshold: float
    r"""The threshold value that triggers the alert. For usage or remaining, this is an absolute count. For usage_percentage or remaining_percentage, this is a percentage (0-100)."""

    threshold_type: PlanThresholdType
    r"""Whether the threshold is an absolute count or a percentage of the usage allowance or remaining balance."""

    feature_id: Optional[str] = None
    r"""The feature ID this alert applies to."""

    enabled: Optional[bool] = True
    r"""Whether this usage alert is enabled."""

    basis: Optional[PlanBasis] = "balance"
    r"""What 100% means. balance: every grant on the feature. included: the plan allowance only. recurring: grants that reset. usage_limit: the cap of the usage limit with the same feature and filter."""

    filter_: Annotated[
        Optional[PlanUsageAlertFilter], pydantic.Field(alias="filter")
    ] = None
    r"""Only valid with basis usage_limit. Points the alert at the usage limit carrying the same filter."""

    name: Optional[str] = None
    r"""Optional user-defined label to distinguish multiple alerts on the same feature."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["feature_id", "enabled", "basis", "filter", "name"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class PlanOverageAllowedTypedDict(TypedDict):
    feature_id: str
    r"""The feature ID this overage allowed control applies to."""
    enabled: NotRequired[bool]
    r"""Whether overage is allowed for this feature."""


class PlanOverageAllowed(BaseModel):
    feature_id: str
    r"""The feature ID this overage allowed control applies to."""

    enabled: Optional[bool] = False
    r"""Whether overage is allowed for this feature."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["enabled"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class PlanBillingControlsTypedDict(TypedDict):
    r"""Plan-level billing controls used as customer defaults."""

    auto_topups: NotRequired[List[PlanAutoTopupTypedDict]]
    r"""List of auto top-up configurations per feature."""
    spend_limits: NotRequired[List[PlanSpendLimitTypedDict]]
    r"""List of overage spend limits per feature (caps overage spend)."""
    usage_limits: NotRequired[List[PlanUsageLimitTypedDict]]
    r"""List of hard usage caps per feature (max units per interval)."""
    usage_alerts: NotRequired[List[PlanUsageAlertTypedDict]]
    r"""List of usage alert configurations per feature."""
    overage_allowed: NotRequired[List[PlanOverageAllowedTypedDict]]
    r"""List of overage allowed controls per feature. When enabled, usage can exceed balance."""


class PlanBillingControls(BaseModel):
    r"""Plan-level billing controls used as customer defaults."""

    auto_topups: Optional[List[PlanAutoTopup]] = None
    r"""List of auto top-up configurations per feature."""

    spend_limits: Optional[List[PlanSpendLimit]] = None
    r"""List of overage spend limits per feature (caps overage spend)."""

    usage_limits: Optional[List[PlanUsageLimit]] = None
    r"""List of hard usage caps per feature (max units per interval)."""

    usage_alerts: Optional[List[PlanUsageAlert]] = None
    r"""List of usage alert configurations per feature."""

    overage_allowed: Optional[List[PlanOverageAllowed]] = None
    r"""List of overage allowed controls per feature. When enabled, usage can exceed balance."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "auto_topups",
                "spend_limits",
                "usage_limits",
                "usage_alerts",
                "overage_allowed",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PlanStatus = Union[
    Literal[
        "active",
        "scheduled",
    ],
    UnrecognizedStr,
]
r"""The customer's current status with this plan. 'active' if attached, 'scheduled' if pending activation."""


AttachAction = Union[
    Literal[
        "activate",
        "upgrade",
        "downgrade",
        "none",
        "purchase",
    ],
    UnrecognizedStr,
]
r"""The action that would occur if this plan were attached to the customer."""


class CustomerEligibilityTypedDict(TypedDict):
    attach_action: AttachAction
    r"""The action that would occur if this plan were attached to the customer."""
    trial_available: NotRequired[bool]
    r"""Whether the trial on this plan is available to this customer. For example, if the customer used the trial in the past, this will be false."""
    status: NotRequired[PlanStatus]
    r"""The customer's current status with this plan. 'active' if attached, 'scheduled' if pending activation."""
    canceling: NotRequired[bool]
    r"""Whether the customer's active instance of this plan is set to cancel."""
    trialing: NotRequired[bool]
    r"""Whether the customer is currently on a free trial of this plan."""


class CustomerEligibility(BaseModel):
    attach_action: AttachAction
    r"""The action that would occur if this plan were attached to the customer."""

    trial_available: Optional[bool] = None
    r"""Whether the trial on this plan is available to this customer. For example, if the customer used the trial in the past, this will be false."""

    status: Optional[PlanStatus] = None
    r"""The customer's current status with this plan. 'active' if attached, 'scheduled' if pending activation."""

    canceling: Optional[bool] = None
    r"""Whether the customer's active instance of this plan is set to cancel."""

    trialing: Optional[bool] = None
    r"""Whether the customer is currently on a free trial of this plan."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["trial_available", "status", "canceling", "trialing"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PlanPriceVariantDetailsInterval = Union[
    Literal[
        "one_off",
        "week",
        "month",
        "quarter",
        "semi_annual",
        "year",
    ],
    UnrecognizedStr,
]
r"""Billing interval (e.g. 'month', 'year')."""


class VariantDetailsAdditionalCurrencyTypedDict(TypedDict):
    currency: str
    r"""Three-letter Stripe-supported currency code (e.g. 'eur', 'gbp')."""
    amount: float
    r"""Price amount in this currency. Set explicitly per currency, not converted from the base amount."""


class VariantDetailsAdditionalCurrency(BaseModel):
    currency: str
    r"""Three-letter Stripe-supported currency code (e.g. 'eur', 'gbp')."""

    amount: float
    r"""Price amount in this currency. Set explicitly per currency, not converted from the base amount."""


class BasePriceTypedDict(TypedDict):
    r"""Base price configuration for a plan."""

    amount: float
    r"""Base price amount for the plan, in major currency units (e.g. dollars)."""
    interval: PlanPriceVariantDetailsInterval
    r"""Billing interval (e.g. 'month', 'year')."""
    interval_count: NotRequired[float]
    r"""Number of intervals per billing cycle. Defaults to 1."""
    additional_currencies: NotRequired[List[VariantDetailsAdditionalCurrencyTypedDict]]
    r"""Base price amounts in additional currencies. The base 'amount' is in the org's default currency."""


class BasePrice(BaseModel):
    r"""Base price configuration for a plan."""

    amount: float
    r"""Base price amount for the plan, in major currency units (e.g. dollars)."""

    interval: PlanPriceVariantDetailsInterval
    r"""Billing interval (e.g. 'month', 'year')."""

    interval_count: Optional[float] = None
    r"""Number of intervals per billing cycle. Defaults to 1."""

    additional_currencies: Optional[List[VariantDetailsAdditionalCurrency]] = None
    r"""Base price amounts in additional currencies. The base 'amount' is in the org's default currency."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["interval_count", "additional_currencies"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PlanAddItemResetInterval = Union[
    Literal[
        "one_off",
        "minute",
        "hour",
        "day",
        "week",
        "month",
        "quarter",
        "semi_annual",
        "year",
    ],
    UnrecognizedStr,
]
r"""Interval at which balance resets (e.g. 'month', 'year'). For consumable features only."""


class PlanVariantDetailsResetTypedDict(TypedDict):
    r"""Reset configuration for consumable features. Omit for non-consumable features like seats."""

    interval: PlanAddItemResetInterval
    r"""Interval at which balance resets (e.g. 'month', 'year'). For consumable features only."""
    interval_count: NotRequired[float]
    r"""Number of intervals between resets. Defaults to 1."""


class PlanVariantDetailsReset(BaseModel):
    r"""Reset configuration for consumable features. Omit for non-consumable features like seats."""

    interval: PlanAddItemResetInterval
    r"""Interval at which balance resets (e.g. 'month', 'year'). For consumable features only."""

    interval_count: Optional[float] = None
    r"""Number of intervals between resets. Defaults to 1."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["interval_count"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class AddItemAdditionalCurrencyTypedDict(TypedDict):
    currency: str
    r"""Three-letter Stripe-supported currency code (e.g. 'eur', 'gbp')."""
    amount: float
    r"""Price amount in this currency. Set explicitly per currency, not converted from the base amount."""


class AddItemAdditionalCurrency(BaseModel):
    currency: str
    r"""Three-letter Stripe-supported currency code (e.g. 'eur', 'gbp')."""

    amount: float
    r"""Price amount in this currency. Set explicitly per currency, not converted from the base amount."""


PlanVariantDetailsPriceToTypedDict = TypeAliasType(
    "PlanVariantDetailsPriceToTypedDict", Union[float, str]
)


PlanVariantDetailsPriceTo = TypeAliasType(
    "PlanVariantDetailsPriceTo", Union[float, str]
)


class VariantDetailsTierAdditionalCurrencyTypedDict(TypedDict):
    currency: str
    r"""Three-letter Stripe-supported currency code (e.g. 'eur', 'gbp')."""
    amount: NotRequired[float]
    r"""Per-unit amount for this tier in this currency."""
    flat_amount: NotRequired[float]
    r"""Flat amount for this tier in this currency, if the tier uses one."""


class VariantDetailsTierAdditionalCurrency(BaseModel):
    currency: str
    r"""Three-letter Stripe-supported currency code (e.g. 'eur', 'gbp')."""

    amount: Optional[float] = None
    r"""Per-unit amount for this tier in this currency."""

    flat_amount: Optional[float] = None
    r"""Flat amount for this tier in this currency, if the tier uses one."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["amount", "flat_amount"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class PlanVariantDetailsPriceTierTypedDict(TypedDict):
    to: PlanVariantDetailsPriceToTypedDict
    amount: float
    flat_amount: NotRequired[float]
    additional_currencies: NotRequired[
        List[VariantDetailsTierAdditionalCurrencyTypedDict]
    ]


class PlanVariantDetailsPriceTier(BaseModel):
    to: PlanVariantDetailsPriceTo

    amount: float

    flat_amount: Optional[float] = None

    additional_currencies: Optional[List[VariantDetailsTierAdditionalCurrency]] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["flat_amount", "additional_currencies"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PlanVariantDetailsTierBehavior = Union[
    Literal[
        "graduated",
        "volume",
    ],
    UnrecognizedStr,
]


PlanAddItemPriceInterval = Union[
    Literal[
        "one_off",
        "week",
        "month",
        "quarter",
        "semi_annual",
        "year",
    ],
    UnrecognizedStr,
]
r"""Billing interval. For consumable features, should match reset.interval."""


PlanAddItemBillingMethod = Union[
    Literal[
        "prepaid",
        "usage_based",
    ],
    UnrecognizedStr,
]
r"""'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go."""


class PlanVariantDetailsPriceTypedDict(TypedDict):
    r"""Pricing for usage beyond included units. Omit for free features."""

    interval: PlanAddItemPriceInterval
    r"""Billing interval. For consumable features, should match reset.interval."""
    billing_method: PlanAddItemBillingMethod
    r"""'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go."""
    amount: NotRequired[float]
    r"""Price per billing_units after included usage. Either 'amount' or 'tiers' is required."""
    additional_currencies: NotRequired[List[AddItemAdditionalCurrencyTypedDict]]
    r"""Amounts in additional currencies for this flat price. The base 'amount' is in the org's default currency. Only valid with 'amount', not 'tiers'."""
    tiers: NotRequired[List[PlanVariantDetailsPriceTierTypedDict]]
    r"""Tiered pricing.  Either 'amount' or 'tiers' is required."""
    tier_behavior: NotRequired[PlanVariantDetailsTierBehavior]
    interval_count: NotRequired[float]
    r"""Number of intervals per billing cycle. Defaults to 1."""
    billing_units: NotRequired[float]
    r"""Units per price increment. Usage is rounded UP when billed (e.g. billing_units=100 means 101 rounds to 200)."""
    max_purchase: NotRequired[Nullable[float]]
    r"""Max units purchasable beyond included. E.g. included=100, max_purchase=300 allows 400 total. Null for no limit."""


class PlanVariantDetailsPrice(BaseModel):
    r"""Pricing for usage beyond included units. Omit for free features."""

    interval: PlanAddItemPriceInterval
    r"""Billing interval. For consumable features, should match reset.interval."""

    billing_method: PlanAddItemBillingMethod
    r"""'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go."""

    amount: Optional[float] = None
    r"""Price per billing_units after included usage. Either 'amount' or 'tiers' is required."""

    additional_currencies: Optional[List[AddItemAdditionalCurrency]] = None
    r"""Amounts in additional currencies for this flat price. The base 'amount' is in the org's default currency. Only valid with 'amount', not 'tiers'."""

    tiers: Optional[List[PlanVariantDetailsPriceTier]] = None
    r"""Tiered pricing.  Either 'amount' or 'tiers' is required."""

    tier_behavior: Optional[PlanVariantDetailsTierBehavior] = None

    interval_count: Optional[float] = 1
    r"""Number of intervals per billing cycle. Defaults to 1."""

    billing_units: Optional[float] = 1
    r"""Units per price increment. Usage is rounded UP when billed (e.g. billing_units=100 means 101 rounds to 200)."""

    max_purchase: OptionalNullable[float] = UNSET
    r"""Max units purchasable beyond included. E.g. included=100, max_purchase=300 allows 400 total. Null for no limit."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "amount",
                "additional_currencies",
                "tiers",
                "tier_behavior",
                "interval_count",
                "billing_units",
                "max_purchase",
            ]
        )
        nullable_fields = set(["max_purchase"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


OnIncrease = Union[
    Literal[
        "bill_immediately",
        "prorate_immediately",
        "prorate_next_cycle",
        "bill_next_cycle",
    ],
    UnrecognizedStr,
]
r"""Billing behavior when quantity increases mid-cycle."""


OnDecrease = Union[
    Literal[
        "prorate",
        "prorate_immediately",
        "prorate_next_cycle",
        "none",
        "no_prorations",
    ],
    UnrecognizedStr,
]
r"""Credit behavior when quantity decreases mid-cycle."""


class ProrationTypedDict(TypedDict):
    r"""Proration settings for prepaid features. Controls mid-cycle quantity change billing."""

    on_increase: OnIncrease
    r"""Billing behavior when quantity increases mid-cycle."""
    on_decrease: OnDecrease
    r"""Credit behavior when quantity decreases mid-cycle."""


class Proration(BaseModel):
    r"""Proration settings for prepaid features. Controls mid-cycle quantity change billing."""

    on_increase: OnIncrease
    r"""Billing behavior when quantity increases mid-cycle."""

    on_decrease: OnDecrease
    r"""Credit behavior when quantity decreases mid-cycle."""


VariantDetailsExpiryDurationType = Union[
    Literal[
        "month",
        "forever",
    ],
    UnrecognizedStr,
]
r"""When rolled over units expire."""


class PlanVariantDetailsRolloverTypedDict(TypedDict):
    r"""Rollover config for unused units. If set, unused included units carry over."""

    expiry_duration_type: VariantDetailsExpiryDurationType
    r"""When rolled over units expire."""
    max: NotRequired[float]
    r"""Max rollover units. Omit for unlimited rollover."""
    max_percentage: NotRequired[float]
    r"""Maximum rollover as a percentage (0-100) of included + prepaid grant. Mutually exclusive with max."""
    expiry_duration_length: NotRequired[float]
    r"""Number of periods before expiry."""


class PlanVariantDetailsRollover(BaseModel):
    r"""Rollover config for unused units. If set, unused included units carry over."""

    expiry_duration_type: VariantDetailsExpiryDurationType
    r"""When rolled over units expire."""

    max: Optional[float] = None
    r"""Max rollover units. Omit for unlimited rollover."""

    max_percentage: Optional[float] = None
    r"""Maximum rollover as a percentage (0-100) of included + prepaid grant. Mutually exclusive with max."""

    expiry_duration_length: Optional[float] = None
    r"""Number of periods before expiry."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["max", "max_percentage", "expiry_duration_length"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class PlanDimensionsVariantDetails4TypedDict(TypedDict):
    match: Dict[str, str]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""
    credit_cost: float
    r"""Credits consumed per billing-unit group when this dimension matches."""
    priority: NotRequired[int]
    r"""Breaks ties between dimensions that match the same number of keys. Higher wins."""


class PlanDimensionsVariantDetails4(BaseModel):
    match: Dict[str, str]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""

    credit_cost: float
    r"""Credits consumed per billing-unit group when this dimension matches."""

    priority: Optional[int] = None
    r"""Breaks ties between dimensions that match the same number of keys. Higher wins."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["priority"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PlanDimensionsToVariantDetailsEnum2 = Literal["inf",]


PlanDimensionsVariantDetailsToUnion2TypedDict = TypeAliasType(
    "PlanDimensionsVariantDetailsToUnion2TypedDict",
    Union[float, PlanDimensionsToVariantDetailsEnum2],
)
r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""


PlanDimensionsVariantDetailsToUnion2 = TypeAliasType(
    "PlanDimensionsVariantDetailsToUnion2",
    Union[float, PlanDimensionsToVariantDetailsEnum2],
)
r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""


class PlanDimensionsVariantDetailsTier2TypedDict(TypedDict):
    to: PlanDimensionsVariantDetailsToUnion2TypedDict
    r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""
    credit_cost: float
    r"""Credits consumed per billing-unit group within this tier."""


class PlanDimensionsVariantDetailsTier2(BaseModel):
    to: PlanDimensionsVariantDetailsToUnion2
    r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""

    credit_cost: float
    r"""Credits consumed per billing-unit group within this tier."""


class PlanDimensionsVariantDetails3TypedDict(TypedDict):
    match: Dict[str, str]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""
    tiers: List[PlanDimensionsVariantDetailsTier2TypedDict]
    priority: NotRequired[int]
    r"""Breaks ties between dimensions that match the same number of keys. Higher wins."""
    tier_behavior: Literal["graduated"]


class PlanDimensionsVariantDetails3(BaseModel):
    match: Dict[str, str]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""

    tiers: List[PlanDimensionsVariantDetailsTier2]

    priority: Optional[int] = None
    r"""Breaks ties between dimensions that match the same number of keys. Higher wins."""

    tier_behavior: Annotated[
        Annotated[Literal["graduated"], AfterValidator(validate_const("graduated"))],
        pydantic.Field(alias="tier_behavior"),
    ] = "graduated"

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["priority"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class PlanCreditSchemaVariantDetails2TypedDict(TypedDict):
    metered_feature_id: str
    r"""ID of the metered feature that draws from this credit system."""
    credit_cost: float
    r"""Credits consumed per billing-unit group."""
    billing_units: NotRequired[float]
    r"""Number of metered-feature units priced together. Defaults to one when omitted."""
    dimensions: NotRequired[Dict[str, PlanVariantDetailsDimensionsUnion2TypedDict]]
    r"""Named rates chosen by event properties. The most specific match sets the rate; with no match the item's own rate applies."""
    multipliers: NotRequired[Dict[str, PlanVariantDetailsMultipliers2TypedDict]]
    r"""Named adjustments chosen by event properties. Every match applies: factors multiply, then adds are summed."""


class PlanCreditSchemaVariantDetails2(BaseModel):
    metered_feature_id: str
    r"""ID of the metered feature that draws from this credit system."""

    credit_cost: float
    r"""Credits consumed per billing-unit group."""

    billing_units: Optional[float] = None
    r"""Number of metered-feature units priced together. Defaults to one when omitted."""

    dimensions: Optional[Dict[str, PlanVariantDetailsDimensionsUnion2]] = None
    r"""Named rates chosen by event properties. The most specific match sets the rate; with no match the item's own rate applies."""

    multipliers: Optional[Dict[str, PlanVariantDetailsMultipliers2]] = None
    r"""Named adjustments chosen by event properties. Every match applies: factors multiply, then adds are summed."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["billing_units", "dimensions", "multipliers"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class PlanDimensionsVariantDetails2TypedDict(TypedDict):
    match: Dict[str, str]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""
    credit_cost: float
    r"""Credits consumed per billing-unit group when this dimension matches."""
    priority: NotRequired[int]
    r"""Breaks ties between dimensions that match the same number of keys. Higher wins."""


class PlanDimensionsVariantDetails2(BaseModel):
    match: Dict[str, str]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""

    credit_cost: float
    r"""Credits consumed per billing-unit group when this dimension matches."""

    priority: Optional[int] = None
    r"""Breaks ties between dimensions that match the same number of keys. Higher wins."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["priority"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PlanVariantDetailsDimensionsUnion2TypedDict = TypeAliasType(
    "PlanVariantDetailsDimensionsUnion2TypedDict",
    Union[
        PlanDimensionsVariantDetails4TypedDict, PlanDimensionsVariantDetails3TypedDict
    ],
)


PlanVariantDetailsDimensionsUnion2 = TypeAliasType(
    "PlanVariantDetailsDimensionsUnion2",
    Union[PlanDimensionsVariantDetails4, PlanDimensionsVariantDetails3],
)


class PlanVariantDetailsMultipliers2TypedDict(TypedDict):
    match: Dict[str, str]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""
    factor: NotRequired[float]
    r"""Multiplies the matched rate. All matching multipliers stack."""
    add: NotRequired[float]
    r"""Added to the rate after every factor is applied, in credits per billing-unit group."""


class PlanVariantDetailsMultipliers2(BaseModel):
    match: Dict[str, str]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""

    factor: Optional[float] = None
    r"""Multiplies the matched rate. All matching multipliers stack."""

    add: Optional[float] = None
    r"""Added to the rate after every factor is applied, in credits per billing-unit group."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["factor", "add"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PlanDimensionsToVariantDetailsEnum1 = Literal["inf",]


PlanDimensionsVariantDetailsToUnion1TypedDict = TypeAliasType(
    "PlanDimensionsVariantDetailsToUnion1TypedDict",
    Union[float, PlanDimensionsToVariantDetailsEnum1],
)
r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""


PlanDimensionsVariantDetailsToUnion1 = TypeAliasType(
    "PlanDimensionsVariantDetailsToUnion1",
    Union[float, PlanDimensionsToVariantDetailsEnum1],
)
r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""


class PlanDimensionsVariantDetailsTier1TypedDict(TypedDict):
    to: PlanDimensionsVariantDetailsToUnion1TypedDict
    r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""
    credit_cost: float
    r"""Credits consumed per billing-unit group within this tier."""


class PlanDimensionsVariantDetailsTier1(BaseModel):
    to: PlanDimensionsVariantDetailsToUnion1
    r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""

    credit_cost: float
    r"""Credits consumed per billing-unit group within this tier."""


class PlanDimensionsVariantDetails1TypedDict(TypedDict):
    match: Dict[str, str]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""
    tiers: List[PlanDimensionsVariantDetailsTier1TypedDict]
    priority: NotRequired[int]
    r"""Breaks ties between dimensions that match the same number of keys. Higher wins."""
    tier_behavior: Literal["graduated"]


class PlanDimensionsVariantDetails1(BaseModel):
    match: Dict[str, str]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""

    tiers: List[PlanDimensionsVariantDetailsTier1]

    priority: Optional[int] = None
    r"""Breaks ties between dimensions that match the same number of keys. Higher wins."""

    tier_behavior: Annotated[
        Annotated[Literal["graduated"], AfterValidator(validate_const("graduated"))],
        pydantic.Field(alias="tier_behavior"),
    ] = "graduated"

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["priority"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PlanVariantDetailsDimensionsUnion1TypedDict = TypeAliasType(
    "PlanVariantDetailsDimensionsUnion1TypedDict",
    Union[
        PlanDimensionsVariantDetails2TypedDict, PlanDimensionsVariantDetails1TypedDict
    ],
)


PlanVariantDetailsDimensionsUnion1 = TypeAliasType(
    "PlanVariantDetailsDimensionsUnion1",
    Union[PlanDimensionsVariantDetails2, PlanDimensionsVariantDetails1],
)


class PlanVariantDetailsMultipliers1TypedDict(TypedDict):
    match: Dict[str, str]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""
    factor: NotRequired[float]
    r"""Multiplies the matched rate. All matching multipliers stack."""
    add: NotRequired[float]
    r"""Added to the rate after every factor is applied, in credits per billing-unit group."""


class PlanVariantDetailsMultipliers1(BaseModel):
    match: Dict[str, str]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""

    factor: Optional[float] = None
    r"""Multiplies the matched rate. All matching multipliers stack."""

    add: Optional[float] = None
    r"""Added to the rate after every factor is applied, in credits per billing-unit group."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["factor", "add"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PlanToVariantDetailsEnum = Literal["inf",]


PlanVariantDetailsFeatureOverrideToUnionTypedDict = TypeAliasType(
    "PlanVariantDetailsFeatureOverrideToUnionTypedDict",
    Union[float, PlanToVariantDetailsEnum],
)
r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""


PlanVariantDetailsFeatureOverrideToUnion = TypeAliasType(
    "PlanVariantDetailsFeatureOverrideToUnion", Union[float, PlanToVariantDetailsEnum]
)
r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""


class PlanVariantDetailsFeatureOverrideTierTypedDict(TypedDict):
    to: PlanVariantDetailsFeatureOverrideToUnionTypedDict
    r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""
    credit_cost: float
    r"""Credits consumed per billing-unit group within this tier."""


class PlanVariantDetailsFeatureOverrideTier(BaseModel):
    to: PlanVariantDetailsFeatureOverrideToUnion
    r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""

    credit_cost: float
    r"""Credits consumed per billing-unit group within this tier."""


class PlanCreditSchemaVariantDetails1TypedDict(TypedDict):
    metered_feature_id: str
    r"""ID of the metered feature that draws from this credit system."""
    tiers: List[PlanVariantDetailsFeatureOverrideTierTypedDict]
    billing_units: NotRequired[float]
    r"""Number of metered-feature units priced together. Defaults to one when omitted."""
    dimensions: NotRequired[Dict[str, PlanVariantDetailsDimensionsUnion1TypedDict]]
    r"""Named rates chosen by event properties. The most specific match sets the rate; with no match the item's own rate applies."""
    multipliers: NotRequired[Dict[str, PlanVariantDetailsMultipliers1TypedDict]]
    r"""Named adjustments chosen by event properties. Every match applies: factors multiply, then adds are summed."""
    tier_behavior: Literal["graduated"]


class PlanCreditSchemaVariantDetails1(BaseModel):
    metered_feature_id: str
    r"""ID of the metered feature that draws from this credit system."""

    tiers: List[PlanVariantDetailsFeatureOverrideTier]

    billing_units: Optional[float] = None
    r"""Number of metered-feature units priced together. Defaults to one when omitted."""

    dimensions: Optional[Dict[str, PlanVariantDetailsDimensionsUnion1]] = None
    r"""Named rates chosen by event properties. The most specific match sets the rate; with no match the item's own rate applies."""

    multipliers: Optional[Dict[str, PlanVariantDetailsMultipliers1]] = None
    r"""Named adjustments chosen by event properties. Every match applies: factors multiply, then adds are summed."""

    tier_behavior: Annotated[
        Annotated[Literal["graduated"], AfterValidator(validate_const("graduated"))],
        pydantic.Field(alias="tier_behavior"),
    ] = "graduated"

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["billing_units", "dimensions", "multipliers"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PlanVariantDetailsCreditSchemaUnionTypedDict = TypeAliasType(
    "PlanVariantDetailsCreditSchemaUnionTypedDict",
    Union[
        PlanCreditSchemaVariantDetails2TypedDict,
        PlanCreditSchemaVariantDetails1TypedDict,
    ],
)


PlanVariantDetailsCreditSchemaUnion = TypeAliasType(
    "PlanVariantDetailsCreditSchemaUnion",
    Union[PlanCreditSchemaVariantDetails2, PlanCreditSchemaVariantDetails1],
)


class VariantDetailsFeatureOverrideTypedDict(TypedDict):
    r"""Overrides fields of this item's feature for customers on this plan (e.g. a credit system's credit_schema)."""

    credit_schema: NotRequired[List[PlanVariantDetailsCreditSchemaUnionTypedDict]]
    r"""For credit system features: replaces the feature's credit_schema entirely for customers on this plan."""


class VariantDetailsFeatureOverride(BaseModel):
    r"""Overrides fields of this item's feature for customers on this plan (e.g. a credit system's credit_schema)."""

    credit_schema: Optional[List[PlanVariantDetailsCreditSchemaUnion]] = None
    r"""For credit system features: replaces the feature's credit_schema entirely for customers on this plan."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["credit_schema"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class PlanItemTypedDict(TypedDict):
    r"""Configuration for a feature item in a plan, including usage limits, pricing, and rollover settings."""

    feature_id: str
    r"""The ID of the feature to configure."""
    included: NotRequired[float]
    r"""Number of free units included. Balance resets to this each interval for consumable features."""
    unlimited: NotRequired[bool]
    r"""If true, customer has unlimited access to this feature."""
    pooled: NotRequired[bool]
    r"""Whether entity-level grants contribute to a shared customer balance."""
    reset: NotRequired[PlanVariantDetailsResetTypedDict]
    r"""Reset configuration for consumable features. Omit for non-consumable features like seats."""
    price: NotRequired[PlanVariantDetailsPriceTypedDict]
    r"""Pricing for usage beyond included units. Omit for free features."""
    proration: NotRequired[ProrationTypedDict]
    r"""Proration settings for prepaid features. Controls mid-cycle quantity change billing."""
    rollover: NotRequired[PlanVariantDetailsRolloverTypedDict]
    r"""Rollover config for unused units. If set, unused included units carry over."""
    feature_override: NotRequired[VariantDetailsFeatureOverrideTypedDict]
    r"""Overrides fields of this item's feature for customers on this plan (e.g. a credit system's credit_schema)."""


class PlanItem(BaseModel):
    r"""Configuration for a feature item in a plan, including usage limits, pricing, and rollover settings."""

    feature_id: str
    r"""The ID of the feature to configure."""

    included: Optional[float] = None
    r"""Number of free units included. Balance resets to this each interval for consumable features."""

    unlimited: Optional[bool] = None
    r"""If true, customer has unlimited access to this feature."""

    pooled: Optional[bool] = False
    r"""Whether entity-level grants contribute to a shared customer balance."""

    reset: Optional[PlanVariantDetailsReset] = None
    r"""Reset configuration for consumable features. Omit for non-consumable features like seats."""

    price: Optional[PlanVariantDetailsPrice] = None
    r"""Pricing for usage beyond included units. Omit for free features."""

    proration: Optional[Proration] = None
    r"""Proration settings for prepaid features. Controls mid-cycle quantity change billing."""

    rollover: Optional[PlanVariantDetailsRollover] = None
    r"""Rollover config for unused units. If set, unused included units carry over."""

    feature_override: Optional[VariantDetailsFeatureOverride] = None
    r"""Overrides fields of this item's feature for customers on this plan (e.g. a credit system's credit_schema)."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "included",
                "unlimited",
                "pooled",
                "reset",
                "price",
                "proration",
                "rollover",
                "feature_override",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PlanRemoveItemBillingMethod = Union[
    Literal[
        "prepaid",
        "usage_based",
    ],
    UnrecognizedStr,
]
r"""Match items with this billing method (prepaid or usage_based)."""


PlanIntervalRemoveItemEnum2 = Union[
    Literal[
        "one_off",
        "minute",
        "hour",
        "day",
        "week",
        "month",
        "quarter",
        "semi_annual",
        "year",
    ],
    UnrecognizedStr,
]


PlanIntervalRemoveItemEnum1 = Union[
    Literal[
        "one_off",
        "week",
        "month",
        "quarter",
        "semi_annual",
        "year",
    ],
    UnrecognizedStr,
]


PlanIntervalUnionTypedDict = TypeAliasType(
    "PlanIntervalUnionTypedDict",
    Union[PlanIntervalRemoveItemEnum1, PlanIntervalRemoveItemEnum2],
)
r"""Match items with this interval. Accepts either a BillingInterval (price-side) or a ResetInterval (reset-side, includes day/hour/minute) so price-less items keyed by reset.interval can be disambiguated."""


PlanIntervalUnion = TypeAliasType(
    "PlanIntervalUnion", Union[PlanIntervalRemoveItemEnum1, PlanIntervalRemoveItemEnum2]
)
r"""Match items with this interval. Accepts either a BillingInterval (price-side) or a ResetInterval (reset-side, includes day/hour/minute) so price-less items keyed by reset.interval can be disambiguated."""


class PlanItemFilterTypedDict(TypedDict):
    r"""Filter for matching plan items. All provided fields must match (AND)."""

    feature_id: NotRequired[str]
    r"""Match items linked to this feature."""
    billing_method: NotRequired[PlanRemoveItemBillingMethod]
    r"""Match items with this billing method (prepaid or usage_based)."""
    interval: NotRequired[PlanIntervalUnionTypedDict]
    r"""Match items with this interval. Accepts either a BillingInterval (price-side) or a ResetInterval (reset-side, includes day/hour/minute) so price-less items keyed by reset.interval can be disambiguated."""
    interval_count: NotRequired[int]
    r"""Match items with this interval_count. Disambiguates between items that share an interval but differ in count."""
    included: NotRequired[float]
    r"""Match items whose grant equals this included usage. Omitted is a wildcard."""


class PlanItemFilter(BaseModel):
    r"""Filter for matching plan items. All provided fields must match (AND)."""

    feature_id: Optional[str] = None
    r"""Match items linked to this feature."""

    billing_method: Optional[PlanRemoveItemBillingMethod] = None
    r"""Match items with this billing method (prepaid or usage_based)."""

    interval: Optional[PlanIntervalUnion] = None
    r"""Match items with this interval. Accepts either a BillingInterval (price-side) or a ResetInterval (reset-side, includes day/hour/minute) so price-less items keyed by reset.interval can be disambiguated."""

    interval_count: Optional[int] = None
    r"""Match items with this interval_count. Disambiguates between items that share an interval but differ in count."""

    included: Optional[float] = None
    r"""Match items whose grant equals this included usage. Omitted is a wildcard."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            ["feature_id", "billing_method", "interval", "interval_count", "included"]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PlanVariantDetailsDurationType1 = Union[
    Literal[
        "day",
        "month",
        "year",
    ],
    UnrecognizedStr,
]
r"""Unit of time for the trial ('day', 'month', 'year')."""


VariantDetailsOnEnd = Union[
    Literal[
        "bill",
        "revert",
    ],
    UnrecognizedStr,
]
r"""Behavior when the trial ends. 'bill' charges the customer (default). 'revert' expires the trial and restores the customer's previous plan."""


class FreeTrialParamsTypedDict(TypedDict):
    r"""Free trial configuration for a plan."""

    duration_length: float
    r"""Number of duration_type periods the trial lasts."""
    duration_type: NotRequired[PlanVariantDetailsDurationType1]
    r"""Unit of time for the trial ('day', 'month', 'year')."""
    card_required: NotRequired[bool]
    r"""If true, a payment method is required to start the trial and the customer is charged when it ends. Defaults to false."""
    on_end: NotRequired[VariantDetailsOnEnd]
    r"""Behavior when the trial ends. 'bill' charges the customer (default). 'revert' expires the trial and restores the customer's previous plan."""


class FreeTrialParams(BaseModel):
    r"""Free trial configuration for a plan."""

    duration_length: float
    r"""Number of duration_type periods the trial lasts."""

    duration_type: Optional[PlanVariantDetailsDurationType1] = "month"
    r"""Unit of time for the trial ('day', 'month', 'year')."""

    card_required: Optional[bool] = False
    r"""If true, a payment method is required to start the trial and the customer is charged when it ends. Defaults to false."""

    on_end: Optional[VariantDetailsOnEnd] = None
    r"""Behavior when the trial ends. 'bill' charges the customer (default). 'revert' expires the trial and restores the customer's previous plan."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["duration_type", "card_required", "on_end"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PlanVariantDetailsPurchaseLimitInterval1 = Union[
    Literal[
        "hour",
        "day",
        "week",
        "month",
    ],
    UnrecognizedStr,
]
r"""The time interval for the purchase limit window."""


class PlanVariantDetailsPurchaseLimit1TypedDict(TypedDict):
    r"""Optional rate limit to cap how often auto top-ups occur. Pass count to set the current window's consumed top-ups."""

    interval: PlanVariantDetailsPurchaseLimitInterval1
    r"""The time interval for the purchase limit window."""
    limit: float
    r"""Maximum number of auto top-ups allowed within the interval."""
    interval_count: NotRequired[float]
    r"""Number of intervals in the purchase limit window."""
    count: NotRequired[float]
    r"""Set the current window's consumed auto top-up count. Omit to leave runtime state unchanged."""


class PlanVariantDetailsPurchaseLimit1(BaseModel):
    r"""Optional rate limit to cap how often auto top-ups occur. Pass count to set the current window's consumed top-ups."""

    interval: PlanVariantDetailsPurchaseLimitInterval1
    r"""The time interval for the purchase limit window."""

    limit: float
    r"""Maximum number of auto top-ups allowed within the interval."""

    interval_count: Optional[float] = 1
    r"""Number of intervals in the purchase limit window."""

    count: Optional[float] = None
    r"""Set the current window's consumed auto top-up count. Omit to leave runtime state unchanged."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["interval_count", "count"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class PlanVariantDetailsAutoTopup1TypedDict(TypedDict):
    feature_id: str
    r"""The ID of the feature (credit balance) to auto top-up."""
    threshold: float
    r"""When the balance drops below this threshold, an auto top-up will be purchased."""
    quantity: float
    r"""Amount of credits to add per auto top-up."""
    enabled: NotRequired[bool]
    r"""Whether auto top-up is enabled."""
    purchase_limit: NotRequired[PlanVariantDetailsPurchaseLimit1TypedDict]
    r"""Optional rate limit to cap how often auto top-ups occur. Pass count to set the current window's consumed top-ups."""
    invoice_mode: NotRequired[bool]
    r"""When true, auto top-up creates a send_invoice invoice instead of auto-charging."""


class PlanVariantDetailsAutoTopup1(BaseModel):
    feature_id: str
    r"""The ID of the feature (credit balance) to auto top-up."""

    threshold: float
    r"""When the balance drops below this threshold, an auto top-up will be purchased."""

    quantity: float
    r"""Amount of credits to add per auto top-up."""

    enabled: Optional[bool] = False
    r"""Whether auto top-up is enabled."""

    purchase_limit: Optional[PlanVariantDetailsPurchaseLimit1] = None
    r"""Optional rate limit to cap how often auto top-ups occur. Pass count to set the current window's consumed top-ups."""

    invoice_mode: Optional[bool] = None
    r"""When true, auto top-up creates a send_invoice invoice instead of auto-charging."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["enabled", "purchase_limit", "invoice_mode"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PlanVariantDetailsLimitType1 = Union[
    Literal[
        "absolute",
        "usage_percentage",
    ],
    UnrecognizedStr,
]
r"""How overage_limit is interpreted: an absolute overage cap (default) or a percentage of the main-plan allowance."""


class PlanVariantDetailsSpendLimit1TypedDict(TypedDict):
    feature_id: NotRequired[str]
    r"""Optional feature ID this spend limit applies to."""
    enabled: NotRequired[bool]
    r"""Whether the overage spend limit is enabled."""
    limit_type: NotRequired[PlanVariantDetailsLimitType1]
    r"""How overage_limit is interpreted: an absolute overage cap (default) or a percentage of the main-plan allowance."""
    overage_limit: NotRequired[float]
    r"""Overage cap for the feature: absolute units, or a percent (e.g. 120) when limit_type is usage_percentage."""
    skip_overage_billing: NotRequired[bool]
    r"""When true, overage for this feature is not posted to Stripe. Usage tracking and balance resets still behave normally."""


class PlanVariantDetailsSpendLimit1(BaseModel):
    feature_id: Optional[str] = None
    r"""Optional feature ID this spend limit applies to."""

    enabled: Optional[bool] = False
    r"""Whether the overage spend limit is enabled."""

    limit_type: Optional[PlanVariantDetailsLimitType1] = None
    r"""How overage_limit is interpreted: an absolute overage cap (default) or a percentage of the main-plan allowance."""

    overage_limit: Optional[float] = None
    r"""Overage cap for the feature: absolute units, or a percent (e.g. 120) when limit_type is usage_percentage."""

    skip_overage_billing: Optional[bool] = None
    r"""When true, overage for this feature is not posted to Stripe. Usage tracking and balance resets still behave normally."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "feature_id",
                "enabled",
                "limit_type",
                "overage_limit",
                "skip_overage_billing",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PlanVariantDetailsUsageLimitInterval1 = Union[
    Literal[
        "day",
        "week",
        "month",
        "year",
    ],
    UnrecognizedStr,
]
r"""Interval for the cap, aligned to the customer's billing cycle."""


PlanVariantDetailsAnchor1 = Union[
    Literal[
        "billing_cycle",
        "utc",
    ],
    UnrecognizedStr,
]
r"""Window alignment. 'billing_cycle' phases the interval to the customer's renewal time; 'utc' aligns to the UTC calendar."""


class PlanVariantDetailsUsageLimitFilter1TypedDict(TypedDict):
    r"""When set, only usage from events whose properties match counts toward this cap. Omit to count all usage of the feature."""

    properties: Dict[str, str]


class PlanVariantDetailsUsageLimitFilter1(BaseModel):
    r"""When set, only usage from events whose properties match counts toward this cap. Omit to count all usage of the feature."""

    properties: Dict[str, str]


class PlanVariantDetailsUsageLimit1TypedDict(TypedDict):
    feature_id: str
    r"""The feature this usage limit applies to."""
    limit: float
    r"""Maximum units allowed per interval."""
    interval: PlanVariantDetailsUsageLimitInterval1
    r"""Interval for the cap, aligned to the customer's billing cycle."""
    enabled: NotRequired[bool]
    r"""Whether this usage limit is enabled."""
    anchor: NotRequired[PlanVariantDetailsAnchor1]
    r"""Window alignment. 'billing_cycle' phases the interval to the customer's renewal time; 'utc' aligns to the UTC calendar."""
    filter_: NotRequired[PlanVariantDetailsUsageLimitFilter1TypedDict]
    r"""When set, only usage from events whose properties match counts toward this cap. Omit to count all usage of the feature."""


class PlanVariantDetailsUsageLimit1(BaseModel):
    feature_id: str
    r"""The feature this usage limit applies to."""

    limit: float
    r"""Maximum units allowed per interval."""

    interval: PlanVariantDetailsUsageLimitInterval1
    r"""Interval for the cap, aligned to the customer's billing cycle."""

    enabled: Optional[bool] = True
    r"""Whether this usage limit is enabled."""

    anchor: Optional[PlanVariantDetailsAnchor1] = None
    r"""Window alignment. 'billing_cycle' phases the interval to the customer's renewal time; 'utc' aligns to the UTC calendar."""

    filter_: Annotated[
        Optional[PlanVariantDetailsUsageLimitFilter1], pydantic.Field(alias="filter")
    ] = None
    r"""When set, only usage from events whose properties match counts toward this cap. Omit to count all usage of the feature."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["enabled", "anchor", "filter"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PlanVariantDetailsThresholdType1 = Union[
    Literal[
        "usage",
        "usage_percentage",
        "remaining",
        "remaining_percentage",
    ],
    UnrecognizedStr,
]
r"""Whether the threshold is an absolute count or a percentage of the usage allowance or remaining balance."""


PlanVariantDetailsBasis1 = Union[
    Literal[
        "balance",
        "included",
        "recurring",
        "usage_limit",
    ],
    UnrecognizedStr,
]
r"""What 100% means. balance: every grant on the feature. included: the plan allowance only. recurring: grants that reset. usage_limit: the cap of the usage limit with the same feature and filter."""


class PlanVariantDetailsUsageAlertFilter1TypedDict(TypedDict):
    r"""Only valid with basis usage_limit. Points the alert at the usage limit carrying the same filter."""

    properties: Dict[str, str]


class PlanVariantDetailsUsageAlertFilter1(BaseModel):
    r"""Only valid with basis usage_limit. Points the alert at the usage limit carrying the same filter."""

    properties: Dict[str, str]


class PlanVariantDetailsUsageAlert1TypedDict(TypedDict):
    threshold: float
    r"""The threshold value that triggers the alert. For usage or remaining, this is an absolute count. For usage_percentage or remaining_percentage, this is a percentage (0-100)."""
    threshold_type: PlanVariantDetailsThresholdType1
    r"""Whether the threshold is an absolute count or a percentage of the usage allowance or remaining balance."""
    feature_id: NotRequired[str]
    r"""The feature ID this alert applies to."""
    enabled: NotRequired[bool]
    r"""Whether this usage alert is enabled."""
    basis: NotRequired[PlanVariantDetailsBasis1]
    r"""What 100% means. balance: every grant on the feature. included: the plan allowance only. recurring: grants that reset. usage_limit: the cap of the usage limit with the same feature and filter."""
    filter_: NotRequired[PlanVariantDetailsUsageAlertFilter1TypedDict]
    r"""Only valid with basis usage_limit. Points the alert at the usage limit carrying the same filter."""
    name: NotRequired[str]
    r"""Optional user-defined label to distinguish multiple alerts on the same feature."""


class PlanVariantDetailsUsageAlert1(BaseModel):
    threshold: float
    r"""The threshold value that triggers the alert. For usage or remaining, this is an absolute count. For usage_percentage or remaining_percentage, this is a percentage (0-100)."""

    threshold_type: PlanVariantDetailsThresholdType1
    r"""Whether the threshold is an absolute count or a percentage of the usage allowance or remaining balance."""

    feature_id: Optional[str] = None
    r"""The feature ID this alert applies to."""

    enabled: Optional[bool] = True
    r"""Whether this usage alert is enabled."""

    basis: Optional[PlanVariantDetailsBasis1] = "balance"
    r"""What 100% means. balance: every grant on the feature. included: the plan allowance only. recurring: grants that reset. usage_limit: the cap of the usage limit with the same feature and filter."""

    filter_: Annotated[
        Optional[PlanVariantDetailsUsageAlertFilter1], pydantic.Field(alias="filter")
    ] = None
    r"""Only valid with basis usage_limit. Points the alert at the usage limit carrying the same filter."""

    name: Optional[str] = None
    r"""Optional user-defined label to distinguish multiple alerts on the same feature."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["feature_id", "enabled", "basis", "filter", "name"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class PlanVariantDetailsOverageAllowed1TypedDict(TypedDict):
    feature_id: str
    r"""The feature ID this overage allowed control applies to."""
    enabled: NotRequired[bool]
    r"""Whether overage is allowed for this feature."""


class PlanVariantDetailsOverageAllowed1(BaseModel):
    feature_id: str
    r"""The feature ID this overage allowed control applies to."""

    enabled: Optional[bool] = False
    r"""Whether overage is allowed for this feature."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["enabled"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class PlanVariantDetailsBillingControls1TypedDict(TypedDict):
    r"""Override the plan's billing controls (auto top-ups, spend limits, usage limits, usage alerts, overage allowed) for this customer."""

    auto_topups: NotRequired[List[PlanVariantDetailsAutoTopup1TypedDict]]
    r"""List of auto top-up configurations per feature."""
    spend_limits: NotRequired[List[PlanVariantDetailsSpendLimit1TypedDict]]
    r"""List of overage spend limits per feature (caps overage spend)."""
    usage_limits: NotRequired[List[PlanVariantDetailsUsageLimit1TypedDict]]
    r"""List of hard usage caps per feature (max units per interval)."""
    usage_alerts: NotRequired[List[PlanVariantDetailsUsageAlert1TypedDict]]
    r"""List of usage alert configurations per feature."""
    overage_allowed: NotRequired[List[PlanVariantDetailsOverageAllowed1TypedDict]]
    r"""List of overage allowed controls per feature. When enabled, usage can exceed balance."""


class PlanVariantDetailsBillingControls1(BaseModel):
    r"""Override the plan's billing controls (auto top-ups, spend limits, usage limits, usage alerts, overage allowed) for this customer."""

    auto_topups: Optional[List[PlanVariantDetailsAutoTopup1]] = None
    r"""List of auto top-up configurations per feature."""

    spend_limits: Optional[List[PlanVariantDetailsSpendLimit1]] = None
    r"""List of overage spend limits per feature (caps overage spend)."""

    usage_limits: Optional[List[PlanVariantDetailsUsageLimit1]] = None
    r"""List of hard usage caps per feature (max units per interval)."""

    usage_alerts: Optional[List[PlanVariantDetailsUsageAlert1]] = None
    r"""List of usage alert configurations per feature."""

    overage_allowed: Optional[List[PlanVariantDetailsOverageAllowed1]] = None
    r"""List of overage allowed controls per feature. When enabled, usage can exceed balance."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "auto_topups",
                "spend_limits",
                "usage_limits",
                "usage_alerts",
                "overage_allowed",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PlanPriceUpsertLicenseInterval = Union[
    Literal[
        "one_off",
        "week",
        "month",
        "quarter",
        "semi_annual",
        "year",
    ],
    UnrecognizedStr,
]
r"""Billing interval (e.g. 'month', 'year')."""


class UpsertLicenseAdditionalCurrencyTypedDict(TypedDict):
    currency: str
    r"""Three-letter Stripe-supported currency code (e.g. 'eur', 'gbp')."""
    amount: float
    r"""Price amount in this currency. Set explicitly per currency, not converted from the base amount."""


class UpsertLicenseAdditionalCurrency(BaseModel):
    currency: str
    r"""Three-letter Stripe-supported currency code (e.g. 'eur', 'gbp')."""

    amount: float
    r"""Price amount in this currency. Set explicitly per currency, not converted from the base amount."""


class UpsertLicenseBasePriceTypedDict(TypedDict):
    r"""Base price configuration for a plan."""

    amount: float
    r"""Base price amount for the plan, in major currency units (e.g. dollars)."""
    interval: PlanPriceUpsertLicenseInterval
    r"""Billing interval (e.g. 'month', 'year')."""
    interval_count: NotRequired[float]
    r"""Number of intervals per billing cycle. Defaults to 1."""
    additional_currencies: NotRequired[List[UpsertLicenseAdditionalCurrencyTypedDict]]
    r"""Base price amounts in additional currencies. The base 'amount' is in the org's default currency."""


class UpsertLicenseBasePrice(BaseModel):
    r"""Base price configuration for a plan."""

    amount: float
    r"""Base price amount for the plan, in major currency units (e.g. dollars)."""

    interval: PlanPriceUpsertLicenseInterval
    r"""Billing interval (e.g. 'month', 'year')."""

    interval_count: Optional[float] = None
    r"""Number of intervals per billing cycle. Defaults to 1."""

    additional_currencies: Optional[List[UpsertLicenseAdditionalCurrency]] = None
    r"""Base price amounts in additional currencies. The base 'amount' is in the org's default currency."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["interval_count", "additional_currencies"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PlanUpsertLicenseResetInterval1 = Union[
    Literal[
        "one_off",
        "minute",
        "hour",
        "day",
        "week",
        "month",
        "quarter",
        "semi_annual",
        "year",
    ],
    UnrecognizedStr,
]
r"""Interval at which balance resets (e.g. 'month', 'year'). For consumable features only."""


class PlanUpsertLicenseResetTypedDict(TypedDict):
    r"""Reset configuration for consumable features. Omit for non-consumable features like seats."""

    interval: PlanUpsertLicenseResetInterval1
    r"""Interval at which balance resets (e.g. 'month', 'year'). For consumable features only."""
    interval_count: NotRequired[float]
    r"""Number of intervals between resets. Defaults to 1."""


class PlanUpsertLicenseReset(BaseModel):
    r"""Reset configuration for consumable features. Omit for non-consumable features like seats."""

    interval: PlanUpsertLicenseResetInterval1
    r"""Interval at which balance resets (e.g. 'month', 'year'). For consumable features only."""

    interval_count: Optional[float] = None
    r"""Number of intervals between resets. Defaults to 1."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["interval_count"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class UpsertLicenseAddItemAdditionalCurrencyTypedDict(TypedDict):
    currency: str
    r"""Three-letter Stripe-supported currency code (e.g. 'eur', 'gbp')."""
    amount: float
    r"""Price amount in this currency. Set explicitly per currency, not converted from the base amount."""


class UpsertLicenseAddItemAdditionalCurrency(BaseModel):
    currency: str
    r"""Three-letter Stripe-supported currency code (e.g. 'eur', 'gbp')."""

    amount: float
    r"""Price amount in this currency. Set explicitly per currency, not converted from the base amount."""


PlanUpsertLicensePriceToTypedDict = TypeAliasType(
    "PlanUpsertLicensePriceToTypedDict", Union[float, str]
)


PlanUpsertLicensePriceTo = TypeAliasType("PlanUpsertLicensePriceTo", Union[float, str])


class UpsertLicenseTierAdditionalCurrencyTypedDict(TypedDict):
    currency: str
    r"""Three-letter Stripe-supported currency code (e.g. 'eur', 'gbp')."""
    amount: NotRequired[float]
    r"""Per-unit amount for this tier in this currency."""
    flat_amount: NotRequired[float]
    r"""Flat amount for this tier in this currency, if the tier uses one."""


class UpsertLicenseTierAdditionalCurrency(BaseModel):
    currency: str
    r"""Three-letter Stripe-supported currency code (e.g. 'eur', 'gbp')."""

    amount: Optional[float] = None
    r"""Per-unit amount for this tier in this currency."""

    flat_amount: Optional[float] = None
    r"""Flat amount for this tier in this currency, if the tier uses one."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["amount", "flat_amount"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class PlanUpsertLicensePriceTierTypedDict(TypedDict):
    to: PlanUpsertLicensePriceToTypedDict
    amount: float
    flat_amount: NotRequired[float]
    additional_currencies: NotRequired[
        List[UpsertLicenseTierAdditionalCurrencyTypedDict]
    ]


class PlanUpsertLicensePriceTier(BaseModel):
    to: PlanUpsertLicensePriceTo

    amount: float

    flat_amount: Optional[float] = None

    additional_currencies: Optional[List[UpsertLicenseTierAdditionalCurrency]] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["flat_amount", "additional_currencies"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PlanUpsertLicenseTierBehavior = Union[
    Literal[
        "graduated",
        "volume",
    ],
    UnrecognizedStr,
]


PlanUpsertLicenseAddItemPriceInterval1 = Union[
    Literal[
        "one_off",
        "week",
        "month",
        "quarter",
        "semi_annual",
        "year",
    ],
    UnrecognizedStr,
]
r"""Billing interval. For consumable features, should match reset.interval."""


PlanUpsertLicenseAddItemBillingMethod1 = Union[
    Literal[
        "prepaid",
        "usage_based",
    ],
    UnrecognizedStr,
]
r"""'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go."""


class PlanUpsertLicensePriceTypedDict(TypedDict):
    r"""Pricing for usage beyond included units. Omit for free features."""

    interval: PlanUpsertLicenseAddItemPriceInterval1
    r"""Billing interval. For consumable features, should match reset.interval."""
    billing_method: PlanUpsertLicenseAddItemBillingMethod1
    r"""'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go."""
    amount: NotRequired[float]
    r"""Price per billing_units after included usage. Either 'amount' or 'tiers' is required."""
    additional_currencies: NotRequired[
        List[UpsertLicenseAddItemAdditionalCurrencyTypedDict]
    ]
    r"""Amounts in additional currencies for this flat price. The base 'amount' is in the org's default currency. Only valid with 'amount', not 'tiers'."""
    tiers: NotRequired[List[PlanUpsertLicensePriceTierTypedDict]]
    r"""Tiered pricing.  Either 'amount' or 'tiers' is required."""
    tier_behavior: NotRequired[PlanUpsertLicenseTierBehavior]
    interval_count: NotRequired[float]
    r"""Number of intervals per billing cycle. Defaults to 1."""
    billing_units: NotRequired[float]
    r"""Units per price increment. Usage is rounded UP when billed (e.g. billing_units=100 means 101 rounds to 200)."""
    max_purchase: NotRequired[Nullable[float]]
    r"""Max units purchasable beyond included. E.g. included=100, max_purchase=300 allows 400 total. Null for no limit."""


class PlanUpsertLicensePrice(BaseModel):
    r"""Pricing for usage beyond included units. Omit for free features."""

    interval: PlanUpsertLicenseAddItemPriceInterval1
    r"""Billing interval. For consumable features, should match reset.interval."""

    billing_method: PlanUpsertLicenseAddItemBillingMethod1
    r"""'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go."""

    amount: Optional[float] = None
    r"""Price per billing_units after included usage. Either 'amount' or 'tiers' is required."""

    additional_currencies: Optional[List[UpsertLicenseAddItemAdditionalCurrency]] = None
    r"""Amounts in additional currencies for this flat price. The base 'amount' is in the org's default currency. Only valid with 'amount', not 'tiers'."""

    tiers: Optional[List[PlanUpsertLicensePriceTier]] = None
    r"""Tiered pricing.  Either 'amount' or 'tiers' is required."""

    tier_behavior: Optional[PlanUpsertLicenseTierBehavior] = None

    interval_count: Optional[float] = 1
    r"""Number of intervals per billing cycle. Defaults to 1."""

    billing_units: Optional[float] = 1
    r"""Units per price increment. Usage is rounded UP when billed (e.g. billing_units=100 means 101 rounds to 200)."""

    max_purchase: OptionalNullable[float] = UNSET
    r"""Max units purchasable beyond included. E.g. included=100, max_purchase=300 allows 400 total. Null for no limit."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "amount",
                "additional_currencies",
                "tiers",
                "tier_behavior",
                "interval_count",
                "billing_units",
                "max_purchase",
            ]
        )
        nullable_fields = set(["max_purchase"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


UpsertLicenseOnIncrease = Union[
    Literal[
        "bill_immediately",
        "prorate_immediately",
        "prorate_next_cycle",
        "bill_next_cycle",
    ],
    UnrecognizedStr,
]
r"""Billing behavior when quantity increases mid-cycle."""


UpsertLicenseOnDecrease = Union[
    Literal[
        "prorate",
        "prorate_immediately",
        "prorate_next_cycle",
        "none",
        "no_prorations",
    ],
    UnrecognizedStr,
]
r"""Credit behavior when quantity decreases mid-cycle."""


class UpsertLicenseProrationTypedDict(TypedDict):
    r"""Proration settings for prepaid features. Controls mid-cycle quantity change billing."""

    on_increase: UpsertLicenseOnIncrease
    r"""Billing behavior when quantity increases mid-cycle."""
    on_decrease: UpsertLicenseOnDecrease
    r"""Credit behavior when quantity decreases mid-cycle."""


class UpsertLicenseProration(BaseModel):
    r"""Proration settings for prepaid features. Controls mid-cycle quantity change billing."""

    on_increase: UpsertLicenseOnIncrease
    r"""Billing behavior when quantity increases mid-cycle."""

    on_decrease: UpsertLicenseOnDecrease
    r"""Credit behavior when quantity decreases mid-cycle."""


UpsertLicenseExpiryDurationType = Union[
    Literal[
        "month",
        "forever",
    ],
    UnrecognizedStr,
]
r"""When rolled over units expire."""


class PlanUpsertLicenseRolloverTypedDict(TypedDict):
    r"""Rollover config for unused units. If set, unused included units carry over."""

    expiry_duration_type: UpsertLicenseExpiryDurationType
    r"""When rolled over units expire."""
    max: NotRequired[float]
    r"""Max rollover units. Omit for unlimited rollover."""
    max_percentage: NotRequired[float]
    r"""Maximum rollover as a percentage (0-100) of included + prepaid grant. Mutually exclusive with max."""
    expiry_duration_length: NotRequired[float]
    r"""Number of periods before expiry."""


class PlanUpsertLicenseRollover(BaseModel):
    r"""Rollover config for unused units. If set, unused included units carry over."""

    expiry_duration_type: UpsertLicenseExpiryDurationType
    r"""When rolled over units expire."""

    max: Optional[float] = None
    r"""Max rollover units. Omit for unlimited rollover."""

    max_percentage: Optional[float] = None
    r"""Maximum rollover as a percentage (0-100) of included + prepaid grant. Mutually exclusive with max."""

    expiry_duration_length: Optional[float] = None
    r"""Number of periods before expiry."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["max", "max_percentage", "expiry_duration_length"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class PlanDimensionsUpsertLicense4TypedDict(TypedDict):
    match: Dict[str, str]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""
    credit_cost: float
    r"""Credits consumed per billing-unit group when this dimension matches."""
    priority: NotRequired[int]
    r"""Breaks ties between dimensions that match the same number of keys. Higher wins."""


class PlanDimensionsUpsertLicense4(BaseModel):
    match: Dict[str, str]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""

    credit_cost: float
    r"""Credits consumed per billing-unit group when this dimension matches."""

    priority: Optional[int] = None
    r"""Breaks ties between dimensions that match the same number of keys. Higher wins."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["priority"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PlanDimensionsToUpsertLicenseEnum2 = Literal["inf",]


PlanDimensionsUpsertLicenseToUnion2TypedDict = TypeAliasType(
    "PlanDimensionsUpsertLicenseToUnion2TypedDict",
    Union[float, PlanDimensionsToUpsertLicenseEnum2],
)
r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""


PlanDimensionsUpsertLicenseToUnion2 = TypeAliasType(
    "PlanDimensionsUpsertLicenseToUnion2",
    Union[float, PlanDimensionsToUpsertLicenseEnum2],
)
r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""


class PlanDimensionsUpsertLicenseTier2TypedDict(TypedDict):
    to: PlanDimensionsUpsertLicenseToUnion2TypedDict
    r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""
    credit_cost: float
    r"""Credits consumed per billing-unit group within this tier."""


class PlanDimensionsUpsertLicenseTier2(BaseModel):
    to: PlanDimensionsUpsertLicenseToUnion2
    r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""

    credit_cost: float
    r"""Credits consumed per billing-unit group within this tier."""


class PlanDimensionsUpsertLicense3TypedDict(TypedDict):
    match: Dict[str, str]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""
    tiers: List[PlanDimensionsUpsertLicenseTier2TypedDict]
    priority: NotRequired[int]
    r"""Breaks ties between dimensions that match the same number of keys. Higher wins."""
    tier_behavior: Literal["graduated"]


class PlanDimensionsUpsertLicense3(BaseModel):
    match: Dict[str, str]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""

    tiers: List[PlanDimensionsUpsertLicenseTier2]

    priority: Optional[int] = None
    r"""Breaks ties between dimensions that match the same number of keys. Higher wins."""

    tier_behavior: Annotated[
        Annotated[Literal["graduated"], AfterValidator(validate_const("graduated"))],
        pydantic.Field(alias="tier_behavior"),
    ] = "graduated"

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["priority"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PlanUpsertLicenseDimensionsUnion2TypedDict = TypeAliasType(
    "PlanUpsertLicenseDimensionsUnion2TypedDict",
    Union[PlanDimensionsUpsertLicense4TypedDict, PlanDimensionsUpsertLicense3TypedDict],
)


PlanUpsertLicenseDimensionsUnion2 = TypeAliasType(
    "PlanUpsertLicenseDimensionsUnion2",
    Union[PlanDimensionsUpsertLicense4, PlanDimensionsUpsertLicense3],
)


class PlanUpsertLicenseMultipliers2TypedDict(TypedDict):
    match: Dict[str, str]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""
    factor: NotRequired[float]
    r"""Multiplies the matched rate. All matching multipliers stack."""
    add: NotRequired[float]
    r"""Added to the rate after every factor is applied, in credits per billing-unit group."""


class PlanUpsertLicenseMultipliers2(BaseModel):
    match: Dict[str, str]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""

    factor: Optional[float] = None
    r"""Multiplies the matched rate. All matching multipliers stack."""

    add: Optional[float] = None
    r"""Added to the rate after every factor is applied, in credits per billing-unit group."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["factor", "add"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class PlanCreditSchemaUpsertLicense2TypedDict(TypedDict):
    metered_feature_id: str
    r"""ID of the metered feature that draws from this credit system."""
    credit_cost: float
    r"""Credits consumed per billing-unit group."""
    billing_units: NotRequired[float]
    r"""Number of metered-feature units priced together. Defaults to one when omitted."""
    dimensions: NotRequired[Dict[str, PlanUpsertLicenseDimensionsUnion2TypedDict]]
    r"""Named rates chosen by event properties. The most specific match sets the rate; with no match the item's own rate applies."""
    multipliers: NotRequired[Dict[str, PlanUpsertLicenseMultipliers2TypedDict]]
    r"""Named adjustments chosen by event properties. Every match applies: factors multiply, then adds are summed."""


class PlanCreditSchemaUpsertLicense2(BaseModel):
    metered_feature_id: str
    r"""ID of the metered feature that draws from this credit system."""

    credit_cost: float
    r"""Credits consumed per billing-unit group."""

    billing_units: Optional[float] = None
    r"""Number of metered-feature units priced together. Defaults to one when omitted."""

    dimensions: Optional[Dict[str, PlanUpsertLicenseDimensionsUnion2]] = None
    r"""Named rates chosen by event properties. The most specific match sets the rate; with no match the item's own rate applies."""

    multipliers: Optional[Dict[str, PlanUpsertLicenseMultipliers2]] = None
    r"""Named adjustments chosen by event properties. Every match applies: factors multiply, then adds are summed."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["billing_units", "dimensions", "multipliers"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class PlanDimensionsUpsertLicense2TypedDict(TypedDict):
    match: Dict[str, str]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""
    credit_cost: float
    r"""Credits consumed per billing-unit group when this dimension matches."""
    priority: NotRequired[int]
    r"""Breaks ties between dimensions that match the same number of keys. Higher wins."""


class PlanDimensionsUpsertLicense2(BaseModel):
    match: Dict[str, str]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""

    credit_cost: float
    r"""Credits consumed per billing-unit group when this dimension matches."""

    priority: Optional[int] = None
    r"""Breaks ties between dimensions that match the same number of keys. Higher wins."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["priority"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PlanDimensionsToUpsertLicenseEnum1 = Literal["inf",]


PlanDimensionsUpsertLicenseToUnion1TypedDict = TypeAliasType(
    "PlanDimensionsUpsertLicenseToUnion1TypedDict",
    Union[float, PlanDimensionsToUpsertLicenseEnum1],
)
r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""


PlanDimensionsUpsertLicenseToUnion1 = TypeAliasType(
    "PlanDimensionsUpsertLicenseToUnion1",
    Union[float, PlanDimensionsToUpsertLicenseEnum1],
)
r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""


class PlanDimensionsUpsertLicenseTier1TypedDict(TypedDict):
    to: PlanDimensionsUpsertLicenseToUnion1TypedDict
    r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""
    credit_cost: float
    r"""Credits consumed per billing-unit group within this tier."""


class PlanDimensionsUpsertLicenseTier1(BaseModel):
    to: PlanDimensionsUpsertLicenseToUnion1
    r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""

    credit_cost: float
    r"""Credits consumed per billing-unit group within this tier."""


class PlanDimensionsUpsertLicense1TypedDict(TypedDict):
    match: Dict[str, str]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""
    tiers: List[PlanDimensionsUpsertLicenseTier1TypedDict]
    priority: NotRequired[int]
    r"""Breaks ties between dimensions that match the same number of keys. Higher wins."""
    tier_behavior: Literal["graduated"]


class PlanDimensionsUpsertLicense1(BaseModel):
    match: Dict[str, str]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""

    tiers: List[PlanDimensionsUpsertLicenseTier1]

    priority: Optional[int] = None
    r"""Breaks ties between dimensions that match the same number of keys. Higher wins."""

    tier_behavior: Annotated[
        Annotated[Literal["graduated"], AfterValidator(validate_const("graduated"))],
        pydantic.Field(alias="tier_behavior"),
    ] = "graduated"

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["priority"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PlanUpsertLicenseDimensionsUnion1TypedDict = TypeAliasType(
    "PlanUpsertLicenseDimensionsUnion1TypedDict",
    Union[PlanDimensionsUpsertLicense2TypedDict, PlanDimensionsUpsertLicense1TypedDict],
)


PlanUpsertLicenseDimensionsUnion1 = TypeAliasType(
    "PlanUpsertLicenseDimensionsUnion1",
    Union[PlanDimensionsUpsertLicense2, PlanDimensionsUpsertLicense1],
)


class PlanUpsertLicenseMultipliers1TypedDict(TypedDict):
    match: Dict[str, str]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""
    factor: NotRequired[float]
    r"""Multiplies the matched rate. All matching multipliers stack."""
    add: NotRequired[float]
    r"""Added to the rate after every factor is applied, in credits per billing-unit group."""


class PlanUpsertLicenseMultipliers1(BaseModel):
    match: Dict[str, str]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""

    factor: Optional[float] = None
    r"""Multiplies the matched rate. All matching multipliers stack."""

    add: Optional[float] = None
    r"""Added to the rate after every factor is applied, in credits per billing-unit group."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["factor", "add"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PlanToUpsertLicenseEnum = Literal["inf",]


PlanUpsertLicenseFeatureOverrideToUnionTypedDict = TypeAliasType(
    "PlanUpsertLicenseFeatureOverrideToUnionTypedDict",
    Union[float, PlanToUpsertLicenseEnum],
)
r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""


PlanUpsertLicenseFeatureOverrideToUnion = TypeAliasType(
    "PlanUpsertLicenseFeatureOverrideToUnion", Union[float, PlanToUpsertLicenseEnum]
)
r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""


class PlanUpsertLicenseFeatureOverrideTierTypedDict(TypedDict):
    to: PlanUpsertLicenseFeatureOverrideToUnionTypedDict
    r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""
    credit_cost: float
    r"""Credits consumed per billing-unit group within this tier."""


class PlanUpsertLicenseFeatureOverrideTier(BaseModel):
    to: PlanUpsertLicenseFeatureOverrideToUnion
    r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""

    credit_cost: float
    r"""Credits consumed per billing-unit group within this tier."""


class PlanCreditSchemaUpsertLicense1TypedDict(TypedDict):
    metered_feature_id: str
    r"""ID of the metered feature that draws from this credit system."""
    tiers: List[PlanUpsertLicenseFeatureOverrideTierTypedDict]
    billing_units: NotRequired[float]
    r"""Number of metered-feature units priced together. Defaults to one when omitted."""
    dimensions: NotRequired[Dict[str, PlanUpsertLicenseDimensionsUnion1TypedDict]]
    r"""Named rates chosen by event properties. The most specific match sets the rate; with no match the item's own rate applies."""
    multipliers: NotRequired[Dict[str, PlanUpsertLicenseMultipliers1TypedDict]]
    r"""Named adjustments chosen by event properties. Every match applies: factors multiply, then adds are summed."""
    tier_behavior: Literal["graduated"]


class PlanCreditSchemaUpsertLicense1(BaseModel):
    metered_feature_id: str
    r"""ID of the metered feature that draws from this credit system."""

    tiers: List[PlanUpsertLicenseFeatureOverrideTier]

    billing_units: Optional[float] = None
    r"""Number of metered-feature units priced together. Defaults to one when omitted."""

    dimensions: Optional[Dict[str, PlanUpsertLicenseDimensionsUnion1]] = None
    r"""Named rates chosen by event properties. The most specific match sets the rate; with no match the item's own rate applies."""

    multipliers: Optional[Dict[str, PlanUpsertLicenseMultipliers1]] = None
    r"""Named adjustments chosen by event properties. Every match applies: factors multiply, then adds are summed."""

    tier_behavior: Annotated[
        Annotated[Literal["graduated"], AfterValidator(validate_const("graduated"))],
        pydantic.Field(alias="tier_behavior"),
    ] = "graduated"

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["billing_units", "dimensions", "multipliers"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PlanUpsertLicenseCreditSchemaUnionTypedDict = TypeAliasType(
    "PlanUpsertLicenseCreditSchemaUnionTypedDict",
    Union[
        PlanCreditSchemaUpsertLicense2TypedDict, PlanCreditSchemaUpsertLicense1TypedDict
    ],
)


PlanUpsertLicenseCreditSchemaUnion = TypeAliasType(
    "PlanUpsertLicenseCreditSchemaUnion",
    Union[PlanCreditSchemaUpsertLicense2, PlanCreditSchemaUpsertLicense1],
)


class UpsertLicenseFeatureOverrideTypedDict(TypedDict):
    r"""Overrides fields of this item's feature for customers on this plan (e.g. a credit system's credit_schema)."""

    credit_schema: NotRequired[List[PlanUpsertLicenseCreditSchemaUnionTypedDict]]
    r"""For credit system features: replaces the feature's credit_schema entirely for customers on this plan."""


class UpsertLicenseFeatureOverride(BaseModel):
    r"""Overrides fields of this item's feature for customers on this plan (e.g. a credit system's credit_schema)."""

    credit_schema: Optional[List[PlanUpsertLicenseCreditSchemaUnion]] = None
    r"""For credit system features: replaces the feature's credit_schema entirely for customers on this plan."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["credit_schema"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class UpsertLicensePlanItemTypedDict(TypedDict):
    r"""Configuration for a feature item in a plan, including usage limits, pricing, and rollover settings."""

    feature_id: str
    r"""The ID of the feature to configure."""
    included: NotRequired[float]
    r"""Number of free units included. Balance resets to this each interval for consumable features."""
    unlimited: NotRequired[bool]
    r"""If true, customer has unlimited access to this feature."""
    pooled: NotRequired[bool]
    r"""Whether entity-level grants contribute to a shared customer balance."""
    reset: NotRequired[PlanUpsertLicenseResetTypedDict]
    r"""Reset configuration for consumable features. Omit for non-consumable features like seats."""
    price: NotRequired[PlanUpsertLicensePriceTypedDict]
    r"""Pricing for usage beyond included units. Omit for free features."""
    proration: NotRequired[UpsertLicenseProrationTypedDict]
    r"""Proration settings for prepaid features. Controls mid-cycle quantity change billing."""
    rollover: NotRequired[PlanUpsertLicenseRolloverTypedDict]
    r"""Rollover config for unused units. If set, unused included units carry over."""
    feature_override: NotRequired[UpsertLicenseFeatureOverrideTypedDict]
    r"""Overrides fields of this item's feature for customers on this plan (e.g. a credit system's credit_schema)."""


class UpsertLicensePlanItem(BaseModel):
    r"""Configuration for a feature item in a plan, including usage limits, pricing, and rollover settings."""

    feature_id: str
    r"""The ID of the feature to configure."""

    included: Optional[float] = None
    r"""Number of free units included. Balance resets to this each interval for consumable features."""

    unlimited: Optional[bool] = None
    r"""If true, customer has unlimited access to this feature."""

    pooled: Optional[bool] = False
    r"""Whether entity-level grants contribute to a shared customer balance."""

    reset: Optional[PlanUpsertLicenseReset] = None
    r"""Reset configuration for consumable features. Omit for non-consumable features like seats."""

    price: Optional[PlanUpsertLicensePrice] = None
    r"""Pricing for usage beyond included units. Omit for free features."""

    proration: Optional[UpsertLicenseProration] = None
    r"""Proration settings for prepaid features. Controls mid-cycle quantity change billing."""

    rollover: Optional[PlanUpsertLicenseRollover] = None
    r"""Rollover config for unused units. If set, unused included units carry over."""

    feature_override: Optional[UpsertLicenseFeatureOverride] = None
    r"""Overrides fields of this item's feature for customers on this plan (e.g. a credit system's credit_schema)."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "included",
                "unlimited",
                "pooled",
                "reset",
                "price",
                "proration",
                "rollover",
                "feature_override",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


PlanUpsertLicenseRemoveItemBillingMethod1 = Union[
    Literal[
        "prepaid",
        "usage_based",
    ],
    UnrecognizedStr,
]
r"""Match items with this billing method (prepaid or usage_based)."""


PlanIntervalUpsertLicenseRemoveItemEnum2 = Union[
    Literal[
        "one_off",
        "minute",
        "hour",
        "day",
        "week",
        "month",
        "quarter",
        "semi_annual",
        "year",
    ],
    UnrecognizedStr,
]


PlanIntervalUpsertLicenseRemoveItemEnum1 = Union[
    Literal[
        "one_off",
        "week",
        "month",
        "quarter",
        "semi_annual",
        "year",
    ],
    UnrecognizedStr,
]


PlanUpsertLicenseIntervalUnion1TypedDict = TypeAliasType(
    "PlanUpsertLicenseIntervalUnion1TypedDict",
    Union[
        PlanIntervalUpsertLicenseRemoveItemEnum1,
        PlanIntervalUpsertLicenseRemoveItemEnum2,
    ],
)
r"""Match items with this interval. Accepts either a BillingInterval (price-side) or a ResetInterval (reset-side, includes day/hour/minute) so price-less items keyed by reset.interval can be disambiguated."""


PlanUpsertLicenseIntervalUnion1 = TypeAliasType(
    "PlanUpsertLicenseIntervalUnion1",
    Union[
        PlanIntervalUpsertLicenseRemoveItemEnum1,
        PlanIntervalUpsertLicenseRemoveItemEnum2,
    ],
)
r"""Match items with this interval. Accepts either a BillingInterval (price-side) or a ResetInterval (reset-side, includes day/hour/minute) so price-less items keyed by reset.interval can be disambiguated."""


class UpsertLicensePlanItemFilterTypedDict(TypedDict):
    r"""Filter for matching plan items. All provided fields must match (AND)."""

    feature_id: NotRequired[str]
    r"""Match items linked to this feature."""
    billing_method: NotRequired[PlanUpsertLicenseRemoveItemBillingMethod1]
    r"""Match items with this billing method (prepaid or usage_based)."""
    interval: NotRequired[PlanUpsertLicenseIntervalUnion1TypedDict]
    r"""Match items with this interval. Accepts either a BillingInterval (price-side) or a ResetInterval (reset-side, includes day/hour/minute) so price-less items keyed by reset.interval can be disambiguated."""
    interval_count: NotRequired[int]
    r"""Match items with this interval_count. Disambiguates between items that share an interval but differ in count."""
    included: NotRequired[float]
    r"""Match items whose grant equals this included usage. Omitted is a wildcard."""


class UpsertLicensePlanItemFilter(BaseModel):
    r"""Filter for matching plan items. All provided fields must match (AND)."""

    feature_id: Optional[str] = None
    r"""Match items linked to this feature."""

    billing_method: Optional[PlanUpsertLicenseRemoveItemBillingMethod1] = None
    r"""Match items with this billing method (prepaid or usage_based)."""

    interval: Optional[PlanUpsertLicenseIntervalUnion1] = None
    r"""Match items with this interval. Accepts either a BillingInterval (price-side) or a ResetInterval (reset-side, includes day/hour/minute) so price-less items keyed by reset.interval can be disambiguated."""

    interval_count: Optional[int] = None
    r"""Match items with this interval_count. Disambiguates between items that share an interval but differ in count."""

    included: Optional[float] = None
    r"""Match items whose grant equals this included usage. Omitted is a wildcard."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            ["feature_id", "billing_method", "interval", "interval_count", "included"]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class UpsertLicenseCustomizeTypedDict(TypedDict):
    price: NotRequired[Nullable[UpsertLicenseBasePriceTypedDict]]
    add_items: NotRequired[List[UpsertLicensePlanItemTypedDict]]
    remove_items: NotRequired[List[UpsertLicensePlanItemFilterTypedDict]]


class UpsertLicenseCustomize(BaseModel):
    price: OptionalNullable[UpsertLicenseBasePrice] = UNSET

    add_items: Optional[List[UpsertLicensePlanItem]] = None

    remove_items: Optional[List[UpsertLicensePlanItemFilter]] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["price", "add_items", "remove_items"])
        nullable_fields = set(["price"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


class UpsertLicenseTypedDict(TypedDict):
    license_plan_id: str
    version_slug: NotRequired[str]
    included: NotRequired[int]
    prepaid_only: NotRequired[bool]
    customize: NotRequired[Nullable[UpsertLicenseCustomizeTypedDict]]
    metadata: NotRequired[Dict[str, Any]]


class UpsertLicense(BaseModel):
    license_plan_id: str

    version_slug: Optional[str] = None

    included: Optional[int] = None

    prepaid_only: Optional[bool] = None

    customize: OptionalNullable[UpsertLicenseCustomize] = UNSET

    metadata: Optional[Dict[str, Any]] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            ["version_slug", "included", "prepaid_only", "customize", "metadata"]
        )
        nullable_fields = set(["customize"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


class RemoveLicenseTypedDict(TypedDict):
    license_plan_id: str


class RemoveLicense(BaseModel):
    license_plan_id: str


class Customize1TypedDict(TypedDict):
    r"""The customization that transforms the base plan into this variant."""

    price: NotRequired[Nullable[BasePriceTypedDict]]
    r"""Override the base price of the plan. Pass null to remove the base price."""
    add_items: NotRequired[List[PlanItemTypedDict]]
    r"""Items to add to the plan."""
    remove_items: NotRequired[List[PlanItemFilterTypedDict]]
    r"""Filters selecting items to remove from the plan."""
    free_trial: NotRequired[Nullable[FreeTrialParamsTypedDict]]
    r"""Override the plan's default free trial. Pass an object to set a custom trial, or null to remove the trial entirely."""
    billing_controls: NotRequired[PlanVariantDetailsBillingControls1TypedDict]
    r"""Override the plan's billing controls (auto top-ups, spend limits, usage limits, usage alerts, overage allowed) for this customer."""
    upsert_licenses: NotRequired[List[UpsertLicenseTypedDict]]
    r"""License links to add or override for this customer, keyed by license_plan_id. Omitted fields inherit the plan catalog link (included defaults to 1 when the license is not in the catalog). A bare entry restores the license to pure catalog inheritance."""
    remove_licenses: NotRequired[List[RemoveLicenseTypedDict]]
    r"""License links to drop, keyed by license_plan_id. Parallel to remove_items."""


class Customize1(BaseModel):
    r"""The customization that transforms the base plan into this variant."""

    price: OptionalNullable[BasePrice] = UNSET
    r"""Override the base price of the plan. Pass null to remove the base price."""

    add_items: Optional[List[PlanItem]] = None
    r"""Items to add to the plan."""

    remove_items: Optional[List[PlanItemFilter]] = None
    r"""Filters selecting items to remove from the plan."""

    free_trial: OptionalNullable[FreeTrialParams] = UNSET
    r"""Override the plan's default free trial. Pass an object to set a custom trial, or null to remove the trial entirely."""

    billing_controls: Optional[PlanVariantDetailsBillingControls1] = None
    r"""Override the plan's billing controls (auto top-ups, spend limits, usage limits, usage alerts, overage allowed) for this customer."""

    upsert_licenses: Optional[List[UpsertLicense]] = None
    r"""License links to add or override for this customer, keyed by license_plan_id. Omitted fields inherit the plan catalog link (included defaults to 1 when the license is not in the catalog). A bare entry restores the license to pure catalog inheritance."""

    remove_licenses: Optional[List[RemoveLicense]] = None
    r"""License links to drop, keyed by license_plan_id. Parallel to remove_items."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "price",
                "add_items",
                "remove_items",
                "free_trial",
                "billing_controls",
                "upsert_licenses",
                "remove_licenses",
            ]
        )
        nullable_fields = set(["price", "free_trial"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


class VariantDetailsTypedDict(TypedDict):
    r"""Details about how this variant relates to its latest base plan."""

    base_plan_id: str
    r"""The ID of the base plan this variant was derived from."""
    customize: NotRequired[Customize1TypedDict]
    r"""The customization that transforms the base plan into this variant."""


class VariantDetails(BaseModel):
    r"""Details about how this variant relates to its latest base plan."""

    base_plan_id: str
    r"""The ID of the base plan this variant was derived from."""

    customize: Optional[Customize1] = None
    r"""The customization that transforms the base plan into this variant."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["customize"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class PlanTypedDict(TypedDict):
    id: str
    r"""Unique identifier for the plan."""
    name: str
    r"""Display name of the plan."""
    description: Nullable[str]
    r"""Optional description of the plan."""
    group: Nullable[str]
    r"""Group identifier for organizing related plans. Plans in the same group are mutually exclusive."""
    version: float
    r"""Version number of the plan. Incremented when plan configuration changes."""
    add_on: bool
    r"""Whether this is an add-on plan that can be attached alongside a main plan."""
    auto_enable: bool
    r"""If true, this plan is automatically attached when a customer is created. Used for free plans."""
    price: Nullable[PlanPriceTypedDict]
    r"""Base recurring price for the plan. Null for free plans or usage-only plans."""
    items: List[ItemTypedDict]
    r"""Feature configurations included in this plan. Each item defines included units, pricing, and reset behavior for a feature."""
    created_at: float
    r"""Unix timestamp (ms) when the plan was created."""
    env: PlanEnv
    r"""Environment this plan belongs to ('sandbox' or 'live')."""
    archived: bool
    r"""Whether the plan is archived. Archived plans cannot be attached to new customers."""
    config: PlanConfigTypedDict
    r"""Miscellaneous plan-level configuration flags."""
    metadata: Dict[str, Any]
    r"""Arbitrary key-value metadata defined by you for your own use. Shared across all versions of the plan."""
    base_variant_id: Nullable[str]
    r"""Deprecated. Use variant_details.base_plan_id instead. If this is a variant, the ID of the base plan it was created from."""
    version_slug: NotRequired[Nullable[str]]
    r"""User-facing version identity. Defaults to v{n} when the version is minted."""
    active: NotRequired[bool]
    r"""Whether this is the active version of the plan. At most one version is active."""
    processors: NotRequired[PlanProcessorsTypedDict]
    r"""Payment processors this plan is connected to. Omitted when unset."""
    free_trial: NotRequired[FreeTrialTypedDict]
    r"""Free trial configuration. If set, new customers can try this plan before being charged."""
    billing_controls: NotRequired[PlanBillingControlsTypedDict]
    r"""Plan-level billing controls used as customer defaults."""
    customer_eligibility: NotRequired[CustomerEligibilityTypedDict]
    variant_details: NotRequired[VariantDetailsTypedDict]
    r"""Details about how this variant relates to its latest base plan."""


class Plan(BaseModel):
    id: str
    r"""Unique identifier for the plan."""

    name: str
    r"""Display name of the plan."""

    description: Nullable[str]
    r"""Optional description of the plan."""

    group: Nullable[str]
    r"""Group identifier for organizing related plans. Plans in the same group are mutually exclusive."""

    version: float
    r"""Version number of the plan. Incremented when plan configuration changes."""

    add_on: bool
    r"""Whether this is an add-on plan that can be attached alongside a main plan."""

    auto_enable: bool
    r"""If true, this plan is automatically attached when a customer is created. Used for free plans."""

    price: Nullable[PlanPrice]
    r"""Base recurring price for the plan. Null for free plans or usage-only plans."""

    items: List[Item]
    r"""Feature configurations included in this plan. Each item defines included units, pricing, and reset behavior for a feature."""

    created_at: float
    r"""Unix timestamp (ms) when the plan was created."""

    env: PlanEnv
    r"""Environment this plan belongs to ('sandbox' or 'live')."""

    archived: bool
    r"""Whether the plan is archived. Archived plans cannot be attached to new customers."""

    config: PlanConfig
    r"""Miscellaneous plan-level configuration flags."""

    metadata: Dict[str, Any]
    r"""Arbitrary key-value metadata defined by you for your own use. Shared across all versions of the plan."""

    base_variant_id: Nullable[str]
    r"""Deprecated. Use variant_details.base_plan_id instead. If this is a variant, the ID of the base plan it was created from."""

    version_slug: OptionalNullable[str] = UNSET
    r"""User-facing version identity. Defaults to v{n} when the version is minted."""

    active: Optional[bool] = None
    r"""Whether this is the active version of the plan. At most one version is active."""

    processors: Optional[PlanProcessors] = None
    r"""Payment processors this plan is connected to. Omitted when unset."""

    free_trial: Optional[FreeTrial] = None
    r"""Free trial configuration. If set, new customers can try this plan before being charged."""

    billing_controls: Optional[PlanBillingControls] = None
    r"""Plan-level billing controls used as customer defaults."""

    customer_eligibility: Optional[CustomerEligibility] = None

    variant_details: Optional[VariantDetails] = None
    r"""Details about how this variant relates to its latest base plan."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "version_slug",
                "active",
                "processors",
                "free_trial",
                "billing_controls",
                "customer_eligibility",
                "variant_details",
            ]
        )
        nullable_fields = set(
            ["description", "group", "version_slug", "price", "base_variant_id"]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


try:
    PlanDimensionsItem3.model_rebuild()
except NameError:
    pass
try:
    PlanDimensionsItem1.model_rebuild()
except NameError:
    pass
try:
    PlanCreditSchemaItemFeatureOverride1.model_rebuild()
except NameError:
    pass
try:
    PlanUsageLimit.model_rebuild()
except NameError:
    pass
try:
    PlanUsageAlert.model_rebuild()
except NameError:
    pass
try:
    PlanDimensionsVariantDetails3.model_rebuild()
except NameError:
    pass
try:
    PlanDimensionsVariantDetails1.model_rebuild()
except NameError:
    pass
try:
    PlanCreditSchemaVariantDetails1.model_rebuild()
except NameError:
    pass
try:
    PlanVariantDetailsUsageLimit1.model_rebuild()
except NameError:
    pass
try:
    PlanVariantDetailsUsageAlert1.model_rebuild()
except NameError:
    pass
try:
    PlanDimensionsUpsertLicense3.model_rebuild()
except NameError:
    pass
try:
    PlanDimensionsUpsertLicense1.model_rebuild()
except NameError:
    pass
try:
    PlanCreditSchemaUpsertLicense1.model_rebuild()
except NameError:
    pass
