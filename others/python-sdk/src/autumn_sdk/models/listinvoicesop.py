"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from autumn_sdk.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
    UnrecognizedStr,
)
from autumn_sdk.utils import FieldMetadata, HeaderMetadata
import pydantic
from pydantic import model_serializer
from typing import List, Literal, Optional, Union
from typing_extensions import Annotated, NotRequired, TypedDict


class ListInvoicesGlobalsTypedDict(TypedDict):
    x_api_version: NotRequired[str]


class ListInvoicesGlobals(BaseModel):
    x_api_version: Annotated[
        Optional[str],
        pydantic.Field(alias="x-api-version"),
        FieldMetadata(header=HeaderMetadata(style="simple", explode=False)),
    ] = "2.3.0"

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["x-api-version"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


ListInvoicesStatus = Literal[
    "draft",
    "open",
    "void",
    "paid",
    "uncollectible",
]


ProcessorTypeRequest = Literal[
    "stripe",
    "revenuecat",
]


class ListInvoicesParamsTypedDict(TypedDict):
    start_cursor: NotRequired[str]
    r"""Opaque pagination cursor. Empty string (default) requests the first page; use next_cursor from a prior response for subsequent pages."""
    limit: NotRequired[int]
    r"""Number of items to return. Default 50, hard ceiling 5000."""
    customer_id: NotRequired[str]
    r"""Filter invoices to a single customer by ID."""
    entity_id: NotRequired[str]
    r"""Filter invoices to a single entity by ID. Must be provided together with customer_id, since entity IDs are only unique per customer."""
    status: NotRequired[List[ListInvoicesStatus]]
    r"""Filter by invoice status (draft, open, paid, void, uncollectible)."""
    processor_types: NotRequired[List[ProcessorTypeRequest]]
    r"""Filter by billing processor (stripe, revenuecat). Invoices recorded before processor tracking count as stripe."""


class ListInvoicesParams(BaseModel):
    start_cursor: Optional[str] = ""
    r"""Opaque pagination cursor. Empty string (default) requests the first page; use next_cursor from a prior response for subsequent pages."""

    limit: Optional[int] = 50
    r"""Number of items to return. Default 50, hard ceiling 5000."""

    customer_id: Optional[str] = None
    r"""Filter invoices to a single customer by ID."""

    entity_id: Optional[str] = None
    r"""Filter invoices to a single entity by ID. Must be provided together with customer_id, since entity IDs are only unique per customer."""

    status: Optional[List[ListInvoicesStatus]] = None
    r"""Filter by invoice status (draft, open, paid, void, uncollectible)."""

    processor_types: Optional[List[ProcessorTypeRequest]] = None
    r"""Filter by billing processor (stripe, revenuecat). Invoices recorded before processor tracking count as stripe."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "start_cursor",
                "limit",
                "customer_id",
                "entity_id",
                "status",
                "processor_types",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


ListInvoicesListProcessorType = Union[
    Literal[
        "stripe",
        "revenuecat",
    ],
    UnrecognizedStr,
]
r"""The billing processor that owns this invoice."""


class ListInvoicesListTypedDict(TypedDict):
    plan_ids: List[str]
    r"""Array of plan IDs included in this invoice"""
    stripe_id: str
    r"""The Stripe invoice ID"""
    status: str
    r"""The status of the invoice"""
    total: float
    r"""The total amount of the invoice"""
    currency: str
    r"""The currency code for the invoice"""
    created_at: float
    r"""Timestamp when the invoice was created"""
    id: str
    r"""The Autumn invoice ID"""
    customer_id: Nullable[str]
    r"""The ID of the customer this invoice belongs to. Null for customers created without an ID."""
    entity_id: Nullable[str]
    r"""The ID of the entity this invoice belongs to, if entity-scoped"""
    amount_paid: Nullable[float]
    r"""The amount paid on the invoice. Null on invoices recorded before amounts paid were tracked."""
    refunded_amount: float
    r"""The total amount refunded on the invoice"""
    processor_type: NotRequired[ListInvoicesListProcessorType]
    r"""The billing processor that owns this invoice."""
    hosted_invoice_url: NotRequired[Nullable[str]]
    r"""URL to the Stripe-hosted invoice page"""


class ListInvoicesList(BaseModel):
    plan_ids: List[str]
    r"""Array of plan IDs included in this invoice"""

    stripe_id: str
    r"""The Stripe invoice ID"""

    status: str
    r"""The status of the invoice"""

    total: float
    r"""The total amount of the invoice"""

    currency: str
    r"""The currency code for the invoice"""

    created_at: float
    r"""Timestamp when the invoice was created"""

    id: str
    r"""The Autumn invoice ID"""

    customer_id: Nullable[str]
    r"""The ID of the customer this invoice belongs to. Null for customers created without an ID."""

    entity_id: Nullable[str]
    r"""The ID of the entity this invoice belongs to, if entity-scoped"""

    amount_paid: Nullable[float]
    r"""The amount paid on the invoice. Null on invoices recorded before amounts paid were tracked."""

    refunded_amount: float
    r"""The total amount refunded on the invoice"""

    processor_type: Optional[ListInvoicesListProcessorType] = "stripe"
    r"""The billing processor that owns this invoice."""

    hosted_invoice_url: OptionalNullable[str] = UNSET
    r"""URL to the Stripe-hosted invoice page"""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["processor_type", "hosted_invoice_url"])
        nullable_fields = set(
            ["hosted_invoice_url", "customer_id", "entity_id", "amount_paid"]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


class ListInvoicesResponseTypedDict(TypedDict):
    r"""OK"""

    list: List[ListInvoicesListTypedDict]
    r"""Items for current page."""
    next_cursor: Nullable[str]
    r"""Opaque cursor for the next page. Null when there are no more results."""


class ListInvoicesResponse(BaseModel):
    r"""OK"""

    list: List[ListInvoicesList]
    r"""Items for current page."""

    next_cursor: Nullable[str]
    r"""Opaque cursor for the next page. Null when there are no more results."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                m[k] = val

        return m
