"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from autumn_sdk.types import BaseModel, Nullable, UNSET_SENTINEL, UnrecognizedStr
from autumn_sdk.utils import FieldMetadata, HeaderMetadata
import pydantic
from pydantic import model_serializer
from typing import List, Literal, Optional, Union
from typing_extensions import Annotated, NotRequired, TypedDict


class MultiUpdateGlobalsTypedDict(TypedDict):
    x_api_version: NotRequired[str]


class MultiUpdateGlobals(BaseModel):
    x_api_version: Annotated[
        Optional[str],
        pydantic.Field(alias="x-api-version"),
        FieldMetadata(header=HeaderMetadata(style="simple", explode=False)),
    ] = "2.3.0"

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["x-api-version"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


MultiUpdateCancelAction = Literal[
    "cancel_immediately",
    "cancel_end_of_cycle",
    "uncancel",
]
r"""Action to perform for cancellation. 'cancel_immediately' cancels now with prorated refund, 'cancel_end_of_cycle' cancels at period end, 'uncancel' reverses a pending cancellation."""


MultiUpdateProrationBehavior = Literal[
    "prorate_immediately",
    "none",
]
r"""How to handle proration for this update. 'prorate_immediately' charges/credits prorated amounts now, 'none' skips creating any charges."""


class MultiUpdateUpdateTypedDict(TypedDict):
    cancel_action: MultiUpdateCancelAction
    r"""Action to perform for cancellation. 'cancel_immediately' cancels now with prorated refund, 'cancel_end_of_cycle' cancels at period end, 'uncancel' reverses a pending cancellation."""
    plan_id: NotRequired[str]
    r"""The ID of the plan to update. Optional if subscription_id is provided."""
    subscription_id: NotRequired[str]
    r"""A unique ID to identify the subscription to update. Useful when a customer has multiple products with the same plan."""
    entity_id: NotRequired[str]
    r"""The ID of the entity this update targets. Overrides the top-level entity_id for this update."""
    proration_behavior: NotRequired[MultiUpdateProrationBehavior]
    r"""How to handle proration for this update. 'prorate_immediately' charges/credits prorated amounts now, 'none' skips creating any charges."""


class MultiUpdateUpdate(BaseModel):
    cancel_action: MultiUpdateCancelAction
    r"""Action to perform for cancellation. 'cancel_immediately' cancels now with prorated refund, 'cancel_end_of_cycle' cancels at period end, 'uncancel' reverses a pending cancellation."""

    plan_id: Optional[str] = None
    r"""The ID of the plan to update. Optional if subscription_id is provided."""

    subscription_id: Optional[str] = None
    r"""A unique ID to identify the subscription to update. Useful when a customer has multiple products with the same plan."""

    entity_id: Optional[str] = None
    r"""The ID of the entity this update targets. Overrides the top-level entity_id for this update."""

    proration_behavior: Optional[MultiUpdateProrationBehavior] = None
    r"""How to handle proration for this update. 'prorate_immediately' charges/credits prorated amounts now, 'none' skips creating any charges."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            ["plan_id", "subscription_id", "entity_id", "proration_behavior"]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class MultiUpdateParamsTypedDict(TypedDict):
    customer_id: str
    r"""The ID of the customer to update plans for."""
    updates: List[MultiUpdateUpdateTypedDict]
    r"""The list of plan updates to apply to the customer."""
    entity_id: NotRequired[str]
    r"""The ID of the entity to update plans for. Individual updates can override this with their own entity_id."""


class MultiUpdateParams(BaseModel):
    customer_id: str
    r"""The ID of the customer to update plans for."""

    updates: List[MultiUpdateUpdate]
    r"""The list of plan updates to apply to the customer."""

    entity_id: Optional[str] = None
    r"""The ID of the entity to update plans for. Individual updates can override this with their own entity_id."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["entity_id"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class MultiUpdateInvoiceTypedDict(TypedDict):
    r"""Invoice details if an invoice was created. Only present when a charge was made."""

    status: Nullable[str]
    r"""The status of the invoice (e.g., 'paid', 'open', 'draft')."""
    stripe_id: str
    r"""The Stripe invoice ID."""
    total: float
    r"""The total amount of the invoice in cents."""
    currency: str
    r"""The three-letter ISO currency code (e.g., 'usd')."""
    hosted_invoice_url: Nullable[str]
    r"""URL to the hosted invoice page where the customer can view and pay the invoice."""


class MultiUpdateInvoice(BaseModel):
    r"""Invoice details if an invoice was created. Only present when a charge was made."""

    status: Nullable[str]
    r"""The status of the invoice (e.g., 'paid', 'open', 'draft')."""

    stripe_id: str
    r"""The Stripe invoice ID."""

    total: float
    r"""The total amount of the invoice in cents."""

    currency: str
    r"""The three-letter ISO currency code (e.g., 'usd')."""

    hosted_invoice_url: Nullable[str]
    r"""URL to the hosted invoice page where the customer can view and pay the invoice."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                m[k] = val

        return m


MultiUpdateCode = Union[
    Literal[
        "3ds_required",
        "payment_method_required",
        "payment_failed",
        "payment_processing",
    ],
    UnrecognizedStr,
]
r"""The type of action required to complete the payment."""


class MultiUpdateRequiredActionTypedDict(TypedDict):
    r"""Details about any action required to complete the payment. Present when the payment could not be processed automatically."""

    code: MultiUpdateCode
    r"""The type of action required to complete the payment."""
    reason: str
    r"""A human-readable explanation of why this action is required."""


class MultiUpdateRequiredAction(BaseModel):
    r"""Details about any action required to complete the payment. Present when the payment could not be processed automatically."""

    code: MultiUpdateCode
    r"""The type of action required to complete the payment."""

    reason: str
    r"""A human-readable explanation of why this action is required."""


class MultiUpdateResponseTypedDict(TypedDict):
    r"""OK"""

    customer_id: str
    r"""The ID of the customer."""
    payment_url: Nullable[str]
    r"""URL to redirect the customer to complete payment. Null if no payment action is required."""
    entity_id: NotRequired[str]
    r"""The ID of the entity, if the plan was attached to an entity."""
    invoice: NotRequired[MultiUpdateInvoiceTypedDict]
    r"""Invoice details if an invoice was created. Only present when a charge was made."""
    required_action: NotRequired[MultiUpdateRequiredActionTypedDict]
    r"""Details about any action required to complete the payment. Present when the payment could not be processed automatically."""


class MultiUpdateResponse(BaseModel):
    r"""OK"""

    customer_id: str
    r"""The ID of the customer."""

    payment_url: Nullable[str]
    r"""URL to redirect the customer to complete payment. Null if no payment action is required."""

    entity_id: Optional[str] = None
    r"""The ID of the entity, if the plan was attached to an entity."""

    invoice: Optional[MultiUpdateInvoice] = None
    r"""Invoice details if an invoice was created. Only present when a charge was made."""

    required_action: Optional[MultiUpdateRequiredAction] = None
    r"""Details about any action required to complete the payment. Present when the payment could not be processed automatically."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["entity_id", "invoice", "required_action"])
        nullable_fields = set(["payment_url"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m
