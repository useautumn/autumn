"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from autumn_sdk.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
    UnrecognizedStr,
)
from autumn_sdk.utils import FieldMetadata, HeaderMetadata, validate_const
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Any, Dict, List, Literal, Optional, Union
from typing_extensions import Annotated, NotRequired, TypeAliasType, TypedDict


class CreateInvoiceGlobalsTypedDict(TypedDict):
    x_api_version: NotRequired[str]


class CreateInvoiceGlobals(BaseModel):
    x_api_version: Annotated[
        Optional[str],
        pydantic.Field(alias="x-api-version"),
        FieldMetadata(header=HeaderMetadata(style="simple", explode=False)),
    ] = "2.4.0"

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["x-api-version"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


CreateInvoiceInterval = Literal[
    "one_off",
    "week",
    "month",
    "quarter",
    "semi_annual",
    "year",
]
r"""Billing interval (e.g. 'month', 'year')."""


class CreateInvoiceStripeTypedDict(TypedDict):
    price_id: str
    r"""Stripe price ID. For prepaid with included > 0 this is the V2 price."""


class CreateInvoiceStripe(BaseModel):
    price_id: str
    r"""Stripe price ID. For prepaid with included > 0 this is the V2 price."""


class CreateInvoiceProcessorsTypedDict(TypedDict):
    r"""Bill this line under an existing Stripe price instead of an inline one."""

    stripe: NotRequired[Nullable[CreateInvoiceStripeTypedDict]]


class CreateInvoiceProcessors(BaseModel):
    r"""Bill this line under an existing Stripe price instead of an inline one."""

    stripe: OptionalNullable[CreateInvoiceStripe] = UNSET

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["stripe"])
        nullable_fields = set(["stripe"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


class CreateInvoicePriceTypedDict(TypedDict):
    amount: float
    r"""Base price amount for the plan, in major currency units (e.g. dollars)."""
    interval: CreateInvoiceInterval
    r"""Billing interval (e.g. 'month', 'year')."""
    interval_count: NotRequired[float]
    r"""Number of intervals per billing cycle. Defaults to 1."""
    processors: NotRequired[CreateInvoiceProcessorsTypedDict]
    r"""Bill this line under an existing Stripe price instead of an inline one."""


class CreateInvoicePrice(BaseModel):
    amount: float
    r"""Base price amount for the plan, in major currency units (e.g. dollars)."""

    interval: CreateInvoiceInterval
    r"""Billing interval (e.g. 'month', 'year')."""

    interval_count: Optional[float] = 1
    r"""Number of intervals per billing cycle. Defaults to 1."""

    processors: Optional[CreateInvoiceProcessors] = None
    r"""Bill this line under an existing Stripe price instead of an inline one."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["interval_count", "processors"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


CreateInvoicePriceToTypedDict = TypeAliasType(
    "CreateInvoicePriceToTypedDict", Union[float, str]
)


CreateInvoicePriceTo = TypeAliasType("CreateInvoicePriceTo", Union[float, str])


class CreateInvoiceAdditionalCurrencyTypedDict(TypedDict):
    currency: str
    r"""Three-letter Stripe-supported currency code (e.g. 'eur', 'gbp')."""
    amount: NotRequired[float]
    r"""Per-unit amount for this tier in this currency."""
    flat_amount: NotRequired[float]
    r"""Flat amount for this tier in this currency, if the tier uses one."""


class CreateInvoiceAdditionalCurrency(BaseModel):
    currency: str
    r"""Three-letter Stripe-supported currency code (e.g. 'eur', 'gbp')."""

    amount: Optional[float] = None
    r"""Per-unit amount for this tier in this currency."""

    flat_amount: Optional[float] = None
    r"""Flat amount for this tier in this currency, if the tier uses one."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["amount", "flat_amount"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class CreateInvoicePriceTierTypedDict(TypedDict):
    to: CreateInvoicePriceToTypedDict
    amount: NotRequired[float]
    flat_amount: NotRequired[float]
    additional_currencies: NotRequired[List[CreateInvoiceAdditionalCurrencyTypedDict]]
    r"""Per-currency amounts for this tier. Tier boundaries ('to') are shared across all currencies."""


class CreateInvoicePriceTier(BaseModel):
    to: CreateInvoicePriceTo

    amount: Optional[float] = None

    flat_amount: Optional[float] = None

    additional_currencies: Optional[List[CreateInvoiceAdditionalCurrency]] = None
    r"""Per-currency amounts for this tier. Tier boundaries ('to') are shared across all currencies."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["amount", "flat_amount", "additional_currencies"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


CreateInvoiceTierBehavior = Literal[
    "graduated",
    "volume",
]


CreateInvoiceItemInterval = Literal[
    "one_off",
    "week",
    "month",
    "quarter",
    "semi_annual",
    "year",
]
r"""Billing interval. For consumable features, should match reset.interval."""


CreateInvoiceBillingMethod = Literal[
    "prepaid",
    "usage_based",
]
r"""'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go."""


class CreateInvoiceItemStripeTypedDict(TypedDict):
    price_id: str
    r"""Stripe price ID. For prepaid with included > 0 this is the V2 price."""


class CreateInvoiceItemStripe(BaseModel):
    price_id: str
    r"""Stripe price ID. For prepaid with included > 0 this is the V2 price."""


class CreateInvoiceItemProcessorsTypedDict(TypedDict):
    r"""Bill this line under an existing Stripe price instead of an inline one."""

    stripe: NotRequired[Nullable[CreateInvoiceItemStripeTypedDict]]


class CreateInvoiceItemProcessors(BaseModel):
    r"""Bill this line under an existing Stripe price instead of an inline one."""

    stripe: OptionalNullable[CreateInvoiceItemStripe] = UNSET

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["stripe"])
        nullable_fields = set(["stripe"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


class CreateInvoiceItemPriceTypedDict(TypedDict):
    r"""Pricing to use for this feature on this invoice."""

    interval: CreateInvoiceItemInterval
    r"""Billing interval. For consumable features, should match reset.interval."""
    billing_method: CreateInvoiceBillingMethod
    r"""'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go."""
    amount: NotRequired[float]
    r"""Price per billing_units after included usage. Either 'amount' or 'tiers' is required."""
    tiers: NotRequired[List[CreateInvoicePriceTierTypedDict]]
    r"""Tiered pricing.  Either 'amount' or 'tiers' is required."""
    tier_behavior: NotRequired[CreateInvoiceTierBehavior]
    interval_count: NotRequired[float]
    r"""Number of intervals per billing cycle. Defaults to 1."""
    billing_units: NotRequired[float]
    r"""Units per price increment. Usage is rounded UP when billed (e.g. billing_units=100 means 101 rounds to 200)."""
    processors: NotRequired[CreateInvoiceItemProcessorsTypedDict]
    r"""Bill this line under an existing Stripe price instead of an inline one."""


class CreateInvoiceItemPrice(BaseModel):
    r"""Pricing to use for this feature on this invoice."""

    interval: CreateInvoiceItemInterval
    r"""Billing interval. For consumable features, should match reset.interval."""

    billing_method: CreateInvoiceBillingMethod
    r"""'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go."""

    amount: Optional[float] = None
    r"""Price per billing_units after included usage. Either 'amount' or 'tiers' is required."""

    tiers: Optional[List[CreateInvoicePriceTier]] = None
    r"""Tiered pricing.  Either 'amount' or 'tiers' is required."""

    tier_behavior: Optional[CreateInvoiceTierBehavior] = None

    interval_count: Optional[float] = 1
    r"""Number of intervals per billing cycle. Defaults to 1."""

    billing_units: Optional[float] = 1
    r"""Units per price increment. Usage is rounded UP when billed (e.g. billing_units=100 means 101 rounds to 200)."""

    processors: Optional[CreateInvoiceItemProcessors] = None
    r"""Bill this line under an existing Stripe price instead of an inline one."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "amount",
                "tiers",
                "tier_behavior",
                "interval_count",
                "billing_units",
                "processors",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


CreateInvoiceDimensionsMatch4TypedDict = TypeAliasType(
    "CreateInvoiceDimensionsMatch4TypedDict", Union[str, float, bool]
)


CreateInvoiceDimensionsMatch4 = TypeAliasType(
    "CreateInvoiceDimensionsMatch4", Union[str, float, bool]
)


class CreateInvoiceDimensions4TypedDict(TypedDict):
    match: Dict[str, CreateInvoiceDimensionsMatch4TypedDict]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""
    credit_cost: float
    r"""Credits consumed per billing-unit group when this dimension matches."""
    priority: NotRequired[int]
    r"""Breaks ties between dimensions that match the same number of keys. Higher wins."""


class CreateInvoiceDimensions4(BaseModel):
    match: Dict[str, CreateInvoiceDimensionsMatch4]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""

    credit_cost: float
    r"""Credits consumed per billing-unit group when this dimension matches."""

    priority: Optional[int] = None
    r"""Breaks ties between dimensions that match the same number of keys. Higher wins."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["priority"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


CreateInvoiceDimensionsMatch3TypedDict = TypeAliasType(
    "CreateInvoiceDimensionsMatch3TypedDict", Union[str, float, bool]
)


CreateInvoiceDimensionsMatch3 = TypeAliasType(
    "CreateInvoiceDimensionsMatch3", Union[str, float, bool]
)


CreateInvoiceDimensionsToEnum2 = Literal["inf",]


CreateInvoiceDimensionsToUnion2TypedDict = TypeAliasType(
    "CreateInvoiceDimensionsToUnion2TypedDict",
    Union[float, CreateInvoiceDimensionsToEnum2],
)
r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""


CreateInvoiceDimensionsToUnion2 = TypeAliasType(
    "CreateInvoiceDimensionsToUnion2", Union[float, CreateInvoiceDimensionsToEnum2]
)
r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""


class CreateInvoiceDimensionsTier2TypedDict(TypedDict):
    to: CreateInvoiceDimensionsToUnion2TypedDict
    r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""
    credit_cost: float
    r"""Credits consumed per billing-unit group within this tier."""


class CreateInvoiceDimensionsTier2(BaseModel):
    to: CreateInvoiceDimensionsToUnion2
    r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""

    credit_cost: float
    r"""Credits consumed per billing-unit group within this tier."""


class CreateInvoiceDimensions3TypedDict(TypedDict):
    match: Dict[str, CreateInvoiceDimensionsMatch3TypedDict]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""
    tiers: List[CreateInvoiceDimensionsTier2TypedDict]
    priority: NotRequired[int]
    r"""Breaks ties between dimensions that match the same number of keys. Higher wins."""
    tier_behavior: Literal["graduated"]


class CreateInvoiceDimensions3(BaseModel):
    match: Dict[str, CreateInvoiceDimensionsMatch3]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""

    tiers: List[CreateInvoiceDimensionsTier2]

    priority: Optional[int] = None
    r"""Breaks ties between dimensions that match the same number of keys. Higher wins."""

    tier_behavior: Annotated[
        Annotated[Literal["graduated"], AfterValidator(validate_const("graduated"))],
        pydantic.Field(alias="tier_behavior"),
    ] = "graduated"

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["priority"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


CreateInvoiceDimensionsUnion2TypedDict = TypeAliasType(
    "CreateInvoiceDimensionsUnion2TypedDict",
    Union[CreateInvoiceDimensions4TypedDict, CreateInvoiceDimensions3TypedDict],
)


CreateInvoiceDimensionsUnion2 = TypeAliasType(
    "CreateInvoiceDimensionsUnion2",
    Union[CreateInvoiceDimensions4, CreateInvoiceDimensions3],
)


CreateInvoiceMultipliersMatch2TypedDict = TypeAliasType(
    "CreateInvoiceMultipliersMatch2TypedDict", Union[str, float, bool]
)


CreateInvoiceMultipliersMatch2 = TypeAliasType(
    "CreateInvoiceMultipliersMatch2", Union[str, float, bool]
)


class CreateInvoiceMultipliers2TypedDict(TypedDict):
    match: Dict[str, CreateInvoiceMultipliersMatch2TypedDict]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""
    factor: NotRequired[float]
    r"""Multiplies the matched rate. All matching multipliers stack."""
    add: NotRequired[float]
    r"""Added to the rate after every factor is applied, in credits per billing-unit group."""


class CreateInvoiceMultipliers2(BaseModel):
    match: Dict[str, CreateInvoiceMultipliersMatch2]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""

    factor: Optional[float] = None
    r"""Multiplies the matched rate. All matching multipliers stack."""

    add: Optional[float] = None
    r"""Added to the rate after every factor is applied, in credits per billing-unit group."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["factor", "add"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class CreateInvoiceCreditSchema2TypedDict(TypedDict):
    metered_feature_id: str
    r"""ID of the metered feature that draws from this credit system."""
    credit_cost: float
    r"""Credits consumed per billing-unit group."""
    billing_units: NotRequired[float]
    r"""Number of metered-feature units priced together. Defaults to one when omitted."""
    dimensions: NotRequired[Dict[str, CreateInvoiceDimensionsUnion2TypedDict]]
    r"""Named rates chosen by event properties. The most specific match sets the rate; with no match the item's own rate applies."""
    multipliers: NotRequired[Dict[str, CreateInvoiceMultipliers2TypedDict]]
    r"""Named adjustments chosen by event properties. Every match applies: factors multiply, then adds are summed."""


class CreateInvoiceCreditSchema2(BaseModel):
    metered_feature_id: str
    r"""ID of the metered feature that draws from this credit system."""

    credit_cost: float
    r"""Credits consumed per billing-unit group."""

    billing_units: Optional[float] = None
    r"""Number of metered-feature units priced together. Defaults to one when omitted."""

    dimensions: Optional[Dict[str, CreateInvoiceDimensionsUnion2]] = None
    r"""Named rates chosen by event properties. The most specific match sets the rate; with no match the item's own rate applies."""

    multipliers: Optional[Dict[str, CreateInvoiceMultipliers2]] = None
    r"""Named adjustments chosen by event properties. Every match applies: factors multiply, then adds are summed."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["billing_units", "dimensions", "multipliers"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


CreateInvoiceDimensionsMatch2TypedDict = TypeAliasType(
    "CreateInvoiceDimensionsMatch2TypedDict", Union[str, float, bool]
)


CreateInvoiceDimensionsMatch2 = TypeAliasType(
    "CreateInvoiceDimensionsMatch2", Union[str, float, bool]
)


class CreateInvoiceDimensions2TypedDict(TypedDict):
    match: Dict[str, CreateInvoiceDimensionsMatch2TypedDict]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""
    credit_cost: float
    r"""Credits consumed per billing-unit group when this dimension matches."""
    priority: NotRequired[int]
    r"""Breaks ties between dimensions that match the same number of keys. Higher wins."""


class CreateInvoiceDimensions2(BaseModel):
    match: Dict[str, CreateInvoiceDimensionsMatch2]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""

    credit_cost: float
    r"""Credits consumed per billing-unit group when this dimension matches."""

    priority: Optional[int] = None
    r"""Breaks ties between dimensions that match the same number of keys. Higher wins."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["priority"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


CreateInvoiceDimensionsMatch1TypedDict = TypeAliasType(
    "CreateInvoiceDimensionsMatch1TypedDict", Union[str, float, bool]
)


CreateInvoiceDimensionsMatch1 = TypeAliasType(
    "CreateInvoiceDimensionsMatch1", Union[str, float, bool]
)


CreateInvoiceDimensionsToEnum1 = Literal["inf",]


CreateInvoiceDimensionsToUnion1TypedDict = TypeAliasType(
    "CreateInvoiceDimensionsToUnion1TypedDict",
    Union[float, CreateInvoiceDimensionsToEnum1],
)
r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""


CreateInvoiceDimensionsToUnion1 = TypeAliasType(
    "CreateInvoiceDimensionsToUnion1", Union[float, CreateInvoiceDimensionsToEnum1]
)
r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""


class CreateInvoiceDimensionsTier1TypedDict(TypedDict):
    to: CreateInvoiceDimensionsToUnion1TypedDict
    r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""
    credit_cost: float
    r"""Credits consumed per billing-unit group within this tier."""


class CreateInvoiceDimensionsTier1(BaseModel):
    to: CreateInvoiceDimensionsToUnion1
    r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""

    credit_cost: float
    r"""Credits consumed per billing-unit group within this tier."""


class CreateInvoiceDimensions1TypedDict(TypedDict):
    match: Dict[str, CreateInvoiceDimensionsMatch1TypedDict]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""
    tiers: List[CreateInvoiceDimensionsTier1TypedDict]
    priority: NotRequired[int]
    r"""Breaks ties between dimensions that match the same number of keys. Higher wins."""
    tier_behavior: Literal["graduated"]


class CreateInvoiceDimensions1(BaseModel):
    match: Dict[str, CreateInvoiceDimensionsMatch1]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""

    tiers: List[CreateInvoiceDimensionsTier1]

    priority: Optional[int] = None
    r"""Breaks ties between dimensions that match the same number of keys. Higher wins."""

    tier_behavior: Annotated[
        Annotated[Literal["graduated"], AfterValidator(validate_const("graduated"))],
        pydantic.Field(alias="tier_behavior"),
    ] = "graduated"

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["priority"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


CreateInvoiceDimensionsUnion1TypedDict = TypeAliasType(
    "CreateInvoiceDimensionsUnion1TypedDict",
    Union[CreateInvoiceDimensions2TypedDict, CreateInvoiceDimensions1TypedDict],
)


CreateInvoiceDimensionsUnion1 = TypeAliasType(
    "CreateInvoiceDimensionsUnion1",
    Union[CreateInvoiceDimensions2, CreateInvoiceDimensions1],
)


CreateInvoiceMultipliersMatch1TypedDict = TypeAliasType(
    "CreateInvoiceMultipliersMatch1TypedDict", Union[str, float, bool]
)


CreateInvoiceMultipliersMatch1 = TypeAliasType(
    "CreateInvoiceMultipliersMatch1", Union[str, float, bool]
)


class CreateInvoiceMultipliers1TypedDict(TypedDict):
    match: Dict[str, CreateInvoiceMultipliersMatch1TypedDict]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""
    factor: NotRequired[float]
    r"""Multiplies the matched rate. All matching multipliers stack."""
    add: NotRequired[float]
    r"""Added to the rate after every factor is applied, in credits per billing-unit group."""


class CreateInvoiceMultipliers1(BaseModel):
    match: Dict[str, CreateInvoiceMultipliersMatch1]
    r"""Event properties this entry applies to. Every key must equal the tracked property, compared as strings."""

    factor: Optional[float] = None
    r"""Multiplies the matched rate. All matching multipliers stack."""

    add: Optional[float] = None
    r"""Added to the rate after every factor is applied, in credits per billing-unit group."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["factor", "add"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


CreateInvoiceToEnum = Literal["inf",]


CreateInvoiceFeatureOverrideToUnionTypedDict = TypeAliasType(
    "CreateInvoiceFeatureOverrideToUnionTypedDict", Union[float, CreateInvoiceToEnum]
)
r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""


CreateInvoiceFeatureOverrideToUnion = TypeAliasType(
    "CreateInvoiceFeatureOverrideToUnion", Union[float, CreateInvoiceToEnum]
)
r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""


class CreateInvoiceFeatureOverrideTierTypedDict(TypedDict):
    to: CreateInvoiceFeatureOverrideToUnionTypedDict
    r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""
    credit_cost: float
    r"""Credits consumed per billing-unit group within this tier."""


class CreateInvoiceFeatureOverrideTier(BaseModel):
    to: CreateInvoiceFeatureOverrideToUnion
    r"""Inclusive upper usage boundary for this graduated tier. The final tier must be 'inf'."""

    credit_cost: float
    r"""Credits consumed per billing-unit group within this tier."""


class CreateInvoiceCreditSchema1TypedDict(TypedDict):
    metered_feature_id: str
    r"""ID of the metered feature that draws from this credit system."""
    tiers: List[CreateInvoiceFeatureOverrideTierTypedDict]
    billing_units: NotRequired[float]
    r"""Number of metered-feature units priced together. Defaults to one when omitted."""
    dimensions: NotRequired[Dict[str, CreateInvoiceDimensionsUnion1TypedDict]]
    r"""Named rates chosen by event properties. The most specific match sets the rate; with no match the item's own rate applies."""
    multipliers: NotRequired[Dict[str, CreateInvoiceMultipliers1TypedDict]]
    r"""Named adjustments chosen by event properties. Every match applies: factors multiply, then adds are summed."""
    tier_behavior: Literal["graduated"]


class CreateInvoiceCreditSchema1(BaseModel):
    metered_feature_id: str
    r"""ID of the metered feature that draws from this credit system."""

    tiers: List[CreateInvoiceFeatureOverrideTier]

    billing_units: Optional[float] = None
    r"""Number of metered-feature units priced together. Defaults to one when omitted."""

    dimensions: Optional[Dict[str, CreateInvoiceDimensionsUnion1]] = None
    r"""Named rates chosen by event properties. The most specific match sets the rate; with no match the item's own rate applies."""

    multipliers: Optional[Dict[str, CreateInvoiceMultipliers1]] = None
    r"""Named adjustments chosen by event properties. Every match applies: factors multiply, then adds are summed."""

    tier_behavior: Annotated[
        Annotated[Literal["graduated"], AfterValidator(validate_const("graduated"))],
        pydantic.Field(alias="tier_behavior"),
    ] = "graduated"

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["billing_units", "dimensions", "multipliers"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


CreateInvoiceCreditSchemaUnionTypedDict = TypeAliasType(
    "CreateInvoiceCreditSchemaUnionTypedDict",
    Union[CreateInvoiceCreditSchema2TypedDict, CreateInvoiceCreditSchema1TypedDict],
)


CreateInvoiceCreditSchemaUnion = TypeAliasType(
    "CreateInvoiceCreditSchemaUnion",
    Union[CreateInvoiceCreditSchema2, CreateInvoiceCreditSchema1],
)


class CreateInvoiceFeatureOverrideTypedDict(TypedDict):
    r"""For credit-system features: a credit rate card to use when converting `usage` on this invoice."""

    credit_schema: NotRequired[List[CreateInvoiceCreditSchemaUnionTypedDict]]
    r"""For credit system features: replaces the feature's credit_schema entirely for customers on this plan."""


class CreateInvoiceFeatureOverride(BaseModel):
    r"""For credit-system features: a credit rate card to use when converting `usage` on this invoice."""

    credit_schema: Optional[List[CreateInvoiceCreditSchemaUnion]] = None
    r"""For credit system features: replaces the feature's credit_schema entirely for customers on this plan."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["credit_schema"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class CreateInvoiceCustomizeItemTypedDict(TypedDict):
    feature_id: str
    r"""The feature whose pricing is overridden on this invoice."""
    price: NotRequired[CreateInvoiceItemPriceTypedDict]
    r"""Pricing to use for this feature on this invoice."""
    feature_override: NotRequired[CreateInvoiceFeatureOverrideTypedDict]
    r"""For credit-system features: a credit rate card to use when converting `usage` on this invoice."""


class CreateInvoiceCustomizeItem(BaseModel):
    feature_id: str
    r"""The feature whose pricing is overridden on this invoice."""

    price: Optional[CreateInvoiceItemPrice] = None
    r"""Pricing to use for this feature on this invoice."""

    feature_override: Optional[CreateInvoiceFeatureOverride] = None
    r"""For credit-system features: a credit rate card to use when converting `usage` on this invoice."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["price", "feature_override"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class CreateInvoiceInvoiceCustomizeTypedDict(TypedDict):
    r"""Pricing overrides applied to this invoice only. The catalog and the customer's plan are not changed."""

    price: NotRequired[Nullable[CreateInvoicePriceTypedDict]]
    r"""Override the plan's base price for this invoice. Pass null or an amount of 0 to omit the base price line."""
    items: NotRequired[List[CreateInvoiceCustomizeItemTypedDict]]
    r"""Override feature pricing for this invoice. Only pricing fields are accepted; grants, resets and rollovers are not part of an invoice."""


class CreateInvoiceInvoiceCustomize(BaseModel):
    r"""Pricing overrides applied to this invoice only. The catalog and the customer's plan are not changed."""

    price: OptionalNullable[CreateInvoicePrice] = UNSET
    r"""Override the plan's base price for this invoice. Pass null or an amount of 0 to omit the base price line."""

    items: Optional[List[CreateInvoiceCustomizeItem]] = None
    r"""Override feature pricing for this invoice. Only pricing fields are accepted; grants, resets and rollovers are not part of an invoice."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["price", "items"])
        nullable_fields = set(["price"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


CreateInvoiceBillingBehavior = Literal[
    "prepaid",
    "usage_based",
]
r"""Which of the feature's prices to use: 'prepaid' or 'usage_based'."""


class CreateInvoiceUsageTypedDict(TypedDict):
    feature_id: str
    r"""The metered feature whose units are being converted."""
    quantity: float
    r"""Billable units of the metered feature. Converted to the credit-system feature through its rate card."""
    properties: NotRequired[Dict[str, Any]]
    r"""Event properties used to pick the rate card dimension and multipliers."""


class CreateInvoiceUsage(BaseModel):
    feature_id: str
    r"""The metered feature whose units are being converted."""

    quantity: float
    r"""Billable units of the metered feature. Converted to the credit-system feature through its rate card."""

    properties: Optional[Dict[str, Any]] = None
    r"""Event properties used to pick the rate card dimension and multipliers."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["properties"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class CreateInvoiceFeatureQuantityTypedDict(TypedDict):
    feature_id: str
    r"""The feature to bill."""
    billing_behavior: CreateInvoiceBillingBehavior
    r"""Which of the feature's prices to use: 'prepaid' or 'usage_based'."""
    quantity: NotRequired[float]
    r"""Billable feature units in total, exclusive of any included usage. Not per seat or per entity. For a credit-system feature this is the number of credits."""
    usage: NotRequired[List[CreateInvoiceUsageTypedDict]]
    r"""For credit-system features: billable units of the source features, converted through the credit rate card. Mutually exclusive with quantity."""
    prorate: NotRequired[bool]
    r"""Whether to prorate this line against period_start / period_end. Defaults to true for prepaid and false for usage-based."""


class CreateInvoiceFeatureQuantity(BaseModel):
    feature_id: str
    r"""The feature to bill."""

    billing_behavior: CreateInvoiceBillingBehavior
    r"""Which of the feature's prices to use: 'prepaid' or 'usage_based'."""

    quantity: Optional[float] = None
    r"""Billable feature units in total, exclusive of any included usage. Not per seat or per entity. For a credit-system feature this is the number of credits."""

    usage: Optional[List[CreateInvoiceUsage]] = None
    r"""For credit-system features: billable units of the source features, converted through the credit rate card. Mutually exclusive with quantity."""

    prorate: Optional[bool] = None
    r"""Whether to prorate this line against period_start / period_end. Defaults to true for prepaid and false for usage-based."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["quantity", "usage", "prorate"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


CreateInvoiceLicenseQuantityInterval = Literal[
    "one_off",
    "week",
    "month",
    "quarter",
    "semi_annual",
    "year",
]
r"""Billing interval (e.g. 'month', 'year')."""


class CreateInvoiceLicenseQuantityStripeTypedDict(TypedDict):
    price_id: str
    r"""Stripe price ID. For prepaid with included > 0 this is the V2 price."""


class CreateInvoiceLicenseQuantityStripe(BaseModel):
    price_id: str
    r"""Stripe price ID. For prepaid with included > 0 this is the V2 price."""


class CreateInvoiceLicenseQuantityProcessorsTypedDict(TypedDict):
    r"""Bill this line under an existing Stripe price instead of an inline one."""

    stripe: NotRequired[Nullable[CreateInvoiceLicenseQuantityStripeTypedDict]]


class CreateInvoiceLicenseQuantityProcessors(BaseModel):
    r"""Bill this line under an existing Stripe price instead of an inline one."""

    stripe: OptionalNullable[CreateInvoiceLicenseQuantityStripe] = UNSET

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["stripe"])
        nullable_fields = set(["stripe"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


class CreateInvoiceLicenseQuantityPriceTypedDict(TypedDict):
    amount: float
    r"""Base price amount for the plan, in major currency units (e.g. dollars)."""
    interval: CreateInvoiceLicenseQuantityInterval
    r"""Billing interval (e.g. 'month', 'year')."""
    interval_count: NotRequired[float]
    r"""Number of intervals per billing cycle. Defaults to 1."""
    processors: NotRequired[CreateInvoiceLicenseQuantityProcessorsTypedDict]
    r"""Bill this line under an existing Stripe price instead of an inline one."""


class CreateInvoiceLicenseQuantityPrice(BaseModel):
    amount: float
    r"""Base price amount for the plan, in major currency units (e.g. dollars)."""

    interval: CreateInvoiceLicenseQuantityInterval
    r"""Billing interval (e.g. 'month', 'year')."""

    interval_count: Optional[float] = 1
    r"""Number of intervals per billing cycle. Defaults to 1."""

    processors: Optional[CreateInvoiceLicenseQuantityProcessors] = None
    r"""Bill this line under an existing Stripe price instead of an inline one."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["interval_count", "processors"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class CreateInvoiceCustomizeTypedDict(TypedDict):
    r"""Override the license's per-seat price on this invoice."""

    price: NotRequired[Nullable[CreateInvoiceLicenseQuantityPriceTypedDict]]


class CreateInvoiceCustomize(BaseModel):
    r"""Override the license's per-seat price on this invoice."""

    price: OptionalNullable[CreateInvoiceLicenseQuantityPrice] = UNSET

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["price"])
        nullable_fields = set(["price"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


CreateInvoiceLicenseQuantityBillingBehavior = Literal[
    "prepaid",
    "usage_based",
]
r"""Which of the feature's prices to use: 'prepaid' or 'usage_based'."""


class CreateInvoiceLicenseQuantityUsageTypedDict(TypedDict):
    feature_id: str
    r"""The metered feature whose units are being converted."""
    quantity: float
    r"""Billable units of the metered feature. Converted to the credit-system feature through its rate card."""
    properties: NotRequired[Dict[str, Any]]
    r"""Event properties used to pick the rate card dimension and multipliers."""


class CreateInvoiceLicenseQuantityUsage(BaseModel):
    feature_id: str
    r"""The metered feature whose units are being converted."""

    quantity: float
    r"""Billable units of the metered feature. Converted to the credit-system feature through its rate card."""

    properties: Optional[Dict[str, Any]] = None
    r"""Event properties used to pick the rate card dimension and multipliers."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["properties"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class CreateInvoiceLicenseQuantityFeatureQuantityTypedDict(TypedDict):
    feature_id: str
    r"""The feature to bill."""
    billing_behavior: CreateInvoiceLicenseQuantityBillingBehavior
    r"""Which of the feature's prices to use: 'prepaid' or 'usage_based'."""
    quantity: NotRequired[float]
    r"""Billable feature units in total, exclusive of any included usage. Not per seat or per entity. For a credit-system feature this is the number of credits."""
    usage: NotRequired[List[CreateInvoiceLicenseQuantityUsageTypedDict]]
    r"""For credit-system features: billable units of the source features, converted through the credit rate card. Mutually exclusive with quantity."""
    prorate: NotRequired[bool]
    r"""Whether to prorate this line against period_start / period_end. Defaults to true for prepaid and false for usage-based."""


class CreateInvoiceLicenseQuantityFeatureQuantity(BaseModel):
    feature_id: str
    r"""The feature to bill."""

    billing_behavior: CreateInvoiceLicenseQuantityBillingBehavior
    r"""Which of the feature's prices to use: 'prepaid' or 'usage_based'."""

    quantity: Optional[float] = None
    r"""Billable feature units in total, exclusive of any included usage. Not per seat or per entity. For a credit-system feature this is the number of credits."""

    usage: Optional[List[CreateInvoiceLicenseQuantityUsage]] = None
    r"""For credit-system features: billable units of the source features, converted through the credit rate card. Mutually exclusive with quantity."""

    prorate: Optional[bool] = None
    r"""Whether to prorate this line against period_start / period_end. Defaults to true for prepaid and false for usage-based."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["quantity", "usage", "prorate"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class CreateInvoiceLicenseQuantityTypedDict(TypedDict):
    license_plan_id: str
    r"""The license plan linked to the parent plan."""
    quantity: int
    r"""Billable seats, exclusive of any included seats."""
    customize: NotRequired[CreateInvoiceCustomizeTypedDict]
    r"""Override the license's per-seat price on this invoice."""
    feature_quantities: NotRequired[
        List[CreateInvoiceLicenseQuantityFeatureQuantityTypedDict]
    ]
    r"""Feature charges priced through the license plan."""
    prorate: NotRequired[bool]
    r"""Whether to prorate seat charges against period_start / period_end. Defaults to true."""


class CreateInvoiceLicenseQuantity(BaseModel):
    license_plan_id: str
    r"""The license plan linked to the parent plan."""

    quantity: int
    r"""Billable seats, exclusive of any included seats."""

    customize: Optional[CreateInvoiceCustomize] = None
    r"""Override the license's per-seat price on this invoice."""

    feature_quantities: Optional[List[CreateInvoiceLicenseQuantityFeatureQuantity]] = (
        None
    )
    r"""Feature charges priced through the license plan."""

    prorate: Optional[bool] = None
    r"""Whether to prorate seat charges against period_start / period_end. Defaults to true."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["customize", "feature_quantities", "prorate"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class PlanAttachDiscountTypedDict(TypedDict):
    r"""A discount to apply. Can be either a reward ID or a promotion code."""

    reward_id: NotRequired[str]
    r"""The ID of the reward to apply as a discount."""
    promotion_code: NotRequired[str]
    r"""The promotion code to apply as a discount."""


class PlanAttachDiscount(BaseModel):
    r"""A discount to apply. Can be either a reward ID or a promotion code."""

    reward_id: Optional[str] = None
    r"""The ID of the reward to apply as a discount."""

    promotion_code: Optional[str] = None
    r"""The promotion code to apply as a discount."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["reward_id", "promotion_code"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class CreateInvoicePlanTypedDict(TypedDict):
    plan_id: str
    r"""The catalog plan (or variant) whose pricing to use."""
    version: NotRequired[float]
    r"""Plan version. Defaults to the active version."""
    customize: NotRequired[CreateInvoiceInvoiceCustomizeTypedDict]
    r"""Pricing overrides applied to this invoice only. The catalog and the customer's plan are not changed."""
    feature_quantities: NotRequired[List[CreateInvoiceFeatureQuantityTypedDict]]
    license_quantities: NotRequired[List[CreateInvoiceLicenseQuantityTypedDict]]
    discounts: NotRequired[List[PlanAttachDiscountTypedDict]]
    r"""Discounts applied only to this plan's lines."""
    prorate: NotRequired[bool]
    r"""Whether to prorate the base price against period_start / period_end. Defaults to true."""


class CreateInvoicePlan(BaseModel):
    plan_id: str
    r"""The catalog plan (or variant) whose pricing to use."""

    version: Optional[float] = None
    r"""Plan version. Defaults to the active version."""

    customize: Optional[CreateInvoiceInvoiceCustomize] = None
    r"""Pricing overrides applied to this invoice only. The catalog and the customer's plan are not changed."""

    feature_quantities: Optional[List[CreateInvoiceFeatureQuantity]] = None

    license_quantities: Optional[List[CreateInvoiceLicenseQuantity]] = None

    discounts: Optional[List[PlanAttachDiscount]] = None
    r"""Discounts applied only to this plan's lines."""

    prorate: Optional[bool] = None
    r"""Whether to prorate the base price against period_start / period_end. Defaults to true."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "version",
                "customize",
                "feature_quantities",
                "license_quantities",
                "discounts",
                "prorate",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class CreateInvoiceCustomLineItemTypedDict(TypedDict):
    amount: float
    r"""Amount in dollars for this line item (e.g. 10.50). Can be negative for credits."""
    description: str
    r"""Description for the line item."""


class CreateInvoiceCustomLineItem(BaseModel):
    amount: float
    r"""Amount in dollars for this line item (e.g. 10.50). Can be negative for credits."""

    description: str
    r"""Description for the line item."""


class CreateInvoiceAttachDiscountTypedDict(TypedDict):
    r"""A discount to apply. Can be either a reward ID or a promotion code."""

    reward_id: NotRequired[str]
    r"""The ID of the reward to apply as a discount."""
    promotion_code: NotRequired[str]
    r"""The promotion code to apply as a discount."""


class CreateInvoiceAttachDiscount(BaseModel):
    r"""A discount to apply. Can be either a reward ID or a promotion code."""

    reward_id: Optional[str] = None
    r"""The ID of the reward to apply as a discount."""

    promotion_code: Optional[str] = None
    r"""The promotion code to apply as a discount."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["reward_id", "promotion_code"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class CreateInvoiceParamsTypedDict(TypedDict):
    customer_id: str
    r"""The customer to invoice."""
    plans: NotRequired[List[CreateInvoicePlanTypedDict]]
    custom_line_items: NotRequired[List[CreateInvoiceCustomLineItemTypedDict]]
    r"""Charges that are not tied to any plan or feature."""
    discounts: NotRequired[List[CreateInvoiceAttachDiscountTypedDict]]
    r"""Discounts applied to the whole invoice."""
    invoice_template_id: NotRequired[str]
    r"""ID of an invoice template whose footer, memo and default payment terms are applied."""
    net_terms_days: NotRequired[int]
    r"""Days until the invoice is due. Defaults to the template's terms, then the org default."""
    issue_date: NotRequired[int]
    r"""Date of issue printed on the invoice, in milliseconds. Defaults to now; cannot be in the future."""
    due_date: NotRequired[int]
    r"""When payment is due, in milliseconds. Must be in the future; takes precedence over net_terms_days."""
    tax_rate_id: NotRequired[str]
    r"""Stripe tax rate ID (txr_...) applied to every line."""
    period_start: NotRequired[int]
    r"""Start of the period being invoiced, in milliseconds. Prorated lines are charged for period_start → period_end against one price interval starting at period_start."""
    period_end: NotRequired[int]
    r"""End of the period being invoiced, in milliseconds."""
    preview: NotRequired[bool]
    r"""If true, returns the calculated lines and totals without creating an invoice."""


class CreateInvoiceParams(BaseModel):
    customer_id: str
    r"""The customer to invoice."""

    plans: Optional[List[CreateInvoicePlan]] = None

    custom_line_items: Optional[List[CreateInvoiceCustomLineItem]] = None
    r"""Charges that are not tied to any plan or feature."""

    discounts: Optional[List[CreateInvoiceAttachDiscount]] = None
    r"""Discounts applied to the whole invoice."""

    invoice_template_id: Optional[str] = None
    r"""ID of an invoice template whose footer, memo and default payment terms are applied."""

    net_terms_days: Optional[int] = None
    r"""Days until the invoice is due. Defaults to the template's terms, then the org default."""

    issue_date: Optional[int] = None
    r"""Date of issue printed on the invoice, in milliseconds. Defaults to now; cannot be in the future."""

    due_date: Optional[int] = None
    r"""When payment is due, in milliseconds. Must be in the future; takes precedence over net_terms_days."""

    tax_rate_id: Optional[str] = None
    r"""Stripe tax rate ID (txr_...) applied to every line."""

    period_start: Optional[int] = None
    r"""Start of the period being invoiced, in milliseconds. Prorated lines are charged for period_start → period_end against one price interval starting at period_start."""

    period_end: Optional[int] = None
    r"""End of the period being invoiced, in milliseconds."""

    preview: Optional[bool] = None
    r"""If true, returns the calculated lines and totals without creating an invoice."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "plans",
                "custom_line_items",
                "discounts",
                "invoice_template_id",
                "net_terms_days",
                "issue_date",
                "due_date",
                "tax_rate_id",
                "period_start",
                "period_end",
                "preview",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


CreateInvoiceProcessorType = Union[
    Literal[
        "stripe",
        "revenuecat",
    ],
    UnrecognizedStr,
]
r"""The billing processor that owns this invoice."""


class CreateInvoiceEntityTypedDict(TypedDict):
    entity_id: str
    r"""The entity this share of the line item is attributed to"""
    quantity: Nullable[float]
    r"""Quantity charged to this entity. Null on fixed-price lines."""
    amount: float
    r"""Amount attributed to this entity, pre-discount and pre-tax"""


class CreateInvoiceEntity(BaseModel):
    entity_id: str
    r"""The entity this share of the line item is attributed to"""

    quantity: Nullable[float]
    r"""Quantity charged to this entity. Null on fixed-price lines."""

    amount: float
    r"""Amount attributed to this entity, pre-discount and pre-tax"""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                m[k] = val

        return m


class CreateInvoiceInvoiceItemTypedDict(TypedDict):
    id: str
    r"""The Autumn invoice line item ID. Stable across reads, and can be used to reference this line in later calls."""
    description: str
    r"""Description of the invoice line item"""
    period_start: Nullable[float]
    r"""Timestamp when the billing period starts"""
    period_end: Nullable[float]
    r"""Timestamp when the billing period ends"""
    plan_id: Nullable[str]
    r"""The plan this line item came from. Null for lines with no Autumn plan behind them."""
    feature_id: Nullable[str]
    r"""The ID of the feature associated with this line item"""
    feature_name: Nullable[str]
    r"""The name of the feature associated with this line item"""
    quantity: Nullable[float]
    r"""Quantity actually charged on this line. Null on fixed-price lines."""
    amount: float
    r"""Amount charged on this line, pre-discount and pre-tax. Negative for credits."""
    entities: List[CreateInvoiceEntityTypedDict]
    r"""How this line splits by entity. Empty for customer-level lines. Only populated for invoices finalized after entity attribution shipped."""


class CreateInvoiceInvoiceItem(BaseModel):
    id: str
    r"""The Autumn invoice line item ID. Stable across reads, and can be used to reference this line in later calls."""

    description: str
    r"""Description of the invoice line item"""

    period_start: Nullable[float]
    r"""Timestamp when the billing period starts"""

    period_end: Nullable[float]
    r"""Timestamp when the billing period ends"""

    plan_id: Nullable[str]
    r"""The plan this line item came from. Null for lines with no Autumn plan behind them."""

    feature_id: Nullable[str]
    r"""The ID of the feature associated with this line item"""

    feature_name: Nullable[str]
    r"""The name of the feature associated with this line item"""

    quantity: Nullable[float]
    r"""Quantity actually charged on this line. Null on fixed-price lines."""

    amount: float
    r"""Amount charged on this line, pre-discount and pre-tax. Negative for credits."""

    entities: List[CreateInvoiceEntity]
    r"""How this line splits by entity. Empty for customer-level lines. Only populated for invoices finalized after entity attribution shipped."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                m[k] = val

        return m


class CreateInvoiceInvoiceTypedDict(TypedDict):
    plan_ids: List[str]
    r"""Array of plan IDs included in this invoice"""
    stripe_id: str
    r"""The Stripe invoice ID"""
    status: str
    r"""The status of the invoice"""
    total: float
    r"""The total amount of the invoice"""
    currency: str
    r"""The currency code for the invoice"""
    created_at: float
    r"""Timestamp when the invoice was created"""
    id: str
    r"""The Autumn invoice ID"""
    customer_id: Nullable[str]
    r"""The ID of the customer this invoice belongs to. Null for customers created without an ID."""
    entity_id: Nullable[str]
    r"""The ID of the entity this invoice belongs to, if entity-scoped"""
    amount_paid: Nullable[float]
    r"""The amount paid on the invoice. Null on invoices recorded before amounts paid were tracked."""
    refunded_amount: float
    r"""The total amount refunded on the invoice"""
    processor_type: NotRequired[CreateInvoiceProcessorType]
    r"""The billing processor that owns this invoice."""
    hosted_invoice_url: NotRequired[Nullable[str]]
    r"""URL to the Stripe-hosted invoice page"""
    items: NotRequired[List[CreateInvoiceInvoiceItemTypedDict]]
    r"""Line items on the invoice, one per line as shown in Stripe. Capped at 100. Empty for invoices recorded before line item storage."""


class CreateInvoiceInvoice(BaseModel):
    plan_ids: List[str]
    r"""Array of plan IDs included in this invoice"""

    stripe_id: str
    r"""The Stripe invoice ID"""

    status: str
    r"""The status of the invoice"""

    total: float
    r"""The total amount of the invoice"""

    currency: str
    r"""The currency code for the invoice"""

    created_at: float
    r"""Timestamp when the invoice was created"""

    id: str
    r"""The Autumn invoice ID"""

    customer_id: Nullable[str]
    r"""The ID of the customer this invoice belongs to. Null for customers created without an ID."""

    entity_id: Nullable[str]
    r"""The ID of the entity this invoice belongs to, if entity-scoped"""

    amount_paid: Nullable[float]
    r"""The amount paid on the invoice. Null on invoices recorded before amounts paid were tracked."""

    refunded_amount: float
    r"""The total amount refunded on the invoice"""

    processor_type: Optional[CreateInvoiceProcessorType] = "stripe"
    r"""The billing processor that owns this invoice."""

    hosted_invoice_url: OptionalNullable[str] = UNSET
    r"""URL to the Stripe-hosted invoice page"""

    items: Optional[List[CreateInvoiceInvoiceItem]] = None
    r"""Line items on the invoice, one per line as shown in Stripe. Capped at 100. Empty for invoices recorded before line item storage."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["processor_type", "hosted_invoice_url", "items"])
        nullable_fields = set(
            ["hosted_invoice_url", "customer_id", "entity_id", "amount_paid"]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


class CreateInvoiceLineTypedDict(TypedDict):
    plan_id: Nullable[str]
    feature_id: Nullable[str]
    description: str
    amount: float
    amount_after_discounts: float
    quantity: Nullable[float]
    prorated: bool
    period_start: Nullable[float]
    period_end: Nullable[float]


class CreateInvoiceLine(BaseModel):
    plan_id: Nullable[str]

    feature_id: Nullable[str]

    description: str

    amount: float

    amount_after_discounts: float

    quantity: Nullable[float]

    prorated: bool

    period_start: Nullable[float]

    period_end: Nullable[float]

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                m[k] = val

        return m


CreateInvoiceStatus = Union[
    Literal[
        "complete",
        "incomplete",
    ],
    UnrecognizedStr,
]


class CreateInvoiceTaxTypedDict(TypedDict):
    total: float
    amount_inclusive: float
    amount_exclusive: float
    status: CreateInvoiceStatus


class CreateInvoiceTax(BaseModel):
    total: float

    amount_inclusive: float

    amount_exclusive: float

    status: CreateInvoiceStatus


class CreateInvoiceInvoiceCreditsTypedDict(TypedDict):
    r"""The customer's Stripe credit balance and how much of it this invoice consumes."""

    balance: float
    r"""Stripe customer credit balance available, expressed as a positive number in major currency units."""
    currency: str
    r"""Three-letter currency code."""
    applied: NotRequired[float]
    r"""How much of that balance this invoice consumes, capped at its total. The rest stays on the customer."""


class CreateInvoiceInvoiceCredits(BaseModel):
    r"""The customer's Stripe credit balance and how much of it this invoice consumes."""

    balance: float
    r"""Stripe customer credit balance available, expressed as a positive number in major currency units."""

    currency: str
    r"""Three-letter currency code."""

    applied: Optional[float] = None
    r"""How much of that balance this invoice consumes, capped at its total. The rest stays on the customer."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["applied"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class CreateInvoicePreviewTypedDict(TypedDict):
    currency: str
    lines: List[CreateInvoiceLineTypedDict]
    subtotal: float
    discount_total: float
    tax: Nullable[CreateInvoiceTaxTypedDict]
    total: float
    amount_due: float
    r"""What the customer pays: the total less any credit applied."""
    issue_date: float
    due_date: Nullable[float]
    invoice_credits: NotRequired[CreateInvoiceInvoiceCreditsTypedDict]
    r"""The customer's Stripe credit balance and how much of it this invoice consumes."""


class CreateInvoicePreview(BaseModel):
    currency: str

    lines: List[CreateInvoiceLine]

    subtotal: float

    discount_total: float

    tax: Nullable[CreateInvoiceTax]

    total: float

    amount_due: float
    r"""What the customer pays: the total less any credit applied."""

    issue_date: float

    due_date: Nullable[float]

    invoice_credits: Optional[CreateInvoiceInvoiceCredits] = None
    r"""The customer's Stripe credit balance and how much of it this invoice consumes."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["invoice_credits"])
        nullable_fields = set(["tax", "due_date"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


class CreateInvoiceResponseTypedDict(TypedDict):
    r"""OK"""

    invoice: Nullable[CreateInvoiceInvoiceTypedDict]
    r"""The created invoice. Null when preview is true."""
    preview: CreateInvoicePreviewTypedDict


class CreateInvoiceResponse(BaseModel):
    r"""OK"""

    invoice: Nullable[CreateInvoiceInvoice]
    r"""The created invoice. Null when preview is true."""

    preview: CreateInvoicePreview

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                m[k] = val

        return m


try:
    CreateInvoiceDimensions3.model_rebuild()
except NameError:
    pass
try:
    CreateInvoiceDimensions1.model_rebuild()
except NameError:
    pass
try:
    CreateInvoiceCreditSchema1.model_rebuild()
except NameError:
    pass
