/** Matches DialogContent's gap-3 so fields, sections and the footer share one rhythm. */
export const FIELD_STACK = "flex flex-col gap-3";

export const FIELD_GRID = "grid grid-cols-2 gap-x-2 gap-y-3";

/** Label-to-control spacing used by the shared TextField. */
export const LABELED_FIELD = "*:not-first:mt-2";
