// Generated by @autumn/atmn-generator from packages/openapi/openapi-internal.yml.
// Do not edit — run `bun generate` in packages/atmn-generator instead.

export type Coupon = {
	id: string;
	name: string;
	/** Use a positive integer length for months, and null for one_off or forever. */
	duration: {
		type: "one_off" | "months" | "forever";
		length: number | null;
	};
	/** Plan IDs must be unique. */
	planIds: Array<string> | null;
	/** Promo code values must be unique. */
	promoCodes: Array<{
		code: string;
		globalMaxRedemption?: number | null;
		firstTimeTransaction?: boolean | null;
	}>;
	/** Address an existing reward by its stable id. Omit when creating — the server generates one. */
	internalId?: string;
	type: "percentage_discount" | "fixed_discount";
	/** Percentage discounts must be at most 100; fixed discounts must be positive. */
	value: number;
};

export type FeatureGrant = {
	id: string;
	name: string;
	/** Feature IDs must be unique. */
	grants: Array<{
		featureId: string;
		/** A non-negative amount to grant, or null for boolean features. */
		included: number | null;
		expiry: {
			/** The unit of time the grant lasts. */
			type: "day" | "week" | "month" | "year";
			/** The positive integer count of periods before the grant expires. */
			length: number;
		} | null;
	}>;
	/** Promo code values must be unique. */
	promoCodes: Array<{
		code: string;
		/** A positive redemption limit, or null for unlimited uses. */
		maxUses: number | null;
	}>;
	/** Address an existing reward by its stable id. Omit when creating — the server generates one. */
	internalId?: string;
};

export type Reward = { coupon: Coupon } | { featureGrant: FeatureGrant };

export const coupon = (input: Coupon): Reward => ({ coupon: input });

export const featureGrant = (input: FeatureGrant): Reward => ({
	featureGrant: input,
});
