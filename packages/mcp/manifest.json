{
  "author": {
    "name": "Autumn"
  },
  "description": "Ask Autumn to look up customers/plans and safely preview and confirm billing changes.",
  "displayName": "Autumn MCP",
  "display_name": "@autumn/mcp",
  "keywords": [
    "@autumn/mcp"
  ],
  "long_description": "",
  "manifest_version": "0.3",
  "name": "@autumn/mcp",
  "prompts": [],
  "server": {
    "type": "node",
    "entry_point": "./bin/mcp-server.js",
    "mcp_config": {
      "command": "node",
      "args": [
        "${__dirname}/bin/mcp-server.js",
        "start",
        "--secret-key",
        "${user_config.secret_key}",
        "--x-api-version",
        "${user_config.x_api_version}",
        "--fail-open",
        "${user_config.fail_open}"
      ]
    }
  },
  "user_config": {
    "secret_key": {
      "type": "string",
      "title": "secretKey",
      "description": "The secretKey to use for the request",
      "required": true,
      "sensitive": true
    },
    "x_api_version": {
      "type": "string",
      "title": "xApiVersion",
      "description": "The xApiVersion to use for the request",
      "required": false,
      "default": "2.3.0"
    },
    "fail_open": {
      "type": "boolean",
      "title": "failOpen",
      "description": "The failOpen to use for the request",
      "required": false,
      "default": true
    }
  },
  "version": "0.0.1",
  "tools": [
    {
      "name": "ask_autumn",
      "description": "Ask Autumn to look up customers/plans or safely preview and confirm billing changes."
    }
  ]
}