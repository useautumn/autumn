/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { ClosedEnum } from "../types/enums.js";
import { Plan, Plan$zodSchema } from "./plan.js";

export type PreviewAttachGlobals = { xApiVersion?: string | undefined };

export const PreviewAttachGlobals$zodSchema: z.ZodType<PreviewAttachGlobals> = z
  .object({
    xApiVersion: z.string().default("2.3.0"),
  });

/**
 * Quantity configuration for a prepaid feature.
 */
export type PreviewAttachFeatureQuantityRequest = {
  feature_id: string;
  quantity?: number | undefined;
  adjustable?: boolean | undefined;
};

export const PreviewAttachFeatureQuantityRequest$zodSchema: z.ZodType<
  PreviewAttachFeatureQuantityRequest
> = z.object({
  adjustable: z.boolean().optional().describe(
    "Whether the customer can adjust the quantity.",
  ),
  feature_id: z.string().describe("The ID of the feature to set quantity for."),
  quantity: z.number().optional().describe("The quantity of the feature."),
}).describe("Quantity configuration for a prepaid feature.");

/**
 * Billing interval (e.g. 'month', 'year').
 */
export const PreviewAttachPriceInterval = {
  OneOff: "one_off",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
/**
 * Billing interval (e.g. 'month', 'year').
 */
export type PreviewAttachPriceInterval = ClosedEnum<
  typeof PreviewAttachPriceInterval
>;

export const PreviewAttachPriceInterval$zodSchema = z.enum([
  "one_off",
  "week",
  "month",
  "quarter",
  "semi_annual",
  "year",
]).describe("Billing interval (e.g. 'month', 'year').");

/**
 * Base price configuration for a plan.
 */
export type PreviewAttachBasePrice = {
  amount: number;
  interval: PreviewAttachPriceInterval;
  interval_count?: number | undefined;
};

export const PreviewAttachBasePrice$zodSchema: z.ZodType<
  PreviewAttachBasePrice
> = z.object({
  amount: z.number().describe("Base price amount for the plan."),
  interval: PreviewAttachPriceInterval$zodSchema.describe(
    "Billing interval (e.g. 'month', 'year').",
  ),
  interval_count: z.number().optional().describe(
    "Number of intervals per billing cycle. Defaults to 1.",
  ),
}).describe("Base price configuration for a plan.");

/**
 * Interval at which balance resets (e.g. 'month', 'year'). For consumable features only.
 */
export const PreviewAttachItemResetInterval = {
  OneOff: "one_off",
  Minute: "minute",
  Hour: "hour",
  Day: "day",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
/**
 * Interval at which balance resets (e.g. 'month', 'year'). For consumable features only.
 */
export type PreviewAttachItemResetInterval = ClosedEnum<
  typeof PreviewAttachItemResetInterval
>;

export const PreviewAttachItemResetInterval$zodSchema = z.enum([
  "one_off",
  "minute",
  "hour",
  "day",
  "week",
  "month",
  "quarter",
  "semi_annual",
  "year",
]).describe(
  "Interval at which balance resets (e.g. 'month', 'year'). For consumable features only.",
);

/**
 * Reset configuration for consumable features. Omit for non-consumable features like seats.
 */
export type PreviewAttachItemReset = {
  interval: PreviewAttachItemResetInterval;
  interval_count?: number | undefined;
};

export const PreviewAttachItemReset$zodSchema: z.ZodType<
  PreviewAttachItemReset
> = z.object({
  interval: PreviewAttachItemResetInterval$zodSchema.describe(
    "Interval at which balance resets (e.g. 'month', 'year'). For consumable features only.",
  ),
  interval_count: z.number().optional().describe(
    "Number of intervals between resets. Defaults to 1.",
  ),
}).describe(
  "Reset configuration for consumable features. Omit for non-consumable features like seats.",
);

export type PreviewAttachItemTo = number | string;

export const PreviewAttachItemTo$zodSchema: z.ZodType<PreviewAttachItemTo> = z
  .union([
    z.number(),
    z.string(),
  ]);

export type PreviewAttachItemTier = {
  to: number | string;
  amount?: number | undefined;
  flat_amount?: number | undefined;
};

export const PreviewAttachItemTier$zodSchema: z.ZodType<PreviewAttachItemTier> =
  z.object({
    amount: z.number().optional(),
    flat_amount: z.number().optional(),
    to: z.union([
      z.number(),
      z.string(),
    ]),
  });

export const PreviewAttachItemTierBehavior = {
  Graduated: "graduated",
  Volume: "volume",
} as const;
export type PreviewAttachItemTierBehavior = ClosedEnum<
  typeof PreviewAttachItemTierBehavior
>;

export const PreviewAttachItemTierBehavior$zodSchema = z.enum([
  "graduated",
  "volume",
]);

/**
 * Billing interval. For consumable features, should match reset.interval.
 */
export const PreviewAttachItemPriceInterval = {
  OneOff: "one_off",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
/**
 * Billing interval. For consumable features, should match reset.interval.
 */
export type PreviewAttachItemPriceInterval = ClosedEnum<
  typeof PreviewAttachItemPriceInterval
>;

export const PreviewAttachItemPriceInterval$zodSchema = z.enum([
  "one_off",
  "week",
  "month",
  "quarter",
  "semi_annual",
  "year",
]).describe(
  "Billing interval. For consumable features, should match reset.interval.",
);

/**
 * 'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go.
 */
export const PreviewAttachItemBillingMethod = {
  Prepaid: "prepaid",
  UsageBased: "usage_based",
} as const;
/**
 * 'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go.
 */
export type PreviewAttachItemBillingMethod = ClosedEnum<
  typeof PreviewAttachItemBillingMethod
>;

export const PreviewAttachItemBillingMethod$zodSchema = z.enum([
  "prepaid",
  "usage_based",
]).describe(
  "'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go.",
);

/**
 * Pricing for usage beyond included units. Omit for free features.
 */
export type PreviewAttachItemPrice = {
  amount?: number | undefined;
  tiers?: Array<PreviewAttachItemTier> | undefined;
  tier_behavior?: PreviewAttachItemTierBehavior | undefined;
  interval: PreviewAttachItemPriceInterval;
  interval_count?: number | undefined;
  billing_units?: number | undefined;
  billing_method: PreviewAttachItemBillingMethod;
  max_purchase?: number | undefined;
};

export const PreviewAttachItemPrice$zodSchema: z.ZodType<
  PreviewAttachItemPrice
> = z.object({
  amount: z.number().optional().describe(
    "Price per billing_units after included usage. Either 'amount' or 'tiers' is required.",
  ),
  billing_method: PreviewAttachItemBillingMethod$zodSchema.describe(
    "'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go.",
  ),
  billing_units: z.number().default(1).describe(
    "Units per price increment. Usage is rounded UP when billed (e.g. billing_units=100 means 101 rounds to 200).",
  ),
  interval: PreviewAttachItemPriceInterval$zodSchema.describe(
    "Billing interval. For consumable features, should match reset.interval.",
  ),
  interval_count: z.number().default(1).describe(
    "Number of intervals per billing cycle. Defaults to 1.",
  ),
  max_purchase: z.number().optional().describe(
    "Max units purchasable beyond included. E.g. included=100, max_purchase=300 allows 400 total.",
  ),
  tier_behavior: PreviewAttachItemTierBehavior$zodSchema.optional(),
  tiers: z.array(z.lazy(() => PreviewAttachItemTier$zodSchema)).optional()
    .describe("Tiered pricing.  Either 'amount' or 'tiers' is required."),
}).describe("Pricing for usage beyond included units. Omit for free features.");

/**
 * Billing behavior when quantity increases mid-cycle.
 */
export const PreviewAttachItemOnIncrease = {
  BillImmediately: "bill_immediately",
  ProrateImmediately: "prorate_immediately",
  ProrateNextCycle: "prorate_next_cycle",
  BillNextCycle: "bill_next_cycle",
} as const;
/**
 * Billing behavior when quantity increases mid-cycle.
 */
export type PreviewAttachItemOnIncrease = ClosedEnum<
  typeof PreviewAttachItemOnIncrease
>;

export const PreviewAttachItemOnIncrease$zodSchema = z.enum([
  "bill_immediately",
  "prorate_immediately",
  "prorate_next_cycle",
  "bill_next_cycle",
]).describe("Billing behavior when quantity increases mid-cycle.");

/**
 * Credit behavior when quantity decreases mid-cycle.
 */
export const PreviewAttachItemOnDecrease = {
  Prorate: "prorate",
  ProrateImmediately: "prorate_immediately",
  ProrateNextCycle: "prorate_next_cycle",
  None: "none",
  NoProrations: "no_prorations",
} as const;
/**
 * Credit behavior when quantity decreases mid-cycle.
 */
export type PreviewAttachItemOnDecrease = ClosedEnum<
  typeof PreviewAttachItemOnDecrease
>;

export const PreviewAttachItemOnDecrease$zodSchema = z.enum([
  "prorate",
  "prorate_immediately",
  "prorate_next_cycle",
  "none",
  "no_prorations",
]).describe("Credit behavior when quantity decreases mid-cycle.");

/**
 * Proration settings for prepaid features. Controls mid-cycle quantity change billing.
 */
export type PreviewAttachItemProration = {
  on_increase: PreviewAttachItemOnIncrease;
  on_decrease: PreviewAttachItemOnDecrease;
};

export const PreviewAttachItemProration$zodSchema: z.ZodType<
  PreviewAttachItemProration
> = z.object({
  on_decrease: PreviewAttachItemOnDecrease$zodSchema.describe(
    "Credit behavior when quantity decreases mid-cycle.",
  ),
  on_increase: PreviewAttachItemOnIncrease$zodSchema.describe(
    "Billing behavior when quantity increases mid-cycle.",
  ),
}).describe(
  "Proration settings for prepaid features. Controls mid-cycle quantity change billing.",
);

/**
 * When rolled over units expire.
 */
export const PreviewAttachItemExpiryDurationType = {
  Month: "month",
  Forever: "forever",
} as const;
/**
 * When rolled over units expire.
 */
export type PreviewAttachItemExpiryDurationType = ClosedEnum<
  typeof PreviewAttachItemExpiryDurationType
>;

export const PreviewAttachItemExpiryDurationType$zodSchema = z.enum([
  "month",
  "forever",
]).describe("When rolled over units expire.");

/**
 * Rollover config for unused units. If set, unused included units carry over.
 */
export type PreviewAttachItemRollover = {
  max?: number | undefined;
  max_percentage?: number | undefined;
  expiry_duration_type: PreviewAttachItemExpiryDurationType;
  expiry_duration_length?: number | undefined;
};

export const PreviewAttachItemRollover$zodSchema: z.ZodType<
  PreviewAttachItemRollover
> = z.object({
  expiry_duration_length: z.number().optional().describe(
    "Number of periods before expiry.",
  ),
  expiry_duration_type: PreviewAttachItemExpiryDurationType$zodSchema.describe(
    "When rolled over units expire.",
  ),
  max: z.number().optional().describe(
    "Max rollover units. Omit for unlimited rollover.",
  ),
  max_percentage: z.number().optional().describe(
    "Maximum rollover as a percentage (0-100) of included + prepaid grant. Mutually exclusive with max.",
  ),
}).describe(
  "Rollover config for unused units. If set, unused included units carry over.",
);

/**
 * Configuration for a feature item in a plan, including usage limits, pricing, and rollover settings.
 */
export type PreviewAttachItemPlanItem = {
  feature_id: string;
  included?: number | undefined;
  unlimited?: boolean | undefined;
  reset?: PreviewAttachItemReset | undefined;
  price?: PreviewAttachItemPrice | undefined;
  proration?: PreviewAttachItemProration | undefined;
  rollover?: PreviewAttachItemRollover | undefined;
};

export const PreviewAttachItemPlanItem$zodSchema: z.ZodType<
  PreviewAttachItemPlanItem
> = z.object({
  feature_id: z.string().describe("The ID of the feature to configure."),
  included: z.number().optional().describe(
    "Number of free units included. Balance resets to this each interval for consumable features.",
  ),
  price: z.lazy(() => PreviewAttachItemPrice$zodSchema).optional().describe(
    "Pricing for usage beyond included units. Omit for free features.",
  ),
  proration: z.lazy(() => PreviewAttachItemProration$zodSchema).optional()
    .describe(
      "Proration settings for prepaid features. Controls mid-cycle quantity change billing.",
    ),
  reset: z.lazy(() => PreviewAttachItemReset$zodSchema).optional().describe(
    "Reset configuration for consumable features. Omit for non-consumable features like seats.",
  ),
  rollover: z.lazy(() => PreviewAttachItemRollover$zodSchema).optional()
    .describe(
      "Rollover config for unused units. If set, unused included units carry over.",
    ),
  unlimited: z.boolean().optional().describe(
    "If true, customer has unlimited access to this feature.",
  ),
}).describe(
  "Configuration for a feature item in a plan, including usage limits, pricing, and rollover settings.",
);

/**
 * Interval at which balance resets (e.g. 'month', 'year'). For consumable features only.
 */
export const PreviewAttachAddItemResetInterval = {
  OneOff: "one_off",
  Minute: "minute",
  Hour: "hour",
  Day: "day",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
/**
 * Interval at which balance resets (e.g. 'month', 'year'). For consumable features only.
 */
export type PreviewAttachAddItemResetInterval = ClosedEnum<
  typeof PreviewAttachAddItemResetInterval
>;

export const PreviewAttachAddItemResetInterval$zodSchema = z.enum([
  "one_off",
  "minute",
  "hour",
  "day",
  "week",
  "month",
  "quarter",
  "semi_annual",
  "year",
]).describe(
  "Interval at which balance resets (e.g. 'month', 'year'). For consumable features only.",
);

/**
 * Reset configuration for consumable features. Omit for non-consumable features like seats.
 */
export type PreviewAttachAddItemReset = {
  interval: PreviewAttachAddItemResetInterval;
  interval_count?: number | undefined;
};

export const PreviewAttachAddItemReset$zodSchema: z.ZodType<
  PreviewAttachAddItemReset
> = z.object({
  interval: PreviewAttachAddItemResetInterval$zodSchema.describe(
    "Interval at which balance resets (e.g. 'month', 'year'). For consumable features only.",
  ),
  interval_count: z.number().optional().describe(
    "Number of intervals between resets. Defaults to 1.",
  ),
}).describe(
  "Reset configuration for consumable features. Omit for non-consumable features like seats.",
);

export type PreviewAttachAddItemTo = number | string;

export const PreviewAttachAddItemTo$zodSchema: z.ZodType<
  PreviewAttachAddItemTo
> = z.union([
  z.number(),
  z.string(),
]);

export type PreviewAttachAddItemTier = {
  to: number | string;
  amount?: number | undefined;
  flat_amount?: number | undefined;
};

export const PreviewAttachAddItemTier$zodSchema: z.ZodType<
  PreviewAttachAddItemTier
> = z.object({
  amount: z.number().optional(),
  flat_amount: z.number().optional(),
  to: z.union([
    z.number(),
    z.string(),
  ]),
});

export const PreviewAttachAddItemTierBehavior = {
  Graduated: "graduated",
  Volume: "volume",
} as const;
export type PreviewAttachAddItemTierBehavior = ClosedEnum<
  typeof PreviewAttachAddItemTierBehavior
>;

export const PreviewAttachAddItemTierBehavior$zodSchema = z.enum([
  "graduated",
  "volume",
]);

/**
 * Billing interval. For consumable features, should match reset.interval.
 */
export const PreviewAttachAddItemPriceInterval = {
  OneOff: "one_off",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
/**
 * Billing interval. For consumable features, should match reset.interval.
 */
export type PreviewAttachAddItemPriceInterval = ClosedEnum<
  typeof PreviewAttachAddItemPriceInterval
>;

export const PreviewAttachAddItemPriceInterval$zodSchema = z.enum([
  "one_off",
  "week",
  "month",
  "quarter",
  "semi_annual",
  "year",
]).describe(
  "Billing interval. For consumable features, should match reset.interval.",
);

/**
 * 'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go.
 */
export const PreviewAttachAddItemBillingMethod = {
  Prepaid: "prepaid",
  UsageBased: "usage_based",
} as const;
/**
 * 'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go.
 */
export type PreviewAttachAddItemBillingMethod = ClosedEnum<
  typeof PreviewAttachAddItemBillingMethod
>;

export const PreviewAttachAddItemBillingMethod$zodSchema = z.enum([
  "prepaid",
  "usage_based",
]).describe(
  "'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go.",
);

/**
 * Pricing for usage beyond included units. Omit for free features.
 */
export type PreviewAttachAddItemPrice = {
  amount?: number | undefined;
  tiers?: Array<PreviewAttachAddItemTier> | undefined;
  tier_behavior?: PreviewAttachAddItemTierBehavior | undefined;
  interval: PreviewAttachAddItemPriceInterval;
  interval_count?: number | undefined;
  billing_units?: number | undefined;
  billing_method: PreviewAttachAddItemBillingMethod;
  max_purchase?: number | undefined;
};

export const PreviewAttachAddItemPrice$zodSchema: z.ZodType<
  PreviewAttachAddItemPrice
> = z.object({
  amount: z.number().optional().describe(
    "Price per billing_units after included usage. Either 'amount' or 'tiers' is required.",
  ),
  billing_method: PreviewAttachAddItemBillingMethod$zodSchema.describe(
    "'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go.",
  ),
  billing_units: z.number().default(1).describe(
    "Units per price increment. Usage is rounded UP when billed (e.g. billing_units=100 means 101 rounds to 200).",
  ),
  interval: PreviewAttachAddItemPriceInterval$zodSchema.describe(
    "Billing interval. For consumable features, should match reset.interval.",
  ),
  interval_count: z.number().default(1).describe(
    "Number of intervals per billing cycle. Defaults to 1.",
  ),
  max_purchase: z.number().optional().describe(
    "Max units purchasable beyond included. E.g. included=100, max_purchase=300 allows 400 total.",
  ),
  tier_behavior: PreviewAttachAddItemTierBehavior$zodSchema.optional(),
  tiers: z.array(z.lazy(() => PreviewAttachAddItemTier$zodSchema)).optional()
    .describe("Tiered pricing.  Either 'amount' or 'tiers' is required."),
}).describe("Pricing for usage beyond included units. Omit for free features.");

/**
 * Billing behavior when quantity increases mid-cycle.
 */
export const PreviewAttachAddItemOnIncrease = {
  BillImmediately: "bill_immediately",
  ProrateImmediately: "prorate_immediately",
  ProrateNextCycle: "prorate_next_cycle",
  BillNextCycle: "bill_next_cycle",
} as const;
/**
 * Billing behavior when quantity increases mid-cycle.
 */
export type PreviewAttachAddItemOnIncrease = ClosedEnum<
  typeof PreviewAttachAddItemOnIncrease
>;

export const PreviewAttachAddItemOnIncrease$zodSchema = z.enum([
  "bill_immediately",
  "prorate_immediately",
  "prorate_next_cycle",
  "bill_next_cycle",
]).describe("Billing behavior when quantity increases mid-cycle.");

/**
 * Credit behavior when quantity decreases mid-cycle.
 */
export const PreviewAttachAddItemOnDecrease = {
  Prorate: "prorate",
  ProrateImmediately: "prorate_immediately",
  ProrateNextCycle: "prorate_next_cycle",
  None: "none",
  NoProrations: "no_prorations",
} as const;
/**
 * Credit behavior when quantity decreases mid-cycle.
 */
export type PreviewAttachAddItemOnDecrease = ClosedEnum<
  typeof PreviewAttachAddItemOnDecrease
>;

export const PreviewAttachAddItemOnDecrease$zodSchema = z.enum([
  "prorate",
  "prorate_immediately",
  "prorate_next_cycle",
  "none",
  "no_prorations",
]).describe("Credit behavior when quantity decreases mid-cycle.");

/**
 * Proration settings for prepaid features. Controls mid-cycle quantity change billing.
 */
export type PreviewAttachAddItemProration = {
  on_increase: PreviewAttachAddItemOnIncrease;
  on_decrease: PreviewAttachAddItemOnDecrease;
};

export const PreviewAttachAddItemProration$zodSchema: z.ZodType<
  PreviewAttachAddItemProration
> = z.object({
  on_decrease: PreviewAttachAddItemOnDecrease$zodSchema.describe(
    "Credit behavior when quantity decreases mid-cycle.",
  ),
  on_increase: PreviewAttachAddItemOnIncrease$zodSchema.describe(
    "Billing behavior when quantity increases mid-cycle.",
  ),
}).describe(
  "Proration settings for prepaid features. Controls mid-cycle quantity change billing.",
);

/**
 * When rolled over units expire.
 */
export const PreviewAttachAddItemExpiryDurationType = {
  Month: "month",
  Forever: "forever",
} as const;
/**
 * When rolled over units expire.
 */
export type PreviewAttachAddItemExpiryDurationType = ClosedEnum<
  typeof PreviewAttachAddItemExpiryDurationType
>;

export const PreviewAttachAddItemExpiryDurationType$zodSchema = z.enum([
  "month",
  "forever",
]).describe("When rolled over units expire.");

/**
 * Rollover config for unused units. If set, unused included units carry over.
 */
export type PreviewAttachAddItemRollover = {
  max?: number | undefined;
  max_percentage?: number | undefined;
  expiry_duration_type: PreviewAttachAddItemExpiryDurationType;
  expiry_duration_length?: number | undefined;
};

export const PreviewAttachAddItemRollover$zodSchema: z.ZodType<
  PreviewAttachAddItemRollover
> = z.object({
  expiry_duration_length: z.number().optional().describe(
    "Number of periods before expiry.",
  ),
  expiry_duration_type: PreviewAttachAddItemExpiryDurationType$zodSchema
    .describe("When rolled over units expire."),
  max: z.number().optional().describe(
    "Max rollover units. Omit for unlimited rollover.",
  ),
  max_percentage: z.number().optional().describe(
    "Maximum rollover as a percentage (0-100) of included + prepaid grant. Mutually exclusive with max.",
  ),
}).describe(
  "Rollover config for unused units. If set, unused included units carry over.",
);

/**
 * Configuration for a feature item in a plan, including usage limits, pricing, and rollover settings.
 */
export type PreviewAttachAddItemPlanItem = {
  feature_id: string;
  included?: number | undefined;
  unlimited?: boolean | undefined;
  reset?: PreviewAttachAddItemReset | undefined;
  price?: PreviewAttachAddItemPrice | undefined;
  proration?: PreviewAttachAddItemProration | undefined;
  rollover?: PreviewAttachAddItemRollover | undefined;
};

export const PreviewAttachAddItemPlanItem$zodSchema: z.ZodType<
  PreviewAttachAddItemPlanItem
> = z.object({
  feature_id: z.string().describe("The ID of the feature to configure."),
  included: z.number().optional().describe(
    "Number of free units included. Balance resets to this each interval for consumable features.",
  ),
  price: z.lazy(() => PreviewAttachAddItemPrice$zodSchema).optional().describe(
    "Pricing for usage beyond included units. Omit for free features.",
  ),
  proration: z.lazy(() => PreviewAttachAddItemProration$zodSchema).optional()
    .describe(
      "Proration settings for prepaid features. Controls mid-cycle quantity change billing.",
    ),
  reset: z.lazy(() => PreviewAttachAddItemReset$zodSchema).optional().describe(
    "Reset configuration for consumable features. Omit for non-consumable features like seats.",
  ),
  rollover: z.lazy(() => PreviewAttachAddItemRollover$zodSchema).optional()
    .describe(
      "Rollover config for unused units. If set, unused included units carry over.",
    ),
  unlimited: z.boolean().optional().describe(
    "If true, customer has unlimited access to this feature.",
  ),
}).describe(
  "Configuration for a feature item in a plan, including usage limits, pricing, and rollover settings.",
);

/**
 * Match items with this billing method (prepaid or usage_based).
 */
export const PreviewAttachRemoveItemBillingMethod = {
  Prepaid: "prepaid",
  UsageBased: "usage_based",
} as const;
/**
 * Match items with this billing method (prepaid or usage_based).
 */
export type PreviewAttachRemoveItemBillingMethod = ClosedEnum<
  typeof PreviewAttachRemoveItemBillingMethod
>;

export const PreviewAttachRemoveItemBillingMethod$zodSchema = z.enum([
  "prepaid",
  "usage_based",
]).describe("Match items with this billing method (prepaid or usage_based).");

/**
 * Match items with this interval.
 */
export const PreviewAttachRemoveItemInterval = {
  OneOff: "one_off",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
/**
 * Match items with this interval.
 */
export type PreviewAttachRemoveItemInterval = ClosedEnum<
  typeof PreviewAttachRemoveItemInterval
>;

export const PreviewAttachRemoveItemInterval$zodSchema = z.enum([
  "one_off",
  "week",
  "month",
  "quarter",
  "semi_annual",
  "year",
]).describe("Match items with this interval.");

/**
 * Filter for matching plan items. All provided fields must match (AND).
 */
export type PreviewAttachPlanItemFilter = {
  feature_id?: string | undefined;
  billing_method?: PreviewAttachRemoveItemBillingMethod | undefined;
  interval?: PreviewAttachRemoveItemInterval | undefined;
};

export const PreviewAttachPlanItemFilter$zodSchema: z.ZodType<
  PreviewAttachPlanItemFilter
> = z.object({
  billing_method: PreviewAttachRemoveItemBillingMethod$zodSchema.optional()
    .describe("Match items with this billing method (prepaid or usage_based)."),
  feature_id: z.string().optional().describe(
    "Match items linked to this feature.",
  ),
  interval: PreviewAttachRemoveItemInterval$zodSchema.optional().describe(
    "Match items with this interval.",
  ),
}).describe(
  "Filter for matching plan items. All provided fields must match (AND).",
);

/**
 * Unit of time for the trial ('day', 'month', 'year').
 */
export const PreviewAttachDurationType = {
  Day: "day",
  Month: "month",
  Year: "year",
} as const;
/**
 * Unit of time for the trial ('day', 'month', 'year').
 */
export type PreviewAttachDurationType = ClosedEnum<
  typeof PreviewAttachDurationType
>;

export const PreviewAttachDurationType$zodSchema = z.enum([
  "day",
  "month",
  "year",
]).describe("Unit of time for the trial ('day', 'month', 'year').");

/**
 * Behavior when the trial ends. 'bill' charges the customer (default). 'revert' expires the trial and restores the customer's previous plan.
 */
export const PreviewAttachOnEnd = {
  Bill: "bill",
  Revert: "revert",
} as const;
/**
 * Behavior when the trial ends. 'bill' charges the customer (default). 'revert' expires the trial and restores the customer's previous plan.
 */
export type PreviewAttachOnEnd = ClosedEnum<typeof PreviewAttachOnEnd>;

export const PreviewAttachOnEnd$zodSchema = z.enum([
  "bill",
  "revert",
]).describe(
  "Behavior when the trial ends. 'bill' charges the customer (default). 'revert' expires the trial and restores the customer's previous plan.",
);

/**
 * Free trial configuration for a plan.
 */
export type PreviewAttachFreeTrialParams = {
  duration_length: number;
  duration_type?: PreviewAttachDurationType | undefined;
  card_required?: boolean | undefined;
  on_end?: PreviewAttachOnEnd | undefined;
};

export const PreviewAttachFreeTrialParams$zodSchema: z.ZodType<
  PreviewAttachFreeTrialParams
> = z.object({
  card_required: z.boolean().default(true).describe(
    "If true, payment method required to start trial. Customer is charged after trial ends.",
  ),
  duration_length: z.number().describe(
    "Number of duration_type periods the trial lasts.",
  ),
  duration_type: PreviewAttachDurationType$zodSchema.default("month").describe(
    "Unit of time for the trial ('day', 'month', 'year').",
  ),
  on_end: PreviewAttachOnEnd$zodSchema.optional().describe(
    "Behavior when the trial ends. 'bill' charges the customer (default). 'revert' expires the trial and restores the customer's previous plan.",
  ),
}).describe("Free trial configuration for a plan.");

/**
 * Customize the plan to attach. Can override the price, items, free trial, or a combination.
 */
export type PreviewAttachCustomize = {
  price?: PreviewAttachBasePrice | null | undefined;
  items?: Array<PreviewAttachItemPlanItem> | undefined;
  add_items?: Array<PreviewAttachAddItemPlanItem> | undefined;
  remove_items?: Array<PreviewAttachPlanItemFilter> | undefined;
  free_trial?: PreviewAttachFreeTrialParams | null | undefined;
};

export const PreviewAttachCustomize$zodSchema: z.ZodType<
  PreviewAttachCustomize
> = z.object({
  add_items: z.array(z.lazy(() => PreviewAttachAddItemPlanItem$zodSchema))
    .optional().describe("Items to add to the plan."),
  free_trial: z.lazy(() => PreviewAttachFreeTrialParams$zodSchema).nullable()
    .optional().describe(
      "Override the plan's default free trial. Pass an object to set a custom trial, or null to remove the trial entirely.",
    ),
  items: z.array(z.lazy(() => PreviewAttachItemPlanItem$zodSchema)).optional()
    .describe(
      "Override the items in the plan (PUT-style — replaces all existing items). Mutually exclusive with add_items / remove_items.",
    ),
  price: z.lazy(() => PreviewAttachBasePrice$zodSchema).nullable().optional()
    .describe(
      "Override the base price of the plan. Pass null to remove the base price.",
    ),
  remove_items: z.array(z.lazy(() => PreviewAttachPlanItemFilter$zodSchema))
    .optional().describe("Filters selecting items to remove from the plan."),
}).describe(
  "Customize the plan to attach. Can override the price, items, free trial, or a combination.",
);

/**
 * Invoice mode creates a draft or open invoice and sends it to the customer, instead of charging their card immediately. This uses Stripe's send_invoice collection method.
 */
export type PreviewAttachInvoiceMode = {
  enabled: boolean;
  enable_plan_immediately?: boolean | undefined;
  finalize?: boolean | undefined;
};

export const PreviewAttachInvoiceMode$zodSchema: z.ZodType<
  PreviewAttachInvoiceMode
> = z.object({
  enable_plan_immediately: z.boolean().default(false).describe(
    "If true, enables the plan immediately even though the invoice is not paid yet.",
  ),
  enabled: z.boolean().describe(
    "When true, creates an invoice and sends it to the customer instead of charging their card immediately. Uses Stripe's send_invoice collection method.",
  ),
  finalize: z.boolean().default(true).describe(
    "If true, finalizes the invoice so it can be sent to the customer. If false, keeps it as a draft for manual review.",
  ),
}).describe(
  "Invoice mode creates a draft or open invoice and sends it to the customer, instead of charging their card immediately. This uses Stripe's send_invoice collection method.",
);

/**
 * How to handle proration when updating an existing subscription. 'prorate_immediately' charges/credits prorated amounts now, 'none' skips creating any charges.
 */
export const PreviewAttachProrationBehavior = {
  ProrateImmediately: "prorate_immediately",
  None: "none",
} as const;
/**
 * How to handle proration when updating an existing subscription. 'prorate_immediately' charges/credits prorated amounts now, 'none' skips creating any charges.
 */
export type PreviewAttachProrationBehavior = ClosedEnum<
  typeof PreviewAttachProrationBehavior
>;

export const PreviewAttachProrationBehavior$zodSchema = z.enum([
  "prorate_immediately",
  "none",
]).describe(
  "How to handle proration when updating an existing subscription. 'prorate_immediately' charges/credits prorated amounts now, 'none' skips creating any charges.",
);

/**
 * Controls when to return a checkout URL. 'always' returns a URL even if payment succeeds, 'if_required' only when payment action is needed, 'never' disables redirects.
 */
export const PreviewAttachRedirectMode = {
  Always: "always",
  IfRequired: "if_required",
  Never: "never",
} as const;
/**
 * Controls when to return a checkout URL. 'always' returns a URL even if payment succeeds, 'if_required' only when payment action is needed, 'never' disables redirects.
 */
export type PreviewAttachRedirectMode = ClosedEnum<
  typeof PreviewAttachRedirectMode
>;

export const PreviewAttachRedirectMode$zodSchema = z.enum([
  "always",
  "if_required",
  "never",
]).describe(
  "Controls when to return a checkout URL. 'always' returns a URL even if payment succeeds, 'if_required' only when payment action is needed, 'never' disables redirects.",
);

/**
 * A discount to apply. Can be either a reward ID or a promotion code.
 */
export type PreviewAttachAttachDiscount = {
  reward_id?: string | undefined;
  promotion_code?: string | undefined;
};

export const PreviewAttachAttachDiscount$zodSchema: z.ZodType<
  PreviewAttachAttachDiscount
> = z.object({
  promotion_code: z.string().optional().describe(
    "The promotion code to apply as a discount.",
  ),
  reward_id: z.string().optional().describe(
    "The ID of the reward to apply as a discount.",
  ),
}).describe(
  "A discount to apply. Can be either a reward ID or a promotion code.",
);

/**
 * When the plan change should take effect. 'immediate' applies now, 'end_of_cycle' schedules for the end of the current billing cycle. By default, upgrades are immediate and downgrades are scheduled.
 */
export const PreviewAttachPlanSchedule = {
  Immediate: "immediate",
  EndOfCycle: "end_of_cycle",
} as const;
/**
 * When the plan change should take effect. 'immediate' applies now, 'end_of_cycle' schedules for the end of the current billing cycle. By default, upgrades are immediate and downgrades are scheduled.
 */
export type PreviewAttachPlanSchedule = ClosedEnum<
  typeof PreviewAttachPlanSchedule
>;

export const PreviewAttachPlanSchedule$zodSchema = z.enum([
  "immediate",
  "end_of_cycle",
]).describe(
  "When the plan change should take effect. 'immediate' applies now, 'end_of_cycle' schedules for the end of the current billing cycle. By default, upgrades are immediate and downgrades are scheduled.",
);

export type PreviewAttachCustomLineItem = {
  amount: number;
  description: string;
};

export const PreviewAttachCustomLineItem$zodSchema: z.ZodType<
  PreviewAttachCustomLineItem
> = z.object({
  amount: z.number().describe(
    "Amount in dollars for this line item (e.g. 10.50). Can be negative for credits.",
  ),
  description: z.string().describe("Description for the line item."),
});

/**
 * Whether to carry over balances from the previous plan.
 */
export type PreviewAttachCarryOverBalances = {
  enabled: boolean;
  feature_ids?: Array<string> | undefined;
};

export const PreviewAttachCarryOverBalances$zodSchema: z.ZodType<
  PreviewAttachCarryOverBalances
> = z.object({
  enabled: z.boolean().describe(
    "Whether to carry over balances from the previous plan.",
  ),
  feature_ids: z.array(z.string()).optional().describe(
    "The IDs of the features to carry over balances from. If left undefined, all features will be carried over.",
  ),
}).describe("Whether to carry over balances from the previous plan.");

/**
 * Whether to carry over usages from the previous plan.
 */
export type PreviewAttachCarryOverUsages = {
  enabled: boolean;
  feature_ids?: Array<string> | undefined;
};

export const PreviewAttachCarryOverUsages$zodSchema: z.ZodType<
  PreviewAttachCarryOverUsages
> = z.object({
  enabled: z.boolean().describe(
    "Whether to carry over usages from the previous plan.",
  ),
  feature_ids: z.array(z.string()).optional().describe(
    "The IDs of the features to carry over usages for. If left undefined, all consumable features will be carried over.",
  ),
}).describe("Whether to carry over usages from the previous plan.");

export type PreviewAttachParams = {
  customer_id: string;
  entity_id?: string | undefined;
  plan_id: string;
  feature_quantities?: Array<PreviewAttachFeatureQuantityRequest> | undefined;
  version?: number | undefined;
  customize?: PreviewAttachCustomize | undefined;
  invoice_mode?: PreviewAttachInvoiceMode | undefined;
  proration_behavior?: PreviewAttachProrationBehavior | undefined;
  redirect_mode?: PreviewAttachRedirectMode | undefined;
  subscription_id?: string | undefined;
  discounts?: Array<PreviewAttachAttachDiscount> | undefined;
  success_url?: string | undefined;
  new_billing_subscription?: boolean | undefined;
  billing_cycle_anchor?: "now" | undefined;
  plan_schedule?: PreviewAttachPlanSchedule | undefined;
  starts_at?: number | undefined;
  ends_at?: number | undefined;
  checkout_session_params?: { [k: string]: any } | undefined;
  custom_line_items?: Array<PreviewAttachCustomLineItem> | undefined;
  processor_subscription_id?: string | undefined;
  carry_over_balances?: PreviewAttachCarryOverBalances | undefined;
  carry_over_usages?: PreviewAttachCarryOverUsages | undefined;
  metadata?: { [k: string]: string } | undefined;
  no_billing_changes?: boolean | undefined;
  enable_plan_immediately?: boolean | undefined;
  tax_rate_id?: string | undefined;
};

export const PreviewAttachParams$zodSchema: z.ZodType<PreviewAttachParams> = z
  .object({
    billing_cycle_anchor: z.literal("now").default("now").optional().describe(
      "Reset the billing cycle anchor immediately with 'now'.",
    ),
    carry_over_balances: z.lazy(() => PreviewAttachCarryOverBalances$zodSchema)
      .optional().describe(
        "Whether to carry over balances from the previous plan.",
      ),
    carry_over_usages: z.lazy(() => PreviewAttachCarryOverUsages$zodSchema)
      .optional().describe(
        "Whether to carry over usages from the previous plan.",
      ),
    checkout_session_params: z.record(z.string(), z.any()).optional().describe(
      "Additional parameters to pass into the creation of the Stripe checkout session.",
    ),
    custom_line_items: z.array(
      z.lazy(() => PreviewAttachCustomLineItem$zodSchema),
    ).optional().describe(
      "Custom line items that override the auto-generated proration invoice. Only valid for immediate plan changes (eg. upgrades or one off plans).",
    ),
    customer_id: z.string().describe(
      "The ID of the customer to attach the plan to.",
    ),
    customize: z.lazy(() => PreviewAttachCustomize$zodSchema).optional()
      .describe(
        "Customize the plan to attach. Can override the price, items, free trial, or a combination.",
      ),
    discounts: z.array(z.lazy(() => PreviewAttachAttachDiscount$zodSchema))
      .optional().describe(
        "List of discounts to apply. Each discount can be an Autumn reward ID, Stripe coupon ID, or Stripe promotion code.",
      ),
    enable_plan_immediately: z.boolean().optional().describe(
      "If true, the customer's plan is activated immediately even when payment is deferred (invoice mode) or pending (Stripe checkout). For Stripe checkout, the customer_product is inserted before the customer completes the hosted form.",
    ),
    ends_at: z.int().optional().describe(
      "Unix timestamp in milliseconds for when the attached plan should end.",
    ),
    entity_id: z.string().optional().describe(
      "The ID of the entity to attach the plan to.",
    ),
    feature_quantities: z.array(
      z.lazy(() => PreviewAttachFeatureQuantityRequest$zodSchema),
    ).optional().describe(
      "If this plan contains prepaid features, use this field to specify the quantity of each prepaid feature. This quantity includes the included amount and billing units defined when setting up the plan.",
    ),
    invoice_mode: z.lazy(() => PreviewAttachInvoiceMode$zodSchema).optional()
      .describe(
        "Invoice mode creates a draft or open invoice and sends it to the customer, instead of charging their card immediately. This uses Stripe's send_invoice collection method.",
      ),
    metadata: z.record(z.string(), z.string()).optional().describe(
      "Key-value metadata to attach to the Stripe subscription, invoice, and checkout session created during this attach flow. Keys prefixed with 'autumn_' are reserved and will be stripped.",
    ),
    new_billing_subscription: z.boolean().optional().describe(
      "Only applicable when the customer has an existing Stripe subscription. If true, creates a new separate subscription instead of merging into the existing one.",
    ),
    no_billing_changes: z.boolean().optional().describe(
      "If true, skips any billing changes for the attach operation.",
    ),
    plan_id: z.string().describe("The ID of the plan."),
    plan_schedule: PreviewAttachPlanSchedule$zodSchema.optional().describe(
      "When the plan change should take effect. 'immediate' applies now, 'end_of_cycle' schedules for the end of the current billing cycle. By default, upgrades are immediate and downgrades are scheduled.",
    ),
    processor_subscription_id: z.string().optional().describe(
      "The processor subscription ID to link. Use this to attach an existing Stripe subscription instead of creating a new one.",
    ),
    proration_behavior: PreviewAttachProrationBehavior$zodSchema.optional()
      .describe(
        "How to handle proration when updating an existing subscription. 'prorate_immediately' charges/credits prorated amounts now, 'none' skips creating any charges.",
      ),
    redirect_mode: PreviewAttachRedirectMode$zodSchema.default("if_required")
      .describe(
        "Controls when to return a checkout URL. 'always' returns a URL even if payment succeeds, 'if_required' only when payment action is needed, 'never' disables redirects.",
      ),
    starts_at: z.int().optional().describe(
      "Unix timestamp in milliseconds for when the attached plan should start. Future dates create a scheduled subscription.",
    ),
    subscription_id: z.string().optional().describe(
      "A unique ID to identify this subscription. Can be used to target specific subscriptions in update operations when a customer has multiple products with the same plan.",
    ),
    success_url: z.string().optional().describe(
      "URL to redirect to after successful checkout.",
    ),
    tax_rate_id: z.string().optional().describe(
      "Stripe tax rate ID (txr_...) to apply as the default tax rate on the created subscription, invoice, or checkout session line items.",
    ),
    version: z.number().optional().describe(
      "The version of the plan to attach.",
    ),
  });

export type PreviewAttachDiscount = {
  amount_off: number;
  percent_off?: number | undefined;
  reward_id?: string | undefined;
  reward_name?: string | undefined;
};

export const PreviewAttachDiscount$zodSchema: z.ZodType<PreviewAttachDiscount> =
  z.object({
    amount_off: z.number(),
    percent_off: z.number().optional(),
    reward_id: z.string().optional(),
    reward_name: z.string().optional(),
  });

/**
 * The period of time that this line item is being charged for.
 */
export type PreviewAttachLineItemPeriod = { start: number; end: number };

export const PreviewAttachLineItemPeriod$zodSchema: z.ZodType<
  PreviewAttachLineItemPeriod
> = z.object({
  end: z.number().describe(
    "The end of the period in milliseconds since the Unix epoch.",
  ),
  start: z.number().describe(
    "The start of the period in milliseconds since the Unix epoch.",
  ),
}).describe("The period of time that this line item is being charged for.");

export type PreviewAttachLineItem = {
  display_name: string;
  description: string;
  subtotal: number;
  total: number;
  discounts?: Array<PreviewAttachDiscount> | undefined;
  plan_id: string;
  feature_id: string | null;
  period?: PreviewAttachLineItemPeriod | undefined;
  quantity: number;
};

export const PreviewAttachLineItem$zodSchema: z.ZodType<PreviewAttachLineItem> =
  z.object({
    description: z.string().describe(
      "A detailed description of the line item.",
    ),
    discounts: z.array(z.lazy(() => PreviewAttachDiscount$zodSchema)).optional()
      .describe("List of discounts applied to this line item."),
    display_name: z.string().describe(
      "The name of the line item to display to the customer if you're building a UI. It will either be the plan name or the feature name.",
    ),
    feature_id: z.string().nullable().describe(
      "The ID of the feature that this line item belongs to.",
    ),
    period: z.lazy(() => PreviewAttachLineItemPeriod$zodSchema).optional()
      .describe("The period of time that this line item is being charged for."),
    plan_id: z.string().describe(
      "The ID of the plan that this line item belongs to.",
    ),
    quantity: z.number().describe("The quantity of the line item."),
    subtotal: z.number().describe(
      "The amount in cents before discounts and tax for this line item.",
    ),
    total: z.number().describe(
      "The final amount in cents after discounts and tax for this line item.",
    ),
  });

export type PreviewAttachNextCycleDiscount = {
  amount_off: number;
  percent_off?: number | undefined;
  reward_id?: string | undefined;
  reward_name?: string | undefined;
};

export const PreviewAttachNextCycleDiscount$zodSchema: z.ZodType<
  PreviewAttachNextCycleDiscount
> = z.object({
  amount_off: z.number(),
  percent_off: z.number().optional(),
  reward_id: z.string().optional(),
  reward_name: z.string().optional(),
});

/**
 * The period of time that this line item is being charged for.
 */
export type PreviewAttachNextCycleLineItemPeriod = {
  start: number;
  end: number;
};

export const PreviewAttachNextCycleLineItemPeriod$zodSchema: z.ZodType<
  PreviewAttachNextCycleLineItemPeriod
> = z.object({
  end: z.number().describe(
    "The end of the period in milliseconds since the Unix epoch.",
  ),
  start: z.number().describe(
    "The start of the period in milliseconds since the Unix epoch.",
  ),
}).describe("The period of time that this line item is being charged for.");

export type PreviewAttachNextCycleLineItem = {
  display_name: string;
  description: string;
  subtotal: number;
  total: number;
  discounts?: Array<PreviewAttachNextCycleDiscount> | undefined;
  plan_id: string;
  feature_id: string | null;
  period?: PreviewAttachNextCycleLineItemPeriod | undefined;
  quantity: number;
};

export const PreviewAttachNextCycleLineItem$zodSchema: z.ZodType<
  PreviewAttachNextCycleLineItem
> = z.object({
  description: z.string().describe("A detailed description of the line item."),
  discounts: z.array(z.lazy(() => PreviewAttachNextCycleDiscount$zodSchema))
    .optional().describe("List of discounts applied to this line item."),
  display_name: z.string().describe(
    "The name of the line item to display to the customer if you're building a UI. It will either be the plan name or the feature name.",
  ),
  feature_id: z.string().nullable().describe(
    "The ID of the feature that this line item belongs to.",
  ),
  period: z.lazy(() => PreviewAttachNextCycleLineItemPeriod$zodSchema)
    .optional().describe(
      "The period of time that this line item is being charged for.",
    ),
  plan_id: z.string().describe(
    "The ID of the plan that this line item belongs to.",
  ),
  quantity: z.number().describe("The quantity of the line item."),
  subtotal: z.number().describe(
    "The amount in cents before discounts and tax for this line item.",
  ),
  total: z.number().describe(
    "The final amount in cents after discounts and tax for this line item.",
  ),
});

/**
 * The period of time that this line item is being charged for.
 */
export type PreviewAttachUsageLineItemPeriod = { start: number; end: number };

export const PreviewAttachUsageLineItemPeriod$zodSchema: z.ZodType<
  PreviewAttachUsageLineItemPeriod
> = z.object({
  end: z.number().describe(
    "The end of the period in milliseconds since the Unix epoch.",
  ),
  start: z.number().describe(
    "The start of the period in milliseconds since the Unix epoch.",
  ),
}).describe("The period of time that this line item is being charged for.");

export type PreviewAttachUsageLineItem = {
  display_name: string;
  plan_id: string;
  feature_id: string | null;
  period?: PreviewAttachUsageLineItemPeriod | undefined;
};

export const PreviewAttachUsageLineItem$zodSchema: z.ZodType<
  PreviewAttachUsageLineItem
> = z.object({
  display_name: z.string().describe(
    "The name of the line item to display to the customer if you're building a UI. It will either be the plan name or the feature name.",
  ),
  feature_id: z.string().nullable().describe(
    "The ID of the feature that this line item belongs to.",
  ),
  period: z.lazy(() => PreviewAttachUsageLineItemPeriod$zodSchema).optional()
    .describe("The period of time that this line item is being charged for."),
  plan_id: z.string().describe(
    "The ID of the plan that this line item belongs to.",
  ),
});

/**
 * Preview of the next billing cycle, if applicable. This shows what the customer will be charged in subsequent cycles.
 */
export type PreviewAttachNextCycle = {
  starts_at: number;
  subtotal: number;
  total: number;
  line_items: Array<PreviewAttachNextCycleLineItem>;
  usage_line_items: Array<PreviewAttachUsageLineItem>;
};

export const PreviewAttachNextCycle$zodSchema: z.ZodType<
  PreviewAttachNextCycle
> = z.object({
  line_items: z.array(z.lazy(() => PreviewAttachNextCycleLineItem$zodSchema))
    .describe("List of line items for the next billing cycle."),
  starts_at: z.number().describe(
    "Unix timestamp (milliseconds) when the next billing cycle starts.",
  ),
  subtotal: z.number().describe(
    "The total amount in cents before discounts and tax for the next cycle.",
  ),
  total: z.number().describe(
    "The final amount in cents after discounts and tax for the next cycle.",
  ),
  usage_line_items: z.array(z.lazy(() => PreviewAttachUsageLineItem$zodSchema))
    .describe("List of line items for usage-based features in the next cycle."),
}).describe(
  "Preview of the next billing cycle, if applicable. This shows what the customer will be charged in subsequent cycles.",
);

export type PreviewAttachIncomingFeatureQuantity = {
  feature_id: string;
  quantity: number;
};

export const PreviewAttachIncomingFeatureQuantity$zodSchema: z.ZodType<
  PreviewAttachIncomingFeatureQuantity
> = z.object({
  feature_id: z.string().describe(
    "The ID of the adjustable feature included in this change.",
  ),
  quantity: z.number().describe(
    "The quantity that will apply for this feature in the change.",
  ),
});

export type PreviewAttachIncoming = {
  plan_id: string;
  plan?: Plan | undefined;
  feature_quantities: Array<PreviewAttachIncomingFeatureQuantity>;
  effective_at: number | null;
  canceled_at: number | null;
  expires_at: number | null;
};

export const PreviewAttachIncoming$zodSchema: z.ZodType<PreviewAttachIncoming> =
  z.object({
    canceled_at: z.number().nullable().describe(
      "When this plan was canceled, in milliseconds since the Unix epoch, or null if it is not canceled.",
    ),
    effective_at: z.number().nullable().describe(
      "When this change takes effect, in milliseconds since the Unix epoch, or null if it applies immediately.",
    ),
    expires_at: z.number().nullable().describe(
      "When this plan expires, in milliseconds since the Unix epoch, or null if it does not expire.",
    ),
    feature_quantities: z.array(
      z.lazy(() => PreviewAttachIncomingFeatureQuantity$zodSchema),
    ).describe(
      "The feature quantity selections associated with this plan change.",
    ),
    plan: Plan$zodSchema.optional(),
    plan_id: z.string().describe(
      "The ID of the plan affected by this preview change.",
    ),
  });

export type PreviewAttachOutgoingFeatureQuantity = {
  feature_id: string;
  quantity: number;
};

export const PreviewAttachOutgoingFeatureQuantity$zodSchema: z.ZodType<
  PreviewAttachOutgoingFeatureQuantity
> = z.object({
  feature_id: z.string().describe(
    "The ID of the adjustable feature included in this change.",
  ),
  quantity: z.number().describe(
    "The quantity that will apply for this feature in the change.",
  ),
});

export type PreviewAttachOutgoing = {
  plan_id: string;
  plan?: Plan | undefined;
  feature_quantities: Array<PreviewAttachOutgoingFeatureQuantity>;
  effective_at: number | null;
  canceled_at: number | null;
  expires_at: number | null;
};

export const PreviewAttachOutgoing$zodSchema: z.ZodType<PreviewAttachOutgoing> =
  z.object({
    canceled_at: z.number().nullable().describe(
      "When this plan was canceled, in milliseconds since the Unix epoch, or null if it is not canceled.",
    ),
    effective_at: z.number().nullable().describe(
      "When this change takes effect, in milliseconds since the Unix epoch, or null if it applies immediately.",
    ),
    expires_at: z.number().nullable().describe(
      "When this plan expires, in milliseconds since the Unix epoch, or null if it does not expire.",
    ),
    feature_quantities: z.array(
      z.lazy(() => PreviewAttachOutgoingFeatureQuantity$zodSchema),
    ).describe(
      "The feature quantity selections associated with this plan change.",
    ),
    plan: Plan$zodSchema.optional(),
    plan_id: z.string().describe(
      "The ID of the plan affected by this preview change.",
    ),
  });

export const CheckoutType = {
  StripeCheckout: "stripe_checkout",
  AutumnCheckout: "autumn_checkout",
} as const;
export type CheckoutType = ClosedEnum<typeof CheckoutType>;

export const CheckoutType$zodSchema = z.enum([
  "stripe_checkout",
  "autumn_checkout",
]);

/**
 * Calculation status ('complete' when Stripe Tax succeeds or 'incomplete' when Stripe Tax returned 0 or errored).
 */
export const PreviewAttachStatus = {
  Complete: "complete",
  Incomplete: "incomplete",
} as const;
/**
 * Calculation status ('complete' when Stripe Tax succeeds or 'incomplete' when Stripe Tax returned 0 or errored).
 */
export type PreviewAttachStatus = ClosedEnum<typeof PreviewAttachStatus>;

export const PreviewAttachStatus$zodSchema = z.enum([
  "complete",
  "incomplete",
]).describe(
  "Calculation status ('complete' when Stripe Tax succeeds or 'incomplete' when Stripe Tax returned 0 or errored).",
);

/**
 * Tax preview for the immediate charge. Contact us to enable the tax flag on your organisation. Shows only with flag enabled, a Stripe customer exists and has a location.
 */
export type PreviewAttachTax = {
  total: number;
  amount_inclusive: number;
  amount_exclusive: number;
  currency: string;
  status: PreviewAttachStatus;
};

export const PreviewAttachTax$zodSchema: z.ZodType<PreviewAttachTax> = z.object(
  {
    amount_exclusive: z.number().describe("Tax added on top of subtotals."),
    amount_inclusive: z.number().describe(
      "Tax included in line item subtotals.",
    ),
    currency: z.string().describe("Three-letter currency code."),
    status: PreviewAttachStatus$zodSchema.describe(
      "Calculation status ('complete' when Stripe Tax succeeds or 'incomplete' when Stripe Tax returned 0 or errored).",
    ),
    total: z.number().describe("Total tax amount in major currency units."),
  },
).describe(
  "Tax preview for the immediate charge. Contact us to enable the tax flag on your organisation. Shows only with flag enabled, a Stripe customer exists and has a location.",
);

/**
 * Stripe customer invoice credits preview.
 */
export type PreviewAttachInvoiceCredits = { balance: number; currency: string };

export const PreviewAttachInvoiceCredits$zodSchema: z.ZodType<
  PreviewAttachInvoiceCredits
> = z.object({
  balance: z.number().describe(
    "Stripe customer credit balance available, expressed as a positive number in major currency units.",
  ),
  currency: z.string().describe("Three-letter currency code."),
}).describe("Stripe customer invoice credits preview.");

/**
 * OK
 */
export type PreviewAttachResponse = {
  customer_id: string;
  line_items: Array<PreviewAttachLineItem>;
  subtotal: number;
  total: number;
  currency: string;
  next_cycle?: PreviewAttachNextCycle | undefined;
  expand?: Array<string> | undefined;
  incoming: Array<PreviewAttachIncoming>;
  outgoing: Array<PreviewAttachOutgoing>;
  redirect_to_checkout: boolean;
  checkout_type: CheckoutType | null;
  tax?: PreviewAttachTax | undefined;
  invoice_credits?: PreviewAttachInvoiceCredits | undefined;
};

export const PreviewAttachResponse$zodSchema: z.ZodType<PreviewAttachResponse> =
  z.object({
    checkout_type: CheckoutType$zodSchema.nullable().describe(
      "The type of checkout that will be used if the customer is redirected to a checkout page.",
    ),
    currency: z.string().describe(
      "The three-letter ISO currency code (e.g., 'usd').",
    ),
    customer_id: z.string().describe("The ID of the customer."),
    expand: z.array(z.string()).optional().describe(
      "Expand the response with additional data.",
    ),
    incoming: z.array(z.lazy(() => PreviewAttachIncoming$zodSchema)).describe(
      "Products or subscription changes being added or updated.",
    ),
    invoice_credits: z.lazy(() => PreviewAttachInvoiceCredits$zodSchema)
      .optional().describe("Stripe customer invoice credits preview."),
    line_items: z.array(z.lazy(() => PreviewAttachLineItem$zodSchema)),
    next_cycle: z.lazy(() => PreviewAttachNextCycle$zodSchema).optional()
      .describe(
        "Preview of the next billing cycle, if applicable. This shows what the customer will be charged in subsequent cycles.",
      ),
    outgoing: z.array(z.lazy(() => PreviewAttachOutgoing$zodSchema)).describe(
      "Products or subscription changes being removed or ended.",
    ),
    redirect_to_checkout: z.boolean().describe(
      "Whether the customer will be redirected to a checkout page if attach is called.",
    ),
    subtotal: z.number().describe(
      "The total amount in cents before discounts and tax for the current billing period.",
    ),
    tax: z.lazy(() => PreviewAttachTax$zodSchema).optional().describe(
      "Tax preview for the immediate charge. Contact us to enable the tax flag on your organisation. Shows only with flag enabled, a Stripe customer exists and has a location.",
    ),
    total: z.number().describe(
      "The final amount in cents after discounts and tax for the current billing period.",
    ),
  }).describe("OK");
