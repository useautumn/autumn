/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { ClosedEnum } from "../types/enums.js";

export type AttachGlobals = { xApiVersion?: string | undefined };

export const AttachGlobals$zodSchema: z.ZodType<AttachGlobals> = z.object({
  xApiVersion: z.string().default("2.3.0"),
});

/**
 * Quantity configuration for a prepaid feature.
 */
export type AttachFeatureQuantity = {
  feature_id: string;
  quantity?: number | undefined;
  adjustable?: boolean | undefined;
};

export const AttachFeatureQuantity$zodSchema: z.ZodType<AttachFeatureQuantity> =
  z.object({
    adjustable: z.boolean().optional().describe(
      "Whether the customer can adjust the quantity.",
    ),
    feature_id: z.string().describe(
      "The ID of the feature to set quantity for.",
    ),
    quantity: z.number().optional().describe("The quantity of the feature."),
  }).describe("Quantity configuration for a prepaid feature.");

/**
 * Billing interval (e.g. 'month', 'year').
 */
export const AttachPriceInterval = {
  OneOff: "one_off",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
/**
 * Billing interval (e.g. 'month', 'year').
 */
export type AttachPriceInterval = ClosedEnum<typeof AttachPriceInterval>;

export const AttachPriceInterval$zodSchema = z.enum([
  "one_off",
  "week",
  "month",
  "quarter",
  "semi_annual",
  "year",
]).describe("Billing interval (e.g. 'month', 'year').");

/**
 * Base price configuration for a plan.
 */
export type AttachBasePrice = {
  amount: number;
  interval: AttachPriceInterval;
  interval_count?: number | undefined;
};

export const AttachBasePrice$zodSchema: z.ZodType<AttachBasePrice> = z.object({
  amount: z.number().describe("Base price amount for the plan."),
  interval: AttachPriceInterval$zodSchema.describe(
    "Billing interval (e.g. 'month', 'year').",
  ),
  interval_count: z.number().optional().describe(
    "Number of intervals per billing cycle. Defaults to 1.",
  ),
}).describe("Base price configuration for a plan.");

/**
 * Interval at which balance resets (e.g. 'month', 'year'). For consumable features only.
 */
export const AttachItemResetInterval = {
  OneOff: "one_off",
  Minute: "minute",
  Hour: "hour",
  Day: "day",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
/**
 * Interval at which balance resets (e.g. 'month', 'year'). For consumable features only.
 */
export type AttachItemResetInterval = ClosedEnum<
  typeof AttachItemResetInterval
>;

export const AttachItemResetInterval$zodSchema = z.enum([
  "one_off",
  "minute",
  "hour",
  "day",
  "week",
  "month",
  "quarter",
  "semi_annual",
  "year",
]).describe(
  "Interval at which balance resets (e.g. 'month', 'year'). For consumable features only.",
);

/**
 * Reset configuration for consumable features. Omit for non-consumable features like seats.
 */
export type AttachItemReset = {
  interval: AttachItemResetInterval;
  interval_count?: number | undefined;
};

export const AttachItemReset$zodSchema: z.ZodType<AttachItemReset> = z.object({
  interval: AttachItemResetInterval$zodSchema.describe(
    "Interval at which balance resets (e.g. 'month', 'year'). For consumable features only.",
  ),
  interval_count: z.number().optional().describe(
    "Number of intervals between resets. Defaults to 1.",
  ),
}).describe(
  "Reset configuration for consumable features. Omit for non-consumable features like seats.",
);

export type AttachItemTo = number | string;

export const AttachItemTo$zodSchema: z.ZodType<AttachItemTo> = z.union([
  z.number(),
  z.string(),
]);

export type AttachItemTier = {
  to: number | string;
  amount?: number | undefined;
  flat_amount?: number | undefined;
};

export const AttachItemTier$zodSchema: z.ZodType<AttachItemTier> = z.object({
  amount: z.number().optional(),
  flat_amount: z.number().optional(),
  to: z.union([
    z.number(),
    z.string(),
  ]),
});

export const AttachItemTierBehavior = {
  Graduated: "graduated",
  Volume: "volume",
} as const;
export type AttachItemTierBehavior = ClosedEnum<typeof AttachItemTierBehavior>;

export const AttachItemTierBehavior$zodSchema = z.enum([
  "graduated",
  "volume",
]);

/**
 * Billing interval. For consumable features, should match reset.interval.
 */
export const AttachItemPriceInterval = {
  OneOff: "one_off",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
/**
 * Billing interval. For consumable features, should match reset.interval.
 */
export type AttachItemPriceInterval = ClosedEnum<
  typeof AttachItemPriceInterval
>;

export const AttachItemPriceInterval$zodSchema = z.enum([
  "one_off",
  "week",
  "month",
  "quarter",
  "semi_annual",
  "year",
]).describe(
  "Billing interval. For consumable features, should match reset.interval.",
);

/**
 * 'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go.
 */
export const AttachItemBillingMethod = {
  Prepaid: "prepaid",
  UsageBased: "usage_based",
} as const;
/**
 * 'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go.
 */
export type AttachItemBillingMethod = ClosedEnum<
  typeof AttachItemBillingMethod
>;

export const AttachItemBillingMethod$zodSchema = z.enum([
  "prepaid",
  "usage_based",
]).describe(
  "'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go.",
);

/**
 * Pricing for usage beyond included units. Omit for free features.
 */
export type AttachItemPrice = {
  amount?: number | undefined;
  tiers?: Array<AttachItemTier> | undefined;
  tier_behavior?: AttachItemTierBehavior | undefined;
  interval: AttachItemPriceInterval;
  interval_count?: number | undefined;
  billing_units?: number | undefined;
  billing_method: AttachItemBillingMethod;
  max_purchase?: number | undefined;
};

export const AttachItemPrice$zodSchema: z.ZodType<AttachItemPrice> = z.object({
  amount: z.number().optional().describe(
    "Price per billing_units after included usage. Either 'amount' or 'tiers' is required.",
  ),
  billing_method: AttachItemBillingMethod$zodSchema.describe(
    "'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go.",
  ),
  billing_units: z.number().default(1).describe(
    "Units per price increment. Usage is rounded UP when billed (e.g. billing_units=100 means 101 rounds to 200).",
  ),
  interval: AttachItemPriceInterval$zodSchema.describe(
    "Billing interval. For consumable features, should match reset.interval.",
  ),
  interval_count: z.number().default(1).describe(
    "Number of intervals per billing cycle. Defaults to 1.",
  ),
  max_purchase: z.number().optional().describe(
    "Max units purchasable beyond included. E.g. included=100, max_purchase=300 allows 400 total.",
  ),
  tier_behavior: AttachItemTierBehavior$zodSchema.optional(),
  tiers: z.array(z.lazy(() => AttachItemTier$zodSchema)).optional().describe(
    "Tiered pricing.  Either 'amount' or 'tiers' is required.",
  ),
}).describe("Pricing for usage beyond included units. Omit for free features.");

/**
 * Billing behavior when quantity increases mid-cycle.
 */
export const AttachItemOnIncrease = {
  BillImmediately: "bill_immediately",
  ProrateImmediately: "prorate_immediately",
  ProrateNextCycle: "prorate_next_cycle",
  BillNextCycle: "bill_next_cycle",
} as const;
/**
 * Billing behavior when quantity increases mid-cycle.
 */
export type AttachItemOnIncrease = ClosedEnum<typeof AttachItemOnIncrease>;

export const AttachItemOnIncrease$zodSchema = z.enum([
  "bill_immediately",
  "prorate_immediately",
  "prorate_next_cycle",
  "bill_next_cycle",
]).describe("Billing behavior when quantity increases mid-cycle.");

/**
 * Credit behavior when quantity decreases mid-cycle.
 */
export const AttachItemOnDecrease = {
  Prorate: "prorate",
  ProrateImmediately: "prorate_immediately",
  ProrateNextCycle: "prorate_next_cycle",
  None: "none",
  NoProrations: "no_prorations",
} as const;
/**
 * Credit behavior when quantity decreases mid-cycle.
 */
export type AttachItemOnDecrease = ClosedEnum<typeof AttachItemOnDecrease>;

export const AttachItemOnDecrease$zodSchema = z.enum([
  "prorate",
  "prorate_immediately",
  "prorate_next_cycle",
  "none",
  "no_prorations",
]).describe("Credit behavior when quantity decreases mid-cycle.");

/**
 * Proration settings for prepaid features. Controls mid-cycle quantity change billing.
 */
export type AttachItemProration = {
  on_increase: AttachItemOnIncrease;
  on_decrease: AttachItemOnDecrease;
};

export const AttachItemProration$zodSchema: z.ZodType<AttachItemProration> = z
  .object({
    on_decrease: AttachItemOnDecrease$zodSchema.describe(
      "Credit behavior when quantity decreases mid-cycle.",
    ),
    on_increase: AttachItemOnIncrease$zodSchema.describe(
      "Billing behavior when quantity increases mid-cycle.",
    ),
  }).describe(
    "Proration settings for prepaid features. Controls mid-cycle quantity change billing.",
  );

/**
 * When rolled over units expire.
 */
export const AttachItemExpiryDurationType = {
  Month: "month",
  Forever: "forever",
} as const;
/**
 * When rolled over units expire.
 */
export type AttachItemExpiryDurationType = ClosedEnum<
  typeof AttachItemExpiryDurationType
>;

export const AttachItemExpiryDurationType$zodSchema = z.enum([
  "month",
  "forever",
]).describe("When rolled over units expire.");

/**
 * Rollover config for unused units. If set, unused included units carry over.
 */
export type AttachItemRollover = {
  max?: number | undefined;
  max_percentage?: number | undefined;
  expiry_duration_type: AttachItemExpiryDurationType;
  expiry_duration_length?: number | undefined;
};

export const AttachItemRollover$zodSchema: z.ZodType<AttachItemRollover> = z
  .object({
    expiry_duration_length: z.number().optional().describe(
      "Number of periods before expiry.",
    ),
    expiry_duration_type: AttachItemExpiryDurationType$zodSchema.describe(
      "When rolled over units expire.",
    ),
    max: z.number().optional().describe(
      "Max rollover units. Omit for unlimited rollover.",
    ),
    max_percentage: z.number().optional().describe(
      "Maximum rollover as a percentage (0-100) of included + prepaid grant. Mutually exclusive with max.",
    ),
  }).describe(
    "Rollover config for unused units. If set, unused included units carry over.",
  );

/**
 * Configuration for a feature item in a plan, including usage limits, pricing, and rollover settings.
 */
export type AttachItemPlanItem = {
  feature_id: string;
  included?: number | undefined;
  unlimited?: boolean | undefined;
  reset?: AttachItemReset | undefined;
  price?: AttachItemPrice | undefined;
  proration?: AttachItemProration | undefined;
  rollover?: AttachItemRollover | undefined;
};

export const AttachItemPlanItem$zodSchema: z.ZodType<AttachItemPlanItem> = z
  .object({
    feature_id: z.string().describe("The ID of the feature to configure."),
    included: z.number().optional().describe(
      "Number of free units included. Balance resets to this each interval for consumable features.",
    ),
    price: z.lazy(() => AttachItemPrice$zodSchema).optional().describe(
      "Pricing for usage beyond included units. Omit for free features.",
    ),
    proration: z.lazy(() => AttachItemProration$zodSchema).optional().describe(
      "Proration settings for prepaid features. Controls mid-cycle quantity change billing.",
    ),
    reset: z.lazy(() => AttachItemReset$zodSchema).optional().describe(
      "Reset configuration for consumable features. Omit for non-consumable features like seats.",
    ),
    rollover: z.lazy(() => AttachItemRollover$zodSchema).optional().describe(
      "Rollover config for unused units. If set, unused included units carry over.",
    ),
    unlimited: z.boolean().optional().describe(
      "If true, customer has unlimited access to this feature.",
    ),
  }).describe(
    "Configuration for a feature item in a plan, including usage limits, pricing, and rollover settings.",
  );

/**
 * Interval at which balance resets (e.g. 'month', 'year'). For consumable features only.
 */
export const AttachAddItemResetInterval = {
  OneOff: "one_off",
  Minute: "minute",
  Hour: "hour",
  Day: "day",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
/**
 * Interval at which balance resets (e.g. 'month', 'year'). For consumable features only.
 */
export type AttachAddItemResetInterval = ClosedEnum<
  typeof AttachAddItemResetInterval
>;

export const AttachAddItemResetInterval$zodSchema = z.enum([
  "one_off",
  "minute",
  "hour",
  "day",
  "week",
  "month",
  "quarter",
  "semi_annual",
  "year",
]).describe(
  "Interval at which balance resets (e.g. 'month', 'year'). For consumable features only.",
);

/**
 * Reset configuration for consumable features. Omit for non-consumable features like seats.
 */
export type AttachAddItemReset = {
  interval: AttachAddItemResetInterval;
  interval_count?: number | undefined;
};

export const AttachAddItemReset$zodSchema: z.ZodType<AttachAddItemReset> = z
  .object({
    interval: AttachAddItemResetInterval$zodSchema.describe(
      "Interval at which balance resets (e.g. 'month', 'year'). For consumable features only.",
    ),
    interval_count: z.number().optional().describe(
      "Number of intervals between resets. Defaults to 1.",
    ),
  }).describe(
    "Reset configuration for consumable features. Omit for non-consumable features like seats.",
  );

export type AttachAddItemTo = number | string;

export const AttachAddItemTo$zodSchema: z.ZodType<AttachAddItemTo> = z.union([
  z.number(),
  z.string(),
]);

export type AttachAddItemTier = {
  to: number | string;
  amount?: number | undefined;
  flat_amount?: number | undefined;
};

export const AttachAddItemTier$zodSchema: z.ZodType<AttachAddItemTier> = z
  .object({
    amount: z.number().optional(),
    flat_amount: z.number().optional(),
    to: z.union([
      z.number(),
      z.string(),
    ]),
  });

export const AttachAddItemTierBehavior = {
  Graduated: "graduated",
  Volume: "volume",
} as const;
export type AttachAddItemTierBehavior = ClosedEnum<
  typeof AttachAddItemTierBehavior
>;

export const AttachAddItemTierBehavior$zodSchema = z.enum([
  "graduated",
  "volume",
]);

/**
 * Billing interval. For consumable features, should match reset.interval.
 */
export const AttachAddItemPriceInterval = {
  OneOff: "one_off",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
/**
 * Billing interval. For consumable features, should match reset.interval.
 */
export type AttachAddItemPriceInterval = ClosedEnum<
  typeof AttachAddItemPriceInterval
>;

export const AttachAddItemPriceInterval$zodSchema = z.enum([
  "one_off",
  "week",
  "month",
  "quarter",
  "semi_annual",
  "year",
]).describe(
  "Billing interval. For consumable features, should match reset.interval.",
);

/**
 * 'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go.
 */
export const AttachAddItemBillingMethod = {
  Prepaid: "prepaid",
  UsageBased: "usage_based",
} as const;
/**
 * 'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go.
 */
export type AttachAddItemBillingMethod = ClosedEnum<
  typeof AttachAddItemBillingMethod
>;

export const AttachAddItemBillingMethod$zodSchema = z.enum([
  "prepaid",
  "usage_based",
]).describe(
  "'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go.",
);

/**
 * Pricing for usage beyond included units. Omit for free features.
 */
export type AttachAddItemPrice = {
  amount?: number | undefined;
  tiers?: Array<AttachAddItemTier> | undefined;
  tier_behavior?: AttachAddItemTierBehavior | undefined;
  interval: AttachAddItemPriceInterval;
  interval_count?: number | undefined;
  billing_units?: number | undefined;
  billing_method: AttachAddItemBillingMethod;
  max_purchase?: number | undefined;
};

export const AttachAddItemPrice$zodSchema: z.ZodType<AttachAddItemPrice> = z
  .object({
    amount: z.number().optional().describe(
      "Price per billing_units after included usage. Either 'amount' or 'tiers' is required.",
    ),
    billing_method: AttachAddItemBillingMethod$zodSchema.describe(
      "'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go.",
    ),
    billing_units: z.number().default(1).describe(
      "Units per price increment. Usage is rounded UP when billed (e.g. billing_units=100 means 101 rounds to 200).",
    ),
    interval: AttachAddItemPriceInterval$zodSchema.describe(
      "Billing interval. For consumable features, should match reset.interval.",
    ),
    interval_count: z.number().default(1).describe(
      "Number of intervals per billing cycle. Defaults to 1.",
    ),
    max_purchase: z.number().optional().describe(
      "Max units purchasable beyond included. E.g. included=100, max_purchase=300 allows 400 total.",
    ),
    tier_behavior: AttachAddItemTierBehavior$zodSchema.optional(),
    tiers: z.array(z.lazy(() => AttachAddItemTier$zodSchema)).optional()
      .describe("Tiered pricing.  Either 'amount' or 'tiers' is required."),
  }).describe(
    "Pricing for usage beyond included units. Omit for free features.",
  );

/**
 * Billing behavior when quantity increases mid-cycle.
 */
export const AttachAddItemOnIncrease = {
  BillImmediately: "bill_immediately",
  ProrateImmediately: "prorate_immediately",
  ProrateNextCycle: "prorate_next_cycle",
  BillNextCycle: "bill_next_cycle",
} as const;
/**
 * Billing behavior when quantity increases mid-cycle.
 */
export type AttachAddItemOnIncrease = ClosedEnum<
  typeof AttachAddItemOnIncrease
>;

export const AttachAddItemOnIncrease$zodSchema = z.enum([
  "bill_immediately",
  "prorate_immediately",
  "prorate_next_cycle",
  "bill_next_cycle",
]).describe("Billing behavior when quantity increases mid-cycle.");

/**
 * Credit behavior when quantity decreases mid-cycle.
 */
export const AttachAddItemOnDecrease = {
  Prorate: "prorate",
  ProrateImmediately: "prorate_immediately",
  ProrateNextCycle: "prorate_next_cycle",
  None: "none",
  NoProrations: "no_prorations",
} as const;
/**
 * Credit behavior when quantity decreases mid-cycle.
 */
export type AttachAddItemOnDecrease = ClosedEnum<
  typeof AttachAddItemOnDecrease
>;

export const AttachAddItemOnDecrease$zodSchema = z.enum([
  "prorate",
  "prorate_immediately",
  "prorate_next_cycle",
  "none",
  "no_prorations",
]).describe("Credit behavior when quantity decreases mid-cycle.");

/**
 * Proration settings for prepaid features. Controls mid-cycle quantity change billing.
 */
export type AttachAddItemProration = {
  on_increase: AttachAddItemOnIncrease;
  on_decrease: AttachAddItemOnDecrease;
};

export const AttachAddItemProration$zodSchema: z.ZodType<
  AttachAddItemProration
> = z.object({
  on_decrease: AttachAddItemOnDecrease$zodSchema.describe(
    "Credit behavior when quantity decreases mid-cycle.",
  ),
  on_increase: AttachAddItemOnIncrease$zodSchema.describe(
    "Billing behavior when quantity increases mid-cycle.",
  ),
}).describe(
  "Proration settings for prepaid features. Controls mid-cycle quantity change billing.",
);

/**
 * When rolled over units expire.
 */
export const AttachAddItemExpiryDurationType = {
  Month: "month",
  Forever: "forever",
} as const;
/**
 * When rolled over units expire.
 */
export type AttachAddItemExpiryDurationType = ClosedEnum<
  typeof AttachAddItemExpiryDurationType
>;

export const AttachAddItemExpiryDurationType$zodSchema = z.enum([
  "month",
  "forever",
]).describe("When rolled over units expire.");

/**
 * Rollover config for unused units. If set, unused included units carry over.
 */
export type AttachAddItemRollover = {
  max?: number | undefined;
  max_percentage?: number | undefined;
  expiry_duration_type: AttachAddItemExpiryDurationType;
  expiry_duration_length?: number | undefined;
};

export const AttachAddItemRollover$zodSchema: z.ZodType<AttachAddItemRollover> =
  z.object({
    expiry_duration_length: z.number().optional().describe(
      "Number of periods before expiry.",
    ),
    expiry_duration_type: AttachAddItemExpiryDurationType$zodSchema.describe(
      "When rolled over units expire.",
    ),
    max: z.number().optional().describe(
      "Max rollover units. Omit for unlimited rollover.",
    ),
    max_percentage: z.number().optional().describe(
      "Maximum rollover as a percentage (0-100) of included + prepaid grant. Mutually exclusive with max.",
    ),
  }).describe(
    "Rollover config for unused units. If set, unused included units carry over.",
  );

/**
 * Configuration for a feature item in a plan, including usage limits, pricing, and rollover settings.
 */
export type AttachAddItemPlanItem = {
  feature_id: string;
  included?: number | undefined;
  unlimited?: boolean | undefined;
  reset?: AttachAddItemReset | undefined;
  price?: AttachAddItemPrice | undefined;
  proration?: AttachAddItemProration | undefined;
  rollover?: AttachAddItemRollover | undefined;
};

export const AttachAddItemPlanItem$zodSchema: z.ZodType<AttachAddItemPlanItem> =
  z.object({
    feature_id: z.string().describe("The ID of the feature to configure."),
    included: z.number().optional().describe(
      "Number of free units included. Balance resets to this each interval for consumable features.",
    ),
    price: z.lazy(() => AttachAddItemPrice$zodSchema).optional().describe(
      "Pricing for usage beyond included units. Omit for free features.",
    ),
    proration: z.lazy(() => AttachAddItemProration$zodSchema).optional()
      .describe(
        "Proration settings for prepaid features. Controls mid-cycle quantity change billing.",
      ),
    reset: z.lazy(() => AttachAddItemReset$zodSchema).optional().describe(
      "Reset configuration for consumable features. Omit for non-consumable features like seats.",
    ),
    rollover: z.lazy(() => AttachAddItemRollover$zodSchema).optional().describe(
      "Rollover config for unused units. If set, unused included units carry over.",
    ),
    unlimited: z.boolean().optional().describe(
      "If true, customer has unlimited access to this feature.",
    ),
  }).describe(
    "Configuration for a feature item in a plan, including usage limits, pricing, and rollover settings.",
  );

/**
 * Match items with this billing method (prepaid or usage_based).
 */
export const AttachRemoveItemBillingMethod = {
  Prepaid: "prepaid",
  UsageBased: "usage_based",
} as const;
/**
 * Match items with this billing method (prepaid or usage_based).
 */
export type AttachRemoveItemBillingMethod = ClosedEnum<
  typeof AttachRemoveItemBillingMethod
>;

export const AttachRemoveItemBillingMethod$zodSchema = z.enum([
  "prepaid",
  "usage_based",
]).describe("Match items with this billing method (prepaid or usage_based).");

/**
 * Match items with this interval.
 */
export const AttachRemoveItemInterval = {
  OneOff: "one_off",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
/**
 * Match items with this interval.
 */
export type AttachRemoveItemInterval = ClosedEnum<
  typeof AttachRemoveItemInterval
>;

export const AttachRemoveItemInterval$zodSchema = z.enum([
  "one_off",
  "week",
  "month",
  "quarter",
  "semi_annual",
  "year",
]).describe("Match items with this interval.");

/**
 * Filter for matching plan items. All provided fields must match (AND).
 */
export type AttachPlanItemFilter = {
  feature_id?: string | undefined;
  billing_method?: AttachRemoveItemBillingMethod | undefined;
  interval?: AttachRemoveItemInterval | undefined;
};

export const AttachPlanItemFilter$zodSchema: z.ZodType<AttachPlanItemFilter> = z
  .object({
    billing_method: AttachRemoveItemBillingMethod$zodSchema.optional().describe(
      "Match items with this billing method (prepaid or usage_based).",
    ),
    feature_id: z.string().optional().describe(
      "Match items linked to this feature.",
    ),
    interval: AttachRemoveItemInterval$zodSchema.optional().describe(
      "Match items with this interval.",
    ),
  }).describe(
    "Filter for matching plan items. All provided fields must match (AND).",
  );

/**
 * Unit of time for the trial ('day', 'month', 'year').
 */
export const AttachDurationType = {
  Day: "day",
  Month: "month",
  Year: "year",
} as const;
/**
 * Unit of time for the trial ('day', 'month', 'year').
 */
export type AttachDurationType = ClosedEnum<typeof AttachDurationType>;

export const AttachDurationType$zodSchema = z.enum([
  "day",
  "month",
  "year",
]).describe("Unit of time for the trial ('day', 'month', 'year').");

/**
 * Behavior when the trial ends. 'bill' charges the customer (default). 'revert' expires the trial and restores the customer's previous plan.
 */
export const AttachOnEnd = {
  Bill: "bill",
  Revert: "revert",
} as const;
/**
 * Behavior when the trial ends. 'bill' charges the customer (default). 'revert' expires the trial and restores the customer's previous plan.
 */
export type AttachOnEnd = ClosedEnum<typeof AttachOnEnd>;

export const AttachOnEnd$zodSchema = z.enum([
  "bill",
  "revert",
]).describe(
  "Behavior when the trial ends. 'bill' charges the customer (default). 'revert' expires the trial and restores the customer's previous plan.",
);

/**
 * Free trial configuration for a plan.
 */
export type AttachFreeTrialParams = {
  duration_length: number;
  duration_type?: AttachDurationType | undefined;
  card_required?: boolean | undefined;
  on_end?: AttachOnEnd | undefined;
};

export const AttachFreeTrialParams$zodSchema: z.ZodType<AttachFreeTrialParams> =
  z.object({
    card_required: z.boolean().default(true).describe(
      "If true, payment method required to start trial. Customer is charged after trial ends.",
    ),
    duration_length: z.number().describe(
      "Number of duration_type periods the trial lasts.",
    ),
    duration_type: AttachDurationType$zodSchema.default("month").describe(
      "Unit of time for the trial ('day', 'month', 'year').",
    ),
    on_end: AttachOnEnd$zodSchema.optional().describe(
      "Behavior when the trial ends. 'bill' charges the customer (default). 'revert' expires the trial and restores the customer's previous plan.",
    ),
  }).describe("Free trial configuration for a plan.");

/**
 * Customize the plan to attach. Can override the price, items, free trial, or a combination.
 */
export type AttachCustomize = {
  price?: AttachBasePrice | null | undefined;
  items?: Array<AttachItemPlanItem> | undefined;
  add_items?: Array<AttachAddItemPlanItem> | undefined;
  remove_items?: Array<AttachPlanItemFilter> | undefined;
  free_trial?: AttachFreeTrialParams | null | undefined;
};

export const AttachCustomize$zodSchema: z.ZodType<AttachCustomize> = z.object({
  add_items: z.array(z.lazy(() => AttachAddItemPlanItem$zodSchema)).optional()
    .describe("Items to add to the plan."),
  free_trial: z.lazy(() => AttachFreeTrialParams$zodSchema).nullable()
    .optional().describe(
      "Override the plan's default free trial. Pass an object to set a custom trial, or null to remove the trial entirely.",
    ),
  items: z.array(z.lazy(() => AttachItemPlanItem$zodSchema)).optional()
    .describe(
      "Override the items in the plan (PUT-style — replaces all existing items). Mutually exclusive with add_items / remove_items.",
    ),
  price: z.lazy(() => AttachBasePrice$zodSchema).nullable().optional().describe(
    "Override the base price of the plan. Pass null to remove the base price.",
  ),
  remove_items: z.array(z.lazy(() => AttachPlanItemFilter$zodSchema)).optional()
    .describe("Filters selecting items to remove from the plan."),
}).describe(
  "Customize the plan to attach. Can override the price, items, free trial, or a combination.",
);

/**
 * Invoice mode creates a draft or open invoice and sends it to the customer, instead of charging their card immediately. This uses Stripe's send_invoice collection method.
 */
export type AttachInvoiceMode = {
  enabled: boolean;
  enable_plan_immediately?: boolean | undefined;
  finalize?: boolean | undefined;
};

export const AttachInvoiceMode$zodSchema: z.ZodType<AttachInvoiceMode> = z
  .object({
    enable_plan_immediately: z.boolean().default(false).describe(
      "If true, enables the plan immediately even though the invoice is not paid yet.",
    ),
    enabled: z.boolean().describe(
      "When true, creates an invoice and sends it to the customer instead of charging their card immediately. Uses Stripe's send_invoice collection method.",
    ),
    finalize: z.boolean().default(true).describe(
      "If true, finalizes the invoice so it can be sent to the customer. If false, keeps it as a draft for manual review.",
    ),
  }).describe(
    "Invoice mode creates a draft or open invoice and sends it to the customer, instead of charging their card immediately. This uses Stripe's send_invoice collection method.",
  );

/**
 * How to handle proration when updating an existing subscription. 'prorate_immediately' charges/credits prorated amounts now, 'none' skips creating any charges.
 */
export const AttachProrationBehavior = {
  ProrateImmediately: "prorate_immediately",
  None: "none",
} as const;
/**
 * How to handle proration when updating an existing subscription. 'prorate_immediately' charges/credits prorated amounts now, 'none' skips creating any charges.
 */
export type AttachProrationBehavior = ClosedEnum<
  typeof AttachProrationBehavior
>;

export const AttachProrationBehavior$zodSchema = z.enum([
  "prorate_immediately",
  "none",
]).describe(
  "How to handle proration when updating an existing subscription. 'prorate_immediately' charges/credits prorated amounts now, 'none' skips creating any charges.",
);

/**
 * Controls when to return a checkout URL. 'always' returns a URL even if payment succeeds, 'if_required' only when payment action is needed, 'never' disables redirects.
 */
export const AttachRedirectMode = {
  Always: "always",
  IfRequired: "if_required",
  Never: "never",
} as const;
/**
 * Controls when to return a checkout URL. 'always' returns a URL even if payment succeeds, 'if_required' only when payment action is needed, 'never' disables redirects.
 */
export type AttachRedirectMode = ClosedEnum<typeof AttachRedirectMode>;

export const AttachRedirectMode$zodSchema = z.enum([
  "always",
  "if_required",
  "never",
]).describe(
  "Controls when to return a checkout URL. 'always' returns a URL even if payment succeeds, 'if_required' only when payment action is needed, 'never' disables redirects.",
);

/**
 * A discount to apply. Can be either a reward ID or a promotion code.
 */
export type AttachAttachDiscount = {
  reward_id?: string | undefined;
  promotion_code?: string | undefined;
};

export const AttachAttachDiscount$zodSchema: z.ZodType<AttachAttachDiscount> = z
  .object({
    promotion_code: z.string().optional().describe(
      "The promotion code to apply as a discount.",
    ),
    reward_id: z.string().optional().describe(
      "The ID of the reward to apply as a discount.",
    ),
  }).describe(
    "A discount to apply. Can be either a reward ID or a promotion code.",
  );

/**
 * When the plan change should take effect. 'immediate' applies now, 'end_of_cycle' schedules for the end of the current billing cycle. By default, upgrades are immediate and downgrades are scheduled.
 */
export const AttachPlanSchedule = {
  Immediate: "immediate",
  EndOfCycle: "end_of_cycle",
} as const;
/**
 * When the plan change should take effect. 'immediate' applies now, 'end_of_cycle' schedules for the end of the current billing cycle. By default, upgrades are immediate and downgrades are scheduled.
 */
export type AttachPlanSchedule = ClosedEnum<typeof AttachPlanSchedule>;

export const AttachPlanSchedule$zodSchema = z.enum([
  "immediate",
  "end_of_cycle",
]).describe(
  "When the plan change should take effect. 'immediate' applies now, 'end_of_cycle' schedules for the end of the current billing cycle. By default, upgrades are immediate and downgrades are scheduled.",
);

export type AttachCustomLineItem = { amount: number; description: string };

export const AttachCustomLineItem$zodSchema: z.ZodType<AttachCustomLineItem> = z
  .object({
    amount: z.number().describe(
      "Amount in dollars for this line item (e.g. 10.50). Can be negative for credits.",
    ),
    description: z.string().describe("Description for the line item."),
  });

/**
 * Whether to carry over balances from the previous plan.
 */
export type AttachCarryOverBalances = {
  enabled: boolean;
  feature_ids?: Array<string> | undefined;
};

export const AttachCarryOverBalances$zodSchema: z.ZodType<
  AttachCarryOverBalances
> = z.object({
  enabled: z.boolean().describe(
    "Whether to carry over balances from the previous plan.",
  ),
  feature_ids: z.array(z.string()).optional().describe(
    "The IDs of the features to carry over balances from. If left undefined, all features will be carried over.",
  ),
}).describe("Whether to carry over balances from the previous plan.");

/**
 * Whether to carry over usages from the previous plan.
 */
export type AttachCarryOverUsages = {
  enabled: boolean;
  feature_ids?: Array<string> | undefined;
};

export const AttachCarryOverUsages$zodSchema: z.ZodType<AttachCarryOverUsages> =
  z.object({
    enabled: z.boolean().describe(
      "Whether to carry over usages from the previous plan.",
    ),
    feature_ids: z.array(z.string()).optional().describe(
      "The IDs of the features to carry over usages for. If left undefined, all consumable features will be carried over.",
    ),
  }).describe("Whether to carry over usages from the previous plan.");

export type AttachParams = {
  customer_id: string;
  entity_id?: string | undefined;
  plan_id: string;
  feature_quantities?: Array<AttachFeatureQuantity> | undefined;
  version?: number | undefined;
  customize?: AttachCustomize | undefined;
  invoice_mode?: AttachInvoiceMode | undefined;
  proration_behavior?: AttachProrationBehavior | undefined;
  redirect_mode?: AttachRedirectMode | undefined;
  subscription_id?: string | undefined;
  discounts?: Array<AttachAttachDiscount> | undefined;
  success_url?: string | undefined;
  new_billing_subscription?: boolean | undefined;
  billing_cycle_anchor?: "now" | undefined;
  plan_schedule?: AttachPlanSchedule | undefined;
  starts_at?: number | undefined;
  ends_at?: number | undefined;
  checkout_session_params?: { [k: string]: any } | undefined;
  custom_line_items?: Array<AttachCustomLineItem> | undefined;
  processor_subscription_id?: string | undefined;
  carry_over_balances?: AttachCarryOverBalances | undefined;
  carry_over_usages?: AttachCarryOverUsages | undefined;
  metadata?: { [k: string]: string } | undefined;
  no_billing_changes?: boolean | undefined;
  enable_plan_immediately?: boolean | undefined;
  tax_rate_id?: string | undefined;
};

export const AttachParams$zodSchema: z.ZodType<AttachParams> = z.object({
  billing_cycle_anchor: z.literal("now").default("now").optional().describe(
    "Reset the billing cycle anchor immediately with 'now'.",
  ),
  carry_over_balances: z.lazy(() => AttachCarryOverBalances$zodSchema)
    .optional().describe(
      "Whether to carry over balances from the previous plan.",
    ),
  carry_over_usages: z.lazy(() => AttachCarryOverUsages$zodSchema).optional()
    .describe("Whether to carry over usages from the previous plan."),
  checkout_session_params: z.record(z.string(), z.any()).optional().describe(
    "Additional parameters to pass into the creation of the Stripe checkout session.",
  ),
  custom_line_items: z.array(z.lazy(() => AttachCustomLineItem$zodSchema))
    .optional().describe(
      "Custom line items that override the auto-generated proration invoice. Only valid for immediate plan changes (eg. upgrades or one off plans).",
    ),
  customer_id: z.string().describe(
    "The ID of the customer to attach the plan to.",
  ),
  customize: z.lazy(() => AttachCustomize$zodSchema).optional().describe(
    "Customize the plan to attach. Can override the price, items, free trial, or a combination.",
  ),
  discounts: z.array(z.lazy(() => AttachAttachDiscount$zodSchema)).optional()
    .describe(
      "List of discounts to apply. Each discount can be an Autumn reward ID, Stripe coupon ID, or Stripe promotion code.",
    ),
  enable_plan_immediately: z.boolean().optional().describe(
    "If true, the customer's plan is activated immediately even when payment is deferred (invoice mode) or pending (Stripe checkout). For Stripe checkout, the customer_product is inserted before the customer completes the hosted form.",
  ),
  ends_at: z.int().optional().describe(
    "Unix timestamp in milliseconds for when the attached plan should end.",
  ),
  entity_id: z.string().optional().describe(
    "The ID of the entity to attach the plan to.",
  ),
  feature_quantities: z.array(z.lazy(() => AttachFeatureQuantity$zodSchema))
    .optional().describe(
      "If this plan contains prepaid features, use this field to specify the quantity of each prepaid feature. This quantity includes the included amount and billing units defined when setting up the plan.",
    ),
  invoice_mode: z.lazy(() => AttachInvoiceMode$zodSchema).optional().describe(
    "Invoice mode creates a draft or open invoice and sends it to the customer, instead of charging their card immediately. This uses Stripe's send_invoice collection method.",
  ),
  metadata: z.record(z.string(), z.string()).optional().describe(
    "Key-value metadata to attach to the Stripe subscription, invoice, and checkout session created during this attach flow. Keys prefixed with 'autumn_' are reserved and will be stripped.",
  ),
  new_billing_subscription: z.boolean().optional().describe(
    "Only applicable when the customer has an existing Stripe subscription. If true, creates a new separate subscription instead of merging into the existing one.",
  ),
  no_billing_changes: z.boolean().optional().describe(
    "If true, skips any billing changes for the attach operation.",
  ),
  plan_id: z.string().describe("The ID of the plan."),
  plan_schedule: AttachPlanSchedule$zodSchema.optional().describe(
    "When the plan change should take effect. 'immediate' applies now, 'end_of_cycle' schedules for the end of the current billing cycle. By default, upgrades are immediate and downgrades are scheduled.",
  ),
  processor_subscription_id: z.string().optional().describe(
    "The processor subscription ID to link. Use this to attach an existing Stripe subscription instead of creating a new one.",
  ),
  proration_behavior: AttachProrationBehavior$zodSchema.optional().describe(
    "How to handle proration when updating an existing subscription. 'prorate_immediately' charges/credits prorated amounts now, 'none' skips creating any charges.",
  ),
  redirect_mode: AttachRedirectMode$zodSchema.default("if_required").describe(
    "Controls when to return a checkout URL. 'always' returns a URL even if payment succeeds, 'if_required' only when payment action is needed, 'never' disables redirects.",
  ),
  starts_at: z.int().optional().describe(
    "Unix timestamp in milliseconds for when the attached plan should start. Future dates create a scheduled subscription.",
  ),
  subscription_id: z.string().optional().describe(
    "A unique ID to identify this subscription. Can be used to target specific subscriptions in update operations when a customer has multiple products with the same plan.",
  ),
  success_url: z.string().optional().describe(
    "URL to redirect to after successful checkout.",
  ),
  tax_rate_id: z.string().optional().describe(
    "Stripe tax rate ID (txr_...) to apply as the default tax rate on the created subscription, invoice, or checkout session line items.",
  ),
  version: z.number().optional().describe("The version of the plan to attach."),
});

/**
 * Invoice details if an invoice was created. Only present when a charge was made.
 */
export type AttachInvoice = {
  status: string | null;
  stripe_id: string;
  total: number;
  currency: string;
  hosted_invoice_url: string | null;
};

export const AttachInvoice$zodSchema: z.ZodType<AttachInvoice> = z.object({
  currency: z.string().describe(
    "The three-letter ISO currency code (e.g., 'usd').",
  ),
  hosted_invoice_url: z.string().nullable().describe(
    "URL to the hosted invoice page where the customer can view and pay the invoice.",
  ),
  status: z.string().nullable().describe(
    "The status of the invoice (e.g., 'paid', 'open', 'draft').",
  ),
  stripe_id: z.string().describe("The Stripe invoice ID."),
  total: z.number().describe("The total amount of the invoice in cents."),
}).describe(
  "Invoice details if an invoice was created. Only present when a charge was made.",
);

/**
 * The type of action required to complete the payment.
 */
export const AttachCode = {
  ThreedsRequired: "3ds_required",
  PaymentMethodRequired: "payment_method_required",
  PaymentFailed: "payment_failed",
} as const;
/**
 * The type of action required to complete the payment.
 */
export type AttachCode = ClosedEnum<typeof AttachCode>;

export const AttachCode$zodSchema = z.enum([
  "3ds_required",
  "payment_method_required",
  "payment_failed",
]).describe("The type of action required to complete the payment.");

/**
 * Details about any action required to complete the payment. Present when the payment could not be processed automatically.
 */
export type AttachRequiredAction = { code: AttachCode; reason: string };

export const AttachRequiredAction$zodSchema: z.ZodType<AttachRequiredAction> = z
  .object({
    code: AttachCode$zodSchema.describe(
      "The type of action required to complete the payment.",
    ),
    reason: z.string().describe(
      "A human-readable explanation of why this action is required.",
    ),
  }).describe(
    "Details about any action required to complete the payment. Present when the payment could not be processed automatically.",
  );

/**
 * OK
 */
export type AttachResponse = {
  customer_id: string;
  entity_id?: string | undefined;
  invoice?: AttachInvoice | undefined;
  payment_url: string | null;
  required_action?: AttachRequiredAction | undefined;
};

export const AttachResponse$zodSchema: z.ZodType<AttachResponse> = z.object({
  customer_id: z.string().describe("The ID of the customer."),
  entity_id: z.string().optional().describe(
    "The ID of the entity, if the plan was attached to an entity.",
  ),
  invoice: z.lazy(() => AttachInvoice$zodSchema).optional().describe(
    "Invoice details if an invoice was created. Only present when a charge was made.",
  ),
  payment_url: z.string().nullable().describe(
    "URL to redirect the customer to complete payment. Null if no payment action is required.",
  ),
  required_action: z.lazy(() => AttachRequiredAction$zodSchema).optional()
    .describe(
      "Details about any action required to complete the payment. Present when the payment could not be processed automatically.",
    ),
}).describe("OK");
