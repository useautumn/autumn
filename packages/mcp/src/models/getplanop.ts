/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { ClosedEnum } from "../types/enums.js";

export type GetPlanGlobals = { xApiVersion?: string | undefined };

export const GetPlanGlobals$zodSchema: z.ZodType<GetPlanGlobals> = z.object({
  xApiVersion: z.string().default("2.3.0"),
});

export type GetPlanParams = { plan_id: string; version?: number | undefined };

export const GetPlanParams$zodSchema: z.ZodType<GetPlanParams> = z.object({
  plan_id: z.string().describe("The ID of the plan to retrieve."),
  version: z.number().optional().describe(
    "The version of the plan to get. Defaults to the latest version.",
  ),
});

/**
 * Billing interval (e.g. 'month', 'year').
 */
export const GetPlanPriceInterval = {
  OneOff: "one_off",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
/**
 * Billing interval (e.g. 'month', 'year').
 */
export type GetPlanPriceInterval = ClosedEnum<typeof GetPlanPriceInterval>;

export const GetPlanPriceInterval$zodSchema = z.enum([
  "one_off",
  "week",
  "month",
  "quarter",
  "semi_annual",
  "year",
]).describe("Billing interval (e.g. 'month', 'year').");

/**
 * Display text for showing this price in pricing pages.
 */
export type GetPlanPriceDisplay = {
  primary_text: string;
  secondary_text?: string | undefined;
};

export const GetPlanPriceDisplay$zodSchema: z.ZodType<GetPlanPriceDisplay> = z
  .object({
    primary_text: z.string().describe(
      "Main display text (e.g. '$10' or '100 messages').",
    ),
    secondary_text: z.string().optional().describe(
      "Secondary display text (e.g. 'per month' or 'then $0.5 per 100').",
    ),
  }).describe("Display text for showing this price in pricing pages.");

export type GetPlanPrice = {
  amount: number;
  interval: GetPlanPriceInterval;
  interval_count?: number | undefined;
  display?: GetPlanPriceDisplay | undefined;
};

export const GetPlanPrice$zodSchema: z.ZodType<GetPlanPrice> = z.object({
  amount: z.number().describe("Base price amount for the plan."),
  display: z.lazy(() => GetPlanPriceDisplay$zodSchema).optional().describe(
    "Display text for showing this price in pricing pages.",
  ),
  interval: GetPlanPriceInterval$zodSchema.describe(
    "Billing interval (e.g. 'month', 'year').",
  ),
  interval_count: z.number().optional().describe(
    "Number of intervals per billing cycle. Defaults to 1.",
  ),
});

/**
 * The type of the feature
 */
export const GetPlanType = {
  Static: "static",
  Boolean: "boolean",
  SingleUse: "single_use",
  ContinuousUse: "continuous_use",
  CreditSystem: "credit_system",
} as const;
/**
 * The type of the feature
 */
export type GetPlanType = ClosedEnum<typeof GetPlanType>;

export const GetPlanType$zodSchema = z.enum([
  "static",
  "boolean",
  "single_use",
  "continuous_use",
  "credit_system",
]).describe("The type of the feature");

export type GetPlanFeatureDisplay = { singular: string; plural: string };

export const GetPlanFeatureDisplay$zodSchema: z.ZodType<GetPlanFeatureDisplay> =
  z.object({
    plural: z.string().describe("The plural display name for the feature."),
    singular: z.string().describe("The singular display name for the feature."),
  });

export type GetPlanCreditSchema = {
  metered_feature_id: string;
  credit_cost: number;
};

export const GetPlanCreditSchema$zodSchema: z.ZodType<GetPlanCreditSchema> = z
  .object({
    credit_cost: z.number().describe("The credit cost of the metered feature."),
    metered_feature_id: z.string().describe(
      "The ID of the metered feature (should be a single_use feature).",
    ),
  });

/**
 * The full feature object if expanded.
 */
export type GetPlanFeature = {
  id: string;
  name?: string | null | undefined;
  type: GetPlanType;
  display?: GetPlanFeatureDisplay | null | undefined;
  credit_schema?: Array<GetPlanCreditSchema> | null | undefined;
  archived?: boolean | null | undefined;
};

export const GetPlanFeature$zodSchema: z.ZodType<GetPlanFeature> = z.object({
  archived: z.boolean().nullable().optional().describe(
    "Whether or not the feature is archived.",
  ),
  credit_schema: z.array(z.lazy(() => GetPlanCreditSchema$zodSchema)).nullable()
    .optional().describe("Credit cost schema for credit system features."),
  display: z.lazy(() => GetPlanFeatureDisplay$zodSchema).nullable().optional()
    .describe("Singular and plural display names for the feature."),
  id: z.string().describe(
    "The ID of the feature, used to refer to it in other API calls like /track or /check.",
  ),
  name: z.string().nullable().optional().describe("The name of the feature."),
  type: GetPlanType$zodSchema.describe("The type of the feature"),
}).describe("The full feature object if expanded.");

/**
 * The interval at which the feature balance resets (e.g. 'month', 'year'). For consumable features, usage resets to 0 and included units are restored.
 */
export const GetPlanResetInterval = {
  OneOff: "one_off",
  Minute: "minute",
  Hour: "hour",
  Day: "day",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
/**
 * The interval at which the feature balance resets (e.g. 'month', 'year'). For consumable features, usage resets to 0 and included units are restored.
 */
export type GetPlanResetInterval = ClosedEnum<typeof GetPlanResetInterval>;

export const GetPlanResetInterval$zodSchema = z.enum([
  "one_off",
  "minute",
  "hour",
  "day",
  "week",
  "month",
  "quarter",
  "semi_annual",
  "year",
]).describe(
  "The interval at which the feature balance resets (e.g. 'month', 'year'). For consumable features, usage resets to 0 and included units are restored.",
);

export type GetPlanReset = {
  interval: GetPlanResetInterval;
  interval_count?: number | undefined;
};

export const GetPlanReset$zodSchema: z.ZodType<GetPlanReset> = z.object({
  interval: GetPlanResetInterval$zodSchema.describe(
    "The interval at which the feature balance resets (e.g. 'month', 'year'). For consumable features, usage resets to 0 and included units are restored.",
  ),
  interval_count: z.number().optional().describe(
    "Number of intervals between resets. Defaults to 1.",
  ),
});

export const GetPlanTierBehavior = {
  Graduated: "graduated",
  Volume: "volume",
} as const;
export type GetPlanTierBehavior = ClosedEnum<typeof GetPlanTierBehavior>;

export const GetPlanTierBehavior$zodSchema = z.enum([
  "graduated",
  "volume",
]);

/**
 * Billing interval for this price. For consumable features, should match reset.interval.
 */
export const GetPlanPriceItemInterval = {
  OneOff: "one_off",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
/**
 * Billing interval for this price. For consumable features, should match reset.interval.
 */
export type GetPlanPriceItemInterval = ClosedEnum<
  typeof GetPlanPriceItemInterval
>;

export const GetPlanPriceItemInterval$zodSchema = z.enum([
  "one_off",
  "week",
  "month",
  "quarter",
  "semi_annual",
  "year",
]).describe(
  "Billing interval for this price. For consumable features, should match reset.interval.",
);

/**
 * 'prepaid' for features like seats where customers pay upfront, 'usage_based' for pay-as-you-go after included usage.
 */
export const GetPlanBillingMethod = {
  Prepaid: "prepaid",
  UsageBased: "usage_based",
} as const;
/**
 * 'prepaid' for features like seats where customers pay upfront, 'usage_based' for pay-as-you-go after included usage.
 */
export type GetPlanBillingMethod = ClosedEnum<typeof GetPlanBillingMethod>;

export const GetPlanBillingMethod$zodSchema = z.enum([
  "prepaid",
  "usage_based",
]).describe(
  "'prepaid' for features like seats where customers pay upfront, 'usage_based' for pay-as-you-go after included usage.",
);

export type GetPlanItemPrice = {
  amount?: number | undefined;
  tiers?: Array<any | null> | undefined;
  tier_behavior?: GetPlanTierBehavior | undefined;
  interval: GetPlanPriceItemInterval;
  interval_count?: number | undefined;
  billing_units: number;
  billing_method: GetPlanBillingMethod;
  max_purchase: number | null;
};

export const GetPlanItemPrice$zodSchema: z.ZodType<GetPlanItemPrice> = z.object(
  {
    amount: z.number().optional().describe(
      "Price per billing_units after included usage is consumed. Mutually exclusive with tiers.",
    ),
    billing_method: GetPlanBillingMethod$zodSchema.describe(
      "'prepaid' for features like seats where customers pay upfront, 'usage_based' for pay-as-you-go after included usage.",
    ),
    billing_units: z.number().describe(
      "Number of units per price increment. Usage is rounded UP to the nearest billing_units when billed (e.g. billing_units=100 means 101 usage rounds to 200).",
    ),
    interval: GetPlanPriceItemInterval$zodSchema.describe(
      "Billing interval for this price. For consumable features, should match reset.interval.",
    ),
    interval_count: z.number().optional().describe(
      "Number of intervals per billing cycle. Defaults to 1.",
    ),
    max_purchase: z.number().nullable().describe(
      "Maximum units a customer can purchase beyond included. E.g. if included=100 and max_purchase=300, customer can use up to 400 total before usage is capped. Null for no limit.",
    ),
    tier_behavior: GetPlanTierBehavior$zodSchema.optional(),
    tiers: z.array(z.any().nullable()).optional().describe(
      "Tiered pricing configuration. Each tier's 'to' INCLUDES the included amount. Either 'tiers' or 'amount' is required.",
    ),
  },
);

/**
 * Display text for showing this item in pricing pages.
 */
export type GetPlanItemDisplay = {
  primary_text: string;
  secondary_text?: string | undefined;
};

export const GetPlanItemDisplay$zodSchema: z.ZodType<GetPlanItemDisplay> = z
  .object({
    primary_text: z.string().describe(
      "Main display text (e.g. '$10' or '100 messages').",
    ),
    secondary_text: z.string().optional().describe(
      "Secondary display text (e.g. 'per month' or 'then $0.5 per 100').",
    ),
  }).describe("Display text for showing this item in pricing pages.");

/**
 * When rolled over units expire.
 */
export const GetPlanExpiryDurationType = {
  Month: "month",
  Forever: "forever",
} as const;
/**
 * When rolled over units expire.
 */
export type GetPlanExpiryDurationType = ClosedEnum<
  typeof GetPlanExpiryDurationType
>;

export const GetPlanExpiryDurationType$zodSchema = z.enum([
  "month",
  "forever",
]).describe("When rolled over units expire.");

/**
 * Rollover configuration for unused units. If set, unused included units roll over to the next period.
 */
export type GetPlanRollover = {
  max: number | null;
  max_percentage?: number | null | undefined;
  expiry_duration_type: GetPlanExpiryDurationType;
  expiry_duration_length?: number | undefined;
};

export const GetPlanRollover$zodSchema: z.ZodType<GetPlanRollover> = z.object({
  expiry_duration_length: z.number().optional().describe(
    "Number of periods before expiry.",
  ),
  expiry_duration_type: GetPlanExpiryDurationType$zodSchema.describe(
    "When rolled over units expire.",
  ),
  max: z.number().nullable().describe(
    "Maximum rollover units. Null for unlimited rollover.",
  ),
  max_percentage: z.number().nullable().optional().describe(
    "Maximum rollover as a percentage (0-100) of included + prepaid grant. Mutually exclusive with max.",
  ),
}).describe(
  "Rollover configuration for unused units. If set, unused included units roll over to the next period.",
);

export type GetPlanItem = {
  feature_id: string;
  feature?: GetPlanFeature | undefined;
  included: number;
  unlimited: boolean;
  reset: GetPlanReset | null;
  price: GetPlanItemPrice | null;
  display?: GetPlanItemDisplay | undefined;
  rollover?: GetPlanRollover | undefined;
};

export const GetPlanItem$zodSchema: z.ZodType<GetPlanItem> = z.object({
  display: z.lazy(() => GetPlanItemDisplay$zodSchema).optional().describe(
    "Display text for showing this item in pricing pages.",
  ),
  feature: z.lazy(() => GetPlanFeature$zodSchema).optional().describe(
    "The full feature object if expanded.",
  ),
  feature_id: z.string().describe(
    "The ID of the feature this item configures.",
  ),
  included: z.number().describe(
    "Number of free units included. For consumable features, balance resets to this number each interval.",
  ),
  price: z.lazy(() => GetPlanItemPrice$zodSchema).nullable().describe(
    "Pricing configuration for usage beyond included units. Null if feature is entirely free.",
  ),
  reset: z.lazy(() => GetPlanReset$zodSchema).nullable().describe(
    "Reset configuration for consumable features. Null for non-consumable features like seats where usage persists across billing cycles.",
  ),
  rollover: z.lazy(() => GetPlanRollover$zodSchema).optional().describe(
    "Rollover configuration for unused units. If set, unused included units roll over to the next period.",
  ),
  unlimited: z.boolean().describe(
    "Whether the customer has unlimited access to this feature.",
  ),
});

/**
 * Unit of time for the trial duration ('day', 'month', 'year').
 */
export const GetPlanDurationType = {
  Day: "day",
  Month: "month",
  Year: "year",
} as const;
/**
 * Unit of time for the trial duration ('day', 'month', 'year').
 */
export type GetPlanDurationType = ClosedEnum<typeof GetPlanDurationType>;

export const GetPlanDurationType$zodSchema = z.enum([
  "day",
  "month",
  "year",
]).describe("Unit of time for the trial duration ('day', 'month', 'year').");

export const GetPlanOnEnd = {
  Bill: "bill",
  Revert: "revert",
} as const;
export type GetPlanOnEnd = ClosedEnum<typeof GetPlanOnEnd>;

export const GetPlanOnEnd$zodSchema = z.enum([
  "bill",
  "revert",
]);

/**
 * Free trial configuration. If set, new customers can try this plan before being charged.
 */
export type GetPlanFreeTrial = {
  duration_length: number;
  duration_type: GetPlanDurationType;
  card_required: boolean;
  on_end?: GetPlanOnEnd | null | undefined;
};

export const GetPlanFreeTrial$zodSchema: z.ZodType<GetPlanFreeTrial> = z.object(
  {
    card_required: z.boolean().describe(
      "Whether a payment method is required to start the trial. If true, customer will be charged after trial ends.",
    ),
    duration_length: z.number().describe(
      "Number of duration_type periods the trial lasts.",
    ),
    duration_type: GetPlanDurationType$zodSchema.describe(
      "Unit of time for the trial duration ('day', 'month', 'year').",
    ),
    on_end: GetPlanOnEnd$zodSchema.nullable().optional().describe(
      "Behavior when the trial ends. 'bill' charges the customer (default). 'revert' expires the trial and restores the customer's previous plan.",
    ),
  },
).describe(
  "Free trial configuration. If set, new customers can try this plan before being charged.",
);

/**
 * Environment this plan belongs to ('sandbox' or 'live').
 */
export const GetPlanEnv = {
  Sandbox: "sandbox",
  Live: "live",
} as const;
/**
 * Environment this plan belongs to ('sandbox' or 'live').
 */
export type GetPlanEnv = ClosedEnum<typeof GetPlanEnv>;

export const GetPlanEnv$zodSchema = z.enum([
  "sandbox",
  "live",
]).describe("Environment this plan belongs to ('sandbox' or 'live').");

/**
 * Miscellaneous plan-level configuration flags.
 */
export type GetPlanConfig = { ignore_past_due?: boolean | undefined };

export const GetPlanConfig$zodSchema: z.ZodType<GetPlanConfig> = z.object({
  ignore_past_due: z.boolean().default(false).describe(
    "If true, entitlements attached to this plan will still reset on schedule even when the customer's product is in a past_due state.",
  ),
}).describe("Miscellaneous plan-level configuration flags.");

/**
 * The customer's current status with this plan. 'active' if attached, 'scheduled' if pending activation.
 */
export const GetPlanStatus = {
  Active: "active",
  Scheduled: "scheduled",
} as const;
/**
 * The customer's current status with this plan. 'active' if attached, 'scheduled' if pending activation.
 */
export type GetPlanStatus = ClosedEnum<typeof GetPlanStatus>;

export const GetPlanStatus$zodSchema = z.enum([
  "active",
  "scheduled",
]).describe(
  "The customer's current status with this plan. 'active' if attached, 'scheduled' if pending activation.",
);

/**
 * The action that would occur if this plan were attached to the customer.
 */
export const GetPlanAttachAction = {
  Activate: "activate",
  Upgrade: "upgrade",
  Downgrade: "downgrade",
  None: "none",
  Purchase: "purchase",
} as const;
/**
 * The action that would occur if this plan were attached to the customer.
 */
export type GetPlanAttachAction = ClosedEnum<typeof GetPlanAttachAction>;

export const GetPlanAttachAction$zodSchema = z.enum([
  "activate",
  "upgrade",
  "downgrade",
  "none",
  "purchase",
]).describe(
  "The action that would occur if this plan were attached to the customer.",
);

export type GetPlanCustomerEligibility = {
  trial_available?: boolean | undefined;
  status?: GetPlanStatus | undefined;
  canceling?: boolean | undefined;
  trialing?: boolean | undefined;
  attach_action: GetPlanAttachAction;
};

export const GetPlanCustomerEligibility$zodSchema: z.ZodType<
  GetPlanCustomerEligibility
> = z.object({
  attach_action: GetPlanAttachAction$zodSchema.describe(
    "The action that would occur if this plan were attached to the customer.",
  ),
  canceling: z.boolean().optional().describe(
    "Whether the customer's active instance of this plan is set to cancel.",
  ),
  status: GetPlanStatus$zodSchema.optional().describe(
    "The customer's current status with this plan. 'active' if attached, 'scheduled' if pending activation.",
  ),
  trial_available: z.boolean().optional().describe(
    "Whether the trial on this plan is available to this customer. For example, if the customer used the trial in the past, this will be false.",
  ),
  trialing: z.boolean().optional().describe(
    "Whether the customer is currently on a free trial of this plan.",
  ),
});

/**
 * A plan defines a set of features, pricing, and entitlements that can be attached to customers.
 */
export type GetPlanResponse = {
  id: string;
  name: string;
  description: string | null;
  group: string | null;
  version: number;
  add_on: boolean;
  auto_enable: boolean;
  price: GetPlanPrice | null;
  items: Array<GetPlanItem>;
  free_trial?: GetPlanFreeTrial | undefined;
  created_at: number;
  env: GetPlanEnv;
  archived: boolean;
  base_variant_id: string | null;
  config: GetPlanConfig;
  customer_eligibility?: GetPlanCustomerEligibility | undefined;
};

export const GetPlanResponse$zodSchema: z.ZodType<GetPlanResponse> = z.object({
  add_on: z.boolean().describe(
    "Whether this is an add-on plan that can be attached alongside a main plan.",
  ),
  archived: z.boolean().describe(
    "Whether the plan is archived. Archived plans cannot be attached to new customers.",
  ),
  auto_enable: z.boolean().describe(
    "If true, this plan is automatically attached when a customer is created. Used for free plans.",
  ),
  base_variant_id: z.string().nullable().describe(
    "If this is a variant, the ID of the base plan it was created from.",
  ),
  config: z.lazy(() => GetPlanConfig$zodSchema).describe(
    "Miscellaneous plan-level configuration flags.",
  ),
  created_at: z.number().describe(
    "Unix timestamp (ms) when the plan was created.",
  ),
  customer_eligibility: z.lazy(() => GetPlanCustomerEligibility$zodSchema)
    .optional(),
  description: z.string().nullable().describe(
    "Optional description of the plan.",
  ),
  env: GetPlanEnv$zodSchema.describe(
    "Environment this plan belongs to ('sandbox' or 'live').",
  ),
  free_trial: z.lazy(() => GetPlanFreeTrial$zodSchema).optional().describe(
    "Free trial configuration. If set, new customers can try this plan before being charged.",
  ),
  group: z.string().nullable().describe(
    "Group identifier for organizing related plans. Plans in the same group are mutually exclusive.",
  ),
  id: z.string().describe("Unique identifier for the plan."),
  items: z.array(z.lazy(() => GetPlanItem$zodSchema)).describe(
    "Feature configurations included in this plan. Each item defines included units, pricing, and reset behavior for a feature.",
  ),
  name: z.string().describe("Display name of the plan."),
  price: z.lazy(() => GetPlanPrice$zodSchema).nullable().describe(
    "Base recurring price for the plan. Null for free plans or usage-only plans.",
  ),
  version: z.number().describe(
    "Version number of the plan. Incremented when plan configuration changes.",
  ),
}).describe(
  "A plan defines a set of features, pricing, and entitlements that can be attached to customers.",
);
