/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { ClosedEnum } from "../types/enums.js";

export type ListPlansGlobals = { xApiVersion?: string | undefined };

export const ListPlansGlobals$zodSchema: z.ZodType<ListPlansGlobals> = z.object(
  {
    xApiVersion: z.string().default("2.3.0"),
  },
);

export type ListPlansParams = {
  customer_id?: string | undefined;
  entity_id?: string | undefined;
  include_archived?: boolean | undefined;
};

export const ListPlansParams$zodSchema: z.ZodType<ListPlansParams> = z.object({
  customer_id: z.string().optional().describe(
    "Customer ID to include eligibility info (trial availability, attach scenario).",
  ),
  entity_id: z.string().optional().describe(
    "Entity ID for entity-scoped plans.",
  ),
  include_archived: z.boolean().optional().describe(
    "If true, includes archived plans in the response.",
  ),
});

/**
 * Billing interval (e.g. 'month', 'year').
 */
export const ListPlansPriceInterval = {
  OneOff: "one_off",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
/**
 * Billing interval (e.g. 'month', 'year').
 */
export type ListPlansPriceInterval = ClosedEnum<typeof ListPlansPriceInterval>;

export const ListPlansPriceInterval$zodSchema = z.enum([
  "one_off",
  "week",
  "month",
  "quarter",
  "semi_annual",
  "year",
]).describe("Billing interval (e.g. 'month', 'year').");

/**
 * Display text for showing this price in pricing pages.
 */
export type ListPlansPriceDisplay = {
  primary_text: string;
  secondary_text?: string | undefined;
};

export const ListPlansPriceDisplay$zodSchema: z.ZodType<ListPlansPriceDisplay> =
  z.object({
    primary_text: z.string().describe(
      "Main display text (e.g. '$10' or '100 messages').",
    ),
    secondary_text: z.string().optional().describe(
      "Secondary display text (e.g. 'per month' or 'then $0.5 per 100').",
    ),
  }).describe("Display text for showing this price in pricing pages.");

export type ListPlansPrice = {
  amount: number;
  interval: ListPlansPriceInterval;
  interval_count?: number | undefined;
  display?: ListPlansPriceDisplay | undefined;
};

export const ListPlansPrice$zodSchema: z.ZodType<ListPlansPrice> = z.object({
  amount: z.number().describe("Base price amount for the plan."),
  display: z.lazy(() => ListPlansPriceDisplay$zodSchema).optional().describe(
    "Display text for showing this price in pricing pages.",
  ),
  interval: ListPlansPriceInterval$zodSchema.describe(
    "Billing interval (e.g. 'month', 'year').",
  ),
  interval_count: z.number().optional().describe(
    "Number of intervals per billing cycle. Defaults to 1.",
  ),
});

/**
 * The type of the feature
 */
export const ListPlansType = {
  Static: "static",
  Boolean: "boolean",
  SingleUse: "single_use",
  ContinuousUse: "continuous_use",
  CreditSystem: "credit_system",
} as const;
/**
 * The type of the feature
 */
export type ListPlansType = ClosedEnum<typeof ListPlansType>;

export const ListPlansType$zodSchema = z.enum([
  "static",
  "boolean",
  "single_use",
  "continuous_use",
  "credit_system",
]).describe("The type of the feature");

export type ListPlansFeatureDisplay = { singular: string; plural: string };

export const ListPlansFeatureDisplay$zodSchema: z.ZodType<
  ListPlansFeatureDisplay
> = z.object({
  plural: z.string().describe("The plural display name for the feature."),
  singular: z.string().describe("The singular display name for the feature."),
});

export type ListPlansCreditSchema = {
  metered_feature_id: string;
  credit_cost: number;
};

export const ListPlansCreditSchema$zodSchema: z.ZodType<ListPlansCreditSchema> =
  z.object({
    credit_cost: z.number().describe("The credit cost of the metered feature."),
    metered_feature_id: z.string().describe(
      "The ID of the metered feature (should be a single_use feature).",
    ),
  });

/**
 * The full feature object if expanded.
 */
export type ListPlansFeature = {
  id: string;
  name?: string | null | undefined;
  type: ListPlansType;
  display?: ListPlansFeatureDisplay | null | undefined;
  credit_schema?: Array<ListPlansCreditSchema> | null | undefined;
  archived?: boolean | null | undefined;
};

export const ListPlansFeature$zodSchema: z.ZodType<ListPlansFeature> = z.object(
  {
    archived: z.boolean().nullable().optional().describe(
      "Whether or not the feature is archived.",
    ),
    credit_schema: z.array(z.lazy(() => ListPlansCreditSchema$zodSchema))
      .nullable().optional().describe(
        "Credit cost schema for credit system features.",
      ),
    display: z.lazy(() => ListPlansFeatureDisplay$zodSchema).nullable()
      .optional().describe(
        "Singular and plural display names for the feature.",
      ),
    id: z.string().describe(
      "The ID of the feature, used to refer to it in other API calls like /track or /check.",
    ),
    name: z.string().nullable().optional().describe("The name of the feature."),
    type: ListPlansType$zodSchema.describe("The type of the feature"),
  },
).describe("The full feature object if expanded.");

/**
 * The interval at which the feature balance resets (e.g. 'month', 'year'). For consumable features, usage resets to 0 and included units are restored.
 */
export const ListPlansResetInterval = {
  OneOff: "one_off",
  Minute: "minute",
  Hour: "hour",
  Day: "day",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
/**
 * The interval at which the feature balance resets (e.g. 'month', 'year'). For consumable features, usage resets to 0 and included units are restored.
 */
export type ListPlansResetInterval = ClosedEnum<typeof ListPlansResetInterval>;

export const ListPlansResetInterval$zodSchema = z.enum([
  "one_off",
  "minute",
  "hour",
  "day",
  "week",
  "month",
  "quarter",
  "semi_annual",
  "year",
]).describe(
  "The interval at which the feature balance resets (e.g. 'month', 'year'). For consumable features, usage resets to 0 and included units are restored.",
);

export type ListPlansReset = {
  interval: ListPlansResetInterval;
  interval_count?: number | undefined;
};

export const ListPlansReset$zodSchema: z.ZodType<ListPlansReset> = z.object({
  interval: ListPlansResetInterval$zodSchema.describe(
    "The interval at which the feature balance resets (e.g. 'month', 'year'). For consumable features, usage resets to 0 and included units are restored.",
  ),
  interval_count: z.number().optional().describe(
    "Number of intervals between resets. Defaults to 1.",
  ),
});

export const ListPlansTierBehavior = {
  Graduated: "graduated",
  Volume: "volume",
} as const;
export type ListPlansTierBehavior = ClosedEnum<typeof ListPlansTierBehavior>;

export const ListPlansTierBehavior$zodSchema = z.enum([
  "graduated",
  "volume",
]);

/**
 * Billing interval for this price. For consumable features, should match reset.interval.
 */
export const ListPlansPriceItemInterval = {
  OneOff: "one_off",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
/**
 * Billing interval for this price. For consumable features, should match reset.interval.
 */
export type ListPlansPriceItemInterval = ClosedEnum<
  typeof ListPlansPriceItemInterval
>;

export const ListPlansPriceItemInterval$zodSchema = z.enum([
  "one_off",
  "week",
  "month",
  "quarter",
  "semi_annual",
  "year",
]).describe(
  "Billing interval for this price. For consumable features, should match reset.interval.",
);

/**
 * 'prepaid' for features like seats where customers pay upfront, 'usage_based' for pay-as-you-go after included usage.
 */
export const ListPlansBillingMethod = {
  Prepaid: "prepaid",
  UsageBased: "usage_based",
} as const;
/**
 * 'prepaid' for features like seats where customers pay upfront, 'usage_based' for pay-as-you-go after included usage.
 */
export type ListPlansBillingMethod = ClosedEnum<typeof ListPlansBillingMethod>;

export const ListPlansBillingMethod$zodSchema = z.enum([
  "prepaid",
  "usage_based",
]).describe(
  "'prepaid' for features like seats where customers pay upfront, 'usage_based' for pay-as-you-go after included usage.",
);

export type ListPlansItemPrice = {
  amount?: number | undefined;
  tiers?: Array<any | null> | undefined;
  tier_behavior?: ListPlansTierBehavior | undefined;
  interval: ListPlansPriceItemInterval;
  interval_count?: number | undefined;
  billing_units: number;
  billing_method: ListPlansBillingMethod;
  max_purchase: number | null;
};

export const ListPlansItemPrice$zodSchema: z.ZodType<ListPlansItemPrice> = z
  .object({
    amount: z.number().optional().describe(
      "Price per billing_units after included usage is consumed. Mutually exclusive with tiers.",
    ),
    billing_method: ListPlansBillingMethod$zodSchema.describe(
      "'prepaid' for features like seats where customers pay upfront, 'usage_based' for pay-as-you-go after included usage.",
    ),
    billing_units: z.number().describe(
      "Number of units per price increment. Usage is rounded UP to the nearest billing_units when billed (e.g. billing_units=100 means 101 usage rounds to 200).",
    ),
    interval: ListPlansPriceItemInterval$zodSchema.describe(
      "Billing interval for this price. For consumable features, should match reset.interval.",
    ),
    interval_count: z.number().optional().describe(
      "Number of intervals per billing cycle. Defaults to 1.",
    ),
    max_purchase: z.number().nullable().describe(
      "Maximum units a customer can purchase beyond included. E.g. if included=100 and max_purchase=300, customer can use up to 400 total before usage is capped. Null for no limit.",
    ),
    tier_behavior: ListPlansTierBehavior$zodSchema.optional(),
    tiers: z.array(z.any().nullable()).optional().describe(
      "Tiered pricing configuration. Each tier's 'to' INCLUDES the included amount. Either 'tiers' or 'amount' is required.",
    ),
  });

/**
 * Display text for showing this item in pricing pages.
 */
export type ListPlansItemDisplay = {
  primary_text: string;
  secondary_text?: string | undefined;
};

export const ListPlansItemDisplay$zodSchema: z.ZodType<ListPlansItemDisplay> = z
  .object({
    primary_text: z.string().describe(
      "Main display text (e.g. '$10' or '100 messages').",
    ),
    secondary_text: z.string().optional().describe(
      "Secondary display text (e.g. 'per month' or 'then $0.5 per 100').",
    ),
  }).describe("Display text for showing this item in pricing pages.");

/**
 * When rolled over units expire.
 */
export const ListPlansExpiryDurationType = {
  Month: "month",
  Forever: "forever",
} as const;
/**
 * When rolled over units expire.
 */
export type ListPlansExpiryDurationType = ClosedEnum<
  typeof ListPlansExpiryDurationType
>;

export const ListPlansExpiryDurationType$zodSchema = z.enum([
  "month",
  "forever",
]).describe("When rolled over units expire.");

/**
 * Rollover configuration for unused units. If set, unused included units roll over to the next period.
 */
export type ListPlansRollover = {
  max: number | null;
  max_percentage?: number | null | undefined;
  expiry_duration_type: ListPlansExpiryDurationType;
  expiry_duration_length?: number | undefined;
};

export const ListPlansRollover$zodSchema: z.ZodType<ListPlansRollover> = z
  .object({
    expiry_duration_length: z.number().optional().describe(
      "Number of periods before expiry.",
    ),
    expiry_duration_type: ListPlansExpiryDurationType$zodSchema.describe(
      "When rolled over units expire.",
    ),
    max: z.number().nullable().describe(
      "Maximum rollover units. Null for unlimited rollover.",
    ),
    max_percentage: z.number().nullable().optional().describe(
      "Maximum rollover as a percentage (0-100) of included + prepaid grant. Mutually exclusive with max.",
    ),
  }).describe(
    "Rollover configuration for unused units. If set, unused included units roll over to the next period.",
  );

export type ListPlansItem = {
  feature_id: string;
  feature?: ListPlansFeature | undefined;
  included: number;
  unlimited: boolean;
  reset: ListPlansReset | null;
  price: ListPlansItemPrice | null;
  display?: ListPlansItemDisplay | undefined;
  rollover?: ListPlansRollover | undefined;
};

export const ListPlansItem$zodSchema: z.ZodType<ListPlansItem> = z.object({
  display: z.lazy(() => ListPlansItemDisplay$zodSchema).optional().describe(
    "Display text for showing this item in pricing pages.",
  ),
  feature: z.lazy(() => ListPlansFeature$zodSchema).optional().describe(
    "The full feature object if expanded.",
  ),
  feature_id: z.string().describe(
    "The ID of the feature this item configures.",
  ),
  included: z.number().describe(
    "Number of free units included. For consumable features, balance resets to this number each interval.",
  ),
  price: z.lazy(() => ListPlansItemPrice$zodSchema).nullable().describe(
    "Pricing configuration for usage beyond included units. Null if feature is entirely free.",
  ),
  reset: z.lazy(() => ListPlansReset$zodSchema).nullable().describe(
    "Reset configuration for consumable features. Null for non-consumable features like seats where usage persists across billing cycles.",
  ),
  rollover: z.lazy(() => ListPlansRollover$zodSchema).optional().describe(
    "Rollover configuration for unused units. If set, unused included units roll over to the next period.",
  ),
  unlimited: z.boolean().describe(
    "Whether the customer has unlimited access to this feature.",
  ),
});

/**
 * Unit of time for the trial duration ('day', 'month', 'year').
 */
export const ListPlansDurationType = {
  Day: "day",
  Month: "month",
  Year: "year",
} as const;
/**
 * Unit of time for the trial duration ('day', 'month', 'year').
 */
export type ListPlansDurationType = ClosedEnum<typeof ListPlansDurationType>;

export const ListPlansDurationType$zodSchema = z.enum([
  "day",
  "month",
  "year",
]).describe("Unit of time for the trial duration ('day', 'month', 'year').");

export const ListPlansOnEnd = {
  Bill: "bill",
  Revert: "revert",
} as const;
export type ListPlansOnEnd = ClosedEnum<typeof ListPlansOnEnd>;

export const ListPlansOnEnd$zodSchema = z.enum([
  "bill",
  "revert",
]);

/**
 * Free trial configuration. If set, new customers can try this plan before being charged.
 */
export type ListPlansFreeTrial = {
  duration_length: number;
  duration_type: ListPlansDurationType;
  card_required: boolean;
  on_end?: ListPlansOnEnd | null | undefined;
};

export const ListPlansFreeTrial$zodSchema: z.ZodType<ListPlansFreeTrial> = z
  .object({
    card_required: z.boolean().describe(
      "Whether a payment method is required to start the trial. If true, customer will be charged after trial ends.",
    ),
    duration_length: z.number().describe(
      "Number of duration_type periods the trial lasts.",
    ),
    duration_type: ListPlansDurationType$zodSchema.describe(
      "Unit of time for the trial duration ('day', 'month', 'year').",
    ),
    on_end: ListPlansOnEnd$zodSchema.nullable().optional().describe(
      "Behavior when the trial ends. 'bill' charges the customer (default). 'revert' expires the trial and restores the customer's previous plan.",
    ),
  }).describe(
    "Free trial configuration. If set, new customers can try this plan before being charged.",
  );

/**
 * Environment this plan belongs to ('sandbox' or 'live').
 */
export const ListPlansEnv = {
  Sandbox: "sandbox",
  Live: "live",
} as const;
/**
 * Environment this plan belongs to ('sandbox' or 'live').
 */
export type ListPlansEnv = ClosedEnum<typeof ListPlansEnv>;

export const ListPlansEnv$zodSchema = z.enum([
  "sandbox",
  "live",
]).describe("Environment this plan belongs to ('sandbox' or 'live').");

/**
 * Miscellaneous plan-level configuration flags.
 */
export type ListPlansConfig = { ignore_past_due?: boolean | undefined };

export const ListPlansConfig$zodSchema: z.ZodType<ListPlansConfig> = z.object({
  ignore_past_due: z.boolean().default(false).describe(
    "If true, entitlements attached to this plan will still reset on schedule even when the customer's product is in a past_due state.",
  ),
}).describe("Miscellaneous plan-level configuration flags.");

/**
 * The customer's current status with this plan. 'active' if attached, 'scheduled' if pending activation.
 */
export const ListPlansStatus = {
  Active: "active",
  Scheduled: "scheduled",
} as const;
/**
 * The customer's current status with this plan. 'active' if attached, 'scheduled' if pending activation.
 */
export type ListPlansStatus = ClosedEnum<typeof ListPlansStatus>;

export const ListPlansStatus$zodSchema = z.enum([
  "active",
  "scheduled",
]).describe(
  "The customer's current status with this plan. 'active' if attached, 'scheduled' if pending activation.",
);

/**
 * The action that would occur if this plan were attached to the customer.
 */
export const ListPlansAttachAction = {
  Activate: "activate",
  Upgrade: "upgrade",
  Downgrade: "downgrade",
  None: "none",
  Purchase: "purchase",
} as const;
/**
 * The action that would occur if this plan were attached to the customer.
 */
export type ListPlansAttachAction = ClosedEnum<typeof ListPlansAttachAction>;

export const ListPlansAttachAction$zodSchema = z.enum([
  "activate",
  "upgrade",
  "downgrade",
  "none",
  "purchase",
]).describe(
  "The action that would occur if this plan were attached to the customer.",
);

export type ListPlansCustomerEligibility = {
  trial_available?: boolean | undefined;
  status?: ListPlansStatus | undefined;
  canceling?: boolean | undefined;
  trialing?: boolean | undefined;
  attach_action: ListPlansAttachAction;
};

export const ListPlansCustomerEligibility$zodSchema: z.ZodType<
  ListPlansCustomerEligibility
> = z.object({
  attach_action: ListPlansAttachAction$zodSchema.describe(
    "The action that would occur if this plan were attached to the customer.",
  ),
  canceling: z.boolean().optional().describe(
    "Whether the customer's active instance of this plan is set to cancel.",
  ),
  status: ListPlansStatus$zodSchema.optional().describe(
    "The customer's current status with this plan. 'active' if attached, 'scheduled' if pending activation.",
  ),
  trial_available: z.boolean().optional().describe(
    "Whether the trial on this plan is available to this customer. For example, if the customer used the trial in the past, this will be false.",
  ),
  trialing: z.boolean().optional().describe(
    "Whether the customer is currently on a free trial of this plan.",
  ),
});

/**
 * A plan defines a set of features, pricing, and entitlements that can be attached to customers.
 */
export type ListPlansList = {
  id: string;
  name: string;
  description: string | null;
  group: string | null;
  version: number;
  add_on: boolean;
  auto_enable: boolean;
  price: ListPlansPrice | null;
  items: Array<ListPlansItem>;
  free_trial?: ListPlansFreeTrial | undefined;
  created_at: number;
  env: ListPlansEnv;
  archived: boolean;
  base_variant_id: string | null;
  config: ListPlansConfig;
  customer_eligibility?: ListPlansCustomerEligibility | undefined;
};

export const ListPlansList$zodSchema: z.ZodType<ListPlansList> = z.object({
  add_on: z.boolean().describe(
    "Whether this is an add-on plan that can be attached alongside a main plan.",
  ),
  archived: z.boolean().describe(
    "Whether the plan is archived. Archived plans cannot be attached to new customers.",
  ),
  auto_enable: z.boolean().describe(
    "If true, this plan is automatically attached when a customer is created. Used for free plans.",
  ),
  base_variant_id: z.string().nullable().describe(
    "If this is a variant, the ID of the base plan it was created from.",
  ),
  config: z.lazy(() => ListPlansConfig$zodSchema).describe(
    "Miscellaneous plan-level configuration flags.",
  ),
  created_at: z.number().describe(
    "Unix timestamp (ms) when the plan was created.",
  ),
  customer_eligibility: z.lazy(() => ListPlansCustomerEligibility$zodSchema)
    .optional(),
  description: z.string().nullable().describe(
    "Optional description of the plan.",
  ),
  env: ListPlansEnv$zodSchema.describe(
    "Environment this plan belongs to ('sandbox' or 'live').",
  ),
  free_trial: z.lazy(() => ListPlansFreeTrial$zodSchema).optional().describe(
    "Free trial configuration. If set, new customers can try this plan before being charged.",
  ),
  group: z.string().nullable().describe(
    "Group identifier for organizing related plans. Plans in the same group are mutually exclusive.",
  ),
  id: z.string().describe("Unique identifier for the plan."),
  items: z.array(z.lazy(() => ListPlansItem$zodSchema)).describe(
    "Feature configurations included in this plan. Each item defines included units, pricing, and reset behavior for a feature.",
  ),
  name: z.string().describe("Display name of the plan."),
  price: z.lazy(() => ListPlansPrice$zodSchema).nullable().describe(
    "Base recurring price for the plan. Null for free plans or usage-only plans.",
  ),
  version: z.number().describe(
    "Version number of the plan. Incremented when plan configuration changes.",
  ),
}).describe(
  "A plan defines a set of features, pricing, and entitlements that can be attached to customers.",
);

/**
 * OK
 */
export type ListPlansResponse = { list: Array<ListPlansList> };

export const ListPlansResponse$zodSchema: z.ZodType<ListPlansResponse> = z
  .object({
    list: z.array(z.lazy(() => ListPlansList$zodSchema)),
  }).describe("OK");
