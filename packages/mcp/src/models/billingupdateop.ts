/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { ClosedEnum } from "../types/enums.js";

export type BillingUpdateGlobals = { xApiVersion?: string | undefined };

export const BillingUpdateGlobals$zodSchema: z.ZodType<BillingUpdateGlobals> = z
  .object({
    xApiVersion: z.string().default("2.3.0"),
  });

/**
 * Quantity configuration for a prepaid feature.
 */
export type BillingUpdateFeatureQuantity = {
  feature_id: string;
  quantity?: number | undefined;
  adjustable?: boolean | undefined;
};

export const BillingUpdateFeatureQuantity$zodSchema: z.ZodType<
  BillingUpdateFeatureQuantity
> = z.object({
  adjustable: z.boolean().optional().describe(
    "Whether the customer can adjust the quantity.",
  ),
  feature_id: z.string().describe("The ID of the feature to set quantity for."),
  quantity: z.number().optional().describe("The quantity of the feature."),
}).describe("Quantity configuration for a prepaid feature.");

/**
 * Billing interval (e.g. 'month', 'year').
 */
export const BillingUpdatePriceInterval = {
  OneOff: "one_off",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
/**
 * Billing interval (e.g. 'month', 'year').
 */
export type BillingUpdatePriceInterval = ClosedEnum<
  typeof BillingUpdatePriceInterval
>;

export const BillingUpdatePriceInterval$zodSchema = z.enum([
  "one_off",
  "week",
  "month",
  "quarter",
  "semi_annual",
  "year",
]).describe("Billing interval (e.g. 'month', 'year').");

/**
 * Base price configuration for a plan.
 */
export type BillingUpdateBasePrice = {
  amount: number;
  interval: BillingUpdatePriceInterval;
  interval_count?: number | undefined;
};

export const BillingUpdateBasePrice$zodSchema: z.ZodType<
  BillingUpdateBasePrice
> = z.object({
  amount: z.number().describe("Base price amount for the plan."),
  interval: BillingUpdatePriceInterval$zodSchema.describe(
    "Billing interval (e.g. 'month', 'year').",
  ),
  interval_count: z.number().optional().describe(
    "Number of intervals per billing cycle. Defaults to 1.",
  ),
}).describe("Base price configuration for a plan.");

/**
 * Interval at which balance resets (e.g. 'month', 'year'). For consumable features only.
 */
export const BillingUpdateItemResetInterval = {
  OneOff: "one_off",
  Minute: "minute",
  Hour: "hour",
  Day: "day",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
/**
 * Interval at which balance resets (e.g. 'month', 'year'). For consumable features only.
 */
export type BillingUpdateItemResetInterval = ClosedEnum<
  typeof BillingUpdateItemResetInterval
>;

export const BillingUpdateItemResetInterval$zodSchema = z.enum([
  "one_off",
  "minute",
  "hour",
  "day",
  "week",
  "month",
  "quarter",
  "semi_annual",
  "year",
]).describe(
  "Interval at which balance resets (e.g. 'month', 'year'). For consumable features only.",
);

/**
 * Reset configuration for consumable features. Omit for non-consumable features like seats.
 */
export type BillingUpdateItemReset = {
  interval: BillingUpdateItemResetInterval;
  interval_count?: number | undefined;
};

export const BillingUpdateItemReset$zodSchema: z.ZodType<
  BillingUpdateItemReset
> = z.object({
  interval: BillingUpdateItemResetInterval$zodSchema.describe(
    "Interval at which balance resets (e.g. 'month', 'year'). For consumable features only.",
  ),
  interval_count: z.number().optional().describe(
    "Number of intervals between resets. Defaults to 1.",
  ),
}).describe(
  "Reset configuration for consumable features. Omit for non-consumable features like seats.",
);

export type BillingUpdateItemTo = number | string;

export const BillingUpdateItemTo$zodSchema: z.ZodType<BillingUpdateItemTo> = z
  .union([
    z.number(),
    z.string(),
  ]);

export type BillingUpdateItemTier = {
  to: number | string;
  amount?: number | undefined;
  flat_amount?: number | undefined;
};

export const BillingUpdateItemTier$zodSchema: z.ZodType<BillingUpdateItemTier> =
  z.object({
    amount: z.number().optional(),
    flat_amount: z.number().optional(),
    to: z.union([
      z.number(),
      z.string(),
    ]),
  });

export const BillingUpdateItemTierBehavior = {
  Graduated: "graduated",
  Volume: "volume",
} as const;
export type BillingUpdateItemTierBehavior = ClosedEnum<
  typeof BillingUpdateItemTierBehavior
>;

export const BillingUpdateItemTierBehavior$zodSchema = z.enum([
  "graduated",
  "volume",
]);

/**
 * Billing interval. For consumable features, should match reset.interval.
 */
export const BillingUpdateItemPriceInterval = {
  OneOff: "one_off",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
/**
 * Billing interval. For consumable features, should match reset.interval.
 */
export type BillingUpdateItemPriceInterval = ClosedEnum<
  typeof BillingUpdateItemPriceInterval
>;

export const BillingUpdateItemPriceInterval$zodSchema = z.enum([
  "one_off",
  "week",
  "month",
  "quarter",
  "semi_annual",
  "year",
]).describe(
  "Billing interval. For consumable features, should match reset.interval.",
);

/**
 * 'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go.
 */
export const BillingUpdateItemBillingMethod = {
  Prepaid: "prepaid",
  UsageBased: "usage_based",
} as const;
/**
 * 'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go.
 */
export type BillingUpdateItemBillingMethod = ClosedEnum<
  typeof BillingUpdateItemBillingMethod
>;

export const BillingUpdateItemBillingMethod$zodSchema = z.enum([
  "prepaid",
  "usage_based",
]).describe(
  "'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go.",
);

/**
 * Pricing for usage beyond included units. Omit for free features.
 */
export type BillingUpdateItemPrice = {
  amount?: number | undefined;
  tiers?: Array<BillingUpdateItemTier> | undefined;
  tier_behavior?: BillingUpdateItemTierBehavior | undefined;
  interval: BillingUpdateItemPriceInterval;
  interval_count?: number | undefined;
  billing_units?: number | undefined;
  billing_method: BillingUpdateItemBillingMethod;
  max_purchase?: number | undefined;
};

export const BillingUpdateItemPrice$zodSchema: z.ZodType<
  BillingUpdateItemPrice
> = z.object({
  amount: z.number().optional().describe(
    "Price per billing_units after included usage. Either 'amount' or 'tiers' is required.",
  ),
  billing_method: BillingUpdateItemBillingMethod$zodSchema.describe(
    "'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go.",
  ),
  billing_units: z.number().default(1).describe(
    "Units per price increment. Usage is rounded UP when billed (e.g. billing_units=100 means 101 rounds to 200).",
  ),
  interval: BillingUpdateItemPriceInterval$zodSchema.describe(
    "Billing interval. For consumable features, should match reset.interval.",
  ),
  interval_count: z.number().default(1).describe(
    "Number of intervals per billing cycle. Defaults to 1.",
  ),
  max_purchase: z.number().optional().describe(
    "Max units purchasable beyond included. E.g. included=100, max_purchase=300 allows 400 total.",
  ),
  tier_behavior: BillingUpdateItemTierBehavior$zodSchema.optional(),
  tiers: z.array(z.lazy(() => BillingUpdateItemTier$zodSchema)).optional()
    .describe("Tiered pricing.  Either 'amount' or 'tiers' is required."),
}).describe("Pricing for usage beyond included units. Omit for free features.");

/**
 * Billing behavior when quantity increases mid-cycle.
 */
export const BillingUpdateItemOnIncrease = {
  BillImmediately: "bill_immediately",
  ProrateImmediately: "prorate_immediately",
  ProrateNextCycle: "prorate_next_cycle",
  BillNextCycle: "bill_next_cycle",
} as const;
/**
 * Billing behavior when quantity increases mid-cycle.
 */
export type BillingUpdateItemOnIncrease = ClosedEnum<
  typeof BillingUpdateItemOnIncrease
>;

export const BillingUpdateItemOnIncrease$zodSchema = z.enum([
  "bill_immediately",
  "prorate_immediately",
  "prorate_next_cycle",
  "bill_next_cycle",
]).describe("Billing behavior when quantity increases mid-cycle.");

/**
 * Credit behavior when quantity decreases mid-cycle.
 */
export const BillingUpdateItemOnDecrease = {
  Prorate: "prorate",
  ProrateImmediately: "prorate_immediately",
  ProrateNextCycle: "prorate_next_cycle",
  None: "none",
  NoProrations: "no_prorations",
} as const;
/**
 * Credit behavior when quantity decreases mid-cycle.
 */
export type BillingUpdateItemOnDecrease = ClosedEnum<
  typeof BillingUpdateItemOnDecrease
>;

export const BillingUpdateItemOnDecrease$zodSchema = z.enum([
  "prorate",
  "prorate_immediately",
  "prorate_next_cycle",
  "none",
  "no_prorations",
]).describe("Credit behavior when quantity decreases mid-cycle.");

/**
 * Proration settings for prepaid features. Controls mid-cycle quantity change billing.
 */
export type BillingUpdateItemProration = {
  on_increase: BillingUpdateItemOnIncrease;
  on_decrease: BillingUpdateItemOnDecrease;
};

export const BillingUpdateItemProration$zodSchema: z.ZodType<
  BillingUpdateItemProration
> = z.object({
  on_decrease: BillingUpdateItemOnDecrease$zodSchema.describe(
    "Credit behavior when quantity decreases mid-cycle.",
  ),
  on_increase: BillingUpdateItemOnIncrease$zodSchema.describe(
    "Billing behavior when quantity increases mid-cycle.",
  ),
}).describe(
  "Proration settings for prepaid features. Controls mid-cycle quantity change billing.",
);

/**
 * When rolled over units expire.
 */
export const BillingUpdateItemExpiryDurationType = {
  Month: "month",
  Forever: "forever",
} as const;
/**
 * When rolled over units expire.
 */
export type BillingUpdateItemExpiryDurationType = ClosedEnum<
  typeof BillingUpdateItemExpiryDurationType
>;

export const BillingUpdateItemExpiryDurationType$zodSchema = z.enum([
  "month",
  "forever",
]).describe("When rolled over units expire.");

/**
 * Rollover config for unused units. If set, unused included units carry over.
 */
export type BillingUpdateItemRollover = {
  max?: number | undefined;
  max_percentage?: number | undefined;
  expiry_duration_type: BillingUpdateItemExpiryDurationType;
  expiry_duration_length?: number | undefined;
};

export const BillingUpdateItemRollover$zodSchema: z.ZodType<
  BillingUpdateItemRollover
> = z.object({
  expiry_duration_length: z.number().optional().describe(
    "Number of periods before expiry.",
  ),
  expiry_duration_type: BillingUpdateItemExpiryDurationType$zodSchema.describe(
    "When rolled over units expire.",
  ),
  max: z.number().optional().describe(
    "Max rollover units. Omit for unlimited rollover.",
  ),
  max_percentage: z.number().optional().describe(
    "Maximum rollover as a percentage (0-100) of included + prepaid grant. Mutually exclusive with max.",
  ),
}).describe(
  "Rollover config for unused units. If set, unused included units carry over.",
);

/**
 * Configuration for a feature item in a plan, including usage limits, pricing, and rollover settings.
 */
export type BillingUpdateItemPlanItem = {
  feature_id: string;
  included?: number | undefined;
  unlimited?: boolean | undefined;
  reset?: BillingUpdateItemReset | undefined;
  price?: BillingUpdateItemPrice | undefined;
  proration?: BillingUpdateItemProration | undefined;
  rollover?: BillingUpdateItemRollover | undefined;
};

export const BillingUpdateItemPlanItem$zodSchema: z.ZodType<
  BillingUpdateItemPlanItem
> = z.object({
  feature_id: z.string().describe("The ID of the feature to configure."),
  included: z.number().optional().describe(
    "Number of free units included. Balance resets to this each interval for consumable features.",
  ),
  price: z.lazy(() => BillingUpdateItemPrice$zodSchema).optional().describe(
    "Pricing for usage beyond included units. Omit for free features.",
  ),
  proration: z.lazy(() => BillingUpdateItemProration$zodSchema).optional()
    .describe(
      "Proration settings for prepaid features. Controls mid-cycle quantity change billing.",
    ),
  reset: z.lazy(() => BillingUpdateItemReset$zodSchema).optional().describe(
    "Reset configuration for consumable features. Omit for non-consumable features like seats.",
  ),
  rollover: z.lazy(() => BillingUpdateItemRollover$zodSchema).optional()
    .describe(
      "Rollover config for unused units. If set, unused included units carry over.",
    ),
  unlimited: z.boolean().optional().describe(
    "If true, customer has unlimited access to this feature.",
  ),
}).describe(
  "Configuration for a feature item in a plan, including usage limits, pricing, and rollover settings.",
);

/**
 * Interval at which balance resets (e.g. 'month', 'year'). For consumable features only.
 */
export const BillingUpdateAddItemResetInterval = {
  OneOff: "one_off",
  Minute: "minute",
  Hour: "hour",
  Day: "day",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
/**
 * Interval at which balance resets (e.g. 'month', 'year'). For consumable features only.
 */
export type BillingUpdateAddItemResetInterval = ClosedEnum<
  typeof BillingUpdateAddItemResetInterval
>;

export const BillingUpdateAddItemResetInterval$zodSchema = z.enum([
  "one_off",
  "minute",
  "hour",
  "day",
  "week",
  "month",
  "quarter",
  "semi_annual",
  "year",
]).describe(
  "Interval at which balance resets (e.g. 'month', 'year'). For consumable features only.",
);

/**
 * Reset configuration for consumable features. Omit for non-consumable features like seats.
 */
export type BillingUpdateAddItemReset = {
  interval: BillingUpdateAddItemResetInterval;
  interval_count?: number | undefined;
};

export const BillingUpdateAddItemReset$zodSchema: z.ZodType<
  BillingUpdateAddItemReset
> = z.object({
  interval: BillingUpdateAddItemResetInterval$zodSchema.describe(
    "Interval at which balance resets (e.g. 'month', 'year'). For consumable features only.",
  ),
  interval_count: z.number().optional().describe(
    "Number of intervals between resets. Defaults to 1.",
  ),
}).describe(
  "Reset configuration for consumable features. Omit for non-consumable features like seats.",
);

export type BillingUpdateAddItemTo = number | string;

export const BillingUpdateAddItemTo$zodSchema: z.ZodType<
  BillingUpdateAddItemTo
> = z.union([
  z.number(),
  z.string(),
]);

export type BillingUpdateAddItemTier = {
  to: number | string;
  amount?: number | undefined;
  flat_amount?: number | undefined;
};

export const BillingUpdateAddItemTier$zodSchema: z.ZodType<
  BillingUpdateAddItemTier
> = z.object({
  amount: z.number().optional(),
  flat_amount: z.number().optional(),
  to: z.union([
    z.number(),
    z.string(),
  ]),
});

export const BillingUpdateAddItemTierBehavior = {
  Graduated: "graduated",
  Volume: "volume",
} as const;
export type BillingUpdateAddItemTierBehavior = ClosedEnum<
  typeof BillingUpdateAddItemTierBehavior
>;

export const BillingUpdateAddItemTierBehavior$zodSchema = z.enum([
  "graduated",
  "volume",
]);

/**
 * Billing interval. For consumable features, should match reset.interval.
 */
export const BillingUpdateAddItemPriceInterval = {
  OneOff: "one_off",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
/**
 * Billing interval. For consumable features, should match reset.interval.
 */
export type BillingUpdateAddItemPriceInterval = ClosedEnum<
  typeof BillingUpdateAddItemPriceInterval
>;

export const BillingUpdateAddItemPriceInterval$zodSchema = z.enum([
  "one_off",
  "week",
  "month",
  "quarter",
  "semi_annual",
  "year",
]).describe(
  "Billing interval. For consumable features, should match reset.interval.",
);

/**
 * 'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go.
 */
export const BillingUpdateAddItemBillingMethod = {
  Prepaid: "prepaid",
  UsageBased: "usage_based",
} as const;
/**
 * 'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go.
 */
export type BillingUpdateAddItemBillingMethod = ClosedEnum<
  typeof BillingUpdateAddItemBillingMethod
>;

export const BillingUpdateAddItemBillingMethod$zodSchema = z.enum([
  "prepaid",
  "usage_based",
]).describe(
  "'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go.",
);

/**
 * Pricing for usage beyond included units. Omit for free features.
 */
export type BillingUpdateAddItemPrice = {
  amount?: number | undefined;
  tiers?: Array<BillingUpdateAddItemTier> | undefined;
  tier_behavior?: BillingUpdateAddItemTierBehavior | undefined;
  interval: BillingUpdateAddItemPriceInterval;
  interval_count?: number | undefined;
  billing_units?: number | undefined;
  billing_method: BillingUpdateAddItemBillingMethod;
  max_purchase?: number | undefined;
};

export const BillingUpdateAddItemPrice$zodSchema: z.ZodType<
  BillingUpdateAddItemPrice
> = z.object({
  amount: z.number().optional().describe(
    "Price per billing_units after included usage. Either 'amount' or 'tiers' is required.",
  ),
  billing_method: BillingUpdateAddItemBillingMethod$zodSchema.describe(
    "'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go.",
  ),
  billing_units: z.number().default(1).describe(
    "Units per price increment. Usage is rounded UP when billed (e.g. billing_units=100 means 101 rounds to 200).",
  ),
  interval: BillingUpdateAddItemPriceInterval$zodSchema.describe(
    "Billing interval. For consumable features, should match reset.interval.",
  ),
  interval_count: z.number().default(1).describe(
    "Number of intervals per billing cycle. Defaults to 1.",
  ),
  max_purchase: z.number().optional().describe(
    "Max units purchasable beyond included. E.g. included=100, max_purchase=300 allows 400 total.",
  ),
  tier_behavior: BillingUpdateAddItemTierBehavior$zodSchema.optional(),
  tiers: z.array(z.lazy(() => BillingUpdateAddItemTier$zodSchema)).optional()
    .describe("Tiered pricing.  Either 'amount' or 'tiers' is required."),
}).describe("Pricing for usage beyond included units. Omit for free features.");

/**
 * Billing behavior when quantity increases mid-cycle.
 */
export const BillingUpdateAddItemOnIncrease = {
  BillImmediately: "bill_immediately",
  ProrateImmediately: "prorate_immediately",
  ProrateNextCycle: "prorate_next_cycle",
  BillNextCycle: "bill_next_cycle",
} as const;
/**
 * Billing behavior when quantity increases mid-cycle.
 */
export type BillingUpdateAddItemOnIncrease = ClosedEnum<
  typeof BillingUpdateAddItemOnIncrease
>;

export const BillingUpdateAddItemOnIncrease$zodSchema = z.enum([
  "bill_immediately",
  "prorate_immediately",
  "prorate_next_cycle",
  "bill_next_cycle",
]).describe("Billing behavior when quantity increases mid-cycle.");

/**
 * Credit behavior when quantity decreases mid-cycle.
 */
export const BillingUpdateAddItemOnDecrease = {
  Prorate: "prorate",
  ProrateImmediately: "prorate_immediately",
  ProrateNextCycle: "prorate_next_cycle",
  None: "none",
  NoProrations: "no_prorations",
} as const;
/**
 * Credit behavior when quantity decreases mid-cycle.
 */
export type BillingUpdateAddItemOnDecrease = ClosedEnum<
  typeof BillingUpdateAddItemOnDecrease
>;

export const BillingUpdateAddItemOnDecrease$zodSchema = z.enum([
  "prorate",
  "prorate_immediately",
  "prorate_next_cycle",
  "none",
  "no_prorations",
]).describe("Credit behavior when quantity decreases mid-cycle.");

/**
 * Proration settings for prepaid features. Controls mid-cycle quantity change billing.
 */
export type BillingUpdateAddItemProration = {
  on_increase: BillingUpdateAddItemOnIncrease;
  on_decrease: BillingUpdateAddItemOnDecrease;
};

export const BillingUpdateAddItemProration$zodSchema: z.ZodType<
  BillingUpdateAddItemProration
> = z.object({
  on_decrease: BillingUpdateAddItemOnDecrease$zodSchema.describe(
    "Credit behavior when quantity decreases mid-cycle.",
  ),
  on_increase: BillingUpdateAddItemOnIncrease$zodSchema.describe(
    "Billing behavior when quantity increases mid-cycle.",
  ),
}).describe(
  "Proration settings for prepaid features. Controls mid-cycle quantity change billing.",
);

/**
 * When rolled over units expire.
 */
export const BillingUpdateAddItemExpiryDurationType = {
  Month: "month",
  Forever: "forever",
} as const;
/**
 * When rolled over units expire.
 */
export type BillingUpdateAddItemExpiryDurationType = ClosedEnum<
  typeof BillingUpdateAddItemExpiryDurationType
>;

export const BillingUpdateAddItemExpiryDurationType$zodSchema = z.enum([
  "month",
  "forever",
]).describe("When rolled over units expire.");

/**
 * Rollover config for unused units. If set, unused included units carry over.
 */
export type BillingUpdateAddItemRollover = {
  max?: number | undefined;
  max_percentage?: number | undefined;
  expiry_duration_type: BillingUpdateAddItemExpiryDurationType;
  expiry_duration_length?: number | undefined;
};

export const BillingUpdateAddItemRollover$zodSchema: z.ZodType<
  BillingUpdateAddItemRollover
> = z.object({
  expiry_duration_length: z.number().optional().describe(
    "Number of periods before expiry.",
  ),
  expiry_duration_type: BillingUpdateAddItemExpiryDurationType$zodSchema
    .describe("When rolled over units expire."),
  max: z.number().optional().describe(
    "Max rollover units. Omit for unlimited rollover.",
  ),
  max_percentage: z.number().optional().describe(
    "Maximum rollover as a percentage (0-100) of included + prepaid grant. Mutually exclusive with max.",
  ),
}).describe(
  "Rollover config for unused units. If set, unused included units carry over.",
);

/**
 * Configuration for a feature item in a plan, including usage limits, pricing, and rollover settings.
 */
export type BillingUpdateAddItemPlanItem = {
  feature_id: string;
  included?: number | undefined;
  unlimited?: boolean | undefined;
  reset?: BillingUpdateAddItemReset | undefined;
  price?: BillingUpdateAddItemPrice | undefined;
  proration?: BillingUpdateAddItemProration | undefined;
  rollover?: BillingUpdateAddItemRollover | undefined;
};

export const BillingUpdateAddItemPlanItem$zodSchema: z.ZodType<
  BillingUpdateAddItemPlanItem
> = z.object({
  feature_id: z.string().describe("The ID of the feature to configure."),
  included: z.number().optional().describe(
    "Number of free units included. Balance resets to this each interval for consumable features.",
  ),
  price: z.lazy(() => BillingUpdateAddItemPrice$zodSchema).optional().describe(
    "Pricing for usage beyond included units. Omit for free features.",
  ),
  proration: z.lazy(() => BillingUpdateAddItemProration$zodSchema).optional()
    .describe(
      "Proration settings for prepaid features. Controls mid-cycle quantity change billing.",
    ),
  reset: z.lazy(() => BillingUpdateAddItemReset$zodSchema).optional().describe(
    "Reset configuration for consumable features. Omit for non-consumable features like seats.",
  ),
  rollover: z.lazy(() => BillingUpdateAddItemRollover$zodSchema).optional()
    .describe(
      "Rollover config for unused units. If set, unused included units carry over.",
    ),
  unlimited: z.boolean().optional().describe(
    "If true, customer has unlimited access to this feature.",
  ),
}).describe(
  "Configuration for a feature item in a plan, including usage limits, pricing, and rollover settings.",
);

/**
 * Match items with this billing method (prepaid or usage_based).
 */
export const BillingUpdateRemoveItemBillingMethod = {
  Prepaid: "prepaid",
  UsageBased: "usage_based",
} as const;
/**
 * Match items with this billing method (prepaid or usage_based).
 */
export type BillingUpdateRemoveItemBillingMethod = ClosedEnum<
  typeof BillingUpdateRemoveItemBillingMethod
>;

export const BillingUpdateRemoveItemBillingMethod$zodSchema = z.enum([
  "prepaid",
  "usage_based",
]).describe("Match items with this billing method (prepaid or usage_based).");

/**
 * Match items with this interval.
 */
export const BillingUpdateRemoveItemInterval = {
  OneOff: "one_off",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
/**
 * Match items with this interval.
 */
export type BillingUpdateRemoveItemInterval = ClosedEnum<
  typeof BillingUpdateRemoveItemInterval
>;

export const BillingUpdateRemoveItemInterval$zodSchema = z.enum([
  "one_off",
  "week",
  "month",
  "quarter",
  "semi_annual",
  "year",
]).describe("Match items with this interval.");

/**
 * Filter for matching plan items. All provided fields must match (AND).
 */
export type BillingUpdatePlanItemFilter = {
  feature_id?: string | undefined;
  billing_method?: BillingUpdateRemoveItemBillingMethod | undefined;
  interval?: BillingUpdateRemoveItemInterval | undefined;
};

export const BillingUpdatePlanItemFilter$zodSchema: z.ZodType<
  BillingUpdatePlanItemFilter
> = z.object({
  billing_method: BillingUpdateRemoveItemBillingMethod$zodSchema.optional()
    .describe("Match items with this billing method (prepaid or usage_based)."),
  feature_id: z.string().optional().describe(
    "Match items linked to this feature.",
  ),
  interval: BillingUpdateRemoveItemInterval$zodSchema.optional().describe(
    "Match items with this interval.",
  ),
}).describe(
  "Filter for matching plan items. All provided fields must match (AND).",
);

/**
 * Unit of time for the trial ('day', 'month', 'year').
 */
export const BillingUpdateDurationType = {
  Day: "day",
  Month: "month",
  Year: "year",
} as const;
/**
 * Unit of time for the trial ('day', 'month', 'year').
 */
export type BillingUpdateDurationType = ClosedEnum<
  typeof BillingUpdateDurationType
>;

export const BillingUpdateDurationType$zodSchema = z.enum([
  "day",
  "month",
  "year",
]).describe("Unit of time for the trial ('day', 'month', 'year').");

/**
 * Behavior when the trial ends. 'bill' charges the customer (default). 'revert' expires the trial and restores the customer's previous plan.
 */
export const BillingUpdateOnEnd = {
  Bill: "bill",
  Revert: "revert",
} as const;
/**
 * Behavior when the trial ends. 'bill' charges the customer (default). 'revert' expires the trial and restores the customer's previous plan.
 */
export type BillingUpdateOnEnd = ClosedEnum<typeof BillingUpdateOnEnd>;

export const BillingUpdateOnEnd$zodSchema = z.enum([
  "bill",
  "revert",
]).describe(
  "Behavior when the trial ends. 'bill' charges the customer (default). 'revert' expires the trial and restores the customer's previous plan.",
);

/**
 * Free trial configuration for a plan.
 */
export type BillingUpdateFreeTrialParams = {
  duration_length: number;
  duration_type?: BillingUpdateDurationType | undefined;
  card_required?: boolean | undefined;
  on_end?: BillingUpdateOnEnd | undefined;
};

export const BillingUpdateFreeTrialParams$zodSchema: z.ZodType<
  BillingUpdateFreeTrialParams
> = z.object({
  card_required: z.boolean().default(true).describe(
    "If true, payment method required to start trial. Customer is charged after trial ends.",
  ),
  duration_length: z.number().describe(
    "Number of duration_type periods the trial lasts.",
  ),
  duration_type: BillingUpdateDurationType$zodSchema.default("month").describe(
    "Unit of time for the trial ('day', 'month', 'year').",
  ),
  on_end: BillingUpdateOnEnd$zodSchema.optional().describe(
    "Behavior when the trial ends. 'bill' charges the customer (default). 'revert' expires the trial and restores the customer's previous plan.",
  ),
}).describe("Free trial configuration for a plan.");

/**
 * Customize the plan to attach. Can override the price, items, free trial, or a combination.
 */
export type BillingUpdateCustomize = {
  price?: BillingUpdateBasePrice | null | undefined;
  items?: Array<BillingUpdateItemPlanItem> | undefined;
  add_items?: Array<BillingUpdateAddItemPlanItem> | undefined;
  remove_items?: Array<BillingUpdatePlanItemFilter> | undefined;
  free_trial?: BillingUpdateFreeTrialParams | null | undefined;
};

export const BillingUpdateCustomize$zodSchema: z.ZodType<
  BillingUpdateCustomize
> = z.object({
  add_items: z.array(z.lazy(() => BillingUpdateAddItemPlanItem$zodSchema))
    .optional().describe("Items to add to the plan."),
  free_trial: z.lazy(() => BillingUpdateFreeTrialParams$zodSchema).nullable()
    .optional().describe(
      "Override the plan's default free trial. Pass an object to set a custom trial, or null to remove the trial entirely.",
    ),
  items: z.array(z.lazy(() => BillingUpdateItemPlanItem$zodSchema)).optional()
    .describe(
      "Override the items in the plan (PUT-style — replaces all existing items). Mutually exclusive with add_items / remove_items.",
    ),
  price: z.lazy(() => BillingUpdateBasePrice$zodSchema).nullable().optional()
    .describe(
      "Override the base price of the plan. Pass null to remove the base price.",
    ),
  remove_items: z.array(z.lazy(() => BillingUpdatePlanItemFilter$zodSchema))
    .optional().describe("Filters selecting items to remove from the plan."),
}).describe(
  "Customize the plan to attach. Can override the price, items, free trial, or a combination.",
);

/**
 * Invoice mode creates a draft or open invoice and sends it to the customer, instead of charging their card immediately. This uses Stripe's send_invoice collection method.
 */
export type BillingUpdateInvoiceMode = {
  enabled: boolean;
  enable_plan_immediately?: boolean | undefined;
  finalize?: boolean | undefined;
};

export const BillingUpdateInvoiceMode$zodSchema: z.ZodType<
  BillingUpdateInvoiceMode
> = z.object({
  enable_plan_immediately: z.boolean().default(false).describe(
    "If true, enables the plan immediately even though the invoice is not paid yet.",
  ),
  enabled: z.boolean().describe(
    "When true, creates an invoice and sends it to the customer instead of charging their card immediately. Uses Stripe's send_invoice collection method.",
  ),
  finalize: z.boolean().default(true).describe(
    "If true, finalizes the invoice so it can be sent to the customer. If false, keeps it as a draft for manual review.",
  ),
}).describe(
  "Invoice mode creates a draft or open invoice and sends it to the customer, instead of charging their card immediately. This uses Stripe's send_invoice collection method.",
);

/**
 * How to handle proration when updating an existing subscription. 'prorate_immediately' charges/credits prorated amounts now, 'none' skips creating any charges.
 */
export const BillingUpdateProrationBehavior = {
  ProrateImmediately: "prorate_immediately",
  None: "none",
} as const;
/**
 * How to handle proration when updating an existing subscription. 'prorate_immediately' charges/credits prorated amounts now, 'none' skips creating any charges.
 */
export type BillingUpdateProrationBehavior = ClosedEnum<
  typeof BillingUpdateProrationBehavior
>;

export const BillingUpdateProrationBehavior$zodSchema = z.enum([
  "prorate_immediately",
  "none",
]).describe(
  "How to handle proration when updating an existing subscription. 'prorate_immediately' charges/credits prorated amounts now, 'none' skips creating any charges.",
);

/**
 * Controls when to return a checkout URL. 'always' returns a URL even if payment succeeds, 'if_required' only when payment action is needed, 'never' disables redirects.
 */
export const BillingUpdateRedirectMode = {
  Always: "always",
  IfRequired: "if_required",
  Never: "never",
} as const;
/**
 * Controls when to return a checkout URL. 'always' returns a URL even if payment succeeds, 'if_required' only when payment action is needed, 'never' disables redirects.
 */
export type BillingUpdateRedirectMode = ClosedEnum<
  typeof BillingUpdateRedirectMode
>;

export const BillingUpdateRedirectMode$zodSchema = z.enum([
  "always",
  "if_required",
  "never",
]).describe(
  "Controls when to return a checkout URL. 'always' returns a URL even if payment succeeds, 'if_required' only when payment action is needed, 'never' disables redirects.",
);

/**
 * A discount to apply. Can be either a reward ID or a promotion code.
 */
export type BillingUpdateAttachDiscount = {
  reward_id?: string | undefined;
  promotion_code?: string | undefined;
};

export const BillingUpdateAttachDiscount$zodSchema: z.ZodType<
  BillingUpdateAttachDiscount
> = z.object({
  promotion_code: z.string().optional().describe(
    "The promotion code to apply as a discount.",
  ),
  reward_id: z.string().optional().describe(
    "The ID of the reward to apply as a discount.",
  ),
}).describe(
  "A discount to apply. Can be either a reward ID or a promotion code.",
);

/**
 * Action to perform for cancellation. 'cancel_immediately' cancels now with prorated refund, 'cancel_end_of_cycle' cancels at period end, 'uncancel' reverses a pending cancellation.
 */
export const BillingUpdateCancelAction = {
  CancelImmediately: "cancel_immediately",
  CancelEndOfCycle: "cancel_end_of_cycle",
  Uncancel: "uncancel",
} as const;
/**
 * Action to perform for cancellation. 'cancel_immediately' cancels now with prorated refund, 'cancel_end_of_cycle' cancels at period end, 'uncancel' reverses a pending cancellation.
 */
export type BillingUpdateCancelAction = ClosedEnum<
  typeof BillingUpdateCancelAction
>;

export const BillingUpdateCancelAction$zodSchema = z.enum([
  "cancel_immediately",
  "cancel_end_of_cycle",
  "uncancel",
]).describe(
  "Action to perform for cancellation. 'cancel_immediately' cancels now with prorated refund, 'cancel_end_of_cycle' cancels at period end, 'uncancel' reverses a pending cancellation.",
);

/**
 * Controls whether balances should be recalculated during the subscription update.
 */
export type BillingUpdateRecalculateBalances = { enabled: boolean };

export const BillingUpdateRecalculateBalances$zodSchema: z.ZodType<
  BillingUpdateRecalculateBalances
> = z.object({
  enabled: z.boolean().describe(
    "If true, recalculates balances during the subscription update. Only applicable when updating feature quantities.",
  ),
}).describe(
  "Controls whether balances should be recalculated during the subscription update.",
);

export type UpdateSubscriptionParams = {
  customer_id: string;
  entity_id?: string | undefined;
  plan_id?: string | undefined;
  feature_quantities?: Array<BillingUpdateFeatureQuantity> | undefined;
  version?: number | undefined;
  customize?: BillingUpdateCustomize | undefined;
  invoice_mode?: BillingUpdateInvoiceMode | undefined;
  proration_behavior?: BillingUpdateProrationBehavior | undefined;
  redirect_mode?: BillingUpdateRedirectMode | undefined;
  subscription_id?: string | undefined;
  discounts?: Array<BillingUpdateAttachDiscount> | undefined;
  cancel_action?: BillingUpdateCancelAction | undefined;
  billing_cycle_anchor?: "now" | undefined;
  no_billing_changes?: boolean | undefined;
  recalculate_balances?: BillingUpdateRecalculateBalances | undefined;
};

export const UpdateSubscriptionParams$zodSchema: z.ZodType<
  UpdateSubscriptionParams
> = z.object({
  billing_cycle_anchor: z.literal("now").default("now").optional().describe(
    "Reset the billing cycle anchor immediately with 'now'",
  ),
  cancel_action: BillingUpdateCancelAction$zodSchema.optional().describe(
    "Action to perform for cancellation. 'cancel_immediately' cancels now with prorated refund, 'cancel_end_of_cycle' cancels at period end, 'uncancel' reverses a pending cancellation.",
  ),
  customer_id: z.string().describe(
    "The ID of the customer to attach the plan to.",
  ),
  customize: z.lazy(() => BillingUpdateCustomize$zodSchema).optional().describe(
    "Customize the plan to attach. Can override the price, items, free trial, or a combination.",
  ),
  discounts: z.array(z.lazy(() => BillingUpdateAttachDiscount$zodSchema))
    .optional().describe(
      "List of discounts to apply. Each discount can be an Autumn reward ID, Stripe coupon ID, or Stripe promotion code.",
    ),
  entity_id: z.string().optional().describe(
    "The ID of the entity to attach the plan to.",
  ),
  feature_quantities: z.array(
    z.lazy(() => BillingUpdateFeatureQuantity$zodSchema),
  ).optional().describe(
    "If this plan contains prepaid features, use this field to specify the quantity of each prepaid feature. This quantity includes the included amount and billing units defined when setting up the plan.",
  ),
  invoice_mode: z.lazy(() => BillingUpdateInvoiceMode$zodSchema).optional()
    .describe(
      "Invoice mode creates a draft or open invoice and sends it to the customer, instead of charging their card immediately. This uses Stripe's send_invoice collection method.",
    ),
  no_billing_changes: z.boolean().optional().describe(
    "If true, the subscription is updated internally without applying billing changes in Stripe.",
  ),
  plan_id: z.string().optional().describe(
    "The ID of the plan to update. Optional if subscription_id is provided, or if the customer has only one product.",
  ),
  proration_behavior: BillingUpdateProrationBehavior$zodSchema.optional()
    .describe(
      "How to handle proration when updating an existing subscription. 'prorate_immediately' charges/credits prorated amounts now, 'none' skips creating any charges.",
    ),
  recalculate_balances: z.lazy(() => BillingUpdateRecalculateBalances$zodSchema)
    .optional().describe(
      "Controls whether balances should be recalculated during the subscription update.",
    ),
  redirect_mode: BillingUpdateRedirectMode$zodSchema.default("if_required")
    .describe(
      "Controls when to return a checkout URL. 'always' returns a URL even if payment succeeds, 'if_required' only when payment action is needed, 'never' disables redirects.",
    ),
  subscription_id: z.string().optional().describe(
    "A unique ID to identify this subscription. Can be used to target specific subscriptions in update operations when a customer has multiple products with the same plan.",
  ),
  version: z.number().optional().describe("The version of the plan to attach."),
});

/**
 * Invoice details if an invoice was created. Only present when a charge was made.
 */
export type BillingUpdateInvoice = {
  status: string | null;
  stripe_id: string;
  total: number;
  currency: string;
  hosted_invoice_url: string | null;
};

export const BillingUpdateInvoice$zodSchema: z.ZodType<BillingUpdateInvoice> = z
  .object({
    currency: z.string().describe(
      "The three-letter ISO currency code (e.g., 'usd').",
    ),
    hosted_invoice_url: z.string().nullable().describe(
      "URL to the hosted invoice page where the customer can view and pay the invoice.",
    ),
    status: z.string().nullable().describe(
      "The status of the invoice (e.g., 'paid', 'open', 'draft').",
    ),
    stripe_id: z.string().describe("The Stripe invoice ID."),
    total: z.number().describe("The total amount of the invoice in cents."),
  }).describe(
    "Invoice details if an invoice was created. Only present when a charge was made.",
  );

/**
 * The type of action required to complete the payment.
 */
export const BillingUpdateCode = {
  ThreedsRequired: "3ds_required",
  PaymentMethodRequired: "payment_method_required",
  PaymentFailed: "payment_failed",
} as const;
/**
 * The type of action required to complete the payment.
 */
export type BillingUpdateCode = ClosedEnum<typeof BillingUpdateCode>;

export const BillingUpdateCode$zodSchema = z.enum([
  "3ds_required",
  "payment_method_required",
  "payment_failed",
]).describe("The type of action required to complete the payment.");

/**
 * Details about any action required to complete the payment. Present when the payment could not be processed automatically.
 */
export type BillingUpdateRequiredAction = {
  code: BillingUpdateCode;
  reason: string;
};

export const BillingUpdateRequiredAction$zodSchema: z.ZodType<
  BillingUpdateRequiredAction
> = z.object({
  code: BillingUpdateCode$zodSchema.describe(
    "The type of action required to complete the payment.",
  ),
  reason: z.string().describe(
    "A human-readable explanation of why this action is required.",
  ),
}).describe(
  "Details about any action required to complete the payment. Present when the payment could not be processed automatically.",
);

/**
 * OK
 */
export type BillingUpdateResponse = {
  customer_id: string;
  entity_id?: string | undefined;
  invoice?: BillingUpdateInvoice | undefined;
  payment_url: string | null;
  required_action?: BillingUpdateRequiredAction | undefined;
};

export const BillingUpdateResponse$zodSchema: z.ZodType<BillingUpdateResponse> =
  z.object({
    customer_id: z.string().describe("The ID of the customer."),
    entity_id: z.string().optional().describe(
      "The ID of the entity, if the plan was attached to an entity.",
    ),
    invoice: z.lazy(() => BillingUpdateInvoice$zodSchema).optional().describe(
      "Invoice details if an invoice was created. Only present when a charge was made.",
    ),
    payment_url: z.string().nullable().describe(
      "URL to redirect the customer to complete payment. Null if no payment action is required.",
    ),
    required_action: z.lazy(() => BillingUpdateRequiredAction$zodSchema)
      .optional().describe(
        "Details about any action required to complete the payment. Present when the payment could not be processed automatically.",
      ),
  }).describe("OK");
