/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { ClosedEnum } from "../types/enums.js";
import { Plan, Plan$zodSchema } from "./plan.js";

export type PreviewUpdateGlobals = { xApiVersion?: string | undefined };

export const PreviewUpdateGlobals$zodSchema: z.ZodType<PreviewUpdateGlobals> = z
  .object({
    xApiVersion: z.string().default("2.3.0"),
  });

/**
 * Quantity configuration for a prepaid feature.
 */
export type PreviewUpdateFeatureQuantityRequest = {
  feature_id: string;
  quantity?: number | undefined;
  adjustable?: boolean | undefined;
};

export const PreviewUpdateFeatureQuantityRequest$zodSchema: z.ZodType<
  PreviewUpdateFeatureQuantityRequest
> = z.object({
  adjustable: z.boolean().optional().describe(
    "Whether the customer can adjust the quantity.",
  ),
  feature_id: z.string().describe("The ID of the feature to set quantity for."),
  quantity: z.number().optional().describe("The quantity of the feature."),
}).describe("Quantity configuration for a prepaid feature.");

/**
 * Billing interval (e.g. 'month', 'year').
 */
export const PreviewUpdatePriceInterval = {
  OneOff: "one_off",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
/**
 * Billing interval (e.g. 'month', 'year').
 */
export type PreviewUpdatePriceInterval = ClosedEnum<
  typeof PreviewUpdatePriceInterval
>;

export const PreviewUpdatePriceInterval$zodSchema = z.enum([
  "one_off",
  "week",
  "month",
  "quarter",
  "semi_annual",
  "year",
]).describe("Billing interval (e.g. 'month', 'year').");

/**
 * Base price configuration for a plan.
 */
export type PreviewUpdateBasePrice = {
  amount: number;
  interval: PreviewUpdatePriceInterval;
  interval_count?: number | undefined;
};

export const PreviewUpdateBasePrice$zodSchema: z.ZodType<
  PreviewUpdateBasePrice
> = z.object({
  amount: z.number().describe("Base price amount for the plan."),
  interval: PreviewUpdatePriceInterval$zodSchema.describe(
    "Billing interval (e.g. 'month', 'year').",
  ),
  interval_count: z.number().optional().describe(
    "Number of intervals per billing cycle. Defaults to 1.",
  ),
}).describe("Base price configuration for a plan.");

/**
 * Interval at which balance resets (e.g. 'month', 'year'). For consumable features only.
 */
export const PreviewUpdateItemResetInterval = {
  OneOff: "one_off",
  Minute: "minute",
  Hour: "hour",
  Day: "day",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
/**
 * Interval at which balance resets (e.g. 'month', 'year'). For consumable features only.
 */
export type PreviewUpdateItemResetInterval = ClosedEnum<
  typeof PreviewUpdateItemResetInterval
>;

export const PreviewUpdateItemResetInterval$zodSchema = z.enum([
  "one_off",
  "minute",
  "hour",
  "day",
  "week",
  "month",
  "quarter",
  "semi_annual",
  "year",
]).describe(
  "Interval at which balance resets (e.g. 'month', 'year'). For consumable features only.",
);

/**
 * Reset configuration for consumable features. Omit for non-consumable features like seats.
 */
export type PreviewUpdateItemReset = {
  interval: PreviewUpdateItemResetInterval;
  interval_count?: number | undefined;
};

export const PreviewUpdateItemReset$zodSchema: z.ZodType<
  PreviewUpdateItemReset
> = z.object({
  interval: PreviewUpdateItemResetInterval$zodSchema.describe(
    "Interval at which balance resets (e.g. 'month', 'year'). For consumable features only.",
  ),
  interval_count: z.number().optional().describe(
    "Number of intervals between resets. Defaults to 1.",
  ),
}).describe(
  "Reset configuration for consumable features. Omit for non-consumable features like seats.",
);

export type PreviewUpdateItemTo = number | string;

export const PreviewUpdateItemTo$zodSchema: z.ZodType<PreviewUpdateItemTo> = z
  .union([
    z.number(),
    z.string(),
  ]);

export type PreviewUpdateItemTier = {
  to: number | string;
  amount?: number | undefined;
  flat_amount?: number | undefined;
};

export const PreviewUpdateItemTier$zodSchema: z.ZodType<PreviewUpdateItemTier> =
  z.object({
    amount: z.number().optional(),
    flat_amount: z.number().optional(),
    to: z.union([
      z.number(),
      z.string(),
    ]),
  });

export const PreviewUpdateItemTierBehavior = {
  Graduated: "graduated",
  Volume: "volume",
} as const;
export type PreviewUpdateItemTierBehavior = ClosedEnum<
  typeof PreviewUpdateItemTierBehavior
>;

export const PreviewUpdateItemTierBehavior$zodSchema = z.enum([
  "graduated",
  "volume",
]);

/**
 * Billing interval. For consumable features, should match reset.interval.
 */
export const PreviewUpdateItemPriceInterval = {
  OneOff: "one_off",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
/**
 * Billing interval. For consumable features, should match reset.interval.
 */
export type PreviewUpdateItemPriceInterval = ClosedEnum<
  typeof PreviewUpdateItemPriceInterval
>;

export const PreviewUpdateItemPriceInterval$zodSchema = z.enum([
  "one_off",
  "week",
  "month",
  "quarter",
  "semi_annual",
  "year",
]).describe(
  "Billing interval. For consumable features, should match reset.interval.",
);

/**
 * 'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go.
 */
export const PreviewUpdateItemBillingMethod = {
  Prepaid: "prepaid",
  UsageBased: "usage_based",
} as const;
/**
 * 'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go.
 */
export type PreviewUpdateItemBillingMethod = ClosedEnum<
  typeof PreviewUpdateItemBillingMethod
>;

export const PreviewUpdateItemBillingMethod$zodSchema = z.enum([
  "prepaid",
  "usage_based",
]).describe(
  "'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go.",
);

/**
 * Pricing for usage beyond included units. Omit for free features.
 */
export type PreviewUpdateItemPrice = {
  amount?: number | undefined;
  tiers?: Array<PreviewUpdateItemTier> | undefined;
  tier_behavior?: PreviewUpdateItemTierBehavior | undefined;
  interval: PreviewUpdateItemPriceInterval;
  interval_count?: number | undefined;
  billing_units?: number | undefined;
  billing_method: PreviewUpdateItemBillingMethod;
  max_purchase?: number | undefined;
};

export const PreviewUpdateItemPrice$zodSchema: z.ZodType<
  PreviewUpdateItemPrice
> = z.object({
  amount: z.number().optional().describe(
    "Price per billing_units after included usage. Either 'amount' or 'tiers' is required.",
  ),
  billing_method: PreviewUpdateItemBillingMethod$zodSchema.describe(
    "'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go.",
  ),
  billing_units: z.number().default(1).describe(
    "Units per price increment. Usage is rounded UP when billed (e.g. billing_units=100 means 101 rounds to 200).",
  ),
  interval: PreviewUpdateItemPriceInterval$zodSchema.describe(
    "Billing interval. For consumable features, should match reset.interval.",
  ),
  interval_count: z.number().default(1).describe(
    "Number of intervals per billing cycle. Defaults to 1.",
  ),
  max_purchase: z.number().optional().describe(
    "Max units purchasable beyond included. E.g. included=100, max_purchase=300 allows 400 total.",
  ),
  tier_behavior: PreviewUpdateItemTierBehavior$zodSchema.optional(),
  tiers: z.array(z.lazy(() => PreviewUpdateItemTier$zodSchema)).optional()
    .describe("Tiered pricing.  Either 'amount' or 'tiers' is required."),
}).describe("Pricing for usage beyond included units. Omit for free features.");

/**
 * Billing behavior when quantity increases mid-cycle.
 */
export const PreviewUpdateItemOnIncrease = {
  BillImmediately: "bill_immediately",
  ProrateImmediately: "prorate_immediately",
  ProrateNextCycle: "prorate_next_cycle",
  BillNextCycle: "bill_next_cycle",
} as const;
/**
 * Billing behavior when quantity increases mid-cycle.
 */
export type PreviewUpdateItemOnIncrease = ClosedEnum<
  typeof PreviewUpdateItemOnIncrease
>;

export const PreviewUpdateItemOnIncrease$zodSchema = z.enum([
  "bill_immediately",
  "prorate_immediately",
  "prorate_next_cycle",
  "bill_next_cycle",
]).describe("Billing behavior when quantity increases mid-cycle.");

/**
 * Credit behavior when quantity decreases mid-cycle.
 */
export const PreviewUpdateItemOnDecrease = {
  Prorate: "prorate",
  ProrateImmediately: "prorate_immediately",
  ProrateNextCycle: "prorate_next_cycle",
  None: "none",
  NoProrations: "no_prorations",
} as const;
/**
 * Credit behavior when quantity decreases mid-cycle.
 */
export type PreviewUpdateItemOnDecrease = ClosedEnum<
  typeof PreviewUpdateItemOnDecrease
>;

export const PreviewUpdateItemOnDecrease$zodSchema = z.enum([
  "prorate",
  "prorate_immediately",
  "prorate_next_cycle",
  "none",
  "no_prorations",
]).describe("Credit behavior when quantity decreases mid-cycle.");

/**
 * Proration settings for prepaid features. Controls mid-cycle quantity change billing.
 */
export type PreviewUpdateItemProration = {
  on_increase: PreviewUpdateItemOnIncrease;
  on_decrease: PreviewUpdateItemOnDecrease;
};

export const PreviewUpdateItemProration$zodSchema: z.ZodType<
  PreviewUpdateItemProration
> = z.object({
  on_decrease: PreviewUpdateItemOnDecrease$zodSchema.describe(
    "Credit behavior when quantity decreases mid-cycle.",
  ),
  on_increase: PreviewUpdateItemOnIncrease$zodSchema.describe(
    "Billing behavior when quantity increases mid-cycle.",
  ),
}).describe(
  "Proration settings for prepaid features. Controls mid-cycle quantity change billing.",
);

/**
 * When rolled over units expire.
 */
export const PreviewUpdateItemExpiryDurationType = {
  Month: "month",
  Forever: "forever",
} as const;
/**
 * When rolled over units expire.
 */
export type PreviewUpdateItemExpiryDurationType = ClosedEnum<
  typeof PreviewUpdateItemExpiryDurationType
>;

export const PreviewUpdateItemExpiryDurationType$zodSchema = z.enum([
  "month",
  "forever",
]).describe("When rolled over units expire.");

/**
 * Rollover config for unused units. If set, unused included units carry over.
 */
export type PreviewUpdateItemRollover = {
  max?: number | undefined;
  max_percentage?: number | undefined;
  expiry_duration_type: PreviewUpdateItemExpiryDurationType;
  expiry_duration_length?: number | undefined;
};

export const PreviewUpdateItemRollover$zodSchema: z.ZodType<
  PreviewUpdateItemRollover
> = z.object({
  expiry_duration_length: z.number().optional().describe(
    "Number of periods before expiry.",
  ),
  expiry_duration_type: PreviewUpdateItemExpiryDurationType$zodSchema.describe(
    "When rolled over units expire.",
  ),
  max: z.number().optional().describe(
    "Max rollover units. Omit for unlimited rollover.",
  ),
  max_percentage: z.number().optional().describe(
    "Maximum rollover as a percentage (0-100) of included + prepaid grant. Mutually exclusive with max.",
  ),
}).describe(
  "Rollover config for unused units. If set, unused included units carry over.",
);

/**
 * Configuration for a feature item in a plan, including usage limits, pricing, and rollover settings.
 */
export type PreviewUpdateItemPlanItem = {
  feature_id: string;
  included?: number | undefined;
  unlimited?: boolean | undefined;
  reset?: PreviewUpdateItemReset | undefined;
  price?: PreviewUpdateItemPrice | undefined;
  proration?: PreviewUpdateItemProration | undefined;
  rollover?: PreviewUpdateItemRollover | undefined;
};

export const PreviewUpdateItemPlanItem$zodSchema: z.ZodType<
  PreviewUpdateItemPlanItem
> = z.object({
  feature_id: z.string().describe("The ID of the feature to configure."),
  included: z.number().optional().describe(
    "Number of free units included. Balance resets to this each interval for consumable features.",
  ),
  price: z.lazy(() => PreviewUpdateItemPrice$zodSchema).optional().describe(
    "Pricing for usage beyond included units. Omit for free features.",
  ),
  proration: z.lazy(() => PreviewUpdateItemProration$zodSchema).optional()
    .describe(
      "Proration settings for prepaid features. Controls mid-cycle quantity change billing.",
    ),
  reset: z.lazy(() => PreviewUpdateItemReset$zodSchema).optional().describe(
    "Reset configuration for consumable features. Omit for non-consumable features like seats.",
  ),
  rollover: z.lazy(() => PreviewUpdateItemRollover$zodSchema).optional()
    .describe(
      "Rollover config for unused units. If set, unused included units carry over.",
    ),
  unlimited: z.boolean().optional().describe(
    "If true, customer has unlimited access to this feature.",
  ),
}).describe(
  "Configuration for a feature item in a plan, including usage limits, pricing, and rollover settings.",
);

/**
 * Interval at which balance resets (e.g. 'month', 'year'). For consumable features only.
 */
export const PreviewUpdateAddItemResetInterval = {
  OneOff: "one_off",
  Minute: "minute",
  Hour: "hour",
  Day: "day",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
/**
 * Interval at which balance resets (e.g. 'month', 'year'). For consumable features only.
 */
export type PreviewUpdateAddItemResetInterval = ClosedEnum<
  typeof PreviewUpdateAddItemResetInterval
>;

export const PreviewUpdateAddItemResetInterval$zodSchema = z.enum([
  "one_off",
  "minute",
  "hour",
  "day",
  "week",
  "month",
  "quarter",
  "semi_annual",
  "year",
]).describe(
  "Interval at which balance resets (e.g. 'month', 'year'). For consumable features only.",
);

/**
 * Reset configuration for consumable features. Omit for non-consumable features like seats.
 */
export type PreviewUpdateAddItemReset = {
  interval: PreviewUpdateAddItemResetInterval;
  interval_count?: number | undefined;
};

export const PreviewUpdateAddItemReset$zodSchema: z.ZodType<
  PreviewUpdateAddItemReset
> = z.object({
  interval: PreviewUpdateAddItemResetInterval$zodSchema.describe(
    "Interval at which balance resets (e.g. 'month', 'year'). For consumable features only.",
  ),
  interval_count: z.number().optional().describe(
    "Number of intervals between resets. Defaults to 1.",
  ),
}).describe(
  "Reset configuration for consumable features. Omit for non-consumable features like seats.",
);

export type PreviewUpdateAddItemTo = number | string;

export const PreviewUpdateAddItemTo$zodSchema: z.ZodType<
  PreviewUpdateAddItemTo
> = z.union([
  z.number(),
  z.string(),
]);

export type PreviewUpdateAddItemTier = {
  to: number | string;
  amount?: number | undefined;
  flat_amount?: number | undefined;
};

export const PreviewUpdateAddItemTier$zodSchema: z.ZodType<
  PreviewUpdateAddItemTier
> = z.object({
  amount: z.number().optional(),
  flat_amount: z.number().optional(),
  to: z.union([
    z.number(),
    z.string(),
  ]),
});

export const PreviewUpdateAddItemTierBehavior = {
  Graduated: "graduated",
  Volume: "volume",
} as const;
export type PreviewUpdateAddItemTierBehavior = ClosedEnum<
  typeof PreviewUpdateAddItemTierBehavior
>;

export const PreviewUpdateAddItemTierBehavior$zodSchema = z.enum([
  "graduated",
  "volume",
]);

/**
 * Billing interval. For consumable features, should match reset.interval.
 */
export const PreviewUpdateAddItemPriceInterval = {
  OneOff: "one_off",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
/**
 * Billing interval. For consumable features, should match reset.interval.
 */
export type PreviewUpdateAddItemPriceInterval = ClosedEnum<
  typeof PreviewUpdateAddItemPriceInterval
>;

export const PreviewUpdateAddItemPriceInterval$zodSchema = z.enum([
  "one_off",
  "week",
  "month",
  "quarter",
  "semi_annual",
  "year",
]).describe(
  "Billing interval. For consumable features, should match reset.interval.",
);

/**
 * 'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go.
 */
export const PreviewUpdateAddItemBillingMethod = {
  Prepaid: "prepaid",
  UsageBased: "usage_based",
} as const;
/**
 * 'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go.
 */
export type PreviewUpdateAddItemBillingMethod = ClosedEnum<
  typeof PreviewUpdateAddItemBillingMethod
>;

export const PreviewUpdateAddItemBillingMethod$zodSchema = z.enum([
  "prepaid",
  "usage_based",
]).describe(
  "'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go.",
);

/**
 * Pricing for usage beyond included units. Omit for free features.
 */
export type PreviewUpdateAddItemPrice = {
  amount?: number | undefined;
  tiers?: Array<PreviewUpdateAddItemTier> | undefined;
  tier_behavior?: PreviewUpdateAddItemTierBehavior | undefined;
  interval: PreviewUpdateAddItemPriceInterval;
  interval_count?: number | undefined;
  billing_units?: number | undefined;
  billing_method: PreviewUpdateAddItemBillingMethod;
  max_purchase?: number | undefined;
};

export const PreviewUpdateAddItemPrice$zodSchema: z.ZodType<
  PreviewUpdateAddItemPrice
> = z.object({
  amount: z.number().optional().describe(
    "Price per billing_units after included usage. Either 'amount' or 'tiers' is required.",
  ),
  billing_method: PreviewUpdateAddItemBillingMethod$zodSchema.describe(
    "'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go.",
  ),
  billing_units: z.number().default(1).describe(
    "Units per price increment. Usage is rounded UP when billed (e.g. billing_units=100 means 101 rounds to 200).",
  ),
  interval: PreviewUpdateAddItemPriceInterval$zodSchema.describe(
    "Billing interval. For consumable features, should match reset.interval.",
  ),
  interval_count: z.number().default(1).describe(
    "Number of intervals per billing cycle. Defaults to 1.",
  ),
  max_purchase: z.number().optional().describe(
    "Max units purchasable beyond included. E.g. included=100, max_purchase=300 allows 400 total.",
  ),
  tier_behavior: PreviewUpdateAddItemTierBehavior$zodSchema.optional(),
  tiers: z.array(z.lazy(() => PreviewUpdateAddItemTier$zodSchema)).optional()
    .describe("Tiered pricing.  Either 'amount' or 'tiers' is required."),
}).describe("Pricing for usage beyond included units. Omit for free features.");

/**
 * Billing behavior when quantity increases mid-cycle.
 */
export const PreviewUpdateAddItemOnIncrease = {
  BillImmediately: "bill_immediately",
  ProrateImmediately: "prorate_immediately",
  ProrateNextCycle: "prorate_next_cycle",
  BillNextCycle: "bill_next_cycle",
} as const;
/**
 * Billing behavior when quantity increases mid-cycle.
 */
export type PreviewUpdateAddItemOnIncrease = ClosedEnum<
  typeof PreviewUpdateAddItemOnIncrease
>;

export const PreviewUpdateAddItemOnIncrease$zodSchema = z.enum([
  "bill_immediately",
  "prorate_immediately",
  "prorate_next_cycle",
  "bill_next_cycle",
]).describe("Billing behavior when quantity increases mid-cycle.");

/**
 * Credit behavior when quantity decreases mid-cycle.
 */
export const PreviewUpdateAddItemOnDecrease = {
  Prorate: "prorate",
  ProrateImmediately: "prorate_immediately",
  ProrateNextCycle: "prorate_next_cycle",
  None: "none",
  NoProrations: "no_prorations",
} as const;
/**
 * Credit behavior when quantity decreases mid-cycle.
 */
export type PreviewUpdateAddItemOnDecrease = ClosedEnum<
  typeof PreviewUpdateAddItemOnDecrease
>;

export const PreviewUpdateAddItemOnDecrease$zodSchema = z.enum([
  "prorate",
  "prorate_immediately",
  "prorate_next_cycle",
  "none",
  "no_prorations",
]).describe("Credit behavior when quantity decreases mid-cycle.");

/**
 * Proration settings for prepaid features. Controls mid-cycle quantity change billing.
 */
export type PreviewUpdateAddItemProration = {
  on_increase: PreviewUpdateAddItemOnIncrease;
  on_decrease: PreviewUpdateAddItemOnDecrease;
};

export const PreviewUpdateAddItemProration$zodSchema: z.ZodType<
  PreviewUpdateAddItemProration
> = z.object({
  on_decrease: PreviewUpdateAddItemOnDecrease$zodSchema.describe(
    "Credit behavior when quantity decreases mid-cycle.",
  ),
  on_increase: PreviewUpdateAddItemOnIncrease$zodSchema.describe(
    "Billing behavior when quantity increases mid-cycle.",
  ),
}).describe(
  "Proration settings for prepaid features. Controls mid-cycle quantity change billing.",
);

/**
 * When rolled over units expire.
 */
export const PreviewUpdateAddItemExpiryDurationType = {
  Month: "month",
  Forever: "forever",
} as const;
/**
 * When rolled over units expire.
 */
export type PreviewUpdateAddItemExpiryDurationType = ClosedEnum<
  typeof PreviewUpdateAddItemExpiryDurationType
>;

export const PreviewUpdateAddItemExpiryDurationType$zodSchema = z.enum([
  "month",
  "forever",
]).describe("When rolled over units expire.");

/**
 * Rollover config for unused units. If set, unused included units carry over.
 */
export type PreviewUpdateAddItemRollover = {
  max?: number | undefined;
  max_percentage?: number | undefined;
  expiry_duration_type: PreviewUpdateAddItemExpiryDurationType;
  expiry_duration_length?: number | undefined;
};

export const PreviewUpdateAddItemRollover$zodSchema: z.ZodType<
  PreviewUpdateAddItemRollover
> = z.object({
  expiry_duration_length: z.number().optional().describe(
    "Number of periods before expiry.",
  ),
  expiry_duration_type: PreviewUpdateAddItemExpiryDurationType$zodSchema
    .describe("When rolled over units expire."),
  max: z.number().optional().describe(
    "Max rollover units. Omit for unlimited rollover.",
  ),
  max_percentage: z.number().optional().describe(
    "Maximum rollover as a percentage (0-100) of included + prepaid grant. Mutually exclusive with max.",
  ),
}).describe(
  "Rollover config for unused units. If set, unused included units carry over.",
);

/**
 * Configuration for a feature item in a plan, including usage limits, pricing, and rollover settings.
 */
export type PreviewUpdateAddItemPlanItem = {
  feature_id: string;
  included?: number | undefined;
  unlimited?: boolean | undefined;
  reset?: PreviewUpdateAddItemReset | undefined;
  price?: PreviewUpdateAddItemPrice | undefined;
  proration?: PreviewUpdateAddItemProration | undefined;
  rollover?: PreviewUpdateAddItemRollover | undefined;
};

export const PreviewUpdateAddItemPlanItem$zodSchema: z.ZodType<
  PreviewUpdateAddItemPlanItem
> = z.object({
  feature_id: z.string().describe("The ID of the feature to configure."),
  included: z.number().optional().describe(
    "Number of free units included. Balance resets to this each interval for consumable features.",
  ),
  price: z.lazy(() => PreviewUpdateAddItemPrice$zodSchema).optional().describe(
    "Pricing for usage beyond included units. Omit for free features.",
  ),
  proration: z.lazy(() => PreviewUpdateAddItemProration$zodSchema).optional()
    .describe(
      "Proration settings for prepaid features. Controls mid-cycle quantity change billing.",
    ),
  reset: z.lazy(() => PreviewUpdateAddItemReset$zodSchema).optional().describe(
    "Reset configuration for consumable features. Omit for non-consumable features like seats.",
  ),
  rollover: z.lazy(() => PreviewUpdateAddItemRollover$zodSchema).optional()
    .describe(
      "Rollover config for unused units. If set, unused included units carry over.",
    ),
  unlimited: z.boolean().optional().describe(
    "If true, customer has unlimited access to this feature.",
  ),
}).describe(
  "Configuration for a feature item in a plan, including usage limits, pricing, and rollover settings.",
);

/**
 * Match items with this billing method (prepaid or usage_based).
 */
export const PreviewUpdateRemoveItemBillingMethod = {
  Prepaid: "prepaid",
  UsageBased: "usage_based",
} as const;
/**
 * Match items with this billing method (prepaid or usage_based).
 */
export type PreviewUpdateRemoveItemBillingMethod = ClosedEnum<
  typeof PreviewUpdateRemoveItemBillingMethod
>;

export const PreviewUpdateRemoveItemBillingMethod$zodSchema = z.enum([
  "prepaid",
  "usage_based",
]).describe("Match items with this billing method (prepaid or usage_based).");

/**
 * Match items with this interval.
 */
export const PreviewUpdateRemoveItemInterval = {
  OneOff: "one_off",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
/**
 * Match items with this interval.
 */
export type PreviewUpdateRemoveItemInterval = ClosedEnum<
  typeof PreviewUpdateRemoveItemInterval
>;

export const PreviewUpdateRemoveItemInterval$zodSchema = z.enum([
  "one_off",
  "week",
  "month",
  "quarter",
  "semi_annual",
  "year",
]).describe("Match items with this interval.");

/**
 * Filter for matching plan items. All provided fields must match (AND).
 */
export type PreviewUpdatePlanItemFilter = {
  feature_id?: string | undefined;
  billing_method?: PreviewUpdateRemoveItemBillingMethod | undefined;
  interval?: PreviewUpdateRemoveItemInterval | undefined;
};

export const PreviewUpdatePlanItemFilter$zodSchema: z.ZodType<
  PreviewUpdatePlanItemFilter
> = z.object({
  billing_method: PreviewUpdateRemoveItemBillingMethod$zodSchema.optional()
    .describe("Match items with this billing method (prepaid or usage_based)."),
  feature_id: z.string().optional().describe(
    "Match items linked to this feature.",
  ),
  interval: PreviewUpdateRemoveItemInterval$zodSchema.optional().describe(
    "Match items with this interval.",
  ),
}).describe(
  "Filter for matching plan items. All provided fields must match (AND).",
);

/**
 * Unit of time for the trial ('day', 'month', 'year').
 */
export const PreviewUpdateDurationType = {
  Day: "day",
  Month: "month",
  Year: "year",
} as const;
/**
 * Unit of time for the trial ('day', 'month', 'year').
 */
export type PreviewUpdateDurationType = ClosedEnum<
  typeof PreviewUpdateDurationType
>;

export const PreviewUpdateDurationType$zodSchema = z.enum([
  "day",
  "month",
  "year",
]).describe("Unit of time for the trial ('day', 'month', 'year').");

/**
 * Behavior when the trial ends. 'bill' charges the customer (default). 'revert' expires the trial and restores the customer's previous plan.
 */
export const PreviewUpdateOnEnd = {
  Bill: "bill",
  Revert: "revert",
} as const;
/**
 * Behavior when the trial ends. 'bill' charges the customer (default). 'revert' expires the trial and restores the customer's previous plan.
 */
export type PreviewUpdateOnEnd = ClosedEnum<typeof PreviewUpdateOnEnd>;

export const PreviewUpdateOnEnd$zodSchema = z.enum([
  "bill",
  "revert",
]).describe(
  "Behavior when the trial ends. 'bill' charges the customer (default). 'revert' expires the trial and restores the customer's previous plan.",
);

/**
 * Free trial configuration for a plan.
 */
export type PreviewUpdateFreeTrialParams = {
  duration_length: number;
  duration_type?: PreviewUpdateDurationType | undefined;
  card_required?: boolean | undefined;
  on_end?: PreviewUpdateOnEnd | undefined;
};

export const PreviewUpdateFreeTrialParams$zodSchema: z.ZodType<
  PreviewUpdateFreeTrialParams
> = z.object({
  card_required: z.boolean().default(true).describe(
    "If true, payment method required to start trial. Customer is charged after trial ends.",
  ),
  duration_length: z.number().describe(
    "Number of duration_type periods the trial lasts.",
  ),
  duration_type: PreviewUpdateDurationType$zodSchema.default("month").describe(
    "Unit of time for the trial ('day', 'month', 'year').",
  ),
  on_end: PreviewUpdateOnEnd$zodSchema.optional().describe(
    "Behavior when the trial ends. 'bill' charges the customer (default). 'revert' expires the trial and restores the customer's previous plan.",
  ),
}).describe("Free trial configuration for a plan.");

/**
 * Customize the plan to attach. Can override the price, items, free trial, or a combination.
 */
export type PreviewUpdateCustomize = {
  price?: PreviewUpdateBasePrice | null | undefined;
  items?: Array<PreviewUpdateItemPlanItem> | undefined;
  add_items?: Array<PreviewUpdateAddItemPlanItem> | undefined;
  remove_items?: Array<PreviewUpdatePlanItemFilter> | undefined;
  free_trial?: PreviewUpdateFreeTrialParams | null | undefined;
};

export const PreviewUpdateCustomize$zodSchema: z.ZodType<
  PreviewUpdateCustomize
> = z.object({
  add_items: z.array(z.lazy(() => PreviewUpdateAddItemPlanItem$zodSchema))
    .optional().describe("Items to add to the plan."),
  free_trial: z.lazy(() => PreviewUpdateFreeTrialParams$zodSchema).nullable()
    .optional().describe(
      "Override the plan's default free trial. Pass an object to set a custom trial, or null to remove the trial entirely.",
    ),
  items: z.array(z.lazy(() => PreviewUpdateItemPlanItem$zodSchema)).optional()
    .describe(
      "Override the items in the plan (PUT-style — replaces all existing items). Mutually exclusive with add_items / remove_items.",
    ),
  price: z.lazy(() => PreviewUpdateBasePrice$zodSchema).nullable().optional()
    .describe(
      "Override the base price of the plan. Pass null to remove the base price.",
    ),
  remove_items: z.array(z.lazy(() => PreviewUpdatePlanItemFilter$zodSchema))
    .optional().describe("Filters selecting items to remove from the plan."),
}).describe(
  "Customize the plan to attach. Can override the price, items, free trial, or a combination.",
);

/**
 * Invoice mode creates a draft or open invoice and sends it to the customer, instead of charging their card immediately. This uses Stripe's send_invoice collection method.
 */
export type PreviewUpdateInvoiceMode = {
  enabled: boolean;
  enable_plan_immediately?: boolean | undefined;
  finalize?: boolean | undefined;
};

export const PreviewUpdateInvoiceMode$zodSchema: z.ZodType<
  PreviewUpdateInvoiceMode
> = z.object({
  enable_plan_immediately: z.boolean().default(false).describe(
    "If true, enables the plan immediately even though the invoice is not paid yet.",
  ),
  enabled: z.boolean().describe(
    "When true, creates an invoice and sends it to the customer instead of charging their card immediately. Uses Stripe's send_invoice collection method.",
  ),
  finalize: z.boolean().default(true).describe(
    "If true, finalizes the invoice so it can be sent to the customer. If false, keeps it as a draft for manual review.",
  ),
}).describe(
  "Invoice mode creates a draft or open invoice and sends it to the customer, instead of charging their card immediately. This uses Stripe's send_invoice collection method.",
);

/**
 * How to handle proration when updating an existing subscription. 'prorate_immediately' charges/credits prorated amounts now, 'none' skips creating any charges.
 */
export const PreviewUpdateProrationBehavior = {
  ProrateImmediately: "prorate_immediately",
  None: "none",
} as const;
/**
 * How to handle proration when updating an existing subscription. 'prorate_immediately' charges/credits prorated amounts now, 'none' skips creating any charges.
 */
export type PreviewUpdateProrationBehavior = ClosedEnum<
  typeof PreviewUpdateProrationBehavior
>;

export const PreviewUpdateProrationBehavior$zodSchema = z.enum([
  "prorate_immediately",
  "none",
]).describe(
  "How to handle proration when updating an existing subscription. 'prorate_immediately' charges/credits prorated amounts now, 'none' skips creating any charges.",
);

/**
 * Controls when to return a checkout URL. 'always' returns a URL even if payment succeeds, 'if_required' only when payment action is needed, 'never' disables redirects.
 */
export const PreviewUpdateRedirectMode = {
  Always: "always",
  IfRequired: "if_required",
  Never: "never",
} as const;
/**
 * Controls when to return a checkout URL. 'always' returns a URL even if payment succeeds, 'if_required' only when payment action is needed, 'never' disables redirects.
 */
export type PreviewUpdateRedirectMode = ClosedEnum<
  typeof PreviewUpdateRedirectMode
>;

export const PreviewUpdateRedirectMode$zodSchema = z.enum([
  "always",
  "if_required",
  "never",
]).describe(
  "Controls when to return a checkout URL. 'always' returns a URL even if payment succeeds, 'if_required' only when payment action is needed, 'never' disables redirects.",
);

/**
 * A discount to apply. Can be either a reward ID or a promotion code.
 */
export type PreviewUpdateAttachDiscount = {
  reward_id?: string | undefined;
  promotion_code?: string | undefined;
};

export const PreviewUpdateAttachDiscount$zodSchema: z.ZodType<
  PreviewUpdateAttachDiscount
> = z.object({
  promotion_code: z.string().optional().describe(
    "The promotion code to apply as a discount.",
  ),
  reward_id: z.string().optional().describe(
    "The ID of the reward to apply as a discount.",
  ),
}).describe(
  "A discount to apply. Can be either a reward ID or a promotion code.",
);

/**
 * Action to perform for cancellation. 'cancel_immediately' cancels now with prorated refund, 'cancel_end_of_cycle' cancels at period end, 'uncancel' reverses a pending cancellation.
 */
export const PreviewUpdateCancelAction = {
  CancelImmediately: "cancel_immediately",
  CancelEndOfCycle: "cancel_end_of_cycle",
  Uncancel: "uncancel",
} as const;
/**
 * Action to perform for cancellation. 'cancel_immediately' cancels now with prorated refund, 'cancel_end_of_cycle' cancels at period end, 'uncancel' reverses a pending cancellation.
 */
export type PreviewUpdateCancelAction = ClosedEnum<
  typeof PreviewUpdateCancelAction
>;

export const PreviewUpdateCancelAction$zodSchema = z.enum([
  "cancel_immediately",
  "cancel_end_of_cycle",
  "uncancel",
]).describe(
  "Action to perform for cancellation. 'cancel_immediately' cancels now with prorated refund, 'cancel_end_of_cycle' cancels at period end, 'uncancel' reverses a pending cancellation.",
);

/**
 * Controls whether balances should be recalculated during the subscription update.
 */
export type PreviewUpdateRecalculateBalances = { enabled: boolean };

export const PreviewUpdateRecalculateBalances$zodSchema: z.ZodType<
  PreviewUpdateRecalculateBalances
> = z.object({
  enabled: z.boolean().describe(
    "If true, recalculates balances during the subscription update. Only applicable when updating feature quantities.",
  ),
}).describe(
  "Controls whether balances should be recalculated during the subscription update.",
);

export type PreviewUpdateParams = {
  customer_id: string;
  entity_id?: string | undefined;
  plan_id?: string | undefined;
  feature_quantities?: Array<PreviewUpdateFeatureQuantityRequest> | undefined;
  version?: number | undefined;
  customize?: PreviewUpdateCustomize | undefined;
  invoice_mode?: PreviewUpdateInvoiceMode | undefined;
  proration_behavior?: PreviewUpdateProrationBehavior | undefined;
  redirect_mode?: PreviewUpdateRedirectMode | undefined;
  subscription_id?: string | undefined;
  discounts?: Array<PreviewUpdateAttachDiscount> | undefined;
  cancel_action?: PreviewUpdateCancelAction | undefined;
  billing_cycle_anchor?: "now" | undefined;
  no_billing_changes?: boolean | undefined;
  recalculate_balances?: PreviewUpdateRecalculateBalances | undefined;
};

export const PreviewUpdateParams$zodSchema: z.ZodType<PreviewUpdateParams> = z
  .object({
    billing_cycle_anchor: z.literal("now").default("now").optional().describe(
      "Reset the billing cycle anchor immediately with 'now'",
    ),
    cancel_action: PreviewUpdateCancelAction$zodSchema.optional().describe(
      "Action to perform for cancellation. 'cancel_immediately' cancels now with prorated refund, 'cancel_end_of_cycle' cancels at period end, 'uncancel' reverses a pending cancellation.",
    ),
    customer_id: z.string().describe(
      "The ID of the customer to attach the plan to.",
    ),
    customize: z.lazy(() => PreviewUpdateCustomize$zodSchema).optional()
      .describe(
        "Customize the plan to attach. Can override the price, items, free trial, or a combination.",
      ),
    discounts: z.array(z.lazy(() => PreviewUpdateAttachDiscount$zodSchema))
      .optional().describe(
        "List of discounts to apply. Each discount can be an Autumn reward ID, Stripe coupon ID, or Stripe promotion code.",
      ),
    entity_id: z.string().optional().describe(
      "The ID of the entity to attach the plan to.",
    ),
    feature_quantities: z.array(
      z.lazy(() => PreviewUpdateFeatureQuantityRequest$zodSchema),
    ).optional().describe(
      "If this plan contains prepaid features, use this field to specify the quantity of each prepaid feature. This quantity includes the included amount and billing units defined when setting up the plan.",
    ),
    invoice_mode: z.lazy(() => PreviewUpdateInvoiceMode$zodSchema).optional()
      .describe(
        "Invoice mode creates a draft or open invoice and sends it to the customer, instead of charging their card immediately. This uses Stripe's send_invoice collection method.",
      ),
    no_billing_changes: z.boolean().optional().describe(
      "If true, the subscription is updated internally without applying billing changes in Stripe.",
    ),
    plan_id: z.string().optional().describe(
      "The ID of the plan to update. Optional if subscription_id is provided, or if the customer has only one product.",
    ),
    proration_behavior: PreviewUpdateProrationBehavior$zodSchema.optional()
      .describe(
        "How to handle proration when updating an existing subscription. 'prorate_immediately' charges/credits prorated amounts now, 'none' skips creating any charges.",
      ),
    recalculate_balances: z.lazy(() =>
      PreviewUpdateRecalculateBalances$zodSchema
    ).optional().describe(
      "Controls whether balances should be recalculated during the subscription update.",
    ),
    redirect_mode: PreviewUpdateRedirectMode$zodSchema.default("if_required")
      .describe(
        "Controls when to return a checkout URL. 'always' returns a URL even if payment succeeds, 'if_required' only when payment action is needed, 'never' disables redirects.",
      ),
    subscription_id: z.string().optional().describe(
      "A unique ID to identify this subscription. Can be used to target specific subscriptions in update operations when a customer has multiple products with the same plan.",
    ),
    version: z.number().optional().describe(
      "The version of the plan to attach.",
    ),
  });

export type PreviewUpdateDiscount = {
  amount_off: number;
  percent_off?: number | undefined;
  reward_id?: string | undefined;
  reward_name?: string | undefined;
};

export const PreviewUpdateDiscount$zodSchema: z.ZodType<PreviewUpdateDiscount> =
  z.object({
    amount_off: z.number(),
    percent_off: z.number().optional(),
    reward_id: z.string().optional(),
    reward_name: z.string().optional(),
  });

/**
 * The period of time that this line item is being charged for.
 */
export type PreviewUpdateLineItemPeriod = { start: number; end: number };

export const PreviewUpdateLineItemPeriod$zodSchema: z.ZodType<
  PreviewUpdateLineItemPeriod
> = z.object({
  end: z.number().describe(
    "The end of the period in milliseconds since the Unix epoch.",
  ),
  start: z.number().describe(
    "The start of the period in milliseconds since the Unix epoch.",
  ),
}).describe("The period of time that this line item is being charged for.");

export type PreviewUpdateLineItem = {
  display_name: string;
  description: string;
  subtotal: number;
  total: number;
  discounts?: Array<PreviewUpdateDiscount> | undefined;
  plan_id: string;
  feature_id: string | null;
  period?: PreviewUpdateLineItemPeriod | undefined;
  quantity: number;
};

export const PreviewUpdateLineItem$zodSchema: z.ZodType<PreviewUpdateLineItem> =
  z.object({
    description: z.string().describe(
      "A detailed description of the line item.",
    ),
    discounts: z.array(z.lazy(() => PreviewUpdateDiscount$zodSchema)).optional()
      .describe("List of discounts applied to this line item."),
    display_name: z.string().describe(
      "The name of the line item to display to the customer if you're building a UI. It will either be the plan name or the feature name.",
    ),
    feature_id: z.string().nullable().describe(
      "The ID of the feature that this line item belongs to.",
    ),
    period: z.lazy(() => PreviewUpdateLineItemPeriod$zodSchema).optional()
      .describe("The period of time that this line item is being charged for."),
    plan_id: z.string().describe(
      "The ID of the plan that this line item belongs to.",
    ),
    quantity: z.number().describe("The quantity of the line item."),
    subtotal: z.number().describe(
      "The amount in cents before discounts and tax for this line item.",
    ),
    total: z.number().describe(
      "The final amount in cents after discounts and tax for this line item.",
    ),
  });

export type PreviewUpdateNextCycleDiscount = {
  amount_off: number;
  percent_off?: number | undefined;
  reward_id?: string | undefined;
  reward_name?: string | undefined;
};

export const PreviewUpdateNextCycleDiscount$zodSchema: z.ZodType<
  PreviewUpdateNextCycleDiscount
> = z.object({
  amount_off: z.number(),
  percent_off: z.number().optional(),
  reward_id: z.string().optional(),
  reward_name: z.string().optional(),
});

/**
 * The period of time that this line item is being charged for.
 */
export type PreviewUpdateNextCycleLineItemPeriod = {
  start: number;
  end: number;
};

export const PreviewUpdateNextCycleLineItemPeriod$zodSchema: z.ZodType<
  PreviewUpdateNextCycleLineItemPeriod
> = z.object({
  end: z.number().describe(
    "The end of the period in milliseconds since the Unix epoch.",
  ),
  start: z.number().describe(
    "The start of the period in milliseconds since the Unix epoch.",
  ),
}).describe("The period of time that this line item is being charged for.");

export type PreviewUpdateNextCycleLineItem = {
  display_name: string;
  description: string;
  subtotal: number;
  total: number;
  discounts?: Array<PreviewUpdateNextCycleDiscount> | undefined;
  plan_id: string;
  feature_id: string | null;
  period?: PreviewUpdateNextCycleLineItemPeriod | undefined;
  quantity: number;
};

export const PreviewUpdateNextCycleLineItem$zodSchema: z.ZodType<
  PreviewUpdateNextCycleLineItem
> = z.object({
  description: z.string().describe("A detailed description of the line item."),
  discounts: z.array(z.lazy(() => PreviewUpdateNextCycleDiscount$zodSchema))
    .optional().describe("List of discounts applied to this line item."),
  display_name: z.string().describe(
    "The name of the line item to display to the customer if you're building a UI. It will either be the plan name or the feature name.",
  ),
  feature_id: z.string().nullable().describe(
    "The ID of the feature that this line item belongs to.",
  ),
  period: z.lazy(() => PreviewUpdateNextCycleLineItemPeriod$zodSchema)
    .optional().describe(
      "The period of time that this line item is being charged for.",
    ),
  plan_id: z.string().describe(
    "The ID of the plan that this line item belongs to.",
  ),
  quantity: z.number().describe("The quantity of the line item."),
  subtotal: z.number().describe(
    "The amount in cents before discounts and tax for this line item.",
  ),
  total: z.number().describe(
    "The final amount in cents after discounts and tax for this line item.",
  ),
});

/**
 * The period of time that this line item is being charged for.
 */
export type PreviewUpdateUsageLineItemPeriod = { start: number; end: number };

export const PreviewUpdateUsageLineItemPeriod$zodSchema: z.ZodType<
  PreviewUpdateUsageLineItemPeriod
> = z.object({
  end: z.number().describe(
    "The end of the period in milliseconds since the Unix epoch.",
  ),
  start: z.number().describe(
    "The start of the period in milliseconds since the Unix epoch.",
  ),
}).describe("The period of time that this line item is being charged for.");

export type PreviewUpdateUsageLineItem = {
  display_name: string;
  plan_id: string;
  feature_id: string | null;
  period?: PreviewUpdateUsageLineItemPeriod | undefined;
};

export const PreviewUpdateUsageLineItem$zodSchema: z.ZodType<
  PreviewUpdateUsageLineItem
> = z.object({
  display_name: z.string().describe(
    "The name of the line item to display to the customer if you're building a UI. It will either be the plan name or the feature name.",
  ),
  feature_id: z.string().nullable().describe(
    "The ID of the feature that this line item belongs to.",
  ),
  period: z.lazy(() => PreviewUpdateUsageLineItemPeriod$zodSchema).optional()
    .describe("The period of time that this line item is being charged for."),
  plan_id: z.string().describe(
    "The ID of the plan that this line item belongs to.",
  ),
});

/**
 * Preview of the next billing cycle, if applicable. This shows what the customer will be charged in subsequent cycles.
 */
export type PreviewUpdateNextCycle = {
  starts_at: number;
  subtotal: number;
  total: number;
  line_items: Array<PreviewUpdateNextCycleLineItem>;
  usage_line_items: Array<PreviewUpdateUsageLineItem>;
};

export const PreviewUpdateNextCycle$zodSchema: z.ZodType<
  PreviewUpdateNextCycle
> = z.object({
  line_items: z.array(z.lazy(() => PreviewUpdateNextCycleLineItem$zodSchema))
    .describe("List of line items for the next billing cycle."),
  starts_at: z.number().describe(
    "Unix timestamp (milliseconds) when the next billing cycle starts.",
  ),
  subtotal: z.number().describe(
    "The total amount in cents before discounts and tax for the next cycle.",
  ),
  total: z.number().describe(
    "The final amount in cents after discounts and tax for the next cycle.",
  ),
  usage_line_items: z.array(z.lazy(() => PreviewUpdateUsageLineItem$zodSchema))
    .describe("List of line items for usage-based features in the next cycle."),
}).describe(
  "Preview of the next billing cycle, if applicable. This shows what the customer will be charged in subsequent cycles.",
);

export type PreviewUpdateIncomingFeatureQuantity = {
  feature_id: string;
  quantity: number;
};

export const PreviewUpdateIncomingFeatureQuantity$zodSchema: z.ZodType<
  PreviewUpdateIncomingFeatureQuantity
> = z.object({
  feature_id: z.string().describe(
    "The ID of the adjustable feature included in this change.",
  ),
  quantity: z.number().describe(
    "The quantity that will apply for this feature in the change.",
  ),
});

export type PreviewUpdateIncoming = {
  plan_id: string;
  plan?: Plan | undefined;
  feature_quantities: Array<PreviewUpdateIncomingFeatureQuantity>;
  effective_at: number | null;
  canceled_at: number | null;
  expires_at: number | null;
};

export const PreviewUpdateIncoming$zodSchema: z.ZodType<PreviewUpdateIncoming> =
  z.object({
    canceled_at: z.number().nullable().describe(
      "When this plan was canceled, in milliseconds since the Unix epoch, or null if it is not canceled.",
    ),
    effective_at: z.number().nullable().describe(
      "When this change takes effect, in milliseconds since the Unix epoch, or null if it applies immediately.",
    ),
    expires_at: z.number().nullable().describe(
      "When this plan expires, in milliseconds since the Unix epoch, or null if it does not expire.",
    ),
    feature_quantities: z.array(
      z.lazy(() => PreviewUpdateIncomingFeatureQuantity$zodSchema),
    ).describe(
      "The feature quantity selections associated with this plan change.",
    ),
    plan: Plan$zodSchema.optional(),
    plan_id: z.string().describe(
      "The ID of the plan affected by this preview change.",
    ),
  });

export type PreviewUpdateOutgoingFeatureQuantity = {
  feature_id: string;
  quantity: number;
};

export const PreviewUpdateOutgoingFeatureQuantity$zodSchema: z.ZodType<
  PreviewUpdateOutgoingFeatureQuantity
> = z.object({
  feature_id: z.string().describe(
    "The ID of the adjustable feature included in this change.",
  ),
  quantity: z.number().describe(
    "The quantity that will apply for this feature in the change.",
  ),
});

export type PreviewUpdateOutgoing = {
  plan_id: string;
  plan?: Plan | undefined;
  feature_quantities: Array<PreviewUpdateOutgoingFeatureQuantity>;
  effective_at: number | null;
  canceled_at: number | null;
  expires_at: number | null;
};

export const PreviewUpdateOutgoing$zodSchema: z.ZodType<PreviewUpdateOutgoing> =
  z.object({
    canceled_at: z.number().nullable().describe(
      "When this plan was canceled, in milliseconds since the Unix epoch, or null if it is not canceled.",
    ),
    effective_at: z.number().nullable().describe(
      "When this change takes effect, in milliseconds since the Unix epoch, or null if it applies immediately.",
    ),
    expires_at: z.number().nullable().describe(
      "When this plan expires, in milliseconds since the Unix epoch, or null if it does not expire.",
    ),
    feature_quantities: z.array(
      z.lazy(() => PreviewUpdateOutgoingFeatureQuantity$zodSchema),
    ).describe(
      "The feature quantity selections associated with this plan change.",
    ),
    plan: Plan$zodSchema.optional(),
    plan_id: z.string().describe(
      "The ID of the plan affected by this preview change.",
    ),
  });

export const Intent = {
  UpdatePlan: "update_plan",
  UpdateQuantity: "update_quantity",
  CancelImmediately: "cancel_immediately",
  CancelEndOfCycle: "cancel_end_of_cycle",
  Uncancel: "uncancel",
  None: "none",
} as const;
export type Intent = ClosedEnum<typeof Intent>;

export const Intent$zodSchema = z.enum([
  "update_plan",
  "update_quantity",
  "cancel_immediately",
  "cancel_end_of_cycle",
  "uncancel",
  "none",
]);

/**
 * Calculation status ('complete' when Stripe Tax succeeds or 'incomplete' when Stripe Tax returned 0 or errored).
 */
export const PreviewUpdateStatus = {
  Complete: "complete",
  Incomplete: "incomplete",
} as const;
/**
 * Calculation status ('complete' when Stripe Tax succeeds or 'incomplete' when Stripe Tax returned 0 or errored).
 */
export type PreviewUpdateStatus = ClosedEnum<typeof PreviewUpdateStatus>;

export const PreviewUpdateStatus$zodSchema = z.enum([
  "complete",
  "incomplete",
]).describe(
  "Calculation status ('complete' when Stripe Tax succeeds or 'incomplete' when Stripe Tax returned 0 or errored).",
);

/**
 * Tax preview for the immediate charge. Contact us to enable the tax flag on your organisation. Shows only with flag enabled, a Stripe customer exists and has a location.
 */
export type PreviewUpdateTax = {
  total: number;
  amount_inclusive: number;
  amount_exclusive: number;
  currency: string;
  status: PreviewUpdateStatus;
};

export const PreviewUpdateTax$zodSchema: z.ZodType<PreviewUpdateTax> = z.object(
  {
    amount_exclusive: z.number().describe("Tax added on top of subtotals."),
    amount_inclusive: z.number().describe(
      "Tax included in line item subtotals.",
    ),
    currency: z.string().describe("Three-letter currency code."),
    status: PreviewUpdateStatus$zodSchema.describe(
      "Calculation status ('complete' when Stripe Tax succeeds or 'incomplete' when Stripe Tax returned 0 or errored).",
    ),
    total: z.number().describe("Total tax amount in major currency units."),
  },
).describe(
  "Tax preview for the immediate charge. Contact us to enable the tax flag on your organisation. Shows only with flag enabled, a Stripe customer exists and has a location.",
);

/**
 * Stripe customer invoice credits preview.
 */
export type PreviewUpdateInvoiceCredits = { balance: number; currency: string };

export const PreviewUpdateInvoiceCredits$zodSchema: z.ZodType<
  PreviewUpdateInvoiceCredits
> = z.object({
  balance: z.number().describe(
    "Stripe customer credit balance available, expressed as a positive number in major currency units.",
  ),
  currency: z.string().describe("Three-letter currency code."),
}).describe("Stripe customer invoice credits preview.");

/**
 * OK
 */
export type PreviewUpdateResponse = {
  customer_id: string;
  line_items: Array<PreviewUpdateLineItem>;
  subtotal: number;
  total: number;
  currency: string;
  next_cycle?: PreviewUpdateNextCycle | undefined;
  expand?: Array<string> | undefined;
  incoming: Array<PreviewUpdateIncoming>;
  outgoing: Array<PreviewUpdateOutgoing>;
  intent: Intent;
  tax?: PreviewUpdateTax | undefined;
  invoice_credits?: PreviewUpdateInvoiceCredits | undefined;
};

export const PreviewUpdateResponse$zodSchema: z.ZodType<PreviewUpdateResponse> =
  z.object({
    currency: z.string().describe(
      "The three-letter ISO currency code (e.g., 'usd').",
    ),
    customer_id: z.string().describe("The ID of the customer."),
    expand: z.array(z.string()).optional().describe(
      "Expand the response with additional data.",
    ),
    incoming: z.array(z.lazy(() => PreviewUpdateIncoming$zodSchema)).describe(
      "Products or subscription changes being added or updated.",
    ),
    intent: Intent$zodSchema,
    invoice_credits: z.lazy(() => PreviewUpdateInvoiceCredits$zodSchema)
      .optional().describe("Stripe customer invoice credits preview."),
    line_items: z.array(z.lazy(() => PreviewUpdateLineItem$zodSchema)).describe(
      "List of line items for the current billing period.",
    ),
    next_cycle: z.lazy(() => PreviewUpdateNextCycle$zodSchema).optional()
      .describe(
        "Preview of the next billing cycle, if applicable. This shows what the customer will be charged in subsequent cycles.",
      ),
    outgoing: z.array(z.lazy(() => PreviewUpdateOutgoing$zodSchema)).describe(
      "Products or subscription changes being removed or ended.",
    ),
    subtotal: z.number().describe(
      "The total amount in cents before discounts and tax for the current billing period.",
    ),
    tax: z.lazy(() => PreviewUpdateTax$zodSchema).optional().describe(
      "Tax preview for the immediate charge. Contact us to enable the tax flag on your organisation. Shows only with flag enabled, a Stripe customer exists and has a location.",
    ),
    total: z.number().describe(
      "The final amount in cents after discounts and tax for the current billing period.",
    ),
  }).describe("OK");
