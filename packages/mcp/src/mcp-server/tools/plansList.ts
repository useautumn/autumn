/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { plansList } from "../../funcs/plansList.js";
import { ListPlansParams$zodSchema } from "../../models/listplansop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListPlansParams$zodSchema.optional(),
};

export const tool$plansList: ToolDefinition<typeof args> = {
  name: "list_plans",
  description:
    `List Autumn plans. Optionally pass a customer ID to include customer eligibility.`,
  annotations: {
    "title": "List plans",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await plansList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
