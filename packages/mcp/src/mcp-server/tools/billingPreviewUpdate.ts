/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { billingPreviewUpdate } from "../../funcs/billingPreviewUpdate.js";
import { PreviewUpdateParams$zodSchema } from "../../models/previewupdateop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: PreviewUpdateParams$zodSchema,
};

export const tool$billingPreviewUpdate: ToolDefinition<typeof args> = {
  name: "preview_update_subscription",
  description:
    `Preview the billing impact of updating a customer subscription. This does not execute the update.`,
  annotations: {
    "title": "Preview update subscription",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await billingPreviewUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
