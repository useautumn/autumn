/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { customersList } from "../../funcs/customersList.js";
import { ListCustomersParams$zodSchema } from "../../models/listcustomersop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListCustomersParams$zodSchema,
};

export const tool$customersList: ToolDefinition<typeof args> = {
  name: "list_customers",
  description:
    `List Autumn customers. Use the search field to find customers by ID, name, or email.`,
  annotations: {
    "title": "List customers",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await customersList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
