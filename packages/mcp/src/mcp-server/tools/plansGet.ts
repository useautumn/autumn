/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { plansGet } from "../../funcs/plansGet.js";
import { GetPlanParams$zodSchema } from "../../models/getplanop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetPlanParams$zodSchema,
};

export const tool$plansGet: ToolDefinition<typeof args> = {
  name: "get_plan",
  description: `Get one Autumn plan by plan ID and optional version.`,
  annotations: {
    "title": "Get plan",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await plansGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
