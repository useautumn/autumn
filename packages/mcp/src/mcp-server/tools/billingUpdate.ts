/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { billingUpdate } from "../../funcs/billingUpdate.js";
import { UpdateSubscriptionParams$zodSchema } from "../../models/billingupdateop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateSubscriptionParams$zodSchema,
};

export const tool$billingUpdate: ToolDefinition<typeof args> = {
  name: "update_subscription",
  description:
    `Update a customer subscription. This may change quantities, cancel, uncancel, invoice, or update billing state. Call preview_update_subscription first, show the billing impact to the user, and get explicit confirmation before calling this tool.`,
  annotations: {
    "title": "Update subscription",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await billingUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
