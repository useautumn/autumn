/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { billingPreviewAttach } from "../../funcs/billingPreviewAttach.js";
import { PreviewAttachParams$zodSchema } from "../../models/previewattachop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: PreviewAttachParams$zodSchema,
};

export const tool$billingPreviewAttach: ToolDefinition<typeof args> = {
  name: "preview_attach",
  description:
    `Preview the billing impact of attaching a plan to a customer. This does not execute the attach.`,
  annotations: {
    "title": "Preview attach",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await billingPreviewAttach(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
