/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { customersGet } from "../../funcs/customersGet.js";
import { GetCustomerParams$zodSchema } from "../../models/getcustomerop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetCustomerParams$zodSchema,
};

export const tool$customersGet: ToolDefinition<typeof args> = {
  name: "get_customer",
  description:
    `Get one Autumn customer by customer ID, with optional expanded fields.`,
  annotations: {
    "title": "Get customer",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await customersGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
