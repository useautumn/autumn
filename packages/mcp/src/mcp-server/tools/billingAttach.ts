/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { billingAttach } from "../../funcs/billingAttach.js";
import { AttachParams$zodSchema } from "../../models/attachop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AttachParams$zodSchema,
};

export const tool$billingAttach: ToolDefinition<typeof args> = {
  name: "attach",
  description:
    `Attach a plan to a customer. This may create or update subscriptions, invoices, or payment links. Call preview_attach first, show the billing impact to the user, and get explicit confirmation before calling this tool.`,
  annotations: {
    "title": "Attach",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await billingAttach(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
