{
	"name": "@autumn/atmn-generator",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"description": "Generates the atmn-nightly CLI from the internal OpenAPI spec.",
	"scripts": {
		"ts": "bunx tsgo --build --noEmit",
		"test": "bun test test",
		"generate": "bun run src/generate.ts",
		"check": "bun run src/checkClean.ts"
	},
	"dependencies": {
		"yaml": "2.9.0"
	},
	"devDependencies": {
		"@types/bun": "latest"
	}
}
