# BalancesCheckScenario

## Example Usage

```typescript
import { BalancesCheckScenario } from "@useautumn/sdk";

let value: BalancesCheckScenario = "usage_limit";
```

## Values

This is an open enum. Unrecognized values will be captured as the `Unrecognized<string>` branded type.

```typescript
"usage_limit" | "feature_flag" | Unrecognized<string>
```