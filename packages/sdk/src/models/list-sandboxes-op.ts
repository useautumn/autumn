/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import * as openEnums from "../types/enums.js";
import { OpenEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./sdk-validation-error.js";

export type ListSandboxesGlobals = {
  xApiVersion?: string | undefined;
};

/**
 * No body. Lists every sandbox belonging to your organization.
 */
export type ListSandboxesParams = {};

/**
 * Colour the dashboard uses to label the sandbox.
 */
export const ListSandboxesColor = {
  Gray: "gray",
  Blue: "blue",
  Green: "green",
  Amber: "amber",
  Red: "red",
  Pink: "pink",
} as const;
/**
 * Colour the dashboard uses to label the sandbox.
 */
export type ListSandboxesColor = OpenEnum<typeof ListSandboxesColor>;

export type ListSandboxesList = {
  /**
   * The sandbox's organization ID.
   */
  id: string;
  /**
   * The sandbox's name.
   */
  name: string;
  /**
   * The sandbox's slug, derived from its name.
   */
  slug: string;
  /**
   * When the sandbox was created, ms since epoch.
   */
  createdAt: number;
  /**
   * Colour the dashboard uses to label the sandbox.
   */
  color: ListSandboxesColor;
  /**
   * Icon the dashboard uses to label the sandbox.
   */
  icon: string;
};

/**
 * OK
 */
export type ListSandboxesResponse = {
  /**
   * Your organization's sandboxes, newest first.
   */
  list: Array<ListSandboxesList>;
};

/** @internal */
export type ListSandboxesParams$Outbound = {};

/** @internal */
export const ListSandboxesParams$outboundSchema: z.ZodMiniType<
  ListSandboxesParams$Outbound,
  ListSandboxesParams
> = z.object({});

export function listSandboxesParamsToJSON(
  listSandboxesParams: ListSandboxesParams,
): string {
  return JSON.stringify(
    ListSandboxesParams$outboundSchema.parse(listSandboxesParams),
  );
}

/** @internal */
export const ListSandboxesColor$inboundSchema: z.ZodMiniType<
  ListSandboxesColor,
  unknown
> = openEnums.inboundSchema(ListSandboxesColor);

/** @internal */
export const ListSandboxesList$inboundSchema: z.ZodMiniType<
  ListSandboxesList,
  unknown
> = z.pipe(
  z.object({
    id: types.string(),
    name: types.string(),
    slug: types.string(),
    created_at: types.number(),
    color: ListSandboxesColor$inboundSchema,
    icon: types.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "created_at": "createdAt",
    });
  }),
);

export function listSandboxesListFromJSON(
  jsonString: string,
): SafeParseResult<ListSandboxesList, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListSandboxesList$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListSandboxesList' from JSON`,
  );
}

/** @internal */
export const ListSandboxesResponse$inboundSchema: z.ZodMiniType<
  ListSandboxesResponse,
  unknown
> = z.object({
  list: z.array(z.lazy(() => ListSandboxesList$inboundSchema)),
});

export function listSandboxesResponseFromJSON(
  jsonString: string,
): SafeParseResult<ListSandboxesResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListSandboxesResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListSandboxesResponse' from JSON`,
  );
}
