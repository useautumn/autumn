/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./sdk-validation-error.js";

export type RefreshKeyGlobals = {
  xApiVersion?: string | undefined;
};

/**
 * No body. The refresh token is supplied as the Bearer credential; the response is a freshly rotated access + refresh pair.
 */
export type RefreshKeyParams = {};

/**
 * OK
 */
export type RefreshKeyResponse = {
  /**
   * Access token (1h, or non-expiring if indefinite), prefixed `am_jwt_`.
   */
  accessToken: string;
  /**
   * Rotating refresh token (24h). Omitted for indefinite tokens.
   */
  refreshToken?: string | undefined;
  /**
   * Access-token expiry, ms since epoch. null for indefinite tokens.
   */
  expiresAt: number | null;
  /**
   * Refresh-token expiry, ms since epoch. Omitted for indefinite tokens.
   */
  refreshExpiresAt?: number | undefined;
};

/** @internal */
export type RefreshKeyParams$Outbound = {};

/** @internal */
export const RefreshKeyParams$outboundSchema: z.ZodMiniType<
  RefreshKeyParams$Outbound,
  RefreshKeyParams
> = z.object({});

export function refreshKeyParamsToJSON(
  refreshKeyParams: RefreshKeyParams,
): string {
  return JSON.stringify(
    RefreshKeyParams$outboundSchema.parse(refreshKeyParams),
  );
}

/** @internal */
export const RefreshKeyResponse$inboundSchema: z.ZodMiniType<
  RefreshKeyResponse,
  unknown
> = z.pipe(
  z.object({
    access_token: types.string(),
    refresh_token: types.optional(types.string()),
    expires_at: types.nullable(types.number()),
    refresh_expires_at: types.optional(types.number()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "access_token": "accessToken",
      "refresh_token": "refreshToken",
      "expires_at": "expiresAt",
      "refresh_expires_at": "refreshExpiresAt",
    });
  }),
);

export function refreshKeyResponseFromJSON(
  jsonString: string,
): SafeParseResult<RefreshKeyResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RefreshKeyResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RefreshKeyResponse' from JSON`,
  );
}
