/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./sdk-validation-error.js";

export type RevokeKeyGlobals = {
  xApiVersion?: string | undefined;
};

export type RevokeKeyParams = {
  /**
   * The customer whose tokens (every outstanding access + refresh token) should be revoked.
   */
  customerId: string;
};

/**
 * OK
 */
export type RevokeKeyResponse = {
  revoked: true;
};

/** @internal */
export type RevokeKeyParams$Outbound = {
  customer_id: string;
};

/** @internal */
export const RevokeKeyParams$outboundSchema: z.ZodMiniType<
  RevokeKeyParams$Outbound,
  RevokeKeyParams
> = z.pipe(
  z.object({
    customerId: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      customerId: "customer_id",
    });
  }),
);

export function revokeKeyParamsToJSON(
  revokeKeyParams: RevokeKeyParams,
): string {
  return JSON.stringify(RevokeKeyParams$outboundSchema.parse(revokeKeyParams));
}

/** @internal */
export const RevokeKeyResponse$inboundSchema: z.ZodMiniType<
  RevokeKeyResponse,
  unknown
> = z.object({
  revoked: types.literal(true),
});

export function revokeKeyResponseFromJSON(
  jsonString: string,
): SafeParseResult<RevokeKeyResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RevokeKeyResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RevokeKeyResponse' from JSON`,
  );
}
