/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import * as openEnums from "../types/enums.js";
import { ClosedEnum, OpenEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { Balance, Balance$inboundSchema } from "./balance.js";
import { Plan, Plan$inboundSchema } from "./plan.js";
import { SDKValidationError } from "./sdk-validation-error.js";

export type ListEntitiesGlobals = {
  xApiVersion?: string | undefined;
};

export type ListEntitiesPlan = {
  id: string;
  versions?: Array<number> | undefined;
};

/**
 * Filter customer products used for entity hydration and plan matching. Defaults to active and scheduled.
 */
export const ListEntitiesSubscriptionStatus = {
  Active: "active",
  Scheduled: "scheduled",
} as const;
/**
 * Filter customer products used for entity hydration and plan matching. Defaults to active and scheduled.
 */
export type ListEntitiesSubscriptionStatus = ClosedEnum<
  typeof ListEntitiesSubscriptionStatus
>;

export const ListEntitiesProcessor = {
  Stripe: "stripe",
  Revenuecat: "revenuecat",
  Vercel: "vercel",
} as const;
export type ListEntitiesProcessor = ClosedEnum<typeof ListEntitiesProcessor>;

export type ListEntitiesParams = {
  /**
   * Opaque pagination cursor. Empty string (default) requests the first page; use next_cursor from a prior response for subsequent pages.
   */
  startCursor?: string | undefined;
  /**
   * Number of items to return. Default 50, hard ceiling 5000.
   */
  limit?: number | undefined;
  /**
   * Filter by plan ID and version. Returns entities with active subscriptions to this plan, including plans inherited from the parent customer.
   */
  plans?: Array<ListEntitiesPlan> | undefined;
  /**
   * Filter customer products used for entity hydration and plan matching. Defaults to active and scheduled.
   */
  subscriptionStatus?: ListEntitiesSubscriptionStatus | undefined;
  /**
   * Search entities by id or name.
   */
  search?: string | undefined;
  /**
   * Filter by parent customer processor type (stripe, revenuecat, vercel).
   */
  processors?: Array<ListEntitiesProcessor> | undefined;
  /**
   * Restrict the response to entities owned by this customer id. Use to bulk-fetch all entities for one customer in a single paginated call instead of iterating entities.get.
   */
  customerId?: string | undefined;
};

/**
 * The environment (sandbox/live)
 */
export const ListEntitiesEnv = {
  Sandbox: "sandbox",
  Live: "live",
} as const;
/**
 * The environment (sandbox/live)
 */
export type ListEntitiesEnv = OpenEnum<typeof ListEntitiesEnv>;

/**
 * Current status of the subscription.
 */
export const ListEntitiesStatus = {
  Active: "active",
  Scheduled: "scheduled",
} as const;
/**
 * Current status of the subscription.
 */
export type ListEntitiesStatus = OpenEnum<typeof ListEntitiesStatus>;

/**
 * Whether this subscription is attached at the customer level or entity level.
 */
export const ListEntitiesSubscriptionScope = {
  Customer: "customer",
  Entity: "entity",
} as const;
/**
 * Whether this subscription is attached at the customer level or entity level.
 */
export type ListEntitiesSubscriptionScope = OpenEnum<
  typeof ListEntitiesSubscriptionScope
>;

export type ListEntitiesSubscription = {
  /**
   * The unique identifier of this subscription. If a subscription_id was provided at attach time, it is used; otherwise, falls back to the internal ID.
   */
  id: string;
  plan?: Plan | undefined;
  /**
   * The unique identifier of the subscribed plan.
   */
  planId: string;
  /**
   * Whether the plan was automatically enabled for the customer.
   */
  autoEnable: boolean;
  /**
   * Whether this is an add-on plan rather than a base subscription.
   */
  addOn: boolean;
  /**
   * Current status of the subscription.
   */
  status: ListEntitiesStatus;
  /**
   * Whether the subscription has overdue payments.
   */
  pastDue: boolean;
  /**
   * Timestamp when the subscription was canceled, or null if not canceled.
   */
  canceledAt: number | null;
  /**
   * Timestamp when the subscription will expire, or null if no expiry set.
   */
  expiresAt: number | null;
  /**
   * Timestamp when the trial period ends, or null if not on trial.
   */
  trialEndsAt: number | null;
  /**
   * Timestamp when the subscription started.
   */
  startedAt: number;
  /**
   * Start timestamp of the current billing period.
   */
  currentPeriodStart: number | null;
  /**
   * End timestamp of the current billing period.
   */
  currentPeriodEnd: number | null;
  /**
   * Number of units of this subscription (for per-seat plans).
   */
  quantity: number;
  /**
   * Whether this subscription is attached at the customer level or entity level.
   */
  scope?: ListEntitiesSubscriptionScope | undefined;
};

/**
 * Whether this purchase is attached at the customer level or entity level.
 */
export const ListEntitiesPurchaseScope = {
  Customer: "customer",
  Entity: "entity",
} as const;
/**
 * Whether this purchase is attached at the customer level or entity level.
 */
export type ListEntitiesPurchaseScope = OpenEnum<
  typeof ListEntitiesPurchaseScope
>;

export type ListEntitiesPurchase = {
  plan?: Plan | undefined;
  /**
   * The unique identifier of the purchased plan.
   */
  planId: string;
  /**
   * Timestamp when the purchase expires, or null for lifetime access.
   */
  expiresAt: number | null;
  /**
   * Timestamp when the purchase was made.
   */
  startedAt: number;
  /**
   * Number of units purchased.
   */
  quantity: number;
  /**
   * Whether this purchase is attached at the customer level or entity level.
   */
  scope?: ListEntitiesPurchaseScope | undefined;
};

/**
 * Feature type: 'boolean' for on/off access, 'metered' for usage-tracked features, 'credit_system' for unified credit pools.
 */
export const ListEntitiesType = {
  Boolean: "boolean",
  Metered: "metered",
  CreditSystem: "credit_system",
} as const;
/**
 * Feature type: 'boolean' for on/off access, 'metered' for usage-tracked features, 'credit_system' for unified credit pools.
 */
export type ListEntitiesType = OpenEnum<typeof ListEntitiesType>;

export type ListEntitiesCreditSchema = {
  /**
   * ID of the metered feature that draws from this credit system.
   */
  meteredFeatureId: string;
  /**
   * Credits consumed per unit of the metered feature.
   */
  creditCost: number;
};

/**
 * Display names for the feature in billing UI and customer-facing components.
 */
export type ListEntitiesDisplay = {
  /**
   * Singular form for UI display (e.g., 'API call', 'seat').
   */
  singular?: string | null | undefined;
  /**
   * Plural form for UI display (e.g., 'API calls', 'seats').
   */
  plural?: string | null | undefined;
};

/**
 * The full feature object if expanded.
 */
export type ListEntitiesFeature = {
  /**
   * The unique identifier for this feature, used in /check and /track calls.
   */
  id: string;
  /**
   * Human-readable name displayed in the dashboard and billing UI.
   */
  name: string;
  /**
   * Feature type: 'boolean' for on/off access, 'metered' for usage-tracked features, 'credit_system' for unified credit pools.
   */
  type: ListEntitiesType;
  /**
   * For metered features: true if usage resets periodically (API calls, credits), false if allocated persistently (seats, storage).
   */
  consumable: boolean;
  /**
   * Event names that trigger this feature's balance. Allows multiple features to respond to a single event.
   */
  eventNames?: Array<string> | undefined;
  /**
   * For credit_system features: maps metered features to their credit costs.
   */
  creditSchema?: Array<ListEntitiesCreditSchema> | undefined;
  /**
   * Display names for the feature in billing UI and customer-facing components.
   */
  display?: ListEntitiesDisplay | undefined;
  /**
   * Whether the feature is archived and hidden from the dashboard.
   */
  archived: boolean;
};

export type ListEntitiesFlags = {
  /**
   * The unique identifier for this flag.
   */
  id: string;
  /**
   * The plan ID this flag originates from, or null for standalone flags.
   */
  planId: string | null;
  /**
   * Timestamp when this flag expires, or null for no expiration.
   */
  expiresAt: number | null;
  /**
   * The feature ID this flag is for.
   */
  featureId: string;
  /**
   * The full feature object if expanded.
   */
  feature?: ListEntitiesFeature | undefined;
};

export type ListEntitiesSpendLimit = {
  /**
   * Optional feature ID this spend limit applies to.
   */
  featureId?: string | undefined;
  /**
   * Whether this spend limit is enabled.
   */
  enabled: boolean;
  /**
   * Maximum allowed overage spend for the target feature.
   */
  overageLimit?: number | undefined;
};

/**
 * Whether the threshold is an absolute count or a percentage of the usage allowance or remaining balance.
 */
export const ListEntitiesThresholdType = {
  Usage: "usage",
  UsagePercentage: "usage_percentage",
  Remaining: "remaining",
  RemainingPercentage: "remaining_percentage",
} as const;
/**
 * Whether the threshold is an absolute count or a percentage of the usage allowance or remaining balance.
 */
export type ListEntitiesThresholdType = OpenEnum<
  typeof ListEntitiesThresholdType
>;

export type ListEntitiesUsageAlert = {
  /**
   * The feature ID this alert applies to.
   */
  featureId?: string | undefined;
  /**
   * Whether this usage alert is enabled.
   */
  enabled: boolean;
  /**
   * The threshold value that triggers the alert. For usage or remaining, this is an absolute count. For usage_percentage or remaining_percentage, this is a percentage (0-100).
   */
  threshold: number;
  /**
   * Whether the threshold is an absolute count or a percentage of the usage allowance or remaining balance.
   */
  thresholdType: ListEntitiesThresholdType;
  /**
   * Optional user-defined label to distinguish multiple alerts on the same feature.
   */
  name?: string | undefined;
};

export type ListEntitiesOverageAllowed = {
  /**
   * The feature ID this overage allowed control applies to.
   */
  featureId: string;
  /**
   * Whether overage is allowed for this feature.
   */
  enabled: boolean;
};

/**
 * Billing controls for the entity.
 */
export type ListEntitiesBillingControls = {
  /**
   * List of overage spend limits per feature.
   */
  spendLimits?: Array<ListEntitiesSpendLimit> | undefined;
  /**
   * List of usage alert configurations per feature.
   */
  usageAlerts?: Array<ListEntitiesUsageAlert> | undefined;
  /**
   * List of overage allowed controls per feature. When enabled, usage can exceed balance.
   */
  overageAllowed?: Array<ListEntitiesOverageAllowed> | undefined;
};

/**
 * The billing processor that owns this invoice.
 */
export const ListEntitiesProcessorType = {
  Stripe: "stripe",
  Revenuecat: "revenuecat",
} as const;
/**
 * The billing processor that owns this invoice.
 */
export type ListEntitiesProcessorType = OpenEnum<
  typeof ListEntitiesProcessorType
>;

export type ListEntitiesInvoice = {
  /**
   * Array of plan IDs included in this invoice
   */
  planIds: Array<string>;
  /**
   * The Stripe invoice ID
   */
  stripeId: string;
  /**
   * The billing processor that owns this invoice.
   */
  processorType: ListEntitiesProcessorType;
  /**
   * The status of the invoice
   */
  status: string;
  /**
   * The total amount of the invoice
   */
  total: number;
  /**
   * The currency code for the invoice
   */
  currency: string;
  /**
   * Timestamp when the invoice was created
   */
  createdAt: number;
  /**
   * URL to the Stripe-hosted invoice page
   */
  hostedInvoiceUrl?: string | null | undefined;
};

export type ListEntitiesList = {
  /**
   * The unique identifier of the entity
   */
  id: string | null;
  /**
   * The name of the entity
   */
  name: string | null;
  /**
   * The customer ID this entity belongs to
   */
  customerId?: string | null | undefined;
  /**
   * The feature ID this entity belongs to
   */
  featureId?: string | null | undefined;
  /**
   * Unix timestamp when the entity was created
   */
  createdAt: number;
  /**
   * The environment (sandbox/live)
   */
  env: ListEntitiesEnv;
  subscriptions: Array<ListEntitiesSubscription>;
  purchases: Array<ListEntitiesPurchase>;
  balances: { [k: string]: Balance };
  flags: { [k: string]: ListEntitiesFlags };
  /**
   * Billing controls for the entity.
   */
  billingControls?: ListEntitiesBillingControls | undefined;
  /**
   * Invoices for this entity (only included when expand=invoices)
   */
  invoices?: Array<ListEntitiesInvoice> | undefined;
};

/**
 * OK
 */
export type ListEntitiesResponse = {
  /**
   * Items for current page.
   */
  list: Array<ListEntitiesList>;
  /**
   * Opaque cursor for the next page. Null when there are no more results.
   */
  nextCursor: string | null;
};

/** @internal */
export type ListEntitiesPlan$Outbound = {
  id: string;
  versions?: Array<number> | undefined;
};

/** @internal */
export const ListEntitiesPlan$outboundSchema: z.ZodMiniType<
  ListEntitiesPlan$Outbound,
  ListEntitiesPlan
> = z.object({
  id: z.string(),
  versions: z.optional(z.array(z.number())),
});

export function listEntitiesPlanToJSON(
  listEntitiesPlan: ListEntitiesPlan,
): string {
  return JSON.stringify(
    ListEntitiesPlan$outboundSchema.parse(listEntitiesPlan),
  );
}

/** @internal */
export const ListEntitiesSubscriptionStatus$outboundSchema: z.ZodMiniEnum<
  typeof ListEntitiesSubscriptionStatus
> = z.enum(ListEntitiesSubscriptionStatus);

/** @internal */
export const ListEntitiesProcessor$outboundSchema: z.ZodMiniEnum<
  typeof ListEntitiesProcessor
> = z.enum(ListEntitiesProcessor);

/** @internal */
export type ListEntitiesParams$Outbound = {
  start_cursor: string;
  limit: number;
  plans?: Array<ListEntitiesPlan$Outbound> | undefined;
  subscription_status?: string | undefined;
  search?: string | undefined;
  processors?: Array<string> | undefined;
  customer_id?: string | undefined;
};

/** @internal */
export const ListEntitiesParams$outboundSchema: z.ZodMiniType<
  ListEntitiesParams$Outbound,
  ListEntitiesParams
> = z.pipe(
  z.object({
    startCursor: z._default(z.string(), ""),
    limit: z._default(z.int(), 50),
    plans: z.optional(z.array(z.lazy(() => ListEntitiesPlan$outboundSchema))),
    subscriptionStatus: z.optional(
      ListEntitiesSubscriptionStatus$outboundSchema,
    ),
    search: z.optional(z.string()),
    processors: z.optional(z.array(ListEntitiesProcessor$outboundSchema)),
    customerId: z.optional(z.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      startCursor: "start_cursor",
      subscriptionStatus: "subscription_status",
      customerId: "customer_id",
    });
  }),
);

export function listEntitiesParamsToJSON(
  listEntitiesParams: ListEntitiesParams,
): string {
  return JSON.stringify(
    ListEntitiesParams$outboundSchema.parse(listEntitiesParams),
  );
}

/** @internal */
export const ListEntitiesEnv$inboundSchema: z.ZodMiniType<
  ListEntitiesEnv,
  unknown
> = openEnums.inboundSchema(ListEntitiesEnv);

/** @internal */
export const ListEntitiesStatus$inboundSchema: z.ZodMiniType<
  ListEntitiesStatus,
  unknown
> = openEnums.inboundSchema(ListEntitiesStatus);

/** @internal */
export const ListEntitiesSubscriptionScope$inboundSchema: z.ZodMiniType<
  ListEntitiesSubscriptionScope,
  unknown
> = openEnums.inboundSchema(ListEntitiesSubscriptionScope);

/** @internal */
export const ListEntitiesSubscription$inboundSchema: z.ZodMiniType<
  ListEntitiesSubscription,
  unknown
> = z.pipe(
  z.object({
    id: types.string(),
    plan: types.optional(Plan$inboundSchema),
    plan_id: types.string(),
    auto_enable: types.boolean(),
    add_on: types.boolean(),
    status: ListEntitiesStatus$inboundSchema,
    past_due: types.boolean(),
    canceled_at: types.nullable(types.number()),
    expires_at: types.nullable(types.number()),
    trial_ends_at: types.nullable(types.number()),
    started_at: types.number(),
    current_period_start: types.nullable(types.number()),
    current_period_end: types.nullable(types.number()),
    quantity: types.number(),
    scope: types.optional(ListEntitiesSubscriptionScope$inboundSchema),
  }),
  z.transform((v) => {
    return remap$(v, {
      "plan_id": "planId",
      "auto_enable": "autoEnable",
      "add_on": "addOn",
      "past_due": "pastDue",
      "canceled_at": "canceledAt",
      "expires_at": "expiresAt",
      "trial_ends_at": "trialEndsAt",
      "started_at": "startedAt",
      "current_period_start": "currentPeriodStart",
      "current_period_end": "currentPeriodEnd",
    });
  }),
);

export function listEntitiesSubscriptionFromJSON(
  jsonString: string,
): SafeParseResult<ListEntitiesSubscription, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListEntitiesSubscription$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListEntitiesSubscription' from JSON`,
  );
}

/** @internal */
export const ListEntitiesPurchaseScope$inboundSchema: z.ZodMiniType<
  ListEntitiesPurchaseScope,
  unknown
> = openEnums.inboundSchema(ListEntitiesPurchaseScope);

/** @internal */
export const ListEntitiesPurchase$inboundSchema: z.ZodMiniType<
  ListEntitiesPurchase,
  unknown
> = z.pipe(
  z.object({
    plan: types.optional(Plan$inboundSchema),
    plan_id: types.string(),
    expires_at: types.nullable(types.number()),
    started_at: types.number(),
    quantity: types.number(),
    scope: types.optional(ListEntitiesPurchaseScope$inboundSchema),
  }),
  z.transform((v) => {
    return remap$(v, {
      "plan_id": "planId",
      "expires_at": "expiresAt",
      "started_at": "startedAt",
    });
  }),
);

export function listEntitiesPurchaseFromJSON(
  jsonString: string,
): SafeParseResult<ListEntitiesPurchase, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListEntitiesPurchase$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListEntitiesPurchase' from JSON`,
  );
}

/** @internal */
export const ListEntitiesType$inboundSchema: z.ZodMiniType<
  ListEntitiesType,
  unknown
> = openEnums.inboundSchema(ListEntitiesType);

/** @internal */
export const ListEntitiesCreditSchema$inboundSchema: z.ZodMiniType<
  ListEntitiesCreditSchema,
  unknown
> = z.pipe(
  z.object({
    metered_feature_id: types.string(),
    credit_cost: types.number(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "metered_feature_id": "meteredFeatureId",
      "credit_cost": "creditCost",
    });
  }),
);

export function listEntitiesCreditSchemaFromJSON(
  jsonString: string,
): SafeParseResult<ListEntitiesCreditSchema, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListEntitiesCreditSchema$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListEntitiesCreditSchema' from JSON`,
  );
}

/** @internal */
export const ListEntitiesDisplay$inboundSchema: z.ZodMiniType<
  ListEntitiesDisplay,
  unknown
> = z.object({
  singular: z.optional(z.nullable(types.string())),
  plural: z.optional(z.nullable(types.string())),
});

export function listEntitiesDisplayFromJSON(
  jsonString: string,
): SafeParseResult<ListEntitiesDisplay, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListEntitiesDisplay$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListEntitiesDisplay' from JSON`,
  );
}

/** @internal */
export const ListEntitiesFeature$inboundSchema: z.ZodMiniType<
  ListEntitiesFeature,
  unknown
> = z.pipe(
  z.object({
    id: types.string(),
    name: types.string(),
    type: ListEntitiesType$inboundSchema,
    consumable: types.boolean(),
    event_names: types.optional(z.array(types.string())),
    credit_schema: types.optional(
      z.array(z.lazy(() => ListEntitiesCreditSchema$inboundSchema)),
    ),
    display: types.optional(z.lazy(() => ListEntitiesDisplay$inboundSchema)),
    archived: types.boolean(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "event_names": "eventNames",
      "credit_schema": "creditSchema",
    });
  }),
);

export function listEntitiesFeatureFromJSON(
  jsonString: string,
): SafeParseResult<ListEntitiesFeature, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListEntitiesFeature$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListEntitiesFeature' from JSON`,
  );
}

/** @internal */
export const ListEntitiesFlags$inboundSchema: z.ZodMiniType<
  ListEntitiesFlags,
  unknown
> = z.pipe(
  z.object({
    id: types.string(),
    plan_id: types.nullable(types.string()),
    expires_at: types.nullable(types.number()),
    feature_id: types.string(),
    feature: types.optional(z.lazy(() => ListEntitiesFeature$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "plan_id": "planId",
      "expires_at": "expiresAt",
      "feature_id": "featureId",
    });
  }),
);

export function listEntitiesFlagsFromJSON(
  jsonString: string,
): SafeParseResult<ListEntitiesFlags, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListEntitiesFlags$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListEntitiesFlags' from JSON`,
  );
}

/** @internal */
export const ListEntitiesSpendLimit$inboundSchema: z.ZodMiniType<
  ListEntitiesSpendLimit,
  unknown
> = z.pipe(
  z.object({
    feature_id: types.optional(types.string()),
    enabled: z._default(types.boolean(), false),
    overage_limit: types.optional(types.number()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "feature_id": "featureId",
      "overage_limit": "overageLimit",
    });
  }),
);

export function listEntitiesSpendLimitFromJSON(
  jsonString: string,
): SafeParseResult<ListEntitiesSpendLimit, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListEntitiesSpendLimit$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListEntitiesSpendLimit' from JSON`,
  );
}

/** @internal */
export const ListEntitiesThresholdType$inboundSchema: z.ZodMiniType<
  ListEntitiesThresholdType,
  unknown
> = openEnums.inboundSchema(ListEntitiesThresholdType);

/** @internal */
export const ListEntitiesUsageAlert$inboundSchema: z.ZodMiniType<
  ListEntitiesUsageAlert,
  unknown
> = z.pipe(
  z.object({
    feature_id: types.optional(types.string()),
    enabled: z._default(types.boolean(), true),
    threshold: types.number(),
    threshold_type: ListEntitiesThresholdType$inboundSchema,
    name: types.optional(types.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "feature_id": "featureId",
      "threshold_type": "thresholdType",
    });
  }),
);

export function listEntitiesUsageAlertFromJSON(
  jsonString: string,
): SafeParseResult<ListEntitiesUsageAlert, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListEntitiesUsageAlert$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListEntitiesUsageAlert' from JSON`,
  );
}

/** @internal */
export const ListEntitiesOverageAllowed$inboundSchema: z.ZodMiniType<
  ListEntitiesOverageAllowed,
  unknown
> = z.pipe(
  z.object({
    feature_id: types.string(),
    enabled: z._default(types.boolean(), false),
  }),
  z.transform((v) => {
    return remap$(v, {
      "feature_id": "featureId",
    });
  }),
);

export function listEntitiesOverageAllowedFromJSON(
  jsonString: string,
): SafeParseResult<ListEntitiesOverageAllowed, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListEntitiesOverageAllowed$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListEntitiesOverageAllowed' from JSON`,
  );
}

/** @internal */
export const ListEntitiesBillingControls$inboundSchema: z.ZodMiniType<
  ListEntitiesBillingControls,
  unknown
> = z.pipe(
  z.object({
    spend_limits: types.optional(
      z.array(z.lazy(() => ListEntitiesSpendLimit$inboundSchema)),
    ),
    usage_alerts: types.optional(
      z.array(z.lazy(() => ListEntitiesUsageAlert$inboundSchema)),
    ),
    overage_allowed: types.optional(
      z.array(z.lazy(() => ListEntitiesOverageAllowed$inboundSchema)),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "spend_limits": "spendLimits",
      "usage_alerts": "usageAlerts",
      "overage_allowed": "overageAllowed",
    });
  }),
);

export function listEntitiesBillingControlsFromJSON(
  jsonString: string,
): SafeParseResult<ListEntitiesBillingControls, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListEntitiesBillingControls$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListEntitiesBillingControls' from JSON`,
  );
}

/** @internal */
export const ListEntitiesProcessorType$inboundSchema: z.ZodMiniType<
  ListEntitiesProcessorType,
  unknown
> = openEnums.inboundSchema(ListEntitiesProcessorType);

/** @internal */
export const ListEntitiesInvoice$inboundSchema: z.ZodMiniType<
  ListEntitiesInvoice,
  unknown
> = z.pipe(
  z.object({
    plan_ids: z.array(types.string()),
    stripe_id: types.string(),
    processor_type: z._default(
      ListEntitiesProcessorType$inboundSchema,
      "stripe",
    ),
    status: types.string(),
    total: types.number(),
    currency: types.string(),
    created_at: types.number(),
    hosted_invoice_url: z.optional(z.nullable(types.string())),
  }),
  z.transform((v) => {
    return remap$(v, {
      "plan_ids": "planIds",
      "stripe_id": "stripeId",
      "processor_type": "processorType",
      "created_at": "createdAt",
      "hosted_invoice_url": "hostedInvoiceUrl",
    });
  }),
);

export function listEntitiesInvoiceFromJSON(
  jsonString: string,
): SafeParseResult<ListEntitiesInvoice, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListEntitiesInvoice$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListEntitiesInvoice' from JSON`,
  );
}

/** @internal */
export const ListEntitiesList$inboundSchema: z.ZodMiniType<
  ListEntitiesList,
  unknown
> = z.pipe(
  z.object({
    id: types.nullable(types.string()),
    name: types.nullable(types.string()),
    customer_id: z.optional(z.nullable(types.string())),
    feature_id: z.optional(z.nullable(types.string())),
    created_at: types.number(),
    env: ListEntitiesEnv$inboundSchema,
    subscriptions: z.array(
      z.lazy(() => ListEntitiesSubscription$inboundSchema),
    ),
    purchases: z.array(z.lazy(() => ListEntitiesPurchase$inboundSchema)),
    balances: z.record(z.string(), Balance$inboundSchema),
    flags: z.record(z.string(), z.lazy(() => ListEntitiesFlags$inboundSchema)),
    billing_controls: types.optional(
      z.lazy(() => ListEntitiesBillingControls$inboundSchema),
    ),
    invoices: types.optional(
      z.array(z.lazy(() => ListEntitiesInvoice$inboundSchema)),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "customer_id": "customerId",
      "feature_id": "featureId",
      "created_at": "createdAt",
      "billing_controls": "billingControls",
    });
  }),
);

export function listEntitiesListFromJSON(
  jsonString: string,
): SafeParseResult<ListEntitiesList, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListEntitiesList$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListEntitiesList' from JSON`,
  );
}

/** @internal */
export const ListEntitiesResponse$inboundSchema: z.ZodMiniType<
  ListEntitiesResponse,
  unknown
> = z.pipe(
  z.object({
    list: z.array(z.lazy(() => ListEntitiesList$inboundSchema)),
    next_cursor: types.nullable(types.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "next_cursor": "nextCursor",
    });
  }),
);

export function listEntitiesResponseFromJSON(
  jsonString: string,
): SafeParseResult<ListEntitiesResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListEntitiesResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListEntitiesResponse' from JSON`,
  );
}
