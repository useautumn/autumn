/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import * as openEnums from "../types/enums.js";
import { OpenEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { smartUnion } from "../types/smart-union.js";
import {
  ListPlansBillingControls,
  ListPlansBillingControls$inboundSchema,
  ListPlansConfig,
  ListPlansConfig$inboundSchema,
  ListPlansCustomerEligibility,
  ListPlansCustomerEligibility$inboundSchema,
  ListPlansEnv,
  ListPlansEnv$inboundSchema,
  ListPlansFreeTrial,
  ListPlansFreeTrial$inboundSchema,
  ListPlansItem,
  ListPlansItem$inboundSchema,
  ListPlansLicense,
  ListPlansLicense$inboundSchema,
  ListPlansPrice,
  ListPlansPrice$inboundSchema,
  ListPlansProcessors,
  ListPlansProcessors$inboundSchema,
  ListPlansVariantAddItemResetInterval,
  ListPlansVariantAddItemResetInterval$inboundSchema,
  ListPlansVariantBasePrice,
  ListPlansVariantBasePrice$inboundSchema,
  ListPlansVariantDetails,
  ListPlansVariantDetails$inboundSchema,
} from "./list-plans-variant-add-item-reset-interval.js";
import { SDKValidationError } from "./sdk-validation-error.js";

/**
 * Reset configuration for consumable features. Omit for non-consumable features like seats.
 */
export type ListPlansVariantAddItemReset = {
  /**
   * Interval at which balance resets (e.g. 'month', 'year'). For consumable features only.
   */
  interval: ListPlansVariantAddItemResetInterval;
  /**
   * Number of intervals between resets. Defaults to 1.
   */
  intervalCount?: number | undefined;
};

export type ListPlansVariantAddItemAdditionalCurrency = {
  currency?: any | undefined;
  amount?: any | undefined;
};

export type ListPlansVariantAddItemTier = {
  to?: any | undefined;
  amount?: any | undefined;
  flatAmount?: any | undefined;
  additionalCurrencies?: any | undefined;
};

export const ListPlansVariantAddItemTierBehavior = {
  Graduated: "graduated",
  Volume: "volume",
} as const;
export type ListPlansVariantAddItemTierBehavior = OpenEnum<
  typeof ListPlansVariantAddItemTierBehavior
>;

/**
 * Billing interval. For consumable features, should match reset.interval.
 */
export const ListPlansVariantAddItemPriceInterval = {
  OneOff: "one_off",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
/**
 * Billing interval. For consumable features, should match reset.interval.
 */
export type ListPlansVariantAddItemPriceInterval = OpenEnum<
  typeof ListPlansVariantAddItemPriceInterval
>;

/**
 * 'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go.
 */
export const ListPlansVariantAddItemBillingMethod = {
  Prepaid: "prepaid",
  UsageBased: "usage_based",
} as const;
/**
 * 'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go.
 */
export type ListPlansVariantAddItemBillingMethod = OpenEnum<
  typeof ListPlansVariantAddItemBillingMethod
>;

/**
 * Pricing for usage beyond included units. Omit for free features.
 */
export type ListPlansVariantAddItemPrice = {
  /**
   * Price per billing_units after included usage. Either 'amount' or 'tiers' is required.
   */
  amount?: number | undefined;
  /**
   * Amounts in additional currencies for this flat price. The base 'amount' is in the org's default currency. Only valid with 'amount', not 'tiers'.
   */
  additionalCurrencies?:
    | Array<ListPlansVariantAddItemAdditionalCurrency>
    | undefined;
  /**
   * Tiered pricing.  Either 'amount' or 'tiers' is required.
   */
  tiers?: Array<ListPlansVariantAddItemTier> | undefined;
  tierBehavior?: ListPlansVariantAddItemTierBehavior | undefined;
  /**
   * Billing interval. For consumable features, should match reset.interval.
   */
  interval: ListPlansVariantAddItemPriceInterval;
  /**
   * Number of intervals per billing cycle. Defaults to 1.
   */
  intervalCount: number;
  /**
   * Units per price increment. Usage is rounded UP when billed (e.g. billing_units=100 means 101 rounds to 200).
   */
  billingUnits: number;
  /**
   * 'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go.
   */
  billingMethod: ListPlansVariantAddItemBillingMethod;
  /**
   * Max units purchasable beyond included. E.g. included=100, max_purchase=300 allows 400 total. Null for no limit.
   */
  maxPurchase?: number | null | undefined;
};

/**
 * Billing behavior when quantity increases mid-cycle.
 */
export const ListPlansVariantOnIncrease = {
  BillImmediately: "bill_immediately",
  ProrateImmediately: "prorate_immediately",
  ProrateNextCycle: "prorate_next_cycle",
  BillNextCycle: "bill_next_cycle",
} as const;
/**
 * Billing behavior when quantity increases mid-cycle.
 */
export type ListPlansVariantOnIncrease = OpenEnum<
  typeof ListPlansVariantOnIncrease
>;

/**
 * Credit behavior when quantity decreases mid-cycle.
 */
export const ListPlansVariantOnDecrease = {
  Prorate: "prorate",
  ProrateImmediately: "prorate_immediately",
  ProrateNextCycle: "prorate_next_cycle",
  None: "none",
  NoProrations: "no_prorations",
} as const;
/**
 * Credit behavior when quantity decreases mid-cycle.
 */
export type ListPlansVariantOnDecrease = OpenEnum<
  typeof ListPlansVariantOnDecrease
>;

/**
 * Proration settings for prepaid features. Controls mid-cycle quantity change billing.
 */
export type ListPlansVariantProration = {
  /**
   * Billing behavior when quantity increases mid-cycle.
   */
  onIncrease: ListPlansVariantOnIncrease;
  /**
   * Credit behavior when quantity decreases mid-cycle.
   */
  onDecrease: ListPlansVariantOnDecrease;
};

/**
 * When rolled over units expire.
 */
export const ListPlansVariantAddItemExpiryDurationType = {
  Month: "month",
  Forever: "forever",
} as const;
/**
 * When rolled over units expire.
 */
export type ListPlansVariantAddItemExpiryDurationType = OpenEnum<
  typeof ListPlansVariantAddItemExpiryDurationType
>;

/**
 * Rollover config for unused units. If set, unused included units carry over.
 */
export type ListPlansVariantAddItemRollover = {
  /**
   * Max rollover units. Omit for unlimited rollover.
   */
  max?: number | undefined;
  /**
   * Maximum rollover as a percentage (0-100) of included + prepaid grant. Mutually exclusive with max.
   */
  maxPercentage?: number | undefined;
  /**
   * When rolled over units expire.
   */
  expiryDurationType: ListPlansVariantAddItemExpiryDurationType;
  /**
   * Number of periods before expiry.
   */
  expiryDurationLength?: number | undefined;
};

/**
 * Overrides fields of this item's feature for customers on this plan (e.g. a credit system's credit_schema).
 */
export type ListPlansVariantAddItemFeatureOverride = {
  /**
   * For credit system features: replaces the feature's credit_schema entirely for customers on this plan.
   */
  creditSchema?: Array<any | null> | undefined;
};

/**
 * Configuration for a feature item in a plan, including usage limits, pricing, and rollover settings.
 */
export type ListPlansVariantPlanItem = {
  /**
   * The ID of the feature to configure.
   */
  featureId: string;
  /**
   * Number of free units included. Balance resets to this each interval for consumable features.
   */
  included?: number | undefined;
  /**
   * If true, customer has unlimited access to this feature.
   */
  unlimited?: boolean | undefined;
  /**
   * Whether entity-level grants contribute to a shared customer balance.
   */
  pooled: boolean;
  /**
   * Reset configuration for consumable features. Omit for non-consumable features like seats.
   */
  reset?: ListPlansVariantAddItemReset | undefined;
  /**
   * Pricing for usage beyond included units. Omit for free features.
   */
  price?: ListPlansVariantAddItemPrice | undefined;
  /**
   * Proration settings for prepaid features. Controls mid-cycle quantity change billing.
   */
  proration?: ListPlansVariantProration | undefined;
  /**
   * Rollover config for unused units. If set, unused included units carry over.
   */
  rollover?: ListPlansVariantAddItemRollover | undefined;
  /**
   * Overrides fields of this item's feature for customers on this plan (e.g. a credit system's credit_schema).
   */
  featureOverride?: ListPlansVariantAddItemFeatureOverride | undefined;
};

/**
 * Match items with this billing method (prepaid or usage_based).
 */
export const ListPlansVariantRemoveItemBillingMethod = {
  Prepaid: "prepaid",
  UsageBased: "usage_based",
} as const;
/**
 * Match items with this billing method (prepaid or usage_based).
 */
export type ListPlansVariantRemoveItemBillingMethod = OpenEnum<
  typeof ListPlansVariantRemoveItemBillingMethod
>;

export const ListPlansIntervalVariantRemoveItemEnum2 = {
  OneOff: "one_off",
  Minute: "minute",
  Hour: "hour",
  Day: "day",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
export type ListPlansIntervalVariantRemoveItemEnum2 = OpenEnum<
  typeof ListPlansIntervalVariantRemoveItemEnum2
>;

export const ListPlansIntervalVariantRemoveItemEnum1 = {
  OneOff: "one_off",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
export type ListPlansIntervalVariantRemoveItemEnum1 = OpenEnum<
  typeof ListPlansIntervalVariantRemoveItemEnum1
>;

/**
 * Match items with this interval. Accepts either a BillingInterval (price-side) or a ResetInterval (reset-side, includes day/hour/minute) so price-less items keyed by reset.interval can be disambiguated.
 */
export type ListPlansVariantIntervalUnion =
  | ListPlansIntervalVariantRemoveItemEnum1
  | ListPlansIntervalVariantRemoveItemEnum2;

/**
 * Filter for matching plan items. All provided fields must match (AND).
 */
export type ListPlansVariantPlanItemFilter = {
  /**
   * Match items linked to this feature.
   */
  featureId?: string | undefined;
  /**
   * Match items with this billing method (prepaid or usage_based).
   */
  billingMethod?: ListPlansVariantRemoveItemBillingMethod | undefined;
  /**
   * Match items with this interval. Accepts either a BillingInterval (price-side) or a ResetInterval (reset-side, includes day/hour/minute) so price-less items keyed by reset.interval can be disambiguated.
   */
  interval?:
    | ListPlansIntervalVariantRemoveItemEnum1
    | ListPlansIntervalVariantRemoveItemEnum2
    | undefined;
  /**
   * Match items with this interval_count. Disambiguates between items that share an interval but differ in count.
   */
  intervalCount?: number | undefined;
  /**
   * Match items whose grant equals this included usage. Omitted is a wildcard.
   */
  included?: number | undefined;
};

/**
 * Unit of time for the trial ('day', 'month', 'year').
 */
export const ListPlansVariantCustomizeDurationType = {
  Day: "day",
  Month: "month",
  Year: "year",
} as const;
/**
 * Unit of time for the trial ('day', 'month', 'year').
 */
export type ListPlansVariantCustomizeDurationType = OpenEnum<
  typeof ListPlansVariantCustomizeDurationType
>;

/**
 * Behavior when the trial ends. 'bill' charges the customer (default). 'revert' expires the trial and restores the customer's previous plan.
 */
export const ListPlansVariantCustomizeOnEnd = {
  Bill: "bill",
  Revert: "revert",
} as const;
/**
 * Behavior when the trial ends. 'bill' charges the customer (default). 'revert' expires the trial and restores the customer's previous plan.
 */
export type ListPlansVariantCustomizeOnEnd = OpenEnum<
  typeof ListPlansVariantCustomizeOnEnd
>;

/**
 * Free trial configuration for a plan.
 */
export type ListPlansVariantFreeTrialParams = {
  /**
   * Number of duration_type periods the trial lasts.
   */
  durationLength: number;
  /**
   * Unit of time for the trial ('day', 'month', 'year').
   */
  durationType: ListPlansVariantCustomizeDurationType;
  /**
   * If true, a payment method is required to start the trial and the customer is charged when it ends. Defaults to false.
   */
  cardRequired: boolean;
  /**
   * Behavior when the trial ends. 'bill' charges the customer (default). 'revert' expires the trial and restores the customer's previous plan.
   */
  onEnd?: ListPlansVariantCustomizeOnEnd | undefined;
};

/**
 * The time interval for the purchase limit window.
 */
export const ListPlansVariantCustomizePurchaseLimitInterval = {
  Hour: "hour",
  Day: "day",
  Week: "week",
  Month: "month",
} as const;
/**
 * The time interval for the purchase limit window.
 */
export type ListPlansVariantCustomizePurchaseLimitInterval = OpenEnum<
  typeof ListPlansVariantCustomizePurchaseLimitInterval
>;

/**
 * Optional rate limit to cap how often auto top-ups occur. Pass count to set the current window's consumed top-ups.
 */
export type ListPlansVariantCustomizePurchaseLimit = {
  /**
   * The time interval for the purchase limit window.
   */
  interval: ListPlansVariantCustomizePurchaseLimitInterval;
  /**
   * Number of intervals in the purchase limit window.
   */
  intervalCount: number;
  /**
   * Maximum number of auto top-ups allowed within the interval.
   */
  limit: number;
  /**
   * Set the current window's consumed auto top-up count. Omit to leave runtime state unchanged.
   */
  count?: number | undefined;
};

export type ListPlansVariantCustomizeAutoTopup = {
  /**
   * The ID of the feature (credit balance) to auto top-up.
   */
  featureId: string;
  /**
   * Whether auto top-up is enabled.
   */
  enabled: boolean;
  /**
   * When the balance drops below this threshold, an auto top-up will be purchased.
   */
  threshold: number;
  /**
   * Amount of credits to add per auto top-up.
   */
  quantity: number;
  /**
   * Optional rate limit to cap how often auto top-ups occur. Pass count to set the current window's consumed top-ups.
   */
  purchaseLimit?: ListPlansVariantCustomizePurchaseLimit | undefined;
  /**
   * When true, auto top-up creates a send_invoice invoice instead of auto-charging.
   */
  invoiceMode?: boolean | undefined;
};

/**
 * How overage_limit is interpreted: an absolute overage cap (default) or a percentage of the main-plan allowance.
 */
export const ListPlansVariantCustomizeLimitType = {
  Absolute: "absolute",
  UsagePercentage: "usage_percentage",
} as const;
/**
 * How overage_limit is interpreted: an absolute overage cap (default) or a percentage of the main-plan allowance.
 */
export type ListPlansVariantCustomizeLimitType = OpenEnum<
  typeof ListPlansVariantCustomizeLimitType
>;

export type ListPlansVariantCustomizeSpendLimit = {
  /**
   * Optional feature ID this spend limit applies to.
   */
  featureId?: string | undefined;
  /**
   * Whether the overage spend limit is enabled.
   */
  enabled: boolean;
  /**
   * How overage_limit is interpreted: an absolute overage cap (default) or a percentage of the main-plan allowance.
   */
  limitType?: ListPlansVariantCustomizeLimitType | undefined;
  /**
   * Overage cap for the feature: absolute units, or a percent (e.g. 120) when limit_type is usage_percentage.
   */
  overageLimit?: number | undefined;
  /**
   * When true, overage for this feature is not posted to Stripe. Usage tracking and balance resets still behave normally.
   */
  skipOverageBilling?: boolean | undefined;
};

/**
 * Interval for the cap, aligned to the customer's billing cycle.
 */
export const ListPlansVariantCustomizeUsageLimitInterval = {
  Day: "day",
  Week: "week",
  Month: "month",
  Year: "year",
} as const;
/**
 * Interval for the cap, aligned to the customer's billing cycle.
 */
export type ListPlansVariantCustomizeUsageLimitInterval = OpenEnum<
  typeof ListPlansVariantCustomizeUsageLimitInterval
>;

/**
 * Window alignment. 'billing_cycle' phases the interval to the customer's renewal time; 'utc' aligns to the UTC calendar.
 */
export const ListPlansVariantCustomizeAnchor = {
  BillingCycle: "billing_cycle",
  Utc: "utc",
} as const;
/**
 * Window alignment. 'billing_cycle' phases the interval to the customer's renewal time; 'utc' aligns to the UTC calendar.
 */
export type ListPlansVariantCustomizeAnchor = OpenEnum<
  typeof ListPlansVariantCustomizeAnchor
>;

/**
 * When set, only usage from events whose properties match counts toward this cap. Omit to count all usage of the feature.
 */
export type ListPlansVariantCustomizeUsageLimitFilter = {
  properties: { [k: string]: any };
};

export type ListPlansVariantCustomizeUsageLimit = {
  /**
   * The feature this usage limit applies to.
   */
  featureId: string;
  /**
   * Whether this usage limit is enabled.
   */
  enabled: boolean;
  /**
   * Maximum units allowed per interval.
   */
  limit: number;
  /**
   * Interval for the cap, aligned to the customer's billing cycle.
   */
  interval: ListPlansVariantCustomizeUsageLimitInterval;
  /**
   * Window alignment. 'billing_cycle' phases the interval to the customer's renewal time; 'utc' aligns to the UTC calendar.
   */
  anchor?: ListPlansVariantCustomizeAnchor | undefined;
  /**
   * When set, only usage from events whose properties match counts toward this cap. Omit to count all usage of the feature.
   */
  filter?: ListPlansVariantCustomizeUsageLimitFilter | undefined;
};

/**
 * Whether the threshold is an absolute count or a percentage of the usage allowance or remaining balance.
 */
export const ListPlansVariantCustomizeThresholdType = {
  Usage: "usage",
  UsagePercentage: "usage_percentage",
  Remaining: "remaining",
  RemainingPercentage: "remaining_percentage",
} as const;
/**
 * Whether the threshold is an absolute count or a percentage of the usage allowance or remaining balance.
 */
export type ListPlansVariantCustomizeThresholdType = OpenEnum<
  typeof ListPlansVariantCustomizeThresholdType
>;

/**
 * What 100% means. balance: every grant on the feature. included: the plan allowance only. recurring: grants that reset. usage_limit: the cap of the usage limit with the same feature and filter.
 */
export const ListPlansVariantCustomizeBasis = {
  Balance: "balance",
  Included: "included",
  Recurring: "recurring",
  UsageLimit: "usage_limit",
} as const;
/**
 * What 100% means. balance: every grant on the feature. included: the plan allowance only. recurring: grants that reset. usage_limit: the cap of the usage limit with the same feature and filter.
 */
export type ListPlansVariantCustomizeBasis = OpenEnum<
  typeof ListPlansVariantCustomizeBasis
>;

/**
 * Only valid with basis usage_limit. Points the alert at the usage limit carrying the same filter.
 */
export type ListPlansVariantCustomizeUsageAlertFilter = {
  properties: { [k: string]: any };
};

export type ListPlansVariantCustomizeUsageAlert = {
  /**
   * The feature ID this alert applies to.
   */
  featureId?: string | undefined;
  /**
   * Whether this usage alert is enabled.
   */
  enabled: boolean;
  /**
   * The threshold value that triggers the alert. For usage or remaining, this is an absolute count. For usage_percentage or remaining_percentage, this is a percentage (0-100).
   */
  threshold: number;
  /**
   * Whether the threshold is an absolute count or a percentage of the usage allowance or remaining balance.
   */
  thresholdType: ListPlansVariantCustomizeThresholdType;
  /**
   * What 100% means. balance: every grant on the feature. included: the plan allowance only. recurring: grants that reset. usage_limit: the cap of the usage limit with the same feature and filter.
   */
  basis: ListPlansVariantCustomizeBasis;
  /**
   * Only valid with basis usage_limit. Points the alert at the usage limit carrying the same filter.
   */
  filter?: ListPlansVariantCustomizeUsageAlertFilter | undefined;
  /**
   * Optional user-defined label to distinguish multiple alerts on the same feature.
   */
  name?: string | undefined;
};

export type ListPlansVariantCustomizeOverageAllowed = {
  /**
   * The feature ID this overage allowed control applies to.
   */
  featureId: string;
  /**
   * Whether overage is allowed for this feature.
   */
  enabled: boolean;
};

/**
 * Override the plan's billing controls (auto top-ups, spend limits, usage limits, usage alerts, overage allowed) for this customer.
 */
export type ListPlansVariantCustomizeBillingControls = {
  /**
   * List of auto top-up configurations per feature.
   */
  autoTopups?: Array<ListPlansVariantCustomizeAutoTopup> | undefined;
  /**
   * List of overage spend limits per feature (caps overage spend).
   */
  spendLimits?: Array<ListPlansVariantCustomizeSpendLimit> | undefined;
  /**
   * List of hard usage caps per feature (max units per interval).
   */
  usageLimits?: Array<ListPlansVariantCustomizeUsageLimit> | undefined;
  /**
   * List of usage alert configurations per feature.
   */
  usageAlerts?: Array<ListPlansVariantCustomizeUsageAlert> | undefined;
  /**
   * List of overage allowed controls per feature. When enabled, usage can exceed balance.
   */
  overageAllowed?: Array<ListPlansVariantCustomizeOverageAllowed> | undefined;
};

/**
 * Billing interval (e.g. 'month', 'year').
 */
export const VariantUpsertLicenseInterval = {
  OneOff: "one_off",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
/**
 * Billing interval (e.g. 'month', 'year').
 */
export type VariantUpsertLicenseInterval = OpenEnum<
  typeof VariantUpsertLicenseInterval
>;

/**
 * Base price configuration for a plan.
 */
export type VariantUpsertLicenseBasePrice = {
  /**
   * Base price amount for the plan, in major currency units (e.g. dollars).
   */
  amount: number;
  /**
   * Billing interval (e.g. 'month', 'year').
   */
  interval: VariantUpsertLicenseInterval;
  /**
   * Number of intervals per billing cycle. Defaults to 1.
   */
  intervalCount?: number | undefined;
  /**
   * Base price amounts in additional currencies. The base 'amount' is in the org's default currency.
   */
  additionalCurrencies?: Array<any | null> | undefined;
};

/**
 * Configuration for a feature item in a plan, including usage limits, pricing, and rollover settings.
 */
export type VariantUpsertLicensePlanItem = {
  featureId?: any | undefined;
  included?: any | undefined;
  unlimited?: any | undefined;
  pooled?: any | undefined;
  reset?: any | undefined;
  price?: any | undefined;
  proration?: any | undefined;
  rollover?: any | undefined;
  featureOverride?: any | undefined;
  entityFeatureId?: any | undefined;
  entitlementId?: any | undefined;
  priceId?: any | undefined;
};

/**
 * Filter for matching plan items. All provided fields must match (AND).
 */
export type VariantUpsertLicensePlanItemFilter = {
  featureId?: any | undefined;
  billingMethod?: any | undefined;
  interval?: any | undefined;
  intervalCount?: any | undefined;
  included?: any | undefined;
};

export type VariantUpsertLicenseCustomize = {
  price?: VariantUpsertLicenseBasePrice | null | undefined;
  addItems?: Array<VariantUpsertLicensePlanItem> | undefined;
  removeItems?: Array<VariantUpsertLicensePlanItemFilter> | undefined;
};

export type VariantUpsertLicense = {
  licensePlanId: string;
  versionSlug?: string | undefined;
  included?: number | undefined;
  prepaidOnly?: boolean | undefined;
  customize?: VariantUpsertLicenseCustomize | null | undefined;
  metadata?: { [k: string]: any } | undefined;
};

export type VariantRemoveLicense = {
  licensePlanId: string;
};

/**
 * The variant's declared divergence from its base plan — exactly what you would re-submit to recreate it.
 */
export type ListPlansVariantCustomize = {
  /**
   * Override the base price of the plan. Pass null to remove the base price.
   */
  price?: ListPlansVariantBasePrice | null | undefined;
  /**
   * Items to add to the plan.
   */
  addItems?: Array<ListPlansVariantPlanItem> | undefined;
  /**
   * Filters selecting items to remove from the plan.
   */
  removeItems?: Array<ListPlansVariantPlanItemFilter> | undefined;
  /**
   * Override the plan's default free trial. Pass an object to set a custom trial, or null to remove the trial entirely.
   */
  freeTrial?: ListPlansVariantFreeTrialParams | null | undefined;
  /**
   * Override the plan's billing controls (auto top-ups, spend limits, usage limits, usage alerts, overage allowed) for this customer.
   */
  billingControls?: ListPlansVariantCustomizeBillingControls | undefined;
  /**
   * License links to add or override for this customer, keyed by license_plan_id. Omitted fields inherit the plan catalog link (included defaults to 1 when the license is not in the catalog). A bare entry restores the license to pure catalog inheritance.
   */
  upsertLicenses?: Array<VariantUpsertLicense> | undefined;
  /**
   * License links to drop, keyed by license_plan_id. Parallel to remove_items.
   */
  removeLicenses?: Array<VariantRemoveLicense> | undefined;
};

export type VariantPlanAdditionalCurrency = {
  /**
   * Three-letter Stripe-supported currency code (e.g. 'eur', 'gbp').
   */
  currency: string;
  /**
   * Price amount in this currency. Set explicitly per currency, not converted from the base amount.
   */
  amount: number;
};

/**
 * Billing interval (e.g. 'month', 'year').
 */
export const PriceVariantPlanInterval = {
  OneOff: "one_off",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
/**
 * Billing interval (e.g. 'month', 'year').
 */
export type PriceVariantPlanInterval = OpenEnum<
  typeof PriceVariantPlanInterval
>;

/**
 * Display text for showing this price in pricing pages.
 */
export type PriceVariantDisplay = {
  /**
   * Main display text (e.g. '$10' or '100 messages').
   */
  primaryText: string;
  /**
   * Secondary display text (e.g. 'per month' or 'then $0.5 per 100').
   */
  secondaryText?: string | undefined;
};

export type PriceVariantStripe = {
  /**
   * Stripe price ID. For prepaid with included > 0 this is the V2 price.
   */
  priceId: string;
};

/**
 * Payment processors this base price is connected to. Omitted when unset.
 */
export type PriceVariantProcessors = {
  stripe?: PriceVariantStripe | null | undefined;
};

export type VariantPlanPrice = {
  /**
   * Base price amount for the plan, in major currency units (e.g. dollars).
   */
  amount: number;
  /**
   * Base price amounts in additional currencies. The base 'amount' is in the org's default currency.
   */
  additionalCurrencies?: Array<VariantPlanAdditionalCurrency> | undefined;
  /**
   * Billing interval (e.g. 'month', 'year').
   */
  interval: PriceVariantPlanInterval;
  /**
   * Number of intervals per billing cycle. Defaults to 1.
   */
  intervalCount?: number | undefined;
  /**
   * Display text for showing this price in pricing pages.
   */
  display?: PriceVariantDisplay | undefined;
  /**
   * Payment processors this base price is connected to. Omitted when unset.
   */
  processors?: PriceVariantProcessors | undefined;
};

/**
 * The type of the feature
 */
export const VariantType = {
  Static: "static",
  Boolean: "boolean",
  SingleUse: "single_use",
  ContinuousUse: "continuous_use",
  CreditSystem: "credit_system",
  AiCreditSystem: "ai_credit_system",
} as const;
/**
 * The type of the feature
 */
export type VariantType = OpenEnum<typeof VariantType>;

export type VariantFeatureDisplay = {
  /**
   * The singular display name for the feature.
   */
  singular: string;
  /**
   * The plural display name for the feature.
   */
  plural: string;
};

export type ListPlansVariantCreditSchema = {
  meteredFeatureId?: any | undefined;
  creditCost?: any | undefined;
};

/**
 * The full feature object if expanded.
 */
export type VariantFeature = {
  /**
   * The ID of the feature, used to refer to it in other API calls like /track or /check.
   */
  id: string;
  /**
   * The name of the feature.
   */
  name?: string | null | undefined;
  /**
   * The type of the feature
   */
  type: VariantType;
  /**
   * Singular and plural display names for the feature.
   */
  display?: VariantFeatureDisplay | null | undefined;
  /**
   * Credit cost schema for credit system features.
   */
  creditSchema?: Array<ListPlansVariantCreditSchema> | null | undefined;
  /**
   * Whether or not the feature is archived.
   */
  archived?: boolean | null | undefined;
};

/**
 * The interval at which the feature balance resets (e.g. 'month', 'year'). For consumable features, usage resets to 0 and included units are restored.
 */
export const ResetVariantItemInterval = {
  OneOff: "one_off",
  Minute: "minute",
  Hour: "hour",
  Day: "day",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
/**
 * The interval at which the feature balance resets (e.g. 'month', 'year'). For consumable features, usage resets to 0 and included units are restored.
 */
export type ResetVariantItemInterval = OpenEnum<
  typeof ResetVariantItemInterval
>;

export type VariantPlanReset = {
  /**
   * The interval at which the feature balance resets (e.g. 'month', 'year'). For consumable features, usage resets to 0 and included units are restored.
   */
  interval: ResetVariantItemInterval;
  /**
   * Number of intervals between resets. Defaults to 1.
   */
  intervalCount?: number | undefined;
};

export type VariantItemAdditionalCurrency = {
  currency?: any | undefined;
  amount?: any | undefined;
};

export type VariantPlanTier = {
  to?: any | undefined;
  amount?: any | undefined;
  flatAmount?: any | undefined;
  additionalCurrencies?: any | undefined;
};

export const VariantPlanTierBehavior = {
  Graduated: "graduated",
  Volume: "volume",
} as const;
export type VariantPlanTierBehavior = OpenEnum<typeof VariantPlanTierBehavior>;

/**
 * Billing interval for this price. For consumable features, should match reset.interval.
 */
export const PriceVariantItemInterval = {
  OneOff: "one_off",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
/**
 * Billing interval for this price. For consumable features, should match reset.interval.
 */
export type PriceVariantItemInterval = OpenEnum<
  typeof PriceVariantItemInterval
>;

/**
 * 'prepaid' for features like seats where customers pay upfront, 'usage_based' for pay-as-you-go after included usage.
 */
export const VariantItemBillingMethod = {
  Prepaid: "prepaid",
  UsageBased: "usage_based",
} as const;
/**
 * 'prepaid' for features like seats where customers pay upfront, 'usage_based' for pay-as-you-go after included usage.
 */
export type VariantItemBillingMethod = OpenEnum<
  typeof VariantItemBillingMethod
>;

export type VariantItemStripe = {
  priceId?: any | undefined;
};

/**
 * Payment processors this item price is connected to. Omitted when unset.
 */
export type VariantItemProcessors = {
  stripe?: VariantItemStripe | null | undefined;
};

export type VariantItemPrice = {
  /**
   * Price per billing_units after included usage is consumed. Mutually exclusive with tiers.
   */
  amount?: number | undefined;
  /**
   * Amounts in additional currencies for this flat price. The base 'amount' is in the org's default currency. Only valid with 'amount', not 'tiers' (tiered prices carry per-currency amounts on each tier).
   */
  additionalCurrencies?: Array<VariantItemAdditionalCurrency> | undefined;
  /**
   * Tiered pricing configuration. Each tier's 'to' INCLUDES the included amount. Either 'tiers' or 'amount' is required.
   */
  tiers?: Array<VariantPlanTier> | undefined;
  tierBehavior?: VariantPlanTierBehavior | undefined;
  /**
   * Billing interval for this price. For consumable features, should match reset.interval.
   */
  interval: PriceVariantItemInterval;
  /**
   * Number of intervals per billing cycle. Defaults to 1.
   */
  intervalCount?: number | undefined;
  /**
   * Number of units per price increment. Usage is rounded UP to the nearest billing_units when billed (e.g. billing_units=100 means 101 usage rounds to 200).
   */
  billingUnits: number;
  /**
   * 'prepaid' for features like seats where customers pay upfront, 'usage_based' for pay-as-you-go after included usage.
   */
  billingMethod: VariantItemBillingMethod;
  /**
   * Maximum units a customer can purchase beyond included. E.g. if included=100 and max_purchase=300, customer can use up to 400 total before usage is capped. Null for no limit.
   */
  maxPurchase: number | null;
  /**
   * Payment processors this item price is connected to. Omitted when unset.
   */
  processors?: VariantItemProcessors | undefined;
};

/**
 * Display text for showing this item in pricing pages.
 */
export type VariantItemDisplay = {
  /**
   * Main display text (e.g. '$10' or '100 messages').
   */
  primaryText: string;
  /**
   * Secondary display text (e.g. 'per month' or 'then $0.5 per 100').
   */
  secondaryText?: string | undefined;
};

/**
 * When rolled over units expire.
 */
export const VariantPlanExpiryDurationType = {
  Month: "month",
  Forever: "forever",
} as const;
/**
 * When rolled over units expire.
 */
export type VariantPlanExpiryDurationType = OpenEnum<
  typeof VariantPlanExpiryDurationType
>;

/**
 * Rollover configuration for unused units. If set, unused included units roll over to the next period.
 */
export type VariantPlanRollover = {
  /**
   * Maximum rollover units. Null for unlimited rollover.
   */
  max: number | null;
  /**
   * Maximum rollover as a percentage (0-100) of included + prepaid grant. Mutually exclusive with max.
   */
  maxPercentage?: number | null | undefined;
  /**
   * When rolled over units expire.
   */
  expiryDurationType: VariantPlanExpiryDurationType;
  /**
   * Number of periods before expiry.
   */
  expiryDurationLength?: number | undefined;
};

/**
 * Overrides fields of this item's feature for customers on this plan (e.g. a credit system's credit_schema).
 */
export type VariantPlanFeatureOverride = {
  /**
   * For credit system features: replaces the feature's credit_schema entirely for customers on this plan.
   */
  creditSchema?: Array<any | null> | undefined;
};

export type VariantItem = {
  /**
   * The ID of the feature this item configures.
   */
  featureId: string;
  /**
   * The full feature object if expanded.
   */
  feature?: VariantFeature | undefined;
  /**
   * Number of free units included. For consumable features, balance resets to this number each interval.
   */
  included: number;
  /**
   * Whether the customer has unlimited access to this feature.
   */
  unlimited: boolean;
  /**
   * Whether entity-level grants contribute to a shared customer balance.
   */
  pooled: boolean;
  /**
   * Reset configuration for consumable features. Null for non-consumable features like seats where usage persists across billing cycles.
   */
  reset: VariantPlanReset | null;
  /**
   * Pricing configuration for usage beyond included units. Null if feature is entirely free.
   */
  price: VariantItemPrice | null;
  /**
   * Display text for showing this item in pricing pages.
   */
  display?: VariantItemDisplay | undefined;
  /**
   * Rollover configuration for unused units. If set, unused included units roll over to the next period.
   */
  rollover?: VariantPlanRollover | undefined;
  /**
   * Overrides fields of this item's feature for customers on this plan (e.g. a credit system's credit_schema).
   */
  featureOverride?: VariantPlanFeatureOverride | undefined;
};

export type VariantStripe = {
  /**
   * Stripe product ID this plan is billed under.
   */
  productId: string;
  /**
   * Extra Stripe product IDs aliased to this plan.
   */
  additionalProductIds?: Array<string> | undefined;
};

export type VariantProduct = {
  /**
   * RevenueCat product ID that grants this plan when purchased.
   */
  productId: string;
  /**
   * Prepaid quantities granted when this specific RevenueCat product is purchased, in feature units.
   */
  featureQuantities?: Array<any | null> | undefined;
};

export type VariantRevenuecat = {
  /**
   * Every RevenueCat product that maps to this plan. Replaces the current set.
   */
  products: Array<VariantProduct>;
};

/**
 * Payment processors this plan is connected to. Omitted when unset.
 */
export type VariantProcessors = {
  stripe?: VariantStripe | null | undefined;
  revenuecat?: VariantRevenuecat | null | undefined;
};

/**
 * Unit of time for the trial duration ('day', 'month', 'year').
 */
export const VariantPlanDurationType = {
  Day: "day",
  Month: "month",
  Year: "year",
} as const;
/**
 * Unit of time for the trial duration ('day', 'month', 'year').
 */
export type VariantPlanDurationType = OpenEnum<typeof VariantPlanDurationType>;

export const VariantPlanOnEnd = {
  Bill: "bill",
  Revert: "revert",
} as const;
export type VariantPlanOnEnd = OpenEnum<typeof VariantPlanOnEnd>;

/**
 * Free trial configuration. If set, new customers can try this plan before being charged.
 */
export type VariantFreeTrial = {
  /**
   * Number of duration_type periods the trial lasts.
   */
  durationLength: number;
  /**
   * Unit of time for the trial duration ('day', 'month', 'year').
   */
  durationType: VariantPlanDurationType;
  /**
   * Whether a payment method is required to start the trial. If true, customer will be charged after trial ends.
   */
  cardRequired: boolean;
  /**
   * Behavior when the trial ends. 'bill' charges the customer (default). 'revert' expires the trial and restores the customer's previous plan.
   */
  onEnd?: VariantPlanOnEnd | null | undefined;
};

/**
 * Environment this plan belongs to ('sandbox' or 'live').
 */
export const VariantEnv = {
  Sandbox: "sandbox",
  Live: "live",
} as const;
/**
 * Environment this plan belongs to ('sandbox' or 'live').
 */
export type VariantEnv = OpenEnum<typeof VariantEnv>;

/**
 * Miscellaneous plan-level configuration flags.
 */
export type VariantConfig = {
  /**
   * If true, entitlements attached to this plan will still reset on schedule even when the customer's product is in a past_due state.
   */
  ignorePastDue: boolean;
};

/**
 * The time interval for the purchase limit window.
 */
export const VariantPlanPurchaseLimitInterval = {
  Hour: "hour",
  Day: "day",
  Week: "week",
  Month: "month",
} as const;
/**
 * The time interval for the purchase limit window.
 */
export type VariantPlanPurchaseLimitInterval = OpenEnum<
  typeof VariantPlanPurchaseLimitInterval
>;

/**
 * Optional rate limit to cap how often auto top-ups occur.
 */
export type VariantPlanPurchaseLimit = {
  /**
   * The time interval for the purchase limit window.
   */
  interval: VariantPlanPurchaseLimitInterval;
  /**
   * Number of intervals in the purchase limit window.
   */
  intervalCount: number;
  /**
   * Maximum number of auto top-ups allowed within the interval.
   */
  limit: number;
};

export type VariantPlanAutoTopup = {
  /**
   * The ID of the feature (credit balance) to auto top-up.
   */
  featureId: string;
  /**
   * Whether auto top-up is enabled.
   */
  enabled: boolean;
  /**
   * When the balance drops below this threshold, an auto top-up will be purchased.
   */
  threshold: number;
  /**
   * Amount of credits to add per auto top-up.
   */
  quantity: number;
  /**
   * Optional rate limit to cap how often auto top-ups occur.
   */
  purchaseLimit?: VariantPlanPurchaseLimit | undefined;
  /**
   * When true, auto top-up creates a send_invoice invoice instead of auto-charging.
   */
  invoiceMode?: boolean | undefined;
};

/**
 * How overage_limit is interpreted: an absolute overage cap (default) or a percentage of the main-plan allowance.
 */
export const VariantPlanLimitType = {
  Absolute: "absolute",
  UsagePercentage: "usage_percentage",
} as const;
/**
 * How overage_limit is interpreted: an absolute overage cap (default) or a percentage of the main-plan allowance.
 */
export type VariantPlanLimitType = OpenEnum<typeof VariantPlanLimitType>;

export type VariantPlanSpendLimit = {
  /**
   * Optional feature ID this spend limit applies to.
   */
  featureId?: string | undefined;
  /**
   * Whether the overage spend limit is enabled.
   */
  enabled: boolean;
  /**
   * How overage_limit is interpreted: an absolute overage cap (default) or a percentage of the main-plan allowance.
   */
  limitType?: VariantPlanLimitType | undefined;
  /**
   * Overage cap for the feature: absolute units, or a percent (e.g. 120) when limit_type is usage_percentage.
   */
  overageLimit?: number | undefined;
  /**
   * When true, overage for this feature is not posted to Stripe. Usage tracking and balance resets still behave normally.
   */
  skipOverageBilling?: boolean | undefined;
};

/**
 * Interval for the cap, aligned to the customer's billing cycle.
 */
export const VariantPlanUsageLimitInterval = {
  Day: "day",
  Week: "week",
  Month: "month",
  Year: "year",
} as const;
/**
 * Interval for the cap, aligned to the customer's billing cycle.
 */
export type VariantPlanUsageLimitInterval = OpenEnum<
  typeof VariantPlanUsageLimitInterval
>;

/**
 * Window alignment. 'billing_cycle' phases the interval to the customer's renewal time; 'utc' aligns to the UTC calendar.
 */
export const VariantPlanAnchor = {
  BillingCycle: "billing_cycle",
  Utc: "utc",
} as const;
/**
 * Window alignment. 'billing_cycle' phases the interval to the customer's renewal time; 'utc' aligns to the UTC calendar.
 */
export type VariantPlanAnchor = OpenEnum<typeof VariantPlanAnchor>;

/**
 * When set, only usage from events whose properties match counts toward this cap. Omit to count all usage of the feature.
 */
export type VariantPlanUsageLimitFilter = {
  properties: { [k: string]: any };
};

export type VariantPlanUsageLimit = {
  /**
   * The feature this usage limit applies to.
   */
  featureId: string;
  /**
   * Whether this usage limit is enabled.
   */
  enabled: boolean;
  /**
   * Maximum units allowed per interval.
   */
  limit: number;
  /**
   * Interval for the cap, aligned to the customer's billing cycle.
   */
  interval: VariantPlanUsageLimitInterval;
  /**
   * Window alignment. 'billing_cycle' phases the interval to the customer's renewal time; 'utc' aligns to the UTC calendar.
   */
  anchor?: VariantPlanAnchor | undefined;
  /**
   * When set, only usage from events whose properties match counts toward this cap. Omit to count all usage of the feature.
   */
  filter?: VariantPlanUsageLimitFilter | undefined;
};

/**
 * Whether the threshold is an absolute count or a percentage of the usage allowance or remaining balance.
 */
export const VariantPlanThresholdType = {
  Usage: "usage",
  UsagePercentage: "usage_percentage",
  Remaining: "remaining",
  RemainingPercentage: "remaining_percentage",
} as const;
/**
 * Whether the threshold is an absolute count or a percentage of the usage allowance or remaining balance.
 */
export type VariantPlanThresholdType = OpenEnum<
  typeof VariantPlanThresholdType
>;

/**
 * What 100% means. balance: every grant on the feature. included: the plan allowance only. recurring: grants that reset. usage_limit: the cap of the usage limit with the same feature and filter.
 */
export const VariantPlanBasis = {
  Balance: "balance",
  Included: "included",
  Recurring: "recurring",
  UsageLimit: "usage_limit",
} as const;
/**
 * What 100% means. balance: every grant on the feature. included: the plan allowance only. recurring: grants that reset. usage_limit: the cap of the usage limit with the same feature and filter.
 */
export type VariantPlanBasis = OpenEnum<typeof VariantPlanBasis>;

/**
 * Only valid with basis usage_limit. Points the alert at the usage limit carrying the same filter.
 */
export type VariantPlanUsageAlertFilter = {
  properties: { [k: string]: any };
};

export type VariantPlanUsageAlert = {
  /**
   * The feature ID this alert applies to.
   */
  featureId?: string | undefined;
  /**
   * Whether this usage alert is enabled.
   */
  enabled: boolean;
  /**
   * The threshold value that triggers the alert. For usage or remaining, this is an absolute count. For usage_percentage or remaining_percentage, this is a percentage (0-100).
   */
  threshold: number;
  /**
   * Whether the threshold is an absolute count or a percentage of the usage allowance or remaining balance.
   */
  thresholdType: VariantPlanThresholdType;
  /**
   * What 100% means. balance: every grant on the feature. included: the plan allowance only. recurring: grants that reset. usage_limit: the cap of the usage limit with the same feature and filter.
   */
  basis: VariantPlanBasis;
  /**
   * Only valid with basis usage_limit. Points the alert at the usage limit carrying the same filter.
   */
  filter?: VariantPlanUsageAlertFilter | undefined;
  /**
   * Optional user-defined label to distinguish multiple alerts on the same feature.
   */
  name?: string | undefined;
};

export type VariantPlanOverageAllowed = {
  /**
   * The feature ID this overage allowed control applies to.
   */
  featureId: string;
  /**
   * Whether overage is allowed for this feature.
   */
  enabled: boolean;
};

/**
 * Plan-level billing controls used as customer defaults.
 */
export type VariantPlanBillingControls = {
  /**
   * List of auto top-up configurations per feature.
   */
  autoTopups?: Array<VariantPlanAutoTopup> | undefined;
  /**
   * List of overage spend limits per feature (caps overage spend).
   */
  spendLimits?: Array<VariantPlanSpendLimit> | undefined;
  /**
   * List of hard usage caps per feature (max units per interval).
   */
  usageLimits?: Array<VariantPlanUsageLimit> | undefined;
  /**
   * List of usage alert configurations per feature.
   */
  usageAlerts?: Array<VariantPlanUsageAlert> | undefined;
  /**
   * List of overage allowed controls per feature. When enabled, usage can exceed balance.
   */
  overageAllowed?: Array<VariantPlanOverageAllowed> | undefined;
};

/**
 * The customer's current status with this plan. 'active' if attached, 'scheduled' if pending activation.
 */
export const VariantStatus = {
  Active: "active",
  Scheduled: "scheduled",
} as const;
/**
 * The customer's current status with this plan. 'active' if attached, 'scheduled' if pending activation.
 */
export type VariantStatus = OpenEnum<typeof VariantStatus>;

/**
 * The action that would occur if this plan were attached to the customer.
 */
export const VariantAttachAction = {
  Activate: "activate",
  Upgrade: "upgrade",
  Downgrade: "downgrade",
  None: "none",
  Purchase: "purchase",
} as const;
/**
 * The action that would occur if this plan were attached to the customer.
 */
export type VariantAttachAction = OpenEnum<typeof VariantAttachAction>;

export type VariantCustomerEligibility = {
  /**
   * Whether the trial on this plan is available to this customer. For example, if the customer used the trial in the past, this will be false.
   */
  trialAvailable?: boolean | undefined;
  /**
   * The customer's current status with this plan. 'active' if attached, 'scheduled' if pending activation.
   */
  status?: VariantStatus | undefined;
  /**
   * Whether the customer's active instance of this plan is set to cancel.
   */
  canceling?: boolean | undefined;
  /**
   * Whether the customer is currently on a free trial of this plan.
   */
  trialing?: boolean | undefined;
  /**
   * The action that would occur if this plan were attached to the customer.
   */
  attachAction: VariantAttachAction;
};

/**
 * The variant's fully resolved plan (base + customize applied). Present when variants are expanded.
 */
export type ListPlansPlan = {
  /**
   * Unique identifier for the plan.
   */
  id: string;
  /**
   * Display name of the plan.
   */
  name: string;
  /**
   * Optional description of the plan.
   */
  description: string | null;
  /**
   * Group identifier for organizing related plans. Plans in the same group are mutually exclusive.
   */
  group: string | null;
  /**
   * Version number of the plan. Incremented when plan configuration changes.
   */
  version: number;
  /**
   * User-facing version identity. Defaults to v{n} when the version is minted.
   */
  versionSlug?: string | null | undefined;
  /**
   * Whether this is the active version of the plan. At most one version is active.
   */
  active?: boolean | undefined;
  /**
   * Whether this is an add-on plan that can be attached alongside a main plan.
   */
  addOn: boolean;
  /**
   * If true, this plan is automatically attached when a customer is created. Used for free plans.
   */
  autoEnable: boolean;
  /**
   * Base recurring price for the plan. Null for free plans or usage-only plans.
   */
  price: VariantPlanPrice | null;
  /**
   * Feature configurations included in this plan. Each item defines included units, pricing, and reset behavior for a feature.
   */
  items: Array<VariantItem>;
  /**
   * Payment processors this plan is connected to. Omitted when unset.
   */
  processors?: VariantProcessors | undefined;
  /**
   * Free trial configuration. If set, new customers can try this plan before being charged.
   */
  freeTrial?: VariantFreeTrial | undefined;
  /**
   * Unix timestamp (ms) when the plan was created.
   */
  createdAt: number;
  /**
   * Environment this plan belongs to ('sandbox' or 'live').
   */
  env: VariantEnv;
  /**
   * Whether the plan is archived. Archived plans cannot be attached to new customers.
   */
  archived: boolean;
  /**
   * Miscellaneous plan-level configuration flags.
   */
  config: VariantConfig;
  /**
   * Plan-level billing controls used as customer defaults.
   */
  billingControls?: VariantPlanBillingControls | undefined;
  /**
   * Arbitrary key-value metadata defined by you for your own use. Shared across all versions of the plan.
   */
  metadata: { [k: string]: any };
  customerEligibility?: VariantCustomerEligibility | undefined;
  /**
   * Deprecated. Use variant_details.base_plan_id instead. If this is a variant, the ID of the base plan it was created from.
   */
  baseVariantId: string | null;
};

export type ListPlansVariant = {
  /**
   * The plan ID of the variant derived from this base plan.
   */
  variantPlanId: string;
  /**
   * Display name of the variant plan.
   */
  name: string;
  /**
   * The variant's declared divergence from its base plan — exactly what you would re-submit to recreate it.
   */
  customize?: ListPlansVariantCustomize | undefined;
  /**
   * The variant's fully resolved plan (base + customize applied). Present when variants are expanded.
   */
  plan?: ListPlansPlan | undefined;
};

/**
 * A plan defines features, pricing, entitlements, assignable licenses, and derived variants.
 */
export type ListPlansList = {
  /**
   * Unique identifier for the plan.
   */
  id: string;
  /**
   * Display name of the plan.
   */
  name: string;
  /**
   * Optional description of the plan.
   */
  description: string | null;
  /**
   * Group identifier for organizing related plans. Plans in the same group are mutually exclusive.
   */
  group: string | null;
  /**
   * Version number of the plan. Incremented when plan configuration changes.
   */
  version: number;
  /**
   * User-facing version identity. Defaults to v{n} when the version is minted.
   */
  versionSlug?: string | null | undefined;
  /**
   * Whether this is the active version of the plan. At most one version is active.
   */
  active?: boolean | undefined;
  /**
   * Whether this is an add-on plan that can be attached alongside a main plan.
   */
  addOn: boolean;
  /**
   * If true, this plan is automatically attached when a customer is created. Used for free plans.
   */
  autoEnable: boolean;
  /**
   * Base recurring price for the plan. Null for free plans or usage-only plans.
   */
  price: ListPlansPrice | null;
  /**
   * Feature configurations included in this plan. Each item defines included units, pricing, and reset behavior for a feature.
   */
  items: Array<ListPlansItem>;
  /**
   * Payment processors this plan is connected to. Omitted when unset.
   */
  processors?: ListPlansProcessors | undefined;
  /**
   * Free trial configuration. If set, new customers can try this plan before being charged.
   */
  freeTrial?: ListPlansFreeTrial | undefined;
  /**
   * Unix timestamp (ms) when the plan was created.
   */
  createdAt: number;
  /**
   * Environment this plan belongs to ('sandbox' or 'live').
   */
  env: ListPlansEnv;
  /**
   * Whether the plan is archived. Archived plans cannot be attached to new customers.
   */
  archived: boolean;
  /**
   * Miscellaneous plan-level configuration flags.
   */
  config: ListPlansConfig;
  /**
   * Plan-level billing controls used as customer defaults.
   */
  billingControls?: ListPlansBillingControls | undefined;
  /**
   * Arbitrary key-value metadata defined by you for your own use. Shared across all versions of the plan.
   */
  metadata: { [k: string]: any };
  customerEligibility?: ListPlansCustomerEligibility | undefined;
  /**
   * Deprecated. Use variant_details.base_plan_id instead. If this is a variant, the ID of the base plan it was created from.
   */
  baseVariantId: string | null;
  /**
   * Details about how this variant relates to its latest base plan.
   */
  variantDetails?: ListPlansVariantDetails | undefined;
  /**
   * Plans offered as assignable licenses under this plan. Omitted when the plan has none.
   */
  licenses?: Array<ListPlansLicense> | undefined;
  /**
   * Variant plans derived from this base plan. Omitted when the plan has none.
   */
  variants?: Array<ListPlansVariant> | undefined;
};

/**
 * OK
 */
export type ListPlansResponse = {
  list: Array<ListPlansList>;
};

/** @internal */
export const ListPlansVariantAddItemReset$inboundSchema: z.ZodMiniType<
  ListPlansVariantAddItemReset,
  unknown
> = z.pipe(
  z.object({
    interval: ListPlansVariantAddItemResetInterval$inboundSchema,
    interval_count: types.optional(types.number()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "interval_count": "intervalCount",
    });
  }),
);

export function listPlansVariantAddItemResetFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansVariantAddItemReset, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansVariantAddItemReset$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansVariantAddItemReset' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantAddItemAdditionalCurrency$inboundSchema:
  z.ZodMiniType<ListPlansVariantAddItemAdditionalCurrency, unknown> = z.object({
    currency: types.optional(z.any()),
    amount: types.optional(z.any()),
  });

export function listPlansVariantAddItemAdditionalCurrencyFromJSON(
  jsonString: string,
): SafeParseResult<
  ListPlansVariantAddItemAdditionalCurrency,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      ListPlansVariantAddItemAdditionalCurrency$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'ListPlansVariantAddItemAdditionalCurrency' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantAddItemTier$inboundSchema: z.ZodMiniType<
  ListPlansVariantAddItemTier,
  unknown
> = z.pipe(
  z.object({
    to: types.optional(z.any()),
    amount: types.optional(z.any()),
    flat_amount: types.optional(z.any()),
    additional_currencies: types.optional(z.any()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "flat_amount": "flatAmount",
      "additional_currencies": "additionalCurrencies",
    });
  }),
);

export function listPlansVariantAddItemTierFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansVariantAddItemTier, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansVariantAddItemTier$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansVariantAddItemTier' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantAddItemTierBehavior$inboundSchema: z.ZodMiniType<
  ListPlansVariantAddItemTierBehavior,
  unknown
> = openEnums.inboundSchema(ListPlansVariantAddItemTierBehavior);

/** @internal */
export const ListPlansVariantAddItemPriceInterval$inboundSchema: z.ZodMiniType<
  ListPlansVariantAddItemPriceInterval,
  unknown
> = openEnums.inboundSchema(ListPlansVariantAddItemPriceInterval);

/** @internal */
export const ListPlansVariantAddItemBillingMethod$inboundSchema: z.ZodMiniType<
  ListPlansVariantAddItemBillingMethod,
  unknown
> = openEnums.inboundSchema(ListPlansVariantAddItemBillingMethod);

/** @internal */
export const ListPlansVariantAddItemPrice$inboundSchema: z.ZodMiniType<
  ListPlansVariantAddItemPrice,
  unknown
> = z.pipe(
  z.object({
    amount: types.optional(types.number()),
    additional_currencies: types.optional(z.array(z.lazy(() =>
      ListPlansVariantAddItemAdditionalCurrency$inboundSchema
    ))),
    tiers: types.optional(z.array(z.lazy(() =>
      ListPlansVariantAddItemTier$inboundSchema
    ))),
    tier_behavior: types.optional(
      ListPlansVariantAddItemTierBehavior$inboundSchema,
    ),
    interval: ListPlansVariantAddItemPriceInterval$inboundSchema,
    interval_count: z._default(types.number(), 1),
    billing_units: z._default(types.number(), 1),
    billing_method: ListPlansVariantAddItemBillingMethod$inboundSchema,
    max_purchase: z.optional(z.nullable(types.number())),
  }),
  z.transform((v) => {
    return remap$(v, {
      "additional_currencies": "additionalCurrencies",
      "tier_behavior": "tierBehavior",
      "interval_count": "intervalCount",
      "billing_units": "billingUnits",
      "billing_method": "billingMethod",
      "max_purchase": "maxPurchase",
    });
  }),
);

export function listPlansVariantAddItemPriceFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansVariantAddItemPrice, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansVariantAddItemPrice$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansVariantAddItemPrice' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantOnIncrease$inboundSchema: z.ZodMiniType<
  ListPlansVariantOnIncrease,
  unknown
> = openEnums.inboundSchema(ListPlansVariantOnIncrease);

/** @internal */
export const ListPlansVariantOnDecrease$inboundSchema: z.ZodMiniType<
  ListPlansVariantOnDecrease,
  unknown
> = openEnums.inboundSchema(ListPlansVariantOnDecrease);

/** @internal */
export const ListPlansVariantProration$inboundSchema: z.ZodMiniType<
  ListPlansVariantProration,
  unknown
> = z.pipe(
  z.object({
    on_increase: ListPlansVariantOnIncrease$inboundSchema,
    on_decrease: ListPlansVariantOnDecrease$inboundSchema,
  }),
  z.transform((v) => {
    return remap$(v, {
      "on_increase": "onIncrease",
      "on_decrease": "onDecrease",
    });
  }),
);

export function listPlansVariantProrationFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansVariantProration, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansVariantProration$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansVariantProration' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantAddItemExpiryDurationType$inboundSchema:
  z.ZodMiniType<ListPlansVariantAddItemExpiryDurationType, unknown> = openEnums
    .inboundSchema(ListPlansVariantAddItemExpiryDurationType);

/** @internal */
export const ListPlansVariantAddItemRollover$inboundSchema: z.ZodMiniType<
  ListPlansVariantAddItemRollover,
  unknown
> = z.pipe(
  z.object({
    max: types.optional(types.number()),
    max_percentage: types.optional(types.number()),
    expiry_duration_type:
      ListPlansVariantAddItemExpiryDurationType$inboundSchema,
    expiry_duration_length: types.optional(types.number()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "max_percentage": "maxPercentage",
      "expiry_duration_type": "expiryDurationType",
      "expiry_duration_length": "expiryDurationLength",
    });
  }),
);

export function listPlansVariantAddItemRolloverFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansVariantAddItemRollover, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansVariantAddItemRollover$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansVariantAddItemRollover' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantAddItemFeatureOverride$inboundSchema:
  z.ZodMiniType<ListPlansVariantAddItemFeatureOverride, unknown> = z.pipe(
    z.object({
      credit_schema: types.optional(z.array(types.nullable(z.any()))),
    }),
    z.transform((v) => {
      return remap$(v, {
        "credit_schema": "creditSchema",
      });
    }),
  );

export function listPlansVariantAddItemFeatureOverrideFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansVariantAddItemFeatureOverride, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      ListPlansVariantAddItemFeatureOverride$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansVariantAddItemFeatureOverride' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantPlanItem$inboundSchema: z.ZodMiniType<
  ListPlansVariantPlanItem,
  unknown
> = z.pipe(
  z.object({
    feature_id: types.string(),
    included: types.optional(types.number()),
    unlimited: types.optional(types.boolean()),
    pooled: z._default(types.boolean(), false),
    reset: types.optional(
      z.lazy(() => ListPlansVariantAddItemReset$inboundSchema),
    ),
    price: types.optional(
      z.lazy(() => ListPlansVariantAddItemPrice$inboundSchema),
    ),
    proration: types.optional(
      z.lazy(() => ListPlansVariantProration$inboundSchema),
    ),
    rollover: types.optional(
      z.lazy(() => ListPlansVariantAddItemRollover$inboundSchema),
    ),
    feature_override: types.optional(
      z.lazy(() => ListPlansVariantAddItemFeatureOverride$inboundSchema),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "feature_id": "featureId",
      "feature_override": "featureOverride",
    });
  }),
);

export function listPlansVariantPlanItemFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansVariantPlanItem, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansVariantPlanItem$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansVariantPlanItem' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantRemoveItemBillingMethod$inboundSchema:
  z.ZodMiniType<ListPlansVariantRemoveItemBillingMethod, unknown> = openEnums
    .inboundSchema(ListPlansVariantRemoveItemBillingMethod);

/** @internal */
export const ListPlansIntervalVariantRemoveItemEnum2$inboundSchema:
  z.ZodMiniType<ListPlansIntervalVariantRemoveItemEnum2, unknown> = openEnums
    .inboundSchema(ListPlansIntervalVariantRemoveItemEnum2);

/** @internal */
export const ListPlansIntervalVariantRemoveItemEnum1$inboundSchema:
  z.ZodMiniType<ListPlansIntervalVariantRemoveItemEnum1, unknown> = openEnums
    .inboundSchema(ListPlansIntervalVariantRemoveItemEnum1);

/** @internal */
export const ListPlansVariantIntervalUnion$inboundSchema: z.ZodMiniType<
  ListPlansVariantIntervalUnion,
  unknown
> = smartUnion([
  ListPlansIntervalVariantRemoveItemEnum1$inboundSchema,
  ListPlansIntervalVariantRemoveItemEnum2$inboundSchema,
]);

export function listPlansVariantIntervalUnionFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansVariantIntervalUnion, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansVariantIntervalUnion$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansVariantIntervalUnion' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantPlanItemFilter$inboundSchema: z.ZodMiniType<
  ListPlansVariantPlanItemFilter,
  unknown
> = z.pipe(
  z.object({
    feature_id: types.optional(types.string()),
    billing_method: types.optional(
      ListPlansVariantRemoveItemBillingMethod$inboundSchema,
    ),
    interval: types.optional(
      smartUnion([
        ListPlansIntervalVariantRemoveItemEnum1$inboundSchema,
        ListPlansIntervalVariantRemoveItemEnum2$inboundSchema,
      ]),
    ),
    interval_count: types.optional(types.number()),
    included: types.optional(types.number()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "feature_id": "featureId",
      "billing_method": "billingMethod",
      "interval_count": "intervalCount",
    });
  }),
);

export function listPlansVariantPlanItemFilterFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansVariantPlanItemFilter, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansVariantPlanItemFilter$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansVariantPlanItemFilter' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantCustomizeDurationType$inboundSchema: z.ZodMiniType<
  ListPlansVariantCustomizeDurationType,
  unknown
> = openEnums.inboundSchema(ListPlansVariantCustomizeDurationType);

/** @internal */
export const ListPlansVariantCustomizeOnEnd$inboundSchema: z.ZodMiniType<
  ListPlansVariantCustomizeOnEnd,
  unknown
> = openEnums.inboundSchema(ListPlansVariantCustomizeOnEnd);

/** @internal */
export const ListPlansVariantFreeTrialParams$inboundSchema: z.ZodMiniType<
  ListPlansVariantFreeTrialParams,
  unknown
> = z.pipe(
  z.object({
    duration_length: types.number(),
    duration_type: z._default(
      ListPlansVariantCustomizeDurationType$inboundSchema,
      "month",
    ),
    card_required: z._default(types.boolean(), false),
    on_end: types.optional(ListPlansVariantCustomizeOnEnd$inboundSchema),
  }),
  z.transform((v) => {
    return remap$(v, {
      "duration_length": "durationLength",
      "duration_type": "durationType",
      "card_required": "cardRequired",
      "on_end": "onEnd",
    });
  }),
);

export function listPlansVariantFreeTrialParamsFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansVariantFreeTrialParams, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansVariantFreeTrialParams$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansVariantFreeTrialParams' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantCustomizePurchaseLimitInterval$inboundSchema:
  z.ZodMiniType<ListPlansVariantCustomizePurchaseLimitInterval, unknown> =
    openEnums.inboundSchema(ListPlansVariantCustomizePurchaseLimitInterval);

/** @internal */
export const ListPlansVariantCustomizePurchaseLimit$inboundSchema:
  z.ZodMiniType<ListPlansVariantCustomizePurchaseLimit, unknown> = z.pipe(
    z.object({
      interval: ListPlansVariantCustomizePurchaseLimitInterval$inboundSchema,
      interval_count: z._default(types.number(), 1),
      limit: types.number(),
      count: types.optional(types.number()),
    }),
    z.transform((v) => {
      return remap$(v, {
        "interval_count": "intervalCount",
      });
    }),
  );

export function listPlansVariantCustomizePurchaseLimitFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansVariantCustomizePurchaseLimit, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      ListPlansVariantCustomizePurchaseLimit$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansVariantCustomizePurchaseLimit' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantCustomizeAutoTopup$inboundSchema: z.ZodMiniType<
  ListPlansVariantCustomizeAutoTopup,
  unknown
> = z.pipe(
  z.object({
    feature_id: types.string(),
    enabled: z._default(types.boolean(), false),
    threshold: types.number(),
    quantity: types.number(),
    purchase_limit: types.optional(
      z.lazy(() => ListPlansVariantCustomizePurchaseLimit$inboundSchema),
    ),
    invoice_mode: types.optional(types.boolean()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "feature_id": "featureId",
      "purchase_limit": "purchaseLimit",
      "invoice_mode": "invoiceMode",
    });
  }),
);

export function listPlansVariantCustomizeAutoTopupFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansVariantCustomizeAutoTopup, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      ListPlansVariantCustomizeAutoTopup$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansVariantCustomizeAutoTopup' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantCustomizeLimitType$inboundSchema: z.ZodMiniType<
  ListPlansVariantCustomizeLimitType,
  unknown
> = openEnums.inboundSchema(ListPlansVariantCustomizeLimitType);

/** @internal */
export const ListPlansVariantCustomizeSpendLimit$inboundSchema: z.ZodMiniType<
  ListPlansVariantCustomizeSpendLimit,
  unknown
> = z.pipe(
  z.object({
    feature_id: types.optional(types.string()),
    enabled: z._default(types.boolean(), false),
    limit_type: types.optional(
      ListPlansVariantCustomizeLimitType$inboundSchema,
    ),
    overage_limit: types.optional(types.number()),
    skip_overage_billing: types.optional(types.boolean()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "feature_id": "featureId",
      "limit_type": "limitType",
      "overage_limit": "overageLimit",
      "skip_overage_billing": "skipOverageBilling",
    });
  }),
);

export function listPlansVariantCustomizeSpendLimitFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansVariantCustomizeSpendLimit, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      ListPlansVariantCustomizeSpendLimit$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansVariantCustomizeSpendLimit' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantCustomizeUsageLimitInterval$inboundSchema:
  z.ZodMiniType<ListPlansVariantCustomizeUsageLimitInterval, unknown> =
    openEnums.inboundSchema(ListPlansVariantCustomizeUsageLimitInterval);

/** @internal */
export const ListPlansVariantCustomizeAnchor$inboundSchema: z.ZodMiniType<
  ListPlansVariantCustomizeAnchor,
  unknown
> = openEnums.inboundSchema(ListPlansVariantCustomizeAnchor);

/** @internal */
export const ListPlansVariantCustomizeUsageLimitFilter$inboundSchema:
  z.ZodMiniType<ListPlansVariantCustomizeUsageLimitFilter, unknown> = z.object({
    properties: z.record(z.string(), z.any()),
  });

export function listPlansVariantCustomizeUsageLimitFilterFromJSON(
  jsonString: string,
): SafeParseResult<
  ListPlansVariantCustomizeUsageLimitFilter,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      ListPlansVariantCustomizeUsageLimitFilter$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'ListPlansVariantCustomizeUsageLimitFilter' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantCustomizeUsageLimit$inboundSchema: z.ZodMiniType<
  ListPlansVariantCustomizeUsageLimit,
  unknown
> = z.pipe(
  z.object({
    feature_id: types.string(),
    enabled: z._default(types.boolean(), true),
    limit: types.number(),
    interval: ListPlansVariantCustomizeUsageLimitInterval$inboundSchema,
    anchor: types.optional(ListPlansVariantCustomizeAnchor$inboundSchema),
    filter: types.optional(
      z.lazy(() => ListPlansVariantCustomizeUsageLimitFilter$inboundSchema),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "feature_id": "featureId",
    });
  }),
);

export function listPlansVariantCustomizeUsageLimitFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansVariantCustomizeUsageLimit, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      ListPlansVariantCustomizeUsageLimit$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansVariantCustomizeUsageLimit' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantCustomizeThresholdType$inboundSchema:
  z.ZodMiniType<ListPlansVariantCustomizeThresholdType, unknown> = openEnums
    .inboundSchema(ListPlansVariantCustomizeThresholdType);

/** @internal */
export const ListPlansVariantCustomizeBasis$inboundSchema: z.ZodMiniType<
  ListPlansVariantCustomizeBasis,
  unknown
> = openEnums.inboundSchema(ListPlansVariantCustomizeBasis);

/** @internal */
export const ListPlansVariantCustomizeUsageAlertFilter$inboundSchema:
  z.ZodMiniType<ListPlansVariantCustomizeUsageAlertFilter, unknown> = z.object({
    properties: z.record(z.string(), z.any()),
  });

export function listPlansVariantCustomizeUsageAlertFilterFromJSON(
  jsonString: string,
): SafeParseResult<
  ListPlansVariantCustomizeUsageAlertFilter,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      ListPlansVariantCustomizeUsageAlertFilter$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'ListPlansVariantCustomizeUsageAlertFilter' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantCustomizeUsageAlert$inboundSchema: z.ZodMiniType<
  ListPlansVariantCustomizeUsageAlert,
  unknown
> = z.pipe(
  z.object({
    feature_id: types.optional(types.string()),
    enabled: z._default(types.boolean(), true),
    threshold: types.number(),
    threshold_type: ListPlansVariantCustomizeThresholdType$inboundSchema,
    basis: z._default(ListPlansVariantCustomizeBasis$inboundSchema, "balance"),
    filter: types.optional(
      z.lazy(() => ListPlansVariantCustomizeUsageAlertFilter$inboundSchema),
    ),
    name: types.optional(types.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "feature_id": "featureId",
      "threshold_type": "thresholdType",
    });
  }),
);

export function listPlansVariantCustomizeUsageAlertFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansVariantCustomizeUsageAlert, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      ListPlansVariantCustomizeUsageAlert$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansVariantCustomizeUsageAlert' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantCustomizeOverageAllowed$inboundSchema:
  z.ZodMiniType<ListPlansVariantCustomizeOverageAllowed, unknown> = z.pipe(
    z.object({
      feature_id: types.string(),
      enabled: z._default(types.boolean(), false),
    }),
    z.transform((v) => {
      return remap$(v, {
        "feature_id": "featureId",
      });
    }),
  );

export function listPlansVariantCustomizeOverageAllowedFromJSON(
  jsonString: string,
): SafeParseResult<
  ListPlansVariantCustomizeOverageAllowed,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      ListPlansVariantCustomizeOverageAllowed$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'ListPlansVariantCustomizeOverageAllowed' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantCustomizeBillingControls$inboundSchema:
  z.ZodMiniType<ListPlansVariantCustomizeBillingControls, unknown> = z.pipe(
    z.object({
      auto_topups: types.optional(z.array(z.lazy(() =>
        ListPlansVariantCustomizeAutoTopup$inboundSchema
      ))),
      spend_limits: types.optional(z.array(z.lazy(() =>
        ListPlansVariantCustomizeSpendLimit$inboundSchema
      ))),
      usage_limits: types.optional(z.array(z.lazy(() =>
        ListPlansVariantCustomizeUsageLimit$inboundSchema
      ))),
      usage_alerts: types.optional(z.array(z.lazy(() =>
        ListPlansVariantCustomizeUsageAlert$inboundSchema
      ))),
      overage_allowed: types.optional(z.array(z.lazy(() =>
        ListPlansVariantCustomizeOverageAllowed$inboundSchema
      ))),
    }),
    z.transform((v) => {
      return remap$(v, {
        "auto_topups": "autoTopups",
        "spend_limits": "spendLimits",
        "usage_limits": "usageLimits",
        "usage_alerts": "usageAlerts",
        "overage_allowed": "overageAllowed",
      });
    }),
  );

export function listPlansVariantCustomizeBillingControlsFromJSON(
  jsonString: string,
): SafeParseResult<
  ListPlansVariantCustomizeBillingControls,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      ListPlansVariantCustomizeBillingControls$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'ListPlansVariantCustomizeBillingControls' from JSON`,
  );
}

/** @internal */
export const VariantUpsertLicenseInterval$inboundSchema: z.ZodMiniType<
  VariantUpsertLicenseInterval,
  unknown
> = openEnums.inboundSchema(VariantUpsertLicenseInterval);

/** @internal */
export const VariantUpsertLicenseBasePrice$inboundSchema: z.ZodMiniType<
  VariantUpsertLicenseBasePrice,
  unknown
> = z.pipe(
  z.object({
    amount: types.number(),
    interval: VariantUpsertLicenseInterval$inboundSchema,
    interval_count: types.optional(types.number()),
    additional_currencies: types.optional(z.array(types.nullable(z.any()))),
  }),
  z.transform((v) => {
    return remap$(v, {
      "interval_count": "intervalCount",
      "additional_currencies": "additionalCurrencies",
    });
  }),
);

export function variantUpsertLicenseBasePriceFromJSON(
  jsonString: string,
): SafeParseResult<VariantUpsertLicenseBasePrice, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VariantUpsertLicenseBasePrice$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VariantUpsertLicenseBasePrice' from JSON`,
  );
}

/** @internal */
export const VariantUpsertLicensePlanItem$inboundSchema: z.ZodMiniType<
  VariantUpsertLicensePlanItem,
  unknown
> = z.pipe(
  z.object({
    feature_id: types.optional(z.any()),
    included: types.optional(z.any()),
    unlimited: types.optional(z.any()),
    pooled: types.optional(z.any()),
    reset: types.optional(z.any()),
    price: types.optional(z.any()),
    proration: types.optional(z.any()),
    rollover: types.optional(z.any()),
    feature_override: types.optional(z.any()),
    entity_feature_id: types.optional(z.any()),
    entitlement_id: types.optional(z.any()),
    price_id: types.optional(z.any()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "feature_id": "featureId",
      "feature_override": "featureOverride",
      "entity_feature_id": "entityFeatureId",
      "entitlement_id": "entitlementId",
      "price_id": "priceId",
    });
  }),
);

export function variantUpsertLicensePlanItemFromJSON(
  jsonString: string,
): SafeParseResult<VariantUpsertLicensePlanItem, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VariantUpsertLicensePlanItem$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VariantUpsertLicensePlanItem' from JSON`,
  );
}

/** @internal */
export const VariantUpsertLicensePlanItemFilter$inboundSchema: z.ZodMiniType<
  VariantUpsertLicensePlanItemFilter,
  unknown
> = z.pipe(
  z.object({
    feature_id: types.optional(z.any()),
    billing_method: types.optional(z.any()),
    interval: types.optional(z.any()),
    interval_count: types.optional(z.any()),
    included: types.optional(z.any()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "feature_id": "featureId",
      "billing_method": "billingMethod",
      "interval_count": "intervalCount",
    });
  }),
);

export function variantUpsertLicensePlanItemFilterFromJSON(
  jsonString: string,
): SafeParseResult<VariantUpsertLicensePlanItemFilter, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      VariantUpsertLicensePlanItemFilter$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VariantUpsertLicensePlanItemFilter' from JSON`,
  );
}

/** @internal */
export const VariantUpsertLicenseCustomize$inboundSchema: z.ZodMiniType<
  VariantUpsertLicenseCustomize,
  unknown
> = z.pipe(
  z.object({
    price: z.optional(
      z.nullable(z.lazy(() => VariantUpsertLicenseBasePrice$inboundSchema)),
    ),
    add_items: types.optional(
      z.array(z.lazy(() => VariantUpsertLicensePlanItem$inboundSchema)),
    ),
    remove_items: types.optional(
      z.array(z.lazy(() => VariantUpsertLicensePlanItemFilter$inboundSchema)),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "add_items": "addItems",
      "remove_items": "removeItems",
    });
  }),
);

export function variantUpsertLicenseCustomizeFromJSON(
  jsonString: string,
): SafeParseResult<VariantUpsertLicenseCustomize, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VariantUpsertLicenseCustomize$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VariantUpsertLicenseCustomize' from JSON`,
  );
}

/** @internal */
export const VariantUpsertLicense$inboundSchema: z.ZodMiniType<
  VariantUpsertLicense,
  unknown
> = z.pipe(
  z.object({
    license_plan_id: types.string(),
    version_slug: types.optional(types.string()),
    included: types.optional(types.number()),
    prepaid_only: types.optional(types.boolean()),
    customize: z.optional(
      z.nullable(z.lazy(() => VariantUpsertLicenseCustomize$inboundSchema)),
    ),
    metadata: types.optional(z.record(z.string(), z.any())),
  }),
  z.transform((v) => {
    return remap$(v, {
      "license_plan_id": "licensePlanId",
      "version_slug": "versionSlug",
      "prepaid_only": "prepaidOnly",
    });
  }),
);

export function variantUpsertLicenseFromJSON(
  jsonString: string,
): SafeParseResult<VariantUpsertLicense, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VariantUpsertLicense$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VariantUpsertLicense' from JSON`,
  );
}

/** @internal */
export const VariantRemoveLicense$inboundSchema: z.ZodMiniType<
  VariantRemoveLicense,
  unknown
> = z.pipe(
  z.object({
    license_plan_id: types.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "license_plan_id": "licensePlanId",
    });
  }),
);

export function variantRemoveLicenseFromJSON(
  jsonString: string,
): SafeParseResult<VariantRemoveLicense, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VariantRemoveLicense$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VariantRemoveLicense' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantCustomize$inboundSchema: z.ZodMiniType<
  ListPlansVariantCustomize,
  unknown
> = z.pipe(
  z.object({
    price: z.optional(z.nullable(ListPlansVariantBasePrice$inboundSchema)),
    add_items: types.optional(
      z.array(z.lazy(() => ListPlansVariantPlanItem$inboundSchema)),
    ),
    remove_items: types.optional(
      z.array(z.lazy(() => ListPlansVariantPlanItemFilter$inboundSchema)),
    ),
    free_trial: z.optional(
      z.nullable(z.lazy(() => ListPlansVariantFreeTrialParams$inboundSchema)),
    ),
    billing_controls: types.optional(
      z.lazy(() => ListPlansVariantCustomizeBillingControls$inboundSchema),
    ),
    upsert_licenses: types.optional(
      z.array(z.lazy(() => VariantUpsertLicense$inboundSchema)),
    ),
    remove_licenses: types.optional(
      z.array(z.lazy(() => VariantRemoveLicense$inboundSchema)),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "add_items": "addItems",
      "remove_items": "removeItems",
      "free_trial": "freeTrial",
      "billing_controls": "billingControls",
      "upsert_licenses": "upsertLicenses",
      "remove_licenses": "removeLicenses",
    });
  }),
);

export function listPlansVariantCustomizeFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansVariantCustomize, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansVariantCustomize$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansVariantCustomize' from JSON`,
  );
}

/** @internal */
export const VariantPlanAdditionalCurrency$inboundSchema: z.ZodMiniType<
  VariantPlanAdditionalCurrency,
  unknown
> = z.object({
  currency: types.string(),
  amount: types.number(),
});

export function variantPlanAdditionalCurrencyFromJSON(
  jsonString: string,
): SafeParseResult<VariantPlanAdditionalCurrency, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VariantPlanAdditionalCurrency$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VariantPlanAdditionalCurrency' from JSON`,
  );
}

/** @internal */
export const PriceVariantPlanInterval$inboundSchema: z.ZodMiniType<
  PriceVariantPlanInterval,
  unknown
> = openEnums.inboundSchema(PriceVariantPlanInterval);

/** @internal */
export const PriceVariantDisplay$inboundSchema: z.ZodMiniType<
  PriceVariantDisplay,
  unknown
> = z.pipe(
  z.object({
    primary_text: types.string(),
    secondary_text: types.optional(types.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "primary_text": "primaryText",
      "secondary_text": "secondaryText",
    });
  }),
);

export function priceVariantDisplayFromJSON(
  jsonString: string,
): SafeParseResult<PriceVariantDisplay, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PriceVariantDisplay$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PriceVariantDisplay' from JSON`,
  );
}

/** @internal */
export const PriceVariantStripe$inboundSchema: z.ZodMiniType<
  PriceVariantStripe,
  unknown
> = z.pipe(
  z.object({
    price_id: types.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "price_id": "priceId",
    });
  }),
);

export function priceVariantStripeFromJSON(
  jsonString: string,
): SafeParseResult<PriceVariantStripe, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PriceVariantStripe$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PriceVariantStripe' from JSON`,
  );
}

/** @internal */
export const PriceVariantProcessors$inboundSchema: z.ZodMiniType<
  PriceVariantProcessors,
  unknown
> = z.object({
  stripe: z.optional(
    z.nullable(z.lazy(() => PriceVariantStripe$inboundSchema)),
  ),
});

export function priceVariantProcessorsFromJSON(
  jsonString: string,
): SafeParseResult<PriceVariantProcessors, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PriceVariantProcessors$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PriceVariantProcessors' from JSON`,
  );
}

/** @internal */
export const VariantPlanPrice$inboundSchema: z.ZodMiniType<
  VariantPlanPrice,
  unknown
> = z.pipe(
  z.object({
    amount: types.number(),
    additional_currencies: types.optional(
      z.array(z.lazy(() => VariantPlanAdditionalCurrency$inboundSchema)),
    ),
    interval: PriceVariantPlanInterval$inboundSchema,
    interval_count: types.optional(types.number()),
    display: types.optional(z.lazy(() => PriceVariantDisplay$inboundSchema)),
    processors: types.optional(
      z.lazy(() => PriceVariantProcessors$inboundSchema),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "additional_currencies": "additionalCurrencies",
      "interval_count": "intervalCount",
    });
  }),
);

export function variantPlanPriceFromJSON(
  jsonString: string,
): SafeParseResult<VariantPlanPrice, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VariantPlanPrice$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VariantPlanPrice' from JSON`,
  );
}

/** @internal */
export const VariantType$inboundSchema: z.ZodMiniType<VariantType, unknown> =
  openEnums.inboundSchema(VariantType);

/** @internal */
export const VariantFeatureDisplay$inboundSchema: z.ZodMiniType<
  VariantFeatureDisplay,
  unknown
> = z.object({
  singular: types.string(),
  plural: types.string(),
});

export function variantFeatureDisplayFromJSON(
  jsonString: string,
): SafeParseResult<VariantFeatureDisplay, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VariantFeatureDisplay$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VariantFeatureDisplay' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantCreditSchema$inboundSchema: z.ZodMiniType<
  ListPlansVariantCreditSchema,
  unknown
> = z.pipe(
  z.object({
    metered_feature_id: types.optional(z.any()),
    credit_cost: types.optional(z.any()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "metered_feature_id": "meteredFeatureId",
      "credit_cost": "creditCost",
    });
  }),
);

export function listPlansVariantCreditSchemaFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansVariantCreditSchema, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansVariantCreditSchema$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansVariantCreditSchema' from JSON`,
  );
}

/** @internal */
export const VariantFeature$inboundSchema: z.ZodMiniType<
  VariantFeature,
  unknown
> = z.pipe(
  z.object({
    id: types.string(),
    name: z.optional(z.nullable(types.string())),
    type: VariantType$inboundSchema,
    display: z.optional(
      z.nullable(z.lazy(() => VariantFeatureDisplay$inboundSchema)),
    ),
    credit_schema: z.optional(z.nullable(z.array(z.lazy(() =>
      ListPlansVariantCreditSchema$inboundSchema
    )))),
    archived: z.optional(z.nullable(types.boolean())),
  }),
  z.transform((v) => {
    return remap$(v, {
      "credit_schema": "creditSchema",
    });
  }),
);

export function variantFeatureFromJSON(
  jsonString: string,
): SafeParseResult<VariantFeature, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VariantFeature$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VariantFeature' from JSON`,
  );
}

/** @internal */
export const ResetVariantItemInterval$inboundSchema: z.ZodMiniType<
  ResetVariantItemInterval,
  unknown
> = openEnums.inboundSchema(ResetVariantItemInterval);

/** @internal */
export const VariantPlanReset$inboundSchema: z.ZodMiniType<
  VariantPlanReset,
  unknown
> = z.pipe(
  z.object({
    interval: ResetVariantItemInterval$inboundSchema,
    interval_count: types.optional(types.number()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "interval_count": "intervalCount",
    });
  }),
);

export function variantPlanResetFromJSON(
  jsonString: string,
): SafeParseResult<VariantPlanReset, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VariantPlanReset$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VariantPlanReset' from JSON`,
  );
}

/** @internal */
export const VariantItemAdditionalCurrency$inboundSchema: z.ZodMiniType<
  VariantItemAdditionalCurrency,
  unknown
> = z.object({
  currency: types.optional(z.any()),
  amount: types.optional(z.any()),
});

export function variantItemAdditionalCurrencyFromJSON(
  jsonString: string,
): SafeParseResult<VariantItemAdditionalCurrency, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VariantItemAdditionalCurrency$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VariantItemAdditionalCurrency' from JSON`,
  );
}

/** @internal */
export const VariantPlanTier$inboundSchema: z.ZodMiniType<
  VariantPlanTier,
  unknown
> = z.pipe(
  z.object({
    to: types.optional(z.any()),
    amount: types.optional(z.any()),
    flat_amount: types.optional(z.any()),
    additional_currencies: types.optional(z.any()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "flat_amount": "flatAmount",
      "additional_currencies": "additionalCurrencies",
    });
  }),
);

export function variantPlanTierFromJSON(
  jsonString: string,
): SafeParseResult<VariantPlanTier, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VariantPlanTier$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VariantPlanTier' from JSON`,
  );
}

/** @internal */
export const VariantPlanTierBehavior$inboundSchema: z.ZodMiniType<
  VariantPlanTierBehavior,
  unknown
> = openEnums.inboundSchema(VariantPlanTierBehavior);

/** @internal */
export const PriceVariantItemInterval$inboundSchema: z.ZodMiniType<
  PriceVariantItemInterval,
  unknown
> = openEnums.inboundSchema(PriceVariantItemInterval);

/** @internal */
export const VariantItemBillingMethod$inboundSchema: z.ZodMiniType<
  VariantItemBillingMethod,
  unknown
> = openEnums.inboundSchema(VariantItemBillingMethod);

/** @internal */
export const VariantItemStripe$inboundSchema: z.ZodMiniType<
  VariantItemStripe,
  unknown
> = z.pipe(
  z.object({
    price_id: types.optional(z.any()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "price_id": "priceId",
    });
  }),
);

export function variantItemStripeFromJSON(
  jsonString: string,
): SafeParseResult<VariantItemStripe, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VariantItemStripe$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VariantItemStripe' from JSON`,
  );
}

/** @internal */
export const VariantItemProcessors$inboundSchema: z.ZodMiniType<
  VariantItemProcessors,
  unknown
> = z.object({
  stripe: z.optional(z.nullable(z.lazy(() => VariantItemStripe$inboundSchema))),
});

export function variantItemProcessorsFromJSON(
  jsonString: string,
): SafeParseResult<VariantItemProcessors, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VariantItemProcessors$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VariantItemProcessors' from JSON`,
  );
}

/** @internal */
export const VariantItemPrice$inboundSchema: z.ZodMiniType<
  VariantItemPrice,
  unknown
> = z.pipe(
  z.object({
    amount: types.optional(types.number()),
    additional_currencies: types.optional(
      z.array(z.lazy(() => VariantItemAdditionalCurrency$inboundSchema)),
    ),
    tiers: types.optional(z.array(z.lazy(() => VariantPlanTier$inboundSchema))),
    tier_behavior: types.optional(VariantPlanTierBehavior$inboundSchema),
    interval: PriceVariantItemInterval$inboundSchema,
    interval_count: types.optional(types.number()),
    billing_units: types.number(),
    billing_method: VariantItemBillingMethod$inboundSchema,
    max_purchase: types.nullable(types.number()),
    processors: types.optional(
      z.lazy(() => VariantItemProcessors$inboundSchema),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "additional_currencies": "additionalCurrencies",
      "tier_behavior": "tierBehavior",
      "interval_count": "intervalCount",
      "billing_units": "billingUnits",
      "billing_method": "billingMethod",
      "max_purchase": "maxPurchase",
    });
  }),
);

export function variantItemPriceFromJSON(
  jsonString: string,
): SafeParseResult<VariantItemPrice, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VariantItemPrice$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VariantItemPrice' from JSON`,
  );
}

/** @internal */
export const VariantItemDisplay$inboundSchema: z.ZodMiniType<
  VariantItemDisplay,
  unknown
> = z.pipe(
  z.object({
    primary_text: types.string(),
    secondary_text: types.optional(types.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "primary_text": "primaryText",
      "secondary_text": "secondaryText",
    });
  }),
);

export function variantItemDisplayFromJSON(
  jsonString: string,
): SafeParseResult<VariantItemDisplay, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VariantItemDisplay$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VariantItemDisplay' from JSON`,
  );
}

/** @internal */
export const VariantPlanExpiryDurationType$inboundSchema: z.ZodMiniType<
  VariantPlanExpiryDurationType,
  unknown
> = openEnums.inboundSchema(VariantPlanExpiryDurationType);

/** @internal */
export const VariantPlanRollover$inboundSchema: z.ZodMiniType<
  VariantPlanRollover,
  unknown
> = z.pipe(
  z.object({
    max: types.nullable(types.number()),
    max_percentage: z.optional(z.nullable(types.number())),
    expiry_duration_type: VariantPlanExpiryDurationType$inboundSchema,
    expiry_duration_length: types.optional(types.number()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "max_percentage": "maxPercentage",
      "expiry_duration_type": "expiryDurationType",
      "expiry_duration_length": "expiryDurationLength",
    });
  }),
);

export function variantPlanRolloverFromJSON(
  jsonString: string,
): SafeParseResult<VariantPlanRollover, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VariantPlanRollover$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VariantPlanRollover' from JSON`,
  );
}

/** @internal */
export const VariantPlanFeatureOverride$inboundSchema: z.ZodMiniType<
  VariantPlanFeatureOverride,
  unknown
> = z.pipe(
  z.object({
    credit_schema: types.optional(z.array(types.nullable(z.any()))),
  }),
  z.transform((v) => {
    return remap$(v, {
      "credit_schema": "creditSchema",
    });
  }),
);

export function variantPlanFeatureOverrideFromJSON(
  jsonString: string,
): SafeParseResult<VariantPlanFeatureOverride, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VariantPlanFeatureOverride$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VariantPlanFeatureOverride' from JSON`,
  );
}

/** @internal */
export const VariantItem$inboundSchema: z.ZodMiniType<VariantItem, unknown> = z
  .pipe(
    z.object({
      feature_id: types.string(),
      feature: types.optional(z.lazy(() => VariantFeature$inboundSchema)),
      included: types.number(),
      unlimited: types.boolean(),
      pooled: z._default(types.boolean(), false),
      reset: types.nullable(z.lazy(() => VariantPlanReset$inboundSchema)),
      price: types.nullable(z.lazy(() => VariantItemPrice$inboundSchema)),
      display: types.optional(z.lazy(() => VariantItemDisplay$inboundSchema)),
      rollover: types.optional(z.lazy(() => VariantPlanRollover$inboundSchema)),
      feature_override: types.optional(
        z.lazy(() => VariantPlanFeatureOverride$inboundSchema),
      ),
    }),
    z.transform((v) => {
      return remap$(v, {
        "feature_id": "featureId",
        "feature_override": "featureOverride",
      });
    }),
  );

export function variantItemFromJSON(
  jsonString: string,
): SafeParseResult<VariantItem, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VariantItem$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VariantItem' from JSON`,
  );
}

/** @internal */
export const VariantStripe$inboundSchema: z.ZodMiniType<
  VariantStripe,
  unknown
> = z.pipe(
  z.object({
    product_id: types.string(),
    additional_product_ids: types.optional(z.array(types.string())),
  }),
  z.transform((v) => {
    return remap$(v, {
      "product_id": "productId",
      "additional_product_ids": "additionalProductIds",
    });
  }),
);

export function variantStripeFromJSON(
  jsonString: string,
): SafeParseResult<VariantStripe, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VariantStripe$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VariantStripe' from JSON`,
  );
}

/** @internal */
export const VariantProduct$inboundSchema: z.ZodMiniType<
  VariantProduct,
  unknown
> = z.pipe(
  z.object({
    product_id: types.string(),
    feature_quantities: types.optional(z.array(types.nullable(z.any()))),
  }),
  z.transform((v) => {
    return remap$(v, {
      "product_id": "productId",
      "feature_quantities": "featureQuantities",
    });
  }),
);

export function variantProductFromJSON(
  jsonString: string,
): SafeParseResult<VariantProduct, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VariantProduct$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VariantProduct' from JSON`,
  );
}

/** @internal */
export const VariantRevenuecat$inboundSchema: z.ZodMiniType<
  VariantRevenuecat,
  unknown
> = z.object({
  products: z.array(z.lazy(() => VariantProduct$inboundSchema)),
});

export function variantRevenuecatFromJSON(
  jsonString: string,
): SafeParseResult<VariantRevenuecat, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VariantRevenuecat$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VariantRevenuecat' from JSON`,
  );
}

/** @internal */
export const VariantProcessors$inboundSchema: z.ZodMiniType<
  VariantProcessors,
  unknown
> = z.object({
  stripe: z.optional(z.nullable(z.lazy(() => VariantStripe$inboundSchema))),
  revenuecat: z.optional(
    z.nullable(z.lazy(() => VariantRevenuecat$inboundSchema)),
  ),
});

export function variantProcessorsFromJSON(
  jsonString: string,
): SafeParseResult<VariantProcessors, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VariantProcessors$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VariantProcessors' from JSON`,
  );
}

/** @internal */
export const VariantPlanDurationType$inboundSchema: z.ZodMiniType<
  VariantPlanDurationType,
  unknown
> = openEnums.inboundSchema(VariantPlanDurationType);

/** @internal */
export const VariantPlanOnEnd$inboundSchema: z.ZodMiniType<
  VariantPlanOnEnd,
  unknown
> = openEnums.inboundSchema(VariantPlanOnEnd);

/** @internal */
export const VariantFreeTrial$inboundSchema: z.ZodMiniType<
  VariantFreeTrial,
  unknown
> = z.pipe(
  z.object({
    duration_length: types.number(),
    duration_type: VariantPlanDurationType$inboundSchema,
    card_required: types.boolean(),
    on_end: z.optional(z.nullable(VariantPlanOnEnd$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "duration_length": "durationLength",
      "duration_type": "durationType",
      "card_required": "cardRequired",
      "on_end": "onEnd",
    });
  }),
);

export function variantFreeTrialFromJSON(
  jsonString: string,
): SafeParseResult<VariantFreeTrial, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VariantFreeTrial$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VariantFreeTrial' from JSON`,
  );
}

/** @internal */
export const VariantEnv$inboundSchema: z.ZodMiniType<VariantEnv, unknown> =
  openEnums.inboundSchema(VariantEnv);

/** @internal */
export const VariantConfig$inboundSchema: z.ZodMiniType<
  VariantConfig,
  unknown
> = z.pipe(
  z.object({
    ignore_past_due: z._default(types.boolean(), false),
  }),
  z.transform((v) => {
    return remap$(v, {
      "ignore_past_due": "ignorePastDue",
    });
  }),
);

export function variantConfigFromJSON(
  jsonString: string,
): SafeParseResult<VariantConfig, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VariantConfig$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VariantConfig' from JSON`,
  );
}

/** @internal */
export const VariantPlanPurchaseLimitInterval$inboundSchema: z.ZodMiniType<
  VariantPlanPurchaseLimitInterval,
  unknown
> = openEnums.inboundSchema(VariantPlanPurchaseLimitInterval);

/** @internal */
export const VariantPlanPurchaseLimit$inboundSchema: z.ZodMiniType<
  VariantPlanPurchaseLimit,
  unknown
> = z.pipe(
  z.object({
    interval: VariantPlanPurchaseLimitInterval$inboundSchema,
    interval_count: z._default(types.number(), 1),
    limit: types.number(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "interval_count": "intervalCount",
    });
  }),
);

export function variantPlanPurchaseLimitFromJSON(
  jsonString: string,
): SafeParseResult<VariantPlanPurchaseLimit, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VariantPlanPurchaseLimit$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VariantPlanPurchaseLimit' from JSON`,
  );
}

/** @internal */
export const VariantPlanAutoTopup$inboundSchema: z.ZodMiniType<
  VariantPlanAutoTopup,
  unknown
> = z.pipe(
  z.object({
    feature_id: types.string(),
    enabled: z._default(types.boolean(), false),
    threshold: types.number(),
    quantity: types.number(),
    purchase_limit: types.optional(
      z.lazy(() => VariantPlanPurchaseLimit$inboundSchema),
    ),
    invoice_mode: types.optional(types.boolean()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "feature_id": "featureId",
      "purchase_limit": "purchaseLimit",
      "invoice_mode": "invoiceMode",
    });
  }),
);

export function variantPlanAutoTopupFromJSON(
  jsonString: string,
): SafeParseResult<VariantPlanAutoTopup, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VariantPlanAutoTopup$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VariantPlanAutoTopup' from JSON`,
  );
}

/** @internal */
export const VariantPlanLimitType$inboundSchema: z.ZodMiniType<
  VariantPlanLimitType,
  unknown
> = openEnums.inboundSchema(VariantPlanLimitType);

/** @internal */
export const VariantPlanSpendLimit$inboundSchema: z.ZodMiniType<
  VariantPlanSpendLimit,
  unknown
> = z.pipe(
  z.object({
    feature_id: types.optional(types.string()),
    enabled: z._default(types.boolean(), false),
    limit_type: types.optional(VariantPlanLimitType$inboundSchema),
    overage_limit: types.optional(types.number()),
    skip_overage_billing: types.optional(types.boolean()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "feature_id": "featureId",
      "limit_type": "limitType",
      "overage_limit": "overageLimit",
      "skip_overage_billing": "skipOverageBilling",
    });
  }),
);

export function variantPlanSpendLimitFromJSON(
  jsonString: string,
): SafeParseResult<VariantPlanSpendLimit, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VariantPlanSpendLimit$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VariantPlanSpendLimit' from JSON`,
  );
}

/** @internal */
export const VariantPlanUsageLimitInterval$inboundSchema: z.ZodMiniType<
  VariantPlanUsageLimitInterval,
  unknown
> = openEnums.inboundSchema(VariantPlanUsageLimitInterval);

/** @internal */
export const VariantPlanAnchor$inboundSchema: z.ZodMiniType<
  VariantPlanAnchor,
  unknown
> = openEnums.inboundSchema(VariantPlanAnchor);

/** @internal */
export const VariantPlanUsageLimitFilter$inboundSchema: z.ZodMiniType<
  VariantPlanUsageLimitFilter,
  unknown
> = z.object({
  properties: z.record(z.string(), z.any()),
});

export function variantPlanUsageLimitFilterFromJSON(
  jsonString: string,
): SafeParseResult<VariantPlanUsageLimitFilter, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VariantPlanUsageLimitFilter$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VariantPlanUsageLimitFilter' from JSON`,
  );
}

/** @internal */
export const VariantPlanUsageLimit$inboundSchema: z.ZodMiniType<
  VariantPlanUsageLimit,
  unknown
> = z.pipe(
  z.object({
    feature_id: types.string(),
    enabled: z._default(types.boolean(), true),
    limit: types.number(),
    interval: VariantPlanUsageLimitInterval$inboundSchema,
    anchor: types.optional(VariantPlanAnchor$inboundSchema),
    filter: types.optional(
      z.lazy(() => VariantPlanUsageLimitFilter$inboundSchema),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "feature_id": "featureId",
    });
  }),
);

export function variantPlanUsageLimitFromJSON(
  jsonString: string,
): SafeParseResult<VariantPlanUsageLimit, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VariantPlanUsageLimit$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VariantPlanUsageLimit' from JSON`,
  );
}

/** @internal */
export const VariantPlanThresholdType$inboundSchema: z.ZodMiniType<
  VariantPlanThresholdType,
  unknown
> = openEnums.inboundSchema(VariantPlanThresholdType);

/** @internal */
export const VariantPlanBasis$inboundSchema: z.ZodMiniType<
  VariantPlanBasis,
  unknown
> = openEnums.inboundSchema(VariantPlanBasis);

/** @internal */
export const VariantPlanUsageAlertFilter$inboundSchema: z.ZodMiniType<
  VariantPlanUsageAlertFilter,
  unknown
> = z.object({
  properties: z.record(z.string(), z.any()),
});

export function variantPlanUsageAlertFilterFromJSON(
  jsonString: string,
): SafeParseResult<VariantPlanUsageAlertFilter, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VariantPlanUsageAlertFilter$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VariantPlanUsageAlertFilter' from JSON`,
  );
}

/** @internal */
export const VariantPlanUsageAlert$inboundSchema: z.ZodMiniType<
  VariantPlanUsageAlert,
  unknown
> = z.pipe(
  z.object({
    feature_id: types.optional(types.string()),
    enabled: z._default(types.boolean(), true),
    threshold: types.number(),
    threshold_type: VariantPlanThresholdType$inboundSchema,
    basis: z._default(VariantPlanBasis$inboundSchema, "balance"),
    filter: types.optional(
      z.lazy(() => VariantPlanUsageAlertFilter$inboundSchema),
    ),
    name: types.optional(types.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "feature_id": "featureId",
      "threshold_type": "thresholdType",
    });
  }),
);

export function variantPlanUsageAlertFromJSON(
  jsonString: string,
): SafeParseResult<VariantPlanUsageAlert, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VariantPlanUsageAlert$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VariantPlanUsageAlert' from JSON`,
  );
}

/** @internal */
export const VariantPlanOverageAllowed$inboundSchema: z.ZodMiniType<
  VariantPlanOverageAllowed,
  unknown
> = z.pipe(
  z.object({
    feature_id: types.string(),
    enabled: z._default(types.boolean(), false),
  }),
  z.transform((v) => {
    return remap$(v, {
      "feature_id": "featureId",
    });
  }),
);

export function variantPlanOverageAllowedFromJSON(
  jsonString: string,
): SafeParseResult<VariantPlanOverageAllowed, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VariantPlanOverageAllowed$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VariantPlanOverageAllowed' from JSON`,
  );
}

/** @internal */
export const VariantPlanBillingControls$inboundSchema: z.ZodMiniType<
  VariantPlanBillingControls,
  unknown
> = z.pipe(
  z.object({
    auto_topups: types.optional(
      z.array(z.lazy(() => VariantPlanAutoTopup$inboundSchema)),
    ),
    spend_limits: types.optional(
      z.array(z.lazy(() => VariantPlanSpendLimit$inboundSchema)),
    ),
    usage_limits: types.optional(
      z.array(z.lazy(() => VariantPlanUsageLimit$inboundSchema)),
    ),
    usage_alerts: types.optional(
      z.array(z.lazy(() => VariantPlanUsageAlert$inboundSchema)),
    ),
    overage_allowed: types.optional(
      z.array(z.lazy(() => VariantPlanOverageAllowed$inboundSchema)),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "auto_topups": "autoTopups",
      "spend_limits": "spendLimits",
      "usage_limits": "usageLimits",
      "usage_alerts": "usageAlerts",
      "overage_allowed": "overageAllowed",
    });
  }),
);

export function variantPlanBillingControlsFromJSON(
  jsonString: string,
): SafeParseResult<VariantPlanBillingControls, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VariantPlanBillingControls$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VariantPlanBillingControls' from JSON`,
  );
}

/** @internal */
export const VariantStatus$inboundSchema: z.ZodMiniType<
  VariantStatus,
  unknown
> = openEnums.inboundSchema(VariantStatus);

/** @internal */
export const VariantAttachAction$inboundSchema: z.ZodMiniType<
  VariantAttachAction,
  unknown
> = openEnums.inboundSchema(VariantAttachAction);

/** @internal */
export const VariantCustomerEligibility$inboundSchema: z.ZodMiniType<
  VariantCustomerEligibility,
  unknown
> = z.pipe(
  z.object({
    trial_available: types.optional(types.boolean()),
    status: types.optional(VariantStatus$inboundSchema),
    canceling: types.optional(types.boolean()),
    trialing: types.optional(types.boolean()),
    attach_action: VariantAttachAction$inboundSchema,
  }),
  z.transform((v) => {
    return remap$(v, {
      "trial_available": "trialAvailable",
      "attach_action": "attachAction",
    });
  }),
);

export function variantCustomerEligibilityFromJSON(
  jsonString: string,
): SafeParseResult<VariantCustomerEligibility, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VariantCustomerEligibility$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VariantCustomerEligibility' from JSON`,
  );
}

/** @internal */
export const ListPlansPlan$inboundSchema: z.ZodMiniType<
  ListPlansPlan,
  unknown
> = z.pipe(
  z.object({
    id: types.string(),
    name: types.string(),
    description: types.nullable(types.string()),
    group: types.nullable(types.string()),
    version: types.number(),
    version_slug: z.optional(z.nullable(types.string())),
    active: types.optional(types.boolean()),
    add_on: types.boolean(),
    auto_enable: types.boolean(),
    price: types.nullable(z.lazy(() => VariantPlanPrice$inboundSchema)),
    items: z.array(z.lazy(() => VariantItem$inboundSchema)),
    processors: types.optional(z.lazy(() => VariantProcessors$inboundSchema)),
    free_trial: types.optional(z.lazy(() => VariantFreeTrial$inboundSchema)),
    created_at: types.number(),
    env: VariantEnv$inboundSchema,
    archived: types.boolean(),
    config: z.lazy(() => VariantConfig$inboundSchema),
    billing_controls: types.optional(
      z.lazy(() => VariantPlanBillingControls$inboundSchema),
    ),
    metadata: z.record(z.string(), z.any()),
    customer_eligibility: types.optional(
      z.lazy(() => VariantCustomerEligibility$inboundSchema),
    ),
    base_variant_id: types.nullable(types.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "version_slug": "versionSlug",
      "add_on": "addOn",
      "auto_enable": "autoEnable",
      "free_trial": "freeTrial",
      "created_at": "createdAt",
      "billing_controls": "billingControls",
      "customer_eligibility": "customerEligibility",
      "base_variant_id": "baseVariantId",
    });
  }),
);

export function listPlansPlanFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansPlan, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansPlan$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansPlan' from JSON`,
  );
}

/** @internal */
export const ListPlansVariant$inboundSchema: z.ZodMiniType<
  ListPlansVariant,
  unknown
> = z.pipe(
  z.object({
    variant_plan_id: types.string(),
    name: types.string(),
    customize: types.optional(
      z.lazy(() => ListPlansVariantCustomize$inboundSchema),
    ),
    plan: types.optional(z.lazy(() => ListPlansPlan$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "variant_plan_id": "variantPlanId",
    });
  }),
);

export function listPlansVariantFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansVariant, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansVariant$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansVariant' from JSON`,
  );
}

/** @internal */
export const ListPlansList$inboundSchema: z.ZodMiniType<
  ListPlansList,
  unknown
> = z.pipe(
  z.object({
    id: types.string(),
    name: types.string(),
    description: types.nullable(types.string()),
    group: types.nullable(types.string()),
    version: types.number(),
    version_slug: z.optional(z.nullable(types.string())),
    active: types.optional(types.boolean()),
    add_on: types.boolean(),
    auto_enable: types.boolean(),
    price: types.nullable(ListPlansPrice$inboundSchema),
    items: z.array(ListPlansItem$inboundSchema),
    processors: types.optional(ListPlansProcessors$inboundSchema),
    free_trial: types.optional(ListPlansFreeTrial$inboundSchema),
    created_at: types.number(),
    env: ListPlansEnv$inboundSchema,
    archived: types.boolean(),
    config: ListPlansConfig$inboundSchema,
    billing_controls: types.optional(ListPlansBillingControls$inboundSchema),
    metadata: z.record(z.string(), z.any()),
    customer_eligibility: types.optional(
      ListPlansCustomerEligibility$inboundSchema,
    ),
    base_variant_id: types.nullable(types.string()),
    variant_details: types.optional(ListPlansVariantDetails$inboundSchema),
    licenses: types.optional(z.array(ListPlansLicense$inboundSchema)),
    variants: types.optional(
      z.array(z.lazy(() => ListPlansVariant$inboundSchema)),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "version_slug": "versionSlug",
      "add_on": "addOn",
      "auto_enable": "autoEnable",
      "free_trial": "freeTrial",
      "created_at": "createdAt",
      "billing_controls": "billingControls",
      "customer_eligibility": "customerEligibility",
      "base_variant_id": "baseVariantId",
      "variant_details": "variantDetails",
    });
  }),
);

export function listPlansListFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansList, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansList$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansList' from JSON`,
  );
}

/** @internal */
export const ListPlansResponse$inboundSchema: z.ZodMiniType<
  ListPlansResponse,
  unknown
> = z.object({
  list: z.array(z.lazy(() => ListPlansList$inboundSchema)),
});

export function listPlansResponseFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansResponse' from JSON`,
  );
}
