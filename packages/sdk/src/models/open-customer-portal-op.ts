/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./sdk-validation-error.js";

export type OpenCustomerPortalGlobals = {
  xApiVersion?: string | undefined;
};

export type OpenCustomerPortalParams = {
  /**
   * The ID of the customer to open the billing portal for.
   */
  customerId: string;
  /**
   * Stripe billing portal configuration ID. Create configurations in your Stripe dashboard.
   */
  configurationId?: string | undefined;
  /**
   * URL to redirect to when back button is clicked in the billing portal
   */
  returnUrl?: string | undefined;
};

/**
 * OK
 */
export type OpenCustomerPortalResponse = {
  /**
   * The ID of the billing portal session
   */
  customerId: string;
  /**
   * URL to the billing portal
   */
  url: string;
};

/** @internal */
export type OpenCustomerPortalParams$Outbound = {
  customer_id: string;
  configuration_id?: string | undefined;
  return_url?: string | undefined;
};

/** @internal */
export const OpenCustomerPortalParams$outboundSchema: z.ZodMiniType<
  OpenCustomerPortalParams$Outbound,
  OpenCustomerPortalParams
> = z.pipe(
  z.object({
    customerId: z.string(),
    configurationId: z.optional(z.string()),
    returnUrl: z.optional(z.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      customerId: "customer_id",
      configurationId: "configuration_id",
      returnUrl: "return_url",
    });
  }),
);

export function openCustomerPortalParamsToJSON(
  openCustomerPortalParams: OpenCustomerPortalParams,
): string {
  return JSON.stringify(
    OpenCustomerPortalParams$outboundSchema.parse(openCustomerPortalParams),
  );
}

/** @internal */
export const OpenCustomerPortalResponse$inboundSchema: z.ZodMiniType<
  OpenCustomerPortalResponse,
  unknown
> = z.pipe(
  z.object({
    customer_id: types.string(),
    url: types.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "customer_id": "customerId",
    });
  }),
);

export function openCustomerPortalResponseFromJSON(
  jsonString: string,
): SafeParseResult<OpenCustomerPortalResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OpenCustomerPortalResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OpenCustomerPortalResponse' from JSON`,
  );
}
