/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import * as openEnums from "../types/enums.js";
import { OpenEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./sdk-validation-error.js";

export type AdvanceTestClockGlobals = {
  xApiVersion?: string | undefined;
};

export type AdvanceTestClockParams = {
  /**
   * The ID of the customer whose Stripe test clock to advance.
   */
  customerId: string;
  /**
   * Target time as a Unix timestamp in milliseconds. Rounded down to whole seconds; must be later than the current clock time.
   */
  frozenTime: number;
};

export const AdvanceTestClockStatus = {
  Advancing: "advancing",
  InternalFailure: "internal_failure",
  Ready: "ready",
} as const;
export type AdvanceTestClockStatus = OpenEnum<typeof AdvanceTestClockStatus>;

/**
 * OK
 */
export type AdvanceTestClockResponse = {
  customerId: string;
  /**
   * The Stripe test clock's frozen time in milliseconds.
   */
  frozenTime: number;
  status: AdvanceTestClockStatus;
};

/** @internal */
export type AdvanceTestClockParams$Outbound = {
  customer_id: string;
  frozen_time: number;
};

/** @internal */
export const AdvanceTestClockParams$outboundSchema: z.ZodMiniType<
  AdvanceTestClockParams$Outbound,
  AdvanceTestClockParams
> = z.pipe(
  z.object({
    customerId: z.string(),
    frozenTime: z.int(),
  }),
  z.transform((v) => {
    return remap$(v, {
      customerId: "customer_id",
      frozenTime: "frozen_time",
    });
  }),
);

export function advanceTestClockParamsToJSON(
  advanceTestClockParams: AdvanceTestClockParams,
): string {
  return JSON.stringify(
    AdvanceTestClockParams$outboundSchema.parse(advanceTestClockParams),
  );
}

/** @internal */
export const AdvanceTestClockStatus$inboundSchema: z.ZodMiniType<
  AdvanceTestClockStatus,
  unknown
> = openEnums.inboundSchema(AdvanceTestClockStatus);

/** @internal */
export const AdvanceTestClockResponse$inboundSchema: z.ZodMiniType<
  AdvanceTestClockResponse,
  unknown
> = z.pipe(
  z.object({
    customer_id: types.string(),
    frozen_time: types.number(),
    status: AdvanceTestClockStatus$inboundSchema,
  }),
  z.transform((v) => {
    return remap$(v, {
      "customer_id": "customerId",
      "frozen_time": "frozenTime",
    });
  }),
);

export function advanceTestClockResponseFromJSON(
  jsonString: string,
): SafeParseResult<AdvanceTestClockResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AdvanceTestClockResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AdvanceTestClockResponse' from JSON`,
  );
}
