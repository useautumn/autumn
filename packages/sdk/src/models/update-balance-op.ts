/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { ClosedEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./sdk-validation-error.js";

export type UpdateBalanceGlobals = {
  xApiVersion?: string | undefined;
};

/**
 * Target a specific balance by its reset interval. Use when the customer has multiple balances for the same feature with different reset intervals.
 */
export const UpdateBalanceInterval = {
  OneOff: "one_off",
  Minute: "minute",
  Hour: "hour",
  Day: "day",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
/**
 * Target a specific balance by its reset interval. Use when the customer has multiple balances for the same feature with different reset intervals.
 */
export type UpdateBalanceInterval = ClosedEnum<typeof UpdateBalanceInterval>;

export type UpdateBalanceParams = {
  /**
   * The ID of the customer.
   */
  customerId: string;
  /**
   * The ID of the feature.
   */
  featureId: string;
  /**
   * The ID of the entity for entity-scoped balances (e.g., per-seat limits).
   */
  entityId?: string | undefined;
  /**
   * Set the remaining balance to this exact value. Cannot be combined with add_to_balance.
   */
  remaining?: number | undefined;
  /**
   * Add this amount to the current balance. Use negative values to subtract. Cannot be combined with current_balance.
   */
  addToBalance?: number | undefined;
  /**
   * The usage amount to update. Cannot be combined with remaining or add_to_balance.
   */
  usage?: number | undefined;
  /**
   * Target a specific balance by its reset interval. Use when the customer has multiple balances for the same feature with different reset intervals.
   */
  interval?: UpdateBalanceInterval | undefined;
};

/**
 * OK
 */
export type UpdateBalanceResponse = {
  success: boolean;
};

/** @internal */
export const UpdateBalanceInterval$outboundSchema: z.ZodMiniEnum<
  typeof UpdateBalanceInterval
> = z.enum(UpdateBalanceInterval);

/** @internal */
export type UpdateBalanceParams$Outbound = {
  customer_id: string;
  feature_id: string;
  entity_id?: string | undefined;
  remaining?: number | undefined;
  add_to_balance?: number | undefined;
  usage?: number | undefined;
  interval?: string | undefined;
};

/** @internal */
export const UpdateBalanceParams$outboundSchema: z.ZodMiniType<
  UpdateBalanceParams$Outbound,
  UpdateBalanceParams
> = z.pipe(
  z.object({
    customerId: z.string(),
    featureId: z.string(),
    entityId: z.optional(z.string()),
    remaining: z.optional(z.number()),
    addToBalance: z.optional(z.number()),
    usage: z.optional(z.number()),
    interval: z.optional(UpdateBalanceInterval$outboundSchema),
  }),
  z.transform((v) => {
    return remap$(v, {
      customerId: "customer_id",
      featureId: "feature_id",
      entityId: "entity_id",
      addToBalance: "add_to_balance",
    });
  }),
);

export function updateBalanceParamsToJSON(
  updateBalanceParams: UpdateBalanceParams,
): string {
  return JSON.stringify(
    UpdateBalanceParams$outboundSchema.parse(updateBalanceParams),
  );
}

/** @internal */
export const UpdateBalanceResponse$inboundSchema: z.ZodMiniType<
  UpdateBalanceResponse,
  unknown
> = z.object({
  success: types.boolean(),
});

export function updateBalanceResponseFromJSON(
  jsonString: string,
): SafeParseResult<UpdateBalanceResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => UpdateBalanceResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'UpdateBalanceResponse' from JSON`,
  );
}
