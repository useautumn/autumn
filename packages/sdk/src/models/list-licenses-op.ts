/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./sdk-validation-error.js";

export type ListLicensesGlobals = {
  xApiVersion?: string | undefined;
};

export type ListLicensesParams = {
  customerId: string;
  entityId?: string | undefined;
};

export type ListLicensesList = {
  /**
   * The plan offered as an assignable license.
   */
  licensePlanId: string;
  /**
   * The plan that offers this license.
   */
  parentPlanId: string;
  /**
   * Display name of the license plan.
   */
  licensePlanName: string;
  /**
   * Total seats the customer has for this license, included plus paid.
   */
  granted: number;
  /**
   * Seats currently assigned to entities.
   */
  usage: number;
  /**
   * Seats still available to assign.
   */
  remaining: number;
  /**
   * Paid seats purchased on top of the plan's included amount.
   */
  paidQuantity: number;
};

/**
 * OK
 */
export type ListLicensesResponse = {
  list: Array<ListLicensesList>;
};

/** @internal */
export type ListLicensesParams$Outbound = {
  customer_id: string;
  entity_id?: string | undefined;
};

/** @internal */
export const ListLicensesParams$outboundSchema: z.ZodMiniType<
  ListLicensesParams$Outbound,
  ListLicensesParams
> = z.pipe(
  z.object({
    customerId: z.string(),
    entityId: z.optional(z.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      customerId: "customer_id",
      entityId: "entity_id",
    });
  }),
);

export function listLicensesParamsToJSON(
  listLicensesParams: ListLicensesParams,
): string {
  return JSON.stringify(
    ListLicensesParams$outboundSchema.parse(listLicensesParams),
  );
}

/** @internal */
export const ListLicensesList$inboundSchema: z.ZodMiniType<
  ListLicensesList,
  unknown
> = z.pipe(
  z.object({
    license_plan_id: types.string(),
    parent_plan_id: types.string(),
    license_plan_name: types.string(),
    granted: types.number(),
    usage: types.number(),
    remaining: types.number(),
    paid_quantity: types.number(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "license_plan_id": "licensePlanId",
      "parent_plan_id": "parentPlanId",
      "license_plan_name": "licensePlanName",
      "paid_quantity": "paidQuantity",
    });
  }),
);

export function listLicensesListFromJSON(
  jsonString: string,
): SafeParseResult<ListLicensesList, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListLicensesList$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListLicensesList' from JSON`,
  );
}

/** @internal */
export const ListLicensesResponse$inboundSchema: z.ZodMiniType<
  ListLicensesResponse,
  unknown
> = z.object({
  list: z.array(z.lazy(() => ListLicensesList$inboundSchema)),
});

export function listLicensesResponseFromJSON(
  jsonString: string,
): SafeParseResult<ListLicensesResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListLicensesResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListLicensesResponse' from JSON`,
  );
}
