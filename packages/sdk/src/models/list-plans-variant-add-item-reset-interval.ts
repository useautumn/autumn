/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import * as openEnums from "../types/enums.js";
import { OpenEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { smartUnion } from "../types/smart-union.js";
import { Plan, Plan$inboundSchema } from "./plan.js";
import { SDKValidationError } from "./sdk-validation-error.js";

export type ListPlansGlobals = {
  xApiVersion?: string | undefined;
};

export type ListPlansParams = {
  /**
   * Customer ID to include eligibility info (trial availability, attach scenario).
   */
  customerId?: string | undefined;
  /**
   * Entity ID for entity-scoped plans.
   */
  entityId?: string | undefined;
  /**
   * If true, includes archived plans in the response.
   */
  includeArchived?: boolean | undefined;
  /**
   * If true, includes all plan versions.
   */
  allVersions?: boolean | undefined;
};

export type ListPlansAdditionalCurrency = {
  /**
   * Three-letter Stripe-supported currency code (e.g. 'eur', 'gbp').
   */
  currency: string;
  /**
   * Price amount in this currency. Set explicitly per currency, not converted from the base amount.
   */
  amount: number;
};

/**
 * Billing interval (e.g. 'month', 'year').
 */
export const ListPlansPriceInterval = {
  OneOff: "one_off",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
/**
 * Billing interval (e.g. 'month', 'year').
 */
export type ListPlansPriceInterval = OpenEnum<typeof ListPlansPriceInterval>;

/**
 * Display text for showing this price in pricing pages.
 */
export type ListPlansPriceDisplay = {
  /**
   * Main display text (e.g. '$10' or '100 messages').
   */
  primaryText: string;
  /**
   * Secondary display text (e.g. 'per month' or 'then $0.5 per 100').
   */
  secondaryText?: string | undefined;
};

export type ListPlansPriceStripe = {
  /**
   * Stripe price ID. For prepaid with included > 0 this is the V2 price.
   */
  priceId: string;
};

/**
 * Payment processors this base price is connected to. Omitted when unset.
 */
export type ListPlansPriceProcessors = {
  stripe?: ListPlansPriceStripe | null | undefined;
};

export type ListPlansPrice = {
  /**
   * Base price amount for the plan, in major currency units (e.g. dollars).
   */
  amount: number;
  /**
   * Base price amounts in additional currencies. The base 'amount' is in the org's default currency.
   */
  additionalCurrencies?: Array<ListPlansAdditionalCurrency> | undefined;
  /**
   * Billing interval (e.g. 'month', 'year').
   */
  interval: ListPlansPriceInterval;
  /**
   * Number of intervals per billing cycle. Defaults to 1.
   */
  intervalCount?: number | undefined;
  /**
   * Display text for showing this price in pricing pages.
   */
  display?: ListPlansPriceDisplay | undefined;
  /**
   * Payment processors this base price is connected to. Omitted when unset.
   */
  processors?: ListPlansPriceProcessors | undefined;
};

/**
 * The type of the feature
 */
export const ListPlansType = {
  Static: "static",
  Boolean: "boolean",
  SingleUse: "single_use",
  ContinuousUse: "continuous_use",
  CreditSystem: "credit_system",
  AiCreditSystem: "ai_credit_system",
} as const;
/**
 * The type of the feature
 */
export type ListPlansType = OpenEnum<typeof ListPlansType>;

export type ListPlansFeatureDisplay = {
  /**
   * The singular display name for the feature.
   */
  singular: string;
  /**
   * The plural display name for the feature.
   */
  plural: string;
};

export type ListPlansFeatureCreditSchema = {
  /**
   * The ID of the metered feature (should be a single_use feature).
   */
  meteredFeatureId: string;
  /**
   * The credit cost of the metered feature.
   */
  creditCost: number;
};

/**
 * The full feature object if expanded.
 */
export type ListPlansFeature = {
  /**
   * The ID of the feature, used to refer to it in other API calls like /track or /check.
   */
  id: string;
  /**
   * The name of the feature.
   */
  name?: string | null | undefined;
  /**
   * The type of the feature
   */
  type: ListPlansType;
  /**
   * Singular and plural display names for the feature.
   */
  display?: ListPlansFeatureDisplay | null | undefined;
  /**
   * Credit cost schema for credit system features.
   */
  creditSchema?: Array<ListPlansFeatureCreditSchema> | null | undefined;
  /**
   * Whether or not the feature is archived.
   */
  archived?: boolean | null | undefined;
};

/**
 * The interval at which the feature balance resets (e.g. 'month', 'year'). For consumable features, usage resets to 0 and included units are restored.
 */
export const ListPlansResetItemInterval = {
  OneOff: "one_off",
  Minute: "minute",
  Hour: "hour",
  Day: "day",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
/**
 * The interval at which the feature balance resets (e.g. 'month', 'year'). For consumable features, usage resets to 0 and included units are restored.
 */
export type ListPlansResetItemInterval = OpenEnum<
  typeof ListPlansResetItemInterval
>;

export type ListPlansItemReset = {
  /**
   * The interval at which the feature balance resets (e.g. 'month', 'year'). For consumable features, usage resets to 0 and included units are restored.
   */
  interval: ListPlansResetItemInterval;
  /**
   * Number of intervals between resets. Defaults to 1.
   */
  intervalCount?: number | undefined;
};

export type ListPlansItemAdditionalCurrency = {
  /**
   * Three-letter Stripe-supported currency code (e.g. 'eur', 'gbp').
   */
  currency: string;
  /**
   * Price amount in this currency. Set explicitly per currency, not converted from the base amount.
   */
  amount: number;
};

export type ListPlansTo = number | string;

export type ListPlansTierAdditionalCurrency = {
  /**
   * Three-letter Stripe-supported currency code (e.g. 'eur', 'gbp').
   */
  currency: string;
  /**
   * Per-unit amount for this tier in this currency.
   */
  amount?: number | undefined;
  /**
   * Flat amount for this tier in this currency, if the tier uses one.
   */
  flatAmount?: number | undefined;
};

export type ListPlansPriceItemTier = {
  to: number | string;
  amount: number;
  flatAmount?: number | undefined;
  additionalCurrencies?: Array<ListPlansTierAdditionalCurrency> | undefined;
};

export const ListPlansItemTierBehavior = {
  Graduated: "graduated",
  Volume: "volume",
} as const;
export type ListPlansItemTierBehavior = OpenEnum<
  typeof ListPlansItemTierBehavior
>;

/**
 * Billing interval for this price. For consumable features, should match reset.interval.
 */
export const ListPlansPriceItemInterval = {
  OneOff: "one_off",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
/**
 * Billing interval for this price. For consumable features, should match reset.interval.
 */
export type ListPlansPriceItemInterval = OpenEnum<
  typeof ListPlansPriceItemInterval
>;

/**
 * 'prepaid' for features like seats where customers pay upfront, 'usage_based' for pay-as-you-go after included usage.
 */
export const ListPlansItemBillingMethod = {
  Prepaid: "prepaid",
  UsageBased: "usage_based",
} as const;
/**
 * 'prepaid' for features like seats where customers pay upfront, 'usage_based' for pay-as-you-go after included usage.
 */
export type ListPlansItemBillingMethod = OpenEnum<
  typeof ListPlansItemBillingMethod
>;

export type ListPlansItemStripe = {
  /**
   * Stripe price ID. For prepaid with included > 0 this is the V2 price.
   */
  priceId: string;
};

/**
 * Payment processors this item price is connected to. Omitted when unset.
 */
export type ListPlansItemProcessors = {
  stripe?: ListPlansItemStripe | null | undefined;
};

export type ListPlansItemPrice = {
  /**
   * Price per billing_units after included usage is consumed. Mutually exclusive with tiers.
   */
  amount?: number | undefined;
  /**
   * Amounts in additional currencies for this flat price. The base 'amount' is in the org's default currency. Only valid with 'amount', not 'tiers' (tiered prices carry per-currency amounts on each tier).
   */
  additionalCurrencies?: Array<ListPlansItemAdditionalCurrency> | undefined;
  /**
   * Tiered pricing configuration. Each tier's 'to' INCLUDES the included amount. Either 'tiers' or 'amount' is required.
   */
  tiers?: Array<ListPlansPriceItemTier> | undefined;
  tierBehavior?: ListPlansItemTierBehavior | undefined;
  /**
   * Billing interval for this price. For consumable features, should match reset.interval.
   */
  interval: ListPlansPriceItemInterval;
  /**
   * Number of intervals per billing cycle. Defaults to 1.
   */
  intervalCount?: number | undefined;
  /**
   * Number of units per price increment. Usage is rounded UP to the nearest billing_units when billed (e.g. billing_units=100 means 101 usage rounds to 200).
   */
  billingUnits: number;
  /**
   * 'prepaid' for features like seats where customers pay upfront, 'usage_based' for pay-as-you-go after included usage.
   */
  billingMethod: ListPlansItemBillingMethod;
  /**
   * Maximum units a customer can purchase beyond included. E.g. if included=100 and max_purchase=300, customer can use up to 400 total before usage is capped. Null for no limit.
   */
  maxPurchase: number | null;
  /**
   * Payment processors this item price is connected to. Omitted when unset.
   */
  processors?: ListPlansItemProcessors | undefined;
};

/**
 * Display text for showing this item in pricing pages.
 */
export type ListPlansItemDisplay = {
  /**
   * Main display text (e.g. '$10' or '100 messages').
   */
  primaryText: string;
  /**
   * Secondary display text (e.g. 'per month' or 'then $0.5 per 100').
   */
  secondaryText?: string | undefined;
};

/**
 * When rolled over units expire.
 */
export const ListPlansItemExpiryDurationType = {
  Month: "month",
  Forever: "forever",
} as const;
/**
 * When rolled over units expire.
 */
export type ListPlansItemExpiryDurationType = OpenEnum<
  typeof ListPlansItemExpiryDurationType
>;

/**
 * Rollover configuration for unused units. If set, unused included units roll over to the next period.
 */
export type ListPlansItemRollover = {
  /**
   * Maximum rollover units. Null for unlimited rollover.
   */
  max: number | null;
  /**
   * Maximum rollover as a percentage (0-100) of included + prepaid grant. Mutually exclusive with max.
   */
  maxPercentage?: number | null | undefined;
  /**
   * When rolled over units expire.
   */
  expiryDurationType: ListPlansItemExpiryDurationType;
  /**
   * Number of periods before expiry.
   */
  expiryDurationLength?: number | undefined;
};

export type ListPlansCreditSchemaItemFeatureOverride2 = {
  /**
   * ID of the metered feature that draws from this credit system.
   */
  meteredFeatureId: string;
  /**
   * Number of metered-feature units priced together. Defaults to one when omitted.
   */
  billingUnits?: number | undefined;
  /**
   * Credits consumed per billing-unit group.
   */
  creditCost: number;
};

export type ListPlansFeatureOverrideTier = {
  to?: any | undefined;
  creditCost?: any | undefined;
};

export type ListPlansCreditSchemaItemFeatureOverride1 = {
  /**
   * ID of the metered feature that draws from this credit system.
   */
  meteredFeatureId: string;
  /**
   * Number of metered-feature units priced together. Defaults to one when omitted.
   */
  billingUnits?: number | undefined;
  tierBehavior: "graduated";
  tiers: Array<ListPlansFeatureOverrideTier>;
};

export type ListPlansItemCreditSchemaUnion =
  | ListPlansCreditSchemaItemFeatureOverride1
  | ListPlansCreditSchemaItemFeatureOverride2;

/**
 * Overrides fields of this item's feature for customers on this plan (e.g. a credit system's credit_schema).
 */
export type ListPlansItemFeatureOverride = {
  /**
   * For credit system features: replaces the feature's credit_schema entirely for customers on this plan.
   */
  creditSchema?:
    | Array<
      | ListPlansCreditSchemaItemFeatureOverride1
      | ListPlansCreditSchemaItemFeatureOverride2
    >
    | undefined;
};

export type ListPlansItem = {
  /**
   * The ID of the feature this item configures.
   */
  featureId: string;
  /**
   * The full feature object if expanded.
   */
  feature?: ListPlansFeature | undefined;
  /**
   * Number of free units included. For consumable features, balance resets to this number each interval.
   */
  included: number;
  /**
   * Whether the customer has unlimited access to this feature.
   */
  unlimited: boolean;
  /**
   * Whether entity-level grants contribute to a shared customer balance.
   */
  pooled: boolean;
  /**
   * Reset configuration for consumable features. Null for non-consumable features like seats where usage persists across billing cycles.
   */
  reset: ListPlansItemReset | null;
  /**
   * Pricing configuration for usage beyond included units. Null if feature is entirely free.
   */
  price: ListPlansItemPrice | null;
  /**
   * Display text for showing this item in pricing pages.
   */
  display?: ListPlansItemDisplay | undefined;
  /**
   * Rollover configuration for unused units. If set, unused included units roll over to the next period.
   */
  rollover?: ListPlansItemRollover | undefined;
  /**
   * Overrides fields of this item's feature for customers on this plan (e.g. a credit system's credit_schema).
   */
  featureOverride?: ListPlansItemFeatureOverride | undefined;
};

export type ListPlansStripe = {
  /**
   * Stripe product ID this plan is billed under.
   */
  productId: string;
  /**
   * Extra Stripe product IDs aliased to this plan.
   */
  additionalProductIds?: Array<string> | undefined;
};

export type ListPlansFeatureQuantity = {
  featureId: string;
  quantity?: number | undefined;
};

export type ListPlansProduct = {
  /**
   * RevenueCat product ID that grants this plan when purchased.
   */
  productId: string;
  /**
   * Prepaid quantities granted when this specific RevenueCat product is purchased, in feature units.
   */
  featureQuantities?: Array<ListPlansFeatureQuantity> | undefined;
};

export type ListPlansRevenuecat = {
  /**
   * Every RevenueCat product that maps to this plan. Replaces the current set.
   */
  products: Array<ListPlansProduct>;
};

/**
 * Payment processors this plan is connected to. Omitted when unset.
 */
export type ListPlansProcessors = {
  stripe?: ListPlansStripe | null | undefined;
  revenuecat?: ListPlansRevenuecat | null | undefined;
};

/**
 * Unit of time for the trial duration ('day', 'month', 'year').
 */
export const ListPlansDurationType = {
  Day: "day",
  Month: "month",
  Year: "year",
} as const;
/**
 * Unit of time for the trial duration ('day', 'month', 'year').
 */
export type ListPlansDurationType = OpenEnum<typeof ListPlansDurationType>;

export const ListPlansOnEnd = {
  Bill: "bill",
  Revert: "revert",
} as const;
export type ListPlansOnEnd = OpenEnum<typeof ListPlansOnEnd>;

/**
 * Free trial configuration. If set, new customers can try this plan before being charged.
 */
export type ListPlansFreeTrial = {
  /**
   * Number of duration_type periods the trial lasts.
   */
  durationLength: number;
  /**
   * Unit of time for the trial duration ('day', 'month', 'year').
   */
  durationType: ListPlansDurationType;
  /**
   * Whether a payment method is required to start the trial. If true, customer will be charged after trial ends.
   */
  cardRequired: boolean;
  /**
   * Behavior when the trial ends. 'bill' charges the customer (default). 'revert' expires the trial and restores the customer's previous plan.
   */
  onEnd?: ListPlansOnEnd | null | undefined;
};

/**
 * Environment this plan belongs to ('sandbox' or 'live').
 */
export const ListPlansEnv = {
  Sandbox: "sandbox",
  Live: "live",
} as const;
/**
 * Environment this plan belongs to ('sandbox' or 'live').
 */
export type ListPlansEnv = OpenEnum<typeof ListPlansEnv>;

/**
 * Miscellaneous plan-level configuration flags.
 */
export type ListPlansConfig = {
  /**
   * If true, entitlements attached to this plan will still reset on schedule even when the customer's product is in a past_due state.
   */
  ignorePastDue: boolean;
};

/**
 * The time interval for the purchase limit window.
 */
export const ListPlansPurchaseLimitInterval = {
  Hour: "hour",
  Day: "day",
  Week: "week",
  Month: "month",
} as const;
/**
 * The time interval for the purchase limit window.
 */
export type ListPlansPurchaseLimitInterval = OpenEnum<
  typeof ListPlansPurchaseLimitInterval
>;

/**
 * Optional rate limit to cap how often auto top-ups occur.
 */
export type ListPlansPurchaseLimit = {
  /**
   * The time interval for the purchase limit window.
   */
  interval: ListPlansPurchaseLimitInterval;
  /**
   * Number of intervals in the purchase limit window.
   */
  intervalCount: number;
  /**
   * Maximum number of auto top-ups allowed within the interval.
   */
  limit: number;
};

export type ListPlansAutoTopup = {
  /**
   * The ID of the feature (credit balance) to auto top-up.
   */
  featureId: string;
  /**
   * Whether auto top-up is enabled.
   */
  enabled: boolean;
  /**
   * When the balance drops below this threshold, an auto top-up will be purchased.
   */
  threshold: number;
  /**
   * Amount of credits to add per auto top-up.
   */
  quantity: number;
  /**
   * Optional rate limit to cap how often auto top-ups occur.
   */
  purchaseLimit?: ListPlansPurchaseLimit | undefined;
  /**
   * When true, auto top-up creates a send_invoice invoice instead of auto-charging.
   */
  invoiceMode?: boolean | undefined;
};

/**
 * How overage_limit is interpreted: an absolute overage cap (default) or a percentage of the main-plan allowance.
 */
export const ListPlansLimitType = {
  Absolute: "absolute",
  UsagePercentage: "usage_percentage",
} as const;
/**
 * How overage_limit is interpreted: an absolute overage cap (default) or a percentage of the main-plan allowance.
 */
export type ListPlansLimitType = OpenEnum<typeof ListPlansLimitType>;

export type ListPlansSpendLimit = {
  /**
   * Optional feature ID this spend limit applies to.
   */
  featureId?: string | undefined;
  /**
   * Whether the overage spend limit is enabled.
   */
  enabled: boolean;
  /**
   * How overage_limit is interpreted: an absolute overage cap (default) or a percentage of the main-plan allowance.
   */
  limitType?: ListPlansLimitType | undefined;
  /**
   * Overage cap for the feature: absolute units, or a percent (e.g. 120) when limit_type is usage_percentage.
   */
  overageLimit?: number | undefined;
  /**
   * When true, overage for this feature is not posted to Stripe. Usage tracking and balance resets still behave normally.
   */
  skipOverageBilling?: boolean | undefined;
};

/**
 * Interval for the cap, aligned to the customer's billing cycle.
 */
export const ListPlansUsageLimitInterval = {
  Day: "day",
  Week: "week",
  Month: "month",
  Year: "year",
} as const;
/**
 * Interval for the cap, aligned to the customer's billing cycle.
 */
export type ListPlansUsageLimitInterval = OpenEnum<
  typeof ListPlansUsageLimitInterval
>;

/**
 * Window alignment. 'billing_cycle' phases the interval to the customer's renewal time; 'utc' aligns to the UTC calendar.
 */
export const ListPlansAnchor = {
  BillingCycle: "billing_cycle",
  Utc: "utc",
} as const;
/**
 * Window alignment. 'billing_cycle' phases the interval to the customer's renewal time; 'utc' aligns to the UTC calendar.
 */
export type ListPlansAnchor = OpenEnum<typeof ListPlansAnchor>;

/**
 * When set, only usage from events whose properties match counts toward this cap. Omit to count all usage of the feature.
 */
export type ListPlansUsageLimitFilter = {
  properties: { [k: string]: string };
};

export type ListPlansUsageLimit = {
  /**
   * The feature this usage limit applies to.
   */
  featureId: string;
  /**
   * Whether this usage limit is enabled.
   */
  enabled: boolean;
  /**
   * Maximum units allowed per interval.
   */
  limit: number;
  /**
   * Interval for the cap, aligned to the customer's billing cycle.
   */
  interval: ListPlansUsageLimitInterval;
  /**
   * Window alignment. 'billing_cycle' phases the interval to the customer's renewal time; 'utc' aligns to the UTC calendar.
   */
  anchor?: ListPlansAnchor | undefined;
  /**
   * When set, only usage from events whose properties match counts toward this cap. Omit to count all usage of the feature.
   */
  filter?: ListPlansUsageLimitFilter | undefined;
};

/**
 * Whether the threshold is an absolute count or a percentage of the usage allowance or remaining balance.
 */
export const ListPlansThresholdType = {
  Usage: "usage",
  UsagePercentage: "usage_percentage",
  Remaining: "remaining",
  RemainingPercentage: "remaining_percentage",
} as const;
/**
 * Whether the threshold is an absolute count or a percentage of the usage allowance or remaining balance.
 */
export type ListPlansThresholdType = OpenEnum<typeof ListPlansThresholdType>;

/**
 * What 100% means. balance: every grant on the feature. included: the plan allowance only. recurring: grants that reset. usage_limit: the cap of the usage limit with the same feature and filter.
 */
export const ListPlansBasis = {
  Balance: "balance",
  Included: "included",
  Recurring: "recurring",
  UsageLimit: "usage_limit",
} as const;
/**
 * What 100% means. balance: every grant on the feature. included: the plan allowance only. recurring: grants that reset. usage_limit: the cap of the usage limit with the same feature and filter.
 */
export type ListPlansBasis = OpenEnum<typeof ListPlansBasis>;

/**
 * Only valid with basis usage_limit. Points the alert at the usage limit carrying the same filter.
 */
export type ListPlansUsageAlertFilter = {
  properties: { [k: string]: string };
};

export type ListPlansUsageAlert = {
  /**
   * The feature ID this alert applies to.
   */
  featureId?: string | undefined;
  /**
   * Whether this usage alert is enabled.
   */
  enabled: boolean;
  /**
   * The threshold value that triggers the alert. For usage or remaining, this is an absolute count. For usage_percentage or remaining_percentage, this is a percentage (0-100).
   */
  threshold: number;
  /**
   * Whether the threshold is an absolute count or a percentage of the usage allowance or remaining balance.
   */
  thresholdType: ListPlansThresholdType;
  /**
   * What 100% means. balance: every grant on the feature. included: the plan allowance only. recurring: grants that reset. usage_limit: the cap of the usage limit with the same feature and filter.
   */
  basis: ListPlansBasis;
  /**
   * Only valid with basis usage_limit. Points the alert at the usage limit carrying the same filter.
   */
  filter?: ListPlansUsageAlertFilter | undefined;
  /**
   * Optional user-defined label to distinguish multiple alerts on the same feature.
   */
  name?: string | undefined;
};

export type ListPlansOverageAllowed = {
  /**
   * The feature ID this overage allowed control applies to.
   */
  featureId: string;
  /**
   * Whether overage is allowed for this feature.
   */
  enabled: boolean;
};

/**
 * Plan-level billing controls used as customer defaults.
 */
export type ListPlansBillingControls = {
  /**
   * List of auto top-up configurations per feature.
   */
  autoTopups?: Array<ListPlansAutoTopup> | undefined;
  /**
   * List of overage spend limits per feature (caps overage spend).
   */
  spendLimits?: Array<ListPlansSpendLimit> | undefined;
  /**
   * List of hard usage caps per feature (max units per interval).
   */
  usageLimits?: Array<ListPlansUsageLimit> | undefined;
  /**
   * List of usage alert configurations per feature.
   */
  usageAlerts?: Array<ListPlansUsageAlert> | undefined;
  /**
   * List of overage allowed controls per feature. When enabled, usage can exceed balance.
   */
  overageAllowed?: Array<ListPlansOverageAllowed> | undefined;
};

/**
 * The customer's current status with this plan. 'active' if attached, 'scheduled' if pending activation.
 */
export const ListPlansStatus = {
  Active: "active",
  Scheduled: "scheduled",
} as const;
/**
 * The customer's current status with this plan. 'active' if attached, 'scheduled' if pending activation.
 */
export type ListPlansStatus = OpenEnum<typeof ListPlansStatus>;

/**
 * The action that would occur if this plan were attached to the customer.
 */
export const ListPlansAttachAction = {
  Activate: "activate",
  Upgrade: "upgrade",
  Downgrade: "downgrade",
  None: "none",
  Purchase: "purchase",
} as const;
/**
 * The action that would occur if this plan were attached to the customer.
 */
export type ListPlansAttachAction = OpenEnum<typeof ListPlansAttachAction>;

export type ListPlansCustomerEligibility = {
  /**
   * Whether the trial on this plan is available to this customer. For example, if the customer used the trial in the past, this will be false.
   */
  trialAvailable?: boolean | undefined;
  /**
   * The customer's current status with this plan. 'active' if attached, 'scheduled' if pending activation.
   */
  status?: ListPlansStatus | undefined;
  /**
   * Whether the customer's active instance of this plan is set to cancel.
   */
  canceling?: boolean | undefined;
  /**
   * Whether the customer is currently on a free trial of this plan.
   */
  trialing?: boolean | undefined;
  /**
   * The action that would occur if this plan were attached to the customer.
   */
  attachAction: ListPlansAttachAction;
};

/**
 * Billing interval (e.g. 'month', 'year').
 */
export const ListPlansPriceVariantDetailsInterval = {
  OneOff: "one_off",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
/**
 * Billing interval (e.g. 'month', 'year').
 */
export type ListPlansPriceVariantDetailsInterval = OpenEnum<
  typeof ListPlansPriceVariantDetailsInterval
>;

export type ListPlansVariantDetailsAdditionalCurrency = {
  /**
   * Three-letter Stripe-supported currency code (e.g. 'eur', 'gbp').
   */
  currency: string;
  /**
   * Price amount in this currency. Set explicitly per currency, not converted from the base amount.
   */
  amount: number;
};

/**
 * Base price configuration for a plan.
 */
export type ListPlansVariantDetailsBasePrice = {
  /**
   * Base price amount for the plan, in major currency units (e.g. dollars).
   */
  amount: number;
  /**
   * Billing interval (e.g. 'month', 'year').
   */
  interval: ListPlansPriceVariantDetailsInterval;
  /**
   * Number of intervals per billing cycle. Defaults to 1.
   */
  intervalCount?: number | undefined;
  /**
   * Base price amounts in additional currencies. The base 'amount' is in the org's default currency.
   */
  additionalCurrencies?:
    | Array<ListPlansVariantDetailsAdditionalCurrency>
    | undefined;
};

/**
 * Interval at which balance resets (e.g. 'month', 'year'). For consumable features only.
 */
export const ListPlansVariantDetailsResetInterval = {
  OneOff: "one_off",
  Minute: "minute",
  Hour: "hour",
  Day: "day",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
/**
 * Interval at which balance resets (e.g. 'month', 'year'). For consumable features only.
 */
export type ListPlansVariantDetailsResetInterval = OpenEnum<
  typeof ListPlansVariantDetailsResetInterval
>;

/**
 * Reset configuration for consumable features. Omit for non-consumable features like seats.
 */
export type ListPlansVariantDetailsReset = {
  /**
   * Interval at which balance resets (e.g. 'month', 'year'). For consumable features only.
   */
  interval: ListPlansVariantDetailsResetInterval;
  /**
   * Number of intervals between resets. Defaults to 1.
   */
  intervalCount?: number | undefined;
};

export type ListPlansVariantDetailsAddItemAdditionalCurrency = {
  /**
   * Three-letter Stripe-supported currency code (e.g. 'eur', 'gbp').
   */
  currency: string;
  /**
   * Price amount in this currency. Set explicitly per currency, not converted from the base amount.
   */
  amount: number;
};

export type ListPlansVariantDetailsTier = {
  to?: any | undefined;
  amount: number;
  flatAmount?: number | undefined;
  additionalCurrencies?: Array<any | null> | undefined;
};

export const ListPlansVariantDetailsTierBehavior = {
  Graduated: "graduated",
  Volume: "volume",
} as const;
export type ListPlansVariantDetailsTierBehavior = OpenEnum<
  typeof ListPlansVariantDetailsTierBehavior
>;

/**
 * Billing interval. For consumable features, should match reset.interval.
 */
export const ListPlansVariantDetailsAddItemPriceInterval = {
  OneOff: "one_off",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
/**
 * Billing interval. For consumable features, should match reset.interval.
 */
export type ListPlansVariantDetailsAddItemPriceInterval = OpenEnum<
  typeof ListPlansVariantDetailsAddItemPriceInterval
>;

/**
 * 'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go.
 */
export const ListPlansVariantDetailsAddItemBillingMethod = {
  Prepaid: "prepaid",
  UsageBased: "usage_based",
} as const;
/**
 * 'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go.
 */
export type ListPlansVariantDetailsAddItemBillingMethod = OpenEnum<
  typeof ListPlansVariantDetailsAddItemBillingMethod
>;

/**
 * Pricing for usage beyond included units. Omit for free features.
 */
export type ListPlansVariantDetailsPrice = {
  /**
   * Price per billing_units after included usage. Either 'amount' or 'tiers' is required.
   */
  amount?: number | undefined;
  /**
   * Amounts in additional currencies for this flat price. The base 'amount' is in the org's default currency. Only valid with 'amount', not 'tiers'.
   */
  additionalCurrencies?:
    | Array<ListPlansVariantDetailsAddItemAdditionalCurrency>
    | undefined;
  /**
   * Tiered pricing.  Either 'amount' or 'tiers' is required.
   */
  tiers?: Array<ListPlansVariantDetailsTier> | undefined;
  tierBehavior?: ListPlansVariantDetailsTierBehavior | undefined;
  /**
   * Billing interval. For consumable features, should match reset.interval.
   */
  interval: ListPlansVariantDetailsAddItemPriceInterval;
  /**
   * Number of intervals per billing cycle. Defaults to 1.
   */
  intervalCount: number;
  /**
   * Units per price increment. Usage is rounded UP when billed (e.g. billing_units=100 means 101 rounds to 200).
   */
  billingUnits: number;
  /**
   * 'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go.
   */
  billingMethod: ListPlansVariantDetailsAddItemBillingMethod;
  /**
   * Max units purchasable beyond included. E.g. included=100, max_purchase=300 allows 400 total. Null for no limit.
   */
  maxPurchase?: number | null | undefined;
};

/**
 * Billing behavior when quantity increases mid-cycle.
 */
export const ListPlansVariantDetailsOnIncrease = {
  BillImmediately: "bill_immediately",
  ProrateImmediately: "prorate_immediately",
  ProrateNextCycle: "prorate_next_cycle",
  BillNextCycle: "bill_next_cycle",
} as const;
/**
 * Billing behavior when quantity increases mid-cycle.
 */
export type ListPlansVariantDetailsOnIncrease = OpenEnum<
  typeof ListPlansVariantDetailsOnIncrease
>;

/**
 * Credit behavior when quantity decreases mid-cycle.
 */
export const ListPlansVariantDetailsOnDecrease = {
  Prorate: "prorate",
  ProrateImmediately: "prorate_immediately",
  ProrateNextCycle: "prorate_next_cycle",
  None: "none",
  NoProrations: "no_prorations",
} as const;
/**
 * Credit behavior when quantity decreases mid-cycle.
 */
export type ListPlansVariantDetailsOnDecrease = OpenEnum<
  typeof ListPlansVariantDetailsOnDecrease
>;

/**
 * Proration settings for prepaid features. Controls mid-cycle quantity change billing.
 */
export type ListPlansVariantDetailsProration = {
  /**
   * Billing behavior when quantity increases mid-cycle.
   */
  onIncrease: ListPlansVariantDetailsOnIncrease;
  /**
   * Credit behavior when quantity decreases mid-cycle.
   */
  onDecrease: ListPlansVariantDetailsOnDecrease;
};

/**
 * When rolled over units expire.
 */
export const ListPlansVariantDetailsExpiryDurationType = {
  Month: "month",
  Forever: "forever",
} as const;
/**
 * When rolled over units expire.
 */
export type ListPlansVariantDetailsExpiryDurationType = OpenEnum<
  typeof ListPlansVariantDetailsExpiryDurationType
>;

/**
 * Rollover config for unused units. If set, unused included units carry over.
 */
export type ListPlansVariantDetailsRollover = {
  /**
   * Max rollover units. Omit for unlimited rollover.
   */
  max?: number | undefined;
  /**
   * Maximum rollover as a percentage (0-100) of included + prepaid grant. Mutually exclusive with max.
   */
  maxPercentage?: number | undefined;
  /**
   * When rolled over units expire.
   */
  expiryDurationType: ListPlansVariantDetailsExpiryDurationType;
  /**
   * Number of periods before expiry.
   */
  expiryDurationLength?: number | undefined;
};

export type ListPlansCreditSchemaVariantDetails2 = {
  meteredFeatureId?: any | undefined;
  billingUnits?: any | undefined;
  creditCost?: any | undefined;
};

export type ListPlansCreditSchemaVariantDetails1 = {
  meteredFeatureId?: any | undefined;
  billingUnits?: any | undefined;
  tierBehavior?: any | undefined;
  tiers?: any | undefined;
};

export type ListPlansVariantDetailsCreditSchemaUnion =
  | ListPlansCreditSchemaVariantDetails1
  | ListPlansCreditSchemaVariantDetails2;

/**
 * Overrides fields of this item's feature for customers on this plan (e.g. a credit system's credit_schema).
 */
export type ListPlansVariantDetailsFeatureOverride = {
  /**
   * For credit system features: replaces the feature's credit_schema entirely for customers on this plan.
   */
  creditSchema?:
    | Array<
      | ListPlansCreditSchemaVariantDetails1
      | ListPlansCreditSchemaVariantDetails2
    >
    | undefined;
};

/**
 * Configuration for a feature item in a plan, including usage limits, pricing, and rollover settings.
 */
export type ListPlansVariantDetailsPlanItem = {
  /**
   * The ID of the feature to configure.
   */
  featureId: string;
  /**
   * Number of free units included. Balance resets to this each interval for consumable features.
   */
  included?: number | undefined;
  /**
   * If true, customer has unlimited access to this feature.
   */
  unlimited?: boolean | undefined;
  /**
   * Whether entity-level grants contribute to a shared customer balance.
   */
  pooled: boolean;
  /**
   * Reset configuration for consumable features. Omit for non-consumable features like seats.
   */
  reset?: ListPlansVariantDetailsReset | undefined;
  /**
   * Pricing for usage beyond included units. Omit for free features.
   */
  price?: ListPlansVariantDetailsPrice | undefined;
  /**
   * Proration settings for prepaid features. Controls mid-cycle quantity change billing.
   */
  proration?: ListPlansVariantDetailsProration | undefined;
  /**
   * Rollover config for unused units. If set, unused included units carry over.
   */
  rollover?: ListPlansVariantDetailsRollover | undefined;
  /**
   * Overrides fields of this item's feature for customers on this plan (e.g. a credit system's credit_schema).
   */
  featureOverride?: ListPlansVariantDetailsFeatureOverride | undefined;
};

/**
 * Match items with this billing method (prepaid or usage_based).
 */
export const ListPlansVariantDetailsRemoveItemBillingMethod = {
  Prepaid: "prepaid",
  UsageBased: "usage_based",
} as const;
/**
 * Match items with this billing method (prepaid or usage_based).
 */
export type ListPlansVariantDetailsRemoveItemBillingMethod = OpenEnum<
  typeof ListPlansVariantDetailsRemoveItemBillingMethod
>;

export const ListPlansIntervalVariantDetailsRemoveItemEnum2 = {
  OneOff: "one_off",
  Minute: "minute",
  Hour: "hour",
  Day: "day",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
export type ListPlansIntervalVariantDetailsRemoveItemEnum2 = OpenEnum<
  typeof ListPlansIntervalVariantDetailsRemoveItemEnum2
>;

export const ListPlansIntervalVariantDetailsRemoveItemEnum1 = {
  OneOff: "one_off",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
export type ListPlansIntervalVariantDetailsRemoveItemEnum1 = OpenEnum<
  typeof ListPlansIntervalVariantDetailsRemoveItemEnum1
>;

/**
 * Match items with this interval. Accepts either a BillingInterval (price-side) or a ResetInterval (reset-side, includes day/hour/minute) so price-less items keyed by reset.interval can be disambiguated.
 */
export type ListPlansVariantDetailsIntervalUnion =
  | ListPlansIntervalVariantDetailsRemoveItemEnum1
  | ListPlansIntervalVariantDetailsRemoveItemEnum2;

/**
 * Filter for matching plan items. All provided fields must match (AND).
 */
export type ListPlansVariantDetailsPlanItemFilter = {
  /**
   * Match items linked to this feature.
   */
  featureId?: string | undefined;
  /**
   * Match items with this billing method (prepaid or usage_based).
   */
  billingMethod?: ListPlansVariantDetailsRemoveItemBillingMethod | undefined;
  /**
   * Match items with this interval. Accepts either a BillingInterval (price-side) or a ResetInterval (reset-side, includes day/hour/minute) so price-less items keyed by reset.interval can be disambiguated.
   */
  interval?:
    | ListPlansIntervalVariantDetailsRemoveItemEnum1
    | ListPlansIntervalVariantDetailsRemoveItemEnum2
    | undefined;
  /**
   * Match items with this interval_count. Disambiguates between items that share an interval but differ in count.
   */
  intervalCount?: number | undefined;
  /**
   * Match items whose grant equals this included usage. Omitted is a wildcard.
   */
  included?: number | undefined;
};

/**
 * Unit of time for the trial ('day', 'month', 'year').
 */
export const ListPlansVariantDetailsDurationType = {
  Day: "day",
  Month: "month",
  Year: "year",
} as const;
/**
 * Unit of time for the trial ('day', 'month', 'year').
 */
export type ListPlansVariantDetailsDurationType = OpenEnum<
  typeof ListPlansVariantDetailsDurationType
>;

/**
 * Behavior when the trial ends. 'bill' charges the customer (default). 'revert' expires the trial and restores the customer's previous plan.
 */
export const ListPlansVariantDetailsOnEnd = {
  Bill: "bill",
  Revert: "revert",
} as const;
/**
 * Behavior when the trial ends. 'bill' charges the customer (default). 'revert' expires the trial and restores the customer's previous plan.
 */
export type ListPlansVariantDetailsOnEnd = OpenEnum<
  typeof ListPlansVariantDetailsOnEnd
>;

/**
 * Free trial configuration for a plan.
 */
export type ListPlansVariantDetailsFreeTrialParams = {
  /**
   * Number of duration_type periods the trial lasts.
   */
  durationLength: number;
  /**
   * Unit of time for the trial ('day', 'month', 'year').
   */
  durationType: ListPlansVariantDetailsDurationType;
  /**
   * If true, a payment method is required to start the trial and the customer is charged when it ends. Defaults to false.
   */
  cardRequired: boolean;
  /**
   * Behavior when the trial ends. 'bill' charges the customer (default). 'revert' expires the trial and restores the customer's previous plan.
   */
  onEnd?: ListPlansVariantDetailsOnEnd | undefined;
};

/**
 * The time interval for the purchase limit window.
 */
export const ListPlansVariantDetailsPurchaseLimitInterval = {
  Hour: "hour",
  Day: "day",
  Week: "week",
  Month: "month",
} as const;
/**
 * The time interval for the purchase limit window.
 */
export type ListPlansVariantDetailsPurchaseLimitInterval = OpenEnum<
  typeof ListPlansVariantDetailsPurchaseLimitInterval
>;

/**
 * Optional rate limit to cap how often auto top-ups occur. Pass count to set the current window's consumed top-ups.
 */
export type ListPlansVariantDetailsPurchaseLimit = {
  /**
   * The time interval for the purchase limit window.
   */
  interval: ListPlansVariantDetailsPurchaseLimitInterval;
  /**
   * Number of intervals in the purchase limit window.
   */
  intervalCount: number;
  /**
   * Maximum number of auto top-ups allowed within the interval.
   */
  limit: number;
  /**
   * Set the current window's consumed auto top-up count. Omit to leave runtime state unchanged.
   */
  count?: number | undefined;
};

export type ListPlansVariantDetailsAutoTopup = {
  /**
   * The ID of the feature (credit balance) to auto top-up.
   */
  featureId: string;
  /**
   * Whether auto top-up is enabled.
   */
  enabled: boolean;
  /**
   * When the balance drops below this threshold, an auto top-up will be purchased.
   */
  threshold: number;
  /**
   * Amount of credits to add per auto top-up.
   */
  quantity: number;
  /**
   * Optional rate limit to cap how often auto top-ups occur. Pass count to set the current window's consumed top-ups.
   */
  purchaseLimit?: ListPlansVariantDetailsPurchaseLimit | undefined;
  /**
   * When true, auto top-up creates a send_invoice invoice instead of auto-charging.
   */
  invoiceMode?: boolean | undefined;
};

/**
 * How overage_limit is interpreted: an absolute overage cap (default) or a percentage of the main-plan allowance.
 */
export const ListPlansVariantDetailsLimitType = {
  Absolute: "absolute",
  UsagePercentage: "usage_percentage",
} as const;
/**
 * How overage_limit is interpreted: an absolute overage cap (default) or a percentage of the main-plan allowance.
 */
export type ListPlansVariantDetailsLimitType = OpenEnum<
  typeof ListPlansVariantDetailsLimitType
>;

export type ListPlansVariantDetailsSpendLimit = {
  /**
   * Optional feature ID this spend limit applies to.
   */
  featureId?: string | undefined;
  /**
   * Whether the overage spend limit is enabled.
   */
  enabled: boolean;
  /**
   * How overage_limit is interpreted: an absolute overage cap (default) or a percentage of the main-plan allowance.
   */
  limitType?: ListPlansVariantDetailsLimitType | undefined;
  /**
   * Overage cap for the feature: absolute units, or a percent (e.g. 120) when limit_type is usage_percentage.
   */
  overageLimit?: number | undefined;
  /**
   * When true, overage for this feature is not posted to Stripe. Usage tracking and balance resets still behave normally.
   */
  skipOverageBilling?: boolean | undefined;
};

/**
 * Interval for the cap, aligned to the customer's billing cycle.
 */
export const ListPlansVariantDetailsUsageLimitInterval = {
  Day: "day",
  Week: "week",
  Month: "month",
  Year: "year",
} as const;
/**
 * Interval for the cap, aligned to the customer's billing cycle.
 */
export type ListPlansVariantDetailsUsageLimitInterval = OpenEnum<
  typeof ListPlansVariantDetailsUsageLimitInterval
>;

/**
 * Window alignment. 'billing_cycle' phases the interval to the customer's renewal time; 'utc' aligns to the UTC calendar.
 */
export const ListPlansVariantDetailsAnchor = {
  BillingCycle: "billing_cycle",
  Utc: "utc",
} as const;
/**
 * Window alignment. 'billing_cycle' phases the interval to the customer's renewal time; 'utc' aligns to the UTC calendar.
 */
export type ListPlansVariantDetailsAnchor = OpenEnum<
  typeof ListPlansVariantDetailsAnchor
>;

/**
 * When set, only usage from events whose properties match counts toward this cap. Omit to count all usage of the feature.
 */
export type ListPlansVariantDetailsUsageLimitFilter = {
  properties: { [k: string]: string };
};

export type ListPlansVariantDetailsUsageLimit = {
  /**
   * The feature this usage limit applies to.
   */
  featureId: string;
  /**
   * Whether this usage limit is enabled.
   */
  enabled: boolean;
  /**
   * Maximum units allowed per interval.
   */
  limit: number;
  /**
   * Interval for the cap, aligned to the customer's billing cycle.
   */
  interval: ListPlansVariantDetailsUsageLimitInterval;
  /**
   * Window alignment. 'billing_cycle' phases the interval to the customer's renewal time; 'utc' aligns to the UTC calendar.
   */
  anchor?: ListPlansVariantDetailsAnchor | undefined;
  /**
   * When set, only usage from events whose properties match counts toward this cap. Omit to count all usage of the feature.
   */
  filter?: ListPlansVariantDetailsUsageLimitFilter | undefined;
};

/**
 * Whether the threshold is an absolute count or a percentage of the usage allowance or remaining balance.
 */
export const ListPlansVariantDetailsThresholdType = {
  Usage: "usage",
  UsagePercentage: "usage_percentage",
  Remaining: "remaining",
  RemainingPercentage: "remaining_percentage",
} as const;
/**
 * Whether the threshold is an absolute count or a percentage of the usage allowance or remaining balance.
 */
export type ListPlansVariantDetailsThresholdType = OpenEnum<
  typeof ListPlansVariantDetailsThresholdType
>;

/**
 * What 100% means. balance: every grant on the feature. included: the plan allowance only. recurring: grants that reset. usage_limit: the cap of the usage limit with the same feature and filter.
 */
export const ListPlansVariantDetailsBasis = {
  Balance: "balance",
  Included: "included",
  Recurring: "recurring",
  UsageLimit: "usage_limit",
} as const;
/**
 * What 100% means. balance: every grant on the feature. included: the plan allowance only. recurring: grants that reset. usage_limit: the cap of the usage limit with the same feature and filter.
 */
export type ListPlansVariantDetailsBasis = OpenEnum<
  typeof ListPlansVariantDetailsBasis
>;

/**
 * Only valid with basis usage_limit. Points the alert at the usage limit carrying the same filter.
 */
export type ListPlansVariantDetailsUsageAlertFilter = {
  properties: { [k: string]: string };
};

export type ListPlansVariantDetailsUsageAlert = {
  /**
   * The feature ID this alert applies to.
   */
  featureId?: string | undefined;
  /**
   * Whether this usage alert is enabled.
   */
  enabled: boolean;
  /**
   * The threshold value that triggers the alert. For usage or remaining, this is an absolute count. For usage_percentage or remaining_percentage, this is a percentage (0-100).
   */
  threshold: number;
  /**
   * Whether the threshold is an absolute count or a percentage of the usage allowance or remaining balance.
   */
  thresholdType: ListPlansVariantDetailsThresholdType;
  /**
   * What 100% means. balance: every grant on the feature. included: the plan allowance only. recurring: grants that reset. usage_limit: the cap of the usage limit with the same feature and filter.
   */
  basis: ListPlansVariantDetailsBasis;
  /**
   * Only valid with basis usage_limit. Points the alert at the usage limit carrying the same filter.
   */
  filter?: ListPlansVariantDetailsUsageAlertFilter | undefined;
  /**
   * Optional user-defined label to distinguish multiple alerts on the same feature.
   */
  name?: string | undefined;
};

export type ListPlansVariantDetailsOverageAllowed = {
  /**
   * The feature ID this overage allowed control applies to.
   */
  featureId: string;
  /**
   * Whether overage is allowed for this feature.
   */
  enabled: boolean;
};

/**
 * Override the plan's billing controls (auto top-ups, spend limits, usage limits, usage alerts, overage allowed) for this customer.
 */
export type ListPlansVariantDetailsBillingControls = {
  /**
   * List of auto top-up configurations per feature.
   */
  autoTopups?: Array<ListPlansVariantDetailsAutoTopup> | undefined;
  /**
   * List of overage spend limits per feature (caps overage spend).
   */
  spendLimits?: Array<ListPlansVariantDetailsSpendLimit> | undefined;
  /**
   * List of hard usage caps per feature (max units per interval).
   */
  usageLimits?: Array<ListPlansVariantDetailsUsageLimit> | undefined;
  /**
   * List of usage alert configurations per feature.
   */
  usageAlerts?: Array<ListPlansVariantDetailsUsageAlert> | undefined;
  /**
   * List of overage allowed controls per feature. When enabled, usage can exceed balance.
   */
  overageAllowed?: Array<ListPlansVariantDetailsOverageAllowed> | undefined;
};

/**
 * Billing interval (e.g. 'month', 'year').
 */
export const ListPlansVariantDetailsUpsertLicenseInterval = {
  OneOff: "one_off",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
/**
 * Billing interval (e.g. 'month', 'year').
 */
export type ListPlansVariantDetailsUpsertLicenseInterval = OpenEnum<
  typeof ListPlansVariantDetailsUpsertLicenseInterval
>;

export type ListPlansUpsertLicenseAdditionalCurrency = {
  currency?: any | undefined;
  amount?: any | undefined;
};

/**
 * Base price configuration for a plan.
 */
export type ListPlansVariantDetailsUpsertLicenseBasePrice = {
  /**
   * Base price amount for the plan, in major currency units (e.g. dollars).
   */
  amount: number;
  /**
   * Billing interval (e.g. 'month', 'year').
   */
  interval: ListPlansVariantDetailsUpsertLicenseInterval;
  /**
   * Number of intervals per billing cycle. Defaults to 1.
   */
  intervalCount?: number | undefined;
  /**
   * Base price amounts in additional currencies. The base 'amount' is in the org's default currency.
   */
  additionalCurrencies?:
    | Array<ListPlansUpsertLicenseAdditionalCurrency>
    | undefined;
};

/**
 * Reset configuration for consumable features. Omit for non-consumable features like seats.
 */
export type ListPlansUpsertLicenseReset = {
  interval?: any | undefined;
  intervalCount?: any | undefined;
};

/**
 * Pricing for usage beyond included units. Omit for free features.
 */
export type ListPlansUpsertLicensePrice = {
  stripePriceId?: any | undefined;
  amount?: any | undefined;
  additionalCurrencies?: any | undefined;
  tiers?: any | undefined;
  tierBehavior?: any | undefined;
  interval?: any | undefined;
  intervalCount?: any | undefined;
  billingUnits?: any | undefined;
  billingMethod?: any | undefined;
  maxPurchase?: any | undefined;
};

/**
 * Proration settings for prepaid features. Controls mid-cycle quantity change billing.
 */
export type ListPlansUpsertLicenseProration = {
  onIncrease?: any | undefined;
  onDecrease?: any | undefined;
};

/**
 * Rollover config for unused units. If set, unused included units carry over.
 */
export type ListPlansUpsertLicenseRollover = {
  max?: any | undefined;
  maxPercentage?: any | undefined;
  expiryDurationType?: any | undefined;
  expiryDurationLength?: any | undefined;
};

/**
 * Overrides fields of this item's feature for customers on this plan (e.g. a credit system's credit_schema).
 */
export type ListPlansUpsertLicenseFeatureOverride = {
  creditSchema?: any | undefined;
};

/**
 * Configuration for a feature item in a plan, including usage limits, pricing, and rollover settings.
 */
export type ListPlansVariantDetailsUpsertLicensePlanItem = {
  /**
   * The ID of the feature to configure.
   */
  featureId: string;
  /**
   * Number of free units included. Balance resets to this each interval for consumable features.
   */
  included?: number | undefined;
  /**
   * If true, customer has unlimited access to this feature.
   */
  unlimited?: boolean | undefined;
  /**
   * Whether entity-level grants contribute to a shared customer balance.
   */
  pooled: boolean;
  /**
   * Reset configuration for consumable features. Omit for non-consumable features like seats.
   */
  reset?: ListPlansUpsertLicenseReset | undefined;
  /**
   * Pricing for usage beyond included units. Omit for free features.
   */
  price?: ListPlansUpsertLicensePrice | undefined;
  /**
   * Proration settings for prepaid features. Controls mid-cycle quantity change billing.
   */
  proration?: ListPlansUpsertLicenseProration | undefined;
  /**
   * Rollover config for unused units. If set, unused included units carry over.
   */
  rollover?: ListPlansUpsertLicenseRollover | undefined;
  /**
   * Overrides fields of this item's feature for customers on this plan (e.g. a credit system's credit_schema).
   */
  featureOverride?: ListPlansUpsertLicenseFeatureOverride | undefined;
};

/**
 * Match items with this billing method (prepaid or usage_based).
 */
export const ListPlansUpsertLicenseBillingMethod = {
  Prepaid: "prepaid",
  UsageBased: "usage_based",
} as const;
/**
 * Match items with this billing method (prepaid or usage_based).
 */
export type ListPlansUpsertLicenseBillingMethod = OpenEnum<
  typeof ListPlansUpsertLicenseBillingMethod
>;

/**
 * Filter for matching plan items. All provided fields must match (AND).
 */
export type ListPlansVariantDetailsUpsertLicensePlanItemFilter = {
  /**
   * Match items linked to this feature.
   */
  featureId?: string | undefined;
  /**
   * Match items with this billing method (prepaid or usage_based).
   */
  billingMethod?: ListPlansUpsertLicenseBillingMethod | undefined;
  /**
   * Match items with this interval. Accepts either a BillingInterval (price-side) or a ResetInterval (reset-side, includes day/hour/minute) so price-less items keyed by reset.interval can be disambiguated.
   */
  interval?: any | undefined;
  /**
   * Match items with this interval_count. Disambiguates between items that share an interval but differ in count.
   */
  intervalCount?: number | undefined;
  /**
   * Match items whose grant equals this included usage. Omitted is a wildcard.
   */
  included?: number | undefined;
};

export type ListPlansVariantDetailsUpsertLicenseCustomize = {
  price?: ListPlansVariantDetailsUpsertLicenseBasePrice | null | undefined;
  addItems?: Array<ListPlansVariantDetailsUpsertLicensePlanItem> | undefined;
  removeItems?:
    | Array<ListPlansVariantDetailsUpsertLicensePlanItemFilter>
    | undefined;
};

export type ListPlansVariantDetailsUpsertLicense = {
  licensePlanId: string;
  versionSlug?: string | undefined;
  included?: number | undefined;
  prepaidOnly?: boolean | undefined;
  customize?: ListPlansVariantDetailsUpsertLicenseCustomize | null | undefined;
  metadata?: { [k: string]: any } | undefined;
};

export type ListPlansVariantDetailsRemoveLicense = {
  licensePlanId: string;
};

/**
 * The customization that transforms the base plan into this variant.
 */
export type ListPlansVariantDetailsCustomize = {
  /**
   * Override the base price of the plan. Pass null to remove the base price.
   */
  price?: ListPlansVariantDetailsBasePrice | null | undefined;
  /**
   * Items to add to the plan.
   */
  addItems?: Array<ListPlansVariantDetailsPlanItem> | undefined;
  /**
   * Filters selecting items to remove from the plan.
   */
  removeItems?: Array<ListPlansVariantDetailsPlanItemFilter> | undefined;
  /**
   * Override the plan's default free trial. Pass an object to set a custom trial, or null to remove the trial entirely.
   */
  freeTrial?: ListPlansVariantDetailsFreeTrialParams | null | undefined;
  /**
   * Override the plan's billing controls (auto top-ups, spend limits, usage limits, usage alerts, overage allowed) for this customer.
   */
  billingControls?: ListPlansVariantDetailsBillingControls | undefined;
  /**
   * License links to add or override for this customer, keyed by license_plan_id. Omitted fields inherit the plan catalog link (included defaults to 1 when the license is not in the catalog). A bare entry restores the license to pure catalog inheritance.
   */
  upsertLicenses?: Array<ListPlansVariantDetailsUpsertLicense> | undefined;
  /**
   * License links to drop, keyed by license_plan_id. Parallel to remove_items.
   */
  removeLicenses?: Array<ListPlansVariantDetailsRemoveLicense> | undefined;
};

/**
 * Details about how this variant relates to its latest base plan.
 */
export type ListPlansVariantDetails = {
  /**
   * The ID of the base plan this variant was derived from.
   */
  basePlanId: string;
  /**
   * The customization that transforms the base plan into this variant.
   */
  customize?: ListPlansVariantDetailsCustomize | undefined;
};

/**
 * Billing interval (e.g. 'month', 'year').
 */
export const ListPlansPriceLicenseInterval = {
  OneOff: "one_off",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
/**
 * Billing interval (e.g. 'month', 'year').
 */
export type ListPlansPriceLicenseInterval = OpenEnum<
  typeof ListPlansPriceLicenseInterval
>;

export type ListPlansLicenseAdditionalCurrency = {
  /**
   * Three-letter Stripe-supported currency code (e.g. 'eur', 'gbp').
   */
  currency: string;
  /**
   * Price amount in this currency. Set explicitly per currency, not converted from the base amount.
   */
  amount: number;
};

/**
 * Base price configuration for a plan.
 */
export type ListPlansLicenseBasePrice = {
  /**
   * Base price amount for the plan, in major currency units (e.g. dollars).
   */
  amount: number;
  /**
   * Billing interval (e.g. 'month', 'year').
   */
  interval: ListPlansPriceLicenseInterval;
  /**
   * Number of intervals per billing cycle. Defaults to 1.
   */
  intervalCount?: number | undefined;
  /**
   * Base price amounts in additional currencies. The base 'amount' is in the org's default currency.
   */
  additionalCurrencies?: Array<ListPlansLicenseAdditionalCurrency> | undefined;
};

/**
 * Interval at which balance resets (e.g. 'month', 'year'). For consumable features only.
 */
export const ListPlansLicenseResetInterval = {
  OneOff: "one_off",
  Minute: "minute",
  Hour: "hour",
  Day: "day",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
/**
 * Interval at which balance resets (e.g. 'month', 'year'). For consumable features only.
 */
export type ListPlansLicenseResetInterval = OpenEnum<
  typeof ListPlansLicenseResetInterval
>;

/**
 * Reset configuration for consumable features. Omit for non-consumable features like seats.
 */
export type ListPlansLicenseReset = {
  /**
   * Interval at which balance resets (e.g. 'month', 'year'). For consumable features only.
   */
  interval: ListPlansLicenseResetInterval;
  /**
   * Number of intervals between resets. Defaults to 1.
   */
  intervalCount?: number | undefined;
};

export type ListPlansLicenseAddItemAdditionalCurrency = {
  currency?: any | undefined;
  amount?: any | undefined;
};

export type ListPlansLicenseTier = {
  to?: any | undefined;
  amount?: any | undefined;
  flatAmount?: any | undefined;
  additionalCurrencies?: any | undefined;
};

export const ListPlansLicenseTierBehavior = {
  Graduated: "graduated",
  Volume: "volume",
} as const;
export type ListPlansLicenseTierBehavior = OpenEnum<
  typeof ListPlansLicenseTierBehavior
>;

/**
 * Billing interval. For consumable features, should match reset.interval.
 */
export const ListPlansLicenseAddItemPriceInterval = {
  OneOff: "one_off",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
/**
 * Billing interval. For consumable features, should match reset.interval.
 */
export type ListPlansLicenseAddItemPriceInterval = OpenEnum<
  typeof ListPlansLicenseAddItemPriceInterval
>;

/**
 * 'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go.
 */
export const ListPlansLicenseAddItemBillingMethod = {
  Prepaid: "prepaid",
  UsageBased: "usage_based",
} as const;
/**
 * 'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go.
 */
export type ListPlansLicenseAddItemBillingMethod = OpenEnum<
  typeof ListPlansLicenseAddItemBillingMethod
>;

/**
 * Pricing for usage beyond included units. Omit for free features.
 */
export type ListPlansLicensePrice = {
  /**
   * Price per billing_units after included usage. Either 'amount' or 'tiers' is required.
   */
  amount?: number | undefined;
  /**
   * Amounts in additional currencies for this flat price. The base 'amount' is in the org's default currency. Only valid with 'amount', not 'tiers'.
   */
  additionalCurrencies?:
    | Array<ListPlansLicenseAddItemAdditionalCurrency>
    | undefined;
  /**
   * Tiered pricing.  Either 'amount' or 'tiers' is required.
   */
  tiers?: Array<ListPlansLicenseTier> | undefined;
  tierBehavior?: ListPlansLicenseTierBehavior | undefined;
  /**
   * Billing interval. For consumable features, should match reset.interval.
   */
  interval: ListPlansLicenseAddItemPriceInterval;
  /**
   * Number of intervals per billing cycle. Defaults to 1.
   */
  intervalCount: number;
  /**
   * Units per price increment. Usage is rounded UP when billed (e.g. billing_units=100 means 101 rounds to 200).
   */
  billingUnits: number;
  /**
   * 'prepaid' for upfront payment (seats), 'usage_based' for pay-as-you-go.
   */
  billingMethod: ListPlansLicenseAddItemBillingMethod;
  /**
   * Max units purchasable beyond included. E.g. included=100, max_purchase=300 allows 400 total. Null for no limit.
   */
  maxPurchase?: number | null | undefined;
};

/**
 * Billing behavior when quantity increases mid-cycle.
 */
export const ListPlansLicenseOnIncrease = {
  BillImmediately: "bill_immediately",
  ProrateImmediately: "prorate_immediately",
  ProrateNextCycle: "prorate_next_cycle",
  BillNextCycle: "bill_next_cycle",
} as const;
/**
 * Billing behavior when quantity increases mid-cycle.
 */
export type ListPlansLicenseOnIncrease = OpenEnum<
  typeof ListPlansLicenseOnIncrease
>;

/**
 * Credit behavior when quantity decreases mid-cycle.
 */
export const ListPlansLicenseOnDecrease = {
  Prorate: "prorate",
  ProrateImmediately: "prorate_immediately",
  ProrateNextCycle: "prorate_next_cycle",
  None: "none",
  NoProrations: "no_prorations",
} as const;
/**
 * Credit behavior when quantity decreases mid-cycle.
 */
export type ListPlansLicenseOnDecrease = OpenEnum<
  typeof ListPlansLicenseOnDecrease
>;

/**
 * Proration settings for prepaid features. Controls mid-cycle quantity change billing.
 */
export type ListPlansLicenseProration = {
  /**
   * Billing behavior when quantity increases mid-cycle.
   */
  onIncrease: ListPlansLicenseOnIncrease;
  /**
   * Credit behavior when quantity decreases mid-cycle.
   */
  onDecrease: ListPlansLicenseOnDecrease;
};

/**
 * When rolled over units expire.
 */
export const ListPlansLicenseExpiryDurationType = {
  Month: "month",
  Forever: "forever",
} as const;
/**
 * When rolled over units expire.
 */
export type ListPlansLicenseExpiryDurationType = OpenEnum<
  typeof ListPlansLicenseExpiryDurationType
>;

/**
 * Rollover config for unused units. If set, unused included units carry over.
 */
export type ListPlansLicenseRollover = {
  /**
   * Max rollover units. Omit for unlimited rollover.
   */
  max?: number | undefined;
  /**
   * Maximum rollover as a percentage (0-100) of included + prepaid grant. Mutually exclusive with max.
   */
  maxPercentage?: number | undefined;
  /**
   * When rolled over units expire.
   */
  expiryDurationType: ListPlansLicenseExpiryDurationType;
  /**
   * Number of periods before expiry.
   */
  expiryDurationLength?: number | undefined;
};

/**
 * Overrides fields of this item's feature for customers on this plan (e.g. a credit system's credit_schema).
 */
export type ListPlansLicenseFeatureOverride = {
  /**
   * For credit system features: replaces the feature's credit_schema entirely for customers on this plan.
   */
  creditSchema?: Array<any | null> | undefined;
};

/**
 * Configuration for a feature item in a plan, including usage limits, pricing, and rollover settings.
 */
export type ListPlansLicensePlanItem = {
  /**
   * The ID of the feature to configure.
   */
  featureId: string;
  /**
   * Number of free units included. Balance resets to this each interval for consumable features.
   */
  included?: number | undefined;
  /**
   * If true, customer has unlimited access to this feature.
   */
  unlimited?: boolean | undefined;
  /**
   * Whether entity-level grants contribute to a shared customer balance.
   */
  pooled: boolean;
  /**
   * Reset configuration for consumable features. Omit for non-consumable features like seats.
   */
  reset?: ListPlansLicenseReset | undefined;
  /**
   * Pricing for usage beyond included units. Omit for free features.
   */
  price?: ListPlansLicensePrice | undefined;
  /**
   * Proration settings for prepaid features. Controls mid-cycle quantity change billing.
   */
  proration?: ListPlansLicenseProration | undefined;
  /**
   * Rollover config for unused units. If set, unused included units carry over.
   */
  rollover?: ListPlansLicenseRollover | undefined;
  /**
   * Overrides fields of this item's feature for customers on this plan (e.g. a credit system's credit_schema).
   */
  featureOverride?: ListPlansLicenseFeatureOverride | undefined;
};

/**
 * Match items with this billing method (prepaid or usage_based).
 */
export const ListPlansLicenseRemoveItemBillingMethod = {
  Prepaid: "prepaid",
  UsageBased: "usage_based",
} as const;
/**
 * Match items with this billing method (prepaid or usage_based).
 */
export type ListPlansLicenseRemoveItemBillingMethod = OpenEnum<
  typeof ListPlansLicenseRemoveItemBillingMethod
>;

export const ListPlansIntervalLicenseRemoveItemEnum2 = {
  OneOff: "one_off",
  Minute: "minute",
  Hour: "hour",
  Day: "day",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
export type ListPlansIntervalLicenseRemoveItemEnum2 = OpenEnum<
  typeof ListPlansIntervalLicenseRemoveItemEnum2
>;

export const ListPlansIntervalLicenseRemoveItemEnum1 = {
  OneOff: "one_off",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
export type ListPlansIntervalLicenseRemoveItemEnum1 = OpenEnum<
  typeof ListPlansIntervalLicenseRemoveItemEnum1
>;

/**
 * Match items with this interval. Accepts either a BillingInterval (price-side) or a ResetInterval (reset-side, includes day/hour/minute) so price-less items keyed by reset.interval can be disambiguated.
 */
export type ListPlansLicenseIntervalUnion =
  | ListPlansIntervalLicenseRemoveItemEnum1
  | ListPlansIntervalLicenseRemoveItemEnum2;

/**
 * Filter for matching plan items. All provided fields must match (AND).
 */
export type ListPlansLicensePlanItemFilter = {
  /**
   * Match items linked to this feature.
   */
  featureId?: string | undefined;
  /**
   * Match items with this billing method (prepaid or usage_based).
   */
  billingMethod?: ListPlansLicenseRemoveItemBillingMethod | undefined;
  /**
   * Match items with this interval. Accepts either a BillingInterval (price-side) or a ResetInterval (reset-side, includes day/hour/minute) so price-less items keyed by reset.interval can be disambiguated.
   */
  interval?:
    | ListPlansIntervalLicenseRemoveItemEnum1
    | ListPlansIntervalLicenseRemoveItemEnum2
    | undefined;
  /**
   * Match items with this interval_count. Disambiguates between items that share an interval but differ in count.
   */
  intervalCount?: number | undefined;
  /**
   * Match items whose grant equals this included usage. Omitted is a wildcard.
   */
  included?: number | undefined;
};

/**
 * The item and price diff applied to this parent-plan link.
 */
export type ListPlansLicenseCustomize = {
  price?: ListPlansLicenseBasePrice | null | undefined;
  addItems?: Array<ListPlansLicensePlanItem> | undefined;
  removeItems?: Array<ListPlansLicensePlanItemFilter> | undefined;
};

export type ListPlansLicense = {
  /**
   * The plan offered as a license under this plan.
   */
  licensePlanId: string;
  /**
   * The exact license-plan version pinned by this link.
   */
  version: number;
  /**
   * Version slug of the license-plan row this link points at.
   */
  versionSlug?: string | undefined;
  /**
   * Number of license assignments included with this plan for free.
   */
  included: number;
  /**
   * Assignments are capped at the included quantity. Must be true for now; overflow billing (false) is not yet available.
   */
  prepaidOnly: boolean;
  /**
   * The item and price diff applied to this parent-plan link.
   */
  customize?: ListPlansLicenseCustomize | undefined;
  plan?: Plan | undefined;
};

/**
 * Billing interval (e.g. 'month', 'year').
 */
export const ListPlansPriceVariantCustomizeInterval = {
  OneOff: "one_off",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
/**
 * Billing interval (e.g. 'month', 'year').
 */
export type ListPlansPriceVariantCustomizeInterval = OpenEnum<
  typeof ListPlansPriceVariantCustomizeInterval
>;

export type ListPlansVariantCustomizeAdditionalCurrency = {
  /**
   * Three-letter Stripe-supported currency code (e.g. 'eur', 'gbp').
   */
  currency: string;
  /**
   * Price amount in this currency. Set explicitly per currency, not converted from the base amount.
   */
  amount: number;
};

/**
 * Base price configuration for a plan.
 */
export type ListPlansVariantBasePrice = {
  /**
   * Base price amount for the plan, in major currency units (e.g. dollars).
   */
  amount: number;
  /**
   * Billing interval (e.g. 'month', 'year').
   */
  interval: ListPlansPriceVariantCustomizeInterval;
  /**
   * Number of intervals per billing cycle. Defaults to 1.
   */
  intervalCount?: number | undefined;
  /**
   * Base price amounts in additional currencies. The base 'amount' is in the org's default currency.
   */
  additionalCurrencies?:
    | Array<ListPlansVariantCustomizeAdditionalCurrency>
    | undefined;
};

/**
 * Interval at which balance resets (e.g. 'month', 'year'). For consumable features only.
 */
export const ListPlansVariantAddItemResetInterval = {
  OneOff: "one_off",
  Minute: "minute",
  Hour: "hour",
  Day: "day",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
/**
 * Interval at which balance resets (e.g. 'month', 'year'). For consumable features only.
 */
export type ListPlansVariantAddItemResetInterval = OpenEnum<
  typeof ListPlansVariantAddItemResetInterval
>;

/** @internal */
export type ListPlansParams$Outbound = {
  customer_id?: string | undefined;
  entity_id?: string | undefined;
  include_archived?: boolean | undefined;
  all_versions?: boolean | undefined;
};

/** @internal */
export const ListPlansParams$outboundSchema: z.ZodMiniType<
  ListPlansParams$Outbound,
  ListPlansParams
> = z.pipe(
  z.object({
    customerId: z.optional(z.string()),
    entityId: z.optional(z.string()),
    includeArchived: z.optional(z.boolean()),
    allVersions: z.optional(z.boolean()),
  }),
  z.transform((v) => {
    return remap$(v, {
      customerId: "customer_id",
      entityId: "entity_id",
      includeArchived: "include_archived",
      allVersions: "all_versions",
    });
  }),
);

export function listPlansParamsToJSON(
  listPlansParams: ListPlansParams,
): string {
  return JSON.stringify(ListPlansParams$outboundSchema.parse(listPlansParams));
}

/** @internal */
export const ListPlansAdditionalCurrency$inboundSchema: z.ZodMiniType<
  ListPlansAdditionalCurrency,
  unknown
> = z.object({
  currency: types.string(),
  amount: types.number(),
});

export function listPlansAdditionalCurrencyFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansAdditionalCurrency, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansAdditionalCurrency$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansAdditionalCurrency' from JSON`,
  );
}

/** @internal */
export const ListPlansPriceInterval$inboundSchema: z.ZodMiniType<
  ListPlansPriceInterval,
  unknown
> = openEnums.inboundSchema(ListPlansPriceInterval);

/** @internal */
export const ListPlansPriceDisplay$inboundSchema: z.ZodMiniType<
  ListPlansPriceDisplay,
  unknown
> = z.pipe(
  z.object({
    primary_text: types.string(),
    secondary_text: types.optional(types.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "primary_text": "primaryText",
      "secondary_text": "secondaryText",
    });
  }),
);

export function listPlansPriceDisplayFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansPriceDisplay, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansPriceDisplay$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansPriceDisplay' from JSON`,
  );
}

/** @internal */
export const ListPlansPriceStripe$inboundSchema: z.ZodMiniType<
  ListPlansPriceStripe,
  unknown
> = z.pipe(
  z.object({
    price_id: types.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "price_id": "priceId",
    });
  }),
);

export function listPlansPriceStripeFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansPriceStripe, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansPriceStripe$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansPriceStripe' from JSON`,
  );
}

/** @internal */
export const ListPlansPriceProcessors$inboundSchema: z.ZodMiniType<
  ListPlansPriceProcessors,
  unknown
> = z.object({
  stripe: z.optional(
    z.nullable(z.lazy(() => ListPlansPriceStripe$inboundSchema)),
  ),
});

export function listPlansPriceProcessorsFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansPriceProcessors, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansPriceProcessors$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansPriceProcessors' from JSON`,
  );
}

/** @internal */
export const ListPlansPrice$inboundSchema: z.ZodMiniType<
  ListPlansPrice,
  unknown
> = z.pipe(
  z.object({
    amount: types.number(),
    additional_currencies: types.optional(
      z.array(z.lazy(() => ListPlansAdditionalCurrency$inboundSchema)),
    ),
    interval: ListPlansPriceInterval$inboundSchema,
    interval_count: types.optional(types.number()),
    display: types.optional(z.lazy(() => ListPlansPriceDisplay$inboundSchema)),
    processors: types.optional(
      z.lazy(() => ListPlansPriceProcessors$inboundSchema),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "additional_currencies": "additionalCurrencies",
      "interval_count": "intervalCount",
    });
  }),
);

export function listPlansPriceFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansPrice, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansPrice$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansPrice' from JSON`,
  );
}

/** @internal */
export const ListPlansType$inboundSchema: z.ZodMiniType<
  ListPlansType,
  unknown
> = openEnums.inboundSchema(ListPlansType);

/** @internal */
export const ListPlansFeatureDisplay$inboundSchema: z.ZodMiniType<
  ListPlansFeatureDisplay,
  unknown
> = z.object({
  singular: types.string(),
  plural: types.string(),
});

export function listPlansFeatureDisplayFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansFeatureDisplay, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansFeatureDisplay$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansFeatureDisplay' from JSON`,
  );
}

/** @internal */
export const ListPlansFeatureCreditSchema$inboundSchema: z.ZodMiniType<
  ListPlansFeatureCreditSchema,
  unknown
> = z.pipe(
  z.object({
    metered_feature_id: types.string(),
    credit_cost: types.number(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "metered_feature_id": "meteredFeatureId",
      "credit_cost": "creditCost",
    });
  }),
);

export function listPlansFeatureCreditSchemaFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansFeatureCreditSchema, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansFeatureCreditSchema$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansFeatureCreditSchema' from JSON`,
  );
}

/** @internal */
export const ListPlansFeature$inboundSchema: z.ZodMiniType<
  ListPlansFeature,
  unknown
> = z.pipe(
  z.object({
    id: types.string(),
    name: z.optional(z.nullable(types.string())),
    type: ListPlansType$inboundSchema,
    display: z.optional(
      z.nullable(z.lazy(() => ListPlansFeatureDisplay$inboundSchema)),
    ),
    credit_schema: z.optional(z.nullable(z.array(z.lazy(() =>
      ListPlansFeatureCreditSchema$inboundSchema
    )))),
    archived: z.optional(z.nullable(types.boolean())),
  }),
  z.transform((v) => {
    return remap$(v, {
      "credit_schema": "creditSchema",
    });
  }),
);

export function listPlansFeatureFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansFeature, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansFeature$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansFeature' from JSON`,
  );
}

/** @internal */
export const ListPlansResetItemInterval$inboundSchema: z.ZodMiniType<
  ListPlansResetItemInterval,
  unknown
> = openEnums.inboundSchema(ListPlansResetItemInterval);

/** @internal */
export const ListPlansItemReset$inboundSchema: z.ZodMiniType<
  ListPlansItemReset,
  unknown
> = z.pipe(
  z.object({
    interval: ListPlansResetItemInterval$inboundSchema,
    interval_count: types.optional(types.number()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "interval_count": "intervalCount",
    });
  }),
);

export function listPlansItemResetFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansItemReset, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansItemReset$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansItemReset' from JSON`,
  );
}

/** @internal */
export const ListPlansItemAdditionalCurrency$inboundSchema: z.ZodMiniType<
  ListPlansItemAdditionalCurrency,
  unknown
> = z.object({
  currency: types.string(),
  amount: types.number(),
});

export function listPlansItemAdditionalCurrencyFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansItemAdditionalCurrency, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansItemAdditionalCurrency$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansItemAdditionalCurrency' from JSON`,
  );
}

/** @internal */
export const ListPlansTo$inboundSchema: z.ZodMiniType<ListPlansTo, unknown> =
  smartUnion([types.number(), types.string()]);

export function listPlansToFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansTo, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansTo$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansTo' from JSON`,
  );
}

/** @internal */
export const ListPlansTierAdditionalCurrency$inboundSchema: z.ZodMiniType<
  ListPlansTierAdditionalCurrency,
  unknown
> = z.pipe(
  z.object({
    currency: types.string(),
    amount: types.optional(types.number()),
    flat_amount: types.optional(types.number()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "flat_amount": "flatAmount",
    });
  }),
);

export function listPlansTierAdditionalCurrencyFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansTierAdditionalCurrency, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansTierAdditionalCurrency$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansTierAdditionalCurrency' from JSON`,
  );
}

/** @internal */
export const ListPlansPriceItemTier$inboundSchema: z.ZodMiniType<
  ListPlansPriceItemTier,
  unknown
> = z.pipe(
  z.object({
    to: smartUnion([types.number(), types.string()]),
    amount: types.number(),
    flat_amount: types.optional(types.number()),
    additional_currencies: types.optional(
      z.array(z.lazy(() => ListPlansTierAdditionalCurrency$inboundSchema)),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "flat_amount": "flatAmount",
      "additional_currencies": "additionalCurrencies",
    });
  }),
);

export function listPlansPriceItemTierFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansPriceItemTier, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansPriceItemTier$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansPriceItemTier' from JSON`,
  );
}

/** @internal */
export const ListPlansItemTierBehavior$inboundSchema: z.ZodMiniType<
  ListPlansItemTierBehavior,
  unknown
> = openEnums.inboundSchema(ListPlansItemTierBehavior);

/** @internal */
export const ListPlansPriceItemInterval$inboundSchema: z.ZodMiniType<
  ListPlansPriceItemInterval,
  unknown
> = openEnums.inboundSchema(ListPlansPriceItemInterval);

/** @internal */
export const ListPlansItemBillingMethod$inboundSchema: z.ZodMiniType<
  ListPlansItemBillingMethod,
  unknown
> = openEnums.inboundSchema(ListPlansItemBillingMethod);

/** @internal */
export const ListPlansItemStripe$inboundSchema: z.ZodMiniType<
  ListPlansItemStripe,
  unknown
> = z.pipe(
  z.object({
    price_id: types.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "price_id": "priceId",
    });
  }),
);

export function listPlansItemStripeFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansItemStripe, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansItemStripe$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansItemStripe' from JSON`,
  );
}

/** @internal */
export const ListPlansItemProcessors$inboundSchema: z.ZodMiniType<
  ListPlansItemProcessors,
  unknown
> = z.object({
  stripe: z.optional(
    z.nullable(z.lazy(() => ListPlansItemStripe$inboundSchema)),
  ),
});

export function listPlansItemProcessorsFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansItemProcessors, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansItemProcessors$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansItemProcessors' from JSON`,
  );
}

/** @internal */
export const ListPlansItemPrice$inboundSchema: z.ZodMiniType<
  ListPlansItemPrice,
  unknown
> = z.pipe(
  z.object({
    amount: types.optional(types.number()),
    additional_currencies: types.optional(
      z.array(z.lazy(() => ListPlansItemAdditionalCurrency$inboundSchema)),
    ),
    tiers: types.optional(
      z.array(z.lazy(() => ListPlansPriceItemTier$inboundSchema)),
    ),
    tier_behavior: types.optional(ListPlansItemTierBehavior$inboundSchema),
    interval: ListPlansPriceItemInterval$inboundSchema,
    interval_count: types.optional(types.number()),
    billing_units: types.number(),
    billing_method: ListPlansItemBillingMethod$inboundSchema,
    max_purchase: types.nullable(types.number()),
    processors: types.optional(
      z.lazy(() => ListPlansItemProcessors$inboundSchema),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "additional_currencies": "additionalCurrencies",
      "tier_behavior": "tierBehavior",
      "interval_count": "intervalCount",
      "billing_units": "billingUnits",
      "billing_method": "billingMethod",
      "max_purchase": "maxPurchase",
    });
  }),
);

export function listPlansItemPriceFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansItemPrice, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansItemPrice$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansItemPrice' from JSON`,
  );
}

/** @internal */
export const ListPlansItemDisplay$inboundSchema: z.ZodMiniType<
  ListPlansItemDisplay,
  unknown
> = z.pipe(
  z.object({
    primary_text: types.string(),
    secondary_text: types.optional(types.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "primary_text": "primaryText",
      "secondary_text": "secondaryText",
    });
  }),
);

export function listPlansItemDisplayFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansItemDisplay, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansItemDisplay$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansItemDisplay' from JSON`,
  );
}

/** @internal */
export const ListPlansItemExpiryDurationType$inboundSchema: z.ZodMiniType<
  ListPlansItemExpiryDurationType,
  unknown
> = openEnums.inboundSchema(ListPlansItemExpiryDurationType);

/** @internal */
export const ListPlansItemRollover$inboundSchema: z.ZodMiniType<
  ListPlansItemRollover,
  unknown
> = z.pipe(
  z.object({
    max: types.nullable(types.number()),
    max_percentage: z.optional(z.nullable(types.number())),
    expiry_duration_type: ListPlansItemExpiryDurationType$inboundSchema,
    expiry_duration_length: types.optional(types.number()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "max_percentage": "maxPercentage",
      "expiry_duration_type": "expiryDurationType",
      "expiry_duration_length": "expiryDurationLength",
    });
  }),
);

export function listPlansItemRolloverFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansItemRollover, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansItemRollover$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansItemRollover' from JSON`,
  );
}

/** @internal */
export const ListPlansCreditSchemaItemFeatureOverride2$inboundSchema:
  z.ZodMiniType<ListPlansCreditSchemaItemFeatureOverride2, unknown> = z.pipe(
    z.object({
      metered_feature_id: types.string(),
      billing_units: types.optional(types.number()),
      credit_cost: types.number(),
    }),
    z.transform((v) => {
      return remap$(v, {
        "metered_feature_id": "meteredFeatureId",
        "billing_units": "billingUnits",
        "credit_cost": "creditCost",
      });
    }),
  );

export function listPlansCreditSchemaItemFeatureOverride2FromJSON(
  jsonString: string,
): SafeParseResult<
  ListPlansCreditSchemaItemFeatureOverride2,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      ListPlansCreditSchemaItemFeatureOverride2$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'ListPlansCreditSchemaItemFeatureOverride2' from JSON`,
  );
}

/** @internal */
export const ListPlansFeatureOverrideTier$inboundSchema: z.ZodMiniType<
  ListPlansFeatureOverrideTier,
  unknown
> = z.pipe(
  z.object({
    to: types.optional(z.any()),
    credit_cost: types.optional(z.any()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "credit_cost": "creditCost",
    });
  }),
);

export function listPlansFeatureOverrideTierFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansFeatureOverrideTier, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansFeatureOverrideTier$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansFeatureOverrideTier' from JSON`,
  );
}

/** @internal */
export const ListPlansCreditSchemaItemFeatureOverride1$inboundSchema:
  z.ZodMiniType<ListPlansCreditSchemaItemFeatureOverride1, unknown> = z.pipe(
    z.object({
      metered_feature_id: types.string(),
      billing_units: types.optional(types.number()),
      tier_behavior: types.literal("graduated"),
      tiers: z.array(z.lazy(() => ListPlansFeatureOverrideTier$inboundSchema)),
    }),
    z.transform((v) => {
      return remap$(v, {
        "metered_feature_id": "meteredFeatureId",
        "billing_units": "billingUnits",
        "tier_behavior": "tierBehavior",
      });
    }),
  );

export function listPlansCreditSchemaItemFeatureOverride1FromJSON(
  jsonString: string,
): SafeParseResult<
  ListPlansCreditSchemaItemFeatureOverride1,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      ListPlansCreditSchemaItemFeatureOverride1$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'ListPlansCreditSchemaItemFeatureOverride1' from JSON`,
  );
}

/** @internal */
export const ListPlansItemCreditSchemaUnion$inboundSchema: z.ZodMiniType<
  ListPlansItemCreditSchemaUnion,
  unknown
> = smartUnion([
  z.lazy(() => ListPlansCreditSchemaItemFeatureOverride1$inboundSchema),
  z.lazy(() => ListPlansCreditSchemaItemFeatureOverride2$inboundSchema),
]);

export function listPlansItemCreditSchemaUnionFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansItemCreditSchemaUnion, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansItemCreditSchemaUnion$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansItemCreditSchemaUnion' from JSON`,
  );
}

/** @internal */
export const ListPlansItemFeatureOverride$inboundSchema: z.ZodMiniType<
  ListPlansItemFeatureOverride,
  unknown
> = z.pipe(
  z.object({
    credit_schema: types.optional(z.array(smartUnion([
      z.lazy(() => ListPlansCreditSchemaItemFeatureOverride1$inboundSchema),
      z.lazy(() =>
        ListPlansCreditSchemaItemFeatureOverride2$inboundSchema
      ),
    ]))),
  }),
  z.transform((v) => {
    return remap$(v, {
      "credit_schema": "creditSchema",
    });
  }),
);

export function listPlansItemFeatureOverrideFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansItemFeatureOverride, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansItemFeatureOverride$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansItemFeatureOverride' from JSON`,
  );
}

/** @internal */
export const ListPlansItem$inboundSchema: z.ZodMiniType<
  ListPlansItem,
  unknown
> = z.pipe(
  z.object({
    feature_id: types.string(),
    feature: types.optional(z.lazy(() => ListPlansFeature$inboundSchema)),
    included: types.number(),
    unlimited: types.boolean(),
    pooled: z._default(types.boolean(), false),
    reset: types.nullable(z.lazy(() => ListPlansItemReset$inboundSchema)),
    price: types.nullable(z.lazy(() => ListPlansItemPrice$inboundSchema)),
    display: types.optional(z.lazy(() => ListPlansItemDisplay$inboundSchema)),
    rollover: types.optional(z.lazy(() => ListPlansItemRollover$inboundSchema)),
    feature_override: types.optional(
      z.lazy(() => ListPlansItemFeatureOverride$inboundSchema),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "feature_id": "featureId",
      "feature_override": "featureOverride",
    });
  }),
);

export function listPlansItemFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansItem, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansItem$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansItem' from JSON`,
  );
}

/** @internal */
export const ListPlansStripe$inboundSchema: z.ZodMiniType<
  ListPlansStripe,
  unknown
> = z.pipe(
  z.object({
    product_id: types.string(),
    additional_product_ids: types.optional(z.array(types.string())),
  }),
  z.transform((v) => {
    return remap$(v, {
      "product_id": "productId",
      "additional_product_ids": "additionalProductIds",
    });
  }),
);

export function listPlansStripeFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansStripe, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansStripe$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansStripe' from JSON`,
  );
}

/** @internal */
export const ListPlansFeatureQuantity$inboundSchema: z.ZodMiniType<
  ListPlansFeatureQuantity,
  unknown
> = z.pipe(
  z.object({
    feature_id: types.string(),
    quantity: types.optional(types.number()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "feature_id": "featureId",
    });
  }),
);

export function listPlansFeatureQuantityFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansFeatureQuantity, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansFeatureQuantity$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansFeatureQuantity' from JSON`,
  );
}

/** @internal */
export const ListPlansProduct$inboundSchema: z.ZodMiniType<
  ListPlansProduct,
  unknown
> = z.pipe(
  z.object({
    product_id: types.string(),
    feature_quantities: types.optional(
      z.array(z.lazy(() => ListPlansFeatureQuantity$inboundSchema)),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "product_id": "productId",
      "feature_quantities": "featureQuantities",
    });
  }),
);

export function listPlansProductFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansProduct, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansProduct$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansProduct' from JSON`,
  );
}

/** @internal */
export const ListPlansRevenuecat$inboundSchema: z.ZodMiniType<
  ListPlansRevenuecat,
  unknown
> = z.object({
  products: z.array(z.lazy(() => ListPlansProduct$inboundSchema)),
});

export function listPlansRevenuecatFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansRevenuecat, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansRevenuecat$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansRevenuecat' from JSON`,
  );
}

/** @internal */
export const ListPlansProcessors$inboundSchema: z.ZodMiniType<
  ListPlansProcessors,
  unknown
> = z.object({
  stripe: z.optional(z.nullable(z.lazy(() => ListPlansStripe$inboundSchema))),
  revenuecat: z.optional(
    z.nullable(z.lazy(() => ListPlansRevenuecat$inboundSchema)),
  ),
});

export function listPlansProcessorsFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansProcessors, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansProcessors$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansProcessors' from JSON`,
  );
}

/** @internal */
export const ListPlansDurationType$inboundSchema: z.ZodMiniType<
  ListPlansDurationType,
  unknown
> = openEnums.inboundSchema(ListPlansDurationType);

/** @internal */
export const ListPlansOnEnd$inboundSchema: z.ZodMiniType<
  ListPlansOnEnd,
  unknown
> = openEnums.inboundSchema(ListPlansOnEnd);

/** @internal */
export const ListPlansFreeTrial$inboundSchema: z.ZodMiniType<
  ListPlansFreeTrial,
  unknown
> = z.pipe(
  z.object({
    duration_length: types.number(),
    duration_type: ListPlansDurationType$inboundSchema,
    card_required: types.boolean(),
    on_end: z.optional(z.nullable(ListPlansOnEnd$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "duration_length": "durationLength",
      "duration_type": "durationType",
      "card_required": "cardRequired",
      "on_end": "onEnd",
    });
  }),
);

export function listPlansFreeTrialFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansFreeTrial, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansFreeTrial$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansFreeTrial' from JSON`,
  );
}

/** @internal */
export const ListPlansEnv$inboundSchema: z.ZodMiniType<ListPlansEnv, unknown> =
  openEnums.inboundSchema(ListPlansEnv);

/** @internal */
export const ListPlansConfig$inboundSchema: z.ZodMiniType<
  ListPlansConfig,
  unknown
> = z.pipe(
  z.object({
    ignore_past_due: z._default(types.boolean(), false),
  }),
  z.transform((v) => {
    return remap$(v, {
      "ignore_past_due": "ignorePastDue",
    });
  }),
);

export function listPlansConfigFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansConfig, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansConfig$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansConfig' from JSON`,
  );
}

/** @internal */
export const ListPlansPurchaseLimitInterval$inboundSchema: z.ZodMiniType<
  ListPlansPurchaseLimitInterval,
  unknown
> = openEnums.inboundSchema(ListPlansPurchaseLimitInterval);

/** @internal */
export const ListPlansPurchaseLimit$inboundSchema: z.ZodMiniType<
  ListPlansPurchaseLimit,
  unknown
> = z.pipe(
  z.object({
    interval: ListPlansPurchaseLimitInterval$inboundSchema,
    interval_count: z._default(types.number(), 1),
    limit: types.number(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "interval_count": "intervalCount",
    });
  }),
);

export function listPlansPurchaseLimitFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansPurchaseLimit, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansPurchaseLimit$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansPurchaseLimit' from JSON`,
  );
}

/** @internal */
export const ListPlansAutoTopup$inboundSchema: z.ZodMiniType<
  ListPlansAutoTopup,
  unknown
> = z.pipe(
  z.object({
    feature_id: types.string(),
    enabled: z._default(types.boolean(), false),
    threshold: types.number(),
    quantity: types.number(),
    purchase_limit: types.optional(
      z.lazy(() => ListPlansPurchaseLimit$inboundSchema),
    ),
    invoice_mode: types.optional(types.boolean()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "feature_id": "featureId",
      "purchase_limit": "purchaseLimit",
      "invoice_mode": "invoiceMode",
    });
  }),
);

export function listPlansAutoTopupFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansAutoTopup, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansAutoTopup$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansAutoTopup' from JSON`,
  );
}

/** @internal */
export const ListPlansLimitType$inboundSchema: z.ZodMiniType<
  ListPlansLimitType,
  unknown
> = openEnums.inboundSchema(ListPlansLimitType);

/** @internal */
export const ListPlansSpendLimit$inboundSchema: z.ZodMiniType<
  ListPlansSpendLimit,
  unknown
> = z.pipe(
  z.object({
    feature_id: types.optional(types.string()),
    enabled: z._default(types.boolean(), false),
    limit_type: types.optional(ListPlansLimitType$inboundSchema),
    overage_limit: types.optional(types.number()),
    skip_overage_billing: types.optional(types.boolean()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "feature_id": "featureId",
      "limit_type": "limitType",
      "overage_limit": "overageLimit",
      "skip_overage_billing": "skipOverageBilling",
    });
  }),
);

export function listPlansSpendLimitFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansSpendLimit, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansSpendLimit$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansSpendLimit' from JSON`,
  );
}

/** @internal */
export const ListPlansUsageLimitInterval$inboundSchema: z.ZodMiniType<
  ListPlansUsageLimitInterval,
  unknown
> = openEnums.inboundSchema(ListPlansUsageLimitInterval);

/** @internal */
export const ListPlansAnchor$inboundSchema: z.ZodMiniType<
  ListPlansAnchor,
  unknown
> = openEnums.inboundSchema(ListPlansAnchor);

/** @internal */
export const ListPlansUsageLimitFilter$inboundSchema: z.ZodMiniType<
  ListPlansUsageLimitFilter,
  unknown
> = z.object({
  properties: z.record(z.string(), types.string()),
});

export function listPlansUsageLimitFilterFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansUsageLimitFilter, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansUsageLimitFilter$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansUsageLimitFilter' from JSON`,
  );
}

/** @internal */
export const ListPlansUsageLimit$inboundSchema: z.ZodMiniType<
  ListPlansUsageLimit,
  unknown
> = z.pipe(
  z.object({
    feature_id: types.string(),
    enabled: z._default(types.boolean(), true),
    limit: types.number(),
    interval: ListPlansUsageLimitInterval$inboundSchema,
    anchor: types.optional(ListPlansAnchor$inboundSchema),
    filter: types.optional(
      z.lazy(() => ListPlansUsageLimitFilter$inboundSchema),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "feature_id": "featureId",
    });
  }),
);

export function listPlansUsageLimitFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansUsageLimit, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansUsageLimit$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansUsageLimit' from JSON`,
  );
}

/** @internal */
export const ListPlansThresholdType$inboundSchema: z.ZodMiniType<
  ListPlansThresholdType,
  unknown
> = openEnums.inboundSchema(ListPlansThresholdType);

/** @internal */
export const ListPlansBasis$inboundSchema: z.ZodMiniType<
  ListPlansBasis,
  unknown
> = openEnums.inboundSchema(ListPlansBasis);

/** @internal */
export const ListPlansUsageAlertFilter$inboundSchema: z.ZodMiniType<
  ListPlansUsageAlertFilter,
  unknown
> = z.object({
  properties: z.record(z.string(), types.string()),
});

export function listPlansUsageAlertFilterFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansUsageAlertFilter, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansUsageAlertFilter$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansUsageAlertFilter' from JSON`,
  );
}

/** @internal */
export const ListPlansUsageAlert$inboundSchema: z.ZodMiniType<
  ListPlansUsageAlert,
  unknown
> = z.pipe(
  z.object({
    feature_id: types.optional(types.string()),
    enabled: z._default(types.boolean(), true),
    threshold: types.number(),
    threshold_type: ListPlansThresholdType$inboundSchema,
    basis: z._default(ListPlansBasis$inboundSchema, "balance"),
    filter: types.optional(
      z.lazy(() => ListPlansUsageAlertFilter$inboundSchema),
    ),
    name: types.optional(types.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "feature_id": "featureId",
      "threshold_type": "thresholdType",
    });
  }),
);

export function listPlansUsageAlertFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansUsageAlert, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansUsageAlert$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansUsageAlert' from JSON`,
  );
}

/** @internal */
export const ListPlansOverageAllowed$inboundSchema: z.ZodMiniType<
  ListPlansOverageAllowed,
  unknown
> = z.pipe(
  z.object({
    feature_id: types.string(),
    enabled: z._default(types.boolean(), false),
  }),
  z.transform((v) => {
    return remap$(v, {
      "feature_id": "featureId",
    });
  }),
);

export function listPlansOverageAllowedFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansOverageAllowed, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansOverageAllowed$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansOverageAllowed' from JSON`,
  );
}

/** @internal */
export const ListPlansBillingControls$inboundSchema: z.ZodMiniType<
  ListPlansBillingControls,
  unknown
> = z.pipe(
  z.object({
    auto_topups: types.optional(
      z.array(z.lazy(() => ListPlansAutoTopup$inboundSchema)),
    ),
    spend_limits: types.optional(
      z.array(z.lazy(() => ListPlansSpendLimit$inboundSchema)),
    ),
    usage_limits: types.optional(
      z.array(z.lazy(() => ListPlansUsageLimit$inboundSchema)),
    ),
    usage_alerts: types.optional(
      z.array(z.lazy(() => ListPlansUsageAlert$inboundSchema)),
    ),
    overage_allowed: types.optional(
      z.array(z.lazy(() => ListPlansOverageAllowed$inboundSchema)),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "auto_topups": "autoTopups",
      "spend_limits": "spendLimits",
      "usage_limits": "usageLimits",
      "usage_alerts": "usageAlerts",
      "overage_allowed": "overageAllowed",
    });
  }),
);

export function listPlansBillingControlsFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansBillingControls, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansBillingControls$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansBillingControls' from JSON`,
  );
}

/** @internal */
export const ListPlansStatus$inboundSchema: z.ZodMiniType<
  ListPlansStatus,
  unknown
> = openEnums.inboundSchema(ListPlansStatus);

/** @internal */
export const ListPlansAttachAction$inboundSchema: z.ZodMiniType<
  ListPlansAttachAction,
  unknown
> = openEnums.inboundSchema(ListPlansAttachAction);

/** @internal */
export const ListPlansCustomerEligibility$inboundSchema: z.ZodMiniType<
  ListPlansCustomerEligibility,
  unknown
> = z.pipe(
  z.object({
    trial_available: types.optional(types.boolean()),
    status: types.optional(ListPlansStatus$inboundSchema),
    canceling: types.optional(types.boolean()),
    trialing: types.optional(types.boolean()),
    attach_action: ListPlansAttachAction$inboundSchema,
  }),
  z.transform((v) => {
    return remap$(v, {
      "trial_available": "trialAvailable",
      "attach_action": "attachAction",
    });
  }),
);

export function listPlansCustomerEligibilityFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansCustomerEligibility, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansCustomerEligibility$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansCustomerEligibility' from JSON`,
  );
}

/** @internal */
export const ListPlansPriceVariantDetailsInterval$inboundSchema: z.ZodMiniType<
  ListPlansPriceVariantDetailsInterval,
  unknown
> = openEnums.inboundSchema(ListPlansPriceVariantDetailsInterval);

/** @internal */
export const ListPlansVariantDetailsAdditionalCurrency$inboundSchema:
  z.ZodMiniType<ListPlansVariantDetailsAdditionalCurrency, unknown> = z.object({
    currency: types.string(),
    amount: types.number(),
  });

export function listPlansVariantDetailsAdditionalCurrencyFromJSON(
  jsonString: string,
): SafeParseResult<
  ListPlansVariantDetailsAdditionalCurrency,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      ListPlansVariantDetailsAdditionalCurrency$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'ListPlansVariantDetailsAdditionalCurrency' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantDetailsBasePrice$inboundSchema: z.ZodMiniType<
  ListPlansVariantDetailsBasePrice,
  unknown
> = z.pipe(
  z.object({
    amount: types.number(),
    interval: ListPlansPriceVariantDetailsInterval$inboundSchema,
    interval_count: types.optional(types.number()),
    additional_currencies: types.optional(z.array(z.lazy(() =>
      ListPlansVariantDetailsAdditionalCurrency$inboundSchema
    ))),
  }),
  z.transform((v) => {
    return remap$(v, {
      "interval_count": "intervalCount",
      "additional_currencies": "additionalCurrencies",
    });
  }),
);

export function listPlansVariantDetailsBasePriceFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansVariantDetailsBasePrice, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansVariantDetailsBasePrice$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansVariantDetailsBasePrice' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantDetailsResetInterval$inboundSchema: z.ZodMiniType<
  ListPlansVariantDetailsResetInterval,
  unknown
> = openEnums.inboundSchema(ListPlansVariantDetailsResetInterval);

/** @internal */
export const ListPlansVariantDetailsReset$inboundSchema: z.ZodMiniType<
  ListPlansVariantDetailsReset,
  unknown
> = z.pipe(
  z.object({
    interval: ListPlansVariantDetailsResetInterval$inboundSchema,
    interval_count: types.optional(types.number()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "interval_count": "intervalCount",
    });
  }),
);

export function listPlansVariantDetailsResetFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansVariantDetailsReset, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansVariantDetailsReset$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansVariantDetailsReset' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantDetailsAddItemAdditionalCurrency$inboundSchema:
  z.ZodMiniType<ListPlansVariantDetailsAddItemAdditionalCurrency, unknown> = z
    .object({
      currency: types.string(),
      amount: types.number(),
    });

export function listPlansVariantDetailsAddItemAdditionalCurrencyFromJSON(
  jsonString: string,
): SafeParseResult<
  ListPlansVariantDetailsAddItemAdditionalCurrency,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      ListPlansVariantDetailsAddItemAdditionalCurrency$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'ListPlansVariantDetailsAddItemAdditionalCurrency' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantDetailsTier$inboundSchema: z.ZodMiniType<
  ListPlansVariantDetailsTier,
  unknown
> = z.pipe(
  z.object({
    to: types.optional(z.any()),
    amount: types.number(),
    flat_amount: types.optional(types.number()),
    additional_currencies: types.optional(z.array(types.nullable(z.any()))),
  }),
  z.transform((v) => {
    return remap$(v, {
      "flat_amount": "flatAmount",
      "additional_currencies": "additionalCurrencies",
    });
  }),
);

export function listPlansVariantDetailsTierFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansVariantDetailsTier, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansVariantDetailsTier$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansVariantDetailsTier' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantDetailsTierBehavior$inboundSchema: z.ZodMiniType<
  ListPlansVariantDetailsTierBehavior,
  unknown
> = openEnums.inboundSchema(ListPlansVariantDetailsTierBehavior);

/** @internal */
export const ListPlansVariantDetailsAddItemPriceInterval$inboundSchema:
  z.ZodMiniType<ListPlansVariantDetailsAddItemPriceInterval, unknown> =
    openEnums.inboundSchema(ListPlansVariantDetailsAddItemPriceInterval);

/** @internal */
export const ListPlansVariantDetailsAddItemBillingMethod$inboundSchema:
  z.ZodMiniType<ListPlansVariantDetailsAddItemBillingMethod, unknown> =
    openEnums.inboundSchema(ListPlansVariantDetailsAddItemBillingMethod);

/** @internal */
export const ListPlansVariantDetailsPrice$inboundSchema: z.ZodMiniType<
  ListPlansVariantDetailsPrice,
  unknown
> = z.pipe(
  z.object({
    amount: types.optional(types.number()),
    additional_currencies: types.optional(z.array(z.lazy(() =>
      ListPlansVariantDetailsAddItemAdditionalCurrency$inboundSchema
    ))),
    tiers: types.optional(z.array(z.lazy(() =>
      ListPlansVariantDetailsTier$inboundSchema
    ))),
    tier_behavior: types.optional(
      ListPlansVariantDetailsTierBehavior$inboundSchema,
    ),
    interval: ListPlansVariantDetailsAddItemPriceInterval$inboundSchema,
    interval_count: z._default(types.number(), 1),
    billing_units: z._default(types.number(), 1),
    billing_method: ListPlansVariantDetailsAddItemBillingMethod$inboundSchema,
    max_purchase: z.optional(z.nullable(types.number())),
  }),
  z.transform((v) => {
    return remap$(v, {
      "additional_currencies": "additionalCurrencies",
      "tier_behavior": "tierBehavior",
      "interval_count": "intervalCount",
      "billing_units": "billingUnits",
      "billing_method": "billingMethod",
      "max_purchase": "maxPurchase",
    });
  }),
);

export function listPlansVariantDetailsPriceFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansVariantDetailsPrice, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansVariantDetailsPrice$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansVariantDetailsPrice' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantDetailsOnIncrease$inboundSchema: z.ZodMiniType<
  ListPlansVariantDetailsOnIncrease,
  unknown
> = openEnums.inboundSchema(ListPlansVariantDetailsOnIncrease);

/** @internal */
export const ListPlansVariantDetailsOnDecrease$inboundSchema: z.ZodMiniType<
  ListPlansVariantDetailsOnDecrease,
  unknown
> = openEnums.inboundSchema(ListPlansVariantDetailsOnDecrease);

/** @internal */
export const ListPlansVariantDetailsProration$inboundSchema: z.ZodMiniType<
  ListPlansVariantDetailsProration,
  unknown
> = z.pipe(
  z.object({
    on_increase: ListPlansVariantDetailsOnIncrease$inboundSchema,
    on_decrease: ListPlansVariantDetailsOnDecrease$inboundSchema,
  }),
  z.transform((v) => {
    return remap$(v, {
      "on_increase": "onIncrease",
      "on_decrease": "onDecrease",
    });
  }),
);

export function listPlansVariantDetailsProrationFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansVariantDetailsProration, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansVariantDetailsProration$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansVariantDetailsProration' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantDetailsExpiryDurationType$inboundSchema:
  z.ZodMiniType<ListPlansVariantDetailsExpiryDurationType, unknown> = openEnums
    .inboundSchema(ListPlansVariantDetailsExpiryDurationType);

/** @internal */
export const ListPlansVariantDetailsRollover$inboundSchema: z.ZodMiniType<
  ListPlansVariantDetailsRollover,
  unknown
> = z.pipe(
  z.object({
    max: types.optional(types.number()),
    max_percentage: types.optional(types.number()),
    expiry_duration_type:
      ListPlansVariantDetailsExpiryDurationType$inboundSchema,
    expiry_duration_length: types.optional(types.number()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "max_percentage": "maxPercentage",
      "expiry_duration_type": "expiryDurationType",
      "expiry_duration_length": "expiryDurationLength",
    });
  }),
);

export function listPlansVariantDetailsRolloverFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansVariantDetailsRollover, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansVariantDetailsRollover$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansVariantDetailsRollover' from JSON`,
  );
}

/** @internal */
export const ListPlansCreditSchemaVariantDetails2$inboundSchema: z.ZodMiniType<
  ListPlansCreditSchemaVariantDetails2,
  unknown
> = z.pipe(
  z.object({
    metered_feature_id: types.optional(z.any()),
    billing_units: types.optional(z.any()),
    credit_cost: types.optional(z.any()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "metered_feature_id": "meteredFeatureId",
      "billing_units": "billingUnits",
      "credit_cost": "creditCost",
    });
  }),
);

export function listPlansCreditSchemaVariantDetails2FromJSON(
  jsonString: string,
): SafeParseResult<ListPlansCreditSchemaVariantDetails2, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      ListPlansCreditSchemaVariantDetails2$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansCreditSchemaVariantDetails2' from JSON`,
  );
}

/** @internal */
export const ListPlansCreditSchemaVariantDetails1$inboundSchema: z.ZodMiniType<
  ListPlansCreditSchemaVariantDetails1,
  unknown
> = z.pipe(
  z.object({
    metered_feature_id: types.optional(z.any()),
    billing_units: types.optional(z.any()),
    tier_behavior: types.optional(z.any()),
    tiers: types.optional(z.any()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "metered_feature_id": "meteredFeatureId",
      "billing_units": "billingUnits",
      "tier_behavior": "tierBehavior",
    });
  }),
);

export function listPlansCreditSchemaVariantDetails1FromJSON(
  jsonString: string,
): SafeParseResult<ListPlansCreditSchemaVariantDetails1, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      ListPlansCreditSchemaVariantDetails1$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansCreditSchemaVariantDetails1' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantDetailsCreditSchemaUnion$inboundSchema:
  z.ZodMiniType<ListPlansVariantDetailsCreditSchemaUnion, unknown> = smartUnion(
    [
      z.lazy(() => ListPlansCreditSchemaVariantDetails1$inboundSchema),
      z.lazy(() => ListPlansCreditSchemaVariantDetails2$inboundSchema),
    ],
  );

export function listPlansVariantDetailsCreditSchemaUnionFromJSON(
  jsonString: string,
): SafeParseResult<
  ListPlansVariantDetailsCreditSchemaUnion,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      ListPlansVariantDetailsCreditSchemaUnion$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'ListPlansVariantDetailsCreditSchemaUnion' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantDetailsFeatureOverride$inboundSchema:
  z.ZodMiniType<ListPlansVariantDetailsFeatureOverride, unknown> = z.pipe(
    z.object({
      credit_schema: types.optional(z.array(smartUnion([
        z.lazy(() => ListPlansCreditSchemaVariantDetails1$inboundSchema),
        z.lazy(() =>
          ListPlansCreditSchemaVariantDetails2$inboundSchema
        ),
      ]))),
    }),
    z.transform((v) => {
      return remap$(v, {
        "credit_schema": "creditSchema",
      });
    }),
  );

export function listPlansVariantDetailsFeatureOverrideFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansVariantDetailsFeatureOverride, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      ListPlansVariantDetailsFeatureOverride$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansVariantDetailsFeatureOverride' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantDetailsPlanItem$inboundSchema: z.ZodMiniType<
  ListPlansVariantDetailsPlanItem,
  unknown
> = z.pipe(
  z.object({
    feature_id: types.string(),
    included: types.optional(types.number()),
    unlimited: types.optional(types.boolean()),
    pooled: z._default(types.boolean(), false),
    reset: types.optional(
      z.lazy(() => ListPlansVariantDetailsReset$inboundSchema),
    ),
    price: types.optional(
      z.lazy(() => ListPlansVariantDetailsPrice$inboundSchema),
    ),
    proration: types.optional(
      z.lazy(() => ListPlansVariantDetailsProration$inboundSchema),
    ),
    rollover: types.optional(
      z.lazy(() => ListPlansVariantDetailsRollover$inboundSchema),
    ),
    feature_override: types.optional(
      z.lazy(() => ListPlansVariantDetailsFeatureOverride$inboundSchema),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "feature_id": "featureId",
      "feature_override": "featureOverride",
    });
  }),
);

export function listPlansVariantDetailsPlanItemFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansVariantDetailsPlanItem, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansVariantDetailsPlanItem$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansVariantDetailsPlanItem' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantDetailsRemoveItemBillingMethod$inboundSchema:
  z.ZodMiniType<ListPlansVariantDetailsRemoveItemBillingMethod, unknown> =
    openEnums.inboundSchema(ListPlansVariantDetailsRemoveItemBillingMethod);

/** @internal */
export const ListPlansIntervalVariantDetailsRemoveItemEnum2$inboundSchema:
  z.ZodMiniType<ListPlansIntervalVariantDetailsRemoveItemEnum2, unknown> =
    openEnums.inboundSchema(ListPlansIntervalVariantDetailsRemoveItemEnum2);

/** @internal */
export const ListPlansIntervalVariantDetailsRemoveItemEnum1$inboundSchema:
  z.ZodMiniType<ListPlansIntervalVariantDetailsRemoveItemEnum1, unknown> =
    openEnums.inboundSchema(ListPlansIntervalVariantDetailsRemoveItemEnum1);

/** @internal */
export const ListPlansVariantDetailsIntervalUnion$inboundSchema: z.ZodMiniType<
  ListPlansVariantDetailsIntervalUnion,
  unknown
> = smartUnion([
  ListPlansIntervalVariantDetailsRemoveItemEnum1$inboundSchema,
  ListPlansIntervalVariantDetailsRemoveItemEnum2$inboundSchema,
]);

export function listPlansVariantDetailsIntervalUnionFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansVariantDetailsIntervalUnion, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      ListPlansVariantDetailsIntervalUnion$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansVariantDetailsIntervalUnion' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantDetailsPlanItemFilter$inboundSchema: z.ZodMiniType<
  ListPlansVariantDetailsPlanItemFilter,
  unknown
> = z.pipe(
  z.object({
    feature_id: types.optional(types.string()),
    billing_method: types.optional(
      ListPlansVariantDetailsRemoveItemBillingMethod$inboundSchema,
    ),
    interval: types.optional(
      smartUnion([
        ListPlansIntervalVariantDetailsRemoveItemEnum1$inboundSchema,
        ListPlansIntervalVariantDetailsRemoveItemEnum2$inboundSchema,
      ]),
    ),
    interval_count: types.optional(types.number()),
    included: types.optional(types.number()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "feature_id": "featureId",
      "billing_method": "billingMethod",
      "interval_count": "intervalCount",
    });
  }),
);

export function listPlansVariantDetailsPlanItemFilterFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansVariantDetailsPlanItemFilter, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      ListPlansVariantDetailsPlanItemFilter$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansVariantDetailsPlanItemFilter' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantDetailsDurationType$inboundSchema: z.ZodMiniType<
  ListPlansVariantDetailsDurationType,
  unknown
> = openEnums.inboundSchema(ListPlansVariantDetailsDurationType);

/** @internal */
export const ListPlansVariantDetailsOnEnd$inboundSchema: z.ZodMiniType<
  ListPlansVariantDetailsOnEnd,
  unknown
> = openEnums.inboundSchema(ListPlansVariantDetailsOnEnd);

/** @internal */
export const ListPlansVariantDetailsFreeTrialParams$inboundSchema:
  z.ZodMiniType<ListPlansVariantDetailsFreeTrialParams, unknown> = z.pipe(
    z.object({
      duration_length: types.number(),
      duration_type: z._default(
        ListPlansVariantDetailsDurationType$inboundSchema,
        "month",
      ),
      card_required: z._default(types.boolean(), false),
      on_end: types.optional(ListPlansVariantDetailsOnEnd$inboundSchema),
    }),
    z.transform((v) => {
      return remap$(v, {
        "duration_length": "durationLength",
        "duration_type": "durationType",
        "card_required": "cardRequired",
        "on_end": "onEnd",
      });
    }),
  );

export function listPlansVariantDetailsFreeTrialParamsFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansVariantDetailsFreeTrialParams, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      ListPlansVariantDetailsFreeTrialParams$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansVariantDetailsFreeTrialParams' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantDetailsPurchaseLimitInterval$inboundSchema:
  z.ZodMiniType<ListPlansVariantDetailsPurchaseLimitInterval, unknown> =
    openEnums.inboundSchema(ListPlansVariantDetailsPurchaseLimitInterval);

/** @internal */
export const ListPlansVariantDetailsPurchaseLimit$inboundSchema: z.ZodMiniType<
  ListPlansVariantDetailsPurchaseLimit,
  unknown
> = z.pipe(
  z.object({
    interval: ListPlansVariantDetailsPurchaseLimitInterval$inboundSchema,
    interval_count: z._default(types.number(), 1),
    limit: types.number(),
    count: types.optional(types.number()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "interval_count": "intervalCount",
    });
  }),
);

export function listPlansVariantDetailsPurchaseLimitFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansVariantDetailsPurchaseLimit, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      ListPlansVariantDetailsPurchaseLimit$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansVariantDetailsPurchaseLimit' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantDetailsAutoTopup$inboundSchema: z.ZodMiniType<
  ListPlansVariantDetailsAutoTopup,
  unknown
> = z.pipe(
  z.object({
    feature_id: types.string(),
    enabled: z._default(types.boolean(), false),
    threshold: types.number(),
    quantity: types.number(),
    purchase_limit: types.optional(
      z.lazy(() => ListPlansVariantDetailsPurchaseLimit$inboundSchema),
    ),
    invoice_mode: types.optional(types.boolean()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "feature_id": "featureId",
      "purchase_limit": "purchaseLimit",
      "invoice_mode": "invoiceMode",
    });
  }),
);

export function listPlansVariantDetailsAutoTopupFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansVariantDetailsAutoTopup, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansVariantDetailsAutoTopup$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansVariantDetailsAutoTopup' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantDetailsLimitType$inboundSchema: z.ZodMiniType<
  ListPlansVariantDetailsLimitType,
  unknown
> = openEnums.inboundSchema(ListPlansVariantDetailsLimitType);

/** @internal */
export const ListPlansVariantDetailsSpendLimit$inboundSchema: z.ZodMiniType<
  ListPlansVariantDetailsSpendLimit,
  unknown
> = z.pipe(
  z.object({
    feature_id: types.optional(types.string()),
    enabled: z._default(types.boolean(), false),
    limit_type: types.optional(ListPlansVariantDetailsLimitType$inboundSchema),
    overage_limit: types.optional(types.number()),
    skip_overage_billing: types.optional(types.boolean()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "feature_id": "featureId",
      "limit_type": "limitType",
      "overage_limit": "overageLimit",
      "skip_overage_billing": "skipOverageBilling",
    });
  }),
);

export function listPlansVariantDetailsSpendLimitFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansVariantDetailsSpendLimit, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansVariantDetailsSpendLimit$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansVariantDetailsSpendLimit' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantDetailsUsageLimitInterval$inboundSchema:
  z.ZodMiniType<ListPlansVariantDetailsUsageLimitInterval, unknown> = openEnums
    .inboundSchema(ListPlansVariantDetailsUsageLimitInterval);

/** @internal */
export const ListPlansVariantDetailsAnchor$inboundSchema: z.ZodMiniType<
  ListPlansVariantDetailsAnchor,
  unknown
> = openEnums.inboundSchema(ListPlansVariantDetailsAnchor);

/** @internal */
export const ListPlansVariantDetailsUsageLimitFilter$inboundSchema:
  z.ZodMiniType<ListPlansVariantDetailsUsageLimitFilter, unknown> = z.object({
    properties: z.record(z.string(), types.string()),
  });

export function listPlansVariantDetailsUsageLimitFilterFromJSON(
  jsonString: string,
): SafeParseResult<
  ListPlansVariantDetailsUsageLimitFilter,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      ListPlansVariantDetailsUsageLimitFilter$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'ListPlansVariantDetailsUsageLimitFilter' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantDetailsUsageLimit$inboundSchema: z.ZodMiniType<
  ListPlansVariantDetailsUsageLimit,
  unknown
> = z.pipe(
  z.object({
    feature_id: types.string(),
    enabled: z._default(types.boolean(), true),
    limit: types.number(),
    interval: ListPlansVariantDetailsUsageLimitInterval$inboundSchema,
    anchor: types.optional(ListPlansVariantDetailsAnchor$inboundSchema),
    filter: types.optional(
      z.lazy(() => ListPlansVariantDetailsUsageLimitFilter$inboundSchema),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "feature_id": "featureId",
    });
  }),
);

export function listPlansVariantDetailsUsageLimitFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansVariantDetailsUsageLimit, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansVariantDetailsUsageLimit$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansVariantDetailsUsageLimit' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantDetailsThresholdType$inboundSchema: z.ZodMiniType<
  ListPlansVariantDetailsThresholdType,
  unknown
> = openEnums.inboundSchema(ListPlansVariantDetailsThresholdType);

/** @internal */
export const ListPlansVariantDetailsBasis$inboundSchema: z.ZodMiniType<
  ListPlansVariantDetailsBasis,
  unknown
> = openEnums.inboundSchema(ListPlansVariantDetailsBasis);

/** @internal */
export const ListPlansVariantDetailsUsageAlertFilter$inboundSchema:
  z.ZodMiniType<ListPlansVariantDetailsUsageAlertFilter, unknown> = z.object({
    properties: z.record(z.string(), types.string()),
  });

export function listPlansVariantDetailsUsageAlertFilterFromJSON(
  jsonString: string,
): SafeParseResult<
  ListPlansVariantDetailsUsageAlertFilter,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      ListPlansVariantDetailsUsageAlertFilter$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'ListPlansVariantDetailsUsageAlertFilter' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantDetailsUsageAlert$inboundSchema: z.ZodMiniType<
  ListPlansVariantDetailsUsageAlert,
  unknown
> = z.pipe(
  z.object({
    feature_id: types.optional(types.string()),
    enabled: z._default(types.boolean(), true),
    threshold: types.number(),
    threshold_type: ListPlansVariantDetailsThresholdType$inboundSchema,
    basis: z._default(ListPlansVariantDetailsBasis$inboundSchema, "balance"),
    filter: types.optional(
      z.lazy(() => ListPlansVariantDetailsUsageAlertFilter$inboundSchema),
    ),
    name: types.optional(types.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "feature_id": "featureId",
      "threshold_type": "thresholdType",
    });
  }),
);

export function listPlansVariantDetailsUsageAlertFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansVariantDetailsUsageAlert, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansVariantDetailsUsageAlert$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansVariantDetailsUsageAlert' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantDetailsOverageAllowed$inboundSchema: z.ZodMiniType<
  ListPlansVariantDetailsOverageAllowed,
  unknown
> = z.pipe(
  z.object({
    feature_id: types.string(),
    enabled: z._default(types.boolean(), false),
  }),
  z.transform((v) => {
    return remap$(v, {
      "feature_id": "featureId",
    });
  }),
);

export function listPlansVariantDetailsOverageAllowedFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansVariantDetailsOverageAllowed, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      ListPlansVariantDetailsOverageAllowed$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansVariantDetailsOverageAllowed' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantDetailsBillingControls$inboundSchema:
  z.ZodMiniType<ListPlansVariantDetailsBillingControls, unknown> = z.pipe(
    z.object({
      auto_topups: types.optional(z.array(z.lazy(() =>
        ListPlansVariantDetailsAutoTopup$inboundSchema
      ))),
      spend_limits: types.optional(z.array(z.lazy(() =>
        ListPlansVariantDetailsSpendLimit$inboundSchema
      ))),
      usage_limits: types.optional(z.array(z.lazy(() =>
        ListPlansVariantDetailsUsageLimit$inboundSchema
      ))),
      usage_alerts: types.optional(z.array(z.lazy(() =>
        ListPlansVariantDetailsUsageAlert$inboundSchema
      ))),
      overage_allowed: types.optional(z.array(z.lazy(() =>
        ListPlansVariantDetailsOverageAllowed$inboundSchema
      ))),
    }),
    z.transform((v) => {
      return remap$(v, {
        "auto_topups": "autoTopups",
        "spend_limits": "spendLimits",
        "usage_limits": "usageLimits",
        "usage_alerts": "usageAlerts",
        "overage_allowed": "overageAllowed",
      });
    }),
  );

export function listPlansVariantDetailsBillingControlsFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansVariantDetailsBillingControls, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      ListPlansVariantDetailsBillingControls$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansVariantDetailsBillingControls' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantDetailsUpsertLicenseInterval$inboundSchema:
  z.ZodMiniType<ListPlansVariantDetailsUpsertLicenseInterval, unknown> =
    openEnums.inboundSchema(ListPlansVariantDetailsUpsertLicenseInterval);

/** @internal */
export const ListPlansUpsertLicenseAdditionalCurrency$inboundSchema:
  z.ZodMiniType<ListPlansUpsertLicenseAdditionalCurrency, unknown> = z.object({
    currency: types.optional(z.any()),
    amount: types.optional(z.any()),
  });

export function listPlansUpsertLicenseAdditionalCurrencyFromJSON(
  jsonString: string,
): SafeParseResult<
  ListPlansUpsertLicenseAdditionalCurrency,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      ListPlansUpsertLicenseAdditionalCurrency$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'ListPlansUpsertLicenseAdditionalCurrency' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantDetailsUpsertLicenseBasePrice$inboundSchema:
  z.ZodMiniType<ListPlansVariantDetailsUpsertLicenseBasePrice, unknown> = z
    .pipe(
      z.object({
        amount: types.number(),
        interval: ListPlansVariantDetailsUpsertLicenseInterval$inboundSchema,
        interval_count: types.optional(types.number()),
        additional_currencies: types.optional(z.array(z.lazy(() =>
          ListPlansUpsertLicenseAdditionalCurrency$inboundSchema
        ))),
      }),
      z.transform((v) => {
        return remap$(v, {
          "interval_count": "intervalCount",
          "additional_currencies": "additionalCurrencies",
        });
      }),
    );

export function listPlansVariantDetailsUpsertLicenseBasePriceFromJSON(
  jsonString: string,
): SafeParseResult<
  ListPlansVariantDetailsUpsertLicenseBasePrice,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      ListPlansVariantDetailsUpsertLicenseBasePrice$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'ListPlansVariantDetailsUpsertLicenseBasePrice' from JSON`,
  );
}

/** @internal */
export const ListPlansUpsertLicenseReset$inboundSchema: z.ZodMiniType<
  ListPlansUpsertLicenseReset,
  unknown
> = z.pipe(
  z.object({
    interval: types.optional(z.any()),
    interval_count: types.optional(z.any()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "interval_count": "intervalCount",
    });
  }),
);

export function listPlansUpsertLicenseResetFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansUpsertLicenseReset, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansUpsertLicenseReset$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansUpsertLicenseReset' from JSON`,
  );
}

/** @internal */
export const ListPlansUpsertLicensePrice$inboundSchema: z.ZodMiniType<
  ListPlansUpsertLicensePrice,
  unknown
> = z.pipe(
  z.object({
    stripe_price_id: types.optional(z.any()),
    amount: types.optional(z.any()),
    additional_currencies: types.optional(z.any()),
    tiers: types.optional(z.any()),
    tier_behavior: types.optional(z.any()),
    interval: types.optional(z.any()),
    interval_count: types.optional(z.any()),
    billing_units: types.optional(z.any()),
    billing_method: types.optional(z.any()),
    max_purchase: types.optional(z.any()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "stripe_price_id": "stripePriceId",
      "additional_currencies": "additionalCurrencies",
      "tier_behavior": "tierBehavior",
      "interval_count": "intervalCount",
      "billing_units": "billingUnits",
      "billing_method": "billingMethod",
      "max_purchase": "maxPurchase",
    });
  }),
);

export function listPlansUpsertLicensePriceFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansUpsertLicensePrice, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansUpsertLicensePrice$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansUpsertLicensePrice' from JSON`,
  );
}

/** @internal */
export const ListPlansUpsertLicenseProration$inboundSchema: z.ZodMiniType<
  ListPlansUpsertLicenseProration,
  unknown
> = z.pipe(
  z.object({
    on_increase: types.optional(z.any()),
    on_decrease: types.optional(z.any()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "on_increase": "onIncrease",
      "on_decrease": "onDecrease",
    });
  }),
);

export function listPlansUpsertLicenseProrationFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansUpsertLicenseProration, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansUpsertLicenseProration$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansUpsertLicenseProration' from JSON`,
  );
}

/** @internal */
export const ListPlansUpsertLicenseRollover$inboundSchema: z.ZodMiniType<
  ListPlansUpsertLicenseRollover,
  unknown
> = z.pipe(
  z.object({
    max: types.optional(z.any()),
    max_percentage: types.optional(z.any()),
    expiry_duration_type: types.optional(z.any()),
    expiry_duration_length: types.optional(z.any()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "max_percentage": "maxPercentage",
      "expiry_duration_type": "expiryDurationType",
      "expiry_duration_length": "expiryDurationLength",
    });
  }),
);

export function listPlansUpsertLicenseRolloverFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansUpsertLicenseRollover, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansUpsertLicenseRollover$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansUpsertLicenseRollover' from JSON`,
  );
}

/** @internal */
export const ListPlansUpsertLicenseFeatureOverride$inboundSchema: z.ZodMiniType<
  ListPlansUpsertLicenseFeatureOverride,
  unknown
> = z.pipe(
  z.object({
    credit_schema: types.optional(z.any()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "credit_schema": "creditSchema",
    });
  }),
);

export function listPlansUpsertLicenseFeatureOverrideFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansUpsertLicenseFeatureOverride, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      ListPlansUpsertLicenseFeatureOverride$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansUpsertLicenseFeatureOverride' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantDetailsUpsertLicensePlanItem$inboundSchema:
  z.ZodMiniType<ListPlansVariantDetailsUpsertLicensePlanItem, unknown> = z.pipe(
    z.object({
      feature_id: types.string(),
      included: types.optional(types.number()),
      unlimited: types.optional(types.boolean()),
      pooled: z._default(types.boolean(), false),
      reset: types.optional(z.lazy(() =>
        ListPlansUpsertLicenseReset$inboundSchema
      )),
      price: types.optional(z.lazy(() =>
        ListPlansUpsertLicensePrice$inboundSchema
      )),
      proration: types.optional(z.lazy(() =>
        ListPlansUpsertLicenseProration$inboundSchema
      )),
      rollover: types.optional(z.lazy(() =>
        ListPlansUpsertLicenseRollover$inboundSchema
      )),
      feature_override: types.optional(z.lazy(() =>
        ListPlansUpsertLicenseFeatureOverride$inboundSchema
      )),
    }),
    z.transform((v) => {
      return remap$(v, {
        "feature_id": "featureId",
        "feature_override": "featureOverride",
      });
    }),
  );

export function listPlansVariantDetailsUpsertLicensePlanItemFromJSON(
  jsonString: string,
): SafeParseResult<
  ListPlansVariantDetailsUpsertLicensePlanItem,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      ListPlansVariantDetailsUpsertLicensePlanItem$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'ListPlansVariantDetailsUpsertLicensePlanItem' from JSON`,
  );
}

/** @internal */
export const ListPlansUpsertLicenseBillingMethod$inboundSchema: z.ZodMiniType<
  ListPlansUpsertLicenseBillingMethod,
  unknown
> = openEnums.inboundSchema(ListPlansUpsertLicenseBillingMethod);

/** @internal */
export const ListPlansVariantDetailsUpsertLicensePlanItemFilter$inboundSchema:
  z.ZodMiniType<ListPlansVariantDetailsUpsertLicensePlanItemFilter, unknown> = z
    .pipe(
      z.object({
        feature_id: types.optional(types.string()),
        billing_method: types.optional(
          ListPlansUpsertLicenseBillingMethod$inboundSchema,
        ),
        interval: types.optional(z.any()),
        interval_count: types.optional(types.number()),
        included: types.optional(types.number()),
      }),
      z.transform((v) => {
        return remap$(v, {
          "feature_id": "featureId",
          "billing_method": "billingMethod",
          "interval_count": "intervalCount",
        });
      }),
    );

export function listPlansVariantDetailsUpsertLicensePlanItemFilterFromJSON(
  jsonString: string,
): SafeParseResult<
  ListPlansVariantDetailsUpsertLicensePlanItemFilter,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      ListPlansVariantDetailsUpsertLicensePlanItemFilter$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'ListPlansVariantDetailsUpsertLicensePlanItemFilter' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantDetailsUpsertLicenseCustomize$inboundSchema:
  z.ZodMiniType<ListPlansVariantDetailsUpsertLicenseCustomize, unknown> = z
    .pipe(
      z.object({
        price: z.optional(z.nullable(z.lazy(() =>
          ListPlansVariantDetailsUpsertLicenseBasePrice$inboundSchema
        ))),
        add_items: types.optional(z.array(z.lazy(() =>
          ListPlansVariantDetailsUpsertLicensePlanItem$inboundSchema
        ))),
        remove_items: types.optional(z.array(z.lazy(() =>
          ListPlansVariantDetailsUpsertLicensePlanItemFilter$inboundSchema
        ))),
      }),
      z.transform((v) => {
        return remap$(v, {
          "add_items": "addItems",
          "remove_items": "removeItems",
        });
      }),
    );

export function listPlansVariantDetailsUpsertLicenseCustomizeFromJSON(
  jsonString: string,
): SafeParseResult<
  ListPlansVariantDetailsUpsertLicenseCustomize,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      ListPlansVariantDetailsUpsertLicenseCustomize$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'ListPlansVariantDetailsUpsertLicenseCustomize' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantDetailsUpsertLicense$inboundSchema: z.ZodMiniType<
  ListPlansVariantDetailsUpsertLicense,
  unknown
> = z.pipe(
  z.object({
    license_plan_id: types.string(),
    version_slug: types.optional(types.string()),
    included: types.optional(types.number()),
    prepaid_only: types.optional(types.boolean()),
    customize: z.optional(z.nullable(z.lazy(() =>
      ListPlansVariantDetailsUpsertLicenseCustomize$inboundSchema
    ))),
    metadata: types.optional(z.record(z.string(), z.any())),
  }),
  z.transform((v) => {
    return remap$(v, {
      "license_plan_id": "licensePlanId",
      "version_slug": "versionSlug",
      "prepaid_only": "prepaidOnly",
    });
  }),
);

export function listPlansVariantDetailsUpsertLicenseFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansVariantDetailsUpsertLicense, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      ListPlansVariantDetailsUpsertLicense$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansVariantDetailsUpsertLicense' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantDetailsRemoveLicense$inboundSchema: z.ZodMiniType<
  ListPlansVariantDetailsRemoveLicense,
  unknown
> = z.pipe(
  z.object({
    license_plan_id: types.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "license_plan_id": "licensePlanId",
    });
  }),
);

export function listPlansVariantDetailsRemoveLicenseFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansVariantDetailsRemoveLicense, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      ListPlansVariantDetailsRemoveLicense$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansVariantDetailsRemoveLicense' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantDetailsCustomize$inboundSchema: z.ZodMiniType<
  ListPlansVariantDetailsCustomize,
  unknown
> = z.pipe(
  z.object({
    price: z.optional(
      z.nullable(z.lazy(() => ListPlansVariantDetailsBasePrice$inboundSchema)),
    ),
    add_items: types.optional(
      z.array(z.lazy(() => ListPlansVariantDetailsPlanItem$inboundSchema)),
    ),
    remove_items: types.optional(
      z.array(
        z.lazy(() => ListPlansVariantDetailsPlanItemFilter$inboundSchema),
      ),
    ),
    free_trial: z.optional(
      z.nullable(z.lazy(() =>
        ListPlansVariantDetailsFreeTrialParams$inboundSchema
      )),
    ),
    billing_controls: types.optional(
      z.lazy(() => ListPlansVariantDetailsBillingControls$inboundSchema),
    ),
    upsert_licenses: types.optional(
      z.array(z.lazy(() => ListPlansVariantDetailsUpsertLicense$inboundSchema)),
    ),
    remove_licenses: types.optional(
      z.array(z.lazy(() => ListPlansVariantDetailsRemoveLicense$inboundSchema)),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "add_items": "addItems",
      "remove_items": "removeItems",
      "free_trial": "freeTrial",
      "billing_controls": "billingControls",
      "upsert_licenses": "upsertLicenses",
      "remove_licenses": "removeLicenses",
    });
  }),
);

export function listPlansVariantDetailsCustomizeFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansVariantDetailsCustomize, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansVariantDetailsCustomize$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansVariantDetailsCustomize' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantDetails$inboundSchema: z.ZodMiniType<
  ListPlansVariantDetails,
  unknown
> = z.pipe(
  z.object({
    base_plan_id: types.string(),
    customize: types.optional(
      z.lazy(() => ListPlansVariantDetailsCustomize$inboundSchema),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "base_plan_id": "basePlanId",
    });
  }),
);

export function listPlansVariantDetailsFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansVariantDetails, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansVariantDetails$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansVariantDetails' from JSON`,
  );
}

/** @internal */
export const ListPlansPriceLicenseInterval$inboundSchema: z.ZodMiniType<
  ListPlansPriceLicenseInterval,
  unknown
> = openEnums.inboundSchema(ListPlansPriceLicenseInterval);

/** @internal */
export const ListPlansLicenseAdditionalCurrency$inboundSchema: z.ZodMiniType<
  ListPlansLicenseAdditionalCurrency,
  unknown
> = z.object({
  currency: types.string(),
  amount: types.number(),
});

export function listPlansLicenseAdditionalCurrencyFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansLicenseAdditionalCurrency, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      ListPlansLicenseAdditionalCurrency$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansLicenseAdditionalCurrency' from JSON`,
  );
}

/** @internal */
export const ListPlansLicenseBasePrice$inboundSchema: z.ZodMiniType<
  ListPlansLicenseBasePrice,
  unknown
> = z.pipe(
  z.object({
    amount: types.number(),
    interval: ListPlansPriceLicenseInterval$inboundSchema,
    interval_count: types.optional(types.number()),
    additional_currencies: types.optional(
      z.array(z.lazy(() => ListPlansLicenseAdditionalCurrency$inboundSchema)),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "interval_count": "intervalCount",
      "additional_currencies": "additionalCurrencies",
    });
  }),
);

export function listPlansLicenseBasePriceFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansLicenseBasePrice, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansLicenseBasePrice$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansLicenseBasePrice' from JSON`,
  );
}

/** @internal */
export const ListPlansLicenseResetInterval$inboundSchema: z.ZodMiniType<
  ListPlansLicenseResetInterval,
  unknown
> = openEnums.inboundSchema(ListPlansLicenseResetInterval);

/** @internal */
export const ListPlansLicenseReset$inboundSchema: z.ZodMiniType<
  ListPlansLicenseReset,
  unknown
> = z.pipe(
  z.object({
    interval: ListPlansLicenseResetInterval$inboundSchema,
    interval_count: types.optional(types.number()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "interval_count": "intervalCount",
    });
  }),
);

export function listPlansLicenseResetFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansLicenseReset, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansLicenseReset$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansLicenseReset' from JSON`,
  );
}

/** @internal */
export const ListPlansLicenseAddItemAdditionalCurrency$inboundSchema:
  z.ZodMiniType<ListPlansLicenseAddItemAdditionalCurrency, unknown> = z.object({
    currency: types.optional(z.any()),
    amount: types.optional(z.any()),
  });

export function listPlansLicenseAddItemAdditionalCurrencyFromJSON(
  jsonString: string,
): SafeParseResult<
  ListPlansLicenseAddItemAdditionalCurrency,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      ListPlansLicenseAddItemAdditionalCurrency$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'ListPlansLicenseAddItemAdditionalCurrency' from JSON`,
  );
}

/** @internal */
export const ListPlansLicenseTier$inboundSchema: z.ZodMiniType<
  ListPlansLicenseTier,
  unknown
> = z.pipe(
  z.object({
    to: types.optional(z.any()),
    amount: types.optional(z.any()),
    flat_amount: types.optional(z.any()),
    additional_currencies: types.optional(z.any()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "flat_amount": "flatAmount",
      "additional_currencies": "additionalCurrencies",
    });
  }),
);

export function listPlansLicenseTierFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansLicenseTier, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansLicenseTier$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansLicenseTier' from JSON`,
  );
}

/** @internal */
export const ListPlansLicenseTierBehavior$inboundSchema: z.ZodMiniType<
  ListPlansLicenseTierBehavior,
  unknown
> = openEnums.inboundSchema(ListPlansLicenseTierBehavior);

/** @internal */
export const ListPlansLicenseAddItemPriceInterval$inboundSchema: z.ZodMiniType<
  ListPlansLicenseAddItemPriceInterval,
  unknown
> = openEnums.inboundSchema(ListPlansLicenseAddItemPriceInterval);

/** @internal */
export const ListPlansLicenseAddItemBillingMethod$inboundSchema: z.ZodMiniType<
  ListPlansLicenseAddItemBillingMethod,
  unknown
> = openEnums.inboundSchema(ListPlansLicenseAddItemBillingMethod);

/** @internal */
export const ListPlansLicensePrice$inboundSchema: z.ZodMiniType<
  ListPlansLicensePrice,
  unknown
> = z.pipe(
  z.object({
    amount: types.optional(types.number()),
    additional_currencies: types.optional(z.array(z.lazy(() =>
      ListPlansLicenseAddItemAdditionalCurrency$inboundSchema
    ))),
    tiers: types.optional(z.array(z.lazy(() =>
      ListPlansLicenseTier$inboundSchema
    ))),
    tier_behavior: types.optional(ListPlansLicenseTierBehavior$inboundSchema),
    interval: ListPlansLicenseAddItemPriceInterval$inboundSchema,
    interval_count: z._default(types.number(), 1),
    billing_units: z._default(types.number(), 1),
    billing_method: ListPlansLicenseAddItemBillingMethod$inboundSchema,
    max_purchase: z.optional(z.nullable(types.number())),
  }),
  z.transform((v) => {
    return remap$(v, {
      "additional_currencies": "additionalCurrencies",
      "tier_behavior": "tierBehavior",
      "interval_count": "intervalCount",
      "billing_units": "billingUnits",
      "billing_method": "billingMethod",
      "max_purchase": "maxPurchase",
    });
  }),
);

export function listPlansLicensePriceFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansLicensePrice, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansLicensePrice$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansLicensePrice' from JSON`,
  );
}

/** @internal */
export const ListPlansLicenseOnIncrease$inboundSchema: z.ZodMiniType<
  ListPlansLicenseOnIncrease,
  unknown
> = openEnums.inboundSchema(ListPlansLicenseOnIncrease);

/** @internal */
export const ListPlansLicenseOnDecrease$inboundSchema: z.ZodMiniType<
  ListPlansLicenseOnDecrease,
  unknown
> = openEnums.inboundSchema(ListPlansLicenseOnDecrease);

/** @internal */
export const ListPlansLicenseProration$inboundSchema: z.ZodMiniType<
  ListPlansLicenseProration,
  unknown
> = z.pipe(
  z.object({
    on_increase: ListPlansLicenseOnIncrease$inboundSchema,
    on_decrease: ListPlansLicenseOnDecrease$inboundSchema,
  }),
  z.transform((v) => {
    return remap$(v, {
      "on_increase": "onIncrease",
      "on_decrease": "onDecrease",
    });
  }),
);

export function listPlansLicenseProrationFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansLicenseProration, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansLicenseProration$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansLicenseProration' from JSON`,
  );
}

/** @internal */
export const ListPlansLicenseExpiryDurationType$inboundSchema: z.ZodMiniType<
  ListPlansLicenseExpiryDurationType,
  unknown
> = openEnums.inboundSchema(ListPlansLicenseExpiryDurationType);

/** @internal */
export const ListPlansLicenseRollover$inboundSchema: z.ZodMiniType<
  ListPlansLicenseRollover,
  unknown
> = z.pipe(
  z.object({
    max: types.optional(types.number()),
    max_percentage: types.optional(types.number()),
    expiry_duration_type: ListPlansLicenseExpiryDurationType$inboundSchema,
    expiry_duration_length: types.optional(types.number()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "max_percentage": "maxPercentage",
      "expiry_duration_type": "expiryDurationType",
      "expiry_duration_length": "expiryDurationLength",
    });
  }),
);

export function listPlansLicenseRolloverFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansLicenseRollover, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansLicenseRollover$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansLicenseRollover' from JSON`,
  );
}

/** @internal */
export const ListPlansLicenseFeatureOverride$inboundSchema: z.ZodMiniType<
  ListPlansLicenseFeatureOverride,
  unknown
> = z.pipe(
  z.object({
    credit_schema: types.optional(z.array(types.nullable(z.any()))),
  }),
  z.transform((v) => {
    return remap$(v, {
      "credit_schema": "creditSchema",
    });
  }),
);

export function listPlansLicenseFeatureOverrideFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansLicenseFeatureOverride, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansLicenseFeatureOverride$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansLicenseFeatureOverride' from JSON`,
  );
}

/** @internal */
export const ListPlansLicensePlanItem$inboundSchema: z.ZodMiniType<
  ListPlansLicensePlanItem,
  unknown
> = z.pipe(
  z.object({
    feature_id: types.string(),
    included: types.optional(types.number()),
    unlimited: types.optional(types.boolean()),
    pooled: z._default(types.boolean(), false),
    reset: types.optional(z.lazy(() => ListPlansLicenseReset$inboundSchema)),
    price: types.optional(z.lazy(() => ListPlansLicensePrice$inboundSchema)),
    proration: types.optional(
      z.lazy(() => ListPlansLicenseProration$inboundSchema),
    ),
    rollover: types.optional(
      z.lazy(() => ListPlansLicenseRollover$inboundSchema),
    ),
    feature_override: types.optional(
      z.lazy(() => ListPlansLicenseFeatureOverride$inboundSchema),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "feature_id": "featureId",
      "feature_override": "featureOverride",
    });
  }),
);

export function listPlansLicensePlanItemFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansLicensePlanItem, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansLicensePlanItem$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansLicensePlanItem' from JSON`,
  );
}

/** @internal */
export const ListPlansLicenseRemoveItemBillingMethod$inboundSchema:
  z.ZodMiniType<ListPlansLicenseRemoveItemBillingMethod, unknown> = openEnums
    .inboundSchema(ListPlansLicenseRemoveItemBillingMethod);

/** @internal */
export const ListPlansIntervalLicenseRemoveItemEnum2$inboundSchema:
  z.ZodMiniType<ListPlansIntervalLicenseRemoveItemEnum2, unknown> = openEnums
    .inboundSchema(ListPlansIntervalLicenseRemoveItemEnum2);

/** @internal */
export const ListPlansIntervalLicenseRemoveItemEnum1$inboundSchema:
  z.ZodMiniType<ListPlansIntervalLicenseRemoveItemEnum1, unknown> = openEnums
    .inboundSchema(ListPlansIntervalLicenseRemoveItemEnum1);

/** @internal */
export const ListPlansLicenseIntervalUnion$inboundSchema: z.ZodMiniType<
  ListPlansLicenseIntervalUnion,
  unknown
> = smartUnion([
  ListPlansIntervalLicenseRemoveItemEnum1$inboundSchema,
  ListPlansIntervalLicenseRemoveItemEnum2$inboundSchema,
]);

export function listPlansLicenseIntervalUnionFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansLicenseIntervalUnion, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansLicenseIntervalUnion$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansLicenseIntervalUnion' from JSON`,
  );
}

/** @internal */
export const ListPlansLicensePlanItemFilter$inboundSchema: z.ZodMiniType<
  ListPlansLicensePlanItemFilter,
  unknown
> = z.pipe(
  z.object({
    feature_id: types.optional(types.string()),
    billing_method: types.optional(
      ListPlansLicenseRemoveItemBillingMethod$inboundSchema,
    ),
    interval: types.optional(
      smartUnion([
        ListPlansIntervalLicenseRemoveItemEnum1$inboundSchema,
        ListPlansIntervalLicenseRemoveItemEnum2$inboundSchema,
      ]),
    ),
    interval_count: types.optional(types.number()),
    included: types.optional(types.number()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "feature_id": "featureId",
      "billing_method": "billingMethod",
      "interval_count": "intervalCount",
    });
  }),
);

export function listPlansLicensePlanItemFilterFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansLicensePlanItemFilter, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansLicensePlanItemFilter$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansLicensePlanItemFilter' from JSON`,
  );
}

/** @internal */
export const ListPlansLicenseCustomize$inboundSchema: z.ZodMiniType<
  ListPlansLicenseCustomize,
  unknown
> = z.pipe(
  z.object({
    price: z.optional(
      z.nullable(z.lazy(() => ListPlansLicenseBasePrice$inboundSchema)),
    ),
    add_items: types.optional(
      z.array(z.lazy(() => ListPlansLicensePlanItem$inboundSchema)),
    ),
    remove_items: types.optional(
      z.array(z.lazy(() => ListPlansLicensePlanItemFilter$inboundSchema)),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "add_items": "addItems",
      "remove_items": "removeItems",
    });
  }),
);

export function listPlansLicenseCustomizeFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansLicenseCustomize, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansLicenseCustomize$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansLicenseCustomize' from JSON`,
  );
}

/** @internal */
export const ListPlansLicense$inboundSchema: z.ZodMiniType<
  ListPlansLicense,
  unknown
> = z.pipe(
  z.object({
    license_plan_id: types.string(),
    version: types.number(),
    version_slug: types.optional(types.string()),
    included: types.number(),
    prepaid_only: types.boolean(),
    customize: types.optional(
      z.lazy(() => ListPlansLicenseCustomize$inboundSchema),
    ),
    plan: types.optional(Plan$inboundSchema),
  }),
  z.transform((v) => {
    return remap$(v, {
      "license_plan_id": "licensePlanId",
      "version_slug": "versionSlug",
      "prepaid_only": "prepaidOnly",
    });
  }),
);

export function listPlansLicenseFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansLicense, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansLicense$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansLicense' from JSON`,
  );
}

/** @internal */
export const ListPlansPriceVariantCustomizeInterval$inboundSchema:
  z.ZodMiniType<ListPlansPriceVariantCustomizeInterval, unknown> = openEnums
    .inboundSchema(ListPlansPriceVariantCustomizeInterval);

/** @internal */
export const ListPlansVariantCustomizeAdditionalCurrency$inboundSchema:
  z.ZodMiniType<ListPlansVariantCustomizeAdditionalCurrency, unknown> = z
    .object({
      currency: types.string(),
      amount: types.number(),
    });

export function listPlansVariantCustomizeAdditionalCurrencyFromJSON(
  jsonString: string,
): SafeParseResult<
  ListPlansVariantCustomizeAdditionalCurrency,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      ListPlansVariantCustomizeAdditionalCurrency$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'ListPlansVariantCustomizeAdditionalCurrency' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantBasePrice$inboundSchema: z.ZodMiniType<
  ListPlansVariantBasePrice,
  unknown
> = z.pipe(
  z.object({
    amount: types.number(),
    interval: ListPlansPriceVariantCustomizeInterval$inboundSchema,
    interval_count: types.optional(types.number()),
    additional_currencies: types.optional(z.array(z.lazy(() =>
      ListPlansVariantCustomizeAdditionalCurrency$inboundSchema
    ))),
  }),
  z.transform((v) => {
    return remap$(v, {
      "interval_count": "intervalCount",
      "additional_currencies": "additionalCurrencies",
    });
  }),
);

export function listPlansVariantBasePriceFromJSON(
  jsonString: string,
): SafeParseResult<ListPlansVariantBasePrice, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListPlansVariantBasePrice$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListPlansVariantBasePrice' from JSON`,
  );
}

/** @internal */
export const ListPlansVariantAddItemResetInterval$inboundSchema: z.ZodMiniType<
  ListPlansVariantAddItemResetInterval,
  unknown
> = openEnums.inboundSchema(ListPlansVariantAddItemResetInterval);
