/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { ClosedEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./sdk-validation-error.js";

export type LinkRevenueCatGlobals = {
  xApiVersion?: string | undefined;
};

export const LinkRevenueCatEnv = {
  Test: "test",
  Live: "live",
} as const;
export type LinkRevenueCatEnv = ClosedEnum<typeof LinkRevenueCatEnv>;

export type LinkRevenueCatParams = {
  organizationSlug: string;
  env: LinkRevenueCatEnv;
  projectName: string;
  redirectUrl: string;
};

/**
 * OK
 */
export type LinkRevenueCatResponse = {
  oauthUrl: string;
};

/** @internal */
export const LinkRevenueCatEnv$outboundSchema: z.ZodMiniEnum<
  typeof LinkRevenueCatEnv
> = z.enum(LinkRevenueCatEnv);

/** @internal */
export type LinkRevenueCatParams$Outbound = {
  organization_slug: string;
  env: string;
  project_name: string;
  redirect_url: string;
};

/** @internal */
export const LinkRevenueCatParams$outboundSchema: z.ZodMiniType<
  LinkRevenueCatParams$Outbound,
  LinkRevenueCatParams
> = z.pipe(
  z.object({
    organizationSlug: z.string(),
    env: LinkRevenueCatEnv$outboundSchema,
    projectName: z.string(),
    redirectUrl: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      organizationSlug: "organization_slug",
      projectName: "project_name",
      redirectUrl: "redirect_url",
    });
  }),
);

export function linkRevenueCatParamsToJSON(
  linkRevenueCatParams: LinkRevenueCatParams,
): string {
  return JSON.stringify(
    LinkRevenueCatParams$outboundSchema.parse(linkRevenueCatParams),
  );
}

/** @internal */
export const LinkRevenueCatResponse$inboundSchema: z.ZodMiniType<
  LinkRevenueCatResponse,
  unknown
> = z.pipe(
  z.object({
    oauth_url: types.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "oauth_url": "oauthUrl",
    });
  }),
);

export function linkRevenueCatResponseFromJSON(
  jsonString: string,
): SafeParseResult<LinkRevenueCatResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => LinkRevenueCatResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'LinkRevenueCatResponse' from JSON`,
  );
}
