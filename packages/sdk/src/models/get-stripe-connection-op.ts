/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { ClosedEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./sdk-validation-error.js";

export type GetStripeConnectionGlobals = {
  xApiVersion?: string | undefined;
};

/**
 * Stripe connection environment to inspect or disconnect.
 */
export const GetStripeConnectionEnv = {
  Test: "test",
  Live: "live",
} as const;
/**
 * Stripe connection environment to inspect or disconnect.
 */
export type GetStripeConnectionEnv = ClosedEnum<typeof GetStripeConnectionEnv>;

export type GetStripeConnectionParams = {
  /**
   * Public tenant organization slug, without the master organization suffix.
   */
  organizationSlug: string;
  /**
   * Stripe connection environment to inspect or disconnect.
   */
  env: GetStripeConnectionEnv;
};

/**
 * OK
 */
export type GetStripeConnectionResponse = {
  /**
   * Whether an OAuth connection is stored. Excludes platform-managed accounts, default sandbox accounts, and separate secret-key connections.
   */
  connected: boolean;
  /**
   * OAuth-connected Stripe account ID, or null when absent.
   */
  accountId: string | null;
  /**
   * Connection timestamp in Unix milliseconds, or null for historical connections without a timestamp and when disconnected.
   */
  connectedAt: number | null;
};

/** @internal */
export const GetStripeConnectionEnv$outboundSchema: z.ZodMiniEnum<
  typeof GetStripeConnectionEnv
> = z.enum(GetStripeConnectionEnv);

/** @internal */
export type GetStripeConnectionParams$Outbound = {
  organization_slug: string;
  env: string;
};

/** @internal */
export const GetStripeConnectionParams$outboundSchema: z.ZodMiniType<
  GetStripeConnectionParams$Outbound,
  GetStripeConnectionParams
> = z.pipe(
  z.object({
    organizationSlug: z.string(),
    env: GetStripeConnectionEnv$outboundSchema,
  }),
  z.transform((v) => {
    return remap$(v, {
      organizationSlug: "organization_slug",
    });
  }),
);

export function getStripeConnectionParamsToJSON(
  getStripeConnectionParams: GetStripeConnectionParams,
): string {
  return JSON.stringify(
    GetStripeConnectionParams$outboundSchema.parse(getStripeConnectionParams),
  );
}

/** @internal */
export const GetStripeConnectionResponse$inboundSchema: z.ZodMiniType<
  GetStripeConnectionResponse,
  unknown
> = z.pipe(
  z.object({
    connected: types.boolean(),
    account_id: types.nullable(types.string()),
    connected_at: types.nullable(types.number()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "account_id": "accountId",
      "connected_at": "connectedAt",
    });
  }),
);

export function getStripeConnectionResponseFromJSON(
  jsonString: string,
): SafeParseResult<GetStripeConnectionResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetStripeConnectionResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetStripeConnectionResponse' from JSON`,
  );
}
