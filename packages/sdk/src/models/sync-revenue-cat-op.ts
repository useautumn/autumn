/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import * as openEnums from "../types/enums.js";
import { ClosedEnum, OpenEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./sdk-validation-error.js";

export type SyncRevenueCatGlobals = {
  xApiVersion?: string | undefined;
};

/**
 * "test" and "sandbox" both target the sandbox environment
 */
export const SyncRevenueCatEnv = {
  Test: "test",
  Sandbox: "sandbox",
  Live: "live",
} as const;
/**
 * "test" and "sandbox" both target the sandbox environment
 */
export type SyncRevenueCatEnv = ClosedEnum<typeof SyncRevenueCatEnv>;

export type SyncRevenueCatParams = {
  organizationSlug: string;
  /**
   * "test" and "sandbox" both target the sandbox environment
   */
  env: SyncRevenueCatEnv;
  /**
   * Plans to push. Omit to sync every plan in the org/env.
   */
  productIds?: Array<string> | undefined;
};

export const SyncRevenueCatStatus = {
  Synced: "synced",
  Skipped: "skipped",
  Error: "error",
} as const;
export type SyncRevenueCatStatus = OpenEnum<typeof SyncRevenueCatStatus>;

export const SyncRevenueCatProduct = {
  Created: "created",
  Updated: "updated",
  Exists: "exists",
} as const;
export type SyncRevenueCatProduct = OpenEnum<typeof SyncRevenueCatProduct>;

export const StorePush = {
  Pushed: "pushed",
  Failed: "failed",
  Skipped: "skipped",
} as const;
export type StorePush = OpenEnum<typeof StorePush>;

export const SyncRevenueCatPrice = {
  Set: "set",
  Skipped: "skipped",
  Failed: "failed",
} as const;
export type SyncRevenueCatPrice = OpenEnum<typeof SyncRevenueCatPrice>;

export type SyncRevenueCatApp = {
  appId: string;
  appType: string;
  product: SyncRevenueCatProduct;
  storePush?: StorePush | undefined;
  price?: SyncRevenueCatPrice | undefined;
  message?: string | undefined;
};

export type Result = {
  planId: string;
  status: SyncRevenueCatStatus;
  storeIdentifier?: string | undefined;
  apps?: Array<SyncRevenueCatApp> | undefined;
  message?: string | undefined;
};

/**
 * OK
 */
export type SyncRevenueCatResponse = {
  results: Array<Result>;
};

/** @internal */
export const SyncRevenueCatEnv$outboundSchema: z.ZodMiniEnum<
  typeof SyncRevenueCatEnv
> = z.enum(SyncRevenueCatEnv);

/** @internal */
export type SyncRevenueCatParams$Outbound = {
  organization_slug: string;
  env: string;
  product_ids?: Array<string> | undefined;
};

/** @internal */
export const SyncRevenueCatParams$outboundSchema: z.ZodMiniType<
  SyncRevenueCatParams$Outbound,
  SyncRevenueCatParams
> = z.pipe(
  z.object({
    organizationSlug: z.string(),
    env: SyncRevenueCatEnv$outboundSchema,
    productIds: z.optional(z.array(z.string())),
  }),
  z.transform((v) => {
    return remap$(v, {
      organizationSlug: "organization_slug",
      productIds: "product_ids",
    });
  }),
);

export function syncRevenueCatParamsToJSON(
  syncRevenueCatParams: SyncRevenueCatParams,
): string {
  return JSON.stringify(
    SyncRevenueCatParams$outboundSchema.parse(syncRevenueCatParams),
  );
}

/** @internal */
export const SyncRevenueCatStatus$inboundSchema: z.ZodMiniType<
  SyncRevenueCatStatus,
  unknown
> = openEnums.inboundSchema(SyncRevenueCatStatus);

/** @internal */
export const SyncRevenueCatProduct$inboundSchema: z.ZodMiniType<
  SyncRevenueCatProduct,
  unknown
> = openEnums.inboundSchema(SyncRevenueCatProduct);

/** @internal */
export const StorePush$inboundSchema: z.ZodMiniType<StorePush, unknown> =
  openEnums.inboundSchema(StorePush);

/** @internal */
export const SyncRevenueCatPrice$inboundSchema: z.ZodMiniType<
  SyncRevenueCatPrice,
  unknown
> = openEnums.inboundSchema(SyncRevenueCatPrice);

/** @internal */
export const SyncRevenueCatApp$inboundSchema: z.ZodMiniType<
  SyncRevenueCatApp,
  unknown
> = z.pipe(
  z.object({
    app_id: types.string(),
    app_type: types.string(),
    product: SyncRevenueCatProduct$inboundSchema,
    store_push: types.optional(StorePush$inboundSchema),
    price: types.optional(SyncRevenueCatPrice$inboundSchema),
    message: types.optional(types.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "app_id": "appId",
      "app_type": "appType",
      "store_push": "storePush",
    });
  }),
);

export function syncRevenueCatAppFromJSON(
  jsonString: string,
): SafeParseResult<SyncRevenueCatApp, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SyncRevenueCatApp$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SyncRevenueCatApp' from JSON`,
  );
}

/** @internal */
export const Result$inboundSchema: z.ZodMiniType<Result, unknown> = z.pipe(
  z.object({
    plan_id: types.string(),
    status: SyncRevenueCatStatus$inboundSchema,
    store_identifier: types.optional(types.string()),
    apps: types.optional(
      z.array(z.lazy(() => SyncRevenueCatApp$inboundSchema)),
    ),
    message: types.optional(types.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "plan_id": "planId",
      "store_identifier": "storeIdentifier",
    });
  }),
);

export function resultFromJSON(
  jsonString: string,
): SafeParseResult<Result, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Result$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Result' from JSON`,
  );
}

/** @internal */
export const SyncRevenueCatResponse$inboundSchema: z.ZodMiniType<
  SyncRevenueCatResponse,
  unknown
> = z.object({
  results: z.array(z.lazy(() => Result$inboundSchema)),
});

export function syncRevenueCatResponseFromJSON(
  jsonString: string,
): SafeParseResult<SyncRevenueCatResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SyncRevenueCatResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SyncRevenueCatResponse' from JSON`,
  );
}
