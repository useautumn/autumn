/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import * as openEnums from "../types/enums.js";
import { ClosedEnum, OpenEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./sdk-validation-error.js";

export type CreateSandboxGlobals = {
  xApiVersion?: string | undefined;
};

/**
 * Colour the dashboard uses to label the sandbox. Defaults to `gray`.
 */
export const ColorRequest = {
  Gray: "gray",
  Blue: "blue",
  Green: "green",
  Amber: "amber",
  Red: "red",
  Pink: "pink",
} as const;
/**
 * Colour the dashboard uses to label the sandbox. Defaults to `gray`.
 */
export type ColorRequest = ClosedEnum<typeof ColorRequest>;

export type CreateSandboxParams = {
  /**
   * A name for the sandbox, unique within your organization.
   */
  name: string;
  /**
   * Colour the dashboard uses to label the sandbox. Defaults to `gray`.
   */
  color?: ColorRequest | undefined;
  /**
   * Icon the dashboard uses to label the sandbox. Defaults to `Flask`.
   */
  icon?: string | undefined;
};

/**
 * Colour the dashboard uses to label the sandbox.
 */
export const CreateSandboxColorResponse = {
  Gray: "gray",
  Blue: "blue",
  Green: "green",
  Amber: "amber",
  Red: "red",
  Pink: "pink",
} as const;
/**
 * Colour the dashboard uses to label the sandbox.
 */
export type CreateSandboxColorResponse = OpenEnum<
  typeof CreateSandboxColorResponse
>;

/**
 * OK
 */
export type CreateSandboxResponse = {
  /**
   * The sandbox's organization ID.
   */
  id: string;
  /**
   * The sandbox's name.
   */
  name: string;
  /**
   * The sandbox's slug, derived from its name.
   */
  slug: string;
  /**
   * When the sandbox was created, ms since epoch.
   */
  createdAt: number;
  /**
   * Colour the dashboard uses to label the sandbox.
   */
  color: CreateSandboxColorResponse;
  /**
   * Icon the dashboard uses to label the sandbox.
   */
  icon: string;
  /**
   * The sandbox's own secret key. Shown once, here — store it before you discard the response.
   */
  secretKey: string;
};

/** @internal */
export const ColorRequest$outboundSchema: z.ZodMiniEnum<typeof ColorRequest> = z
  .enum(ColorRequest);

/** @internal */
export type CreateSandboxParams$Outbound = {
  name: string;
  color?: string | undefined;
  icon?: string | undefined;
};

/** @internal */
export const CreateSandboxParams$outboundSchema: z.ZodMiniType<
  CreateSandboxParams$Outbound,
  CreateSandboxParams
> = z.object({
  name: z.string(),
  color: z.optional(ColorRequest$outboundSchema),
  icon: z.optional(z.string()),
});

export function createSandboxParamsToJSON(
  createSandboxParams: CreateSandboxParams,
): string {
  return JSON.stringify(
    CreateSandboxParams$outboundSchema.parse(createSandboxParams),
  );
}

/** @internal */
export const CreateSandboxColorResponse$inboundSchema: z.ZodMiniType<
  CreateSandboxColorResponse,
  unknown
> = openEnums.inboundSchema(CreateSandboxColorResponse);

/** @internal */
export const CreateSandboxResponse$inboundSchema: z.ZodMiniType<
  CreateSandboxResponse,
  unknown
> = z.pipe(
  z.object({
    id: types.string(),
    name: types.string(),
    slug: types.string(),
    created_at: types.number(),
    color: CreateSandboxColorResponse$inboundSchema,
    icon: types.string(),
    secret_key: types.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "created_at": "createdAt",
      "secret_key": "secretKey",
    });
  }),
);

export function createSandboxResponseFromJSON(
  jsonString: string,
): SafeParseResult<CreateSandboxResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CreateSandboxResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CreateSandboxResponse' from JSON`,
  );
}
