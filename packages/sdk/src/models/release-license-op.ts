/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./sdk-validation-error.js";

export type ReleaseLicenseGlobals = {
  xApiVersion?: string | undefined;
};

export type ReleaseLicenseParams = {
  customerId: string;
  /**
   * Scopes the release when an entity holds licenses of multiple plans.
   */
  licensePlanId?: string | undefined;
  entityIds: Array<string>;
};

/**
 * OK
 */
export type ReleaseLicenseResponse = {
  success: boolean;
};

/** @internal */
export type ReleaseLicenseParams$Outbound = {
  customer_id: string;
  license_plan_id?: string | undefined;
  entity_ids: Array<string>;
};

/** @internal */
export const ReleaseLicenseParams$outboundSchema: z.ZodMiniType<
  ReleaseLicenseParams$Outbound,
  ReleaseLicenseParams
> = z.pipe(
  z.object({
    customerId: z.string(),
    licensePlanId: z.optional(z.string()),
    entityIds: z.array(z.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      customerId: "customer_id",
      licensePlanId: "license_plan_id",
      entityIds: "entity_ids",
    });
  }),
);

export function releaseLicenseParamsToJSON(
  releaseLicenseParams: ReleaseLicenseParams,
): string {
  return JSON.stringify(
    ReleaseLicenseParams$outboundSchema.parse(releaseLicenseParams),
  );
}

/** @internal */
export const ReleaseLicenseResponse$inboundSchema: z.ZodMiniType<
  ReleaseLicenseResponse,
  unknown
> = z.object({
  success: types.boolean(),
});

export function releaseLicenseResponseFromJSON(
  jsonString: string,
): SafeParseResult<ReleaseLicenseResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ReleaseLicenseResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ReleaseLicenseResponse' from JSON`,
  );
}
