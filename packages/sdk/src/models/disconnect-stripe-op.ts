/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { ClosedEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./sdk-validation-error.js";

export type DisconnectStripeGlobals = {
  xApiVersion?: string | undefined;
};

/**
 * Stripe connection environment to inspect or disconnect.
 */
export const DisconnectStripeEnv = {
  Test: "test",
  Live: "live",
} as const;
/**
 * Stripe connection environment to inspect or disconnect.
 */
export type DisconnectStripeEnv = ClosedEnum<typeof DisconnectStripeEnv>;

export type DisconnectStripeParams = {
  /**
   * Public tenant organization slug, without the master organization suffix.
   */
  organizationSlug: string;
  /**
   * Stripe connection environment to inspect or disconnect.
   */
  env: DisconnectStripeEnv;
};

/**
 * OK
 */
export type DisconnectStripeResponse = {
  success: true;
};

/** @internal */
export const DisconnectStripeEnv$outboundSchema: z.ZodMiniEnum<
  typeof DisconnectStripeEnv
> = z.enum(DisconnectStripeEnv);

/** @internal */
export type DisconnectStripeParams$Outbound = {
  organization_slug: string;
  env: string;
};

/** @internal */
export const DisconnectStripeParams$outboundSchema: z.ZodMiniType<
  DisconnectStripeParams$Outbound,
  DisconnectStripeParams
> = z.pipe(
  z.object({
    organizationSlug: z.string(),
    env: DisconnectStripeEnv$outboundSchema,
  }),
  z.transform((v) => {
    return remap$(v, {
      organizationSlug: "organization_slug",
    });
  }),
);

export function disconnectStripeParamsToJSON(
  disconnectStripeParams: DisconnectStripeParams,
): string {
  return JSON.stringify(
    DisconnectStripeParams$outboundSchema.parse(disconnectStripeParams),
  );
}

/** @internal */
export const DisconnectStripeResponse$inboundSchema: z.ZodMiniType<
  DisconnectStripeResponse,
  unknown
> = z.object({
  success: types.literal(true),
});

export function disconnectStripeResponseFromJSON(
  jsonString: string,
): SafeParseResult<DisconnectStripeResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DisconnectStripeResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DisconnectStripeResponse' from JSON`,
  );
}
