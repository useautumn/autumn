/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./sdk-validation-error.js";

export type MintKeyGlobals = {
  xApiVersion?: string | undefined;
};

export type MintKeyParams = {
  /**
   * The customer to mint a token for.
   */
  customerId: string;
  /**
   * If true, mint a non-expiring access token (no refresh token). Revoke via keys.revoke.
   */
  indefinite?: boolean | undefined;
};

/**
 * OK
 */
export type MintKeyResponse = {
  /**
   * Access token (1h, or non-expiring if indefinite), prefixed `am_jwt_`.
   */
  accessToken: string;
  /**
   * Rotating refresh token (24h). Omitted for indefinite tokens.
   */
  refreshToken?: string | undefined;
  /**
   * Access-token expiry, ms since epoch. null for indefinite tokens.
   */
  expiresAt: number | null;
  /**
   * Refresh-token expiry, ms since epoch. Omitted for indefinite tokens.
   */
  refreshExpiresAt?: number | undefined;
};

/** @internal */
export type MintKeyParams$Outbound = {
  customer_id: string;
  indefinite?: boolean | undefined;
};

/** @internal */
export const MintKeyParams$outboundSchema: z.ZodMiniType<
  MintKeyParams$Outbound,
  MintKeyParams
> = z.pipe(
  z.object({
    customerId: z.string(),
    indefinite: z.optional(z.boolean()),
  }),
  z.transform((v) => {
    return remap$(v, {
      customerId: "customer_id",
    });
  }),
);

export function mintKeyParamsToJSON(mintKeyParams: MintKeyParams): string {
  return JSON.stringify(MintKeyParams$outboundSchema.parse(mintKeyParams));
}

/** @internal */
export const MintKeyResponse$inboundSchema: z.ZodMiniType<
  MintKeyResponse,
  unknown
> = z.pipe(
  z.object({
    access_token: types.string(),
    refresh_token: types.optional(types.string()),
    expires_at: types.nullable(types.number()),
    refresh_expires_at: types.optional(types.number()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "access_token": "accessToken",
      "refresh_token": "refreshToken",
      "expires_at": "expiresAt",
      "refresh_expires_at": "refreshExpiresAt",
    });
  }),
);

export function mintKeyResponseFromJSON(
  jsonString: string,
): SafeParseResult<MintKeyResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => MintKeyResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MintKeyResponse' from JSON`,
  );
}
