/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { ClosedEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./sdk-validation-error.js";

export type FinalizeLockGlobals = {
  xApiVersion?: string | undefined;
};

/**
 * Use 'confirm' to commit the deduction, or 'release' to return the held balance.
 */
export const Action = {
  Confirm: "confirm",
  Release: "release",
} as const;
/**
 * Use 'confirm' to commit the deduction, or 'release' to return the held balance.
 */
export type Action = ClosedEnum<typeof Action>;

export type FinalizeBalanceParams = {
  /**
   * The lock ID that was passed into the previous check call.
   */
  lockId: string;
  /**
   * Use 'confirm' to commit the deduction, or 'release' to return the held balance.
   */
  action: Action;
  /**
   * Additional properties to attach to this finalize lock event.
   */
  overrideValue?: number | undefined;
  /**
   * Additional properties to attach to this finalize lock event.
   */
  properties?: { [k: string]: any } | undefined;
};

/**
 * OK
 */
export type FinalizeLockResponse = {
  success: boolean;
};

/** @internal */
export const Action$outboundSchema: z.ZodMiniEnum<typeof Action> = z.enum(
  Action,
);

/** @internal */
export type FinalizeBalanceParams$Outbound = {
  lock_id: string;
  action: string;
  override_value?: number | undefined;
  properties?: { [k: string]: any } | undefined;
};

/** @internal */
export const FinalizeBalanceParams$outboundSchema: z.ZodMiniType<
  FinalizeBalanceParams$Outbound,
  FinalizeBalanceParams
> = z.pipe(
  z.object({
    lockId: z.string(),
    action: Action$outboundSchema,
    overrideValue: z.optional(z.number()),
    properties: z.optional(z.record(z.string(), z.any())),
  }),
  z.transform((v) => {
    return remap$(v, {
      lockId: "lock_id",
      overrideValue: "override_value",
    });
  }),
);

export function finalizeBalanceParamsToJSON(
  finalizeBalanceParams: FinalizeBalanceParams,
): string {
  return JSON.stringify(
    FinalizeBalanceParams$outboundSchema.parse(finalizeBalanceParams),
  );
}

/** @internal */
export const FinalizeLockResponse$inboundSchema: z.ZodMiniType<
  FinalizeLockResponse,
  unknown
> = z.object({
  success: types.boolean(),
});

export function finalizeLockResponseFromJSON(
  jsonString: string,
): SafeParseResult<FinalizeLockResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FinalizeLockResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FinalizeLockResponse' from JSON`,
  );
}
