/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { ClosedEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./sdk-validation-error.js";

export type CreateBalanceGlobals = {
  xApiVersion?: string | undefined;
};

/**
 * The interval at which the balance resets (e.g., 'month', 'day', 'year').
 */
export const CreateBalanceInterval = {
  OneOff: "one_off",
  Minute: "minute",
  Hour: "hour",
  Day: "day",
  Week: "week",
  Month: "month",
  Quarter: "quarter",
  SemiAnnual: "semi_annual",
  Year: "year",
} as const;
/**
 * The interval at which the balance resets (e.g., 'month', 'day', 'year').
 */
export type CreateBalanceInterval = ClosedEnum<typeof CreateBalanceInterval>;

/**
 * Reset configuration for the balance. If not provided, the balance is a one-time grant that never resets.
 */
export type CreateBalanceReset = {
  /**
   * The interval at which the balance resets (e.g., 'month', 'day', 'year').
   */
  interval: CreateBalanceInterval;
  /**
   * Number of intervals between resets. Defaults to 1 (e.g., interval_count: 2 with interval: 'month' resets every 2 months).
   */
  intervalCount?: number | undefined;
};

export type CreateBalanceParams = {
  /**
   * The ID of the customer.
   */
  customerId: string;
  /**
   * The ID of the feature.
   */
  featureId: string;
  /**
   * The ID of the entity for entity-scoped balances (e.g., per-seat limits).
   */
  entityId?: string | undefined;
  /**
   * The initial balance amount to grant. For metered features, this is the number of units the customer can use.
   */
  included?: number | undefined;
  /**
   * If true, the balance has unlimited usage. Cannot be combined with 'included'.
   */
  unlimited?: boolean | undefined;
  /**
   * Reset configuration for the balance. If not provided, the balance is a one-time grant that never resets.
   */
  reset?: CreateBalanceReset | undefined;
  /**
   * Unix timestamp (milliseconds) when the balance expires. Mutually exclusive with reset.
   */
  expiresAt?: number | undefined;
  grantedBalance?: number | undefined;
};

/**
 * OK
 */
export type CreateBalanceResponse = {
  success: boolean;
};

/** @internal */
export const CreateBalanceInterval$outboundSchema: z.ZodMiniEnum<
  typeof CreateBalanceInterval
> = z.enum(CreateBalanceInterval);

/** @internal */
export type CreateBalanceReset$Outbound = {
  interval: string;
  interval_count?: number | undefined;
};

/** @internal */
export const CreateBalanceReset$outboundSchema: z.ZodMiniType<
  CreateBalanceReset$Outbound,
  CreateBalanceReset
> = z.pipe(
  z.object({
    interval: CreateBalanceInterval$outboundSchema,
    intervalCount: z.optional(z.number()),
  }),
  z.transform((v) => {
    return remap$(v, {
      intervalCount: "interval_count",
    });
  }),
);

export function createBalanceResetToJSON(
  createBalanceReset: CreateBalanceReset,
): string {
  return JSON.stringify(
    CreateBalanceReset$outboundSchema.parse(createBalanceReset),
  );
}

/** @internal */
export type CreateBalanceParams$Outbound = {
  customer_id: string;
  feature_id: string;
  entity_id?: string | undefined;
  included?: number | undefined;
  unlimited?: boolean | undefined;
  reset?: CreateBalanceReset$Outbound | undefined;
  expires_at?: number | undefined;
  granted_balance?: number | undefined;
};

/** @internal */
export const CreateBalanceParams$outboundSchema: z.ZodMiniType<
  CreateBalanceParams$Outbound,
  CreateBalanceParams
> = z.pipe(
  z.object({
    customerId: z.string(),
    featureId: z.string(),
    entityId: z.optional(z.string()),
    included: z.optional(z.number()),
    unlimited: z.optional(z.boolean()),
    reset: z.optional(z.lazy(() => CreateBalanceReset$outboundSchema)),
    expiresAt: z.optional(z.number()),
    grantedBalance: z.optional(z.number()),
  }),
  z.transform((v) => {
    return remap$(v, {
      customerId: "customer_id",
      featureId: "feature_id",
      entityId: "entity_id",
      expiresAt: "expires_at",
      grantedBalance: "granted_balance",
    });
  }),
);

export function createBalanceParamsToJSON(
  createBalanceParams: CreateBalanceParams,
): string {
  return JSON.stringify(
    CreateBalanceParams$outboundSchema.parse(createBalanceParams),
  );
}

/** @internal */
export const CreateBalanceResponse$inboundSchema: z.ZodMiniType<
  CreateBalanceResponse,
  unknown
> = z.object({
  success: types.boolean(),
});

export function createBalanceResponseFromJSON(
  jsonString: string,
): SafeParseResult<CreateBalanceResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CreateBalanceResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CreateBalanceResponse' from JSON`,
  );
}
